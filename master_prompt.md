# MASTER PROMPT — Starcluster

> Единый вводный документ для агента в новом чате. Прочитай **целиком** перед любой работой.
> Он самодостаточен: миссия, жёсткие ограничения, карта кода, реализованный локальный
> режим полёта, методология проверок, дорожная карта. Документ **живой** — расширяй его
> по мере работы (в конце есть раздел «Как расширять этот документ»).

---

## 0. TL;DR для быстрого старта

- Проект: **Starcluster** — космо-экономическая игра на **C++11 + SDL2** (только SDL2, ничего больше).
- Путь: `/Users/jirnyak/Mirror/starcluster`. Сборка: `make` → бинарь `./game`.
- Пользователь пишет **по-русски**, дал **полную автономию** «сделать игру интересной». Отвечай по-русски.
- Рабочий цикл: **срез → фидбек → дальше**. Делаешь играбельный кусок → пользователь играет → правишь по фидбеку.
- Максимально **используй субагентов** для параллельной работы (пользователь явно просил).
- **Жёсткие архитектурные правила** — раздел 2. Нарушение = переделка. Читай до кода.
- Свежая крупная фича: **локальный режим полёта** (раздел 5). Срез 1 + два прохода углубления
  (juice/бой/контент — §5.10) собраны и провалидированы (smoke + ASan/UBSan + soak-тест 120k кадров). Ждёт плейтеста.
- Поверх хоткеев добавлена **единая кликабельная панель действий** (§5.11) — контекстные кнопки
  active/inactive; чинит «не зайти в звезду».
- ✅ **СДЕЛАНО (2026-07-28):** локальный режим — **полный 3D по трём осям (Elite, +roll,
  тело-относительный базис без gimbal lock), перспективная камера от корабля** (переключаемый
  орто-вид-карта по `C`), **весь кластер виден фоном как LOD-скайбокс** из реальных `ClusterStar`.
  §2.7 обновлён: перспектива только в локале. Все проверки + soak 120k зелёные. Детали — **§5.13**.
- 🎮 **Плейтест №1 пройден (§7.1.1):** 3D одобрен, бой «сочный» (не ослаблять!).
- 💎 **СДЕЛАНО (2026-08-12): ХАЙТЕК-ЭТАЖ — разделы 31-36. Крупная сессия связывания.**
  Таблица элементов кончается там же, где кончается химия (8 МэВ/нуклон), и поздняя игра
  теперь живёт ЗА этим пределом: **антивещество, нейтрониум, конденсат** (§31), у каждого
  свой редкий тип звезды (рынок у 12% систем, источники — 275/37/28 из 8192). Антивещество
  едет в бункере и входит в топливную смесь (1863 МэВ/нуклон), нейтрониум наваривается на
  корпус слоями, конденсат — тело хромокора, и **кузница даёт ядро ВЫБРАННОГО стата** вместо
  рулетки. Рейс полным отсеком = 2.04x капитала. Рядом — **акции держав** (§33): цена от
  капитализации дохода, книга публикуется с задержкой, дивиденд идёт на счёт со скоростью
  света. **Флот получил автопилот** (§35), **колония наконец меняет свой рынок** (§36),
  **торговля и разведка дают исследования** (§34), панель целей стала лестницей из 14 ступеней.
  ⚠️ Сейв поднят **13 -> 17**. ⚠️ Читай §31.4 (переоснастка живёт НА КОРПУСЕ и входит в
  полезную нагрузку планировщика) и §35 (почему автопилот — копия, а не вызов `updateTrader`).
- 🔒 **⚠️ ГЛАВНОЕ (2026-08-12): игра ЗАПИРАЛАСЬ на третьем рейсе — раздел 42.** Каждая
  механика была здорова по отдельности (75 проверок `make balance` зелёные), а вместе они
  давали 2..4 сделки и вечные 10 000 Cr. Кнопка «залить» заливала бак на сотню рейсов вперёд
  (26 000…112 000 Cr при рейсе в 3 000), «купить максимум» не оставляло на топливо, трюм не
  был потолком покупки, а советчик при сухом баке молчал, не назвав причины, — и он же не
  вычитал дорогу из обещанной прибыли. После правок: 13 000 -> 509 000 Cr за 418 лет.
  **Читай §42.1: ни одна из четырёх ям не была багом сама по себе.** Новая проверка
  `testAdvisorRunsCompound` играет в игру по-человечески и ловит возврат любой из них.
- 🚑 **СДЕЛАНО (2026-08-12): закрыты тупики и починен первый час — разделы 38 и 39.**
  **Буксир** (§38) закрывает единственное место, где партия запиралась насмерть: сухой бак
  плюс `X STOP` = вечный дрейф, а на `enRoute` завязано всё. **ESC перестал убивать партию**
  без спроса и без сохранения, **`R ROB` спрашивает подтверждение** (на стартовом корпусе это
  ~70% потерять корабль), **отозванную лицензию можно ОТРАБОТАТЬ** продажей — раньше замерзала
  и продажа накопанного, вопреки собственному комментарию кода. Плюс: панель целей больше не
  налезает на панель фракций, клавиша `B` уважает поле объёма, в карточке F1 появилось
  управление внутри системы, коды прокачки заменены именами. ⚠️ `UI::closeWindow` была
  объявлена в заголовке с самого начала и ни разу не написана.
- 🐛 **СДЕЛАНО (2026-08-12): три сквозных аудита субагентами — разделы 32 и 34.**
  Главное: **загрузка сейва убивала экономику всего скопления** — модель нужды не
  восстанавливалась, и `Market::update` заполнял `needs` нулями (замер: спрос 81 млн -> 0,
  оборот системы в 16 раз вниз, тысячелетняя сверка замирала). **Хромокоры исчезали при
  покупке корпуса** — три ветки прокачки из семи (§32.2). **Микромир был печатным станком**:
  `L`,`L` возвращали те же радиоисточники, тех же пиратов и полный щит (§32.1). **Варп шёл с
  вечной красной тряской** — зона опасности была больше всей системы (§32.3). Плюс: покупка
  «на максимум» уводила кошелёк в минус на размер пошлины, модуль в трюме прерывал «продать
  всё», `V SELL` из карточки F1 был недостижим (продажа переехала на `Q`).
- 🎓 **СДЕЛАНО (2026-08-07): вступительная новелла Тимертии выросла до 28 реплик — раздел 26.**
  Она молчала о том, чем игра НАКАЗЫВАЕТ: о квоте лицензии (§21) и о том, что топливо и рабочее
  тело — два разных вещества (§12). Теперь новелла сама открывает окно `ТРЮМ / БАКИ` и
  доску `ЗАКАЗЫ` — так же, как раньше открывала систему и рынок, — и договаривает про
  локальный полёт `L`, покупку системы `C` и карточку `F1`. Ждёт фидбека пользователя.
- 💥 **СДЕЛАНО (2026-08-06): раскол астероида + разыгранный размер скопления — раздел 22.**
  Выработанная глыба больше не висит в поясе пустой декорацией: она РАСКАЛЫВАЕТСЯ тем же
  попиксельным `renderRockLit` — 10 клиньев Вороного разъезжаются вдоль своих направлений,
  по рёбрам идёт раскалённый рез, куски гаснут вразнобой (§22.2; при `brk=0` объединение
  клиньев ПОБИТОВО равно целому камню — ни скачка, ни «поп-ина»). Скопление — теперь ОДНО
  число **`STAR_COUNT = 8192`** (было 10 000; розыгрыш размера и «опорное скопление»
  пользователем отменены, §22.3). Все три числа мира — степени двойки: 8192 системы, 1024
  борта, 256 контрактов; трафик НА СИСТЕМУ от этого гуще на 22% (кораблей в абсолюте
  столько же, 1011), доска не изменилась. Соак-бейзлайн сдвинут — новые числа в §22.3.
  ⚠️ **Читай §22.4: память и
  время генерации КВАДРАТИЧНЫ по числу звёзд** (`routeNextHop` — таблица всех пар): 8 192
  стоят 509 МБ и 5.3 с против 656 МБ и 7.4 с у 10 000, а потолок 16 384 стоил бы 1.2 ГБ и
  20 с. Хочешь мир больше — сперва корень, и пересчитай `*_TARGET_FULL`.
- 🧮 **СДЕЛАНО (2026-08-06): шкала лицензии считается в уме — раздел 21.** Тариф ровно **10%**,
  квота первого периода ровно **10 000 Cr**, `LICENCE_PRICE_K` 25 → 100 (вторая лицензия —
  по-прежнему миллион). ⚠️ Прежний инвариант «квота = ~120 рейсов» **сознательно разменян**
  пользователем на читаемость: теперь ~24 рейса. Правишь квоту — читай §21.1, там сказано,
  что именно потеряно. Заодно вычищены из интерфейса подсказки про добычу по `M` в обзоре
  скопления и кнопка `AUTO` в торговле (§21.2).
- ⚖️ **СДЕЛАНО (2026-08-06): тысячелетняя сверка стала настоящей — раздел 19.** Одно число
  делало две работы с противоположной нормировкой. Теперь привод цен мерит от ЯКОРЯ (честный
  замер вместо накопителя, который показывал 0.409 при правде 0.348 и не сходился никогда), а
  ставка тарифа — от уровня ПРИ ОСНОВАНИИ партии, ступенькой на сверке. `LICENCE_TARIFF_BASE`
  впервые действует: ставка стартует ровно с 0.08 и дрейфует к 0.108, а не лежит на полу 0.05.
  Сверка перестала быть таймером. ⚠️ Там же найден баг в МОЕЙ проверке из §17: она делила на
  кошелёк, не выдавая его игроку, — все числа про «куш» были шумом (§19.4).
- 🏛️ **СДЕЛАНО (2026-08-06): доход колонии и клиринговая палата — раздел 18.** Доход владельца
  системы перестал быть формулой по `star.industry` (давал **1.4 Cr/год**) и стал долей
  ГОДОВОГО ВЫПУСКА НЕДР — той же величины, из которой считается цена системы: медиана
  **11.4 млн Cr/год**, окупаемость медианно ~10 000 лет с разбросом 4.4k…44k (разброс — фича,
  гарантии нет ни у одной системы). Колонии сами телеграфируют прибыль владельцу, у игрока —
  через поплавок §16. Появилась фракция-банк **Clearing House**: собирает все лицензионные
  деньги и субсидирует задыхающиеся колонии. ⚠️ Это закрыло дыру §16: тариф капал в казну
  своей же фракции, и игрок снимал его обратно — квота не стоила ничего.
- 🌍 **СДЕЛАНО (2026-08-06): масштаб мира — раздел 17. Система перестала быть посёлком.**
  Население было 500..25 500 человек на ЗВЁЗДНУЮ СИСТЕМУ, и вся экономика наследовала этот
  масштаб: глубина рынка 35 Cr, полный трюм рудовоза обваливал цену вдвое, ВВП скопления
  3.27e6 Cr/год против цены системы 1e9. Источник один — генератор; население стало
  логарифмическим по 3.5 порядкам (медиана **4.7e7**), размер хозяйства — множительным
  (люди × индустриализация). Итог: **новичок (трюм 110) не двигает рынок вовсе (0.9989
  котировки), Giga-Freighter роняет втрое (0.329)**; типичная сделка 3.0x капитала, куш >5x
  лишь у 5–7% систем. **Тронешь генератор населения — перемеряй `SYSTEM_PRICE_*_REF` и
  `LICENCE_QUOTA_BASE` (§17.5).**
- 🏦 **СДЕЛАНО (2026-08-06): счёт фракции ходит СВЕТОМ — раздел 16.** Кошелёк лежит на
  борту, но есть и счёт, доступный из любой системы. Взнос попадает на счёт мгновенно, а
  ПОТРАТИТЬ его можно только когда известие покрыло всё скопление (замер: 111 лет из родной
  системы при радиусе 101 ly). Двойная трата двумя бортами в разных концах становится
  невозможной по построению, а ограничение — механикой: либо жди сверку, либо вези кошельком.
  Кнопки `TO ACCOUNT` / `FROM ACCOUNT` в окне биржи. **Поплавок обязан быть в сейве**, иначе
  загрузка печатает деньги (§16.5).
- 🔎 **СДЕЛАНО (2026-08-06): сквозной аудит — раздел 15. ЧИТАЙ §15.2 ДО ЛЮБОЙ РАБОТЫ.**
  Девять правок, которых лестница проверок не ловила: модули не переживали ни покупку нового
  корпуса, ни гибель корабля (бонусы запечены в поля, список — нет; снятие фантома уводило
  корпус в −565); второй борт рождался в `(0,0,0)`, а не на своей верфи; `maxModules` не
  сохранялся, и после загрузки у ВСЕХ бортов становилось 3 слота; ограничитель расхода
  считал энергию струи классически, а списание — релятивистски; двигатель перенастраивался
  В ПОЛЁТЕ вопреки §12.4 (теперь гейт `enRoute`, после `STOP` в пустоте настраивать МОЖНО);
  удалён мёртвый 2D-прототип `civ/galaxy/graphic`; денежный уровень скопления вынесен в
  партию (`Game::clusterPriceLevel`) — раньше ставка тарифа зависела от возраста ПРОЦЕССА,
  а не партии; покупка второго борта больше не бросает игрока на пустой корабль, деньги между
  своими бортами возятся кнопками `GIVE CR`/`TAKE CR` в окне HOLD. **Сейв поднят 12 → 13.**
  **§15.2 — что осталось, всё с замерами.** ⚠️ Главное: **экономика деревенская, лестница
  прогрессии галактическая.** Население системы — медиана 13 166 человек, оборот 2 581 Cr/год,
  ВВП ВСЕГО скопления 3.27e6 Cr/год, глубина рынка по элементу 35 Cr. Полный трюм рудовоза =
  пятилетний ВВП системы, отсюда потолок 74 000 Cr за рейс — он выведен, а не назначен. А
  цена системы 9.2e8 = 356 449 годовых оборотов этой же системы. Корень ОДИН и он в
  генераторе (`star.population = 500 + u*25000`), но пересчёт сквозной. Там же: вариант
  «доход колонии = пошлины с трафика» замером ОТМЕНЁН (116 агентов делают 21 сделку за 120
  лет — рейс длится век); `LICENCE_TARIFF_BASE` не действует никогда; казна `Player Freehold`
  — дыра, куда уходят тарифы самого игрока.
- 🏦 **СДЕЛАНО (2026-08-06): собственность на систему — раздел 13.** Колонии сведены к ОДНОЙ
  операции: покупка системы целиком (порядок **миллиард** — тир капитальных кораблей). Цена —
  произведение характеристик (люди, заводы, обитаемость, годовой выпуск недр В КРЕДИТАХ,
  построенное, суверенная надбавка), разбор виден в окне. Своя система торгует с игроком по
  цене 0, но запас и цена рынка двигаются как обычно. Касса колонии доступна только на месте.
  Убраны основание за материалы, «подкрепление» и наём кораблей; попутно вычищено `colonies[starIndex]`
  (чтение за границей вектора). +4 балансовых и +5 клик-проверок. **Читай §13.2 до правок владения.**
- ⚖️ **СДЕЛАНО (2026-08-06): квота лицензии 1 000 → 10 000** (§13.8, замер и цена решения — §10.4).
  Тысяча закрывалась за ~440 лет из тысячелетнего периода — обязательство, которого не чувствуешь.
  Десять тысяч стартовым корпусом за период оборотом не берутся: первое тысячелетие кончается
  доплатой кредитами, дальше квота снова берётся торговлей. `LICENCE_PRICE_K` опущен 1000 → 100,
  чтобы вторая лицензия осталась миллионом.
- 🚀 **СДЕЛАНО (2026-08-05): двигательная модель — раздел 12. КРУПНАЯ ПЕРЕДЕЛКА.** Топливо и
  рабочее тело — два РАЗНЫХ вещества, оба смеси элементов в объёмных ёмкостях. Энергия из кривой
  связи (ноль на железе), релятивистский Циолковский со стеной достижимости `k >= 1`, зола
  возвращается в трюм товаром. Три семейства движков, ручка режима и крейсерской скорости,
  окно `HOLD / TANKS`. Единственная подгонка — `DELTAV_SCALE` (250x), названа явно в §12.3.
  Симуляция стала на 40% дешевле (272 → 159 мс на игровой год). **Читай §12.5 «грабли» до правок.**
- ✅ **СДЕЛАНО (2026-07-28, срез 2):** **вид из кокпита (от первого лица)** — глаз камеры в позиции
  корабля, взгляд вдоль носа; собственная стрелка убрана, вместо неё прицел-«мушка» по курсу +
  маркер вектора скорости (prograde). Chase-cam заменён на кокпит; вид-карта (`C`) не тронута.
  Камера сведена в единый помощник **`buildLocalCamera`** (`local.h`), общий для игры и
  скриншот-харнеса (`make shots`). `+/−` в 3D теперь оптический зум/FOV (`localFovScale`). Близкий
  собственный выхлоп больше не раздувается в экранные квадраты (кап размера частиц по типу). Все
  проверки + soak 120k зелёные; 5 скриншотов сняты и проверены. Детали — **§5.13 / §7.1.1**.
- 🎮 **Плейтест №2 пройден → проход углубления №3 (§5.13.1, §7.1.2):** по фидбеку закрыты 5 пунктов —
  **звезда как аналитическая сфера** (чистая геометрия: центр + радиус, никаких моделей; изнутри —
  заливка плазмой), **окклюзия луч↔сфера** (планеты не просвечивают сквозь звезду), **гигантский
  масштаб** (`starRadius 90..135 LU`, планеты/корабли — крапинки), **мышелук** (шутерный обзор в
  кокпите, ЛКМ — огонь), фикс **синего квадрата при стрельбе**. Все проверки + soak 120k + 10 шотов
  зелёные.
- 🎮 **Плейтест №3 пройден → проход углубления №4 (§5.13.2, §7.1.3):** «масштаб всё равно звезда
  маленькая». Инсайт: вид звезды зависит только от **отношения D/R** — орбиты уходили к R·13.
  Переписаны как **доли радиуса** (внешняя ≤ R·3.4), звезда ×2.9 (**260..360 LU**), старт D/R≈2.8
  (~82% высоты) → звезда доминирует кадр ВЕЗДЕ. + фикс «спиц» эклиптики, бесшовный порог заливки
  R·1.7. Все проверки + soak 120k + 10 шотов зелёные.
- 🎮 **Плейтест №4 пройден → проход углубления №5 (§5.13.3, §7.1.4):** масштаб одобрен, но «баг
  костыль — подлетаю, всё жёлтое со всех сторон… звезда сделана неправильно… используй шейдеры, шар
  плазмы». Баг = `fillRect(весь экран)`-заливка при D≤R·1.7. Заменён **попиксельным ray-sphere
  «шейдером»** (`renderStarPlasma`): изотропная сфера (центр+радиус), плазма = функция геометрии луча
  (лимб-даркенинг + турбулентность + корона), стриминг-текстура (SDL2-only). Ноль порогов/хардкода.
  Проверка `shot_star_lookaway` (вблизи, глядя прочь → ЧЁРНЫЙ космос). soak 120k + 11 шотов + ASan
  шейдера — зелёные.
- ✅ **СДЕЛАНО (2026-07-28, автономный срез рендера, §5.13.4):** **планеты и луны — тоже освещённые
  ray-sphere сферы** (тот же приём, что звезда §5.13.3, распространён с самосветящейся звезды на
  отражающие тела). Свет — звезда в (0,0,0): ламбертов терминатор день/ночь → фазы/серпы; попиксельная
  окклюзия луч↔звезда; поверхность из нормали по типу тела (полосы газ-гиганта, мозаичность скалы,
  альбедо+полюса льда, моря луны); мягкий терминатор + атмосферный ободок лимба. Только перспектива;
  орто-карта (§2.7) и станции — по-прежнему диски (побитово не тронуты). Отдельная полуразрешающая
  стриминг-текстура, SDL2-only. Вся лестница + soak 120k + **12 шотов** + ASan сферы — зелёные.
- ✅ **СДЕЛАНО (2026-07-28, автономный срез рендера, §5.13.5):** **кольца газовых гигантов в
  перспективе** (ray↔plane аннулюс в экваториальной плоскости, нормаль = ось вращения планеты).
  Щели Кассини, радиальные полосы, мягкая тень планеты на кольцах (impact-parameter полутень),
  grazing-opacity (с ребра плотнее), композит по глубине сфера↔кольцо (передняя дуга над планетой,
  задняя за сферой), окклюзия звездой. Встроено в тот же `renderBodySphere`; орто-карта не тронута.
  Добавлена цель `make shot_asan`. Вся лестница + soak 120k + 12 шотов (`shot_planet_near` = Сатурн) +
  ASan колец — зелёные.
- ✅ **СДЕЛАНО (2026-07-28, автономный срез рендера, §5.13.6):** **полировка звезды-плазмы** —
  спектральный контраст O→M (горячая сине-белая ↔ холодная красно-оранж), хромосферный ободок у лимба,
  протуберанцы/вспышки на короне (угловые плюмы), пятна-сунспоты (тёмные клетки), доменный варп 2-го
  порядка + доп. октава. Всё — чистая функция геометрии (изотропно, без порогов). Новый шот
  `shot_star_hot` (пульсар) — пара к холодной STAR70. **13 шотов**, вся лестница + soak 120k зелёные.
- ✅ **СДЕЛАНО (2026-07-28, автономный срез рендера, §5.13.7):** **астероиды пояса — освещённые
  «глыбы»** (тот же ray-sphere, что тела; закрыт последний крупный «плоский» разрыв). Ламбертов
  терминатор, **выгрызенный** неровный силуэт, кратеры, **кувыркание** (переиспользован `LocalRock.spin`);
  бюджет — шейдер для `screenR≥5`, мелочь фазовым диском; орто-карта побитово цела (§2.7). Новый шот
  `shot_belt_near`. **14 шотов**, вся лестница + soak 120k зелёные.
- ✅ **СДЕЛАНО (2026-07-28, автономный срез рендера, §5.13.8):** **туманность-фон стала объёмным
  газовым полем на небесной сфере** (`renderNebula`, попиксельно, тот же изотропный «функция луча»
  подход). Многооктавный шум по направлению мирового луча (пухлые сгустки + волокна) + доменный
  варп + крупномасштабный градиент «банки» (плотнее в одной части неба, ось детерминирована из
  тона) + слабое побеление ядер волокон; полупрозрачная (звёзды скайбокса просвечивают), дрейф по
  `fxClock`. Только перспектива; **орто-карта `C` (плоский тон + 3 блоба) побитово не тронута** (§2.7).
  Новый forced-шот `shot_nebula`. **15 шотов**, вся лестница + soak 120k зелёные.
- ✅ **СДЕЛАНО (2026-07-28, автономный срез СИМУЛЯЦИИ, §5.13.9):** **живая система — целевой
  трафик NPC + жизненный цикл прилёта/отлёта** (первый срез, углубляющий не рендер, а поведение).
  Не-боевые NPC (торговцы/гражданские/патруль вне боя) больше не блуждают вслепую — они выполняют
  РЕЙСЫ: круиз к рынку → стоянка «у причала» (разнос вокруг тела по индексу, гасят ход) → новый
  рейс; чисто-локальные (`agentIndex<0`) изредка (`ERRAND_DEPART_PROB=0.30`) уходят к краю системы
  и деспаунятся за ним. Прилёты подсаживают свежих торговцев с края, пока кораблей < `TRAFFIC_CAP`
  (10), по таймеру `TRAFFIC_MIN..MAX_H` — трафик течёт в обе стороны, у станции всегда жизнь.
  Зеркала макро-агентов (`agentIndex>=0`) курсируют, но систему НЕ покидают (иначе рассинхрон с
  макро). Пираты не тронуты (охотники — combat invariant). Локальный seeded RNG (§2.3), global rng
  не тронут; макро не пишется (write-back отложен → **реализован в §5.13.14**). Шотов не добавлял (поведение видно только в
  динамике). **15 шотов**, вся лестница + soak 120k (invariantFails=0, `craft`≤10, deep space
  пустеет) зелёные.
- ✅ **СДЕЛАНО (2026-07-28, автономный срез, §5.13.10):** **читаемость трафика §5.13.9 из кабины**
  (поведение НЕ менялось — сделали его видимым). Прилёт/отлёт теперь дают **циан-вспышку «варпа»**
  (кольцо + аддитивное ядро + искры, `emitWarp`), уход из-под причала — **синеватый пуфф форсажа**,
  стоянка `errand==1` — **пульсирующее янтарное кольцо-швартовку** (≠ синий щит, ≠ циан-рамка), а в
  панели радара над `SIG` появился **тальник `IN <n>  DOCK <n>`** (сколько идёт к рынку / стоит у
  причала). Строго аддитивно (пираты/урон/ИИ не тронуты), FX под `FX_MAX` (`fxPeak` вырос 6–9 → 16–26),
  орто-карта побитово цела. Добавлен шот №15 `shot_traffic_dock`. **16 шотов**, вся лестница + soak
  120k (invariantFails=0) зелёные. Правки: `localsim.cpp`, `localdraw.cpp`, `shot_test.cpp`.
- ✅ **СДЕЛАНО (2026-07-28, автономный срез, §5.13.11):** **бедствие конвоя + пейофф за защиту** —
  окружающие рейды «пират vs торговец» стали читаемым, действенным событием. Пират (`CK_PIRATE`)
  в состоянии атаки (`aiState==1`) на конкретный НЕ-пиратский борт помечает жертву `underAttack`;
  та получает **красный пульсирующий «SOS»-маяк** (кольцо у борта + краевой маркер за кадром) и
  **отдельное состояние панели цели** (SOS-рамка + красная метка + чип `SOS`); на переднем фронте
  рейда — тост **`CONVOY RAID`** (нарочно ≠ POI-радиоисточник `DISTRESS CALL`, это разные сущности).
  **Пейофф:** уничтожил пирата, который в этот момент угрожал конвою → **бонус кредитов + очки
  исследования + тост `CONVOY SAVED` + положительный rep-бамп** фракции жертвы (`adjustFactionRelation`).
  Строго аддитивно — **пираты НЕ ослаблены** (combat invariant соблюдён), никакого global rng (§2.3).
  Добавлен шот №16 `shot_convoy_distress`. **17 шотов**, вся лестница (shot_asan обязателен —
  правился `localdraw.cpp`) + soak 120k (invariantFails=0) зелёные. Правки: `local.h`, `localsim.cpp`,
  `localdraw.cpp`, `shot_test.cpp`.
- ✅ **СДЕЛАНО (2026-07-28, автономный срез, §5.13.12):** **эскорт-патрули реагируют на бедствие
  конвоя** — прямое углубление §5.13.11. Патруль (`CK_PATROL`) теперь «слышит» рейд с большого радиуса
  (`PATROL_DISTRESS_R=1400 LU`): среди пиратов в этом радиусе ищет **налётчика** — пирата ближе
  `RAID_NEAR=260 LU` к любому торговцу/civilian — и **приоритетно** идёт на перехват (флаг `defending`)
  вместо простого «ближайшего пирата». Детект по СЫРЫМ позициям (пират рядом с не-пиратом), а не по
  per-frame флагам §5.13.11 → порядок индексов не важен. Читаемость: **зелёный пульс-кольцо «эскорт»**
  у идущего на перехват патруля (радиусы `sz+8/sz+13`, чтобы не сливаться с циан-рамкой цели `sz+6`),
  **зелёный краевой маркер** «помощь идёт» из-за кадра и **ESCORT-состояние панели цели** (зелёная
  рамка/метка + чип `ESCORT`). Всё зелёное `90,220,130` = «свои» (≠ SOS-красный жертвы, ≠ янтарь причала).
  Строго аддитивно — **пираты НЕ ослаблены**: расширен лишь радиус ОСВЕДОМЛЁННОСТИ патруля, огонь всё так
  же гейтится `WEAPON_RANGE=150 LU` (патруль долетает и бьётся в упор, а не снайпит с 1400 LU). Никакого
  global rng (§2.3). Добавлен шот №17 `shot_patrol_escort`. **18 шотов**, вся лестница (shot_asan
  обязателен — правился `localdraw.cpp`) + soak 120k (invariantFails=0) зелёные. Правки: `local.h`,
  `localsim.cpp`, `localdraw.cpp`, `shot_test.cpp`.
- ✅ **СДЕЛАНО (2026-07-28, автономный срез рендера, §5.13.13):** **вспышки-СОБЫТИЯ на короне звезды**
  (flare-цикл) — углубление §5.13.6 (был статичный «протуберанец»-шум). В `renderStarPlasma` (ветка
  короны) добавлены **две активные области**, которые медленно дрейфуют по лимбу (угол `ap` растёт с
  `fxClock`) и **периодически эруптируют**: цикл `cyc` → `frac→0` даёт всплеск `flareBoost` (яркость
  ×до 2.2, локальный вырост короны +30%, цвет сдвигается к горячему бело-голубому `255,248,235`).
  Полностью **детерминированно** (только `fxClock`, ноль RNG) и **нулевой регресс при `fxClock=0`**:
  на всех прежних звёздных шотах (рендерятся при `fxClock=0`) `flareBoost` на видимом лимбе <2%, корона
  осталась изотропной; вспышку ловит лишь новый прицельный шот `shot_star_flare` (№18, D/R=2.3,
  `fxClock=26` → область №0 в пике эрупции на правом лимбе — яркий горячий протуберанец). `coronaK`/bbox/
  амбиентная корона не тронуты (вырост ограничен прежним гейтом `coronaR2`), `std::exp` зовётся только
  при `cosang>0.6` (редко). **19 шотов**, вся лестница (shot_asan обязателен — правился `localdraw.cpp`,
  19/19 без ошибок) + soak 120k (invariantFails=0, deathPath=ok) зелёные, звёздные PNG просмотрены
  глазами (вспышка видна в `shot_star_flare`, прочие звёздные шоты без регресса). Правки: `localdraw.cpp`,
  `shot_test.cpp`.
- ✅ **СДЕЛАНО (2026-07-28, автономный срез СИМУЛЯЦИИ, §5.13.14):** **write-back локальных убийств в макро**
  — первый срез ПОСЛЕДСТВИЙ (не рендер). Сбитый в локальном полёте борт-**зеркало** реального NPC
  (`LocalCraft.agentIndex>=0`) теперь «умирает» и в ПОСТОЯННОМ мире: соответствующий `game.agents[i]`
  деградирует в **спас-капсулу** (ship-класс 0), груз сброшен, `cargoCost=0`, **кредиты сохранены**
  («credits immaterial»). Общий помощник `downgradeAgentToEscapePod(Agent&)` **вынесен из `robAgent`**
  (устранил дубль двух byte-identical inline-блоков даунгрейда) и зовётся и из макро-боя, и из локального
  убийства `(E)`. **Детерминированно** (без RNG — §2.3-safe: `shipAutofit` — чистая арифметика). Репрессия
  фракции уже применялась локально (§5.13.11) ⇒ write-back делает **только** даунгрейд, без двойного счёта
  отношений. Игрок-агент в зеркала не берётся (`localgen` пропускает `playerAgent`) + явная страховка
  `agentIndex != playerAgent`. **Проверка:** детерминированный write-back-пробник в `soak` (привязали
  зеркало к реальному агенту, добили выстрелом игрока — учтено туннелирование снаряда `520·0.05=26 LU/кадр` —
  и проверили `game.agents[idx].ship.name=="Escape Pod"`, оружие=0, груз пуст). Лестница зелёная: `make`
  (0 предупр.), оба smoke (exit 0), `make shot_asan` **19/19** (localdraw не трогали, но дёшево — 0 ошибок),
  `make soak` **120000 кадров, invariantFails=0, deathPath=ok, writeBack=ok**; `craftDestroyed=2`/
  `radioClaims=3` — как в бейзлайне (регресса нет). Правки: `game.h`, `game.cpp`, `localsim.cpp`,
  `soak_test.cpp`.
- ✅ **СДЕЛАНО (2026-07-28, автономный срез РЕНДЕРА, §5.13.15):** **разнотипные породы астероидов по
  элементу** — прямое углубление §5.13.7 (закрыт пункт «разнотипные породы металл/лёд/углерод по
  `element`»). Новая ЧИСТАЯ ф-я `rockAppearance(element)` (`localgen.cpp`, декл. `local.h`) относит камень к
  классу по атомному номеру/металличности элемента (реальная таксономия): **лёд/летучие** (H,He,N,O) —
  яркая голубовато-белая, блестит; **углеродистые** (C,S) — тёмный уголь, матовый; **металлические**
  (Fe/Co/Ni + тяжёлые металлы/актиниды с `metallicTrait>0.35`) — стальной, яркий блик; **силикаты** (всё
  прочее) — тёпло-бурый камень, матовый (≈ прежний вид пояса — безопасный дефолт). Палитра запекается в
  `rock.r/g/b` + новое поле `LocalRock.spec` (зеркальность 0..1); лёгкая вариация яркости — из
  ДЕТЕРМИНИРОВАННОГО хэша индекса (**не** `lrng`), поэтому поток генерации не сдвинут ⇒ станция/радио/корабли
  спавнятся ровно как прежде (строго аддитивно). В `renderRockLit` добавлен **зеркальный блик Блинна–Фонга**
  (только `view.perspective`, гейт `rk.spec>0` и освещённая сторона): лёд/металл дают горячий хайлайт,
  углерод/силикат матовы. Детерминированно (§2.3 — global rng не тронут; `rockAppearance` без RNG). **Новый
  шот `shot_belt_types`** (4 камня в ряд — лёд/углерод/металл/силикат — палитра из той же `rockAppearance`).
  Лестница зелёная: `make` (0 предупр.), оба smoke (exit 0), `make shot_asan` **20/20** (покрывает новую
  specular-математику под ASan/UBSan — 0 ошибок), `make soak` **120000 кадров, invariantFails=0** (+новый
  инвариант `spec∈[0,1]` по всем 648 породам), `deathPath=ok, writeBack=ok`, `craftDestroyed=2`/`radioClaims=3`
  — как в бейзлайне (нулевой регресс спавнов), `rocks=648 shiny=391` (реальное разнообразие материалов);
  `make shots` **20/20**, просмотрены `shot_belt_types` (4 чётко различимых материала + блики на льду/металле,
  матовость углерода/силиката) и `shot_belt_near` (силикатный пояс STAR70 ≈ прежний вид, кольца добычи целы).
  Правки: `local.h`, `localgen.cpp`, `localdraw.cpp`, `shot_test.cpp`, `soak_test.cpp`.
- ✅ **СДЕЛАНО (2026-07-28, автономный срез СИМУЛЯЦИИ, §5.13.16):** **состав породы влияет на темп
  добычи** — прямое углубление §5.13.15 (закрыт Known-Gap «тип влияет только на вид, не на добычу»).
  Классификация §5.13.15 вынесена в **единый чистый `rockClass(int element)→enum RockClass`**
  (`localgen.cpp`, декл. `local.h`), который теперь кормит И рендер (палитра/блик), И симуляцию — рассинхрон
  «выглядит как металл, копается как силикат» невозможен. Множитель выхода `rockYieldMult` к базовому темпу
  `MINE_RATE_BASE=9.0`: металл **×1.60** (богатая руда), лёд **×1.25**, углерод **×0.65**, силикат **×1.00**
  (=прежний ⇒ нулевой регресс дефолт-класса). `rockAppearance` зарефакторен через `rockClass` — облик
  **байт-в-байт** (specs `0.55/0.05/0.62/0.08`, shiny=391). Тост добычи `MINING <symbol> <CLASS>`, HUD-оверлей
  `MINING <CLASS> +<accum>`. Combat/AI/генерация не тронуты. Лестница зелёная: `make` (0 предупр.), оба smoke
  (exit 0), `make shot_asan` **20/20** (`[belt-types] specs=0.55/0.05/0.62/0.08 class=ICE/CARBON/METAL/SILICATE
  yields=1.25/0.65/1.60/1.00` — 0 ошибок ASan/UBSan), `make soak` **120000 кадров, invariantFails=0**
  (+новый инвариант класс/выход по всем 648 породам), `deathPath=ok, writeBack=ok`, `craftDestroyed=2`/
  `radioClaims=3`/`rocks=648 shiny=391` — всё как в бейзлайне §5.13.15 (нулевой регресс генерации и облика);
  `make shots` **20/20**, `shot_belt_types` без визуального регресса. Правки: `local.h`, `localgen.cpp`,
  `localsim.cpp`, `localdraw.cpp`, `shot_test.cpp`, `soak_test.cpp`.
- ✅ **СДЕЛАНО (2026-07-28, автономный срез РЕНДЕРА, §5.13.17):** **майнинг-лазер из кокпита, окрашенный
  в цвет КЛАССА породы** — прямое углубление §5.13.15/16: замыкает петлю «вид → выход → **действие**».
  Пока идёт бурение (задан `miningRock` И цель В ЗОНЕ извлечения `d² ≤ (radius+MINE_RANGE)²` — те же
  условия, при которых `localsim` реально извлекает руду — И не заслонена звездой) из кокпитного «дула»
  (`px + pfwd·5 − pup·1.6`, как вспышка выстрела) к глыбе тянется луч: ореол = цвет класса `rk.r/g/b`
  (сталь-металл / бело-голубой лёд / тёмный углерод), сердцевина к бело-горячему, мягкая толщина ⟂ лучу,
  точка удара + вспышка у дула. Пульс/хум — **детерминированно** по `sin(fxClock·20)`, без RNG (§2.3).
  **Только перспектива**; вне зоны/не бурим — не рисуется ⇒ нулевой регресс (в т.ч. `shot_belt_near`, где
  глаз дальше `MINE_RANGE`: луч не появляется, тост `MINING SILICATE +0` цел). Draw-only — симуляция не
  тронута. Лестница зелёная: `make` (0 предупр.), оба smoke (exit 0), `make shot_asan` **21/21**
  (`[mining-beam] class=METAL inRange=YES beamRGB=154,150,154`; `[belt-types]` байт-в-байт `0.55/0.05/0.62/0.08`;
  0 ошибок ASan/UBSan), `make soak` **120000 кадров, invariantFails=0** — `radioClaims=3 craftDestroyed=2
  rocks=648 shiny=391 deathPath=ok writeBack=ok` (**точь-в-точь** бейзлайн §5.13.16 ⇒ доказан draw-only),
  `make shots` **21/21**: `shot_mining_beam` = стальной луч дуло→металл-глыба, `shot_belt_near` без луча
  (вне зоны). Правки: `localdraw.cpp`, `shot_test.cpp`.
- ✅ **СДЕЛАНО (2026-07-28, автономный срез СИМУЛЯЦИИ, §5.13.18):** **зеркала-торговцы продают груз на
  местном рынке при стыковке** — второй срез ПОСЛЕДСТВИЙ после §5.13.14 (закрыт Known-Gap «торговцы летят
  к рынкам, но не торгуют»): живая экономика теперь ПИШЕТ в постоянный мир, а не только читает из него.
  Когда co-located борт-зеркало реального макро-агента (`agentIndex ≥ 0 && kind == CK_TRADER`) завершает
  переход швартовки (`errand 0→1` в `localsim`) в валидной рыночной системе и агент несёт груз — зовётся
  тот же **детерминированный** `sellCargo`, что макро-`updateTrader` дёргает на прибытии (game.cpp): реальные
  кредиты/груз/цены/казна фракции сдвигаются в персистентном мире. **SELL-ONLY** — без `buyCargo`/`startJourney`
  (те трогают глобальный rng и макро-локацию), поэтому §2.3-safe: локальный режим не касается глобального rng.
  Двойного счёта с макро-`updateTrader` нет — **макро-симуляция на время локального полёта заморожена** (main.cpp).
  Обёртка `localDockSellCargo` (game.h/game.cpp) — по паттерну §5.13.14 `downgradeAgentToEscapePod`; лежит ВНЕ
  анонимного namespace game.cpp (внешняя линковка для `localsim`, но видит внутренний `sellCargo` по неквалиф.
  поиску TU). Строго аддитивно: чисто-локальные craft (`agentIndex < 0`) и не-торговцы не тронуты. Читаемость —
  дебаунс-тост `TRADER SOLD <SYM>` (шрифт авто-в-верхний-регистр) через новый `scene.tradeCooldown`. Лестница
  зелёная: `make` (0 предупр.), оба smoke (exit 0), `make shot_asan` **21/21** (0 ошибок ASan/UBSan; `[mining-beam]`
  и `[belt-types]` байт-в-байт), `make soak` **120000 кадров, invariantFails=0**, **новый детерминированный
  sell-write-back-пробник `sellWriteBack=ok`** (в свежем `Game`: груз агента уменьшился, кредиты выросли,
  счётчик сделок > 0) + инвариант «деньги всех агентов конечны»; `radioClaims=3 craftDestroyed=2 rocks=648
  shiny=391 deathPath=ok writeBack=ok` — **точь-в-точь** бейзлайн §5.13.16/17 ⇒ доказан нулевой регресс
  существующей симуляции; `trades=0` органически (soak-харнесс заставляет игрока палить по всем бортам — торговцы
  гибнут/рассеиваются до мирной стыковки; пробник моделирует мирный путь). Правки: `game.h`, `game.cpp`,
  `local.h`, `localsim.cpp`, `soak_test.cpp` (draw-код НЕ тронут).
- ✅ **СДЕЛАНО (2026-07-28, автономный срез РЕНДЕРА, §5.13.19):** **пылевые полосы и эмиссионные
  ядра туманности** — углубление §5.13.8 (был единый ровный полупрозрачный слой газа). В `renderNebula`
  (leaf-функция, единственный вызов из перспективной ветки) добавлены два строго аддитивных прохода
  ПОВЕРХ прежней плотности газа, не трогая старую модель облаков: (1) **пылевые полосы** — ОТДЕЛЬНЫЙ
  домен-варп (иные фазы/частоты, декоррелирован от сгустков) + сумма синусов (полосовая, нитевидная
  структура) → тьма у нулевого контура: `dens *= 0.20 + 0.80·lane` (ядро полосы гасит свечение газа до
  ~20% ⇒ тёмный провал и просвет неба сквозь «пыль» — физичное поглощение); (2) **эмиссионные ядра** —
  `core = dens³` (резкий отклик ТОЛЬКО у пиков плотности, уже за пылью), `emit = 0.55·core`: альфа
  ×(1+0.6·core) и `wf = 0.30·dens + emit` (clamp 0.92) — плотнейшие волокна светятся ярче, к белому
  («звёздные ясли»). Полностью **детерминированно** (только `fxClock` + направление луча, ноль RNG —
  §2.3), **перспектива-only** (§2.7: орто-путь — ОТДЕЛЬНАЯ ветка, плоский тон + 3 блоба, побитово цел),
  **нулевой регресс** прочего рендера (изолирована от звезды/колец/астероидов). Осознанно ОТЛОЖЕНО:
  эмиссия вокруг вкраплённых скайбокс-звёзд — потребовала бы прокинуть `game.cluster.stars` в
  `renderNebula` (сломала бы draw-only/zero-coupling). Шот не добавлял — `shot_nebula` уже форсит
  перспективную туманность и детерминированно лочит эффект. Лестница зелёная: `make` (0 предупр.), оба
  smoke (exit 0), `make shot_asan` **21/21** (0 ошибок ASan/UBSan; `[mining-beam]`/`[belt-types]`
  байт-в-байт ⇒ не-туманные пути не тронуты), `make soak` **120000 кадров, invariantFails=0** —
  `radioClaims=3 craftDestroyed=2 rocks=648 shiny=391 deathPath=ok writeBack=ok sellWriteBack=ok` —
  **точь-в-точь** бейзлайн §5.13.18 ⇒ доказан draw-only, `make shots` **21/21**: глазами `shot_nebula`
  (тёмная пылевая полоса рассекает газ + эмиссионные ядра) и `shot_system_cockpit` (звезда/пояс/HUD без
  регресса). Правки: `localdraw.cpp` (+ `master_prompt.md`, `README.md`).
- ✅ **СДЕЛАНО (2026-07-28, автономный срез СИМУЛЯЦИИ, §5.13.20):** **композиционно-взвешенная
  генерация пояса** — закрыт Known-Gap «спавн пород состав-слеп». В `buildLocalScene` (`localgen.cpp`)
  класс каждого камня теперь тянется к составу СИСТЕМЫ-хозяина: металличность → металлический пояс,
  бедность металлом → льдистый, экономический профиль `resourceFocus` → свой класс. До цикла камней
  (чистая арифметика над `star`/`defs`, НОЛЬ `lrng`) считаются пулы элементов по классам
  (`classPool[4]`/`focusPool[4]` через `rockClass`) и веса классов `w[4]` (silicate 1.6; metal
  0.5+2.6·mNorm; ice 0.5+1.8·(1−mNorm); carbon 0.8; +0.9·|focusPool[c]|; ноль там, где класс пуст;
  `mNorm=clamp((metallicity−0.22)/0.95,0,1)` — диапазон генерации [0.22,1.17]→[0,1]). В цикле класс
  выбирается по CDF от ДЕТЕРМИНИРОВАННОГО хэша индекса `i` (`sin(i·78.233+12.9898)`, декоррелирован от
  §5.13.15-цвета), элемент — из `focusPool` своего класса, иначе из общего `classPool` (индекс — второй
  хэш `sin(i·39.425+7.311)`); фолбэк на базовый `elem`, если пул пуст. КЛЮЧ (§2.3): базовые
  `irand`-розыгрыши элемента СОХРАНЕНЫ 1-в-1 (стрим-якорь) — поток `lrng` не сдвинут, поэтому
  станция/трафик/радио/write-back спавнятся ПОБИТОВО как прежде; ноль `lrng` в самом взвешивании ⇒ строго
  аддитивно. Лестница зелёная: `make` (0 предупр.), оба smoke (exit 0), `make shot_asan` **21/21**
  (0 ASan/UBSan; индексация `w[4]`/пулов чиста; `[belt-types]` форс-пробник байт-в-байт), `make soak`
  **120000 кадров, invariantFails=0** — `radioClaims=3 craftDestroyed=2 rocks=648 deathPath=ok
  writeBack=ok sellWriteBack=ok trades=0` **точь-в-точь** бейзлайн §5.13.19, а `shiny` 391→**382** —
  ЕДИНСТВЕННЫЙ ожидаемый сдвиг (отпечаток фичи: доля блеска ~59% та же, но теперь РАСПРЕДЕЛЕНА по составу
  системы, а не по дихотомии пустой/непустой `resourceFocus`). `make shots` **21/21**: глазами
  `shot_belt_near` (силикат в прицеле + металл/лёд в поясе) и `shot_mining_beam` (`MINING METAL +42`).
  Правки: `localgen.cpp` (+ `master_prompt.md`, `README.md`).
- ✅ **СДЕЛАНО (2026-07-28, автономный срез РЕНДЕРА, §5.13.21):** **тень колец на планете** —
  завершён «Сатурн»: тело уже отбрасывало тень на кольца, но кольца НЕ затеняли планету (Known-Gap).
  Обратный эффект в `renderBodySphere` (`localdraw.cpp`): из каждой ОСВЕЩЁННОЙ точки поверхности S луч
  к звезде (в 0,0,0); если он пересекает аннулус колец МЕЖДУ поверхностью и звездой — прямой свет
  гасится (`diff *= 1−occ`, до ~85%). Плотность кольца в точке пересечения — ТА ЖЕ модель, что у самих
  колец (мягкие кромки + деления Кассини + полосы), поэтому сквозь ЩЕЛЬ Кассини тени нет (видно в шоте
  как светлый разрыв в тёмной полосе). Вставлено между клампом `diff` и `lightF`, под гейтом `hasRing`
  ⇒ у всех НЕ-кольцевых тел код не исполняется, `diff` цел ⇒ ПОБИТОВО прежний вид (нулевой регресс).
  Детерминированно (ноль RNG, даже без `fxClock`), перспектива-only (`renderBodySphere` зовётся лишь
  под `view.perspective`; орто-путь — отдельная ветка `fillCircle`). Лестница зелёная: `make` (0 предупр.),
  оба smoke (exit 0), **`make shot_asan` 21/21** (0 ASan/UBSan; деление на `dSA` под гейтом `fabs>1e-6`),
  **`make soak` 120000 кадров, invariantFails=0** — ВЕСЬ бейзлайн §5.13.20 включая `shiny=382` цел (чистый
  draw ⇒ ноль влияния на генерацию), `make shots` **21/21**: глазами `shot_planet_near` — тёмная полоса
  тени колец рассекает диск газового гиганта, щель Кассини видна как просвет в тени. Правки:
  `localdraw.cpp` (+ `master_prompt.md`, `README.md`).
- ✅ **СДЕЛАНО (2026-07-28, автономный срез СИМУЛЯЦИИ, §5.13.22):** **запас руды пояса = класс породы ×
  насыщенность системы** — до сих пор `rock.ore` был плоским `frand(20,120)`, слепым И к классу, И к
  `star.miningRichness` (последняя влияла ТОЛЬКО на макро-добычу `mining.cpp`, локальный пояс её
  игнорировал — Known-Gap). Теперь после выбора элемента камня `rock.ore *= oreRichnessMult(rockClass(elem),
  star.miningRichness)` (`localgen.cpp`): множитель = класс (metal 1.5 / silicate 1.0 / ice 0.9 / carbon 0.8 —
  ОТДЕЛЬНАЯ ось «тоннаж», в отличие от `MINE_YIELD_*`=«темп») × насыщенность (`0.6+0.8·clamp((rich−0.45)/1.5)`,
  диапазон [0.45,1.95]→фактор [0.6,1.4]). Обе компоненты нормированы к среднему ≈1.0 ⇒ общий тоннаж пояса не
  раздувается, а ПЕРЕРАСПРЕДЕЛЯется к металлу в богатых системах (богатая металлическая глыба до ×2.1, бедный
  углерод до ×0.48). КЛЮЧ (§2.3): множитель применён к УЖЕ разыгранному `frand` — НОЛЬ новых `lrng`, стрим-якорь
  §5.13.20 цел ⇒ спавн/soak ПОБИТОВО прежние. Лестница зелёная: `make` (0 предупр.), оба smoke (exit 0),
  **`make shot_asan` 21/21** (0 ASan/UBSan), **`make soak` 120000 кадров, invariantFails=0** — ВЕСЬ бейзлайн
  §5.13.21 цел ВКЛЮЧАЯ `rocks=648 shiny=382 trades=0` (ни один инвариант soak не читает `ore`; помощник
  `oreRichnessMult` чист, без RNG). Правки: `local.h`, `localgen.cpp` (+ `master_prompt.md`, `README.md`).
- ✅ **СДЕЛАНО (2026-07-28, автономный срез РЕНДЕРА, §5.13.23):** **HUD-читаемость запаса руды** — §5.13.22
  сделал тоннаж руды переменным (класс × насыщенность), но в кокпите это было НЕВИДИМО (только цвет класса).
  Две правки HUD в `localdraw.cpp`: (1) индикатор добычи `MINING <CLASS> +<accum>` → `MINING <CLASS>
  +<accum>  ORE <остаток>` (видно, как глыба истощается к «ROCK DEPLETED»); (2) подсказка `PRESS M TO MINE`
  → `PRESS M TO MINE - <CLASS>  ORE <остаток>` (игрок ВИДИТ класс+запас цели ДО бурения ⇒ выбирает богатую
  глыбу — прямой пейофф §5.13.22). Читаем существующие поля (`mr.ore`/`tr.ore`, `rockClassName(rockClass(...))`),
  янтарь `%.0F` (§2.6), добавлен баунд-чек `minePrompt < rocks.size()`. Чистый draw, ноль RNG, ноль правок
  симуляции/генерации. Лестница зелёная: `make` (0 предупр.), оба smoke (exit 0), **`make shot_asan` 21/21**
  (0 ASan/UBSan — баунд-чек держит), **`make soak` 120000 кадров, invariantFails=0** — ВЕСЬ бейзлайн §5.13.22
  цел (`rocks=648 shiny=382 trades=0`; soak headless ⇒ HUD не влияет), глазами `shot_mining_beam.png` —
  `MINING METAL  +42  ORE 80`. Правки: `localdraw.cpp` (+ `master_prompt.md`, `README.md`).
- ✅ **СДЕЛАНО (2026-07-28, автономный срез СИМУЛЯЦИИ, §5.13.24):** **награда за розыск (bounty) на пиратах** —
  после 4 подряд «добывающих» срезов (§5.13.16/.20/.22/.23) открыт НОВЫЙ слой: часть пиратов «в розыске» платят
  бонус при убийстве игроком. Развитие §5.13.11 (`CONVOY SAVED`) в самостоятельный стимул ОХОТИТЬСЯ, а не только
  оборонять конвои. Новые поля `LocalCraft.wanted`/`wantedBounty` ставятся при генерации (цикл пиратов `localgen.cpp`)
  ДЕТЕРМИНИРОВАННО из хэша `(starIndex,p)` — ноль `lrng` ⇒ стрим-якорь цел (та же техника §5.13.20/.22): ~38% пиратов
  в розыске, сумма 250..900 CR (шаг 25) по второму декоррелированному хэшу. В блоке убийства `(E)` (`localsim.cpp`):
  `money += c.wantedBounty` + `addResearch(1.5)` + тост `BOUNTY CLAIMED +N CR` + шейк — ПОВЕРХ базовой награды и
  `CONVOY SAVED`. §2.3/combat-инвариант: пиратов НЕ ослабляли (урон/ИИ/цель не тронуты, только доп. награда), деньги
  ограничены ⇒ finite. Лестница зелёная: `make` (0 предупр.), оба smoke (exit 0), **`make shot_asan` 21/21** (смена
  layout `LocalCraft` UB-чиста), **`make soak` 120000 кадров, invariantFails=0** — ВЕСЬ бейзлайн §5.13.23 ПОБИТОВО
  (`rocks=648 shiny=382 trades=0 radioClaims=3 craftDestroyed=2` — флаг не влияет ни на спавн, ни на счётчик убийств),
  probe: ~38% wanted, bounty [250,900], детерминизм ok. Правки: `local.h`, `localgen.cpp`, `localsim.cpp`
  (+ `master_prompt.md`, `README.md`).
- ✅ **СДЕЛАНО (2026-07-28, автономный срез РЕНДЕРА, §5.13.25):** **розыск в кокпите** — награда §5.13.24 стала
  видна пилоту. Три золотых сигнала (`255,205,60`, темп пульса `sin(fxClock·5.2)` ≠ швартовка 3.0/эскорт 4.5/SOS 6.0,
  чтобы близкий к янтарю розыск читался по ритму): (1) кольцо-розыск у борта пирата `wanted` (`sz+9/sz+14`, вне
  cyan-рамки цели и зелёного эскорта); (2) панель цели — золотая рамка + метка + чип `WANTED` (после `LOCK`) + строка
  `BOUNTY %.0F CR` под корпусом (высота `ph += 14`); (3) краевой маркер `nearWanted` ближайшего розыскного за кадром —
  точная параллель SOS/ESCORT §5.13.11/.12. Чистый draw-only (`localdraw.cpp`): ноль RNG, ноль правок симуляции/
  генерации; `wanted` ставится лишь пиратам ⇒ взаимоискл. с `sos`/`esc`, поэтому ветка `wnt` вставлена ПЕРЕД фолбэком
  `t.hostile?red` в тернаре цвета. Новый шот №18 `shot_wanted` (пират bounty 650, наведён) ⇒ харнес **22/22**. Лестница
  зелёная: `make` (0 предупр.), оба smoke (exit 0), **`make shot_asan` 22/22** (0 ASan/UBSan), **`make soak` 120000
  кадров, invariantFails=0** — ВЕСЬ бейзлайн §5.13.24 ПОБИТОВО (`rocks=648 shiny=382 trades=0 radioClaims=3
  craftDestroyed=2`; soak headless ⇒ HUD не влияет), глазами `shot_wanted.png` (золотые кольцо+панель+`BOUNTY 650 CR`),
  `shot_convoy_distress`/`_patrol_escort` без регресса. Правки: `localdraw.cpp`, `shot_test.cpp` (+ `master_prompt.md`,
  `README.md`).
- ✅ **СДЕЛАНО (2026-07-28, автономный срез СИМУЛЯЦИИ, §5.13.26):** **свидетельство пиратства пишется в макро** —
  закрыта единственная асимметрия §5.13.14: раньше write-back в постоянный мир срабатывал ТОЛЬКО на убийстве
  игроком, а смерть NPC-vs-NPC была для макро инертной (`localsim.cpp:662` — лишь `spawnWreck` «без награды/фракций»).
  Теперь на том же участке два строго аддитивных последствия: **(A)** жертва-зеркало (`agentIndex>=0`, `≠ playerAgent`)
  гибнет и в макро — тот же `downgradeAgentToEscapePod`, что и у игрока (кредиты/RNG не трогаем); **(G)** пират,
  убивший НЕ-пирата, попадает в розыск в реальном времени — `wanted=true`, `wantedBounty` 300 → +200 за каждую
  жертву до 1500 (переиспользуем поля §5.13.24, ноль RNG/новых полей). Розыск §5.13.24 стал ещё и ЭМЕРДЖЕНТНЫМ:
  увиденный вами налёт зажигает золотые подсказки §5.13.25 на пирате, и его можно выследить за bounty — замкнут цикл
  §5.13.11→.12→.24→.25→.26. Новый soak-пробник **`npcWriteBack=ok`** (свежий `Game`: пират добивает зеркало-агента →
  ассерт даунгрейда в макро), симметричный player-kill-пробнику §5.13.14. Лестница зелёная: `make` (0 предупр.), оба
  smoke (exit 0), **`make soak` 120000 кадров, invariantFails=0** — ВСЕ числовые счётчики §5.13.25 ПОБИТОВО
  (`craftDestroyed=2 rocks=648 shiny=382 trades=0 radioClaims=3`, ASan/UBSan чисты), +1 аддитивное поле
  `npcWriteBack=ok`; **22/22** шотов без регресса (render не тронут ⇒ `shot_asan` не требовался). Правки:
  `localsim.cpp`, `soak_test.cpp` (+ `master_prompt.md`, `README.md`).
- ✅ **СДЕЛАНО (2026-07-28, автономный срез РЕНДЕРА, §5.13.27):** **радар кодирует состояние контакта цветом** —
  цикл блипов радара (`localdraw.cpp:1979`) был ЕДИНСТВЕННОЙ панелью, слепой к угрозе/бедствию: красил блип лишь
  «враг=красный, иначе серый». Теперь цвет читает уже живые поля борта тем же языком, что и кабинные подсказки
  §5.13.11/.12/.25: `wanted`→золотой (`255,205,60`, пульс 5.2), `underAttack`→SOS-красный (пульс 6.0),
  `defending`→зелёный (`90,220,132`, пульс 4.5), прочий `hostile`→ровный красный, остальное→тусклый серый. Приоритет
  розыск>SOS>эскорт>враг>нейтраль (ветка `wanted` первой, т.к. розыскной пират ещё и `hostile`). Чистый draw-only:
  ноль новых полей, ноль RNG, ноль правок симуляции — радар (дальность 1400 LU) теперь показывает эмерджентный розыск
  §5.13.26(G) на весь диапазон, а не только у наведённой цели. Новый шот №19 `shot_radar_threat` (пять бортов в пяти
  состояниях → пять цветов блипов), цвета выверены попиксельно. Лестница зелёная: `make` (0 предупр.), оба smoke
  (exit 0), **`make shot_asan` 23/23** (0 ASan/UBSan; обязателен — тронут `localdraw.cpp`), **`make soak` 120000
  кадров, invariantFails=0** — ВЕСЬ бейзлайн §5.13.26 ПОБИТОВО (`craftDestroyed=2 rocks=648 shiny=382 trades=0
  radioClaims=3 npcWriteBack=ok`; soak headless ⇒ радар не рисуется), глазами `shot_radar_threat.png`
  (золотой/красный/зелёный/красный/серый), `shot_convoy_distress`/`_patrol_escort` перебазированы без регресса.
  Правки: `localdraw.cpp`, `shot_test.cpp` (+ `master_prompt.md`, `README.md`).
- ✅ **СДЕЛАНО (2026-07-28, автономный срез СИМУЛЯЦИИ, §5.13.28):** **закон охотится на розыскного** — патруль
  (`CK_PATROL`, `localsim.cpp`) получил ПРИОРИТЕТ-0: перед старым сканом налётчика (§5.13.12) теперь ищет
  ближайшего `wanted`-пирата в радиусе бедствия `PATROL_DISTRESS_R` (1400 LU) и БЕЗ условия «у беззащитного
  борта» — объявленный вне закона есть цель сам по себе, даже когда прямо сейчас не грабит; при находке
  цель=розыскной + `defending=true`, иначе прежняя ветка налётчик→фолбэк целиком (обёрнута в `else`). Читаем
  живое поле `o.wanted` — ноль RNG, ноль новых полей; огонь всё так же заперт `WEAPON_RANGE` (патруль сближается
  и бьёт в упор, не «снайпит» с 1400 LU). Так замкнут закон-цикл §5.13.24 (предген розыска) → §5.13.26(G)
  (розыск за убийство) → §5.13.27 (золотой блип радара) → **§5.13.28 (патруль летит на золотой контакт)**; на
  радаре зелёный (`defending`) патруль сходится с золотым (`wanted`) пиратом — два рендер-слайса и этот sim-слайс
  видимо складываются. Комбат-инвариант цел: меняем ТОЛЬКО выбор цели ПАТРУЛЯ, урон/агрессию пирата не трогаем.
  Ключ к бейзлайну: ветку `CK_PATROL` headless-soak НЕ исполняет (патрулей в замороженном кластере нет —
  подтверждено пробником-разведкой), поэтому 120k-счётчики ГАРАНТИРОВАННО побитово прежние. Лестница зелёная:
  `make` (0 предупр.), оба smoke (exit 0), **`make soak` 120000 кадров, invariantFails=0** — ВЕСЬ бейзлайн
  §5.13.27 ПОБИТОВО (`craftDestroyed=2 rocks=648 shiny=382 trades=0 radioClaims=3 npcWriteBack=ok`, ASan/UBSan
  чисты) + новый детерминированный пробник **`patrolHuntsWanted=ok`** (свежий `Game`, две подпроверки с
  пиньнутыми позициями: (A) розыскной-дальше побеждает простого-ближе → `wantedOverDistance=YES`; (B) без розыска
  бьётся ближайший → `fallbackNearest=YES`, фолбэк §5.13.12 цел). Шоты НЕ требовались (render не тронут; `saveShot`
  не шагает симуляцию ⇒ ИИ-правка на кадр не влияет). Правки:
  `localsim.cpp`, `local.h`, `soak_test.cpp` (+ `master_prompt.md`, `README.md`).
- ✅ **СДЕЛАНО (2026-07-28, автономный срез РЕНДЕРА, §5.13.29):** **вектор перехвата закона** — рендер визуализирует
  §5.13.28-погоню (`localdraw.cpp`). Лямбда `pursuitQuarry(ci)` заново выводит приоритет-0 скан §5.13.28 (ближайший
  живой `wanted`-пират в `PATROL_DISTRESS_R` от патруля) из живых полей ⇒ т.к. приоритет-0 перебивает всё, у
  `defending`-патруля цель ГАРАНТИРОВАННО этот розыскной — рендер совпадает с sim'ом по построению, без нового поля.
  Три cue: (1) 3D золотой «бегущий» пунктир + стрелка патруль→розыскной (вектор перехвата); (2) золотая связка блипов
  на радаре (кто за кем); (3) строка `IN PURSUIT <label>` в панели наведённого патруля. Читаются лишь
  `x/y/z/kind/wanted/defending/hullHP`; ноль RNG/новых полей/правок sim. `renderLocalScene` в соаке не зовётся ⇒
  `localdraw.cpp` лишь компилируется под ASan ⇒ 120k-бейзлайн §5.13.28 ПОБИТОВО цел. Лестница зелёная: `make`
  (0 предупр.), оба smoke (exit 0), **`make shot_asan` 24/24 (0 ASan/UBSan)**, **`make soak` 120000 кадров,
  invariantFails=0**, весь бейзлайн §5.13.28 побитово (`patrolHuntsWanted=ok`), **`make shots` 24/24** с новым
  `shot_law_pursuit.png` (прочитан глазами: золотой вектор + стрелка PATROL→PIRATE + панель `ESCORT`/`IN PURSUIT
  PIRATE` + связка блипов). Следующий (§5.13.30, СИМУЛЯЦИЯ) — не определён. Правки: `localdraw.cpp`, `shot_test.cpp`
  (+ `master_prompt.md`, `README.md`).
- ✅ **СДЕЛАНО (2026-07-29, автономный срез СИМУЛЯЦИИ, §5.13.30):** **защита стаи — бэкап загнанному изгою**
  (`localsim.cpp`, хвост ветки `CK_PIRATE`). ПРОСТАИВАЮЩИЙ пират (`atkCraft<0 && !atkPlayer`, 0 урона), «слышащий»
  рядом закон — патруль в `PATROL_DISTRESS_R`, у которого есть СВОЙ розыскной пират в том же радиусе (условие охоты
  §5.13.28, ВЫВЕДЕННОЕ заново из живых полей `kind/wanted/x/y/z`, как рендер §5.13.29, а НЕ чтением кадрового
  `defending` ⇒ независимо от порядка обработки бортов/своп-попа) — берёт этот патруль ЦЕЛЬЮ: стая обороняет
  объявленного вне закона. СТРОГО АДДИТИВНО, комбат-инвариант УСИЛЕН (не ослаблен): гейт `atkCraft<0` НИКОГДА не уводит
  пирата от игрока/торговца, лишь простой пират становится атакующим ⇒ больше агрессии, направленной на закон (охота за
  головой ТРУДНЕЕ). Патрулей в headless-мире нет ⇒ в соаке блок no-op ПО ПОСТРОЕНИЮ; ноль RNG, без нового поля (реюз
  `PATROL_DISTRESS_R`). Лестница зелёная: `make` (0 предупр.), оба smoke (exit 0), **`make soak` 120000 кадров,
  invariantFails=0**, весь бейзлайн §5.13.28/29 ПОБИТОВО (`packRalliesOutlaw=ok` — новый пробник: rally срабатывает И
  заперт гейтом). Шоты не требовались (`localdraw.cpp` не тронут, прецедент §5.13.28). Следующий (§5.13.31, РЕНДЕР) — не
  определён. Правки: `localsim.cpp`, `soak_test.cpp` (+ `master_prompt.md`, `README.md`).
- ✅ **СДЕЛАНО (2026-07-29, автономный срез РЕНДЕРА, §5.13.31):** **рендер набата стаи** — draw-only спутник
  §5.13.30 (`localdraw.cpp`, `shot_test.cpp`). Лямбда `rallyTarget` ПЕРЕВЫВОДИТ решение §5.13.30 из живых полей
  (`kind/wanted/aiState/x/y/z` + тот же idle-гейт «никого в 750 LU» и скан «патруль-охотник в `PATROL_DISTRESS_R`»),
  как `pursuitQuarry` §5.13.29 — ноль новых полей/RNG/правок sim. Три cue: (1) горячо-оранжевый «бегущий» пунктир
  + стрелка пират→патруль (вектор ТАРАНА), быстрее/жарче золота §5.13.29 (пульс 7.0/марш 46 vs 5.2/34) — агрессия,
  не охота, рисуется ПОВЕРХ золота на пересечении; (2) оранжевая связка блипов на радаре; (3) строка `UNDER PACK Xn`
  в панели наведённого патруля — прямо под золотой `IN PURSUIT`, так что панель загнанного-охотника показывает ВЕСЬ
  закон-цикл: `ESCORT` + `IN PURSUIT OUTLAW` + `UNDER PACK X1`. `renderLocalScene` в соаке не зовётся ⇒ `localdraw.cpp`
  лишь компилируется под ASan ⇒ 120k-бейзлайн §5.13.28/30 ПОБИТОВО цел. Лестница зелёная: `make` (0 предупр.), оба smoke
  (exit 0), **`make shot_asan` 25/25 (0 ASan/UBSan)**, **`make soak` 120000 кадров, invariantFails=0**, весь бейзлайн
  §5.13.28/30 побитово (`packRalliesOutlaw=ok`), **`make shots` 25/25** с новым `shot_pack_rally.png` (прочитан глазами:
  оранжевый вектор RAIDER→PATROL через кадр + золотой PATROL→OUTLAW + панель `ESCORT`/`IN PURSUIT OUTLAW`/`UNDER PACK X1`
  + связки на радаре). Следующий (§5.13.32, СИМУЛЯЦИЯ) — не определён. Правки: `localdraw.cpp`, `shot_test.cpp`
  (+ `master_prompt.md`, `README.md`).
- ✅ **СДЕЛАНО (2026-07-29, автономный срез СИМУЛЯЦИИ, §5.13.32):** **подмога закона — патруль спешит к загнанному
  охотнику** (`localsim.cpp`, ветка `CK_PATROL`, приоритет-0.5 МЕЖДУ §5.13.28-охотой и §5.13.12-сканом налётчика).
  Ответный beat к §5.13.30: свободный патруль (СВОЕГО розыскного §5.13.28 не нашёл) сканирует ближайшего живого ПИРАТА в
  своём `PATROL_DISTRESS_R`, который сам сидит в `PATROL_DISTRESS_R` ДРУГОГО патруля-ОХОТНИКА `q` (у `q` живой `wanted`-
  пират в радиусе) — и берёт этого пирата целью (`defending=true`) ⇒ весь наряд сходится на свалку, где изгойская стая
  (§5.13.30) облепила одинокого охотника. «`q` охотится» ВЫВЕДЕНО заново из живых `kind/wanted/x/y/z` (приём D, как
  `pursuitQuarry` §5.13.29 / rally §5.13.30), НЕ из кадрового `q.defending` ⇒ независимо от порядка бортов/своп-попа при
  гибели. СТРОГО АДДИТИВНО, комбат-инвариант УСИЛЕН: правило живёт ЦЕЛИКОМ в выборе цели ПАТРУЛЯ, не читает/пишет НИЧЕГО
  у пирата, цель всегда `CK_PIRATE` (без friendly-fire) ⇒ закон агрессивнее, охота за головой ТРУДНЕЕ. Ноль RNG, без
  нового поля (реюз `PATROL_DISTRESS_R` + маркер `defending`). Тир срабатывает лишь при ≥2 патрулях — в соаке патрулей 0,
  в обоих прежних пробниках ровно 1 ⇒ no-op ПО ПОСТРОЕНИЮ, 120k-бейзлайн ПОБИТОВО цел. Лестница зелёная: `make` (0
  предупр.), оба smoke (exit 0), **`make soak` 120000 кадров, invariantFails=0**, весь бейзлайн §5.13.30/31 ПОБИТОВО
  (`patrolBacksUpSwarmed=ok` — новый изолированный пробник: подмога срабатывает И заперта гейтом «нет живого охотника»).
  Шоты не требовались (`localdraw.cpp` не тронут, прецедент §5.13.28/30). Следующий (§5.13.33, РЕНДЕР) — не определён.
  Правки: `localsim.cpp`, `soak_test.cpp` (+ `master_prompt.md`, `README.md`).
- ✅ **СДЕЛАНО (2026-07-29, автономный срез РЕНДЕРА, §5.13.33):** **вектор подмоги закона — рисуем ответ §5.13.32**
  (`localdraw.cpp`, draw-only спутник §5.13.32). Лямбда `backupTarget(ci)` (близнец `rallyTarget` §5.13.31 / `pursuitQuarry`
  §5.13.29) ПЕРЕВЫВОДИТ подмогу из живых полей: свободный патруль (P0-скан `wanted` пуст ⇒ сам не охотник; НЕ гейтим на
  `defending` — патруль бывает defending и ради рейд-обороны §5.13.12) → ближайший пират в `PATROL_DISTRESS_R`, увязший у
  ДРУГОГО патруля-охотника; возвращает индекс ПИРАТА (как sim `atkCraft=backupPir`). Даёт: (7г) сине-белый вектор `R→pack`
  в 3D (пульс 4.2 / марш 28 — холоднее и размереннее оранжа 7.0/46 и золота 5.2/34: дисциплина, не ярость), синюю связку на
  радаре и строку `BACKUP INBOUND Xn` в панели ЗАГНАННОГО охотника (счёт по предикату «пират-цель подмоги в
  `PATROL_DISTRESS_R` от нашего охотника», НЕ равенством — backupTarget возвращает пирата, а не охотника). Чистый рендер:
  ноль полей/RNG/шага sim. Лестница РЕНДЕРА зелёная: `make` (0 предупр.), оба smoke (exit 0), **`make shot_asan` 26/26 без
  ASan/UBSan** (исполнил новый код), **`make soak` ПОБИТОВО бейзлайн §5.13.32** (`patrolBacksUpSwarmed=ok`; правок sim нет),
  **`make shots` 26/26** с новым `shot_law_backup.png` (прочитан глазами: синий вектор `R→pack` + оранж `pack→H` + золото
  `H→outlaw` в одном кадре; панель охотника `ESCORT`/`IN PURSUIT OUTLAW`/`UNDER PACK X1`/`BACKUP INBOUND X1` — весь
  закон-цикл гон→набат→подмога). Следующий (§5.13.34, СИМУЛЯЦИЯ) — не определён. Правки: `localdraw.cpp`, `shot_test.cpp`
  (+ `master_prompt.md`, `README.md`).
- ✅ **СДЕЛАНО (2026-07-29, автономный срез СИМУЛЯЦИИ, §5.13.34):** **облава — закон гонится за одиозной дичью быстрее**
  (`localsim.cpp` + `LocalCfg`). Патруль, реально ведущий P0-охоту (§5.13.28) на ЖИВОГО `wanted`-пирата, получает прибавку
  к предельной скорости, растущую с наградой за его голову (250→1500 CR ⇒ ×1.0→×1.5 при `MANHUNT_SPEED_GAIN=0.5`). Награда
  §5.13.24, прежде дремавшая суммой, впервые значит МЕХАНИЧЕСКИ: чем громче слава, тем неотвратимее закон. Файл-статик
  `manhuntSpeedMult(scene,ci)` ПЕРЕВЫВОДИТ P0-скан дословно (ближайший живой `wanted`-`CK_PIRATE` в `PATROL_DISTRESS_R`,
  приём D — цель совпадает с sim по построению, ибо P0 вытесняет младшие тиры), гейтится `CK_PATROL && defending` (player-
  override гасит `defending` ⇒ отсечён; бегущий/низкокорпусный патруль `aiState≠1` ⇒ тоже), множится в `effMax` кадрового
  движения. СТРОГО АДДИТИВНО, комбат-инвариант УСИЛЕН: закон только БЫСТРЕЕ на крупной дичи, никогда медленнее (награда у
  пола ⇒ множитель 1.0 = прежнее поведение, нулевой регресс); скорость/агрессия ПИРАТА не тронуты. Ноль RNG, без нового
  поля у борта (лишь тюнинг-константа). Ветка `CK_PATROL` в соаке не исполняется (патрулей 0) ⇒ no-op ПО ПОСТРОЕНИЮ,
  120k-бейзлайн ПОБИТОВО цел. Лестница СИМ зелёная: `make` (0 предупр.), оба smoke (exit 0), **`make soak` 120000 кадров,
  invariantFails=0**, весь бейзлайн §5.13.33 ПОБИТОВО (`manhuntScalesWithBounty=ok` — новый пробник: награда 1500 ⇒
  терминальная скорость 60→90, награда 250 ⇒ ровно 60 = нулевой регресс, high строго > low). Шоты не требовались
  (`localdraw.cpp` не тронут: правится СКОРОСТЬ, не выбор цели ⇒ рендер-лямбды §5.13.29/31/33 верны даром; прецедент
  §5.13.28/30/32). Следующий (§5.13.35, РЕНДЕР) — жар облавы (ниже). Правки: `local.h`, `localsim.cpp`, `soak_test.cpp`
  (+ `master_prompt.md`, `README.md`).
- ✅ **СДЕЛАНО (2026-07-29, автономный срез РЕНДЕРА, §5.13.35):** **жар облавы — золотой вектор погони калится с наградой
  дичи** (`localdraw.cpp` + новый шот). Draw-only спутник §5.13.34: та самая «интенсивность облавы», что в sim КАЛИТ
  СКОРОСТЬ патруля (`manhuntSpeedMult`), теперь ЧИТАЕТСЯ ИЗ КОКПИТА. Золотой вектор перехвата §5.13.29 (патруль→розыскной)
  и строки его панели тем горячее, чем крупнее награда за голову дичи. В блоке «(7б)» перед выбором цвета введён
  `heat = clamp((qc.wantedBounty−250)/(1500−250), 0, 1)` — ПОБИТОВО та же нормировка, что sim-`manhuntSpeedMult`; `qc` —
  дичь, чей индекс уже свёл `pursuitQuarry(i)` (приём D ⇒ ровно тот пират, чья награда движет облавой). Три согласованные
  модуляции вида `base + heat·delta`: тон `rgba(255, 205+heat·50, 60+heat·195, …)` золото→бело-калёный, базовая альфа
  ярче (+50 при потолке), марш штрихов `fxClock·(34+heat·17)` до ×1.5 (34→51 — зеркало прибавки скорости §5.13.34). При
  `heat=0` (награда у пола 250) все три РОВНО прежние ⇒ нулевой визуальный регресс (`int(0·x)==0`). Панель наведённого
  патруля: строка `IN PURSUIT <label>` перекрашена тем же heat-тоном + ПОД ней новая строка `MANHUNT %.0F CR` (награда
  ГЛАЗАМИ ЗАКОНА — не нужно перенаводиться на пирата), резерв высоты `ph += 14 → += 28`. Чистый draw-only: ноль правок
  sim/полей/RNG (§2.3), `wantedBounty` читается СЫРЫМ в draw-лерп (не CHROMOCORE-ребейк §2.4), HUD-число `%.0F` uppercase
  (§2.6). Лестница РЕНДЕРА зелёная: `make` (0 предупр.), оба smoke (exit 0), **`make shot_asan` 27/27 без ASan/UBSan**
  (исполнил новый код), **`make soak` ПОБИТОВО бейзлайн §5.13.34** (`renderLocalScene` в соаке не зовётся ⇒ no-op по
  построению), **`make shots` 27/27** с новым `shot_manhunt.png` (награда 1500 CR ⇒ heat=1) — прочитан глазами в паре с
  `shot_law_pursuit.png` (650 CR): 1500 калится добела, 650 спокойное золото ⇒ кодировка heat подтверждена. Следующий
  (§5.13.36, СИМУЛЯЦИЯ) — благодарность закона (ниже). Правки: `localdraw.cpp`, `shot_test.cpp` (+ `master_prompt.md`, `README.md`).
- ✅ **СДЕЛАНО (2026-07-29, автономный срез СИМУЛЯЦИИ, §5.13.36):** **благодарность закона — совместное добивание
  розыскного даёт +реп с фракцией патруля-охотника** (`localsim.cpp` + пробник `lawGratitude`). Замыкает §5.13.24 (денежная
  выплата за розыскного) по оси РЕПУТАЦИИ — зеркало §5.13.11 `CONVOY SAVED`, но для ЗАКОНА: если игрок добивает
  `wanted`-пирата, которого ГНАЛ патруль (P0-охота §5.13.28), фракция патруля даёт `+6` rep ПОВЕРХ выплаты. «Патруль гнал
  ИМЕННО этого пирата» выведено приёмом D через ИНВЕРТИРОВАННЫЙ P0-скан: убитый борт `c` уже `hullHP≤0` (удаление мёртвых
  отложено до блока L), потому прямой P0-скан (пропускает `hullHP≤0`) неприменим — вместо этого ищем ближайший
  обороняющийся (`defending`) `CK_PATROL` в `PATROL_DISTRESS_R`, для которого `c` был ближайшей ЖИВОЙ розыскной дичью (ни
  один другой живой `wanted`-пират не ближе к патрулю) ⇒ ПО ПОСТРОЕНИЮ = «патруль P0-охотился на c». Строго АДДИТИВНО: блок
  в ветке начисления выплаты `(E)` — ПОСЛЕ смерти пирата, трогает лишь `adjustFactionRelation` + новость/тост
  `LAW GRATEFUL +REP`/тряску, ноль правок урона/агрессии/аптайма пирата ⇒ комбат-инвариант §0.2-G цел. No-op в соаке ПО
  ПОСТРОЕНИЮ: headless-соак не спавнит `CK_PATROL` ⇒ `gratPatrol` всегда −1 ⇒ `adjustFactionRelation` из нового блока не
  зовётся ⇒ `factionRelations`+все счётчики ПОБИТОВО как в §5.13.35; покрытие даёт ручной патруль в пробнике `lawGratitude`
  (сцена A: патруль в радиусе ⇒ `+6`; сцена B: патруль за 2000 LU ⇒ убийство есть, реп не двинут). Лестница СИМ зелёная:
  `make` (0 предупр.), оба smoke (exit 0), **`make soak` ПОБИТОВО бейзлайн §5.13.35** + новый `lawGratitude=ok`. Шотов
  по-прежнему **27** (СИМ-срез кадров не добавляет). Следующий (§5.13.37, РЕНДЕР) — кокпит-подсказка благодарности закона.
  Правки: `localsim.cpp`, `soak_test.cpp` (+ `master_prompt.md`, `README.md`).
- ✅ **СДЕЛАНО (2026-07-29, автономный срез РЕНДЕРА, §5.13.37):** **рендер стояния — панель цели несёт строку
  `STANDING <WORD> ±N`, репутацию игрока с фракцией цели** (`localdraw.cpp` + новый шот). РЕНДЕР-спутник §5.13.36: +6-эхо
  благодарности — событие ТРАНЗИЕНТНОЕ (тост стрельнул раз, добитый пират снят в конце кадра ⇒ покадрового состояния для
  ре-деривации у него НЕТ), а его СТОЙКОЕ ПОСЛЕДСТВИЕ — сама релейшн; этот срез выводит её в кокпит. Для наведённой цели с
  валидной ЧУЖОЙ фракцией (`t.faction >= 0 && game.playerFaction >= 0 && t.faction != game.playerFaction`) под баром корпуса (и
  под опц. строками BOUNTY/IN PURSUIT/MANHUNT/UNDER PACK/BACKUP INBOUND) добавлена строка `STANDING <WORD> ±N`: читает
  `game.factionRelation(playerFaction, t.faction)` НА ЛЁТУ и мапит int в слово+цвет ОБЩИМ классификатором
  `classifyFactionRelationTension` (в `faction.cpp` — единый источник порогов, тот же, что макрослой: Enemy≤−96, Hostile≤−48,
  Tense<−16, Neutral≤16, Friendly<80, Ally≥80). Цвет по тирам: Enemy/Hostile красный (238,88,82 / 238,120,82), Tense янтарь
  (245,191,78), Neutral тускло-серый `P.dim`, Friendly зелёный (90,220,132), Ally ярко-зелёный (120,240,150); резерв высоты
  `ph += 14` (как у прочих опц. строк). Так игрок ВИДИТ накопленный итог со-оп добиваний §5.13.36 + `CONVOY SAVED` §5.13.11 +
  расправ прямо в панели: число ползёт вверх зелёным (реп растёт) или вниз красным (фракция во вражде). Чистый draw-only: ноль
  новых полей/RNG (§2.3), НЕ CHROMOCORE-ребейк (§2.4 — читается релейшн-int в момент рисовки, не запечённое поле корабля),
  `%s`+`%+d` uppercase-safe (§2.6 — все нужные глифы есть: буквы, цифры, «+», «-», пробел). Пираты БЕСФРАКЦИОННЫ
  (`faction = −1`) ⇒ строка им НИКОГДА не видна ⇒ комбат-фильтр §0.2-G тривиально цел (draw-only чтение релейшн; урона/агрессии/
  аптайма пирата не касается — строго аддитивно). Лестница РЕНДЕРА зелёная: `make` (0 предупр.), оба smoke (exit 0),
  **`make shot_asan` 28/28 без ASan/UBSan**, **`make soak` ПОБИТОВО бейзлайн §5.13.36** (no-op ПО ПОСТРОЕНИЮ —
  `renderLocalScene` в соаке не зовётся), **`make shots` 28/28** с новым `shot_law_standing.png` (одинокий дружественный
  патруль фракции 0, реп выставлен в +96 ⇒ `STANDING ALLY +96` ярко-зелёным — прочитан ГЛАЗАМИ) — а четыре прежних law-шота
  (`shot_law_pursuit`/`shot_pack_rally`/`shot_law_backup`/`shot_manhunt`, все на патрулях фракции 1) теперь ТОЖЕ несут
  `STANDING NEUTRAL +0` серым (реп игрок↔NPC = 0 на старте worldgen; подтверждено глазами в `shot_manhunt.png`: полная панель
  ESCORT + IN PURSUIT PIRATE + MANHUNT 1500 CR + STANDING NEUTRAL +0). Шотов теперь **28**. Следующий (§5.13.38, СИМУЛЯЦИЯ) —
  новое sim-правило (см. форвард-бэклог §4.3). Правки: `localdraw.cpp`, `shot_test.cpp` (+ `master_prompt.md`, `README.md`).
- ✅ **СДЕЛАНО (2026-07-29, автономный срез СИМУЛЯЦИИ, §5.13.38):** **коп-килл — розыскной пират, ДОБИВШИЙ патруль-ОХОТНИКА,
  получает +`COP_KILL_BOUNTY` (500 CR) СВЕРХ обычного +200 за смерть не-пирата (§5.13.26G)** (`local.h` + `localsim.cpp` +
  `soak_test.cpp`). СИМ-спутник §5.13.37; чередование §5.13.36 sim → .37 render → **.38 sim**. Закрывает петлю закон-цикла: патруль
  гонит розыскного (§5.13.28), облава калится с наградой (§5.13.34/35), закон добил — фракция благодарит (§5.13.36); но когда изгой
  ВЫИГРЫВАЛ ту дуэль — ничего. Теперь: за убой копа-охотника награда пирата +700 (против +200 за торговца) ⇒ следующая облава ГОРЯЧЕЕ
  (быстрее §5.13.34, белее §5.13.35), не холоднее — цена убийства копа есть бо́льшая цена на голове самого убийцы. В блоке смерти
  NPC-vs-NPC (`localsim.cpp`): (1) `cWasWantedBefore = c.wanted` захвачен ДО §5.13.26G-бампа (тот ставит `c.wanted=true`) ⇒ чистый
  пират-первопреступник, ставший `wanted` этим же убоем, ОТСЕЧЁН (патруль не мог гнать не-розыскного); (2) если `wanted`-пират `c`
  добил `CK_PATROL` `o`, ПЕРЕВЫВОДИМ, был ли `c` его P0-дичью — ПРЯМОЙ скан (зеркало `manhuntSpeedMult` §5.13.34, приём D): `c` —
  ближайший ЖИВОЙ `wanted`-пират к павшему патрулю в `PATROL_DISTRESS_R` (нет иного `wanted` строго ближе) ⇒ цель §5.13.28-охоты ПО
  ПОСТРОЕНИЮ; при да — `wantedBounty += 500` (кламп 1500). Рядового пирата таран-стаи §5.13.30 валит УЖЕ геометрия (не ближайший
  `wanted`) ⇒ лишь обычные +200. Намеренно БЕЗ гейта `o.defending` (в отличие §5.13.36): `c` — P0-дичь ПО ГЕОМЕТРИИ, а гейт ложно
  потерял бы законный «загнал патруль до бегства/оверрайда, затем добил» (награда лишь растёт ⇒ инвариант цел при любом состоянии
  патруля). Комбат-инвариант §0.2-G УСИЛЕН (награда только ↑ ⇒ облава горячее ⇒ больше боя; урон/агрессия/аптайм пирата не тронуты);
  ноль RNG (§2.3), не CHROMOCORE-ребейк (§2.4 — пишет геймплейный `wantedBounty`, не запечённое поле), без новых полей у борта
  (1 константа + 1 локальный bool). No-op в соаке ПО ПОСТРОЕНИЮ: headless-соак не спавнит `CK_PATROL` ⇒ гейт `o.kind==CK_PATROL` не
  срабатывает ⇒ 120k-бейзлайн ПОБИТОВО цел; покрытие — двусторонний пробник `copKillRaisesBounty` (сцена A: убийца — единственный
  `wanted` ⇒ дичь ⇒ 650+200+500=1350; сцена B: ближе патруля стоит другой `wanted`-приманка ⇒ убийца не дичь ⇒ лишь 650+200=850;
  застрявшее «вкл» валит B, «выкл» валит A). Лестница СИМ зелёная: `make` (0 предупр.), оба smoke (exit 0), **`make soak` ПОБИТОВО
  бейзлайн §5.13.36** (`radioClaims=3 craftDestroyed=2 rocks=648 shiny=382 trades=0`) + новый `copKillRaisesBounty=ok`, ноль
  ASan/UBSan. Шотов по-прежнему **28** (СИМ-срез кадров не добавляет). Ревью SHIP (все 10 проверок). Следующий (§5.13.39, РЕНДЕР) —
  подсветка курсового пипа/рамки цели по `STANDING`-тиру (реюз классификатора §5.13.37). Правки: `localdraw.cpp`, `shot_test.cpp`
  (+ `master_prompt.md`, `README.md`).
- ✅ **СДЕЛАНО (2026-07-29, автономный срез РЕНДЕРА, §5.13.39):** **прицел (рамка цели в 3D-мире + курсовой лид-пип упреждения)
  красится по `STANDING`-тиру локнутой цели** (`localdraw.cpp` + `shot_test.cpp`). РЕНДЕР-спутник §5.13.38; чередование §5.13.37
  render → .38 sim → **.39 render**. Реюз классификатора §5.13.37 `classifyFactionRelationTension`: стояние читается прямо с
  прицела, без чтения строки панели. Новый файловый хелпер `standingCue(const Game&, int targetFaction, SDL_Color&)` возвращает
  `true` + цвет тира ТОЛЬКО для НЕ-нейтрального стояния с ДРУГОЙ фракцией (Enemy/Hostile/Tense/Friendly/Ally); иначе `false` ⇒
  вызывающий держит дефолт (циан-рамка / янтарь-пип). Применён в двух местах: рамка цели (была `P.cyan`) и лид-пип (был
  `tc.hostile ? амбер-красный : янтарь` — теперь стояние важнее сырого `hostile`, альфа сохранена 205/185). **Пираты
  бесфракционны (`faction < 0`) ⇒ `standingCue` возвращает `false` на первой строке ⇒ их прицел ПОБИТОВО как раньше** ⇒ §0.2-G
  цел (draw-only, ноль полей/RNG/ребейка; «уменьшает урон/агрессию/аптайм пирата?» — НЕТ). `factionRelation` двойне защищён
  (`validFaction` + плоский индекс-баунд ⇒ ноль OOB для любого `faction` борта; безопаснее уже отгруженного вызова §5.13.37).
  Намеренно НЕ рефакторил switch-палитру панели §5.13.37 (не трогаю отгруженный срез; 5 литералов тира продублированы в
  `standingCue`, отмечено в комментарии как зеркало палитры панели — косметика). Лестница РЕНДЕРА зелёная: `make` (0 предупр.),
  оба smoke (exit 0), `make soak` ПОБИТОВО бейзлайн §5.13.36 (`radioClaims=3 craftDestroyed=2 rocks=648 shiny=382 trades=0`, все
  пробники ok вкл. `copKillRaisesBounty=ok`, `invariantFails=0`, ноль ASan/UBSan — `renderLocalScene` в соаке не зовётся ⇒
  SIM-бейзлайн нетронут), **`make shot_asan` 29/29 ноль ASan/UBSan**, `make shots` 29/29. Новый шот `shot_standing_reticle`
  (case 25): вражефракционный ТОРГОВЕЦ `hostile=false`, rep −100 (Enemy) ⇒ КРАСНЫЙ прицел ИЗ СТОЯНИЯ (не из hostile) — изолирует
  причину; пара к `shot_law_standing` (тот же тир ALLY +96 ⇒ зелёный). Визуальный плейтест подтвердил враг=красный /
  союзник=зелёный / нейтрал=циан. Ревью SHIP (все 10 проверок, 3 косметические заметки). Шотов теперь **29**. Следующий
  (§5.13.40, СИМУЛЯЦИЯ) — новое sim-правило (см. форвард-бэклог §4.3). Правки: `local.h`/`localsim.cpp`/`soak_test.cpp`
  (+ `master_prompt.md`, `README.md`).
- ✅ **СДЕЛАНО (2026-07-29, автономный срез СИМУЛЯЦИИ, §5.13.40):** **«месть фракции» — ЗНАК репутационного итога co-op-добивания
  зависит от того, где игрок УЖЕ стоит с фракцией патруля** (`local.h` + `localsim.cpp` + `soak_test.cpp`). СИМ-спутник §5.13.39;
  чередование §5.13.37 render → .38 sim → .39 render → **.40 sim**. §5.13.36 всегда давал +6 за добивание розыскного, которого гнал
  патруль (§5.13.28-погоня); теперь ЗНАК берётся из ТОГО ЖЕ классификатора порогов `classifyFactionRelationTension`, что и панель
  §5.13.37 и прицел §5.13.39: тир **Enemy/Hostile** (реп ≤ −48) ⇒ отстрел «их» дичи в их операции читается как вооружённое
  вмешательство ⇒ **−6** («FACTION REPRISAL -REP», news kind 3); **Tense/Neutral/Friendly/Ally** ⇒ помощь облаве ⇒ **+6**
  (прежнее «LAW GRATEFUL +REP», §5.13.36). Величина вынесена в `LocalCfg::LAW_GRATITUDE_REP = 6` (единый источник |Δ|, заменил
  литерал `6`); тот же инверт-P0-скан `gratPatrol` §5.13.36 без изменений. **Строго ADDITIVE**: двигаем лишь реп-число (пират уже
  мёртв — его урон/агрессию/uptime не читаем), §0.2-G цел («уменьшает урон/агрессию/аптайм пирата?» — НЕТ; −REP если и влияет, то в
  сторону БОЛЬШЕЙ будущей враждебности фракции). Ноль RNG (§2.3), не CHROMOCORE-ребейк (§2.4 — пишет геймплейный `factionRelation`),
  без новых полей у борта (1 константа + локальные). `factionRelation` двойне-защищён (`validFaction` + индекс-баунд ⇒ 0 для любого
  `patFac` ⇒ Neutral ⇒ +6, ноль OOB); диагональ (`patFac==playerFaction`) закреплена в 128=Ally, а `adjustFactionRelation` гейтит
  `A==B` ⇒ no-op + «LAW GRATEFUL» — ПОБИТОВО §5.13.36 для патруля своей фракции. **No-op в соаке ПО ПОСТРОЕНИЮ**: headless-соак не
  спавнит `CK_PATROL` ⇒ блок не срабатывает ⇒ 120k-бейзлайн ПОБИТОВО цел; покрытие — трёхсторонний пробник `factionReprisal` (C: реп
  −60/Hostile ⇒ −6; D: +40/Friendly ⇒ +6; **E: −30/Tense ⇒ +6** — пиньнит ТИР-семантику, наивный «rep<0 ⇒ −6» валит E; застрявший
  «+6» валит C, «−6» валит D). Пробник §5.13.36 `lawGratitude` стартует с реп 0 = Neutral ⇒ +6 ⇒ остаётся зелёным без правок. Лестница
  СИМ зелёная: `make` (0 предупр.), оба smoke (exit 0), **`make soak` ПОБИТОВО бейзлайн §5.13.39** (`radioClaims=3 craftDestroyed=2
  rocks=648 shiny=382 trades=0`) + новый `factionReprisal=ok` (C/D/E все ok) + прежний `lawGratitude=ok`, `invariantFails=0`, ноль
  ASan/UBSan. Шотов по-прежнему **29** (СИМ-срез кадров не добавляет). Ревью SHIP (A–H + 5 хард-правил; заметка про Tense-покрытие
  закрыта третьим сидом E ещё до коммита). Следующий (§5.13.41, РЕНДЕР) — визуализация «мести»/стояния патруля в кокпите (см.
  форвард-бэклог §4.3). Правки: `local.h`/`localsim.cpp`/`soak_test.cpp` (+ `master_prompt.md`, `README.md`).
- ✅ **СДЕЛАНО (2026-07-29, автономный срез РЕНДЕРА, §5.13.41):** **золотой ВЕКТОР перехвата закона (§5.13.29) теперь
  красится по СТОЯНИЮ игрока с фракцией патруля-охотника** (`localdraw.cpp` + `shot_test.cpp`) — предиктивный спутник
  §5.13.40; чередование §5.13.38 render → .39 render → .40 sim → **.41 render**. Тот же вектор (маршевый пунктир патруль→
  розыскная жертва в 3D + связка на радаре), что и §5.13.29, теперь берёт HUE у ТОГО ЖЕ классификатора `standingCue`
  (§5.13.39), что и панель `STANDING` §5.13.37 и рамка/пип прицела §5.13.39 — по стоянию с **фракцией патруля** (`pc.faction`):
  тир **Enemy/Hostile** ⇒ **красный** вектор (грядёт `−rep` мести §5.13.40), **Friendly/Ally** ⇒ **зелёный** (грядёт `+rep`
  благодарности), **Tense** ⇒ янтарь (пока +rep, но на грани), **Neutral/бесфракц.** ⇒ `standingCue=false` ⇒ **РОВНО прежнее
  золото** (нулевой визуальный регресс). Так знак §5.13.40 читается ещё ДО выстрела, прямо на приглашающей геометрии; три
  индикатора (панель/прицел/вектор) согласованы ПО ПОСТРОЕНИЮ (единый классификатор). Меняется лишь **HUE** — жар/пульс/марш/
  альфа (§5.13.35 «горячая облава») сохранены (RGB подменяем, `gold.a` держим). **Строго ADDITIVE**: чистый рендер-вывод,
  только чтение репутации — §0.2-G тривиально цел («уменьшает урон/агрессию/аптайм пирата?» — НЕТ). Ноль RNG (§2.3), без новых
  полей, не CHROMOCORE-ребейк (§2.4); `factionRelation` двойне-защищён ⇒ любой `patFac` безопасен (OOB ⇒ 0 ⇒ Neutral ⇒ золото).
  Лестница РЕНДЕРА зелёная: `make` (0 предупр.), оба smoke (exit 0), **`make soak` ПОБИТОВО бейзлайн §5.13.40** (`localdraw.cpp`
  не в headless-пути ⇒ `factionReprisal=ok`, `invariantFails=0`, ноль ASan/UBSan), **`make shot_asan` 31/31 ноль ASan/UBSan**,
  `make shots` 31/31. **+2 шота** (не обычный +1): `shot_reprisal_vector` (Enemy −100 ⇒ красный) + `shot_gratitude_vector`
  (Ally +96 ⇒ зелёный) — клон геометрии/награды `shot_law_pursuit` (Neutral ⇒ золото-контроль), меняется ТОЛЬКО стояние ⇒ цвет
  ведёт репутация; счётчик 29 → **31**. Глазами (sips→PNG): триада золото/красный/зелёный подтверждена, вектор тянется поперёк
  экрана с остриём в жертву. Ревью SHIP. Следующий (§5.13.42, СИМУЛЯЦИЯ) — новое sim-правило (см. форвард-бэклог §4.3). Правки:
  `localdraw.cpp`/`shot_test.cpp` (+ `master_prompt.md`, `README.md`).
- ✅ **СДЕЛАНО (2026-07-29, автономный срез СИМУЛЯЦИИ, §5.13.42):** **закон, который ты ПЕРЕШЁЛ, охотится на тебя** —
  патруль берёт игрока целью БЕЗ свежей провокации, если игрок стоит с его фракцией на тире **Enemy/Hostile** (реп ≤ −48)
  (`localsim.cpp` + `soak_test.cpp`); чередование §5.13.39 render → .40 sim → .41 render → **.42 sim**. Даёт «мести фракции»
  §5.13.40 ЗУБЫ: одна строка в ветке `CK_PATROL` расширяет СУЩЕСТВУЮЩИЙ гейт предпочтения игрока `c.hostile` →
  `(c.hostile || factionHuntsPlayer)`, где `factionHuntsPlayer` = тир Enemy/Hostile из ТОГО ЖЕ классификатора
  `classifyFactionRelationTension`, что панель §5.13.37 / прицел §5.13.39 / вектор §5.13.41 ⇒ читалка и поведение согласованы
  ПО ПОСТРОЕНИЮ. Реюз гейта `pd2 < atkBest`: патруль бросит дичь ради игрока ТОЛЬКО если игрок БЛИЖЕ (как уже делал
  спровоцированный патруль) ⇒ никакого нового тир-приоритета, охота §5.13.28 в обычном случае цела. **Строго ADDITIVE**:
  §0.2-G усилен — патруль, отвлечённый на игрока, даёт пирату аптайма БОЛЬШЕ, не меньше («уменьшает урон/агрессию/аптайм
  пирата?» — НЕТ); пираты бесфракционны и не тронуты. Ноль RNG (§2.3), без новых полей, не CHROMOCORE-ребейк (§2.4);
  `factionRelation` двойне-защищён ⇒ мусор/своя фракция ⇒ Neutral ⇒ прежнее поведение. Лестница СИМ зелёная: `make`
  (0 предупр.), оба smoke (exit 0), **`make soak` ПОБИТОВО бейзлайн §5.13.41** (`frames=120000 deaths=0 invariantFails=0
  rocks=648 shiny=382`, ноль ASan/UBSan). **Почему soak — no-op (ЭМПИРИЧЕСКИ проверено врем. инструментом, НЕ «на веру»):**
  ветка `CK_PATROL` в ОСНОВНОМ 120k-цикле не бежит — `init()` ставит **18** воен-агентов (тип «military» ⇒ `CK_PATROL`,
  `localgen.cpp:453`) гарнизонами на `controlledStars` их фракций, РАЗБРОСАННЫХ по кластеру-1200 (замерено: 78/792/1073/314/…,
  `enRoute=0`, `atVisitedStars=0`), а soak облетает ФИКСИРОВАННЫЙ disjoint-маршрут `{-1,0,1,2,3,5,7,11,23,99}`; `buildLocalScene`
  co-локейтит агента лишь при `currentStar==звезда` (`localgen.cpp:438`) ⇒ `patrols=0` на ВСЕХ 10 звёздах. ⚠️ **Ревью-поправка
  ОПРОВЕРГНУТА:** ревью заключило «military-агенты в `init()` ⇒ патрули спавнятся в соаке, значит „нет патрулей“ ложно» — это
  НЕВЕРНО: агенты существуют, но их гарнизонные звёзды НЕ пересекают маршрут soak ⇒ инструментальный `patrols=0` подтверждён.
  (Тот же механизм объясняет no-op §5.13.28/30/32/34/36/38/40 — «нет патрулей в соаке» верно ПО ЗАМЕРУ, а не по заморозке.)
  Новый двухфазный пробник
  `lawHuntsHostile` (свежий `Game`, ОДИН патруль фракции `F`, `resize(1)` ⇒ ни пиратов ни иных целей, игрок пиньнут в 300 LU:
  >`WEAPON_RANGE` 150 ⇒ огонь не достаёт, <`AW2` 750 ⇒ в осведомлённости): (A) реп −100/Enemy ⇒ патруль берёт игрока
  (`aiState==1`), (B, контроль) реп 0/Neutral ⇒ простаивает (`aiState==0`) — доказывает, что триггер СТОЯНИЕ, а не провокация.
  Следующий (§5.13.43, РЕНДЕР) — кокпит-cue «закон охотится на ТЕБЯ» (см. §4.3). Правки: `localsim.cpp`/`soak_test.cpp`
  (+ `master_prompt.md`, `README.md`).
- ✅ **СДЕЛАНО (2026-07-29, автономный срез РЕНДЕРА, §5.13.43):** **кокпит-cue «HUNTING YOU» — видно, что закон охотится на
  ТЕБЯ по стоянию** — визуальный спутник §5.13.42: патруль, взявший игрока целью БЕЗ провокации (по тиру Enemy/Hostile), несёт
  над бортом багровую подпись `HUNTING YOU` + самый быстрый/яркий пульс (темп 7.4 ≠ 3.0/4.5/5.2/6.0; круги ярче всех статусных,
  альфа 110..255), а за кадром — краевую стрелку цветом стояния; чередование §5.13.41 render → .42 sim → **.43 render**. Игрок
  теперь ПОНИМАЕТ, почему на него напали без его выстрела. **Ключевое решение — ноль новых полей, ноль нового sim-поведения:**
  задача просила «без изменения симуляции», и это выполнено буквально — оживлено СУЩЕСТВОВАВШЕЕ мёртвое поле
  `LocalCraft::aiTarget` (до сих пор только `init=-1` в `localgen.cpp`, НИГДЕ в симуляции не читалось — проверено grep'ом).
  Симуляция пломбирует его read-only сразу после присвоения `aiState` (`localsim.cpp:630`): при атаке (`aiState==1`) —
  `LOCAL_TARGET_PLAYER`, если цель игрок, иначе индекс борта; вне атаки −1. Раз НИ ОДНО sim-решение его не читает ⇒ 0 влияния
  на поведение ⇒ соак ПОБИТОВО идентичен. Рендер-предикат
  `c.kind==CK_PATROL && c.aiState==1 && c.aiTarget==LOCAL_TARGET_PLAYER && !c.hostile && c.hullHP>0` (обе площадки — on-screen
  cue и off-screen `nearHunt` — зовут ОДИН общий хелпер `huntsPlayerByStanding()`, `localdraw.cpp`, чтобы предикаты не разъехались,
  адверсарное ревью §5.13.43 item #4) ⟺ сработал override §5.13.42 ПО СТОЯНИЮ И охотник НЕ бежит: для патруля `atkPlayer=true`
  возможно ТОЛЬКО на строке §5.13.42 (стр.402 — лишь CK_PIRATE, стр.579 — лишь CK_TRADER/CIVILIAN), а `!c.hostile` отсекает
  спровоцированный патруль (его враждебность самоочевидна). Тонкость `⟺`: `aiState==1` (не `==2`) СОЗНАТЕЛЬНО исключает беглеца —
  низкохалловый охотник переходит в flee (`aiState=2 ⇒ aiTarget=−1 ⇒ cue гаснет`), что честно (убегающего не зовут активным
  охотником). Цвет — тот же `standingCue` §5.13.39 (Enemy багровый, Hostile оранжево-красный; fallback `P.red` ⇒ ноль
  неинициализированного цвета). Краевой `nearHunt` (нужен `!c.hostile`) и старый `nearHos` (нужен `c.hostile`, `localdraw.cpp:1964`)
  — DISJOINT ⇒ ноль двойного маркера; когда охотник, получив выстрел, флипает `hostile`, чистый хэндофф nearHunt→nearHos (красный).
  Лестница РЕНДЕРА зелёная: `make` (0 предупр.),
  оба smoke (exit 0), **`make soak` ПОБИТОВО бейзлайн §5.13.42** (`frames=120000 deaths=0 invariantFails=0 rocks=648 shiny=382`,
  по-звёздный разбор неизменен, ноль ASan/UBSan) + НОВЫЙ пробник `patrolTargetSignal=ok`, читающий `aiTarget` НАПРЯМУЮ (фаза A:
  Enemy ⇒ `aiTarget==LOCAL_TARGET_PLAYER`; фаза B, анти-ложняк: более близкий розыскной пират ⇒ патруль целит В ПИРАТА,
  `aiTarget>=0`, а не сентинел игрока), **`make shot_asan` 34/34 ноль ASan/UBSan**, `make shots` 34/34. **+3 шота** (не обычный +1):
  `shot_hunted_standing` (Enemy, `aiTarget=PLAYER` ⇒ cue ГОРИТ) + `shot_hunted_none` (тот же Enemy-патруль, но `aiTarget`=индекс
  пирата ⇒ cue ПОГАС) — меняется ТОЛЬКО `aiTarget`, изолируя гейт; + `shot_hunted_edge` (тот же охотник, но нос камеры отвёрнут ⇒
  борт ЗА КАДРОМ) — покрывает OFF-SCREEN ветку `nearHunt`/`drawEdgeMarker` (адверсарное ревью §5.13.43 item #3: on-screen пара
  #28/#29 её не задевала); счётчик 31 → **34**. Глазами (sips→PNG): в A над патрулём багровое `HUNTING YOU` + кольца, панель
  `STANDING ENEMY −100`; в B — та же панель, но подписи НЕТ; в edge — багровая стрелка на правом ободке (зелёная слева = штатный
  указатель на рынок `nearMkt`, ортогонален). Следующий (§5.13.44, СИМ) —
  см. форвард-бэклог §4.3. Правки: `localsim.cpp`/`localdraw.cpp`/`shot_test.cpp`/`soak_test.cpp` (+ `master_prompt.md`, `README.md`).
- ✅ **СДЕЛАНО (2026-07-29, автономный срез СИМУЛЯЦИИ, §5.13.44):** **ВЕНДЕТТА закона — патруль, что гонится ЗА ТОБОЙ по
  стоянию, летит тем резвее, чем ГЛУБЖЕ твоя вражда** — sim-спутник §5.13.42/43, замыкающий закон-цикл: §5.13.42 (закон берёт
  игрока целью при реп ≤ −48) → §5.13.43 (кокпит показывает «HUNTING YOU», оживив мёртвое `aiTarget`) → **§5.13.44 (симуляция
  наконец ПОТРЕБЛЯЕТ это `aiTarget` — поле дорастает из чисто-рендерного до ВХОДА sim)**; чередование .42 sim → .43 render →
  **.44 sim**. Механика — точное ЗЕРКАЛО облавы §5.13.34 (`manhuntSpeedMult`), но ключ иной: не награда за пирата, а ЛИЧНАЯ
  репутация. Новая `vendettaSpeedMult(game, scene, ci)` (`localsim.cpp`, рядом с облавой): множитель 1.0, если борт не
  `CK_PATROL`, мёртв, `aiTarget != LOCAL_TARGET_PLAYER`, или фракция невалидна/своя; иначе `heat = clamp((−48 − rep)/80, 0, 1)`,
  `rep = game.factionRelation(playerFaction, c.faction)` ⇒ возврат `1 + VENDETTA_SPEED_GAIN·heat`. Накал: реп −48 (порог
  Hostile, где включается охота §5.13.42) ⇒ heat 0 (ноль бонуса), реп −96 ⇒ 0.6, реп −128 (дно) ⇒ 1.0. `VENDETTA_SPEED_GAIN=0.6`
  (`local.h` LocalCfg, рядом с `MANHUNT_SPEED_GAIN=0.5`) — БОЛЬШЕ облавы: личная вражда неотвратимее профессиональной охоты за
  головой. Множится в `effMax` (`localsim.cpp:~950`) рядом с облавой; **дизъюнктны ПО ПОСТРОЕНИЮ**: облава гейтится `c.defending`,
  а override §5.13.42 ГАСИТ `defending` (стр.596), плюс в `manhuntSpeedMult` добавлен явный guard `if (aiTarget==PLAYER) return
  1.0` ⇒ на одном борту активен максимум ОДИН множитель. Читает ЖИВОЙ `aiTarget` (свежий: движение — отдельный проход ПОСЛЕ
  ИИ-цикла ⇒ все борта уже спломбировали цель). Комбат-инвариант §0.2-G УСИЛЕН: патруль лишь БЫСТРЕЕ гонит игрока (больше боя,
  не меньше); пирата не трогаем; реп выше −48 ⇒ множитель 1.0 = прежнее поведение (нулевой регресс). Ноль RNG, ноль новых полей,
  ноль глобального `rng` (§2.3). **Рендер НЕ трогали ⇒ шотов не добавили** (как §5.13.34: облава тоже без шота; кокпит-cue
  «вендетта» — кандидат будущего РЕНДЕР-среза). Лестница СИМ зелёная: `make` (0 предупр.), оба smoke (exit 0), **`make soak`
  ПОБИТОВО бейзлайн §5.13.43** (`frames=120000 deaths=0 invariantFails=0 rocks=648 shiny=382`, по-звёздный разбор неизменен,
  ноль ASan/UBSan; облавный пробник по-прежнему `90.00/60.00` ⇒ guard не сдвинул старое поведение) + НОВЫЙ пробник
  `vendettaScalesWithStanding=ok`: патруль фракции F + игрок (resize(1)), пин в 600 LU (>WEAPON_RANGE 150, <AW2 750), фаза A реп
  −128 ⇒ `deepEnmity=96.00 (exp 96.00)`, фаза B реп −48 ⇒ `threshold=60.00 (exp 60.00)`, `faster=yes`, **`bothHuntPlayer=yes`**
  (ОБА борта реально целят игрока — бонус растёт с ГЛУБИНОЙ вражды, а не с фактом охоты). `make shot_asan` 34/34, `make shots`
  34/34 (без изменений). Следующий (§5.13.45, РЕНДЕР по чередованию) — см. форвард-бэклог §4.3. Правки:
  `local.h`/`localsim.cpp`/`soak_test.cpp` (+ `master_prompt.md`, `README.md`).
- ✅ **СДЕЛАНО (2026-07-29, автономный срез РЕНДЕРА, §5.13.45):** **ЖАР ВЕНДЕТТЫ — кокпит-cue «HUNTING YOU» калится с
  ГЛУБИНОЙ твоей вражды** — render-спутник §5.13.44, замыкающий связку .42→.43→.44→**.45** (чередование .44 sim → .45 render):
  §5.13.43 зажёг багровую подпись «HUNTING YOU» + пульс-кольца, когда закон берёт игрока целью по стоянию; §5.13.45 МОДУЛИРУЕТ
  этот cue тем же `heat`, что калит sim-скорость §5.13.44 — глубже минус фракции охотника ⇒ ГОРЯЧЕЕ тон, РЕЗЧЕ пульс, ЯРЧЕ база.
  Приём D (как §5.13.35 калил вектор облавы по heat §5.13.34): `heat` выведен draw-side из ЖИВОЙ репутации формулой, ПОБИТОВО
  совпадающей с `vendettaSpeedMult` (`(−48 − rep)/80`, тот же clamp). Новый файл-хелпер `vendettaHuntColor(game, c, heat&)`
  (`localdraw.cpp`, рядом с `huntsPlayerByStanding`): базовый цвет стояния (`standingCue`, fallback `P.red`), тёплый анил к
  бело-калёному (`r += heat·40`, `g/b += heat·150`; `rgba` клампит [0,255]) ⇒ багрянец → бело-горячий (НЕ cyan); выводит `heat`.
  On-screen cue: пульс `7.4 + heat·3.7` (7.4 → 11.1, ×1.5 как марш облавы §5.13.35), база альфы `110 + heat·50` (110 → 160).
  Off-screen маркер `nearHunt` берёт тот же `vendettaHuntColor` ⇒ тон on/off-screen НЕ разъедется (та же анти-дрейф-дисциплина,
  что общий предикат `huntsPlayerByStanding` §5.13.43). **Нулевой регресс при heat=0** (реп −48, порог включения §5.13.42):
  `int(0·k)==0`, темп 7.4 ⇒ РОВНО прежний §5.13.43 cue. Чистый draw-only: читает `factionRelation` на лету, ноль RNG/новых
  полей/правок sim ⇒ **`make soak` ПОБИТОВО** (localdraw в headless не бежит). Лестница РЕНДЕР зелёная: `make` (0 предупр.), оба
  smoke (exit 0), soak бейзлайн §5.13.44 (`frames=120000 invariantFails=0 rocks=648 shiny=382`, все пробники ok), **`make
  shot_asan` 36/36** (0 ASan/UBSan), `make shots` 36/36. +2 шота-эндпоинта: `shot_hunted_vendetta_hot` (реп −128, heat 1.0 ⇒
  бело-горячий/резкий) и `shot_hunted_vendetta_cold` (реп −48, heat 0 ⇒ РОВНО §5.13.43, пин нулевого регресса); A/B подтверждён
  глазами. Следующий (§5.13.46, СИМ по чередованию) — см. форвард-бэклог §4.3. Правки: `localdraw.cpp`/`shot_test.cpp`
  (+ `master_prompt.md`, `README.md`).
- ✅ **СДЕЛАНО (2026-07-29, автономный срез СИМУЛЯЦИИ, §5.13.46):** **СТАЯ ЗАКОНА — глубокая вражда СЗЫВАЕТ патрули
  в скоординированную охоту на игрока** — sim-спутник линии .42→.43→.44→.45→**.46** (чередование .45 render → .46 sim):
  §5.13.42 переключает ОДИНОЧНЫЙ патруль на игрока лишь если игрок БЛИЖЕ его розыскной пиратской дичи (`pd2 < atkBest`) —
  патруль с более близким пиратом гонит пирата. §5.13.46 добавляет ПАК-override ПОСЛЕДНИМ тиром ветки CK_PATROL: Enemy/
  Hostile-стоящий патруль, что ВИДИТ игрока (`pd2 < AW2`), но у кого §5.13.42 не сработал (`!atkPlayer`, пират ближе),
  БРОСАЕТ пирата и присоединяется к охоте на игрока, ЕСЛИ рядом (в `PATROL_DISTRESS_R`) есть ДРУГОЙ Enemy/Hostile-патруль,
  тоже видящий игрока. Так глубокая вражда СЗЫВАЕТ стаю — зеркало набата пиратов §5.13.30 / подмоги патрулей §5.13.32, но
  по ИГРОКУ. Приём D: «сосед РАСПОЛОЖЕН охотиться» ре-выводится из ЖИВЫХ `kind`/`faction`/позиций (НЕ из спломбированного
  `aiTarget`/`defending` соседа) ⇒ два патруля коммитятся КАЖДЫЙ по РАСПОЛОЖЕННОСТИ другого ⇒ порядок-независимо, без
  дедлока/рекурсии. Нужна ПАРА (одиночка без расположенного соседа гонит пирата по §5.13.42 — контроль пробы). `atkBest=pd2`
  как в §5.13.42 (иначе `tdist=√atkBest` взял бы дистанцию до ПИРАТА). §0.2-G УСИЛЕН — патруль ушёл с пирата на игрока ⇒ у
  пирата аптайма БОЛЬШЕ, у игрока боя БОЛЬШЕ; строго аддитивно (нет пары / реп > −48 / игрок вне AW2 ⇒ прежнее поведение).
  Ноль RNG, без нового поля; emergent-бонус: cue «HUNTING YOU» §5.13.43/.45 авто-визуализирует каждого члена стаи. Лестница
  СИМ зелёная: `make` (0 предупр.), оба smoke (exit 0), **`make soak` ПОБИТОВО** бейзлайн §5.13.44/.45 (`frames=120000
  deaths=0 invariantFails=0 rocks=648 shiny=382`, все прежние пробники ok — в 120k-цикле патрулей нет ⇒ тир no-op), новый
  изолир-пробник `lawPackHuntsPlayer` (packOverridesGate=ok + loneChasesPirate=ok) зелёный, `make shots`/`make shot_asan`
  36/36 (SIM-срез шотов не добавляет). Следующий (§5.13.47, РЕНДЕР по чередованию) — см. форвард-бэклог §4.3. Правки:
  `localsim.cpp`/`soak_test.cpp` (+ `master_prompt.md`, `README.md`).
- ✅ **СДЕЛАНО (2026-07-29, автономный срез РЕНДЕРА, §5.13.47):** **ВИДНАЯ СТАЯ ЗАКОНА — баннер «PACK HUNT X%d» +
  сходящиеся нити радара** — render-спутник §5.13.46 (чередование .46 sim → .47 render): §5.13.46 СЗЫВАЕТ пару+ патрулей
  на охоту по стоянию, а игрок узнавал об этом лишь по разрозненным per-craft cue «HUNTING YOU» (§5.13.43/.45). §5.13.47
  добавляет ДВЕ чисто-рисовательные подсказки. (1) **Экранный баннер сверху-центр** `PACK HUNT X%d` — загорается ТОЛЬКО при
  N≥2 патрулях, что СЕЙЧАС охотятся на игрока по стоянию (это и есть «стая» §5.13.46; одиночку уже озвучивает §5.13.43, не
  дублируем), тоном самого ГОРЯЧЕГО жара вендетты в стае (worst-enemy задаёт накал), темп/яркость — как on-screen cue
  §5.13.45. (2) **Нити стаи на радаре** — от блипа КАЖДОГО охотника-по-стоянию (N≥1) тянется цветная линия к ЦЕНТРУ (=игрок):
  «сеть затягивается»; заодно чинит слепое пятно радара — неспровоцированный охотник `!hostile` рисовался тускло-серым (§5.13.27),
  неотличимо от мирного трафика, а теперь помечен цветной нитью. Приём D: обе подсказки ре-выводят «кто охотник» из ЖИВЫХ полей
  общим предикатом `huntsPlayerByStanding` + красят общим `vendettaHuntColor` (те же, что per-craft cue/радар-блип ⇒ анти-дрейф,
  цвета не разъедутся). Ноль нового поля/RNG; `radarXY` для dd>RANGE кладёт точку РОВНО на обод (радиус R) ⇒ нити не улетают за
  радар. §0.2-G тривиально (чистый рендер, лишь чтение репутации; sim не тронут). Лестница РЕНДЕР зелёная: `make` (0 предупр.),
  оба smoke (exit 0), **`make soak` ПОБИТОВО** тот же бейзлайн (`frames=120000 deaths=0 invariantFails=0 rocks=648 shiny=382`,
  `lawPackHuntsPlayer=ok` + все прежние пробники ok — `localdraw` в headless не бежит), `make shots`/`make shot_asan`
  **37/37** (ASan/UBSan без единой диагностики), новый шот `shot_pack_hunt` (три охотника: два фракции 0 при −128 бело-калёные,
  третий фракции 1 при −70 теплее ⇒ баннер «PACK HUNT X3» + три нити к центру + три «HUNTING YOU»). Негативы бесплатны:
  N=1 (`shot_hunted_standing` #28) — баннер молчит, одна нить; N=0 (`shot_hunted_none` #29) — ни баннера, ни нити. Следующий
  (§5.13.48, СИМ по чередованию) — см. форвард-бэклог §4.3. Правки: `localdraw.cpp`/`shot_test.cpp` (+ `master_prompt.md`, `README.md`).
- ✅ **СДЕЛАНО (2026-07-29, автономный срез СИМУЛЯЦИИ, §5.13.48):** **КОПОУБИЙЦА — убийство ПАТРУЛЯ игроком бьёт по
  репутации ВДВОЕ и не прощается даже за уже-hostile патруль** — sim-срез (чередование .47 render → .48 sim), замыкающий
  петлю закон-цикла со стороны ПОСЛЕДСТВИЙ. До .48 убийство любого не-hostile борта фракции стоило generic −8 (ветка
  §5.13.14), а уже-hostile патруль — 0 (гейт `!hostile`). §5.13.48 добавляет патруль-СПЕЦИФИЧНУЮ, hostile-AGNOSTIC ветку
  в блок убийства игроком `(E)`: если сбитый — `CK_PATROL` с фракцией, штраф `−COP_KILL_REP` (16, ВДВОЕ от −8)
  НЕЗАВИСИМО от `hostile`; ветка ЗАМЕНЯЕТ generic −8 для патруля (иначе двойной штраф), не-патрули идут прежней
  generic-веткой `else if` БАЙТ-в-БАЙТ. Глубже минус ⇒ раньше порог охоты §5.13.42, горячее вендетта §5.13.44, крупнее
  стая §5.13.46 — и всё это УЖЕ ВИДНО баннером/нитями §5.13.47 ⇒ идеально замкнутая петля sim→render, НОЛЬ новых шотов;
  момент помечен тостом `COP KILLED -REP`, уцелевшие борты фракции звереют (как generic-ветка). Строго ADDITIVE, §0.2-G
  УСИЛЕН (глубже вражда закона = больше боя игроку; патрули, ушедшие с пиратов на игрока, дают пиратам аптайма больше),
  ноль RNG/новых полей. Лестница СИМ зелёная: `make` (0 предупр.), оба smoke (exit 0), **`make soak` (санитайзерный)
  ПОБИТОВО** тот же бейзлайн (`frames=120000 deaths=0 radioClaims=3 craftDestroyed=2 invariantFails=0 rocks=648 shiny=382
  trades=0` — в соаке патрулей нет ⇒ `CK_PATROL`-ветка no-op ПО ПОСТРОЕНИЮ) + новый пробник `copKillDeeperReprisal=ok`
  (3 сцены: не-hostile патруль −16, hostile патруль −16, торговец-контроль −8), ноль ASan/UBSan; `make shot_asan` **37/37**
  (СИМ-срез шотов не добавляет). Следующий (§5.13.49, РЕНДЕР по чередованию) — см. форвард-бэклог §4.3. Правки:
  `local.h`/`localsim.cpp`/`soak_test.cpp` (+ `master_prompt.md`, `README.md`).
- 🚨 **2026-08-04 — ПРЕД-РЕЛИЗНЫЙ АУДИТ (§10). Читай §10 ПЕРЕД §5.13.**
  Цикл «автономных срезов локального режима» ОСТАНОВЛЕН: проект готовится к релизу, и аудит
  нашёл, что несколько его несущих допущений были ложными. Коротко:
  - **Релиз не содержал ни одного ассета.** `build.yml` не клал `timertia.png` и музыку ни в
    `.app`, ни в `dist/`; пути были относительно CWD (у `.app` это `/`). Игрок получал немую
    игру без портрета Тимертии в интро. Починено: `SDL_GetBasePath()` + упаковка в CI.
  - **Сейв не работал в релизе** (писался в CWD). Переведён на `SDL_GetPrefPath`.
  - **Мир был недетерминирован** — см. §2.3. Все прежние отчёты «соак побитово» недействительны.
  - **Легенда управления была закомментирована** (`ui.cpp`), 29 хоткеев не мог узнать никто,
    включая `L` — вход в локальный режим, то есть весь трек §5.13 был невидим для покупателя.
  - **Торговля исполнялась без проскальзывания** — весь груз по цене ДО сделки. Замер: один
    рейс давал **x44 капитала**. Введена средняя цена исполнения (§10.3).
  - **Мир был пуст**: 124 борта и 28 контрактов на 10 000 звёзд. Поднято до 1011 и 324.
  - Новая механика-крючок: **лицензионная квота оборота** (§10.4).
- Коммит/пуш — **только по прямой просьбе** пользователя; если на `main` — сперва ветка.

### 0.1 Старт для следующего агента (передача контекста, актуально на 2026-07-29)

> ⚠️ **Раздел 0.1 описывает состояние на 2026-07-29 и во многом УСТАРЕЛ.** Ветка, приоритеты и
> «что ждёт плейтеста» сменились после пред-релизного аудита 2026-08-04. Актуальная передача
> контекста — **§10**. Ниже оставлено как история трека локального режима.

**Где мы сейчас.** Ветка `local-flight-mode`. За сессию пройдены плейтесты №1–4 (каждый закрыт
проходом углубления §5.13.1 мышелук+сфера, §5.13.2 масштаб D/R, §5.13.3 плазма-шейдер), а поверх —
**автономный срез рендера §5.13.4: планеты и луны стали освещёнными ray-sphere сферами** (тот же
приём, что звезда, распространён на отражающие тела; свет — звезда в 0,0,0 → терминатор день/ночь,
фазы/серпы), следом **§5.13.5: кольца газовых гигантов в перспективе** (ray↔plane, щели Кассини,
тень планеты на кольцах, композит по глубине — полный Сатурн в 3D), **§5.13.6: полировка звезды**
(спектр O→M, хромосферный ободок, протуберанцы короны, пятна), **§5.13.7: астероиды пояса
стали освещёнными «глыбами»** (тот же ray-sphere + выгрызенный неровный силуэт, кратеры, кувыркание —
закрыт последний крупный «плоский» разрыв) и, наконец, **§5.13.8: туманность-фон стала объёмным
газовым полем на небесной сфере** (`renderNebula`: многооктавный шум по лучу + доменный варп +
градиент «банки», полупрозрачная — последний «плоский» задник в перспективе тоже углублён).
Полный «плоскостной» разрыв рендера закрыт (звезда/планеты/кольца/астероиды/туманность — всё
объёмное), поэтому цикл повернул от косметики к жизни: **§5.13.9 — первый срез симуляции: живой
трафик NPC** (рейсы к рынкам + прилёты/отлёты, система дышит, а не стоит), затем **§5.13.10 —
читаемость этого трафика из кабины** (варп-вспышки прилёта/отлёта, янтарное кольцо-причал, HUD-тальник
`IN/DOCK`), чтобы уже существующая жизнь была видна игроку, а не только soak-логам, **§5.13.11
— бедствие конвоя + пейофф за защиту** (окружающие рейды «пират vs торговец» стали действенным событием:
SOS-маяк у жертвы + панель бедствия + тост `CONVOY RAID`, а за уничтожение угрожавшего пирата — бонус
кредитов/исследования, тост `CONVOY SAVED` и rep-бамп фракции жертвы; пираты НЕ ослаблены) и, наконец,
**§5.13.12 — эскорт-патрули реагируют на это бедствие** (патруль слышит рейд с 1400 LU, приоритетно летит
на перехват налётчика — флаг `defending`; зелёный «эскорт»-пульс/краевой маркер/панель `ESCORT`; огонь
всё так же в упор по `WEAPON_RANGE`, пираты НЕ ослаблены). Затем цикл вернулся к косметике одним точечным
углублением звезды: **§5.13.13 — вспышки-СОБЫТИЯ на короне** (две активные области дрейфуют по лимбу и
периодически эруптируют — всплеск яркости + горячий бело-голубой сдвиг + локальный вырост короны;
детерминированно по `fxClock`, нулевой регресс при `fxClock=0`, ловит только прицельный шот
`shot_star_flare`). Затем цикл снова повернул к ЖИЗНИ — первым срезом ПОСЛЕДСТВИЙ: **§5.13.14 —
write-back в макро** (сбитый в локальном полёте борт-зеркало реального NPC «умирает» и в постоянном
мире — его макро-агент деградирует в спас-капсулу, груз сброшен, кредиты целы; общий помощник
`downgradeAgentToEscapePod` вынесен из `robAgent`, репрессия фракции не дублируется, детерминированно;
проверено write-back-пробником в soak). Затем — снова точечная косметика: **§5.13.15 — разнотипные
породы астероидов** (каждый камень пояса классифицируется по своему `element` в реальный класс
лёд/углерод/металл/силикат → своя палитра + зеркальность `LocalRock.spec`; лёд/металл дают
specular-блик Блинна–Фонга, углерод/силикат матовы; силикат-дефолт = прежний вид пояса ⇒ нулевой
регресс; вариация яркости из детерминированного хэша индекса, поток генерации не сдвинут), и, замыкая
петлю «вид → смысл», **§5.13.16 — состав породы влияет на темп добычи** (классификация §5.13.15 вынесена
в единый чистый `rockClass()`, кормящий и рендер, и симуляцию; металл копается ×1.60, лёд ×1.25, углерод
×0.65, силикат ×1.00 = прежний ⇒ нулевой регресс; видимый блеск металла стал игровым маяком богатой руды;
облик пояса и поток генерации байт-в-байт как в §5.13.15), и, доводя ту же петлю до **действия**,
**§5.13.17 — майнинг-лазер из кокпита, окрашенный в цвет класса породы** (пока идёт бурение и цель в зоне
извлечения, из «дула» к глыбе тянется луч: ореол = цвет класса, сердцевина бело-горячая, точка удара;
пульс детерминированно по `fxClock`, без RNG; чистый draw-only ⇒ soak байт-в-байт как §5.13.16 —
«блестит ⇒ богато ⇒ яркий стальной луч» теперь видно прямо в момент добычи), и, вторым срезом ПОСЛЕДСТВИЙ
после §5.13.14, **§5.13.18 — зеркала-торговцы продают груз на местном рынке при стыковке** (co-located
борт-зеркало реального макро-агента-торговца при швартовке зовёт тот же детерминированный `sellCargo`, что
макро-`updateTrader` на прибытии — реальные кредиты/груз/цены/казна двигаются в постоянном мире; SELL-ONLY,
без rng и без сдвига макро-локации, §2.3-safe; макро-сим на время локального полёта заморожен ⇒ нет двойного
счёта; тост `TRADER SOLD <SYM>` — так живая экономика впервые ПИШЕТ в мир, а не только читает). Всё закоммичено и запушено; рабочее дерево чистое.

**Мост §5.13.19→30 (что было ПОСЛЕ §5.13.18 — детали в §0-бюллетенях выше, в §5.13.* и §9).**
С §5.13.19 срезы пошли строгим **чередованием СИМ ⇄ РЕНДЕР** и сплели сквозную нить —
**эмерджентный ЗАКОН-ЦИКЛ**: §5.13.19–23 (полировка туманности и тени колец + запас руды пояса
как «класс × масса» и его HUD-читаемость) → **§5.13.24** предген награды-за-розыск (bounty) →
**.25** розыск в кабине → **.26** (write-back смерти NPC в макро + розыск за убийство) → **.27**
золотой блип радара → **.28** патруль охотится на розыскного → **.29** рендер вектора перехвата →
**.30** стая обороняет изгоя → **.31** рендер набата (оранжевый вектор тарана стаи в патруль + связка на
радаре + строка `UNDER PACK`) → **.32** закон шлёт подмогу загнанному охотнику (симметрия набату — наряд
сходится на свалку) → **.33** рендер подмоги (сине-белый вектор выручки патруль→пират + связка на радаре +
строка `BACKUP INBOUND`) → **.34** облава: патруль на P0-охоте ускоряется тем сильнее, чем крупнее награда розыскного
(`MANHUNT_SPEED_GAIN`; награда §5.13.24 впервые значит механически) → **.35** рендер жара облавы: золотой вектор погони
§5.13.29 калится добела и штрихи бегут в ×1.5 быстрее с ростом награды дичи + строка `MANHUNT N CR` в панели (тот же
`heat`, что sim-скорость §5.13.34, приём D) → **.36** благодарность закона: co-op добивание розыскного при
патруле-охотнике рядом даёт `+6` rep фракции патруля (зеркало §5.13.11 по оси репутации; «патруль гнал ИМЕННО его» —
инверсия P0-скана из живых полей, приём D) → **.37** рендер стояния: панель цели несёт `STANDING <WORD> ±N` — репутация
игрока с фракцией цели, на лету из `factionRelation` тем же классификатором порогов, что и макрослой (накопленный итог
§5.13.36/§5.13.11/расправ читается из кокпита) → **.38** коп-килл: розыскной, ДОБИВШИЙ патруль-ОХОТНИКА, получает +500
(`COP_KILL_BOUNTY`) СВЕРХ +200 §5.13.26G ⇒ следующая облава дороже/горячее (приём D — ПРЯМОЙ P0-скан: `c` — ближайший живой
`wanted` к павшему патрулю в `PATROL_DISTRESS_R`) → **.39** рендер стояния на прицеле: рамка цели + курсовой лид-пип
красятся по `STANDING`-тиру локнутой цели тем же классификатором §5.13.37 (стояние читается без строки панели; пираты
бесфракционны ⇒ прицел им не меняется, §0.2-G цел) → **.40** месть фракции: та же co-op-добивка §5.13.36 даёт **−6** вместо
+6, если фракция патруля игроку ВРАЖДЕБНА (тир Enemy/Hostile ≤ −48 по тому же классификатору §5.13.37/§5.13.39) — отстрел
«их» дичи в их операции; дружеств./нейтр. по-прежнему +6. Один co-op-убой, знак из УЖЕ живого `factionRelation` (приём D),
`LAW_GRATITUDE_REP=6` — единый |Δ| → **.41** рендер стояния на ВЕКТОРЕ: золотой вектор перехвата §5.13.29 (мир + радар)
красится по стоянию с фракцией патруля-охотника тем же `standingCue` §5.13.39 (Enemy/Hostile ⇒ красный, Friendly/Ally ⇒
зелёный, Neutral ⇒ прежнее золото) — знак §5.13.40 виден ДО выстрела на приглашающей геометрии; меняется лишь HUE (жар/марш
§5.13.35 цел), чистый рендер, §0.2-G цел → **.42** закон-по-стоянию: патруль берёт ИГРОКА целью БЕЗ провокации, если тот стоит с его фракцией на тире
Enemy/Hostile (реп ≤ −48) — одна строка расширяет гейт предпочтения игрока в `CK_PATROL` (`c.hostile` →
`(c.hostile || factionHuntsPlayer)`, тир из того же классификатора §5.13.37/39/40); «месть» §5.13.40 получила зубы, охота
§5.13.28 цела (реюз `pd2 < atkBest`), пираты не тронуты (§0.2-G усилен) → **.43** рендер cue «HUNTING YOU»: патруль, локнувший
игрока по стоянию §5.13.42, несёт над бортом багровую подпись + самый быстрый пульс (темп 7.4) + краевую стрелку цветом стояния,
чтобы игрок ПОНЯЛ, почему на него напали без выстрела; оживлено мёртвое поле `aiTarget` (ноль нового sim-поведения ⇒ соак
побитово), предикат `CK_PATROL && aiState==1 && aiTarget==PLAYER && !hostile` ⟺ сработал override §5.13.42, чистый рендер, §0.2-G
цел → **.44** ВЕНДЕТТА: патруль, что взял игрока целью по стоянию §5.13.42, летит тем резвее, чем ГЛУБЖЕ вражда его фракции к
игроку (`vendettaSpeedMult`: `heat = clamp((−48−rep)/80, 0, 1)`, `1 + VENDETTA_SPEED_GAIN·heat`, GAIN=0.6 > облавы 0.5 — личная
вражда неотвратимее охоты за головой) — зеркало облавы §5.13.34 приёмом D, но ключ ЖИВОЙ `aiTarget==PLAYER`, что §5.13.43 начал
заполнять ⇒ поле дорастает из рендерного до ВХОДА sim; дизъюнктно облаве по построению (гейт `!defending` + guard
`aiTarget==PLAYER`), пирата не трогаем (§0.2-G усилен: больше боя), соак побитово (ветка патруля не бежит) + пробник
`vendettaScalesWithStanding`. **§5.13.45 (РЕНДЕР)** калит кокпит-cue «HUNTING YOU» §5.13.43 тем же `heat` (тон анилом к
бело-калёному, темп пульса `7.4+heat·3.7`, база альфы `110+heat·50`; приём D, общий хелпер `vendettaHuntColor` для on/off-screen
⇒ анти-дрейф; draw-only, ноль новых полей ⇒ соак побитово, +2 A/B-шота горячий/холодный). **§5.13.46 (СИМ)** СТАЯ ЗАКОНА:
Enemy/Hostile-патруль, видящий игрока, но у кого §5.13.42 не сработал (пират ближе ⇒ `!atkPlayer`), БРОСАЕТ пирата и берёт
игрока, ЕСЛИ рядом (в `PATROL_DISTRESS_R`) есть ДРУГОЙ такой же патруль, тоже видящий игрока — зеркало набата §5.13.30 /
подмоги §5.13.32 приёмом D, но по ИГРОКУ; нужна ПАРА (одиночка гонит пирата), `atkBest=pd2` (иначе tdist до пирата), §0.2-G
усилен (больше боя игроку, больше аптайма пирату), соак побитово (ветка патруля не бежит) + пробник `lawPackHuntsPlayer`.
**§5.13.47 (РЕНДЕР)** делает стаю §5.13.46 ВИДНОЙ: экранный баннер `PACK HUNT X%d` (только при N≥2 охотниках-по-стоянию, тон
самого горячего жара вендетты) + нити радара от блипа КАЖДОГО охотника (N≥1) к центру-игроку (чинит слепое пятно тускло-серого
`!hostile`-блипа §5.13.27); приём D — общий предикат `huntsPlayerByStanding` + `vendettaHuntColor` с per-craft cue/блипом ⇒
анти-дрейф; draw-only, ноль полей/RNG, соак побитово, `shot_pack_hunt` +1 ⇒ шотов 37 (негативы N=1/N=0 бесплатны из #28/#29).
**§5.13.48 (СИМ)** КОПОУБИЙЦА: убийство ПАТРУЛЯ игроком бьёт по стоянию `−COP_KILL_REP` (16, ВДВОЕ от generic −8) и
hostile-AGNOSTIC (закрывает до-.48 дыру, где hostile-патруль стоил 0); патруль-специфичная ветка блока `(E)` ЗАМЕНЯЕТ
generic −8 для `CK_PATROL`, не-патрули — прежняя `else if` байт-в-байт; глубже реп ⇒ раньше §5.13.42, горячее §5.13.44,
крупнее §5.13.46 — уже ВИДНО §5.13.47 ⇒ петля sim→render замкнута БЕЗ новых шотов (тост `COP KILLED -REP`); §0.2-G усилен,
ноль RNG/полей, соак побитово (патрулей нет ⇒ ветка no-op) + пробник `copKillDeeperReprisal`, шотов 37.
Следующий (§5.13.49) — РЕНДЕР. Идёт цикл **срез → плейтест → углубление**; система приёмов, которыми это делалось, — в плейбуке **§0.2**.

**Первые шаги в новом чате:**
1. Прочитай этот документ целиком (особенно §2 — жёсткие правила, и §5.13.* — локальный режим).
2. `make` (ожидается 0 предупреждений) → `./game`. Для быстрой проверки: `./game --smoke` и
   `./game --localsmoke` (оба exit 0). Визуал микромира — `make shots` (37 PNG, смотри их глазами).
3. Уточни у пользователя фидбек плейтеста №5 (см. ниже) и работай по нему. Пользователь пишет и ждёт
   ответ **по-русски**.

**Что ждёт живого плейтеста №5.** Плазма-шар (§5.13.3) и **сферы планет/лун (§5.13.4)** проверены на
шотах, но НЕ на реальном полёте. Смотри на реакцию: (а) ощущается ли звезда «шаром плазмы» в динамике
(анимация турбулентности, корона); (б) читаются ли планеты как объёмные тела — терминатор день/ночь,
фазы/серпы, полосы газ-гигантов, атмосферный ободок; (в) не мелькает ли что на подлёте/облёте;
(г) читаются ли **кольца газ-гигантов** (§5.13.5) — открытый эллипс, передняя дуга над планетой,
задняя за сферой, щели Кассини, тень планеты на кольцах; (д) читаются ли **астероиды** (§5.13.7) как
освещённые кувыркающиеся глыбы с кратерами и неровным силуэтом (а не плоские диски) при подлёте и
добыче; (е) ощущается ли **туманность** (§5.13.8) объёмным газовым полем (пухлые сгустки, волокна,
градиент плотности по небу, дрейф) при вращении камеры — и не «плывёт» ли она рассинхронно со
скайбоксом; (ж) достаточно ли «богат» рендер или ещё полировать; (з) **ощущается ли система живой**
(§5.13.9) — торговцы летят к станции/рынку, «стоят у причала», уходят к краю; новые гости
появляются со временем (лучше видно под варпом) — не толпятся ли в одной точке, не «мигают» ли;
и **читается ли трафик** (§5.13.10) — заметна ли циан-вспышка прилёта/отлёта, ясно ли янтарное
кольцо «у причала», совпадает ли HUD-тальник `IN/DOCK` с тем, что видно за стеклом; (и) **работает ли
бедствие конвоя** (§5.13.11) — при рейде пирата на торговца виден ли красный SOS-маяк у жертвы (кольцо
у борта + краевой маркер за кадром), поднимается ли тост `CONVOY RAID`, показывает ли панель цели
SOS-состояние (рамка/метка/чип); и на пейоффе — прилетает ли `CONVOY SAVED` с бонусом кредитов и
rep-бампом, когда добиваешь угрожавшего пирата (и НЕ прилетает при обычном убийстве не-угрожавшего);
(й) **реагируют ли эскорт-патрули** (§5.13.12) — когда рядом идёт рейд, разворачивается ли патруль
(`CK_PATROL`) на перехват именно налётчика (а не просто ближайшего пирата), виден ли зелёный
«эскорт»-пульс у идущего на помощь патруля и зелёный краевой маркер «помощь идёт» из-за кадра,
показывает ли панель цели `ESCORT`-состояние; и НЕ снайпит ли патруль издалека (должен долетать и
бить в упор по `WEAPON_RANGE`, а не с 1400 LU). (к) **живёт ли корона звезды** (§5.13.13) — при
зависании у звезды время от времени вспыхивают ли на лимбе яркие горячие протуберанцы (области
эрупции дрейфуют, всплеск бело-голубой + локальный вырост короны), не «дёргается» ли эффект, не
портит ли изотропию диска в спокойной фазе. (л) **последствия боя доходят до макро** (§5.13.14) — сбей
в локальном полёте несколько бортов-зеркал реальных NPC (не чисто-локальный трафик), затем вернись в
макро/на карту: стали ли соответствующие агенты спас-капсулами (ship-класс 0, груз пропал, но кредиты
целы), не задет ли ошибочно другой агент или ты сам, и остаётся ли мир в остальном стабильным.
(м) **различимы ли типы пород** (§5.13.15) — в поясе видно ли, что астероиды разного состава выглядят
по-разному: ледяные/летучие — бело-голубые с зеркальным бликом, углеродистые — тёмно-угольные матовые,
металлические — сталь-серые с бликом, силикатные (дефолт, ≈ старый вид пояса) — таново-матовые; блестят
только лёд и металл (блик уходит при повороте от звезды), матовые классы блика не дают; не «пропал» ли
прежний вид силикатного пояса (дефолт-класс сохраняет старую палитру).
(н) **читается ли состав как ценность добычи** (§5.13.16) — при бурении разных камней виден ли класс в
тосте/HUD (`MINING FE METAL` и т.п.) и ощущается ли разница темпа: металл (блестящий) наполняет трюм заметно
быстрее силиката, углерод — медленнее; тянет ли это охотиться именно за блестящими глыбами (вид = награда);
не сломался ли прежний темп на силикате (дефолт-класс = ×1.00, как раньше).
(о) **виден ли майнинг-луч и совпадает ли его цвет с классом** (§5.13.17) — при бурении тянется ли из
кокпитного «дула» к глыбе луч, окрашен ли его ореол в цвет класса (сталь-металл, бело-голубой лёд, тёмный
углерод) при бело-горячей сердцевине, есть ли точка удара на камне и пульс/хум; гаснет ли луч при отлёте за
зону извлечения (тост добычи может ещё висеть, а луч уже нет — как в `shot_belt_near`) и при заслоне звездой;
не мерцает ли рвано (пульс должен быть плавным), не рисуется ли на орто-карте (только перспектива).
(п) **пишет ли живая экономика в мир при стыковке торговца** (§5.13.18) — понаблюдай за зеркалом-торговцем
(борт реального NPC, `agentIndex ≥ 0`), который швартуется у рыночного тела: всплывает ли тост `TRADER SOLD <SYM>`
в момент причаливания; затем вернись в макро/на рынок — уменьшился ли груз этого агента и выросли ли его кредиты
(и сдвинулись ли цены/предложение рынка), т.е. видно ли, что сделка РЕАЛЬНО прошла в постоянном мире, а не только
нарисован тост; НЕ дублируется ли продажа (тост один на швартовку, не дребезжит), НЕ трогает ли чисто-локальный
трафик (`agentIndex < 0`) и не-торговцев; и остаётся ли экономика стабильной (без скачков цен в ноль/бесконечность).

**Идеи на будущее (НЕ обязательства — жди сигнала пользователя):**
- ✅ *Полировка звезды* — **СДЕЛАНО (§5.13.6 + §5.13.13)**: спектральный контраст O→M, хромосферный
  ободок у лимба, протуберанцы/вспышки на короне, пятна-сунспоты, доменный варп 2-го порядка + доп.
  октава; **§5.13.13 — вспышки-СОБЫТИЯ (flare-цикл по `fxClock`)**: две активные области дрейфуют по
  лимбу и периодически эруптируют (всплеск яркости + горячий бело-голубой сдвиг + локальный вырост
  короны), детерминированно, нулевой регресс при `fxClock=0`. Осталось *дальше* (идеи): гранулярная
  конвекция (Вороной-подобные ячейки), корональные петли между активными областями.
- ✅ *Тот же ray-sphere-подход для планет/лун* — **СДЕЛАНО (§5.13.4)**: планеты/луны стали
  освещёнными сферами (полосы газ-гиганта/мозаичность/лёд/моря из нормали).
- ✅ *Кольца газ-гигантов в перспективе* — **СДЕЛАНО (§5.13.5)**: ray↔plane аннулюс, эллипс Сатурна,
  задняя дуга за сферой планеты, щели Кассини, тень планеты на кольцах, grazing-opacity. Осталось
  *дальше* (идеи): спутниковые щели-резонансы от лун, вертикальная толщина колец у ребра, пылевые
  «спицы» кольца B, лёгкое самозатенение колец.
- ✅ *Освещённые астероиды пояса* — **СДЕЛАНО (§5.13.7)**: ray-sphere глыбы с ламбертовым терминатором,
  выгрызенным силуэтом, кратерами и кувырканием (`LocalRock.spin`). Закрыт последний крупный «плоский»
  разрыв. **§5.13.15 — разнотипные породы по `element`** (**СДЕЛАНО**): `rockAppearance()` относит камень к
  классу (лёд/углерод/металл/силикат) по атомному номеру/металличности → палитра в `rock.r/g/b` + блеск
  `LocalRock.spec`; specular-блик Блинна–Фонга на льду/металле в `renderRockLit`. Осталось *дальше* (идеи):
  обломки-спутники у крупных глыб, простая имперфектная геометрия (би-лобы) для самых больших камней.
- ✅ *Туманность-фон* — **СДЕЛАНО (§5.13.8)**: объёмное газовое поле на небесной сфере (`renderNebula`):
  многооктавный шум по направлению луча (пухлые сгустки + волокна), доменный варп, крупномасштабный
  градиент «банки», полупрозрачность (звёзды просвечивают), дрейф по `fxClock`. Орто-карта не тронута.
  Осталось *дальше* (идеи): звёзды-подсветка изнутри туманности (эмиссия у ярких скайбокс-точек),
  тёмные пылевые прожилки (поглощение), лёгкая параллакс-глубина в 2–3 слоя.
- ✅ *Живой трафик NPC* — **СДЕЛАНО (§5.13.9)**: не-боевые NPC выполняют рейсы к рынкам (круиз →
  стоянка → новый рейс), чисто-локальные уходят к краю и деспаунятся, прилёты подсаживают гостей до
  `TRAFFIC_CAP`. Первый срез, углубляющий поведение, а не рендер. Осталось *дальше* (идеи):
  **write-back в макро** (убил локальный корабль зеркала → макро-агент стал спас-капсулой — **СДЕЛАНО §5.13.14**),
  торговля с реальным грузом/кредитами (**ПРОДАЖА при стыковке — СДЕЛАНО §5.13.18**: зеркало-торговец продаёт груз на
  местном рынке, двигая кредиты/цены в постоянном мире; осталась ПОКУПКА/полный рейс «купил дёшево → отвёз дорого» —
  требует rng-safe пути), реакция на пиратов (конвои, эскорт-патрули, сигналы бедствия при атаке), очередь стыковки/варп-эффект на прилёте.
- ✅ *Читаемость трафика* — **СДЕЛАНО (§5.13.10)**: варп-вспышка на прилёте/отлёте (`emitWarp`: циан
  кольцо + аддитивное ядро + искры), пуфф форсажа при уходе от причала, пульсирующее янтарное
  кольцо-швартовка (`errand==1`), HUD-тальник `IN/DOCK` в панели радара. Закрыт пункт «варп-эффект на
  прилёте».
- ✅ *Бедствие конвоя + пейофф за защиту* — **СДЕЛАНО (§5.13.11, Slice B)**: пират в атаке на конкретный
  не-пиратский борт помечает жертву `underAttack` → красный SOS-маяк (кольцо у борта + краевой маркер) +
  панель бедствия (рамка/метка/чип `SOS`) + тост `CONVOY RAID` на фронте рейда; за уничтожение
  угрожавшего пирата — бонус кредитов + исследование + `CONVOY SAVED` + положительный rep-бамп фракции
  жертвы. Строго аддитивно (пираты не ослаблены). Закрыт пункт «реакция на пиратов / сигналы бедствия».
- ✅ *Эскорт-патрули реагируют на бедствие* — **СДЕЛАНО (§5.13.12, Slice C)**: патруль (`CK_PATROL`)
  слышит рейд с `PATROL_DISTRESS_R=1400 LU`, среди пиратов ищет **налётчика** (ближе `RAID_NEAR=260 LU`
  к торговцу/civilian) и приоритетно летит на перехват (флаг `defending`); зелёный «эскорт»-пульс +
  краевой маркер + панель `ESCORT`. Огонь всё так же по `WEAPON_RANGE` — пираты не ослаблены. Закрыт
  пункт «эскорт-патрули/конвои с охраной». Осталось *дальше* (идеи): ~~write-back в макро~~ (**СДЕЛАНО
  §5.13.14**: убил зеркало → макро-агент стал спас-капсулой, груз сброшен, кредиты целы), ~~торговля
  реальным грузом (продажа)~~ (**СДЕЛАНО §5.13.18**: зеркало-торговец продаёт груз на рынке при стыковке —
  реальные кредиты/цены в постоянном мире; осталась покупка/полный рейс «купил дёшево → отвёз дорого»),
  тост-уведомление гостя на прилёте, очередь/слоты стыковки у станции, конвои-группы с общим сопровождением.
- *Производительность:* шейдер сейчас полуразрешение + bbox. Если на реальном железе тормозит внутри
  звезды (полноэкранный проход) — снизить частоту шума или кэшировать. Пока не оптимизировал заранее.
- *Роадмап-кандидаты* (§7.3): фон макро-симуляции при выходе, живность в системе (стыковки NPC,
  маршруты), звенья ИИ, экономика лута/радио. Плюс контент-бэклог `plans_1..10` (§7.4).

**Незыблемое:** C++11 + SDL2 только (§2); data-oriented (§2.5); локальный режим не трогает global rng
(§2.6); перспектива — только локальный режим (§2.7); орбиты §5.13.2 одобрены — не трогать. Коммит/пуш
— по стоячей директиве «комить-пушить готовое и работать дальше», на `main` — сперва ветка. Трейлер
коммита: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

### 0.2 Плейбук для будущих агентов — паттерны, которые уже доказали себя

> Дистиллят приёмов из §5.13.4–30: здесь «почему это работает» одним взглядом, «как именно» —
> в ссылающихся срезах и в §6. Продолжай список по мере новых уроков (правило §8). Если пишешь
> автономный срез локального режима — прочти эти восемь пунктов ПЕРЕД кодом.

**A. Чередование СИМ ⇄ РЕНДЕР.** Автономные срезы идут строго по очереди: sim-срез углубляет
ПОВЕДЕНИЕ, render-срез делает его ВИДИМЫМ из кабины (…§5.13.28 sim → .29 render → .30 sim →
.31 render). Так у каждого поведения сразу есть читаемость, а у каждого рендера — что показывать.
Выбирая следующий срез, бери тип, ПРОТИВОПОЛОЖНЫЙ предыдущему.

**B. Эмерджентный закон-цикл — сквозная нить дизайна.** Мелкие аддитивные срезы сложились в живую
петлю «преступление → розыск → погоня → оборона»: §5.13.11 бедствие конвоя → .12 эскорт реагирует
→ .24 предген bounty → .25 розыск в кабине → .26G розыск-за-убийство (+ write-back в макро) → .27
золотой блип радара → .28 патруль охотится на розыскного → .29 рендер вектора перехвата → .30 стая
обороняет изгоя → .31 рендер набата → .32 подмога закона → .33 рендер подмоги → .34 облава-скорость → .35 рендер жара
→ .36 благодарность закона (co-op добивание розыскного → +реп фракции патруля) → .37 рендер стояния (панель цели несёт
`STANDING <WORD> ±N` — накопленное ПОСЛЕДСТВИЕ репутации §5.13.36 теперь ВИДНО в кокпите, замыкая render-петлю на .36). НОВЫЙ срез должен ПРОДОЛЖАТЬ этот мир, а не вводить изолированную
механику. Кандидаты продолжения — в форвард-бэклоге ниже и в §7.3.

**C. Безопасность-по-построению — как держать soak-бейзлайн ПОБИТОВО.** Три проверенных на 120k
разновидности:
1. *Render-срез:* `renderLocalScene` в headless-soak НЕ зовётся ⇒ `localdraw.cpp` только
   КОМПИЛИРУЕТСЯ (под ASan через `shot_asan`), но не исполняется числовым прогоном ⇒ чисто-draw
   правка физически не может сдвинуть бейзлайн.
2. *Sim-срез, зависящий от `CK_PATROL`:* замороженный кластер soak НЕ спавнит патрулей (§5.13.28),
   поэтому patrol-зависимая ветка (.28, .30) в 120k никогда не исполняется ⇒ бейзлайн побитово;
   покрытие переносится на изолированный пробник (см. E).
3. *Новое поле `LocalCraft`* дефолтится в прежнее значение ⇒ нулевой регресс на старых путях (.9/.15).
> Правило: перед sim-срезом спроси «исполнится ли моя ветка в soak?». Да ⇒ бейзлайн сдвинется,
> нужен явный ассерт-объяснение; нет ⇒ прикрой пробником.

**D. Ре-деривация вместо нового поля / кадрового флага.** Когда рендеру или другому ИИ нужно
решение, которое сим не персистит (реальная цель патруля — локальная `atkCraft` в
`updateLocalScene`), НЕ добавляй поле и НЕ читай чужой кадровый флаг: `defending` ставится в ЭТОМ
же кадре ⇒ виден лишь у бортов, обработанных РАНЬШЕ, а своп-поп на смерти делает результат
порядко-зависимым. Вместо этого ЗАНОВО выведи решение из стабильных живых полей (`kind/wanted/
x/y/z`). Т.к. приоритет-0 перебивает все прочие цели, результат совпадает с симом ПО ПОСТРОЕНИЮ,
а не по догадке. Так сделаны §5.13.29 (рендер `pursuitQuarry`) и §5.13.30 (сим-набат стаи).

**E. Рецепт пробника (`soak_test.cpp`) для no-op-в-соаке правил.** Свежий `Game g; g.init(seed);`
→ `buildLocalScene(...)` → `s.active=true` → ресайз `s.craft` до N → КАЖДЫЙ кадр пинить позиции/
корпуса (нулевой дрейф/RNG; огромные `hullHP`, чтобы своп-поп не переиндексировал борта) → игрока
припарковать далеко (`s.px=100000`) → крутить `updateLocalScene` десятки кадров → ассертить на
НАБЛЮДАЕМОМ следствии (`aiState`, урон, `wanted`). Пример §5.13.30: две фазы (bait `wanted` on/off)
→ (A) rally срабатывает; (B) без охоты гейт заперт. Добавь агрегат в «SOAK DONE» printf
(`имя=%s`, `ok`/`FAILED`) и в `if (!ok) ++invariantFails`.

**F. Рецепт шота (`shot_test.cpp`) + учёт числа шотов.** Детерминированная СТАТИЧНАЯ сцена (сим НЕ
шагаем — `saveShot` только рендерит кадр) → `buildLocalCamera` → forced-render → BMP→PNG; добавляя
шот, ставь `total += 1` перед вызовом — счётчик «SHOTS DONE %d/%d» ДИНАМИЧЕСКИЙ, литерала числа в
`shot_test.cpp` НЕТ. Шотов сейчас **37** (последний — `shot_pack_hunt`, §5.13.47; ранее —
`shot_hunted_vendetta_hot`/`shot_hunted_vendetta_cold`, §5.13.45; `shot_hunted_standing`/`shot_hunted_none`/`shot_hunted_edge`, §5.13.43;
§5.13.42 (СИМ) кадров не добавил; §5.13.43 (РЕНДЕР) добавил **триаду** cue-горит/cue-погас/за-кадром ⇒ +3, не обычный +1 —
«до/после»-контраст изолирует гейт `aiTarget`, а edge-кадр покрывает off-screen `nearHunt`; §5.13.44 (СИМ) кадров не добавил —
чистая правка скорости, `saveShot` сим не шагает; §5.13.45 (РЕНДЕР) добавил **A/B-пару** горячий/холодный ⇒ +2 — heat=1 калит
тон/темп/яркость, heat=0 пинит нулевой регресс к §5.13.43; §5.13.46 (СИМ) кадров не добавил — пак-тир меняет лишь sim-выбор
цели, покрытие ушло в `soak_test.cpp`-пробник `lawPackHuntsPlayer`; §5.13.47 (РЕНДЕР) добавил обычный **+1** `shot_pack_hunt` —
N≥2 стая ⇒ баннер `PACK HUNT X3` + нити радара, а негативы N=1/N=0 БЕСПЛАТНЫ из уже существующих `shot_hunted_standing`/`shot_hunted_none`).
render-срез обычно +1, но контраст/off-screen/A-B стоят лишних кадров. При добавлении синхронизируй ЖИВЫЕ
«сколько PNG» в этом файле: строка «make shots (NN PNG…)» в §0.1 и строка `make shots  # NN BMP->PNG … §5.13.3–5.13.NN` в §3 (расширь и диапазон);
README число в счётчике не держит. Числа в per-срез бюллетенях/§9 (напр. «`shot_asan` 24/24» у
§5.13.29) — ИСТОРИЯ конкретного прогона, их НЕ трогай.

**G. Комбат-инвариант как ФИЛЬТР дизайна (не только запрет).** «Не ослаблять урон/агрессию/аптайм
пирата» — это тест, который проходит КАЖДЫЙ кандидат-срез ДО реализации: «уменьшает ли это урон/
агрессию/время-в-бою пирата?». Да ⇒ отклонить или переделать в аддитив. Лучшие срезы инвариант
УСИЛИВАЮТ (§5.13.30: простой пират → атакующий закон ⇒ агрессии больше). Подтверждён живым
плейтестом №1 («бой сочный; быстрая смерть от пирата — плюс»).

**H. Лестница проверок — решающее правило (детали §6.2).** Всегда: `make` 0-warn → оба smoke
exit 0 → `make soak` 120k (0 фейлов + твой пробник; бейзлайн цитируй ПОБИТОВО в коммите).
`make shot_asan` + `make shots` ОБЯЗАТЕЛЬНЫ ⇔ тронут `localdraw.cpp` (render-срез); для чисто-sim-
среза шоты НЕ нужны (`saveShot` не шагает сим ⇒ ИИ-правка на кадр не влияет).

**Форвард-бэклог (кандидаты §5.13.38+ — идеи, НЕ обязательства).**
- ✅ *§5.13.31 (РЕНДЕР) — СДЕЛАНО:* набат стаи визуализирован (горячо-оранжевый вектор тарана
  пират→патруль + оранжевая связка на радаре + строка `UNDER PACK Xn` под золотой `IN PURSUIT`);
  лямбда `rallyTarget(ci)` — та же ре-деривация (D), no-op в соаке по построению. Детали — §9.
- ✅ *§5.13.32 (СИМ) — СДЕЛАНО:* патруль шлёт подмогу загнанному охотнику — свободный патруль (без
  СВОЕГО розыскного) берёт целью пирата, облепившего ДРУГОЙ патруль-охотник (приоритет-0.5; «охотник»
  выведен заново из `kind/wanted/позиций`, приём D); строго аддитивно, no-op в соаке по построению
  (нужно ≥2 патрулей), пробник `patrolBacksUpSwarmed`. Детали — §9.
- ✅ *§5.13.33 (РЕНДЕР) — СДЕЛАНО:* подмога закона визуализирована — сине-белый вектор ВЫРУЧКИ
  респондер-патруль→цель-пират (пульс 4.2/марш 28, холоднее оранжа/золота) + синяя связка на радаре +
  строка `BACKUP INBOUND Xn` в панели осаждённого охотника; лямбда `backupTarget(ci)` — та же
  ре-деривация (D), no-op в соаке по построению. Один кадр показывает три силы: золото(закон→изгой) +
  оранж(стая→закон) + синь(закон→закон). Детали — §9.
- ✅ *§5.13.34 (СИМ) — СДЕЛАНО:* облава — патруль на P0-охоте ускоряется тем сильнее, чем крупнее награда розыскного
  (`manhuntSpeedMult`, множитель `effMax`; награда §5.13.24 впервые значит механически); строго аддитивно (у пола награды
  множитель 1.0 = нулевой регресс), no-op в соаке по построению, пробник `manhuntScalesWithBounty`. Детали — §9.
- ✅ *§5.13.35 (РЕНДЕР) — СДЕЛАНО:* рендер жара облавы — золотой вектор погони §5.13.29 калится к бело-калёному и штрихи
  бегут в ×1.5 быстрее с ростом награды дичи (`heat=clamp((bounty−250)/1250,0,1)`, ПОБИТОВО тот же, что sim-скорость
  §5.13.34) + строка `MANHUNT N CR` в панели наведённого патруля; лямбда `pursuitQuarry` уже даёт индекс дичи (приём D),
  `heat=0` ⇒ прежнее золото/марш побитово (нулевой регресс). Детали — §9.
- ✅ *§5.13.36 (СИМ) — СДЕЛАНО:* благодарность закона — co-op добивание розыскного. Игрок, добивший `wanted`-пирата,
  которого ГНАЛ патруль (P0-охота §5.13.28; «гнал ИМЕННО его» выведено ИНВЕРСИЕЙ P0-скана из живых полей — убитый борт уже
  `hullHP≤0`, потому ищем `defending`-патруль, для которого мёртвый был ближайшей живой розыскной дичью; приём D), получает
  `+6` rep с фракцией патруля ПОВЕРХ выплаты §5.13.24 (зеркало §5.13.11 по оси репутации). Строго аддитивно (пират уже мёртв
  ⇒ комбат-инвариант цел), no-op в соаке по построению (патрулей нет ⇒ `gratPatrol=−1`), пробник `lawGratitude`. Детали — §9.
- ✅ *§5.13.37 (РЕНДЕР) — СДЕЛАНО:* рендер стояния — панель наведённой цели несёт строку `STANDING <WORD> ±N` (репутация
  игрока с фракцией цели, на лету из `game.factionRelation(playerFaction, t.faction)` тем же классификатором порогов
  `classifyFactionRelationTension`, что и макрослой; словарь+цвет по тирам Enemy…Ally, `ph += 14`). Стойкое ПОСЛЕДСТВИЕ
  §5.13.36/§5.13.11/расправ (сама релейшн) впервые читается из кокпита — число ползёт зелёным вверх / красным вниз. Чистый
  draw-only (ноль полей/RNG/ребейка), пираты бесфракционны ⇒ строка им не видна ⇒ §0.2-G цел; no-op в соаке по построению,
  новый шот `shot_law_standing` (STANDING ALLY +96). Детали — §9.
- ✅ *§5.13.38 (СИМ) — СДЕЛАНО:* коп-килл — розыскной пират, добивший патруль-ОХОТНИКА (его P0-дичь §5.13.28), получает
  +`COP_KILL_BOUNTY` (500 CR) СВЕРХ обычного +200 §5.13.26G (итого +700 за копа против +200 за торговца) ⇒ следующая облава
  дороже/горячее (быстрее §5.13.34, белее §5.13.35). «Патруль гнал ИМЕННО его» — ПРЯМОЙ P0-скан из живых полей (приём D, зеркало
  `manhuntSpeedMult`: `c` — ближайший живой `wanted` к павшему патрулю в `PATROL_DISTRESS_R`; гейт `cWasWantedBefore` ДО
  §5.13.26G-бампа отсекает первопреступника, геометрия — рядового таран-стаи §5.13.30). Строго аддитивно (награда лишь ↑, кламп
  1500 ⇒ §0.2-G усилен), намеренно без гейта `o.defending` (`c` — P0-дичь по геометрии; гейт потерял бы «загнал до бегства, добил»),
  no-op в соаке по построению (патрулей нет ⇒ гейт `o.kind==CK_PATROL` не срабатывает), двусторонний пробник `copKillRaisesBounty`.
  Ревью SHIP (10/10). Детали — §9.
- ✅ *§5.13.39 (РЕНДЕР) — СДЕЛАНО:* прицел (рамка цели в 3D-мире + курсовой лид-пип упреждения) красится по `STANDING`-тиру
  локнутой цели — файловый хелпер `standingCue` даёт цвет тира лишь для НЕ-нейтрального стояния с ДРУГОЙ фракцией (реюз
  классификатора §5.13.37), иначе дефолт (циан-рамка / янтарь-пип). Пираты бесфракционны ⇒ прицел им ПОБИТОВО прежний ⇒ §0.2-G цел
  (draw-only, ноль полей/RNG/ребейка; `factionRelation` двойне-баунд-безопасен). Новый шот `shot_standing_reticle` (ENEMY-стояние
  ⇒ красный прицел; `hostile=false` изолирует стояние как причину; пара к ALLY-зелёному `shot_law_standing`). Ревью SHIP (все 10
  проверок, 3 косметические заметки). Детали — §9.
- ✅ *§5.13.40 (СИМ) — СДЕЛАНО:* «месть фракции» — та же co-op-добивка розыскного при патруле-охотнике §5.13.36 даёт `−6` реп вместо
  `+6`, если фракция патруля игроку ВРАЖДЕБНА (тир Enemy/Hostile ≤ −48 по тому же классификатору §5.13.37/§5.13.39); дружеств./нейтр.
  по-прежнему `+6`. Знак — из УЖЕ живого `factionRelation(playerFaction, patrolFaction)` (приём D), тот же инверт-P0-скан `gratPatrol`
  §5.13.36; `LocalCfg::LAW_GRATITUDE_REP=6` — единый |Δ|. Строго аддитивно (лишь реп-число; пират мёртв ⇒ §0.2-G цел), no-op в соаке
  по построению (патрулей нет); трёхсторонний пробник `factionReprisal` (Hostile −60⇒−6 / Friendly +40⇒+6 / Tense −30⇒+6 —
  пиньнит тир-семантику). Ревью SHIP (A–H + 5 хард-правил). Детали — §9.
- *§5.13.41 (РЕНДЕР) — ✅ СДЕЛАНО:* выбран тинт золотого вектора погони §5.13.29 (мир + связка на радаре) по `STANDING`-тиру
  фракции патруля (реюз `standingCue` §5.13.39) ⇒ игрок ВИДИТ, чью облаву он вот-вот усилит/сорвёт, ещё ДО выстрела: Enemy/Hostile
  ⇒ красный, Friendly/Ally ⇒ зелёный, Neutral ⇒ прежнее золото. Второй кандидат (cue-вспышка/кольцо у момента добивания) оставлен
  на будущее render-углубление, если понадобится усилить именно МОМЕНТ, а не предиктивную геометрию.
- ✅ *§5.13.42 (СИМ) — СДЕЛАНО:* «закон, который ты перешёл, охотится на тебя» — патруль берёт игрока целью БЕЗ провокации при
  стоянии Enemy/Hostile (реп ≤ −48) с его фракцией; одна строка расширяет гейт предпочтения игрока в ветке `CK_PATROL`
  (`c.hostile` → `(c.hostile || factionHuntsPlayer)`, тир из `classifyFactionRelationTension` §5.13.37/39/40). Реюз `pd2 < atkBest`
  ⇒ не новый приоритет, а расширение существующего override (охота §5.13.28 при более близком пирате цела); §0.2-G усилен (патруль,
  отвлечённый на игрока, даёт пирату аптайма больше). Двухфазный пробник `lawHuntsHostile` (A: Enemy ⇒ aiState 1; B-контроль:
  Neutral ⇒ aiState 0 — доказывает, что триггер стояние, а не провокация). Детали — §9.
- ✅ *§5.13.43 (РЕНДЕР) — СДЕЛАНО:* кокпит-cue «HUNTING YOU» — вражефракционный патруль, локнувший ИГРОКА по стоянию §5.13.42
  (`aiState==1 && aiTarget==LOCAL_TARGET_PLAYER && !hostile` ⟺ сработал override .42), несёт над бортом багровую подпись + самый
  быстрый пульс-кольца (темп 7.4) цветом `standingCue` §5.13.39 + краевую стрелку за кадром — игрок ПОНИМАЕТ, почему на него напали
  без его выстрела. Ноль новых полей и НУЛЬ нового sim-поведения: оживлено доселе МЁРТВОЕ поле `aiTarget` (read-only ⇒ соак
  побитово); пробник `patrolTargetSignal` запирает предикат (фаза A ⇒ игрок, фаза B анти-ложняк ⇒ пират). Детали — §9.
- ✅ *§5.13.44 (СИМ) — СДЕЛАНО:* ВЕНДЕТТА — патруль, что взял игрока целью по стоянию §5.13.42 (`aiTarget==LOCAL_TARGET_PLAYER`),
  летит тем резвее, чем ГЛУБЖЕ вражда его фракции к игроку (`vendettaSpeedMult`: `heat=clamp((−48−rep)/80,0,1)`, множитель
  `1+VENDETTA_SPEED_GAIN·heat`, GAIN=0.6). Зеркало облавы §5.13.34 (приём D), но ключ — ЛИЧНАЯ репутация, а не награда за пирата;
  ПОТРЕБЛЯЕТ живой `aiTarget`, что §5.13.43 начал заполнять ⇒ поле дорастает из рендерного до ВХОДА sim. Дизъюнктно облаве по
  построению (гейт `!defending` + guard `aiTarget==PLAYER` в `manhuntSpeedMult`); строго аддитивно (реп выше −48 ⇒ 1.0 = прежнее
  поведение), §0.2-G усилен (патруль лишь БЫСТРЕЕ гонит игрока), no-op в соаке по построению (патрулей нет), пробник
  `vendettaScalesWithStanding` (реп −128 ⇒ ×1.6 / реп −48 ⇒ ×1.0, оба реально целят игрока). Рендер не тронут ⇒ 0 шотов. Детали — §9.
- ✅ *§5.13.45 (РЕНДЕР) — СДЕЛАНО:* ЖАР ВЕНДЕТТЫ — кокпит-cue «HUNTING YOU» §5.13.43 калится тем же `heat`, что sim-скорость .44
  (тон анилом к бело-калёному, темп пульса `7.4+heat·3.7`, база альфы `110+heat·50`; приём D — как §5.13.35 калил вектор облавы по
  `heat` .34). Файл-хелпер `vendettaHuntColor` даёт единый тон on-screen cue И off-screen маркеру `nearHunt` ⇒ анти-дрейф.
  Draw-only (ноль новых полей/RNG/правок sim) ⇒ соак побитово; heat=0 при реп −48 ⇒ РОВНО §5.13.43 (нулевой регресс). +2 A/B-шота
  (горячий −128 / холодный −48). Детали — §9.
- ✅ *§5.13.46 (СИМ) — СДЕЛАНО:* СТАЯ ЗАКОНА — глубокая вражда сзывает патрули в скоординированную охоту на игрока. Пак-override
  ПОСЛЕДНИМ тиром ветки CK_PATROL: Enemy/Hostile-патруль, видящий игрока (`pd2<AW2`), но у кого §5.13.42 не сработал (пират
  ближе ⇒ `!atkPlayer`), БРОСАЕТ пирата и берёт игрока, ЕСЛИ в `PATROL_DISTRESS_R` есть ДРУГОЙ Enemy/Hostile-патруль, тоже
  видящий игрока. Приём D (сосед-расположенность из ЖИВЫХ `kind`/`faction`/поз., НЕ из `aiTarget` ⇒ порядок-независимо, без
  дедлока); нужна ПАРА (одиночка гонит пирата); `atkBest=pd2` (иначе tdist до пирата). §0.2-G УСИЛЕН (пирату аптайма больше,
  игроку боя больше). Соак побитово + изолир-пробник `lawPackHuntsPlayer` (пак перебил гейт у пары / одиночка гонит пирата).
  Зеркало набата §5.13.30 / подмоги §5.13.32, но по ИГРОКУ. Детали — §9.
- ✅ *§5.13.47 (РЕНДЕР) — СДЕЛАНО:* ВИДНАЯ СТАЯ — координацию §5.13.46 видно с одного взгляда. (1) Экранный баннер сверху-центр
  `PACK HUNT X%d` при N≥2 охотниках-по-стоянию, тоном самого горячего жара вендетты (worst-enemy задаёт накал). (2) Нити радара
  от блипа КАЖДОГО охотника (N≥1) к центру-игроку тем же тоном — «сеть стаи затягивается»; заодно чинит слепое пятно тускло-серого
  `!hostile`-блипа §5.13.27. Приём D: общий предикат `huntsPlayerByStanding` + `vendettaHuntColor` с per-craft cue §5.13.43/.45 и
  радар-блипом ⇒ анти-дрейф. Draw-only (ноль полей/RNG/правок sim) ⇒ соак побитово; §0.2-G тривиально. Шот `shot_pack_hunt` (+1 ⇒
  37/37 shots/shot_asan); негативы N=1/N=0 бесплатны из `shot_hunted_standing` #28 / `shot_hunted_none` #29. Детали — §9.
- *§5.13.48+ (СИМ/РЕНДЕР), кандидаты:* СЛЕДУЮЩИЙ — СИМ (.48): репутация «остывает» со временем (heat спадает без новых грехов —
  сейчас реп двигают лишь события, сама она никогда не релаксирует), ЛИБО коп-килл растит численность/частоту волны патрулей.
  Дальше по циклу — РЕНДЕР (.49): визуальный спутник выбранного sim-правила. Фильтр §0.2-G обязателен на любой sim-кандидат.
  ОТВЕРГНУТО фильтром §0.2-G (ослабляют аптайм пирата): «стая рассыпается при потере изгоя» (§5.13.30-набат и так stateless
  per-frame ⇒ избыточно; форсированный роспуск уводит пирата из боя) и «розыскной уходит в варп» (убирает пирата из сцены ⇒ ноль
  урона/времени-в-бою).
- *Жизнь/косметика:* стыковочные очереди у станции; внутрисистемные торговые линии видимой «нитью»;
  волны иммиграции/эвакуации по событиям.

---

## 1. Миссия и стиль работы

### 1.1 Что просил пользователь (дословно, из ранних сессий)
> «игра пока очень скучная и пустая но я даю тебе ПОЛНУЮ АВТОНОМИЮ УЛУЧШИТЬ ЭТУ ИГРУ И
> СДЕЛАТЬ ИНТЕРЕСНОЙ ДЛЯ ИГРОКОВ … основной кор уже создан надо теперь максимально
> геймплей сделать интересным для игроков у тебя неограниченные ресурсы неограниченно
> токенов по максимуму используй субагентов»

Расшифровка действующих директив:
- **Полная автономия** улучшать игру и делать её интересной. Не спрашивай разрешения на очевидные шаги — действуй.
- **Неограниченные токены** → не жалей вычислений; **максимально задействуй субагентов** (параллельные `Agent`/`Workflow`).
- Кор готов — фокус на **геймплее и его читаемости**.

### 1.2 Зафиксированные пользователем решения (не переоткрывать без спроса)
- Чинить **и контент, и легибельность** одновременно («И то, и то»).
- **«Сок» (juice) в текущем минималистичном стиле**: НИКАКИХ новых рендереров, спрайтов,
  шейдеров, звука. Только существующие примитивы (векторная 2D-графика, 5×7 шрифт).
- Доставка через **срез → фидбек → дальше**: небольшой играбельный вертикальный срез,
  пользователь тестит, ты правишь по фидбеку.

### 1.3 Язык и тон
- Пиши пользователю **по-русски**. Технические идентификаторы/код — как есть (англ.).
- Отчитывайся честно: если тест упал — покажи вывод; если шаг пропущен — скажи прямо.

---

## 2. ЖЁСТКИЕ АРХИТЕКТУРНЫЕ ОГРАНИЧЕНИЯ (CRITICAL)

Эти правила повторяются во всех `plans_*.md`. Нарушение ломает стиль/детерминизм проекта.

1. **Data-oriented design. НИКАКОГО ООП-наследования, `virtual`, новых классов-иерархий,
   `new`.** Только C-style `struct`, непрерывные `std::vector`, числовые id/индексы.
   Логика — свободные функции над данными.

2. **Только C++11 и только SDL2.** Никаких сторонних библиотек, никакого C++14+ синтаксиса
   (проверяется флагом `-std=c++11`).

3. **Детерминированный глобальный ГПСЧ.** Есть `extern std::mt19937 rng;` и
   `int randomer(std::mt19937&, int max)` — ВКЛЮЧИТЕЛЬНЫЙ диапазон `[0, max]`.
   - **Локальный режим НЕ трогает глобальный `rng`** (это рассинхронизировало бы макро-симуляцию).
     Для локальной случайности заводи **свой** `std::mt19937`, засеянный от сида звезды/индекса.
   - **Единственный источник засева — `Game::seed`.** `Game::init` делает `rng.seed(seed)` и
     передаёт тот же seed в `Cluster::generate`. Один seed ⇒ побитово один и тот же мир.
     ⚠️ **Исторический урок (2026-08-04).** До этой правки было наоборот: `rng` засевался
     из `std::random_device` прямо при инициализации глобала, а `Game::init` писал
     `seed = rng()` — то есть поле лишь ЗАПИСЫВАЛО случайное число и ни на что не влияло.
     Мир менялся при каждом запуске неуправляемо: один и тот же бинарь давал `deaths=5968`
     и `deaths=718` в двух прогонах soak подряд. Из-за этого **все отчёты «соак побитово /
     нулевой регресс» в §5.13.* и §9 за период до 2026-08-04 сравнивали случайные числа
     и ничего не гарантировали.** Не полагайся на них как на бейзлайн — перемеряй.
   - **Ничто вне симуляции не смеет тянуть из `rng`.** Выбор музыкального трека в `main.cpp`
     делал `randomer(rng, …)`, поэтому момент окончания трека (зависящий от звуковой карты)
     сдвигал мир. У аудио теперь свой `std::mt19937 musicRng`. Любой новый недетерминированный
     потребитель (UI, звук, ввод) обязан завести собственный генератор.

4. **Правило CHROMOCORE (запекание).** Материалы/кинематика/тактика уже ЗАПЕЧЕНЫ в поля
   корабля (`cargoCapacity, maxHullHP, speed, acceleration, heavyWeapons, lightWeapons,
   armor`). Код, читающий корабль (в т.ч. локальный), **читает СЫРЫЕ поля и НЕ домножает их снова**.

5. **Кастомный 5×7 битмап-шрифт.** Каждый глиф — строка ровно **35 символов** ('0'/'1'),
   5 столбцов × 7 строк. Горизонтальный шаг пера `6*scale`, вертикальный `8*scale`. Текст
   авто-переводится в ВЕРХНИЙ регистр. Таблица глифов — единственная, в `render2d.cpp::glyph()`.
   ⚠️ Если добавляешь символ — строка ДОЛЖНА быть 35 символов (иначе out-of-bounds в `drawText`, см. §6.3).

6. **printf-формат в ВЕРХНЕМ регистре:** `%.0F`, `%.2F`, `%d` (шрифт всё равно капсит).

7. **Ортографическая 3D→2D проекция — по умолчанию.** Макро/кластер/карта СТРОГО
   ортографические (без деления на перспективу): `depth` — только для порядка отрисовки
   (painter) и затухания (fade). Ветка `perspective==false` в `projectPointWithBasis`
   ПОБИТОВО совпадает с исходником — макро-путь не изменён.
   - **Исключение (РЕШЕНО пользователем, §5.13): перспектива разрешена ТОЛЬКО в локальном
     режиме полёта** (`View3D::perspective==true`, стиль Elite). Это единственное место с
     делением на глубину. Верхний вид «карты» в локальном режиме (клавиша C) — снова
     ортографический. Макро НИКОГДА не включает перспективу. См. `camera.h`.

---

## 3. Сборка, запуск, тесты

```bash
make balance               # РЕГРЕССИЯ БАЛАНСА (§10.12, §12.9) — 32 проверки.
                           # ОБЯЗАТЕЛЬНА при правках экономики И двигательной модели.
                           # Ловит то, что глазами не видно: схождение прогноза маршрута
                           # с реальным полётом, размен масса/объём, стену достижимости.
make                       # сборка -> ./game  (см. Makefile, SOURCES перечисляет все .cpp)
./game                     # запуск игры (SDL-окно); печатает в консоль `world seed: N`
./game --seed 12345        # повторить конкретный мир (для воспроизведения бага по отчёту игрока)
make asan                  # санитайзер-сборка -> ./game_asan (ASan+UBSan, для проверок §3.1)
make soak                  # собрать и прогнать soak-тест локального режима (§6.5)
make uiclick               # регрессия маршрутизации кликов между перекрывающимися окнами
make econ                  # балансовый стенд экономики (матрица способностей, цены, замещение)
make shots                 # 37 BMP->PNG микромира через софт-рендерер (визуал, §5.13.3–5.13.47)
make uishots               # снимки ОКОН на обоих языках (переполнения русского текста)
make shot_asan             # тот же харнес под ASan/UBSan — покрывает попиксельные шейдеры (§5.13.5/8)
make clean                 # удалить бинари (game, game_asan, soak_asan, shot_test*, *.dSYM)

# Smoke-тесты (headless, для CI/проверки без окна):
SDL_VIDEODRIVER=dummy ./game --smoke        # макро-путь: 12 кадров, ожидаемый exit 0
SDL_VIDEODRIVER=dummy ./game --localsmoke   # локальный путь: авто-вход в систему,
                                            # инъекция ввода (огонь/добыча/тяга/варп), exit 0
```

Компилятор: `c++ … -O3 -std=c++11 -Wall -Wextra -Wno-unused-parameter -I/opt/homebrew/include/SDL2 …`.

⚠️ **Предупреждения включены с 2026-08-04 (`WARNFLAGS` в Makefile).** До этого флагов `-Wall`
не было вовсе, поэтому отчёты «сборка без предупреждений» ничего не значили: при первом же
включении `-Wall -Wextra` вылезло 18 штук (мёртвые функции, неиспользуемые константы лимитов
цен, знаковые сравнения). Все вычищены. **Не отключай флаги, чтобы «сделать сборку чистой».**

Smoke-прогоны принудительно берут `seed = 42` — падение в CI обязано воспроизводиться.

### 3.1 Проверка памяти (ASan/UBSan) — делай это для нового кода
```bash
make asan                                                                    # -> ./game_asan (цель в Makefile)
SDL_VIDEODRIVER=dummy ASAN_OPTIONS=detect_leaks=0 ./game_asan --localsmoke   # exit 0, 0 ошибок
SDL_VIDEODRIVER=dummy ASAN_OPTIONS=detect_leaks=0 ./game_asan --smoke        # ~1.5 мин под ASan, exit 0
make clean                                                                   # убрать одноразовые бинари/*.dSYM
```
ASan под `--smoke` идёт долго (~90 с) из-за тяжёлой макро-симуляции — это НЕ зависание.
Для локального режима есть ещё `make soak` — детерминированный soak-тест на тысячи кадров (§6.5).

### 3.2 Шумовые диагностики (игнорировать)
clangd в редакторе пишет `'SDL.h' file not found`, `Unknown type name 'SDL_Color'/'SDL_Rect'/'Uint8'`,
`Use of undeclared identifier 'SDL_SCANCODE_*'` — это **безвредный шум language-server** (не знает
`-I/opt/homebrew/include/SDL2`). Компилятор через `make` собирает чисто. Игнорируй эти диагностики.

---

## 4. Карта кода

### 4.1 Макро-слой (исходная игра — «кор»)
| Файл | Роль |
|---|---|
| `main.cpp` | Точка входа, SDL-цикл, ввод, камера (макро + локальная), рендер-диспетчер. Мир поднимается не здесь, а в `Shell::run` (§11). |
| `shell.{h,cpp}` | **Оболочка игры (§11):** заставка студии (клеточное поле TENEVIK GAMES, лента разбегания + промотка, курсор толкает клетки), главное меню, комикс-пролог, экран генерации. Генерация мира и загрузка сейва — в фоновом `std::thread`, поэтому 9 секунд ожидания превращены в пролог. Свой `std::mt19937` (§2.3: глобальный `rng` не трогаем). Исключён из `LIBSOURCES` — опирается на `assetPath()` из `main.cpp`. |
| `game.{h,cpp}` | Центральный `struct Game`: агенты, кластер, экономика, сейв/лоад, `advanceGame()`. **Свободная ф-я `downgradeAgentToEscapePod(Agent&)` (§5.13.14): «смерть» макро-агента — корабль→спас-капсула (класс 0) + сброс груза (`cargoCost=0`), кредиты целы; вынесена из `robAgent` (устранён дубль двух inline-блоков), зовётся из макро-боя И из write-back локальных убийств. Чистая арифметика (`shipAutofit`), без RNG — §2.3-safe.** **Свободная ф-я `localDockSellCargo(Game&,int agentIndex,int starIndex)` (§5.13.18): мост из локального режима к макро-`sellCargo` — зеркало-торговец продаёт передний стак груза на местном рынке при швартовке (тот же путь, что `updateTrader` на прибытии). Лежит ВНЕ анонимного namespace (внешняя линковка для `localsim`, видит внутренний `sellCargo` по неквалиф. поиску TU). Без RNG — §2.3-safe.** |
| `cluster.{h,cpp}` | Звёздный кластер (~10 000 звёзд), `ClusterStar` (name, stellarClass, population, metallicity, resourceFocus, координаты). |
| `resource.{h,cpp}` | Элементы/ресурсы: `elementDefinitions()`, `elementCount()`, `ElementDefinition{symbol, metallicTrait, basePrice,…}`, `resourceUnitMassByIndex()`. **§12:** добавлены `density` (подогнана по 41 реальной плотности, типичная ошибка 1.5x), `ionizationEase`, `specificEnergy` (расстояние до пика кривой связи) + `elementUnitVolume()`. `elementIndex()` — теперь O(1) хеш, был линейный поиск с двумя сравнениями строк (горячее место). |
| `market.{h,cpp}` | Рынки, цены, спрос/предложение; чёрный рынок/контрабанда. |
| `ship.{h,cpp}` | `struct Ship` + вся двигательная физика (**§12**): смеси `fuel`/`propellant` в ОБЪЁМНЫХ ёмкостях, релятивистский Циолковский `shipRouteCost` со стеной `k >= 1`, дифференциальный расход `shipConsumeForDeltaV` с золой, `shipTuneDrive` (рабочая точка ФИКСИРУЕТСЯ), `shipClassMaxSpeed` (лестница скоростей выведена из цены и тяговооружённости), перелив трюм↔ёмкости. `shipCargoMass()`. |
| `drive.{h,cpp}` | **Новый (§12).** Семейства движков: Thermal (греет рабочее тело, `ve ~ sqrt(T/A)`), Ion (поле, компактные баки), Torch (топливо = выхлоп). `driveIgnitionFraction` — ПЛАВНАЯ сходимость вместо запрета: что реактор не тянет, едет балластом. `MixSummary` — средневзвешенные свойства смеси. |
| `agent.{h,cpp}` | `struct Agent` (ship, money, currentStar, type ["pirate"/…], faction). |
| `faction.{h,cpp}` | Фракции, `colorR/G/B`. |
| `colony.{h,cpp}` | Колонии, население. **§13:** для игрока колония — это ПРАВО СОБСТВЕННОСТИ на систему (`Game::systemPrice` / `playerBuySystem` / касса `playerColonyDeposit`+`playerColonyWithdraw`), а не отдельная механика основания. Сама симуляция колонии (`Game::updateColonies`, очередь строительства) не тронута. |
| `contract.h` | Заказы = контракты, одна сущность на семь типов. **§24:** поля `tier` (0..1, НЕПРЕРЫВНЫЙ тир от репутации — от него масса, дальность и множитель платы), `rushFactor` (срочный заказ: срок урезан, ставка выше) и `carriers` (борта, по которым разложен крупный груз; сдать можно только всем караваном). Репутация — `Game::factionReputation`, СВОЯ У КАЖДОЙ ФРАКЦИИ, в сданных заказах; успех +1, провал −2·√(масса/база). Семь типов (`ContractType`: Delivery/Courier/Scout/Bounty/Escort/Raid/ColonySupply). **§23:** и срок, и плата выведены из ОДНОЙ величины — годы ОПОРНОГО рейса (`contractRouteYears`, `game.cpp`: стартовый «Hauler» с полным трюмом, считается той же моделью полёта, что и ETA игрока). Константы `CONTRACT_PAY_PER_YEAR`/`CONTRACT_DEADLINE_SLACK`/`CONTRACT_GRACE_FACTOR`/`CONTRACT_LATE_FACTOR` — в `game.h`; мерить их можно ТОЛЬКО в Cr за год полёта против свободного рейса того же кошелька. Взятый заказ теперь может СГОРЕТЬ (до §23 висел вечно). |
| `ui.{h,cpp}` | Оконный UI, торговое окно, панели. Использует примитивы `namespace UI` из `render2d`. **§23:** окно `Transactions` («ЖУРНАЛ») — уже не кассовая лента: у строки есть ВИД (`enum JournalKind` в `game.h`) и свой цвет — белый взятый заказ / зелёный сданный с суммой / красный сгоревший / касса как была. Пишется через `Game::pushJournal`; `Game::journalExplained` не даёт одной выплате лечь в журнал дважды. Текст собирается по-английски и переводится словарём §14. |

### 4.2 Контентные модули (реализованы по `plans_*.md`)
| Файл | План | Что делает |
|---|---|---|
| `modules.{h,cpp}` | plans_1 | Модульная кастомизация корабля (двигатели, трюмы, баки) — прогрессия «жажда корабля». |
| `chromo.cpp` | plans_2 | Хромокоры: эндгейм-мультипликаторы (навигация/экономика/сенсоры/добыча), запекаются в поля. |
| `market.cpp` (доп.) | plans_4 | Контрабанда, нелегальные элементы, патрули. |
| `spaceevents.cpp` | plans_5 | Динамические рыночные ивенты (Бум/Эпидемия/Война/Прорыв). |
| `combat.cpp` | plans_6 | Data-oriented охота за головами / упрощённый автобой (макро). |
| `mining.cpp` | plans_7 | Ручная добыча руды на астероидах (макро-механика). |
| `anomaly.cpp` | plans_10 | Аномалии/брошенные корабли (deep-space POI). |
| *(в игре)* | plans_3/8/9 | Пассажиры/VIP, наёмный флот, исследование/сканирование — см. соответствующие планы. |

`design.md` — фундаментальный gameplay loop («медитативный гринд», экономическая спираль капитала).
`plans_1.md … plans_10.md` — детальные ТЗ каждой контентной фичи (контекст, ограничения, шаги). **Читай их, когда трогаешь соответствующую систему.**

### 4.3 Локальный слой (эта сессия — раздел 5)
| Файл | Роль |
|---|---|
| `camera.h` | Проекция 3D→2D (вынесена из main): `View3D` (+`perspective/focal/nearPlane`), `CameraBasis` (+явный right/up/fwd), `ProjectedPoint` (+`behind`/`camX..Z`), `makeCameraBasis` (орто, ПОБИТОВО как раньше), **`makeCameraFrameBasis`** (frame-базис от `pfwd`/`pup`, §5.13), `projectPointWithBasis` (орто-ветка не тронута / перспектива для локала), **`projectDirectionWithBasis`** (LOD-скайбокс), `depthFade`. Только `<cmath>`. inline. |
| `render2d.{h,cpp}` | `namespace UI` (external linkage): `Palette P`, `color/fillRect/strokeRect/panel/glyph/drawText/bar` + новые `fillCircle/strokeCircle`. Единственная таблица шрифта. |
| `local.h` | Модель данных локальной сцены + `namespace LocalCfg` (тюнинг) + 4 контракта функций + inline-хелперы `localSetForward`/`localShipRight`/**`buildLocalCamera`** (единая сборка камеры кокпит/карта для игры и харнеса, §5.13). **`LocalRock.spec` (0..1 зеркальность материала) + контракт `rockAppearance(element,…)` — чистая палитра/блеск по классу породы (§5.13.15).** **§5.13.16: `enum RockClass` + контракт `rockClass(int element)` (единый классификатор для рендера и добычи) + inline `rockYieldMult`/`rockClassName` + константы `MINE_RATE_BASE`/`MINE_YIELD_*` в `LocalCfg`.** **§5.13.18: поля `LocalScene.tradeCooldown` (дебаунс тоста `TRADER SOLD`) + `tradesExecuted` (счётчик сделок за сцену, soak-пруф).** **§5.13.24: поля `LocalCraft.wanted`+`wantedBounty` — маркер «в розыске» + сумма (250..900), ставятся ДЕТЕРМИНИРОВАННО в `localgen` (ноль `lrng`); читаются на убийстве (доп. кредиты §5.13.24), в HUD-кокпите §5.13.25, на радаре §5.13.27 (золотой блип), в ИИ ПАТРУЛЯ §5.13.28 (охота на розыскного), в рендере вектора перехвата §5.13.29 (draw-only свёртка цели погони) и в ИИ ПИРАТА §5.13.30 (набат стаи: простой пират выводит §5.13.28-охоту заново из `wanted` и берёт патруль-охотник целью), и в рендере набата §5.13.31 (draw-only ре-деривация лямбдой `rallyTarget`: оранжевый вектор тарана + связка на радаре + строка `UNDER PACK`), и в подмоге закона §5.13.32 (свободный патруль ре-деривует «патруль-охотник» по `wanted` соседа и берёт целью облепившего его пирата), и в рендере подмоги §5.13.33 (draw-only `backupTarget`: сине-белый вектор выручки респондер→пират + связка на радаре + строка `BACKUP INBOUND`); combat-инвариант пирата цел.** **§5.13.34: `LocalCfg::MANHUNT_SPEED_GAIN` (0.5) — макс. прибавка к предельной скорости патруля в облаве (растёт с наградой розыскного); чисто кинематический множитель геймплея (как `NPC_BOOST_MULT`), НЕ CHROMOCORE-ребейк, §2.4-safe.** **§5.13.38: `LocalCfg::COP_KILL_BOUNTY` (500) — прибавка к награде розыскного за убийство патруля-ОХОТНИКА СВЕРХ +200 §5.13.26G (кламп 1500); чистый геймплей-тюнинг, только растёт, урон/агрессию пирата не трогает (§0.2-G усилен).** **§5.13.40: `LocalCfg::LAW_GRATITUDE_REP` (6) — единый |Δrep| co-op-добивания розыскного при патруле-охотнике; ЗНАК берётся в `localsim` из тира `classifyFactionRelationTension(factionRelation(player, patrolFaction))`: Enemy/Hostile ⇒ −6 (месть), иначе +6 (§5.13.36); заменил литерал `6`. Чистый геймплей-тюнинг, §2.4-safe.** **§5.13.44: `LocalCfg::VENDETTA_SPEED_GAIN` (0.6) — макс. прибавка к предельной скорости патруля, гонящегося ЗА ИГРОКОМ по стоянию; зеркало облавы §5.13.34, но ключ — не награда за пирата, а ЛИЧНАЯ репутация фракции к игроку: −48 (порог Hostile, где включается player-override §5.13.42) ⇒ +0, −128 (дно) ⇒ +GAIN. GAIN>облавы (0.6>0.5): личная вражда неотвратимее профессиональной охоты. Чисто кинематический множитель (как `NPC_BOOST_MULT`/`MANHUNT_SPEED_GAIN`), строго ADDITIVE (реп выше −48 ⇒ ×1.0 = прежнее), НЕ CHROMOCORE-ребейк, §2.4-safe.** Включает `camera.h`. |
| `localgen.cpp` | `buildLocalScene()` — детерминированная генерация: звезда, планеты + кольца + луны, пояс, туманность, корабли, радиоисточники; стартовая ориентация носа через `localSetForward` + Z-разброс тел/пояса (объём для 3D, §5.13). **Пост-проход: не-боевым NPC даёт стартовый рейс на рынок (`errand`/`errandBody`) + рандомит `trafficTimer` (§5.13.9).** **Свободная функция `rockAppearance(element,…)`: классифицирует астероид по атомному номеру/`metallicTrait` элемента → 4 класса пород (лёд/углерод/металл/силикат) → базовый RGB + `spec`; в цикле пояса запекается в `rock.r/g/b/spec` с яркостной вариацией из ДЕТЕРМИНИРОВАННОГО хэша индекса (НЕ из `lrng` — поток генерации не сдвинут, строго аддитивно §2.3/§5.13.15).** **§5.13.16: классификация вынесена в единый ЧИСТЫЙ `rockClass(int element)→enum RockClass` (лёд/углерод/металл/силикат по атомному номеру/`metallicTrait`), `rockAppearance` теперь зовёт его через `switch` — облик байт-в-байт; тот же `rockClass` кормит и добычу (`localsim`), т.е. вид и ценность породы всегда согласованы.** **§5.13.20: пояс перестал быть состав-слепым — до цикла камней (без `lrng`) считаются пулы элементов по классам + веса классов `w[4]` из состава системы (металличность→металл, бедность→лёд, `resourceFocus`→свой класс); в цикле класс/элемент выбираются по ДЕТЕРМИНИРОВАННОМУ хэшу `i`, а базовые `irand`-розыгрыши элемента сохранены 1-в-1 (стрим-якорь) ⇒ спавн станции/трафика/радио байт-в-байт (§2.3), меняется лишь состав пояса.** **§5.13.22: ЗАПАС руды камня `rock.ore` теперь = базовый `frand(20,120)` × `oreRichnessMult(rockClass(elem), star.miningRichness)` (`local.h`) — класс (металл 1.5 / силикат 1.0 / лёд 0.9 / углерод 0.8, ось «тоннаж» ≠ «темп» §5.13.16) × насыщенность системы (прежде влияла лишь на макро-`mining.cpp`); множитель на УЖЕ разыгранный `frand` ⇒ ноль новых `lrng`, поток цел, среднее ≈1.0 (перераспределение, не инфляция).** **§5.13.24: в цикле пиратов — метка `wanted`+сумма `wantedBounty` (250..900, шаг 25) из ДЕТЕРМИНИРОВАННОГО хэша `(starIndex,p)` (~38% пиратов), ноль `lrng` ⇒ поток спавна побитово прежний (§2.3); только награда, урон/ИИ пирата не тронуты (combat-инвариант).** |
| `localsim.cpp` | `updateLocalScene()` — физика/орбиты/варп + **3-осевое вращение (Родриг + Грам–Шмидт по `pfwd`/`pup`, §5.13)** + смышлёный ИИ/бой/щиты/смерть/лут/добыча/радио-клейм/частицы за кадр. **Живой трафик (§5.13.9): конечный автомат рейса не-боевых NPC (круиз к рынку → стоянка → отлёт), `edgeR`, деспаун ушедших `(L)` + прилёты новых торговцев `(L2)` до `TRAFFIC_CAP`; пираты — прежнее блуждание.** **Читаемость трафика (§5.13.10): `emitWarp` — циан-сигнатура «прыжка» (кольцо+ядро+искры) на прилёте `(L2)`/отлёте `(L)`; пуфф форсажа при `errand 1→2` у причала. Всё локальный seeded RNG, под `FX_MAX`.** **Бедствие конвоя (§5.13.11): пре-проход сброса флагов → в проходе ИИ пират `aiState==1` на не-пирата помечает жертву `underAttack` + себя `threatConvoy`; на фронте (0→>0) — тост `CONVOY RAID` (с дебаунсом `distressCooldown`); в блоке убийства `(E)` — пейофф `CONVOY SAVED` (бонус+исследование+rep-бамп) за пирата с `threatConvoy`. Строго аддитивно, пираты не ослаблены.** **Эскорт-патрули (§5.13.12): `CK_PATROL` ищет налётчика по СЫРЫМ позициям (пират в `PATROL_DISTRESS_R²` от патруля, чей ближайший торговец/civilian в `RAID_NEAR²`) → приоритетный перехват + `defending=true`; иначе фолбэк на ближайшего пирата в `AW2`. Огонь всё так же гейтится `WEAPON_RANGE` — расширена лишь осведомлённость, урон/агрессия пиратов не тронуты.** **Write-back в макро (§5.13.14): в блоке убийства игроком `(E)` сбитый борт-зеркало (`agentIndex>=0`, `≠ playerAgent`) даунгрейдит свой макро-агент через `downgradeAgentToEscapePod` — постоянное последствие в персистентном мире; репрессия фракции уже применена выше по блоку, здесь ТОЛЬКО даунгрейд (без двойного счёта отношений).** **Добыча по составу (§5.13.16): темп извлечения = `MINE_RATE_BASE(9.0)·rockYieldMult(rockClass(rk.element))` — металл ×1.60, лёд ×1.25, углерод ×0.65, силикат ×1.00 (=прежний флэт-темп ⇒ нулевой регресс); стартовый тост `MINING <symbol> <CLASS>`. Combat/AI/генерация не тронуты.** **Продажа груза зеркалом-торговцем (§5.13.18): на переходе швартовки `errand 0→1`, если `kind==CK_TRADER && agentIndex>=0 && cargo непуст`, зовётся `localDockSellCargo(game,agentIndex,scene.starIndex)` (детерминированный макро-`sellCargo`) — реальная продажа в персистентном мире (SELL-ONLY, без rng/journey; макро-сим заморожен ⇒ нет дубля); дебаунс-тост `TRADER SOLD <SYM>` через `tradeCooldown`, счётчик `tradesExecuted`. Строго аддитивно: `agentIndex<0`/не-торговцы не тронуты.** **Награда за розыск (§5.13.24): в блоке убийства `(E)`, если сбитый — пират с `wanted`, начисляется `wantedBounty` ПОВЕРХ базовой награды и `CONVOY SAVED` + тост `BOUNTY CLAIMED +N CR`; деньги ограничены (soak «money finite» цел), число убийств не меняется (`craftDestroyed` цел). Строго аддитивно — пираты не ослаблены.** **Свидетельство пиратства (§5.13.26): в ЕДИНСТВЕННОЙ точке смерти NPC-vs-NPC (прямой DPS пирата, `localsim.cpp:662`, прежде лишь `spawnWreck` «без награды/фракций») — два строго аддитивных последствия: (A) сбитый борт-зеркало (`o.agentIndex>=0`, `≠ playerAgent`) даунгрейдит свой макро-агент через `downgradeAgentToEscapePod` (та же семантика, что player-kill §5.13.14; кредиты/глобальный RNG не трогаем) ⇒ закрыта асимметрия «write-back только у игрока»; (G) пират, убивший НЕ-пирата, попадает в розыск в реальном времени (`wanted=true`, `wantedBounty` 300→+200 до 1500; гейт `c.kind==CK_PIRATE` не метит патруль-мстителя), переиспользуя поля §5.13.24 ⇒ розыск стал ещё и эмерджентным. Ноль RNG/новых полей; все числовые счётчики soak побитово целы, пробник `npcWriteBack=ok`.** **Охота патруля на розыскного (§5.13.28): в ветке `CK_PATROL` перед сканом налётчика §5.13.12 — приоритет-0 скан ближайшего `wanted`-пирата в `PATROL_DISTRESS_R` (без условия «у беззащитного борта»); при находке цель=розыскной + `defending=true`, иначе прежний налётчик→фолбэк (обёрнут в `else`). Огонь по-прежнему заперт `WEAPON_RANGE`; читаем `o.wanted`, ноль RNG/новых полей; правится ТОЛЬКО выбор цели ПАТРУЛЯ (агрессия/урон пирата целы). Ветка `CK_PATROL` в soak не исполняется ⇒ бейзлайн побитово цел; пробник `patrolHuntsWanted=ok`.** **Защита стаи (§5.13.30): в ХВОСТЕ ветки `CK_PIRATE` — если пират сам без цели (`atkCraft<0 && !atkPlayer`, простаивает), ищет рядом (в `PATROL_DISTRESS_R`) патруль-ОХОТНИК (`CK_PATROL` с живым `wanted`-пиратом в его `PATROL_DISTRESS_R`) и берёт его ЦЕЛЬЮ (`atkCraft=патруль`) — стая обороняет изгоя. Охота ВЫВОДИТСЯ ЗАНОВО из `kind/wanted/позиций` (как рендер §5.13.29), НЕ из кадрового `defending` ⇒ независимо от порядка бортов/своп-попа. Строго аддитивно, комбат-инвариант УСИЛЕН: гейт `atkCraft<0` не уводит пирата с игрока/торговца, лишь простой пират → атакующий закон (агрессии больше, охота ТРУДНЕЕ). Патрулей в soak нет ⇒ no-op по построению, бейзлайн §5.13.28/29 побитово цел; пробник `packRalliesOutlaw=ok`. Ноль RNG/новых полей (реюз `PATROL_DISTRESS_R`).** **Подмога закона (§5.13.32): в ветке `CK_PATROL` МЕЖДУ приоритетом-0 (§5.13.28-охота) и сканом налётчика (§5.13.12, обёрнут в ещё один `else`) — приоритет-0.5: свободный патруль (СВОЕГО `wanted` не нашёл) берёт целью ближайшего пирата в `PATROL_DISTRESS_R`, который сам в `PATROL_DISTRESS_R` ДРУГОГО патруля-ОХОТНИКА `q` (у `q` живой `wanted`-пират в радиусе) ⇒ наряд сходится на облепленного охотника. «`q` охотится» ВЫВЕДЕНО заново из `kind/wanted/x/y/z` (приём D, как рендер §5.13.29 / rally §5.13.30), НЕ из кадрового `q.defending` ⇒ независимо от порядка бортов/своп-попа. Строго аддитивно: правило целиком в выборе цели ПАТРУЛЯ, у пирата ничего не читается/пишется, цель всегда `CK_PIRATE` (без friendly-fire) ⇒ агрессии закона больше, охота ТРУДНЕЕ. Тир требует ≥2 патрулей ⇒ в соаке (0 патрулей) и обоих прежних пробниках (по 1) no-op ПО ПОСТРОЕНИЮ, бейзлайн побитово цел; пробник `patrolBacksUpSwarmed=ok`. Ноль RNG/новых полей (реюз `PATROL_DISTRESS_R` + маркер `defending`).** **Облава (§5.13.34): файл-статик `manhuntSpeedMult(scene,ci)` (рядом с `rodriguesRotate`, до `updateLocalScene`) ПЕРЕВЫВОДИТ P0-скан §5.13.28 дословно (ближайший живой `wanted`-`CK_PIRATE` в `PATROL_DISTRESS_R`, приём D ⇒ цель = фактический `atkCraft` патруля ПО ПОСТРОЕНИЮ, ибо P0 вытесняет младшие тиры); гейт `CK_PATROL && defending` (player-override/бегство гасят `defending` ⇒ отсечены), множитель `1.0 + MANHUNT_SPEED_GAIN·clamp((wantedBounty−250)/1250,0,1)` врезан в `effMax` кадрового движения — патруль-охотник тем резвее, чем крупнее награда. Правится ТОЛЬКО скорость патруля (не выбор цели ⇒ рендер-лямбды §5.13.29/31/33 верны даром; скорость/агрессия пирата целы). Ноль RNG/новых полей; ветка `CK_PATROL` в соаке не исполняется ⇒ бейзлайн побитово цел, пробник `manhuntScalesWithBounty=ok`.** **§5.13.36: благодарность закона — в блоке убийства игроком `(E)`, ПОСЛЕ начисления `BOUNTY CLAIMED`, если сбитый — `wanted`-`CK_PIRATE`, ре-деривуется (приём D, ИНВЕРТИРОВАННЫЙ P0-скан: убитый уже `hullHP≤0`, потому ищем ближайший `defending`-`CK_PATROL` в `PATROL_DISTRESS_R`, для которого мёртвый был ближайшей ЖИВОЙ розыскной дичью — ни один другой живой `wanted` не ближе) патруль-охотник, и его фракции даётся `+6` rep через `game.adjustFactionRelation` ПОВЕРХ выплаты §5.13.24 (+ новость + тост `LAW GRATEFUL +REP` + тряска). Строго аддитивно — блок после смерти пирата, урон/агрессия/аптайм не тронуты (комбат-инвариант §0.2-G цел); ноль RNG/новых полей (реюз `PATROL_DISTRESS_R`+`defending`+`wanted`+`faction`). Патрулей в соаке нет ⇒ `gratPatrol=−1` ⇒ no-op по построению, бейзлайн §5.13.35 побитово цел; пробник `lawGratitude=ok`.** **§5.13.38: коп-килл — в блоке смерти NPC-vs-NPC, ПОСЛЕ §5.13.26G-бампа, если розыскной пират `c` добил `CK_PATROL` `o`, ПЕРЕВЫВОДИТСЯ (ПРЯМОЙ P0-скан, приём D — зеркало `manhuntSpeedMult`: `c` — ближайший живой `wanted`-`CK_PIRATE` к `o` в `PATROL_DISTRESS_R`, ни один другой `wanted` строго не ближе), был ли `c` P0-дичью павшего патруля; при да `wantedBounty += COP_KILL_BOUNTY` (кламп 1500) СВЕРХ обычного +200. `cWasWantedBefore` захвачен ДО §5.13.26G-бампа (тот ставит `c.wanted=true`) ⇒ первопреступник, ставший `wanted` этим убоем, отсечён; геометрия `cWasQuarry` отсекает рядового таран-стаи §5.13.30 (не ближайший); намеренно БЕЗ гейта `o.defending` (`c` — P0-дичь по геометрии, гейт потерял бы «загнал до бегства, добил»). Строго аддитивно (награда лишь ↑ ⇒ §0.2-G усилен: горячее облава = больше боя), ноль RNG/новых полей (1 константа + 1 локальный bool). Патрулей в соаке нет ⇒ гейт `o.kind==CK_PATROL` no-op ПО ПОСТРОЕНИЮ, бейзлайн §5.13.36 побитово цел; пробник `copKillRaisesBounty=ok`.** **§5.13.40: месть фракции — в блоке §5.13.36 (`gratPatrol≥0`) ЗНАК rep берётся из тира `classifyFactionRelationTension(game.factionRelation(playerFaction, patFac))`: Enemy/Hostile (≤ −48) ⇒ `dRep = −LAW_GRATITUDE_REP` (тост `FACTION REPRISAL -REP`, news kind 3), иначе `+LAW_GRATITUDE_REP` (прежнее `LAW GRATEFUL +REP`, §5.13.36); тот же инверт-P0-скан `gratPatrol`, реюз классификатора панели §5.13.37/прицела §5.13.39. Строго аддитивно (лишь реп; пират мёртв ⇒ §0.2-G цел), ноль RNG/новых полей; `factionRelation` двойне-защищён (0 ⇒ Neutral ⇒ +6), диагональ=128=Ally + гейт `A==B` ⇒ патруль своей фракции ПОБИТОВО §5.13.36. Патрулей в соаке нет ⇒ no-op по построению; пробник `factionReprisal=ok`.** **§5.13.42: закон-по-стоянию — в ветке `CK_PATROL` СУЩЕСТВУЮЩИЙ гейт предпочтения игрока расширен с `c.hostile` до `(c.hostile || factionHuntsPlayer)`, где `factionHuntsPlayer` = тир Enemy/Hostile (реп ≤ −48) из `classifyFactionRelationTension(factionRelation(playerFaction, c.faction))` (тот же классификатор §5.13.37/39/40; classify пропускается, если патруль уже `hostile`). Так патруль ВРАЖДЕБНОЙ игроку фракции берёт ИГРОКА целью БЕЗ свежей провокации — «месть фракции» §5.13.40 получила зубы. Реюз гейта `pd2 < atkBest` ⇒ патруль бросит дичь ради игрока лишь если игрок БЛИЖЕ (как уже делал спровоцированный патруль) ⇒ не новый тир-приоритет, охота §5.13.28 при более близком пирате цела. Строго аддитивно (§0.2-G усилен: патруль, отвлечённый на игрока, даёт пирату аптайма больше; пираты не тронуты), ноль RNG/новых полей; `factionRelation` двойне-защищён (мусор/своя фракция ⇒ Neutral ⇒ false). Ветка `CK_PATROL` в ОСНОВНОМ soak-цикле не бежит (18 воен-агентов-гарнизонов на `controlledStars`, разбросанных по кластеру-1200, disjoint фикс-маршруту soak ⇒ `patrols=0` на всех 10 — ЗАМЕРЕНО; co-location гейт `currentStar==звезда`, `localgen.cpp:438`) ⇒ no-op, бейзлайн §5.13.41 побитово цел; пробник `lawHuntsHostile=ok` инъектит синтетический `CK_PATROL` ⇒ активное покрытие ветки.** **Пломбировка цели для рендера (§5.13.43): сразу после присвоения `aiState` доселе МЁРТВОЕ поле `aiTarget` заполняется read-only (`aiState==1` ? (цель-игрок ? `LOCAL_TARGET_PLAYER` : индекс борта) : −1). НИ ОДНО sim-решение его не читает (раньше был только `init=-1` в `localgen`) ⇒ 0 влияния на поведение ⇒ соак побитово; единственный потребитель — cue `HUNTING YOU` в `localdraw` §5.13.43.** **Вендетта закона (§5.13.44): поле `aiTarget`, что §5.13.43 начал заполнять read-only, ВПЕРВЫЕ ПОТРЕБЛЯЕТСЯ симом — дорастает из чисто-рендерного до входа СИМ. Файл-статик `vendettaSpeedMult(game,scene,ci)` (рядом с `manhuntSpeedMult`): для `CK_PATROL` с `aiTarget==LOCAL_TARGET_PLAYER` и чужой живой фракцией множитель `1.0 + VENDETTA_SPEED_GAIN·clamp((−48−rep)/80,0,1)` врезан в `effMax` кадрового движения — патруль тем резвее, чем глубже минус его фракции к игроку (−48 ⇒ ×1.0, −128 ⇒ ×1.6). Движ-цикл — ОТДЕЛЬНЫЙ проход ПОСЛЕ цикла ИИ ⇒ `aiTarget` уже закоммичен. Дизъюнктность с облавой §5.13.34 по построению: (1) `manhuntSpeedMult` гейтится `c.defending`, а player-override §5.13.42 гасит `defending`; (2) добавлен явный guard `if (aiTarget==LOCAL_TARGET_PLAYER) return 1.0` в `manhuntSpeedMult` ⇒ ≤1 множителя >1 на борт. Правится ТОЛЬКО скорость патруля (не выбор цели/урон; пират не тронут ⇒ §0.2-G усилен: закон резвее гонит игрока = больше боя). Ноль RNG/новых полей; ветка `CK_PATROL` в соаке не исполняется ⇒ бейзлайн побитово цел, пробник `vendettaScalesWithStanding=ok`.** **§5.13.46: СТАЯ ЗАКОНА — в ветке `CK_PATROL` ПОСЛЕДНИМ тиром (сразу после player-override §5.13.42; `factionHuntsPlayer`/`atkPlayer`/`atkBest`/`pd2`/`AW2` в области видимости): если Enemy/Hostile-стоящий патруль ВИДИТ игрока (`pd2<AW2`), но §5.13.42 не взял игрока (`!atkPlayer` — пират ближе), И в `PATROL_DISTRESS_R` есть ДРУГОЙ живой `CK_PATROL` чужой фракции, тоже Enemy/Hostile-стоящей и тоже видящей игрока (игрок в `AW2` соседа), — патруль присоединяется к охоте на игрока (`atkBest=pd2; atkPlayer=true; atkCraft=-1; c.defending=false`). `atkBest=pd2` как в §5.13.42 (иначе `tdist=√atkBest` на fire-range-чеке взял бы дистанцию до ПИРАТА). Приём D: соседская расположенность выведена из ЖИВЫХ `kind`/`faction`/`hullHP`/позиций (НЕ из кадрового `aiTarget`/`defending`) ⇒ два расположенных патруля коммитятся КАЖДЫЙ по расположенности другого ⇒ порядок-независимо, без дедлока/рекурсии; нужна ПАРА (одиночка гонит пирата §5.13.42). Зеркало набата §5.13.30 / подмоги §5.13.32, но по ИГРОКУ; §0.2-G усилен (патруль ушёл с пирата на игрока ⇒ пирату аптайма больше, игроку боя больше), строго аддитивно (нет пары / реп>−48 / игрок вне AW2 ⇒ прежнее). Ноль RNG/новых полей; ветка `CK_PATROL` в соаке не исполняется ⇒ бейзлайн §5.13.44 побитово цел, пробник `lawPackHuntsPlayer=ok`.** |
| `localdraw.cpp` | `renderLocalScene()` — оба режима (ветка `view.perspective`): **3D-кокпит от первого лица (перспектива, глаз в корабле) + LOD-скайбокс кластера** / орто-карта сверху; сцена + частицы + HUD/радар/тряска поверх `UI::` и проекции `camera.h`. В кокпите свой корабль не рисуется (мушка+прогрейд вместо стрелки); частицы клампятся по типу, чтобы близкий выхлоп не заливал экран. **Планеты/луны в перспективе — освещённые ray-sphere сферы (`renderBodySphere`: свет от звезды в 0,0,0 → терминатор/фазы, поверхность по типу тела, атм. ободок); мелкие/орто-карта — диск-фолбэк (§5.13.4). Кольца газ-гигантов — попиксельный ray↔plane аннулюс в том же `renderBodySphere`: щели Кассини, тень планеты на кольцах (полутень), grazing-opacity, композит по глубине сфера↔кольцо (§5.13.5). Звезда (`renderStarPlasma`) отполирована: спектральный контраст O→M, хромосферный ободок, протуберанцы короны, пятна-сунспоты, варп 2-го порядка (§5.13.6). **Вспышки-СОБЫТИЯ короны (§5.13.13): две активные области дрейфуют по лимбу (угол растёт с `fxClock`) и периодически эруптируют — `flareBoost` даёт всплеск яркости (×до 2.2), локальный вырост короны (+30% reach) и горячий бело-голубой сдвиг; детерминированно, нулевой регресс при `fxClock=0`, `std::exp` только при `cosang>0.6`.** Туманность-фон в перспективе — попиксельное газовое поле на небесной сфере (`renderNebula`: многооктавный шум по лучу + доменный варп + градиент «банки», полупрозрачная, дрейф `fxClock`); орто-карта — прежний плоский тон + 3 блоба, побитово цела (§5.13.8). §5.13.19: + **пылевые полосы** (ОТДЕЛЬНЫЙ домен-варп, сумма синусов, тьма у нулевого контура: `dens*=0.20+0.80·lane` гасит газ до ~20%) + **эмиссионные ядра** (`core=dens³` → ярче альфа/белизна ЛИШЬ у пиков плотности), draw-only, орто-путь цел.** **Читаемость трафика (§5.13.10): пульсирующее янтарное кольцо-швартовка для `errand==1` (≠ синий щит/циан-рамка); HUD-тальник `IN <n> DOCK <n>` над `SIG` (панель радара `rh` 158→178).** **Бедствие конвоя (§5.13.11): для `underAttack` — быстрый красный «SOS»-пульс (кольцо у борта) + краевой SOS-маркер ближайшей жертвы за кадром; панель цели-жертвы — SOS-рамка + красная метка + чип `SOS` (все SOS-красные `238,88,82`, ≠ янтарь/циан).** **Эскорт-патрули (§5.13.12): для `defending` — зелёный «эскорт»-пульс (кольца `sz+8/sz+13`, вне циан-рамки цели `sz+6`) + зелёный краевой маркер ближайшего защитника за кадром; панель цели-защитника — зелёная рамка/метка + чип `ESCORT` (всё зелёное `90,220,130` = «свои»).** **Типы пород (§5.13.15): `renderRockLit` добавляет зеркальный блик Блинна–Фонга — только для блестящих материалов (`rk.spec>0`, лёд/металл) и на освещённой стороне (свет из 0,0,0: `L=−S/|S|`, `V=−dir`, `H=norm(L+V)`, `pow(N·H,24)·spec·lightF`, аддитивно к диффузному, почти-белый с 0.28 базового тона); матовые (углерод/силикат, spec≈0) блика не дают. Диск-фолбэк и базовый цвет читают `rk.r/g/b`.** **§5.13.16: живой HUD-оверлей добычи `MINING <CLASS> +<accum>` (класс из `rockClassName(rockClass(...))`) — подтверждает состав во время бурения.** **§5.13.17: майнинг-лазер из кокпита (блок «(7a) ЛУЧ ДОБЫЧИ» после цикла снарядов) — гейт `view.perspective && miningRock валиден && d²≤(radius+MINE_RANGE)² && !occ(...)` (ровно те условия, при которых `localsim` извлекает руду); «дуло» = `px+pfwd·5−pup·1.6` (как вспышка выстрела), ореол окрашен в цвет КЛАССА `rk.r/g/b` (связка вид§5.13.15↔выход§5.13.16), сердцевина к бело-горячему, мягкая толщина ⟂ лучу через нормаль экранного отрезка, точка удара на глыбе + вспышка у дула; пульс `sin(fxClock·20)` — детерминированно, без RNG (§2.3). Чистый draw-only: симуляция не тронута, вне зоны/не бурим/орто-карта — не рисуется (нулевой регресс).** **§5.13.21: тень колец на планете (обратная к §5.13.5-тени тела на кольцах) — в `renderBodySphere` из освещённой точки поверхности S луч к звезде (0,0,0); если пересекает аннулус колец между поверхностью и звездой, гасит `diff` (до ~85%) по ТОЙ ЖЕ плотностной модели колец (кромки/Кассини/полосы ⇒ сквозь щель тени нет). Вставка между клампом `diff` и `lightF`, под гейтом `hasRing` ⇒ у не-кольцевых тел `diff` цел (нулевой регресс); детерминировано, без RNG/`fxClock`, перспектива-only.** **§5.13.23: HUD-читаемость запаса руды — индикатор `MINING <CLASS> +<accum>` дополнен `  ORE <mr.ore>` (видно истощение к «ROCK DEPLETED»), подсказка `PRESS M TO MINE` → `... - <CLASS>  ORE <tr.ore>` для `scene.rocks[minePrompt]` (класс+запас цели ДО бурения ⇒ выбор богатой глыбы, пейофф §5.13.22); янтарь `%.0F`, баунд-чек `minePrompt<rocks.size()`, чистый draw без RNG.** **§5.13.25: розыск в кокпите — три золотых сигнала (`255,205,60`) для `c.wanted`: (1) кольцо-розыск у борта `sz+9/sz+14` (вне cyan-рамки `sz+6` и зелёного эскорта `sz+8/sz+13`), пульс `sin(fxClock·5.2)` ≠ швартовка 3.0/эскорт 4.5/SOS 6.0; (2) панель цели — золотая рамка + метка + чип `WANTED` (после `LOCK`) + строка `BOUNTY %.0F CR` под корпусом (`ph += 14`); ветка `wnt` в тернаре цвета стоит ПЕРЕД фолбэком `t.hostile?red`, т.к. розыск только у пиратов (взаимоискл. с `sos`/`esc`); (3) краевой маркер `nearWanted` (`wanted && hullHP>0`) — параллель `nearVic`/`nearDef`. Чистый draw-only, `wanted` только читается.** **§5.13.27: раскраска контактов радара по состоянию — цикл блипов (`localdraw.cpp:1979`), прежде `c.hostile?red:серый`, теперь читает живые поля борта лестницей приоритета `wanted`→золото `255,205,60` (пульс 5.2) / `underAttack`→SOS-красный (6.0) / `defending`→зелёный `90,220,132` (4.5) / прочий `hostile`→ровный красный / иначе серый; язык и темпы пульса = кабинные cue §5.13.11/.12/.25, ветка `wanted` первой (розыскной пират одновременно `hostile`), альфа-пульс 150→255 со сдвигом `double(i)`. Чистый draw-only: ноль новых полей/RNG/правок sim; радар (1400 LU) показывает эмерджентный розыск §5.13.26(G) на весь диапазон, а не только у наведённой цели.** **§5.13.29: вектор перехвата закона — `renderLocalScene` заново выводит §5.13.28-погоню лямбдой `pursuitQuarry(ci)` (повтор приоритет-0 скана §5.13.28: ближайший живой `wanted`-пират в `PATROL_DISTRESS_R` от `defending`-патруля, строгий `<`) ⇒ т.к. приоритет-0 перебивает всё, у патруля цель ГАРАНТИРОВАННО этот розыскной = рендер совпадает с sim'ом ПО ПОСТРОЕНИЮ (без чтения приватного `atkCraft`/нового поля). Три золотых cue (`255,205,60`): (1) 3D «бегущий» пунктир + 2-линейная стрелка патруль→розыскной (блок «(7б)» после цикла снарядов; гейт «оба конца видимы и не за `occ`», альфа-пульс `sin(fxClock·5.2)`); (2) на радаре — золотой отрезок блип-патруля↔блип-розыскного (лямбда `radarXY`, отрисован ДО цикла блипов ⇒ блипы поверх); (3) строка `IN PURSUIT <label>` под корпусом в панели наведённого `defending`-патруля (`ph += 14`). Читаются лишь `x/y/z/kind/wanted/defending/hullHP`; чистый draw-only, ноль RNG/новых полей/правок sim (в соаке `renderLocalScene` не зовётся ⇒ бейзлайн побитово цел).** **§5.13.31: рендер набата стаи — лямбда `rallyTarget(ci)` (idle-гейт + скан набата §5.13.30 заново из живых полей) ⇒ (7в) горячо-оранжевый (`255,120,32`) «бегущий» пунктир + стрелка пират→патруль-охотник (пульс 7.0/марш 46 — быстрее/жарче золота), оранжевая связка на радаре (после золотой ⇒ поверх) и строка `UNDER PACK Xn` в панели патруля под `IN PURSUIT`. Чистый draw-only, ноль RNG/полей/правок sim.** **§5.13.33: рендер подмоги закона — лямбда `backupTarget(ci)` (зеркало §5.13.32: свободный патруль без своего `wanted` → ближайший пират, увязший у ДРУГОГО патруля-охотника; возвращает индекс ПИРАТА, НЕ гейтит на `defending`) ⇒ (7г) сине-белый (`120,180,255`) «бегущий» пунктир + стрелка респондер-патруль→пират (пульс 4.2/марш 28 — холоднее/размереннее оранжа и золота: дисциплина, не ярость), синяя связка на радаре (после оранжевой ⇒ поверх) и строка `BACKUP INBOUND Xn` в панели ЗАГНАННОГО охотника (счёт по предикату «цель подмоги в `PATROL_DISTRESS_R` от нашего охотника», НЕ равенством — `backupTarget` возвращает пирата, а не охотника). Один кадр = весь закон-цикл гон→набат→подмога. Чистый draw-only, ноль RNG/полей/правок sim.** **§5.13.35: ЖАР ОБЛАВЫ — золотой вектор погони §5.13.29 и строки панели `IN PURSUIT`/новая `MANHUNT %.0F CR` модулируются `heat=clamp((дичь.wantedBounty−250)/1250,0,1)` (ПОБИТОВО тот же, что sim-скорость §5.13.34, `manhuntSpeedMult`): при потолке награды тон калится к бело-калёному (`g` 205→255, `b` 60→255), базовая альфа ярче (+50), марш штрихов ×1.5 (34→51); `heat=0` (награда у пола) ⇒ РОВНО прежнее золото/марш §5.13.29 (`int(0·x)==0` ⇒ нулевой регресс). `qc`/`qy` берутся из `pursuitQuarry` (приём D — индекс дичи уже сведён), строка `MANHUNT N CR` кажет награду ГЛАЗАМИ ЗАКОНА (`ph += 28`); alpha может дойти до 270 при heat=1, но `rgba()` — кламп-хелпер (§8-нота ревью). Ноль RNG/полей/правок sim.** **§5.13.37: строка `STANDING <WORD> ±N` в панели цели (репутация с фракцией, на лету из `game.factionRelation(playerFaction, t.faction)`, классификатор `classifyFactionRelationTension` — единый источник порогов faction.cpp); словарь+цвет по тирам Enemy…Ally, `ph += 14`; показывается лишь для валидной чужой фракции (пираты бесфракционны ⇒ невидима). Draw-only: ноль RNG/полей/ребейка.** **§5.13.39: `STANDING`-тир красит и ПРИЦЕЛ — файловый хелпер `standingCue(const Game&, int targetFaction, SDL_Color&)` возвращает `true`+цвет тира лишь для НЕ-нейтрального стояния с ДРУГОЙ фракцией (тот же `classifyFactionRelationTension`, что и панель §5.13.37; палитра-зеркало), иначе `false`. Применён в (a) рамке цели (была `P.cyan` ⇒ теперь тир-цвет, жёсткий Tab-лок держит альфу 170) и (b) курсовом лид-пипе упреждения (был `tc.hostile?амбер-красный:янтарь` ⇒ тир важнее сырого `hostile`, альфа 205/185 сохранена). Пираты бесфракционны (`faction<0`) ⇒ `standingCue` даёт `false` ⇒ их прицел ПОБИТОВО прежний (§0.2-G цел). Draw-only: ноль RNG/полей/ребейка; `factionRelation` двойне-баунд-безопасен.** **Cue «HUNTING YOU» (§5.13.43): патруль с `aiState==1 && aiTarget==LOCAL_TARGET_PLAYER && !hostile` (⟺ сработал override §5.13.42 по СТОЯНИЮ — единственный путь `atkPlayer=true` в ветке патруля) несёт над бортом багровую подпись + самый быстрый пульс-кольца (темп 7.4, ярче всех статусных) цветом `standingCue` §5.13.39 (fallback `P.red`), за кадром — краевой маркер `nearHunt` (disjoint со старым `nearHos` по флагу `hostile` ⇒ ноль двойного маркера + чистый хэндофф, когда охотник флипает в hostile). Draw-only: читает уже заполненный в симуляции `aiTarget`, ноль RNG/полей/ребейка.** **§5.13.45: тот же «HUNTING YOU» cue калён ЖАРОМ ВЕНДЕТТЫ — файл-хелпер `vendettaHuntColor(game,c,heat&)` выводит `heat=clamp((−48−rep)/80,0,1)` (побитово == `vendettaSpeedMult` §5.13.44) draw-side и анилит тон к бело-калёному (`r+=heat·40`, `g/b+=heat·150`; `rgba@39` клампит [0,255]); пульс `7.4+heat·3.7` и база альфы `110+heat·50` растут с враждой. On-screen cue И off-screen `nearHunt`-маркер берут ОДИН хелпер ⇒ тон не разъедется (анти-дрейф). heat=0 при реп −48 ⇒ РОВНО §5.13.43 (нулевой регресс). Draw-only, ноль RNG/полей/ребейка.** **§5.13.47: ВИДНАЯ СТАЯ — две draw-only подсказки поверх §5.13.46. (1) Экранный баннер сверху-центр `PACK HUNT X%d` (y=46, ниже тоста y=24) — гейт N≥2 патрулей, что СЕЙЧАС охотятся на игрока по стоянию (общий файл-предикат `huntsPlayerByStanding`: `CK_PATROL && aiState==1 && aiTarget==LOCAL_TARGET_PLAYER && !hostile && hullHP>0`), тоном самого ГОРЯЧЕГО жара `vendettaHuntColor` в стае (worst-enemy задаёт накал; `heat>=maxHeat` с maxHeat=0 ⇒ packCol всегда соответствует пиковому охотнику), темп/яркость `7.4+heat·3.7`/`150+heat·50+pl·55` == on-screen cue §5.13.45; одиночку НЕ дублируем (её несёт per-craft §5.13.43). (2) НИТИ стаи на радаре — от блипа КАЖДОГО охотника (N≥1) к центру-игроку тоном его жара `vendettaHuntColor`; тот же `radarXY`, что блипы/связки (`dd>RANGE ⇒ kk=R/dd ⇒ точка РОВНО на ободе` ⇒ нить не улетает за радар), отрисованы ПОСЛЕ связок подмоги §5.13.33 и ДО цикла блипов ⇒ контакты остаются сверху; заодно чинит слепое пятно тускло-серого `!hostile`-блипа §5.13.27 (неспровоцированный охотник был неотличим от мирного трафика). Обе подсказки ре-выводят «кто охотник» из ЖИВЫХ полей ОДНИМ предикатом+хелпером с per-craft cue §5.13.43/.45 и блипом ⇒ анти-дрейф (цвета/гейт не разъедутся). Draw-only, ноль RNG/полей/ребейка; §0.2-G тривиально (лишь чтение репутации).** Читает `cluster.h` для скайбокса. §5.13. |
| `soak_test.cpp` | Вне игры (своя `main`, линкуется с `LIBSOURCES`). Детерминированный soak-прогон локального режима под ASan; цель `make soak` (§6.5). **§5.13.16: инвариант класса/выхода породы (класс `∈[ROCK_SILICATE,ROCK_METAL]`, `rockYieldMult` конечен и >0) по всем породам каждой стартовой сцены; сводка `rocks=`/`shiny=`.** **§5.13.18: детерминированный sell-write-back-пробник (свежий `Game`, зеркало-торговец с грузом швартуется → ассерт «груз↓ + кредиты↑ + сделка прошла», поле `sellWriteBack=`), инвариант финитных денег всех агентов, счётчик `trades=` (органические сделки за прогон).** **§5.13.26: детерминированный пробник `npcWriteBack=` — в свежем `Game` пират добивает зеркало-торговца (игрок отставлен на 100000 LU и не стреляет ⇒ смерть чисто NPC-vs-NPC), ассерт даунгрейда макро-агента в спас-капсулу (симметрия player-kill-пробнику §5.13.14). Числовой бейзлайн soak побитово цел; поле строго аддитивно.** **§5.13.28: детерминированный пробник `patrolHuntsWanted=` — свежий `Game`, две подпроверки с пиньнутыми позициями (игрок отставлен, не стреляет): (A) розыскной-дальше побеждает простого-ближе (`wantedOverDistance`), (B) без розыска бьётся ближайший (`fallbackNearest`) ⇒ фолбэк §5.13.12 цел. Единственное headless-покрытие ИИ патруля (ветка `CK_PATROL` в основном прогоне не исполняется); числовой бейзлайн побитово цел.** **§5.13.30: детерминированный пробник `packRalliesOutlaw=` — свежий `Game`, три борта с пиньнутыми каждый кадр позициями (ноль дрейфа/RNG, огромные корпуса ⇒ индексы стабильны): патруль в 0, розыскная приманка на 1000 LU (делает патруль охотником), простаивающий пират-стайник на 1100 LU (вне своей осведомлённости 750, в радиусе набата 1400). Ассерт на `aiState` (стайник за `WEAPON_RANGE` ⇒ урона нет, но захват цели виден): (A) приманка розыскная ⇒ стайник берёт патруль (`aiState==1`, `rallyFires`); (B) приманка не розыскная ⇒ набат заперт гейтом (`aiState==0`, `gatedOnHunt`). Единственное headless-покрытие правила (в основном прогоне патрулей нет ⇒ блок no-op); числовой бейзлайн §5.13.28/29 побитово цел.** **§5.13.32: детерминированный пробник `patrolBacksUpSwarmed=` — свежий `Game`, ЧЕТЫРЕ борта с пиньнутыми каждый кадр позициями (огромные корпуса ⇒ индексы стабильны): охотник A в (0,0,0); `wanted`-изгой на (300,0,0) — <1400 от A ⇒ делает A охотником, >1400 от B ⇒ вне сканов B; пират-стайник на (0,1000,0) — в мели A (<1400) и в окне 750<d<1400 от B; свободный ответчик B на (0,2100,0). Ассерт на `aiState` B (стайник за `WEAPON_RANGE` ⇒ урона нет, но захват цели виден): (A) изгой РОЗЫСКНОЙ ⇒ A охотник ⇒ B берёт стайника (`aiState==1`, `respondsToSwarm`); (B) изгой НЕ розыскной ⇒ A не охотник ⇒ подмога заперта, налётчика нет, фолбэк капнут радиусом 750 при стайнике на 1000 ⇒ B простаивает (`aiState==0`, `gatedOnHunt`). ЕДИНСТВЕННОЕ headless-покрытие тира (нужно ≥2 патрулей, а прежние пробники по 1); числовой бейзлайн §5.13.30/31 побитово цел.** **§5.13.34: пробник `manhuntScalesWithBounty=` — свежий `Game`, две фазы, идентичные КРОМЕ награды (патруль в 0, розыскной пират на 1200 LU: <1400 ⇒ P0-охота, >150 ⇒ без стрельбы; позиция патруля пиньнута каждый кадр, но СКОРОСТЬ НЕ трогается ⇒ насыщается на `effMax`). Меряем max|v| патруля: награда 1500 ⇒ терминальная скорость ≈ maxSpeed·(1+GAIN)=90, награда 250 ⇒ ровно maxSpeed=60 (нулевой регресс), high строго > low; эталоны сверены с `LocalCfg::MANHUNT_SPEED_GAIN` (устойчиво к тюнингу). Единственное headless-покрытие облавы (патрулей в основном прогоне нет); числовой бейзлайн §5.13.33 побитово цел.** **§5.13.36: пробник `lawGratitude=` — свежий `Game glg.init(1200)`, `F` = первая фракция ≠ `playerFaction`; сцена A: `wanted`-пират добит игроком в ~300 LU от `defending`-патруля фракции `F` (в `PATROL_DISTRESS_R`) ⇒ ассерт `factionRelation(pf,F)==rel0+6`; сцена B: тот же патруль отставлен на 2000 LU ⇒ ассерт «убийство есть, но реп НЕ двинут» (гейт радиуса цел; `rel0` перезахватывается в начале B ⇒ двусторонний тест: застрявший-вкл/застрявший-выкл/двойной-грант — все падают). Крутится ПОСЛЕ 120k и всех прежних пробников ⇒ свой `Game`/RNG бейзлайн не трясут. Единственное headless-покрытие правила (патрулей в основном прогоне нет ⇒ блок no-op); числовой бейзлайн §5.13.35 побитово цел.** **§5.13.38: пробник `copKillRaisesBounty=` — свежий `Game gck.init(1200)`, `F` = первая фракция ≠ `playerFaction`; ДВЕ сцены с пиньнутыми каждый кадр позициями/корпусами (ноль RNG/дрейфа; `wantedBounty` НЕ пиньнут — это измеряемое), убийца — розыскной пират `heavy=60` (`wantedBounty0=650`), расстреливающий безоружный патруль (корпус→1) с 30 LU (< `WEAPON_RANGE=150`): (A) убийца — ЕДИНСТВЕННЫЙ `wanted` ⇒ он P0-дичь ⇒ награда 650 +200 (§5.13.26G) +500 (`COP_KILL_BOUNTY`) = 1350; (B) добавлен ДРУГОЙ `wanted`-пират (безоружная приманка) ВПЛОТНУЮ к патрулю (10 LU < 30) ⇒ убийца НЕ ближайший `wanted` ⇒ `cWasQuarry` ложь ⇒ лишь обычные +200 = 850. Двусторонний (застрявшее «вкл» валит B, «выкл» валит A); убийца найден по `heavy>50`, игрок в 5000 LU и не стреляет. Единственное headless-покрытие правила (патрулей в основном прогоне нет ⇒ блок no-op); числовой бейзлайн §5.13.36 побитово цел.** **§5.13.40: пробник `factionReprisal=` — свежий `Game glg.init(1200)`, `F` = первая фракция ≠ `playerFaction`; ТРИ сцены (идентичны Scene A §5.13.36: сидячий розыскной по носу, здоровый неподвижный патруль `F` в 300 LU), но реп `(pf,F)` ПРЕДпосеян симметричным `adjustFactionRelation`: (C) −60/Hostile ⇒ ассерт `Δ==−6`; (D) +40/Friendly ⇒ `Δ==+6`; (E) −30/Tense ⇒ `Δ==+6` (пиньнит ТИР-семантику — наивный «rep<0 ⇒ −6» валит E). Трёхсторонний (застрявший «+6» валит C, «−6» валит D). Посев и все три итога в clamp [−128,128]; крутится ПОСЛЕ 120k+прежних пробников (свой `Game` бейзлайн не трясёт). Единственное headless-покрытие ЗНАКА (патрулей в основном прогоне нет ⇒ блок no-op); числовой бейзлайн §5.13.39 побитово цел.** **§5.13.42: пробник `lawHuntsHostile=` — свежий `Game glh.init(1200)`, `F` = первая фракция ≠ `playerFaction`; ДВЕ фазы, сцена = РОВНО один борт (`resize(1)` ⇒ ни пиратов, ни иных целей): НЕспровоцированный патруль фракции `F` (огромный корпус, `hostile=false` пиньнется каждый кадр ⇒ триггер ЧИСТО стояние), игрок пиньнут в 300 LU (>`WEAPON_RANGE`=150 ⇒ огонь не достаёт, сцена чистая; <`AW2`=750 ⇒ игрок в осведомлённости), игрок НЕ стреляет. Пиратов нет ⇒ P0/подмога/рейдер/фолбэк пусты ⇒ единственная возможная цель — игрок, единственный триггер — СТОЯНИЕ. Ассерт на `aiState`: (A) реп −100/Enemy ⇒ патруль берёт игрока (`aiState==1`, `huntsAtEnemyStanding`); (B, контроль) реп 0/Neutral ⇒ простаивает (`aiState==0`, `idleAtNeutral`) — баг «всегда охотится» валит B, «никогда» валит A. Реюз одного `Game` меж фаз: реп пере-сеется `adjustFactionRelation(pf,F, seed−cur)`. Единственное headless-покрытие правила (в осн. прогоне `patrols=0` ЗАМЕРЕНО — 18 воен-гарнизонов на `controlledStars` disjoint маршруту soak ⇒ ветка no-op); числовой бейзлайн §5.13.41 побитово цел.** **Пробник `patrolTargetSignal` (§5.13.43): читает поле `aiTarget` НАПРЯМУЮ — фаза A (Enemy-реп, одинокий патруль, `resize(1)`) ⇒ `aiState==1 && aiTarget==LOCAL_TARGET_PLAYER`; фаза B (анти-ложняк: ближе игрока — розыскной пират) ⇒ патруль целит В ПИРАТА (`aiTarget>=0`, НЕ сентинел игрока), запирая предикат cue §5.13.43 (т.к. `localdraw` вне headless-пути). Главный 120k-цикл остаётся побитово.** **§5.13.44: пробник `vendettaScalesWithStanding=` — свежий `Game gv.init(1200)`, `F` = первая фракция ≠ `playerFaction`, сцена = РОВНО один патруль (`resize(1)`, позиция пиньнута каждый кадр в 600 LU от игрока: <`AW2`=750 ⇒ player-override §5.13.42 срабатывает, >`WEAPON_RANGE`=150 ⇒ без огня ⇒ насыщается на `effMax`; СКОРОСТЬ не трогается). ДВЕ фазы, идентичны КРОМЕ репутации: (A) −128/дно ⇒ ассерт max|v| ≈ maxSpeed·(1+GAIN)=96; (B, контроль) −48/порог Hostile ⇒ патруль ВСЁ ЕЩЁ целит игрока (`aiTarget==PLAYER`, `bothHuntPlayer`), но heat=0 ⇒ ровно maxSpeed=60 (нулевой регресс) ⇒ пиньнит, что бонус растёт с ГЛУБИНОЙ стояния, а не с самим фактом охоты. high строго > low; эталоны сверены с `LocalCfg::VENDETTA_SPEED_GAIN`. Единственное headless-покрытие вендетты (патрулей в осн. прогоне нет ⇒ блок no-op); числовой бейзлайн §5.13.43 побитово цел.** **§5.13.46: пробник `lawPackHuntsPlayer=` — свежий `Game gp.init(1200)`, `F` = первая фракция ≠ `playerFaction`, Enemy-реп −100; ДВЕ сцены с пиньнутыми каждый кадр позициями/корпусами (ноль RNG/дрейфа): (A) стая — ДВА патруля `F` в 300 LU от игрока и 600 LU друг от друга (<`PATROL_DISTRESS_R`=1400 ⇒ в стае, <`AW2`=750 ⇒ оба видят игрока), У КАЖДОГО свой `wanted`-пират в 80 LU (ближе игрока ⇒ §5.13.42 сам игрока НЕ берёт, `atkBest`=пиратова дистанция) ⇒ пак-тир (последний в ветке ⇒ перебивает P0/§5.13.42) флипает ОБОИХ на игрока в ОДНОМ кадре ⇒ ассерт `aiState==1 && aiTarget==LOCAL_TARGET_PLAYER` у ОБОИХ (`packOverridesGate`), порядок бортов не важен (приём D); (B, контроль) ОДИН такой патруль + его ближний `wanted`-пират, БЕЗ соседа ⇒ стаи нет ⇒ гонит ПИРАТА (`aiTarget>=0`, НИКОГДА игрока — `loneChasesPirate`) ⇒ доказывает, что нужна ПАРА. Единственное headless-покрытие правила (патрулей в осн. прогоне нет ⇒ тир no-op); числовой бейзлайн §5.13.44 побитово цел.** |
| `shot_test.cpp` | Вне игры (своя `main`, `LIBSOURCES`). Скриншот-харнес: рендерит **37 кадров** микромира (система/крен/карта/бой/глубокий космос/`shot_combat_lead` — цель наперерез для лид-пипа/`shot_star_near` — у поверхности звезды/`shot_star_lookaway` — вблизи, но нос ПРОЧЬ ⇒ чёрный космос (проверка изотропии сферы §5.13.3)/`shot_star_occlusion` — ближнее тело видно, дальнее скрыто сферой/`shot_muzzle` — вспышка выстрела/`shot_star_inside` — глаз внутри вещества звезды/`shot_planet_near` — окольцованный газ-гигант вблизи, камера приподнята над плоскостью колец ⇒ полосы+терминатор+атм. ободок + открытые кольца с щелями Кассини и тенью планеты (проверка сфер+колец §5.13.4/§5.13.5)/`shot_star_hot` — пульсар (сине-белый), пара к холодной STAR70 для спектрального контраста O→M (§5.13.6)/`shot_belt_near` — подлёт сбоку к крупнейшей глыбе пояса: терминатор день/ночь, кратеры, выгрызенный силуэт, кувыркание + кольца добычи (проверка `renderRockLit` §5.13.7)/`shot_nebula` — глубокий космос с ПРИНУДИТЕЛЬНОЙ туманностью (strength 0.60, magenta), нос вдоль оси «банки» ⇒ пухлое газовое поле с волокнами и градиентом плотности (проверка `renderNebula` §5.13.8)/`shot_traffic_dock` — пришвартованный торговец у рыночного тела: янтарное кольцо-швартовка + циан варп-вспышка рядом + HUD-тальник `IN/DOCK` (проверка читаемости трафика §5.13.10)/`shot_convoy_distress` — пират в дальности оружия у торговца-жертвы (`underAttack` форсирован): красный SOS-маяк-кольцо у жертвы + панель бедствия (SOS-рамка/красная метка/чип `SOS`) + красный muzzle пирата; цель наведена на жертву (проверка §5.13.11)/`shot_patrol_escort` — три борта: торговец-жертва (`underAttack`), пират-налётчик (`threatConvoy`) и патруль-защитник (`defending`, ~99 LU от пирата < `WEAPON_RANGE`); цель наведена на патруль ⇒ зелёное «эскорт»-кольцо у патруля (вне циан-рамки) + зелёная панель `ESCORT` + красный SOS-маяк у торговца рядом (проверка §5.13.12)/`shot_star_flare` — звезда снаружи (D/R=2.3, диск целиком с полем короны), `fxClock=26` подобран так, что активная область эрупции стоит на правом лимбе в пике: яркий горячий бело-голубой протуберанец + локальный вырост короны, тогда как левый/верхний лимб — тонкая ровная корона (проверка вспышек-событий и нулевого регресса §5.13.13)/`shot_belt_types` — четыре камня в ряд, по одному на класс материала (`element` = O/C/Fe/Si через `rockAppearance`, specs 0.55/0.05/0.62/0.08): лёд бело-голубой с бликом, углерод тёмно-угольный матовый, металл сталь-серый с бликом, силикат таново-матовый ⇒ материалы визуально различимы и блестят только лёд/металл (проверка таксономии пород и блика Блинна–Фонга §5.13.15; **§5.13.16 доп. печатает `class=…` и `yields=…` — гейт на классификатор+множитель добычи и на byte-identical specs**)/`shot_mining_beam` — крупнейшую глыбу пояса ПРИНУДИТЕЛЬНО делаем металлической (`element=Fe ⇒ ROCK_METAL`, перепекаем палитру), ставим глаз В ЗОНЕ добычи (`< radius+MINE_RANGE`) и включаем `miningRock` ⇒ рисуется майнинг-луч из кокпита к камню, ореол сталь-серый (цвет класса), сердцевина бело-горячая, точка удара; `fxClock` подобран у пика пульса; печатает `[mining-beam] class=METAL inRange=YES beamRGB=…` — гейт «луч только в зоне» + окраска по классу (§5.13.17)/`shot_wanted` — пират «в розыске» (`wanted`, `wantedBounty=650`) в кадре, наведён: золотое кольцо-розыск у борта + панель цели с золотой рамкой/чипом `WANTED`/строкой `BOUNTY 650 CR`; темп золотого пульса 5.2 ≠ SOS(6.0)/эскорт(4.5) ⇒ ритмом отличим от красного/зелёного (проверка §5.13.25)/`shot_radar_threat` — пять бортов в пределах дальности радара в пяти состояниях (розыск/SOS/эскорт/простой враг/нейтраль), разнесённые по `(dx,dy)`, цель не задана ⇒ фокус на панели радара: пять цветов блипов (золото/красный-пульс/зелёный/красный/серый), выверены попиксельно скриптом (проверка §5.13.27)/`shot_law_pursuit` — ровно два борта: `defending`-патруль и `wanted`-пират (`wantedBounty=650`) в ~166 LU, цель наведена на патруль ⇒ золотой вектор перехвата («бегущий» пунктир + стрелка PATROL→PIRATE) + зелёная панель `ESCORT` с золотой строкой `IN PURSUIT PIRATE` под корпусом + золотая связка блипов патруль↔розыскной на радаре (проверка визуализации §5.13.28-погони, §5.13.29)/`shot_pack_rally` — три борта: `defending`-патруль-охотник, вплотную `wanted`-изгой (близкий бой ⇒ его idle-гейт закрыт, оранжа от него нет) и поодаль простаивающий пират-налётчик (790 LU > 750 ⇒ сам без цели, < 1400 ⇒ «слышит» закон); цель наведена на патруль ⇒ горячо-оранжевый вектор ТАРАНА пират→патруль («бегущий» пунктир + стрелка, быстрее/жарче золота §5.13.29) + оранжевая связка блипов на радаре + строка `UNDER PACK X1` в панели ПОД золотой `IN PURSUIT OUTLAW` ⇒ весь закон-цикл в одном кадре (проверка визуализации §5.13.30-набата, §5.13.31)/`shot_law_backup` — четыре борта под x≈8000 (звезда в origin позади камеры ⇒ ноль окклюзии): `defending`-патруль-ОХОТНИК H (наведён ⇒ панель), впритык `wanted`-изгой (делает H охотником ⇒ золото IN PURSUIT), простаивающий пират стаи в мелее H (⇒ оранж UNDER PACK) и поодаль СВОБОДНЫЙ патруль R (изгой в 1690>1400 ⇒ сам не охотник; пират стаи в 1300<1400 ⇒ берёт его целью выручки); цель наведена на H ⇒ на нём весь закон-цикл сразу: золотой вектор погони + оранжевый вектор тарана стаи + сине-белый вектор выручки R→пират и строка `BACKUP INBOUND X1` ПОД `UNDER PACK X1` (проверка визуализации §5.13.32-подмоги, §5.13.33)/`shot_manhunt` — ровно два борта (та же геометрия, что `shot_law_pursuit`, но награда пирата 1500 вместо 650 ⇒ `heat=1`): `defending`-патруль и `wanted`-пират с МАКС. наградой в ~125 LU, цель наведена на патруль ⇒ золотой вектор погони §5.13.29 КАЛИТСЯ ДОБЕЛА + штрихи ×1.5 быстрее + панель `ESCORT` с бело-калёными строками `IN PURSUIT PIRATE`/`MANHUNT 1500 CR`; в паре с `shot_law_pursuit` (650 CR, спокойное золото) рядом читается жар облавы (проверка визуализации §5.13.35)/`shot_law_standing` — одинокий дружественный патруль (фракция 0 ≠ игрок, реп выставлен в +96 через `setFactionRelation`), цель наведена ⇒ панель несёт строку `STANDING ALLY +96` ярко-зелёным (репутация игрока с фракцией цели глазами кокпита, на лету из `factionRelation` тем же классификатором `classifyFactionRelationTension`; прочитана глазами) — а четыре прежних law-шота теперь несут `STANDING NEUTRAL +0` серым (реп игрок↔NPC = 0 на старте) (проверка визуализации §5.13.37)/`shot_standing_reticle` — вражефракционный ТОРГОВЕЦ (фракция 0, реп выставлен в −100 через `setFactionRelation` ⇒ тир ENEMY) с `hostile=false`, цель наведена ⇒ и РАМКА цели в 3D-мире, и курсовой лид-пип упреждения КРАСНЫЕ ИЗ СТОЯНИЯ, а не из `hostile` (тот `false` ⇒ красный hostile-дефолт исключён) — изолирует стояние как единственную причину; пара к `shot_law_standing` (тот же путь, тир ALLY +96 ⇒ ЗЕЛЁНЫЕ рамка/пип), красный↔зелёный читаются рядом (проверка визуализации §5.13.39)/`shot_reprisal_vector` — та же геометрия/награда, что `shot_law_pursuit` (`defending`-патруль + `wanted`-пират 650 CR), но реп игрок↔фракция-патруля выставлен в −100 (`setFactionRelation`) ⇒ тир ENEMY ⇒ и золотой вектор перехвата §5.13.29 (мир + связка блипов на радаре), и рамка/пип §5.13.39 КРАСНЫЕ: co-op-добивка тут стоила бы репутации (§5.13.40 отдал бы −rep), знак виден ДО выстрела; пара разнесена на 300 LU вдоль +Y, борт-обзор ⇒ вектор тянется поперёк экрана с остриём в жертву/`shot_gratitude_vector` — та же сцена, но реп +96 ⇒ тир ALLY ⇒ тот же вектор + рамка/пип ЗЕЛЁНЫЕ (co-op-добивка принесла бы +rep §5.13.40); рядом с красным собратом наглядно, что §5.13.40 меняет не ВЕЛИЧИНУ, а ЗНАК, и знак читается на приглашающей геометрии; реп восстановлен в 0 в конце (гигиена для будущих сцен); Neutral-патруль (`shot_law_pursuit`) дал бы прежнее золото ⇒ нулевой визуальный регресс (проверка визуализации §5.13.41)/`shot_hunted_standing` — вражефракционный патруль (фракция 0, реп −100 ⇒ тир ENEMY) взял ИГРОКА целью БЕЗ провокации (`hostile=false`, `aiState=1`, `aiTarget=LOCAL_TARGET_PLAYER` — в живой игре так бывает ТОЛЬКО через override §5.13.42 по стоянию), цель наведена на патруль ⇒ над бортом багровая подпись `HUNTING YOU` + самый быстрый пульс-кольца (темп 7.4, ярче всех статусных) цветом стояния + панель `STANDING ENEMY −100`: игрок видит, ПОЧЕМУ на него напали без его выстрела/`shot_hunted_none` — тот же Enemy-патруль (реп −100) в бою (`aiState=1`), но `aiTarget`=индекс борта пирата (НЕ игрок) ⇒ подсказка `HUNTING YOU` НЕ рисуется, хотя фракция враждебна и патруль атакует — доказывает, что триггер = поле `aiTarget` (кого патруль реально преследует), а не просто CK_PATROL+Enemy-стояние; пара к `shot_hunted_standing`, меняется ТОЛЬКО `aiTarget` ⇒ изолирует гейт; реп восстановлен в 0 в конце (гигиена)/`shot_hunted_edge` — тот же Enemy-патруль-охотник (`aiTarget=PLAYER`), но нос камеры ОТВёрнут ⇒ борт ЗА КАДРОМ ⇒ on-screen подпись не рисуется, зато краевой маркер `nearHunt` клампится на правый ободок стрелкой цветом стояния (Enemy багровый); покрывает OFF-SCREEN ветку `drawEdgeMarker` (адверсарное ревью §5.13.43 item #3, не задетую on-screen парой); зелёная стрелка слева = штатный указатель на ближайший рынок `nearMkt`, ортогонален срезу (проверка визуализации §5.13.43)/`shot_hunted_vendetta_hot` — враже-фракционный патруль-охотник с реп −128 (`aiTarget=PLAYER`, `hostile=false`, тот же путь, что `shot_hunted_standing`), `fxClock` в пике синус-пульса ⇒ подпись `HUNTING YOU` и пульс-кольца БЕЛО-КАЛЁНЫЕ, темп максимальный (heat=1 ⇒ `7.4+3.7=11.1`), панель `STANDING ENEMY −128`/`shot_hunted_vendetta_cold` — тот же охотник, но реп −48 (порог включения охоты §5.13.42) ⇒ heat=0 ⇒ подпись/кольца РОВНО как §5.13.43 (оранж-багрянец стояния, темп 7.4, база альфы 110), панель `STANDING HOSTILE −48`; A/B-пара пинит инвариант: тон/темп/яркость растут с ГЛУБИНОЙ вражды (hot), а heat=0 даёт нулевой регресс к §5.13.43 (cold); реп восстановлен в 0 после обоих (гигиена для соседних сцен) (проверка визуализации §5.13.45)/`shot_pack_hunt` — ТРИ вражефракционных патруля берут ИГРОКА целью по стоянию (`hostile=false`, `aiState=1`, `aiTarget=LOCAL_TARGET_PLAYER` — как рождает override §5.13.42/.46): два фракции 0 при реп −128 (heat 1.0 ⇒ бело-калёные), третий фракции 1 при −70 (heat ≈0.28 ⇒ теплее, но не белый), все < 1400 LU ⇒ на радаре; цель наведена на ближнего ⇒ сверху-центр загорается баннер `PACK HUNT X3` тоном самого ГОРЯЧЕГО охотника (worst-enemy задаёт накал, N≥2 ⇒ «стая») + от блипа КАЖДОГО охотника цветная НИТЬ к центру-игроку («сеть стаи затягивается») + над каждым бортом штатная `HUNTING YOU`; нити красятся ПОФАКТОРНО (два бело-калёных, один тёплый), баннер берёт МАКСИМУМ жара; панель `STANDING ENEMY −128`; общий предикат `huntsPlayerByStanding` + `vendettaHuntColor` с per-craft cue/блипом ⇒ анти-дрейф; негативы бесплатны — N=1 (баннер молчит, одна нить) в `shot_hunted_standing`, N=0 в `shot_hunted_none`; реп обеих фракций восстановлен в 0 (гигиена) (проверка визуализации §5.13.47)) в BMP через программный рендерер SDL той же `buildLocalCamera`, что и игра; цель `make shots` → BMP→PNG через `sips`. Собирается и под ASan/UBSan (`renderLocalScene` + попиксельные `renderStarPlasma`/`renderBodySphere`/`renderRockLit`/`renderNebula`) через цель `make shot_asan`. Артефакты в `.gitignore`. §5.13 / §5.13.1 / §5.13.3 / §5.13.4 / §5.13.5 / §5.13.6 / §5.13.7 / §5.13.8 / §5.13.10 / §5.13.11 / §5.13.12 / §5.13.13 / §5.13.15 / §5.13.16 / §5.13.17 / §5.13.25 / §5.13.27 / §5.13.29 / §5.13.31 / §5.13.33 / §5.13.35 / §5.13.37 / §5.13.39 / §5.13.41 / §5.13.43 / §5.13.45 / §5.13.47. |

`features.h` — общие фиче-флаги (при наличии). `SDL2-2.30.10.dmg`, `test_app/` — вспомогательное, не трогать в геймплее.

---

## 5. Локальный режим полёта («микромир») — главный незакрытый трек

### 5.1 Идея
Поверх макро-кластера игрок «ныряет» в одну звёздную систему (или в пустой глубокий космос)
и летает в **реальном масштабе**, инерционно (как в Elite). Звезда в центре, планеты по
кеплеровым орбитам, пояс астероидов, другие корабли, **живой бой**, **добыча руды на месте**,
**стыковка с планетой = рынок**. Пустота между звёздами в основном пуста, но бой и события работают.

**Целевой дизайн (решено пользователем 2026-07-28, §5.13):** локальный режим — полный **3D по трём
осям** (полёт как в Elite, настоящие манёвры), камера от корабля, и **весь звёздный кластер виден
фоном как LOD-скайбокс** по реальным координатам `ClusterStar`. Текущий вид-сверху → §5.12.

### 5.2 Решения пользователя (зафиксированы, не переоткрывать)
1. **Полёт = инерционный (Elite-style):** тяга + дрейф, не «аркадный доворот».
2. **Вход в режим — в любой момент** (клавиша `L`).
3. **Первый срез обязан включать ВСЕ ЧЕТЫРЕ:** (а) другие корабли летают, (б) добыча астероидов
   на месте, (в) живой бой, (г) стыковка с планетой = рынок. — Сделано.
4. **Темп = «долго + автопилот-ускорение»:** реалистично большие расстояния; удержание `Shift`
   ускоряет течение времени (supercruise-подобный варп ×60).
5. **Полный 3D по трём осям, полёт как в Elite** (2026-07-28) — **СДЕЛАНО**. Три оси
   (yaw/pitch/**roll**) манёвренности реально решают в бою/навигации; инерция (п.1) сохранена.
   Реализация — **тело-относительный ортонормированный базис** (`pfwd`/`pup`, right=`pfwd×pup`)
   вместо мировых углов Эйлера: устраняет gimbal lock и делает крен честным. Камера — **из кокпита
   (от первого лица, глаз в корабле)** (срез 2, 2026-07-28); ортогональный вид-карта сверху остаётся
   переключаемым тактическим режимом (клавиша `C`), но не основным. Детали реализации — §5.13.
6. **Весь кластер виден из локального режима как LOD-скайбокс** (2026-07-28) — **СДЕЛАНО**:
   реальные `ClusterStar` рисуются фоном по истинным направлениям (`star.pos − anchor` в
   координатах кластера), LOD по обратной дистанции от якоря (ближние/ярче — крестик/2px,
   дальние — точка). Настоящий фон из данных, детерминированный, без глобального `rng`. §5.13.

### 5.3 Единицы и время
- Пространство: **LU** (local units). Время: локальные **ЧАСЫ**.
- Базовый темп: `1 реальная сек = HOURS_PER_SEC (=1) локальный час`. Удержание варпа умножает поток
  времени на `WARP_MULT (=60)`.
- Орбиты кеплеровы: `orbitSpeed ~ 40 / r^1.5`. Позиции тел пересчитываются из `localHours` каждый кадр.
- Физика идёт **субшагами** (чтобы снаряды не «протуннелировали» цель под варпом):
  `N = clamp(ceil(dtHours/0.05), 1, 64)`, шаг `h = dtHours/N`.

### 5.4 Тюнинг (`namespace LocalCfg` в `local.h`) — крутилки баланса
```
HOURS_PER_SEC   = 1.0     WARP_MULT       = 60.0
PLAYER_ACCEL    = 30.0    PLAYER_MAXSPEED = 95.0    TURN_RATE = 2.4 (рад/реальную сек)
DOCK_RANGE      = 26.0    MINE_RANGE      = 20.0
PROJ_SPEED      = 520.0   PROJ_LIFE_HOURS = 1.2     WEAPON_RANGE = 150.0   FIRE_COOLDOWN_H = 0.10
SHIELD_REGEN_DELAY_H = 3.0   SHIELD_REGEN_RATE = 7.0/ч   PLAYER_SHIELD_BASE = 22.0 (+ armor*1.5)
LOOT_SCOOP_RANGE = 24.0   LOOT_LIFE_HOURS = 300.0   NPC_BOOST_MULT = 1.8 (форсаж при бегстве)
RADIO_REVEAL_RANGE = 300.0 (×(1+0.6*tier))   RADIO_CLAIM_RANGE = 36.0   FX_MAX = 3600 (жёсткий потолок пула частиц)
ROCK_BREAK_TIME = 1.10 (реальные сек)   ROCK_SHARD_COUNT = 10   ROCK_SHARD_SPREAD = 1.35 (радиусов) — раскол §22
```
Прочие числа баланса (в коде): урон игрока `heavy*0.6 + light*0.4 + 2.0`; урон NPC `… + 1.0`;
награда за килл `150 + maxHullHP*6` + `addResearch(3.0)`; скорость добычи `9.0` массы/час +
`addResearch(0.2*dtHours)`; hit-radius по кораблю `5.0 + armor*0.1`, по игроку `5.0`.

### 5.5 Модель данных (`local.h`) — кратко
Перечисления: `LocalBodyKind { LB_ROCKY, LB_GASGIANT, LB_ICE, LB_STATION, LB_MOON }`,
`LocalCraftKind { CK_TRADER, CK_PIRATE, CK_PATROL, CK_CIVILIAN }`,
`LocalFxKind { FX_SPARK, FX_DEBRIS, FX_TRAIL, FX_RING, FX_MUZZLE, FX_SMOKE }`,
`LocalRadioKind { RS_DERELICT, RS_CACHE, RS_DISTRESS, RS_ANOMALY }`.
`constexpr int LOCAL_TARGET_PLAYER = -1000;` — sentinel «цель ИИ = игрок» для `aiTarget`.
- `LocalBody` — тело на орбите (orbitRadius/Phase/Speed/inclination, x/y/z, radius, kind, `parent`
  (для лун — индекс планеты, орбита считается вокруг неё), `ringInner/ringOuter` (кольца газовых
  гигантов), r/g/b, `hasMarket`, name).
- `LocalRock` — астероид, **двигается**: `orbitR/orbitAng/orbitVel` (пояс медленно вращается вокруг
  звезды) + `spin/spinVel` (собственное вращение); x/y/z пересчитываются каждый кадр; `ore/element/rgb`.
- `LocalCraft` — корабль: позиция/скорость/цель, maxSpeed/accel, hull, `shield/maxShield/shieldRegenTimer`,
  **сырые** heavy/light/armor, faction, `kind`, `agentIndex` (>=0 → зеркалит макро-агента), `hostile`,
  `aiState/aiTarget` (ИИ), `boost` (форсаж), `hitFlash/thrustGlow` (juice), таймеры, r/g/b, label.
- `LocalProjectile` — снаряд (позиция/скорость, life, damage, `team` 0=игрок/1=враг, rgb-трассер).
- `LocalFx` — частица (позиция/скорость, life/maxLife в часах, size, kind, rgb). Пул ≤ `FX_MAX`.
- `LocalLoot` — канистра из обломков (позиция/скорость, `element/amount`, life). Авто-сбор в зоне.
- `LocalRadioSource` — радиоисточник (x/y/z, `kind`, `tier` 0..2, `strength`, `reward/research/element/
  amount`, `revealed/resolved`, label). Ловится детектором ≥ своего тира.
- `LocalScene` — вся сцена: active, starIndex (-1 = deep space), hasStar, звезда, `localHours`,
  векторы bodies/rocks/craft/shots/**fx/loot/radio**, туманность (`nebulaRGB/nebulaStrength`),
  игрок (px..pz, pvx..pvz, **ориентация телом-относительным базисом `pfwdX..Z`+`pupX..Z`**
  вместо углов Эйлера — см. §5.13, fireCooldown, playerMaxSpeed/Accel,
  **pShield/pMaxShield/pShieldTimer, playerDestroyed, playerHitFlash**), `detectorTier` (из
  `game.tech.sensors`), `lockTarget`, `shake/fxClock` (тряска/анимация), miningRock/miningAccum,
  dockPrompt/**minePrompt** (ближайший рынок/руда в зоне — источник доступности кнопок панели §5.11),
  toast/toastTimer, warping/thrusting.
- `LocalInput` — ввод за кадр: thrust/brake/yawL/yawR/pitchU/pitchD/**rollL/rollR**/fire/warp
  (удержания) + mineToggle(**M**)/dock/cycleTarget (edge-фронты за 1 кадр).

### 5.6 Три контракта (реализации в трёх файлах)
```cpp
// localgen.cpp — детерминированно строит сцену. Сид: 0x9E3779B9u ^ uint32_t(starIndex)
//   (starIndex>=0), иначе 0xD1B54A35u. НИКОГДА не трогает глобальный rng. game — только чтение.
void buildLocalScene(const Game& game, int starIndex, LocalScene& scene);

// localsim.cpp — двигает сцену на кадр (dtReal — реальные сек). Мутирует и сцену, и game:
//   добыча/лут кладут элементы в трюм игрока (уважая cargoCapacity через shipCargoMass); бой меняет
//   money/hullHP; при hull<=0 ставит scene.playerDestroyed (обработка — в main.cpp, §5.7);
//   килл чужой фракции даёт adjustFactionRelation(-8); радио-клейм — money/research/элемент.
//   Возвращает индекс звезды для стыковки, иначе -1.
int updateLocalScene(Game& game, LocalScene& scene, const LocalInput& in, double dtReal);

// localdraw.cpp — рисует сцену и HUD. Одна функция обслуживает ДВА режима, ветвясь на
//   view.perspective: (1) 3D-кокпит — перспективная камера от первого лица (глаз в корабле,
//   свой корабль не рисуется: мушка+прогрейд) + LOD-скайбокс кластера; (2) карта — орто вид
//   сверху (клавиша C), старый top-down путь ПОБИТОВО. Камера/базис строятся buildLocalCamera
//   (local.h): makeCameraFrameBasis для кокпита / makeCameraBasis для карты.
//   Переиспользует camera.h-проекцию и render2d.h-примитивы (namespace UI). §5.13.
void renderLocalScene(SDL_Renderer*, const Game&, const LocalScene&,
                      const View3D&, const CameraBasis&, int winW, int winH);
```

Генерация (`buildLocalScene`): звезда по `stellarClass`; 3–6 планет (rocky/gas/ice), крупнейшая
rocky получает `hasMarket`; при `population>0` добавляется STATION; пояс 45–89 астероидов (элемент
из `resourceFocus`, либо при `metallicTrait>0.6` fallback на индекс 25); 0–8 co-located макро-агентов
(сырые поля корабля; `hostile` если `type=="pirate"`; цвета фракций); 1–3 торговца, 0–2 пирата;
игрок стартует на `innermostR+120`, ориентация носом внутрь к звезде через `localSetForward(scene,-1,0,0)`
(тело-относительный базис, §5.13). Deep space (`starIndex<0`): `hasStar=false`, 0–2 торговца, игрок в
начале координат, нос `localSetForward(scene,1,0,0)`; тела/пояс получают Z-разброс (объём для 3D).

### 5.7 Интеграция в `main.cpp` (где искать)
- Инклюды: `camera.h`, `local.h` (после `ui.h`). Флаг `--localsmoke` (устанавливает `smoke=true; localSmoke=true`).
- Состояние (~строки 520–534): `LocalScene localScene; View3D localView; double localZoom=0.8;`
  edge-флаги `localMineEdge/localDockEdge/localTargetEdge`. При `localSmoke` — build + `active=true`
  + перенос игрока к `rocks[0]` для headless-покрытия добычи.
- Обработка клавиш (~640–673): `L` — вход/выход (anchor = currentStar игрока если пристыкован и
  `!enRoute`, иначе `selectedStar`, иначе `-1`). В активной сцене: `ESC` выход; `E/K/Tab` → edge;
  `+/-` → `localZoom` (clamp 0.05..40.0); затем `continue` (макро-клавиши подавляются).
- Апдейт (~776–811): при `localScene.active` собирается `LocalInput` из `SDL_GetKeyboardState`
  (W/S/A/D/R/F/Space/Shift) + edge-флаги → `updateLocalScene`. Возврат `dockStar>=0` → выход из локала
  + `UI::openTradeWindow`. Иначе если `!paused` → `advanceGame`. **Пока сцена активна, макро-симуляция
  ЗАМОРОЖЕНА.** При `localSmoke` в апдейт инъецируется синтетический ввод (огонь всегда; добыча на
  кадре 0; тяга с кадра 4; варп с кадра 6) — прогоняет все ветки headless.
- Рендер (~824–835): камера строится **единым помощником `buildLocalCamera` (`local.h`, тот же код,
  что и в скриншот-харнесе `make shots`)** прямо перед `renderLocalScene`, режим зависит от флага:
  **кокпит (3D от первого лица)**: `View3D{perspective=true, focal=winW·localFovScale, center=корабль
  (глаз в корабле)}` + `makeCameraFrameBasis(pfwd,pup)`; **карта** (клавиша `C`):
  `View3D{perspective=false, center=player, scale=localZoom}` + `makeCameraBasis`.
  Далее `renderLocalScene` + present + учёт smoke-кадров + пейсинг + `continue` (макро-рендер пропускается).
- **Аварийный прыжок (смерть игрока):** сразу после `updateLocalScene`, если `localScene.playerDestroyed`
  — выходим из локала, восстанавливаем `hullHP` до 30% max, списываем 20% денег, `lastEvent` + `pushNews`.
  Обрабатывается ПЕРЕД веткой стыковки (`dockStar>=0`).

### 5.8 Управление (игрок) — 3D-полёт по трём осям (§5.13)
```
L        вход/выход из локального режима (из макро — по выбранной/пристыкованной звезде)
Мышь     обзор (мышелук, yaw/pitch) — SDL relative mode, только в 3D-кокпите  ← §5.13.1
W        тяга по носу        S  тормоз (гашение вектора скорости)
A / D    рыскание (yaw)      R / F  тангаж (pitch)   Q / E  крен (roll)   ← 3 оси, без клэмпа (резерв к мыши)
Space/ЛКМ огонь              Shift  варп времени ×60 (удержание)
M        вкл/выкл добычу ближайшего астероида в зоне  (перенесено с E — E теперь roll)
C        переключить вид: 3D-кокпит от первого лица (перспектива) ⇄ карта сверху (орто)
K        стыковка с телом-рынком в зоне → открывает торговое окно
Tab      переключить HUD-цель
+ / -    в кокпите — оптический зум/FOV (localFovScale); в карте — ортозум (localZoom)
ESC      выход из режима
```
Оси поворота — **тело-относительные** (yaw вокруг «верха» корабля, pitch вокруг «правого»,
roll вокруг носа); нет мировых углов Эйлера → нет gimbal lock, полный переворот допустим.

> С этой сессии все дискретные действия продублированы **кликабельной панелью** снизу (§5.11);
> движение (W/S/A/D, R/F, Shift-варп) осталось на клавишах — «удержание» на кнопку не мапится.
> В **3D-кокпите** курсор захвачен (мышелук), поэтому ЛКМ там **стреляет**, а панель действий
> ловит клики только в **карте** (`C`); мышь-обзор отключается в карте/меню (§5.13.1 п.4).

### 5.9 Осознанные упрощения (что ещё НЕ сделано — это не баги)
- Пока игрок в локальном режиме, **макро-симуляция заморожена** (`advanceGame` не вызывается);
  фон не идёт, «прошло N времени по выходу» пока нет.
- NPC-vs-NPC бой считается **прямым DPS** (не снарядами): индексы кораблей нестабильны под
  swap-pop удалением, хранить владельца по индексу — баг. Бой с участием игрока — настоящие снаряды.
- Луны/кольца/туманность — **косметика** (на физику/геймплей не влияют, только визуальная навигация).
- Что БЫЛО упрощением среза 1 и теперь СНЯТО — см. §5.10 (игрок убиваем, астероиды двигаются, радио есть).

### 5.10 Углубление после среза 1 (два прохода — реализованы и провалидированы)
Оба прохода правили тот же набор файлов (`localgen/localsim/localdraw` + интеграция в `main.cpp`),
собирались параллельно субагентами (§6.1) и прошли все уровни проверки, включая soak (§6.5).

**Проход 1 — ощущение + глубина симуляции:**
- **Juice/частицы:** пул `LocalFx` — следы двигателя, вспышки выстрела, искры/обломки/дым попаданий,
  большой взрыв при смерти корабля, пыль от добычи. Прозрачность/размер по life/maxLife. Потолок `FX_MAX`.
- **Экранная тряска:** `scene.shake` (пиксели), растёт на попаданиях/взрывах/смерти, затухает; в draw
  смещает копию камеры (HUD не трясётся).
- **Щиты:** у игрока и NPC. Поглощают урон до корпуса, регенерят через `SHIELD_REGEN_DELAY_H` со
  скоростью `SHIELD_REGEN_RATE`. База игрока `PLAYER_SHIELD_BASE + armor*1.5`.
- **Игрок убиваем:** пол `hullHP>=1.0` снят; при hull<=0 → `playerDestroyed` + взрыв + аварийный прыжок (§5.7).
- **Смышлёный ИИ:** цели пересчитываются каждый кадр по `kind` (пират→ближайшая жертва; патруль→пират;
  торговец/гражданский→бегство от пирата, иначе странствие). Сближение / страйф на дистанции / бегство
  с форсажем (`NPC_BOOST_MULT`) при низком корпусе; упреждение при стрельбе.
- **Лут:** обломки роняют `LocalLoot` (элемент + кол-во), дрейфуют/тают (`LOOT_LIFE_HOURS`),
  авто-подбор в `LOOT_SCOOP_RANGE` с учётом трюма.
- **Живые астероиды:** пояс медленно вращается вокруг звезды, камни крутятся.
- **Реакция фракций:** килл небандитского корабля → `adjustFactionRelation(-8)` + вся фракция в
  системе становится враждебной. Килл пирата — награда без штрафа репутации.

**Проход 2 — контент + глубокий космос:**
- **Луны:** `LB_MOON` с `parent`, орбита вокруг планеты (sim добавляет позицию родителя). В генерации
  добавляются ПОСЛЕДНИМИ, чтобы не сдвигать индексную арифметику пояса/промежутков.
- **Кольца планет:** `ringInner/ringOuter` у ~55% газовых гигантов; рисуются концентрическими окружностями.
- **Туманность:** `nebulaRGB/nebulaStrength`, детерминированно по системе; плоская подсветка + мягкие пятна.
- **Радио + детекторы:** `LocalRadioSource` (kind/tier/reward/research/element). `detectorTier` из
  `game.tech.sensors` (0/1/2 при <1.08 / ≥1.08 / ≥1.20). Проявляется в `RADIO_REVEAL_RANGE*(1+0.6*tier)`
  если `detectorTier>=tier`; клейм в `RADIO_CLAIM_RANGE` даёт деньги + исследование + элемент в трюм.
  HUD-радар (справа снизу) + мировые/краевые маркеры. Это закрывает §7.2.

### 5.11 Единая панель действий (кликабельные кнопки) — срез, ждёт плейтеста
Проблема (фидбек пользователя, дословно): «стало слишком много горячих клавиш … не смог зайти
в звезду … надо сделать из них единую универсальную кнопочную панель … кнопки active/inactive».
Решение: **единая контекстная панель кнопок внизу экрана.** Доступные действия — яркие и
кликабельны, недоступные — затемнены и не срабатывают. Хоткеи НЕ убраны: панель добавлена
поверх них (кнопка и клавиша зовут один и тот же `dispatch`).

- **Где живёт:** целиком в `main.cpp` (он владеет `game`/`ui`/`localScene`/выбором/`buildLocalScene`).
  Переиспользованы публичные примитивы `UI::` (`fillRect/strokeRect/drawText/P`) + инлайновый
  hit-test по `SDL_Rect`. Готовые `contains()`/`drawButton()` в `ui.cpp` — внутренняя линковка
  (анонимный namespace), снаружи не видны, поэтому не тянулись — панель написана заново на публичных примитивах.
- **Данные (файловая область `main.cpp`):** `enum ActionId` (ACT_ENTER/GO/STOP/TRADE/SHIPYARD/
  REPAIR/HIRE/PAUSE/SPEED/FIRE/MINE/DOCK/TARGET/ZOOM_IN/ZOOM_OUT/EXIT) +
  `struct ActionButton { SDL_Rect rect; std::string label; SDL_Color color; bool enabled; bool on; int action; }`.
- **Логика (лямбды в `main.cpp`):** `buildBar()` собирает контекстный набор с флагами `enabled`/`on`;
  `drawBar()` рисует ряд по центру снизу; `dispatch(action)` — **единая точка исполнения** (и клик,
  и хоткей идут через неё). Клик по активной → `dispatch`; клик по неактивной проглатывается;
  клик мимо кнопок в макро-режиме → прежний пикинг звезды/агента (сохранён).
- **Макро-набор:** `L ENTER` (enabled если есть якорь-звезда — пристыкованная иначе выбранная;
  **это и чинит «не зайти в звезду»** — кнопка видима и никогда не проваливается в пустоту, плюс
  минует подавление клавиш при редактировании объёма в торговом окне), `G GO`/`X STOP` (маршрут),
  `T TRADE`, `U YARD`, `J REPAIR` (только пристыкован + есть урон), `H HIRE` (пристыкован),
  `PAUSE/PLAY` (on=пауза), `SPD X{1,2,5,10}` (цикл скорости).
- **Локальный набор:** `SPC FIRE` (по кулдауну), `M MINE`/`M STOP` (в зоне руды; on=добывает),
  `K DOCK` (в зоне рынка), `TAB LOCK` (есть цели; on=есть захват), **`C 3D`/`C MAP`** (переключатель
  кокпит⇄карта), `- ZOOM`/`+ ZOOM` (границы; в кокпите — оптический зум/FOV `localFovScale` 0.40..1.60,
  в карте — ортозум `localZoom`), `L EXIT`. Движение (W/S/A/D, R/F, **Q/E-крен**, Shift-варп) осталось на
  клавишах — «удержание» на кнопку не мапится.
- **Единый источник доступности добычи:** в sim добавлен `scene.minePrompt` (зеркалит `dockPrompt`;
  ближайший астероид в `MINE_RANGE`, `localsim.cpp`) + подсказка «PRESS M TO MINE» (`localdraw.cpp`;
  добыча перенесена с `E` на `M`, т.к. `E` теперь крен). UI не дублирует скан дальности — читает готовый флаг.
- **Раскладка:** один ряд по центру; `h=24, gap=6, pad=14, scale=1`; ширина кнопки `pad + len*6*scale`.
  В локале панель выше (`winH-h-34`), в макро ниже (`winH-h-6`); панели новостей/ивентов слева снизу
  подняты в `ui.cpp` (news 52→72, event 50→70), чтобы не перекрываться панелью.
- **Проверки:** `make` + оба smoke + ASan/UBSan smoke + `make soak` 120k — зелено, 0 ошибок.

### 5.12 [ИСТОРИЧЕСКОЕ] Технический фон: почему был вид «2D сверху» и что переход в 3D не ломал §2
> ✅ УСТАРЕЛО (2026-07-28): полный 3D **уже реализован** — см. **§5.13 (DONE)**. Вид-карта сверху
> теперь переключаемый (клавиша `C`), а не основной режим. Ниже — исходный технический фон
> (почему сначала был вид сверху и почему 3D укладывался в §2); оставлено как история решения.

Исходный вопрос пользователя: «почему игра 2D в системе? когда надо полное 3D».

**Мир уже трёхмерный — «2D» это только КАМЕРА.** Модель данных полностью 3D: у тел и камней есть
`x/y/z`, у орбит — наклонение (`inclination`), у игрока не только `pyaw`, но и `ppitch` (тангаж);
снаряды/лут/частицы живут в 3D. Проекция (`camera.h`) **ортографическая**: `depth` идёт только на
порядок отрисовки (painter) и затухание (fade) — это архитектурное правило §2.7, не случайность.

**Почему камера сейчас строго сверху (yaw=pitch=0):**
- `localView` создаётся дефолтным `View3D()`; заданы только центр (позиция игрока) и `scale` (зум),
  углы остаются 0 → чистый вид сверху (шапка `localdraw.cpp` это фиксирует; `main.cpp` §983–987).
- **Легибельность.** Вид сверху + 5×7 шрифт + векторные примитивы читаются как тактическая карта:
  орбиты — концентрические окружности, дистанции честные, ничто не загораживает цель. Это осознанный
  выбор под минималистичный стиль (§1.2), а НЕ техническое ограничение.
- Ось Z сейчас почти косметична: наклонение орбит и `ppitch` видны лишь через painter-порядок и
  fade, но не как ощущаемая «высота».

**Важно: полный 3D НЕ требует нарушать §2.** `makeCameraBasis(View3D)` уже принимает произвольные
yaw/pitch — то есть камеру-преследователь (chase-cam), поворачивающуюся за носом корабля, можно
включить **без перспективного деления, без новых библиотек, оставаясь в §2** (ортография, C++11, SDL2).
Это работа трёх шагов: (а) завязать `localView.yaw/pitch` на `pyaw/ppitch` игрока; (б) переписать
HUD/радар/off-screen-маркеры под вращающуюся камеру; (в) отладить читаемость (сейчас её даёт именно
неподвижный «вид сверху»).

**Когда полный 3D действительно НУЖЕН (а не просто «красивее»):**
- Когда вертикальный манёвр должен *решать* — настоящий бой по трём осям, уклонение по высоте,
  мгновенное чтение «цель выше/ниже меня». Сверху ось Z схлопнута, поэтому `ppitch` игрока сейчас
  почти не влияет на восприятие — это главный аргумент «за 3D».
- Когда навигация по Z станет содержательной (многослойные пояса, засады из-под плоскости эклиптики).

**Против (почему не сразу):** вид сверху максимально читаем «из коробки»; вращающаяся 3D-камера —
заметная работа по HUD/маркерам/радару и риск потерять ясность ради вида. Поэтому это **кандидат
роадмапа (§7.3), а не срочный долг.** Рекомендация: делать 3D **опциональным chase-cam режимом**
(переключатель клавишей/кнопкой), сохранив вид-карту сверху как основной тактический.

> ОБНОВЛЕНО §5.13: пользователь решил сделать 3D **основным** режимом (вид-карта — переключаемый),
> плюс LOD-скайбокс всего кластера. Открытый вопрос орто(§2.7)-vs-перспектива вынесен в §5.13.

### 5.13 Полный 3D + LOD-скайбокс кластера — ✅ РЕАЛИЗОВАНО (2026-07-28)
Решение пользователя (§5.2 п.5–6): локальный режим переведён в **полноценный 3D по трём осям,
полёт как в Elite**, и **весь звёздный кластер виден фоном как LOD-скайбокс**. Отменяет прежнюю
рекомендацию «3D опционально» из §5.12 — 3D теперь **основной** режим (вид-карта — переключаемый).
Архитектурный выбор проекции — **вариант (б): перспектива ТОЛЬКО в локальном режиме** (см. ниже);
пользователь явно выбрал «Перспектива (Elite)». §2.7 обновлён под это исключение.

**Что реализовано:**
1. **Полёт по 3 осям (Elite):** yaw + pitch + **roll**. Реализация — НЕ мировые углы Эйлера, а
   **тело-относительный ортонормированный базис** корабля: `pfwd` (нос), `pup` (верх), правый =
   `pfwd×pup`. Каждый кадр повороты применяются вращением Родрига вокруг собственных осей корабля
   (yaw вокруг `pup`, pitch вокруг right, roll вокруг `pfwd`), затем Грам–Шмидт ре-ортонормирует
   базис. Убирает gimbal lock, делает крен честным. Инерция (тяга+дрейф) сохранена. `localsim.cpp`.
   - **Управление:** A/D — yaw, R/F — pitch, **Q/E — roll**, W/S — тяга, Shift — варп. Добыча
     перенесена с `E` на **`M`** (конфликт устранён). См. §5.8.
2. **Камера из кокпита (от первого лица, срез 2 — 2026-07-28):** `eye = позиция корабля` (глаз в
   самом корабле), взгляд вдоль `pfwd`, верх `pup`; базис камеры = `makeCameraFrameBasis(pfwd, pup)`
   (`camera.h`). Проекция **перспективная** (`View3D::perspective=true`, `focal=winW·localFovScale`,
   `localFovScale` 0.40..1.60 — оптический зум на `+/−`; near-plane отсечение). Собственная стрелка
   игрока в полёте **не рисуется** — вместо неё центральная мушка-прицел + маркер прогрейда (вектор
   скорости) + дуга щита (§7.1.1). Переключатель **`C`** даёт ортогональный вид-карту сверху
   (`perspective=false`, старый top-down путь ПОБИТОВО сохранён; там жёлтая стрелка игрока осталась).
   Камера строится единым помощником **`buildLocalCamera` (`local.h`)** — тот же код для игры и для
   скриншот-харнеса (`make shots`), так что кадр в тесте побитово совпадает с игровым.
   - **Ориентир эклиптики (искусственный горизонт, 2026-07-28):** в кокпите рисуется «линия схода»
     плоскости системы (мировая X-Y, нормаль +Z; см. `localgen`) — набор направлений `(cosθ,sinθ,0)`
     через тот же `projectDirectionWithBasis`, что и скайбокс. **Наклон линии = крен, смещение
     относительно мушки = тангаж** относительно диска системы → снимает дезориентацию в 6DOF. Тела
     орбит лежат на этой линии (проверка корректности). В глубоком космосе (`!hasStar`) опорной
     плоскости нет — линия не рисуется. Тусклый холодный цвет, под мушкой. `localdraw.cpp` секция (8a).
   - **Лид-прицел + панель цели (читаемость боя, 2026-07-28):** для текущей цели в кокпите — **пип
     упреждения** (перехват в относительной скорости `u=targetVel−playerVel`, т.к. снаряд несёт скорость
     корабля; квадратка `(u·u−S²)t²+2(r·u)t+r·r=0`, наименьший `t>0`; янтарь/красно-янтарь, радиус 600 LU,
     гейт `<1500 LU`). Панель цели (сверху-справа) получила **полосу щита** (циан) и **скорость сближения
     `CLS`**. Строго аддитивно, врага не ослабляли. Детали — §7.1.1. `localdraw.cpp`.
3. **LOD-скайбокс из настоящего кластера:** реальные `ClusterStar` (`cluster.h`) рисуются фоном по
   истинным направлениям `normalize(star.pos − anchor)` в координатах кластера (`projectDirectionWithBasis`
   — точка «на бесконечности», важна только ориентация камеры). Якорь = текущая звезда, в глубоком
   космосе — центроид кластера. LOD по обратной дистанции от якоря (нормировка на среднюю): ближе
   среднего — крестик/2px и ярче, дальше — 1px и тусклее; цвет по `stellarClass`. Вращается вместе с
   кораблём → ориентация и «ты внутри скопления». Детерминированно, без глобального `rng`. `localdraw.cpp`.

**Как это уложилось в §2 (архитектура):**
- **Перспектива — только локальный режим.** Ветка `perspective==false` в `projectPointWithBasis`
  осталась ПОБИТОВО как исходник → макро/кластер/карта не изменены (§2.7 обновлён под исключение).
- Никакого ООП/`virtual`/`new`-иерархий: добавлены поля в `View3D`/`ProjectedPoint`/`CameraBasis`
  (POD) и свободные функции `makeCameraFrameBasis`/`projectDirectionWithBasis`. C++11 + SDL2.
- Локальный рендер по-прежнему НЕ трогает глобальный `rng` (скайбокс — из данных кластера и хешей).

**Рендер (`localdraw.cpp`) — одна функция обслуживает оба режима, ветвясь на `view.perspective`:**
- Экранный радиус тела: перспектива `R·focal/depth`, орто `R·scale`. Порядок — painter по глубине.
- Тряска: перспектива — угловое дрожание камеры (равномерный сдвиг на любой глубине); орто — сдвиг центра.
- Плоские кольца (орбиты/кольца гиганта/орбита луны) рисуются ТОЛЬКО в орто-карте (в перспективе это
  были бы неверные эллипсы). Фейд сцены мягче в перспективе (глубина достигает тысяч LU).
- Радар — тело-относительный: `right`/`pfwd` (нос → вверх радара). Off-screen-маркеры учитывают `behind`.

**Файлы:** `camera.h` (перспектива + frame-базис), `local.h` (`pfwd`/`pup` + roll-инпут + хелперы
`localShipRight`/`localSetForward` + **`buildLocalCamera`** — единый билд камеры для игры и харнеса,
включает `camera.h`), `localsim.cpp` (вращения Родрига + Грам–Шмидт), `localgen.cpp`
(стартовая ориентация + Z-разброс тел/пояса), `main.cpp` (`buildLocalCamera` кокпит/карта, `localFovScale`,
клавиши Q/E/M/C, `+/−`), `localdraw.cpp` (перспективный рендер из кокпита + мушка/прогрейд + LOD-скайбокс
+ режимо-зависимый HUD/радар + потолки размера частиц + **лид-пип упреждения и панель цели со щитом/
сближением**), `soak_test.cpp`/`shot_test.cpp` (наведение через `localSetForward`; харнес рендерит кадры
тем же `buildLocalCamera`, включая кадр `shot_combat_lead` с целью наперерез).

**Проверки (все зелёные, 0 ошибок):** `make`; `--smoke`/`--localsmoke` (exit 0); `make asan` +
оба smoke под ASan/UBSan (exit 0) + **харнес под ASan/UBSan** (гоняет `renderLocalScene` с активной целью);
`make soak` — **120000 кадров, invariantFails=0, deathPath=ok**; `make shots` — 6 кадров (6/6 ok),
просмотрены визуально.

**Плейтест №1 (2026-07-28):** 3D подтверждён как улучшение; бой ощущается «сочно» (не ослаблять).
Открытый пункт «жёлтая стрелка игрока в chase-виде выглядит странно» → **✅ ЗАКРЫТ в срезе 2**:
переход на вид из кокпита (от первого лица), стрелка убрана в полёте, добавлены мушка + маркер
прогрейда. Детали и запись «как сделано» — **§7.1.1**.

#### 5.13.1 Проход углубления №3 — плейтест №2: гигантская звезда, окклюзия, мышелук (2026-07-28)
Фидбек №2 (§7.1.2) вскрыл 5 пунктов. Ключевая идея пользователя — **чистая геометрия, никаких
моделей**: «звезда это координаты центра + радиус, всё; мы не рисуем звезду — мы аналитически знаем
сферу и локально заполняем ею пространство». Реализовано ровно так — везде через `хиты луч↔сфера`,
без буфера глубины и без хранения меша.

1. **Аналитический рендер звезды как сферы (не диск по дистанции).** Звезда — сфера в начале мира
   `(0,0,0)` с радиусом `starRadius`. Экранный радиус диска берём из геометрии касательной, а не из
   `R·focal/D`: `D = |eye| = sqrt(cx²+cy²+cz²)` (центр звезды в мировом нуле), угловой радиус
   `α = asin(R/D)`, **`screenR = focal·tan(α) = focal·R/sqrt(D²−R²)`**. При `D≤R` (глаз внутри вещества
   звезды) — **заливка всего экрана** плазмой. Это чинит «масштаб»: у поверхности `D→R` радиус
   корректно уходит в бесконечность (старый `R·focal/D` насыщался и звезда казалась мелкой). Диск —
   аддитивная корона + 3 концентрических заливки с лимб-даркенингом (тёмный край → яркое ядро).
   Клампится до `4·max(winW,winH)`. Ветка только для `perspective`; в орто-карте (`C`) — прежний диск.
   `localdraw.cpp`.
2. **Окклюзия луч↔сфера (планеты больше не просвечивают сквозь звезду).** Свободная функция
   `segIntersectsSphere(eye, point, center=0, R)`: отрезок глаз→тело перекрыт, если ближний корень
   `t0 = −b − sqrt(b²−c)` лежит в `[0,L]`. Лямбда `occ(wx,wy,wz)` (активна только при `hasStar &&
   perspective`) прогоняется для КАЖДОГО типа объектов перед отрисовкой: тела/астероиды/лут/корабли/
   выстрелы/частицы `→ continue|return`; радиоисточники за звездой становятся edge-маркерами вместо
   точки. Без z-буфера (painter-only 2D) — окклюзия чисто аналитическая. `localdraw.cpp`.
3. **Гигантский масштаб (тысячи планет влезут в звезду, корабли — ничто).** `localgen.cpp`:
   `starRadius = 90 + metallicity·45` (90..135 LU; было 28+met·10). Планеты: скалистые `2.5..5`,
   газовые `7..14`, ледяные `3.5..7`; пояс `0.6..1.8`; станция `3.0`; луны `1.0..2.4` (в `local.h`
   дефолты `LocalBody.radius 8→3`, `LocalScene.starRadius 30→110`). Первая орбита отодвинута за
   поверхность: `starRadius·1.55 + …`; шаг орбит `160..320`. Магический радиус кораблей в кокпите
   `radiusPx(3.0→1.6)`. Итог: звезда доминирует кадр, тела/корабли — крапинки (визуально подтверждено).
4. **Мышелук (FPS-обзор в кокпите).** `SDL_SetRelativeMouseMode` включается, когда `localScene.active
   && !localMapMode && !localSmoke` (`main.cpp`); дельты `SDL_GetRelativeMouseState` × `LOCAL_MOUSE_SENS
   (0.0025)` → `li.mouseYaw = dx·s`, `li.mousePitch = −dy·s`. В `LocalInput` — поля `mouseYaw/mousePitch`
   (это **готовые углы за кадр**, НЕ умножаются на `turn`). В `localsim.cpp` вложены в тот же поворот
   Родрига: `yawA = (клав) − mouseYaw`, `pitchA = (клав) + mousePitch` (мышь вправо→нос вправо,
   мышь вверх→нос вверх). ЛКМ в кокпите теперь **стреляет** (`localFireHeld`), а панель действий (§5.11)
   ловит клики только в карте. Клавиши A/D/R/F/Q/E остаются как резерв. `main.cpp`/`local.h`/`localsim.cpp`.
5. **Синий квадрат при стрельбе — баг устранён.** Причина: муззл-частица рождалась в самом глазу
   (камера в корабле) и `R·focal/depth` раздувала её на пол-экрана синим прямоугольником. Фикс:
   спавн смещён вниз от линии прицела (`−pup·1.6`) и вперёд (`+dir·5`), `size 0.8`, `life 0.05`;
   рендер `FX_MUZZLE` — **аддитивный** двойной bloom (цвет + белое ядро), кап размера `90→14`.
   `localsim.cpp` (спавн) + `localdraw.cpp` (рендер). Визуально: маленькая белая вспышка, без квадрата.

**Проверки прохода №3 (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0);
`make asan` + оба smoke под ASan/UBSan (0 ошибок); `make soak` — **120000 кадров, invariantFails=0,
deathPath=ok**; `make shots` — **10 кадров** (добавлены `shot_star_near`, `shot_star_occlusion`,
`shot_muzzle`, `shot_star_inside`), просмотрены визуально: гигантская звезда с лимб-даркенингом,
дальняя планета скрыта звездой (ближняя видна), маленькая аддитивная вспышка выстрела, заливка
плазмой изнутри звезды.

#### 5.13.2 Проход углубления №4 — плейтест №3: геометрия D/R, звезда доминирует ВЕЗДЕ (2026-07-28)
Фидбек №3 (§7.1.3): «масштаб всё равно звезда маленькая… понял мою идею?». Мышелук и
аналитический рендер сферы (§5.13.1) работают, НО в реальном полёте звезда мелкая. Причина —
**геометрия, а не рендер**.

**Ключевой инсайт (resolution-independent).** Экранный радиус звезды зависит ТОЛЬКО от отношения
`D/R` (дистанция до центра ÷ радиус), не от абсолютного масштаба: `screenR = focal·R/sqrt(D²−R²)`,
`focal = winW·fovScale`. Доля высоты кадра (16:9, fovScale 0.60): `fill ≈ 2.133 / sqrt((D/R)²−1)`.
Значит равномерное раздувание всех LU-величин НЕ меняет вид — важно только отношение орбит к
радиусу. Проверено: D/R=2.8→~82%, 3.44→~65%, 2.4→~98%, 1.45→100%.

**Почему было мелко.** §5.13.1 растил абсолютный `starRadius` (90..135), но орбиты шли `R·1.55`,
затем `+160..320` за планету (до 6) → внешние орбиты ≈ **R·13** → D/R≈13 → звезда ~5% экрана.
Игрок почти всегда далеко. Фикс — задать **орбиты как доли R** и держать D/R малым по всей системе.

1. **Орбиты как доли радиуса (`localgen.cpp`).** Двухветочная формула заменена на линейную по `i`,
   count-independent: `orbitRadius = starRadius · (1.20 + 0.42·i + frand(0, 0.14))`. При 6 планетах
   внешняя ≤ **R·3.44**. Кеплер `orbitSpeed = 40/pow(orbit,1.5)` оставлен.
2. **Звезда-гигант ×2.9 (`localgen.cpp`).** `starRadius = 260 + met·100` (260..360 LU; было 90..135).
   Тела крупнее для видимости, но ≪ R: скалистые `6..12`, газовые `18..38`, ледяные `9..18`,
   пояс `1.5..4.5`, станция `9`, луны `3..7` (offset `frand(14,34)+moonNo·frand(10,20)`).
3. **Выбор игрока — Option B «Доминирует + воздух».** Внешние орбиты ≤ R·3.4: звезда доминирует
   всюду (≥~60% высоты), но есть воздух для навигации/боя; крошечные планеты для контраста. Старт
   `px = starRadius·2.8` (D/R≈2.8, звезда ~82%, нос к звезде вдоль −X). py=pz=0.
4. **Спавны не внутри звезды.** Нижние границы NPC (co-located/traders/pirates) и радио падали
   ВНУТРЬ нового 260..360 LU радиуса → подняты к `starRadius·1.3` / `innermostOrbit`; радио сжато
   до `outermostOrbit·1.1` (не улетает в D/R>5). Геймплейные радиусы (MINE/DOCK/SCOOP/CLAIM/AI/radar/
   скорость) НЕ трогали — абсолютные дистанции сопоставимы со старыми, они пропорциональны.
5. **Порог заливки R·1.7 (`localdraw.cpp`).** `if (D <= R*1.0008)` → `if (D <= R*1.7)`: диск и так
   переполняет экран уже при D/R≈1.45, поэтому переход бесшовный, а тангенциальный тёмный зазор в
   полосе (1.0, 1.7)·R исчезает. Близкие планеты рисуются шагом (4) ПОСЛЕ звезды → видны поверх заливки.
6. **Фикс «спиц» эклиптики (`localdraw.cpp`).** Ориентир-эклиптика при near-edge-on направлениях:
   `focal/camZ` (порог `camZ>0.06`) выбрасывал соседние точки за экран в разные стороны → длинные
   лучи. Порог поднят до `camZ>0.14` — near-edge-on отсекается раньше, лучи к бесконечности не рисуются.

Важная находка (инвентарь констант): **физической коллизии со звездой/телами НЕТ** — влёт в звезду
чисто косметический (occ + заливка), смерть только от HP→0. Тугая упаковка орбит вокруг гиганта
безопасна. `shot_test.cpp` по дистанциям не трогали (сценарии ratio-based на `starRadius`,
авто-подстройка), только коммент near-сценария → «D/R=1.25<1.7 ⇒ стена плазмы».

**Проверки прохода №4 (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0);
`make asan` + оба smoke под ASan/UBSan (0 ошибок); `make soak` — **120000 кадров, invariantFails=0,
deathPath=ok** (генерация здоровая: тела 7–11, камни 47–84, корабли 2–5, радио 2–4); `make shots` —
**10/10**, просмотрены: `shot_system_cockpit` (звезда ~80% высоты + планеты-пылинки + чистая линия
эклиптики, спиц НЕТ), `shot_star_occlusion` (~98%, ближняя планета видна, дальняя скрыта),
`shot_system_rolled` (наклонная эклиптика без спиц), `shot_star_near` (бесшовная заливка без зазора).

#### 5.13.3 Проход углубления №5 — плейтест №4: изотропная сфера-плазма (ray-sphere «шейдер») (2026-07-28)
Фидбек №4 (§7.1.4): «баг костыль… подлетаю к звезде — как будто переключается режим и всё
становится жёлтым причём со всех сторон… звезда сделана неправильно, ты что-то нахардкодил, а надо
было заполнять пространство как функция от радиуса всех тел… без костылей… используй шейдеры чтобы
звезда прям была шаром плазмы… рендер вообще бедный». Орбиты/орбитальное движение (§5.13.2) —
одобрены («по орбите вращаются круто»), НЕ трогаем.

**Диагноз (баг подтверждён).** §5.13.2 п.5 `if (D <= R*1.7) fillRect(весь экран, плазма)` — это
костыль-заглушка: при заходе ближе 1.7·R **весь кадр** заливался ровным цветом НЕЗАВИСИМО от
направления взгляда → «жёлтый со всех сторон», в т.ч. когда смотришь ПРОЧЬ от звезды. Плюс биллборд-
диск (`fillCircle` вокруг проекции центра) — не изотропен и не физичен. Пользователь прав: тело — это
**центр + радиус**, а плазма должна быть **функцией геометрии луча**, одинаковой со всех сторон.

**Решение — попиксельный аналитический ray-sphere «шейдер»** (`renderStarPlasma` в `localdraw.cpp`),
заменил ОБА старых пути (заливку и диск). Никаких порогов и «переключения режима»:
1. **Изотропия по построению.** Для каждого пикселя восстанавливаем мировой луч из глаза
   (`dir = right·(sx/focal) + up·(−sy/focal) + fwd`, нормировка) и аналитически пересекаем со сферой
   (центр 0,0,0, радиус R): `b=O·dir`, `dmin² = |O|²−b²`. Попадание ⇔ `dmin²≤R²` И дальний корень
   `t1=−b+√(R²−dmin²)>0` (иначе сфера позади — промах). Результат зависит только от R и |eye|
   относительно луча ⇒ смотришь на звезду — плазма, отвернулся — космос, влетел внутрь (D<R, вход
   te=0 от глаза) — вещество вокруг с направленной структурой. Ноль хардкода направления.
2. **Плазма-шар (шейдинг из геометрии).** Лимб-даркенинг `mu=√(1−dmin²/R²)`; многооктавная
   турбулентность с доменным варпом (3 `sin` по мировой нормали поверхности + дрейф по `fxClock`) →
   «кипящие» филаменты/гранулы, не гладкий градиент. Цветовая рампа лимб(глубокий оранж-красный)→ядро
   (бело-горячее) по `mu²`. Запечённая корона за лимбом (промахи с `b<0`, `dmin∈(R, 1.55R)`, спад g²).
3. **Рендер (SDL2-only, без GL).** Персистентная стриминг-текстура ARGB8888 половинного разрешения,
   `SDL_BLENDMODE_BLEND` + линейный upscale: диск непрозрачен (alpha=255 → честно перекрывает скайбокс,
   согласуясь с ray-sphere окклюзией `occ`), корона полупрозрачна. Далёкую звезду считаем только в bbox
   проекции (`screenR·1.55`), вблизи/внутри — весь кадр. Чистый SDL — детерминированные шоты (софт-
   рендерер) продолжают работать; шейдер покрыт ASan/UBSan через `shot_asan`.
4. **«Спицы».** Их источник в скрине №4 — снятые концентрические `strokeCircle` короны; после удаления
   диска остаётся ТОЛЬКО задуманная одиночная линия-эклиптика (искусственный горизонт, `camZ>0.14`),
   на edge-on центрированной звезде — одна чистая горизонталь, радиальных лучей нет.

**Новые сценарии шотов (`shot_test.cpp`).** Добавлен **решающий** `shot_star_lookaway` (глаз D/R=1.3,
нос ПРОЧЬ от звезды → обязан быть ЧЁРНЫЙ космос, не плазма — прямая проверка, что костыль-заливка
удалён и рендер изотропен). Комменты near/inside переписаны под сферу (не «стена/заливка»).

**Проверки прохода №5 (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0);
`make asan` + оба smoke (0 ошибок ASan/UBSan); **`shot_asan`** — все 11 сценариев под ASan/UBSan
(покрывает lock/попиксельную запись шейдера, вкл. полноэкранный inside-star) — 0 ошибок; `make soak`
— **120000 кадров, invariantFails=0, deathPath=ok**; `make shots` — **11/11**, просмотрены:
`shot_star_lookaway` (ЧЁРНЫЙ космос вблизи, глядя прочь — баг устранён, изотропно ✔),
`shot_system_cockpit`/`shot_star_near`/`shot_star_inside` (богатый плазма-шар: лимб-даркенинг +
филаменты + корона), `shot_star_occlusion` (FRONT на диске виден, BACK за сферой честно скрыт).

#### 5.13.4 Автономный срез рендера — планеты/луны как освещённые ray-sphere сферы (2026-07-28)
**Не по плейтесту** (плейтест №5 ещё не снят) — автономный проход по стоячему мандату «улучшай
проект и добавляй новое» и по записанной идее §0.1 («тот же ray-sphere-приём для планет»). Прямо
предвосхищает фидбек №4 «рендер вообще бедный… заполнять пространство как функция от радиуса всех
тел»: если звезда — шар плазмы (§5.13.3), то планеты не должны оставаться плоскими дисками.

**Что сделано.** Распространили аналитический ray-sphere-рендер со *самосветящейся* звезды на
*отражающие* тела. Планеты и луны (`LB_ROCKY/GASGIANT/ICE/MOON`) в **перспективе** рисуются
попиксельным «шейдером» `renderBodySphere` (`localdraw.cpp`) — как настоящие освещённые сферы.
Станции и **орто-карта** (§2.7) НЕ тронуты — по-прежнему диски + орбитальные кольца (побитово).

1. **Свет — звезда в мировом (0,0,0).** Для пикселя восстанавливаем мировой луч из глаза, аналитически
   пересекаем со сферой тела (центр `C`, радиус `Rb`), берём ближний корень → точку поверхности `S` и
   нормаль `N=(S−C)/Rb`. Направление на свет `L=normalize(−S)` (звезда в начале координат), ламбертова
   диффузия `diff=N·L`. Отсюда **настоящий терминатор день/ночь**: тело между глазом и звездой
   показывает тёмную ночную сторону, сбоку — фазу/серп, ровно как реальная планета. Геометрия, не хардкод.
2. **Попиксельная окклюзия звездой.** Тот же луч глаз→поверхность тела проверяем на пересечение со
   сферой звезды; если ближний корень звезды ближе, чем тело — пиксель пропускаем (тело за звездой,
   там уже нарисована плазма §5.13.3). Центро-ориентированный ранний `occ()` для полностью скрытых тел
   сохранён; попиксельная проверка добивает частичное перекрытие лимба.
3. **Поверхность из нормали (по типу тела, детерминированно, без rng).** Seed — из орбитальных
   параметров тела (`orbitRadius/orbitPhase/Rb`), ось наклона — из seed. Газ-гигант: широтные полосы +
   турбулентный варп + завихрение + медленный дрейф по `fxClock`. Скала: мозаичность (2 октавы `sin`).
   Лёд: высокое альбедо + светлее к полюсам + мелкая крапинка. Луна: серые «моря» (maria). Всё — чистая
   функция нормали/seed, шоты остаются детерминированными.
4. **Мягкий терминатор + атмосфера.** Wrap-lighting `(diff+0.10)/1.10` смягчает край день/ночь; фон
   `ambient=0.15` (плотное звёздное окружение глобулярного скопления) держит ночную сторону читаемой,
   не заливая её. Для газа/льда — атмосферный ободок лимба (Френель-подобный `rim·diff`) на освещённом
   крае.
5. **Рендер (SDL2-only).** Своя персистентная стриминг-текстура ARGB8888 половинного разрешения
   (отдельный `static` от звезды), sub-rect bbox-lock вокруг проекции тела, `BLEND` + линейный upscale,
   `RenderCopy`. Мелкие тела (`screenR<1px`) и орто-карта → диск-фолбэк (`fillCircle`, как раньше).
   Художественный порядок (painter, far→near) не тронут.

**Новый шот `shot_planet_near`** (`shot_test.cpp`): находит крупнейший газ-гигант звезды 0, ставит
игрока перпендикулярно линии звезда–планета с лёгким смещением к звезде (~3/4 фазы), нос на планету →
видны полосы + терминатор + ободок. Всего шотов теперь **12**.

**Проверки (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0); `make asan` + оба
smoke (0 ошибок); `shot_test_asan` — все 12 сценариев под ASan/UBSan (покрывает sub-rect lock/попиксельную
запись `renderBodySphere`) — 0 ошибок; `make soak` — **120000 кадров, invariantFails=0, deathPath=ok**;
`make shots` — **12/12**, просмотрены `shot_planet_near` (газ-гигант: полосы+терминатор+ободок; луна
рядом — сфера с морями и своим терминатором) и `shot_combat_cockpit` (ночная сторона крупной планеты —
читаемый ambient, без регрессий). Орто-карта перепроверена: диски+кольца как раньше.

#### 5.13.5 Автономный срез рендера — кольца газовых гигантов в перспективе (ray↔plane) (2026-07-28)
**Не по плейтесту** — автономный проход по стоячему мандату «улучшай проект и добавляй новое» и по
записанной в §0.1 идее «дальше — кольца». Логическое продолжение §5.13.4: если планеты стали
освещёнными сферами, то кольца газовых гигантов (`ringInner/ringOuter` уже в модели `local.h`, до сих
пор рисовались ТОЛЬКО на орто-карте кольцами) должны появиться и в 3D-кокпите — как настоящий диск
Сатурна, а не плоская окружность.

**Что сделано.** Кольца встроены в тот же `renderBodySphere` (`localdraw.cpp`), попиксельно, вместе со
сферой планеты — один общий цикл, одна sub-rect текстура. Никакого отдельного прохода/биллборда.
Только газ-гиганты с `ringOuter > ringInner > 0` (флаг `hasRing`). Орто-карта (§2.7) НЕ тронута.

1. **Геометрия — ray↔plane.** Кольца — это **аннулюс в экваториальной плоскости планеты**, чья нормаль
   = ось вращения `A` (та же `A`, что даёт широтные полосы сферы в §5.13.4 — кольца и полосы соосны, как
   в природе). Для пикселя: `denom = d·A`, `t = −ocA/denom` (где `ocA=(E−C)·A`, `E` — глаз, `d` — луч,
   `C` — центр планеты); точка пересечения `P=E+d·t`, радиус в плоскости `rad=|P−C|`; кольцо, если
   `Ri ≤ rad ≤ Ro`. Эллипс на экране (открытые/edge-on кольца) получается сам собой из проекции.
2. **Шейдинг кольца (`u=(rad−Ri)/(Ro−Ri)`).** Радиальный спад к внутр./внешн. кромке; **две
   гауссовы щели Кассини** (u≈0.52 и u≈0.80, разной глубины); тонкие радиальные полосы
   `0.72+0.28·sin(rad·0.85+seed·2)`; цвет — смесь тела и льдисто-песочного (214,205,188), домноженная на
   свет звезды в точке `P` (кольца затеняются на ночной стороне, как сфера).
3. **Тень планеты на кольцах — мягкая полутень (impact-parameter).** Проецируем `P` на луч планета→звезда,
   берём прицельное расстояние `√(|C|²−(P·C)²/|P|²)`; переход умбра(0.16)→свет по `(dist−Rb)/(0.18·Rb)`.
   Даёт естественный градиент падающей тени на кольца (не жёсткую ступеньку — первую версию с бинарным
   тестом корня заменили именно на полутень).
4. **Grazing-opacity.** У edge-on колец луч идёт вдоль плоскости → оптически толще: `graze` растёт при
   малом `|denom|` (до ×2.2). Кольца плашмя — почти прозрачные, с ребра — плотные.
5. **Композит по глубине (сфера vs кольцо, `t0` vs `tR`) — бесплатно.** В одном цикле есть оба корня:
   сфера перед кольцом → непрозрачная сфера; кольцо перед сферой → alpha-blend поверх; только кольцо →
   полупрозрачно поверх фона. Так передний край кольца ложится ПОВЕРХ планеты, а задняя дуга честно
   уходит ЗА сферу. Окклюзия звездой — тем же лучом гасит и сферу, и кольцо (`ts0<t0`/`ts0<tR`).
6. **BBox расширен под проекцию `Ro`** (`Ro·focal/(depth−Ro)`), с клампом на `winW+winH` и границы
   буфера — открытые кольца шире диска планеты и не должны обрезаться.

**Шот `shot_planet_near` теперь целит в окольцованный газ-гигант.** `shot_test.cpp` сканирует все звёзды
кластера, выбирает крупнейший газ-гигант с кольцами, ставит камеру приподнято над плоскостью колец
(кольца раскрыты) → виден полный Сатурн: открытый эллипс, передняя дуга над планетой, задняя за сферой,
щели Кассини, радиальные полосы, мягкая тень планеты на кольцах, терминатор/полосы самой планеты.

**Проверки (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0); `make asan` + оба
smoke (0 ошибок); **`make shot_asan`** (новая цель Makefile) — все 12 сценариев под ASan/UBSan
(покрывает попиксельную ring-математику, композит сфера/кольцо, расширенный sub-rect lock) — 0 ошибок;
`make soak` — **120000 кадров, invariantFails=0, deathPath=ok**; `make shots` — **12/12**, просмотрен
`shot_planet_near` — корректный естественный Сатурн (после замены жёсткой тени на полутень градиент
плавный). Орто-карта перепроверена — без регрессий.

#### 5.13.6 Автономный срез рендера — полировка звезды-плазмы (спектр, хромосфера, протуберанцы, пятна) (2026-07-28)
**Не по плейтесту** — автономный проход «улучшай проект». Обогащает `renderStarPlasma` (§5.13.3),
делая звезду богаче и физичнее, СТРОГО в рамках её философии: всё — чистая функция геометрии луча
(изотропно по построению), без порогов/переключений режима, половинное разрешение + bbox, только SDL2.

1. **Спектральный контраст O→M.** Прокси температуры `hot01=clamp((starB−150)/105)` (горячая звезда —
   сине-белая, высокая B; холодная — красно-оранж, низкая B). Ядро: горячая → сине-белое (буст B),
   холодная → жёлто-белое (буст R/G). Лимб: холодная → глубокий красно-оранж (сильное покраснение),
   горячая → синее и ярче. Пульсар (`stellarClass==1`, 190,210,255) теперь читается как настоящий
   бело-голубой гигант, тёплая звезда — как красно-оранжевый. Всё — константы на вызов (бесплатно).
2. **Хромосферный ободок.** Тонкий аддитивный слой у самого лимба (`rim=(1−mu)^4`, mu — косинус к
   лимбу) — H-alpha излучение. Тон спектральный (`chR/chG/chB`): холодная → красно-розовый, горячая →
   сине-белый (чтобы ободок не диссонировал на сине-белом диске). Лёгкая турбулентная неровность кромки.
3. **Протуберанцы/вспышки на короне.** Корона (лучи мимо сферы, `bb0<0`) теперь модулируется угловой
   функцией от направления точки наибольшего сближения луча со звездой (`P=O−d·(O·d)`, нормировано) +
   медленный дрейф по `fxClock`: несколько плюмов по лимбу тянутся дальше (`reach=coronaR·(1+0.28·prom)`)
   и ярче (`flare`), между ними — провалы. Тон короны спектральный (`coR/coG/coB`): холодный предел =
   ТОЧНО прежний оранж (`lm·`) ⇒ без регресса тёплых звёзд; горячий → сине-белый. `coronaK` 1.55→1.70.
4. **Пятна (сунспоты).** Низкочастотное поле (произведение трёх `sin` по нормали + медленный дрейф),
   возведённое в 4-ю степень → редкие мелкие тёмные «клетки» (умбра+мягкая полутень), затемняют
   яркость до −50%. Без порога — гладкая степенная функция.
5. **2-й порядок доменного варпа + доп. октава.** Добавлены `wrp2` (варп варпа) в `n2/n3` и мелкая
   октава `n4` (частота 31) → более «кипящие» филаменты, менее регулярная турбулентность.

**Новый шот `shot_star_hot`** (`shot_test.cpp`): сканирует кластер, находит пульсар (`stellarClass==1`,
самый синий; фолбэк — тёплая звезда с макс. металличностью), кадрирует полный диск с короной (D/R=2.2).
Рядом с `shot_system_cockpit` (холодная STAR70, красно-оранж) даёт наглядную пару спектрального
контраста O→M. Всего шотов теперь **13**.

**Проверки (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0, обычная сборка и
ASan); `make shot_asan` — все 13 сценариев под ASan/UBSan (покрывает новую попиксельную математику
шейдера) — 0 ошибок; `make soak` — **120000 кадров, invariantFails=0, deathPath=ok**; `make shots` —
**13/13**, просмотрены `shot_star_hot` (бело-голубой гигант: сине-белое ядро, синий ободок/корона,
пятна, филаменты) и `shot_system_cockpit` (холодная STAR70 без регресса: красно-оранж диск, глубоко-
красный хромосферный ободок, оранжевые плюмы короны, тёмные пятна).

---

#### 5.13.7 Автономный срез рендера — освещённые «глыбовые» астероиды (ray-sphere + выгрызенный силуэт, кратеры, кувыркание) (2026-07-28)
**Не по плейтесту** — автономный проход «улучшай проект». После того как звезда (§5.13.3/6), планеты,
луны и кольца (§5.13.4/5) стали освещёнными ray-sphere телами, крупнейшим оставшимся визуальным
разрывом стал **пояс астероидов**: 45–89 камней рисовались плоскими залитыми дисками (максимальная
суммарная площадь экрана среди всего «плоского»). Новый `renderRockLit` (`localdraw.cpp`) приводит их
к той же философии — тело = центр + радиус, поверхность = чистая функция геометрии луча.

1. **Ламбертова сфера-глыба.** Попиксельный ray-sphere (тот же приём, что `renderBodySphere`): мировой
   луч из глаза ∩ сфера камня, нормаль из точки попадания, ламбертов терминатор день/ночь от звезды в
   (0,0,0) с wrap-lighting (`diff=(lam+0.10)/1.10`) → у камня видна освещённая и теневая стороны, как
   у планет. Базовый цвет — из `rk.r/g/b`.
2. **Выгрызенный (неровный) силуэт.** Вместо идеального круга — `reff = Rb·(1+disp)`, где `disp`
   почти всегда отрицателен (диапазон ≈[−0.25,+0.05]); пиксель принимается, только если
   `disc ≥ Rb²−reff²`. Т.к. вычитаем внутрь, силуэт ⊆ номинального диска ⇒ **не может** вылезти за
   bbox (гарантия против переполнения текстуры). Камень читается как обколотая глыба, не как монета.
3. **Кратеры и мозаичность.** Произведение `sin` по координате нормали: `crater` в 4-й степени →
   редкие резкие тёмные ямки (затемняют до −55%); отдельный низкочастотный `mott` (±20%) — крупная
   пятнистость поверхности. Без порогов — гладкие степенные функции.
4. **Кувыркание (tumbling).** Переиспользован уже существовавший, но не использовавшийся в рендере
   `LocalRock.spin` (интегрируется `localsim` из `spinVel`): координаты выборки шума поворачиваются
   вокруг оси Z на `rk.spin` ⇒ кратеры/сколы **крутятся вместе с камнем**. Детерминированно (только
   `spin` + `idx`-хэш-сид, global `rng` не тронут — §2.3).
5. **Окклюзия звездой + бюджет.** Попиксельная окклюзия луч↔звезда (как у тел): камень за диском
   звезды не просвечивает. Шейдер включается только для `screenR≥5` px (ограничивает число
   `SDL_LockTexture`/`RenderCopy` в плотном поясе); мелкие камни идут дешёвым **фазовым диском**
   (яркость по фазе звезда–камень–камера). Фолбэк и фазовое освещение гейтятся на `view.perspective`
   ⇒ **орто-карта `C` побитово не тронута** (§2.7): в орто `br=1.0` воспроизводит прежний `fade(...)`.

**Новый шот `shot_belt_near`** (`shot_test.cpp`): находит крупнейший камень пояса, ставит глаз сбоку
(перпендикулярно направлению света) на дистанции ~6 радиусов и наводит нос на глыбу ⇒ терминатор
день/ночь идёт по центру диска, видны кратеры и выгрызенный лимб; плюс янтарные кольца добычи
(`miningRock`) вокруг цели и мелкие соседние камни фолбэком. Всего шотов теперь **14**.

**Проверки (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0); `make shot_asan` —
все 14 сценариев под ASan/UBSan (покрывает новую попиксельную математику `renderRockLit`, sub-rect
lock, выгрызенный bbox) — 0 ошибок; `make soak` — **120000 кадров, invariantFails=0, deathPath=ok**;
`make shots` — **14/14**, просмотрен `shot_belt_near` (освещённая глыба с терминатором, кратерами,
неровным силуэтом; вторая соседняя глыба тоже шейдится; мелочь — фазовые диски; кольца добычи и
прицел композятся поверх). Орбиты §5.13.2 не тронуты. Правки: `localdraw.cpp`, `shot_test.cpp`.

#### 5.13.8 Автономный срез рендера — туманность как объёмное газовое поле на небесной сфере (2026-07-28)
**Не по плейтесту** — автономный проход «улучшай проект». После того как звезда, планеты, луны,
кольца и астероиды (§5.13.3–7) стали объёмными ray-функциями геометрии, **последним «плоским»
элементом кадра в перспективе остался фон-туманность**: полноэкранный `fillRect` одного тона + 3
хеш-круга. Он читался как цветной прямоугольник, а не как газ. Новый `renderNebula` (`localdraw.cpp`)
приводит его к той же изотропной философии — цвет/плотность = чистая функция **направления мирового
луча** (значит, поле «висит» на небесной сфере и вращается заодно со скайбоксом и LOD-звёздами).

1. **Небесная сфера, а не экран.** Тот же попиксельный конвейер и **точно та же конвенция луча**, что
   у `renderStarPlasma`/скайбокса (`fx=bx·2+0.5`, `scx=(fx−halfW)/focal`, `dir=normalize(right·scx +
   up·scy + fwd)`), на полуразрешающей стриминг-текстуре ARGB8888 (BLEND, Linear). Плотность —
   функция `dir`, поэтому при повороте камеры туманность и звёзды скайбокса едут **синхронно** (нет
   «плавания» фона отдельно от звёзд).
2. **Многооктавный шум = пухлые сгустки + волокна.** НИЗКОчастотные **произведения** синусов по `dir`
   дают локализованные облака (а не полосы, как сумма синусов); поверх — средняя и мелкая октавы —
   волокна. Домен предварительно искажён **доменным варпом** (`w1` из `sin·sin`), поэтому сгустки не
   выровнены по осям и не образуют регулярную решётку.
3. **Крупномасштабный градиент «банки».** `bank = 0.5 + 0.5·(dir·axis)` — линейный градиент плотности
   поперёк неба (в одной части туманность гуще, в противоположной сходит на нет), как реальные
   молекулярные облака. Ось `axis` **детерминирована из тона** (`sin` от RGB, нормирована; global rng
   не тронут — §2.3), поэтому каждой сцене соответствует свой устойчивый «крен» облака.
4. **Полупрозрачность и цвет.** Итоговая альфа (`MAXA=strength·200`, потолок в ядре банки) масштабирует
   *только* фон — звёзды скайбокса **просвечивают** сквозь газ (рисуется до звёзд/тел). Ядра волокон
   чуть белеют (`wf`), мягкий пол по плотности (`−0.18`) даёт клочья вместо сплошной заливки, слабый
   контраст (`dens·(0.6+0.4·dens)`) не давит средние тона. Дрейф — `fxClock` (медленное «дыхание»).
5. **Орто-карта не тронута (§2.7).** `renderNebula` вызывается **только** при `view.perspective`; в
   орто (top-down карта `C`) выполняется прежняя ветка — плоский `fillRect` одного тона + 3
   детерминированных хеш-круга, **побитово идентично** прежнему коду.

**Новый шот `shot_nebula`** (`shot_test.cpp`): глубокий космос (без звезды), **принудительно** заданы
`nebulaStrength=0.60` и magenta-тон (110,50,120), глаз в origin, нос наведён **вдоль оси «банки»** ⇒
кадр гарантированно смотрит в плотное ядро ⇒ виден пухлый газ с волокнами и градиентом (натуральные
ролы туманности зависят от сцены/направления, поэтому для регрессии нужен форсированный кадр). Всего
шотов теперь **15**.

**Проверки (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0); `make shot_asan` —
все 15 сценариев под ASan/UBSan (покрывает новую полноэкранную попиксельную математику `renderNebula`,
lock/unlock, запись альфы) — 0 ошибок; `make soak` — **120000 кадров, invariantFails=0, deathPath=ok**;
`make shots` — **15/15**, просмотрены `shot_nebula` (форсированное поле: пухлые magenta-сгустки,
волокна, градиент плотности, без полос-артефактов) и `shot_system_cockpit` (натуральная янтарная
туманность даёт мягкое гало вокруг звезды, не забивая её ядро). Орто-карта побитово цела. Орбиты
§5.13.2 не тронуты. Правки: `localdraw.cpp`, `shot_test.cpp`.

#### 5.13.9 Автономный срез СИМУЛЯЦИИ — живой трафик NPC (рейсы к рынкам + прилёты/отлёты) (2026-07-28)
**Не по плейтесту** — автономный проход «улучшай проект и добавляй новое». Рендер-развёртка §5.13.3–8
закрыла все «плоские» разрывы кадра; **первый срез, углубляющий не картинку, а поведение**. До него
не-боевые NPC (`localsim.cpp`, ветка `aiState==0`) просто выбирали случайную путевую точку раз в
6–14 ч — «броуновское» движение без цели: система выглядела как набор дрейфующих точек, у станции
никогда ничего не происходило, число кораблей было заморожено на старте. Теперь у не-боевых NPC —
**жизненный цикл рейса**, а у системы — **приток/отток**. Пираты сознательно НЕ тронуты (они охотники;
combat invariant запрещает ослаблять агрессию), их прежнее блуждание вынесено в отдельную ветку.

1. **Конечный автомат рейса (поле `LocalCraft.errand` 0/1/2).** Не-боевой NPC вне боя (`kind != CK_PIRATE`,
   `aiState==0`):
   - **0 — круиз.** Цель движения — тело-назначение (`errandBody`), позиция берётся КАЖДЫЙ кадр, так что
     курс следит за орбитой станции/планеты. Долетел в радиус `body.radius + DOCK_RANGE` → «швартуется»:
     переходит в стоянку, гасит ход (`v *= 0.15`).
   - **1 — стоянка «у причала».** Держится рядом с телом со **сдвигом по индексу** (`i·1.3` рад вокруг,
     ±Z), чтобы гости не сходились в одну точку; `thrustGlow` низкий. По таймеру `DOCK_LINGER_MIN..MAX_H`
     (3–9 ч) выбирает новый рейс.
   - **2 — отлёт.** Только чисто-локальные (`agentIndex<0`), с вероятностью `ERRAND_DEPART_PROB=0.30`:
     цель — точка за краем системы (`edgeR+120`) в случайном направлении, короткий форсаж (`boost`).
     Удаляется циклом (L), когда вышел за `edgeR+60`.
2. **Край системы `edgeR`.** Считается раз в кадр перед ИИ-циклом: макс. дистанция тела + радиус + 150,
   пол 600 LU. Служит и точкой отлёта, и порогом деспауна.
3. **Прилёты (`(L2)` после удаления мёртвых).** Пока система реальная (`starIndex>=0`, есть тела) и
   `craft.size() < TRAFFIC_CAP` (10), по таймеру `scene.trafficTimer` (интервал `TRAFFIC_MIN..MAX_H`,
   8–22 ч) с края влетает **один** свежий торговец/гражданский (`agentIndex=-1`, неагрессивный, курс на
   рынок, стартовая скорость внутрь). Таймер сбрасывается даже при достигнутом потолке — RNG не дёргается
   каждый кадр. Потолок + пейсинг держат `scene.craft` жёстко ограниченным (инвариант soak
   `craft.size() ≤ 200000` соблюдён с гигантским запасом — реально ≤10).
4. **Зеркала макро-агентов не «исчезают».** `errand==2`/деспаун разрешён только для `agentIndex<0`.
   Зеркало макро-агента (`agentIndex>=0`) курсирует между телами (круиз↔стоянка), но систему не
   покидает — иначе локальный ростер рассинхронизировался бы с макро-состоянием («агент здесь»).
   **Write-back в макро отложен** (убийство/уход локального зеркала пока не влияет на макро-агента —
   как и раньше); это следующий шаг углубления. → **Снято в §5.13.14** (убийство зеркала игроком
   деградирует макро-агента в спас-капсулу).
5. **Старт (`localgen.cpp`).** Пост-проход в конце CASE B: все не-боевые сгенерированные корабли
   (co-located + процедурные торговцы) стартуют с `errand=0` и курсом на рыночное тело (хаб/станция),
   а не на случайную планету — со старта у станции виден трафик. `scene.trafficTimer` рандомится
   (`TRAFFIC_MIN..MAX_H`), чтобы первый гость появился не мгновенно.
6. **Детерминизм (§2.3).** Все розыгрыши — локальные `std::mt19937`, сид из `i`/`localHours`/
   `craft.size()`/`bodies.size()` (идиома §2.3). **Global `rng` не тронут; макро не мутируется.** Выбор
   тела-рынка — reservoir-sampling без временных векторов (детерминирован при данном движке).

**Шотов не добавлял.** Трафик — поведение во времени; один статический кадр его не покажет, а сцены
шот-харнеса рендерятся почти сразу после генерации. Регрессия покрыта **soak** (120k кадров реально
гоняют спавн/деспаун/смену фаз и инварианты) + `--localsmoke`. Осознанно оставил **15 шотов**.

**Проверки (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0); `make shot_asan` —
15/15 под ASan/UBSan (новые поля `LocalCraft`/`LocalScene`, индексация спавна/деспауна безопасны) —
0 ошибок; `make soak` — **120000 кадров, invariantFails=0, deathPath=ok**, при этом наблюдаемо: реальные
системы наполняются до потолка (`craft 3→10`, `2→10`, …), **deep space пустеет** (`2→0` — бродяги
уходят, прилётов нет), `fxPeak` 6–9 ≪ 3600. Орбиты §5.13.2, орто-карта и рендер не тронуты. Правки:
`local.h`, `localsim.cpp`, `localgen.cpp`.

---

#### 5.13.10 Автономный срез — ЧИТАЕМОСТЬ трафика (варп-вспышки прилёта/отлёта + причал + HUD-тальник) (2026-07-28)
**Не по плейтесту** — автономный проход, прямое продолжение §5.13.9. Живой трафик там уже работал в
симуляции (soak это доказал), но из кабины был **невидим**: гость влетал/уходил без сигнала, «стоянка у
причала» ничем не отличалась от медленного дрейфа, а сколько бортов идёт к рынку игрок узнать не мог.
Срез не меняет поведение — он делает уже существующую жизнь **воспринимаемой**. Всё строго аддитивно
(ни ИИ, ни урон, ни пираты не тронуты — combat invariant цел), FX по-прежнему ограничены `FX_MAX`,
орто-карта (C) побитово цела (тронут только HUD-оверлей радара + перспективные декорации).

1. **Сигнатура «варпа» (`emitWarp`, `localsim.cpp`).** Новый локальный хелпер рядом со `spawnWreck`:
   **циан-кольцо** `FX_RING` (size 26, растёт в draw) + **аддитивная вспышка-ядро** `FX_MUZZLE` (яркий
   бело-циан core) + **8 циан-искр** `FX_SPARK`. Голубой тон намеренно отличается от тёплых взрывов —
   читается как «прыжок», а не гибель. Ставится в двух местах: **прилёт** — сразу после `push_back`
   нового торговца в `(L2)` (у края); **отлёт** — в цикле удаления `(L)`, если борт `departed` (гибель
   там НЕ трогаем — у неё свой `spawnWreck`). Сид локальный (`fx.size`/`localHours`/позиция), `FX_MAX`
   уважается на каждом push. По soak это ~+10 FX на событие — `fxPeak` вырос с 6–9 до 16–26, что всё
   ещё ничтожно против 3600.
2. **Пуфф форсажа у причала (`localsim.cpp`).** При переходе борта в отлёт (`errand 1→2`) в точке причала
   выбрасывается **4 синеватых дымка** `FX_SMOKE` (используется уже-локальный `wr`/`u01` ветки) — «я
   отчаливаю», заметно у рынка. Тоже под `FX_MAX`.
3. **Индикатор причала (`localdraw.cpp`, ветка кораблей).** Для `c.errand == 1` рисуется **пульсирующее
   янтарное кольцо-швартовка** (радиус `sz+7`, alpha «дышит» 60↔180 по `sin(fxClock·3 + i·1.7)`) + слабое
   внешнее кольцо `sz+10`. Янтарь ≠ синий щит ≠ циан-рамка захвата — три ясно различимых состояния. Пульс
   привязан к `fxClock` (детерминирован в статичном шоте).
4. **HUD-тальник трафика (`localdraw.cpp`, панель радара).** Над счётчиком `SIG` добавлена строка
   `IN <n>  DOCK <n>` (scale 1): `IN` = неагрессивные борта в круизе к рынку (`errand==0 && errandBody>=0`),
   `DOCK` = стоящие у причала (`errand==1`). Панель радара подросла `rh 158→178`, чтобы строка легла
   между кольцом радара и `SIG` без тесноты (низ панели закреплён у `winH-52`, растёт вверх).
5. **Шот (`shot_test.cpp`, №15 `shot_traffic_dock`).** Рыночное тело зв.0; `craft[0]` форсирован в
   пришвартованного торговца (`errand=1` у причала) + рядом вручную положены циан `FX_RING`+`FX_MUZZLE`
   (имитация прилёта). Кадр разом проверяет: янтарное кольцо причала, HUD-строку (`IN 2 DOCK 1`) и рендер
   warp-FX. Шотов теперь **16**.

**Проверки (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0); `make shot_asan` —
**16/16** под ASan/UBSan (правка `localdraw.cpp` ⇒ обязательна) — 0 ошибок; `make soak` — **120000 кадров,
invariantFails=0, deathPath=ok**, `fxPeak` 16–26 ≪ 3600, трафик по-прежнему наполняет реальные системы
до потолка и опустошает deep space (поведение §5.13.9 не изменилось); `make shots` — PNG просмотрены
глазами (варп-вспышка, причал и тальник читаются). Орбиты §5.13.2, орто-карта побитово не тронуты.
Правки: `localsim.cpp`, `localdraw.cpp`, `shot_test.cpp`.

---

#### 5.13.11 Автономный срез (Slice B) — БЕДСТВИЕ КОНВОЯ + пейофф за защиту (2026-07-28)
**Не по плейтесту** — автономный проход, прямое продолжение §5.13.9/§5.13.10. Живой трафик (§5.13.9)
и его читаемость (§5.13.10) уже были, а пираты уже атаковали торговцев в симуляции (soak давал
`craftDestroyed>0` — NPC гибли в стычках), но для игрока эти рейды были **немой фоновой массовкой**:
ни сигнала «на торговца напали», ни причины вмешаться. Этот срез превращает уже происходящий рейд
«пират vs торговец» в **читаемое, действенное событие** — с маяком бедствия и наградой за защиту.
Строго аддитивно: **пираты НЕ ослаблены** (урон/агрессия/ИИ не тронуты — combat invariant), никакого
global rng (§2.3), только новые флаги-разметка и косметика/награда поверх существующих путей.

1. **Модель данных (`local.h`).** `LocalCraft` получает три поля: `bool underAttack` (жертва — этот
   не-пират сейчас цель атакующего пирата), `bool threatConvoy` (пират — сейчас атакует не-пирата) и
   `int threatVictimFaction` (фракция жертвы на момент угрозы — для положительного rep-бампа). `LocalScene`
   получает `int distressPrev` (число жертв «под атакой» в прошлом кадре — телеметрия/совместимость) и
   `double distressCooldown` (сек «рейд ещё активен» — дебаунс тоста от дребезга счётчика, см. п.4).
   Все — чистая разметка; на решения ИИ/физику НЕ влияют.
2. **Пре-проход сброса (`localsim.cpp`, перед проходом ИИ).** `underAttack`/`threatConvoy`/
   `threatVictimFaction` сбрасываются ОТДЕЛЬНЫМ проходом по всем бортам ДО главного цикла ИИ. Это
   обязательно: индекс жертвы может быть **больше** индекса пирата, поэтому очистка в начале обработки
   самой жертвы затёрла бы флаг, который пират с меньшим индексом уже выставил в этом же кадре.
3. **Разметка в проходе ИИ (`localsim.cpp`).** ИИ (раз в кадр) уже вычисляет для пирата ближайшую цель
   `atkCraft`/`atkPlayer` и состояние `aiState`. Сразу после установки `aiState` добавлено: если
   `c.kind==CK_PIRATE && c.aiState==1 && atkCraft>=0` (атака на КОНКРЕТНЫЙ не-пиратский борт, не игрок) →
   `scene.craft[atkCraft].underAttack=true`, `c.threatConvoy=true`, `c.threatVictimFaction=victim.faction`.
   Порядок безопасен: флаги не участвуют в решениях ИИ; `atkCraft != i`; реаллокации между `c` и `victim`
   нет; индексы стабильны (удаление мёртвых вынесено за субшаги — см. `(L)`).
4. **Тост рейда (`localsim.cpp`, после прохода ИИ) — с дебаунсом.** Считаем `distressNow` (живые борта с
   `underAttack`). Пират у порога низкого HP дёргается attack↔flee, роняя `distressNow` до 0 на кадр-другой —
   это ТОТ ЖЕ рейд, переобъявлять нельзя (иначе тост `CONVOY RAID` мигает по кругу). Поэтому вместо голого
   переднего фронта `distressPrev==0→>0` держим `distressCooldown`: пока есть жертвы — взводим его в **6.0 с**;
   когда жертв нет — гасим на `dtReal`. Тост бросаем только на **холодном** слоте (`distressCooldown<=0` И
   `toastTimer<=0`) — т.е. на переходе из устойчивого затишья в рейд. Текст НАРОЧНО отличается от
   POI-радиоисточника `RS_DISTRESS` (его label — `DISTRESS CALL`, `localgen.cpp`): это разные сущности
   (статичный сканируемый POI ≠ живой рейд). Детализацию по каждой жертве несут маяки, поэтому тост скромен.
   `distressPrev` сохранён (телеметрия). Найдено ревью-субагентом при аудите среза, поправлено сразу.
5. **Пейофф за защиту (`localsim.cpp`, блок убийства игроком `(E)`).** Существующий путь награды
   (bounty + `addResearch` + `spawnWreck` + тряска) дополнен: если сбитый борт — пират И `c.threatConvoy`
   (угрожал конвою в этом кадре — флаг свежий: выставлен в п.3 этого же кадра, читается в субшагах до
   сброса следующим кадром) → **+200 CR бонус**, **+2 исследования**, тост перебивается на
   **`CONVOY SAVED +<bounty+bonus> CR`**, тряска до 18, и при `threatVictimFaction>=0` —
   **положительный** `adjustFactionRelation(playerFaction, victimFaction, +6)` + `pushNews`. Пираты обычно
   `hostile=true`, поэтому старый штрафной блок реприза (`!c.hostile`) их не касается — конфликта нет.
6. **Маяк бедствия (`localdraw.cpp`, ветка кораблей).** Для `c.underAttack` — **быстрый красный
   «SOS»-пульс**: кольцо `sz+9` (alpha 90↔230 по `sin(fxClock·6 + i)`, ярче/быстрее янтарной швартовки)
   + слабое внешнее `sz+13`. Цвет SOS-красный `238,88,82` (тот же, что у `RS_DISTRESS`). За кадром —
   **краевой SOS-маркер** ближайшей живой жертвы (рядом с красным маркером ближайшего врага), ведёт к
   бедствию, даже если оно вне поля зрения.
7. **Панель бедствия (`localdraw.cpp`, верх-справа «цель»).** Если наведённая цель — жертва
   (`t.underAttack`): вокруг панели **пульсирующая SOS-красная рамка** (двойная), метка цели красится в
   SOS-красный, а справа сверху — чип **`SOS`** (если слот не занят значком `LOCK`). Три ясно различимых
   акцента поверх обычной панели.
8. **Шот (`shot_test.cpp`, №16 `shot_convoy_distress`).** В открытом космосе вдали от светила (без
   окклюзии) инсценированы торговец-жертва (`underAttack` форсирован — sim в headless не крутится) и
   пират в ~50 LU (< `WEAPON_RANGE`=150) с красным muzzle к жертве; камера сзади-сбоку, цель наведена на
   жертву. Кадр разом проверяет: SOS-маяк-кольцо, SOS-панель (рамка/метка/чип) и геометрию рейда. Шотов
   теперь **17**.

**Проверки (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0); `make shot_asan` —
**17/17** под ASan/UBSan (правки `localdraw.cpp` ⇒ обязательно) — 0 ошибок; `make soak` — **120000 кадров,
invariantFails=0, deathPath=ok**, `fxPeak` 16–26 ≪ 3600, трафик наполняет системы и пустошит deep space
как прежде (поведение §5.13.9/§5.13.10 не изменилось); `make shots` — PNG просмотрены глазами
(`shot_convoy_distress`: красный SOS-маяк у жертвы, SOS-панель `TRADER`/чип, пират с muzzle; регресс
`shot_combat_cockpit` чист). Орбиты §5.13.2, орто-карта побитово не тронуты. Правки: `local.h`,
`localsim.cpp`, `localdraw.cpp`, `shot_test.cpp`.

#### 5.13.12 Автономный срез (Slice C) — ЭСКОРТ-ПАТРУЛИ реагируют на бедствие конвоя (2026-07-28)
**Не по плейтесту** — автономный проход, прямое углубление §5.13.11. После Slice B рейды «пират vs
торговец» стали читаемым событием (SOS-маяк, панель, тост, пейофф), но **никто в мире на них не
реагировал**: патрули (`CK_PATROL`) тупо гонялись за «ближайшим пиратом», игнорируя, что этот пират
прямо сейчас режет конвой в соседнем секторе. Этот срез делает патруль **осмысленным защитником** — он
слышит рейд издалека и приоритетно летит на перехват именно налётчика. Строго аддитивно: **пираты НЕ
ослаблены** (урон/агрессия/ИИ пиратов не тронуты — combat invariant); расширяется лишь радиус
ОСВЕДОМЛЁННОСТИ патруля, а огонь всё так же гейтится `WEAPON_RANGE` (§ниже, п.4); никакого global rng (§2.3).

1. **Модель данных (`local.h`).** В `LocalCfg` — две константы: `PATROL_DISTRESS_R=1400.0` (радиус, с
   которого патруль реагирует на рейд, LU) и `RAID_NEAR=260.0` (пират считается «налётчиком», если ближе
   этого к любому не-пирату, LU). `LocalCraft` получает `bool defending` (патруль сейчас летит на
   перехват налётчика — маркер эскорта для рендера). Чистая разметка + два порога; на физику не влияет.
2. **Пре-проход сброса (`localsim.cpp`).** В тот же отдельный проход, что сбрасывает флаги §5.13.11
   (`underAttack`/`threatConvoy`/`threatVictimFaction`), добавлен `defending=false`. Выставит его проход ИИ.
3. **Перехват налётчика (`localsim.cpp`, блок цели `CK_PATROL`).** Прежний «ближайший пират в `AW2`»
   заменён двухуровневым выбором. Сначала патруль ищет **налётчика**: перебирает пиратов в
   `PATROL_DISTRESS_R²` от себя и для каждого проверяет по СЫРЫМ позициям, есть ли рядом (< `RAID_NEAR²`)
   хоть один торговец/civilian; ближайший такой пират-налётчик становится целью, `c.defending=true`.
   Если налётчиков нет — **фолбэк** на прежнее поведение (ближайший пират в `AW2`), `defending`
   остаётся false. **Почему по сырым позициям, а не по флагам §5.13.11:** индекс жертвы может быть больше
   индекса патруля, и `underAttack`/`threatConvoy` того кадра ещё могут быть не выставлены к моменту
   обработки патруля. Чтение реальных координат делает детект **порядко-независимым** — не требуется
   двигать/рефакторить уже проверенную разметку Slice B.
4. **Огонь по-прежнему в упор — НЕ снайп (`localsim.cpp:559`).** Радиус 1400 LU — это только
   «услышать и полететь». Стрельба всё так же гейтится `tdist < WEAPON_RANGE` (150 LU): патруль долетает
   до налётчика и бьётся в упор, как все. То есть пирату не прилетает урон с 1400 LU — enemy nerf не
   вводится, чит-эскорта нет. Патрули не получают ни бонуса к оружию, ни к скорости — только новую цель.
5. **Сброс маркера при потере боя (`localsim.cpp`, после финализации `aiState`).** `if (c.kind==CK_PATROL
   && c.aiState != 1) c.defending = false;` — если патруль по какой-то причине вышел из состояния атаки
   (цель ушла/погибла/сменилась на flee), зелёный маркер эскорта гаснет в тот же кадр (не «залипает»).
   Плюс: если патруль спровоцировали и он выбрал целью игрока — `defending` тоже снимается (он больше не
   «свой»-защитник, а противник игрока).
6. **Кольцо эскорта (`localdraw.cpp`, ветка кораблей).** Для `c.defending` — **зелёный пульс-кольцо**:
   `sz+8` (alpha 80↔200 по `sin(fxClock·4.5 + i·0.9)`) + слабое внешнее `sz+13`. Радиусы нарочно вынесены
   ЗА циан-рамку цели (`sz+6`), иначе на наведённом патруле зелёное кольцо сливалось бы с рамкой (нашли на
   первом шоте, поправили `sz+6/sz+11 → sz+8/sz+13`). Цвет зелёный `90,220,130` = «свои» — ясно ≠ SOS-красный
   жертвы `238,88,82` и ≠ янтарь причала.
7. **Краевой маркер + панель `ESCORT` (`localdraw.cpp`).** За кадром — **зелёный краевой маркер** ближайшего
   `defending`-патруля («помощь идёт» из-за поля зрения), рядом с красным SOS-маркером жертвы. Панель цели:
   если наведён `defending`-патруль — **зелёная рамка/метка + чип `ESCORT`** (тем же приёмом, что SOS-панель
   §5.13.11; `sos`/`esc` взаимоисключимы — жертва это торговец/civilian, защитник это патруль).
8. **Шот (`shot_test.cpp`, №17 `shot_patrol_escort`).** Инсценированы три борта: торговец-жертва
   (`underAttack`, hull 50%), пират-налётчик (`aiState=1`, `threatConvoy`) и патруль (`defending`, ~99 LU от
   пирата < `WEAPON_RANGE`, label `PATROL`). Камера аимлена на пирата; цель наведена на патруль ⇒ панель
   `ESCORT`. Два muzzle FX: пират→жертва (красный), патруль→пират (циан). Кадр разом проверяет: зелёное
   кольцо эскорта (вне циан-рамки), панель `ESCORT`, зелёный маркер и геометрию перехвата. Шотов теперь **18**.

**Проверки (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0); `make shot_asan` —
**18/18** под ASan/UBSan (правки `localdraw.cpp` ⇒ обязательно) — 0 ошибок; `make soak` — **120000 кадров,
deaths=0, radioClaims=3, craftDestroyed=2, invariantFails=0, deathPath=ok** (агрегат идентичен до-Slice-C —
патруль-логика не меняет исход soak-сценария), `fxPeak` 6–26 ≪ 3600; `make shots` — PNG просмотрены глазами
(`shot_patrol_escort`: зелёное кольцо эскорта чисто вне циан-рамки, зелёная панель `ESCORT`/`PATROL`, SOS-маяк
у соседнего торговца; регресс `shot_convoy_distress`/`shot_combat_cockpit` чист). Орбиты §5.13.2, орто-карта
побитово не тронуты. Правки: `local.h`, `localsim.cpp`, `localdraw.cpp`, `shot_test.cpp`.

#### 5.13.13 Автономный срез рендера — ВСПЫШКИ-СОБЫТИЯ на короне звезды (flare-цикл) (2026-07-28)
**Не по плейтесту** — автономный проход, точечное углубление §5.13.6. Полировка звезды (§5.13.6) дала
короне статичный «протуберанец»-шум (`prom`): красиво, но неизменно во времени — корона одинаково
пушится по всему лимбу и никогда не «вспыхивает». Этот срез добавляет **события**: две активные области
дрейфуют по лимбу и периодически эруптируют ярким горячим протуберанцем. Чисто рендер, строго аддитивно,
**детерминированно** (только `fxClock`, ноль RNG — §2.3) и с **нулевым регрессом при `fxClock=0`** (все
прежние звёздные шоты рендерятся именно там).

1. **Flare-цикл (`localdraw.cpp`, ветка короны `renderStarPlasma`).** Рядом с прежним фоновым `prom`
   добавлен цикл по `k∈{0,1}` — две активные области. Направление области `k` — единичный вектор,
   вращающийся по лимбу с `fxClock` (`ap = tcl·(0.06+0.015k) + k·2.1`, `rz` слегка колышется), т.е.
   области **медленно дрейфуют**. Для пикселя короны с направлением `(ax,ay,az)`: если `cosang` между ним
   и осью области `> 0.6` — область его «накрывает» (мягкое пятно `tight²`). Временной профиль эрупции —
   `cyc = tcl·(0.5+0.1k) + k·3.7`, `frac = cyc − floor(cyc)`, всплеск `exp(−frac·5)`: резкий пик в начале
   каждого цикла, затем спад — то есть область **периодически вспыхивает** и гаснет. Сумма по областям —
   `flareBoost`.
2. **Что делает вспышка.** `flareBoost` (пик ≈1.0) влияет на три вещи ЛОКАЛЬНО (только накрытые пиксели):
   яркость короны `flare = 0.6 + … + 1.6·flareBoost` (до ×2.2), досягаемость `reach = coronaR·(1 + 0.28·prom
   + 0.30·flareBoost)` (локальный вырост плюма +30%), и цвет — сдвиг к горячему бело-голубому
   `255,248,235` по `fb=min(1,flareBoost)`. Получается яркий горячий протуберанец, растущий из лимба и
   спадающий по циклу.
3. **Почему нулевой регресс при `fxClock=0`.** При `t=0` углы `ap` и `cyc` в фазе `k·…`, а `frac`
   близок к 0 только для одной области — но её ось при `t=0` смотрит в сторону, и на любом ВИДИМОМ лимбе
   `cosang` мал ⇒ вклад `flareBoost` <2%. Плюс плюм ограничен прежним гейтом `coronaR2` (bbox/`coronaK`
   не трогались), а `std::exp` зовётся только при `cosang>0.6` (редкая ветка — дёшево). Итог: диск и
   корона на всех прежних звёздных шотах (`shot_star_near/lookaway/occlusion/inside/hot`) **изотропны,
   как были** — проверено глазами.
4. **Шот (`shot_test.cpp`, №18 `shot_star_flare`).** Камера снаружи звезды 0, D/R=2.3 (диск целиком в
   кадре с полем короны вокруг), нос вдоль −X. `fxClock=26` подобран так, что ось области №0 (`ap≈1.56`)
   встаёт почти точно на лимб (X-компонента ≈0.01 — сбоку от наблюдателя) в пике цикла (`cyc=13.0 →
   frac=0 → всплеск=1`): на правом лимбе — яркий горячий бело-голубой вырост короны, тогда как левый/
   верхний лимб — тонкая ровная корона. Единственный шот, ловящий эффект; все прочие звёздные — контроль
   регресса при `fxClock=0`. Шотов теперь **19**.

**Проверки (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0); `make shot_asan` —
**19/19** под ASan/UBSan (правки `localdraw.cpp` ⇒ обязательно) — 0 ошибок; `make soak` — **120000 кадров,
deaths=0, invariantFails=0, deathPath=ok** (рендер не влияет на симуляцию — агрегат как прежде),
`fxPeak` 6–26 ≪ 3600; `make shots` — звёздные PNG просмотрены глазами (`shot_star_flare`: горячий
протуберанец на правом лимбе, асимметрия видна; `shot_star_near/lookaway/occlusion/inside/hot` — без
регресса, корона изотропна). Орбиты §5.13.2, орто-карта побитово не тронуты. Правки: `localdraw.cpp`,
`shot_test.cpp`.

#### 5.13.14 Автономный срез СИМУЛЯЦИИ — write-back локальных убийств в макро (последствия) (2026-07-28)
**Не по плейтесту** — автономный проход, первый срез ПОСЛЕДСТВИЙ (не косметика). §5.13.9 дал живой
трафик бортов-**зеркал** реальных макро-агентов, но их гибель в локальном полёте не оставляла следа в
постоянном мире (write-back был отложен). Этот срез замыкает петлю: **убил зеркало в кабине → его
макро-агент «умер» и в макро**. Строго аддитивно, детерминированно (§2.3), кредиты неприкосновенны.

1. **Общий помощник `downgradeAgentToEscapePod(Agent&)` (`game.h`/`game.cpp`).** В `robAgent` даунгрейд в
   спас-капсулу был дважды скопирован inline (жертва + атакующий) — byte-identical. Вынесен в свободную
   функцию (прецедент — `shipAutofit`/`shipClasses`, §2.1): читает `shipClasses()[0]` (Escape Pod),
   присваивает сырые поля корабля, `cargo.clear()`, `cargoCost=0`, финальный `shipAutofit`. Оба места в
   `robAgent` заменены на вызов — **поведение сохранено** (`lootCargo` уже чистит груз ⇒ повторный
   `clear()` идемпотентен; деньги не трогаются). Чистая арифметика, без RNG — безопасно звать из
   локального режима (§2.3).
2. **Write-back (`localsim.cpp`, блок убийства игроком `(E)`, конец).** В самом конце блока
   `if (before>0 && c.hullHP<=0 && playerValid)` — после существующих награды/тоста/разбора/тряски/бонуса
   конвоя/репрессии фракции, пока `c` ещё жив (до swap-pop) — добавлен write-back: если
   `c.agentIndex >= 0 && < agents.size() && ≠ playerAgent`, зовём `downgradeAgentToEscapePod(game.agents[c.agentIndex])`.
   Репрессия фракции уже применена **выше** в этом же блоке (§5.13.11) ⇒ здесь **ТОЛЬКО** даунгрейд, без
   двойного счёта отношений. Игрок в зеркала не берётся (`localgen` пропускает `playerAgent`) — проверка
   `≠ playerAgent` лишь страховка.
3. **Почему безопасно / без регресса.** Агенты **никогда не стираются** (индекс стабилен: вектор
   очищается лишь при load/init); write-back меняет только поля корабля агента (не `currentStar`/
   `enRoute`), поэтому агент продолжает зеркалиться в тех же системах — уже как слабая капсула.
   Чисто-локальный трафик (`agentIndex=-1`) не затрагивается. Соак-агрегат совпал с бейзлайном
   (`craftDestroyed=2`, `radioClaims=3`), инварианты чисты.
4. **Детерминированный write-back-пробник (`soak_test.cpp`).** Изолированный свежий `Game g2` (seed 1200):
   привязали единственное зеркало сцены к реальному не-игроку агенту (дали ему настоящий корабль + груз),
   обнулили корпус/щит цели, подняли игроку оружие и добили. Учтено **туннелирование снаряда**:
   `PROJ_SPEED·dtHours = 520·0.05 = 26 LU/кадр` ⇒ цель ставится ровно на шаг (+26 LU) с большим радиусом
   попадания (`armor=800 → hr=85`), иначе снаряд «перескакивает» мишень между кадрами. Ассерт: после гибели
   `game.agents[idx].ship.name=="Escape Pod"`, `heavyWeapons==0`, `cargo.empty()`. Провал ⇒ `++invariantFails`
   (гейтит exit-код soak). Печатается `writeBack=ok`.

**Проверки (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0); `make shot_asan` —
**19/19** под ASan/UBSan (рендер не менялся — `localdraw.cpp` не трогали, но прогнали: 0 ошибок);
`make soak` — **120000 кадров, deaths=0, invariantFails=0, deathPath=ok, writeBack=ok**,
`craftDestroyed=2`/`radioClaims=3` как в бейзлайне. Шотов по-прежнему **19** (нет рендер-изменений).
Орбиты §5.13.2, орто-карта и макро-`robAgent` (после рефактора) побитово/поведенчески не тронуты.
Правки: `game.h`, `game.cpp`, `localsim.cpp`, `soak_test.cpp`.

---

#### 5.13.15 Автономный срез РЕНДЕРА — разнотипные породы астероидов по элементу (M/C/S/лёд) (2026-07-28)
**Не по плейтесту** — автономный проход, углубление §5.13.7 (освещённые ray-sphere глыбы). §5.13.7 сделал
все камни пояса одинаково буро-серыми. Этот срез даёт им **состав**: каждый астероид классифицируется по
своему `element` в одну из четырёх реальных таксономических групп → своя палитра + зеркальность. Пояс
перестаёт быть монотонным, а материал читается на глаз (лёд/металл блестят, углерод/силикат матовы).
Строго аддитивно, детерминированно (§2.3), поток генерации не сдвинут — станции/радио/корабли спавнятся
байт-в-байт как в бейзлайне.

1. **Свободная функция `rockAppearance(int element, double& r,g,b,spec)` (`localgen.cpp`, контракт в `local.h`).**
   Чистая арифметика над `elementDefinitions()` — без RNG, без global rng (§2.3), безопасна из харнеса.
   Индекс элемента = `Z−1` (O=7, C=5, Fe=25, Si=13). Классы по атомному номеру / `metallicTrait`:
   - **Лёд/летучие** (`Z∈{1,2,7,8}` — H/He/N/O): бело-голубой `(198,214,236)`, `spec 0.55`.
   - **Углеродистый** (`Z∈{6,16}` — C/S): тёмно-угольный `(56,52,49)`, `spec 0.05` (матовый).
   - **Металлический** (`Z∈{26,27,28}` — Fe/Co/Ni — **или** `metallicTrait>0.35`): сталь-серый
     `(150+metal·28, 150, 158−metal·22)`, `spec 0.62`.
   - **Силикат** (дефолт, всё остальное — S-тип): таново-бурый `(150,128,102)`, `spec 0.08`. **Дефолт-класс
     = старый вид пояса** ⇒ прежний облик силикатных глыб сохранён (нулевой визуальный регресс §5.13.7).
2. **Запекание в поясе (`localgen.cpp`, цикл камней).** После `rock.element = elem` зовём `rockAppearance`,
   пишем в `rock.r/g/b/spec`. Яркостная вариация камень-к-камню (`≈[0.86,1.10]`) берётся из
   **ДЕТЕРМИНИРОВАННОГО хэша индекса** `fract(sin(i·12.9898+π)·43758.5453)`, а **НЕ** из `lrng` — критично:
   лишний `frand()` в цикле сдвинул бы весь нижележащий поток генерации (первый прогон дал
   `radioClaims=6` вместо 3, `craftDestroyed` разошёлся). Хэш не трогает поток ⇒ генерация byte-identical.
3. **Зеркальный блик (`localdraw.cpp`, `renderRockLit`).** Блинн–Фонг, гейтится `rk.spec>0 && lam>0`
   (только блестящие материалы, освещённая сторона). Свет из `(0,0,0)`: `L=−S/|S|`; взгляд `V=−dir`
   (нормированный луч из глаза); `H=norm(L+V)`; `spc = pow(N·H,24)·spec·lightF`, добавляется почти-белым
   `spc·(0.72·255 + 0.28·base)` к диффузному цвету (общий клэмп ниже). Матовые (углерод/силикат) блика не
   дают. Диск-фолбэк (`ROCK_SHADE_MIN`) и базовый цвет читают те же `rk.r/g/b` — регресса нет.
4. **Поле `LocalRock.spec` (`local.h`).** `double spec = 0.0` — зеркальность материала 0..1; дефолт 0
   (безопасно для любых камней, созданных не через `rockAppearance`).
5. **Скриншот `shot_belt_types` (`shot_test.cpp`).** Четыре камня в ряд, по одному на класс
   (`element` = O/C/Fe/Si), specs печатаются `0.55/0.05/0.62/0.08` — точное совпадение с дизайном.
   Камера сбоку у звезды: на глаз материалы различимы, блик только у льда/металла. Кадров стало **20**.
6. **Инвариант пород в соаке (`soak_test.cpp`).** Для каждого камня каждой стартовой сцены: `spec`
   конечен и `∈[0,1]` (иначе `++invariantFails`); счётчики `rocks`/`shiny` (spec>0.5) в сводке
   `SOAK DONE` для контроля разнообразия.

**Проверки (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0); **`make shot_asan` —
20/20** под ASan/UBSan, 0 ошибок (`shot_belt_types` печатает `specs=0.55/0.05/0.62/0.08`); **`make soak` —
120000 кадров, deaths=0, invariantFails=0, deathPath=ok, writeBack=ok, `radioClaims=3`/`craftDestroyed=2`
как в бейзлайне (нулевой регресс), rocks=648 shiny=391** (≈60% пояса — блестящие лёд/металл); **`make shots`
— 20/20**. Глазами: `shot_belt_types.png` — четыре явно различимых материала, блик у льда/металла;
`shot_belt_near.png` — силикатный пояс STAR70 ≈ прежний таново-бурый вид, кольца добычи целы (дефолт-класс
сохранил облик §5.13.7). Орбиты §5.13.2 и орто-карта не тронуты. Правки: `local.h`, `localgen.cpp`,
`localdraw.cpp`, `shot_test.cpp`, `soak_test.cpp`.

#### 5.13.16 Автономный срез СИМУЛЯЦИИ — состав породы влияет на темп добычи (маяк = блеск металла) (2026-07-28)
**Не по плейтесту** — автономный проход, прямое углубление §5.13.15. §5.13.15 дал камням видимый класс
(палитра + зеркальность), но чисто косметически — README (Known Gaps) прямо отмечал, что тип «пока НЕ
влияет на добычу/выход/ценность». Этот срез замыкает петлю: **видимый блеск теперь читается как геймплей**
— металлические глыбы блестят *и* дают богатейшую руду, углерод тускл *и* беден. Игрок учится опознавать
ценную породу глазами. Строго аддитивно, детерминированно (§2.3), поток генерации и облик пояса —
байт-в-байт как в бейзлайне §5.13.15.

1. **Единый источник истины `rockClass(int element) → enum RockClass` (`localgen.cpp`, контракт в `local.h`).**
   Вынес четырёхстороннюю классификацию §5.13.15 (лёд `Z∈{1,2,7,8}` / углерод `Z∈{6,16}` / металл
   `Z∈{26,27,28}` либо `metallicTrait>0.35` / силикат-дефолт) из `rockAppearance` в отдельную **чистую**
   функцию. Теперь ОДНА логика класса кормит и рендер (палитра/блик §5.13.15), и симуляцию (множитель добычи)
   — рассинхрон «выглядит как металл, копается как силикат» невозможен конструктивно. `enum RockClass
   {ROCK_SILICATE=0, ROCK_ICE=1, ROCK_CARBON=2, ROCK_METAL=3}` — обычный C-enum (именованный int, §2.1 не
   нарушен). `rockAppearance` зарефакторен: тот же `switch(rockClass(element))`, те же цвета/spec-формулы ⇒
   **байт-в-байт тот же облик** (specs `0.55/0.05/0.62/0.08`, shiny=391 — доказано в шоте и соаке).
2. **Множитель выхода `rockYieldMult(int rc)` + константы (`local.h`).** К базовому темпу
   `MINE_RATE_BASE=9.0` (прежний флэт-темп) применяется множитель по классу: силикат **1.00**
   (=прежний ⇒ **нулевой регресс для дефолт-класса**), лёд **1.25** (летучие легко возгоняются), углерод
   **0.65** (рыхлая низкосортная порода), металл **1.60** (плотная богатая руда — ищи блестящие глыбы!).
   Чистая арифметика, без RNG.
3. **Применение в добыче (`localsim.cpp`).** Одна строка: `rate = MINE_RATE_BASE *
   rockYieldMult(rockClass(rk.element))` вместо флэт-9.0; дальше всё как было (`amt = rate*dtHours`,
   ограничение по остатку руды, зачисление в груз игрока по символу элемента). Combat/AI/агрессия/генерация
   не тронуты (§ combat-инвариант). Стартовый тост добычи стал `MINING <symbol> <CLASS>` (напр.
   `MINING FE METAL`) — сразу видно и элемент, и класс.
4. **Индикатор добычи (`localdraw.cpp`, `renderRockLit`-HUD).** Живой оверлей у прицела теперь
   `MINING <CLASS>  +<accum>` (был просто `MINING +<accum>`) — класс подтверждается во время бурения.
   `rockClassName(int rc)` — общий помощник для тоста и HUD.
5. **Проверка выхода в шоте (`shot_test.cpp`).** Строка `[belt-types]` дополнена
   `class=%s/%s/%s/%s yields=%.2f/.../.2f`. Ожидаемо и подтверждено:
   `class=ICE/CARBON/METAL/SILICATE yields=1.25/0.65/1.60/1.00`, а `specs=0.55/0.05/0.62/0.08` **не
   изменились** — прямое доказательство byte-identical облика после рефактора.
6. **Инвариант класса/выхода в соаке (`soak_test.cpp`).** Для каждого камня: класс `∈[ROCK_SILICATE,
   ROCK_METAL]` и `rockYieldMult` конечен и `>0` (иначе `++invariantFails`) — гейт на новую классификацию.

**Проверки (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0); **`make shot_asan` —
20/20** под ASan/UBSan, 0 ошибок (`[belt-types] specs=0.55/0.05/0.62/0.08 class=ICE/CARBON/METAL/SILICATE
yields=1.25/0.65/1.60/1.00`); **`make soak` — 120000 кадров, deaths=0, invariantFails=0, deathPath=ok,
writeBack=ok, `radioClaims=3`/`craftDestroyed=2`, `rocks=648 shiny=391`** — всё как в бейзлайне §5.13.15
(нулевой регресс генерации *и* облика); **`make shots` — 20/20**. Глазами: `shot_belt_types.png` — четыре
материала различимы как прежде, блик у льда/металла на месте. Орбиты §5.13.2 и орто-карта не тронуты.
Правки: `local.h`, `localgen.cpp`, `localsim.cpp`, `localdraw.cpp`, `shot_test.cpp`, `soak_test.cpp`.

---

#### 5.13.17 Автономный срез РЕНДЕРА — майнинг-лазер из кокпита, окрашенный в цвет класса породы (2026-07-28)
**Не по плейтесту** — автономный проход, прямое углубление §5.13.15/16. §5.13.15 дал породе видимый класс,
§5.13.16 — читаемый выход добычи; но само **действие** добычи оставалось невидимым (только тост/HUD-текст +
дрейф пылинок). Этот срез замыкает петлю «вид → выход → **действие**»: пока идёт бурение, из кокпита к глыбе
тянется луч, ОКРАШЕННЫЙ в цвет класса, — «блестит ⇒ богато ⇒ яркий стальной луч» теперь видно прямо в
момент добычи. Строго аддитивно, детерминированно (§2.3), **чистый draw-only** — симуляция не тронута
(soak байт-в-байт как §5.13.16).

1. **Блок «(7a) ЛУЧ ДОБЫЧИ» в `renderLocalScene` (`localdraw.cpp`), сразу после цикла снарядов.** Гейт на
   отрисовку: `view.perspective` (только 3D-кокпит, §2.7 — орто-карта строго ортографична, не тронута) **и**
   `miningRock` валиден (в диапазоне `scene.rocks`) **и** цель В ЗОНЕ извлечения `d² ≤ (radius+MINE_RANGE)²`
   (**ровно** те условия, при которых `localsim` реально извлекает руду — луч = «идёт добыча», не «нацелен»)
   **и** `!occ(mr.x,mr.y,mr.z)` (не заслонена звездой). Вне любого из условий — не рисуется ⇒ нулевой регресс.
2. **Геометрия луча.** «Дуло» майнера = `px + pfwd·5.0 − pup·1.6` — тот же кокпитный muzzle-поинт, что у
   вспышки выстрела (`localsim`), поэтому луч выходит из той же точки, что и снаряды. Оба конца проецируются
   через `projectPointWithBasis`; при `behind` любого — пропуск. Мягкая толщина ⟂ лучу: нормаль экранного
   отрезка `(−ey,ex)/|e|`, ±1/±2 px, альфа спадает к краю.
3. **Окраска по КЛАССУ (связка §5.13.15↔§5.13.16).** Ореол = сырой цвет класса `rk.r/g/b` (сталь-серый
   металл, бело-голубой лёд, тёмный углерод), сердцевина сдвинута к бело-горячему (`min(255, c·0.4+153)`).
   Цвет луча = цвет породы = класс = выход: один визуальный язык на все три среза.
4. **Пульс/удар — детерминированно, без RNG (§2.3).** «Хум» лазера `pulse = 0.72 + 0.28·sin(fxClock·20)`
   модулирует альфу и радиус; точка удара на глыбе (`fillCircle`+`strokeCircle`) и вспышка у дула. `fxClock`
   уже тикает в сцене — новых источников времени/энтропии нет.
5. **Шот `shot_mining_beam` (`shot_test.cpp`), кадров стало 21.** Крупнейшую глыбу пояса ПРИНУДИТЕЛЬНО делаем
   металлической (`element=Fe ⇒ ROCK_METAL`, перепекаем `rockAppearance`), ставим глаз В ЗОНЕ
   (`< radius+MINE_RANGE`) с носом на камень, включаем `miningRock`. Печать
   `[mining-beam] class=METAL inRange=YES beamRGB=…` — гейт «луч только в зоне» + окраска по классу.
   `soak_test.cpp` НЕ трогаем (луч — draw-only, соак не рендерит).

**Проверки (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0); **`make shot_asan` —
21/21** под ASan/UBSan, 0 ошибок (`[mining-beam] rock=23 R=4.49 class=METAL dist=15.6 range=24.5 inRange=YES
beamRGB=154,150,154`; `[belt-types]` байт-в-байт `specs=0.55/0.05/0.62/0.08`); **`make soak` — 120000 кадров,
deaths=0, invariantFails=0, deathPath=ok, writeBack=ok, `radioClaims=3`/`craftDestroyed=2`, `rocks=648
shiny=391`** — **точь-в-точь** бейзлайн §5.13.16 ⇒ доказано, что срез чисто рендерный; **`make shots` —
21/21**. Глазами: `shot_mining_beam.png` — стальной луч из кокпитного дула к металл-глыбе, бело-горячая
сердцевина, точка удара под прицелом, HUD `MINING METAL +42`; `shot_belt_near.png` — **без луча** (глаз вне
`MINE_RANGE`: тост `MINING SILICATE +0` цел, но луч не рисуется — гейт «в зоне» работает). Орбиты §5.13.2 и
орто-карта не тронуты. Правки: `localdraw.cpp`, `shot_test.cpp`.

---

#### 5.13.18 Автономный срез СИМУЛЯЦИИ — зеркала-торговцы продают груз на местном рынке при стыковке (2026-07-28)
**Не по плейтесту** — автономный проход, второй срез ПОСЛЕДСТВИЙ после §5.13.14. §5.13.9–12 дали живой трафик
(торговцы летят к рынкам, стоят у причала, уходят), а §5.13.14 — первый write-back в макро (сбитое зеркало
«умирает» и в постоянном мире). Но оставался Known-Gap: **торговцы визуально швартуются, но НИЧЕГО не покупают
и не продают** — экономика читала мир, но не писала в него. Этот срез замыкает петлю: co-located зеркало реального
макро-торговца при швартовке **реально продаёт** груз на местном рынке — двигаются кредиты, груз, цены рынка и казна
фракции. Строго аддитивно, **детерминированно (§2.3, `sellCargo` — чистая арифметика без rng)**.

1. **Обёртка `localDockSellCargo(Game&, int agentIndex, int starIndex)` (декл. `game.h`, опр. `game.cpp`).** По
   паттерну §5.13.14 `downgradeAgentToEscapePod`: тонкий мост, зовущий существующий макро-`sellCargo(game, agent,
   starIndex)` (продаёт передний стак `cargo[0]` по дефолту). **КРИТИЧНО:** `sellCargo` лежит в анонимном namespace
   `game.cpp` (внутренняя линковка), поэтому обёртка ОБЯЗАНА жить **вне** этого namespace (после его `}`), чтобы
   иметь внешнюю линковку для `localsim.cpp`, но при этом видеть `sellCargo` по неквалифицированному поиску в той же
   единице трансляции. (Первая попытка положить её рядом с `sellCargo` дала undefined-symbol на линковке — урок в код.)
2. **Хук продажи в `localsim.cpp`, ровно на переходе швартовки `errand 0→1`.** Сразу после `c.errand = 1; …; c.vx*=0.15…`
   (гашение хода на причал) — гейт: `c.kind == CK_TRADER && c.agentIndex >= 0 && c.agentIndex < agents.size() &&
   scene.starIndex >= 0 && !agents[c.agentIndex].ship.cargo.empty()`. Захватываем символ переднего стака (`cargo[0].element` —
   это `std::string`, готовый для тоста) ДО продажи, зовём `localDockSellCargo`. Это тот же момент, что макро-`updateTrader`
   выбирает для продажи (на прибытии). **SELL-ONLY** — намеренно без `buyCargo`/`startJourney`: те трогают глобальный rng и
   макро-локацию (§2.3-нарушение из локального режима). Двойного счёта нет: **макро-сим на время локального полёта заморожен**
   (main.cpp `if (localScene.active) … else if (!paused) advanceGame(…)`).
3. **Читаемость — дебаунс-тост.** На успешной продаже: `++scene.tradesExecuted`, тост `"TRADER SOLD " + <SYM>`
   (`toastTimer=2.5`), гейт `tradeCooldown<=0 && toastTimer<=0` (не дребезжим при пачке швартовок), `tradeCooldown=5.0`;
   покадровый спад `tradeCooldown` рядом со спадом `toastTimer`. Новые поля `LocalScene`: `tradeCooldown`, `tradesExecuted`.
   Draw-код НЕ тронут (тост идёт существующим `drawText`-путём) — shot_asan не обязателен, но прогнан для полноты.
4. **Детерминированный sell-write-back-пробник (`soak_test.cpp`).** По паттерну §5.13.14: свежий `Game g3`, берём элемент
   с макс. рыночной ценой (гарантия выручки > 0), кладём не-игроку в трюм, делаем craft[0] зеркалом-торговцем в круизе,
   пиним его на теле-цели → срабатывает переход швартовки → продажа. Ассерт в ПОСТОЯННОМ мире: `tradesExecuted > 0 &&`
   груз уменьшился `&&` деньги выросли. Плюс инвариант «деньги всех агентов конечны» после основного цикла и счётчик
   `tradesTotal` (органические сделки за soak) в `SOAK DONE`.

**Проверки (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0); **`make shot_asan` — 21/21** под
ASan/UBSan, 0 ошибок (`[mining-beam]`/`[belt-types]` байт-в-байт — draw не тронут); **`make soak` — 120000 кадров,
invariantFails=0, `sellWriteBack=ok`** (новый пробник: груз↓, кредиты↑, сделка прошла) + инвариант финитных денег;
`deaths=0 radioClaims=3 craftDestroyed=2 rocks=648 shiny=391 deathPath=ok writeBack=ok` — **точь-в-точь** бейзлайн
§5.13.16/17 ⇒ доказан нулевой регресс существующей симуляции; `trades=0` органически (soak-харнесс палит по всем бортам ⇒
торговцы гибнут до мирной стыковки — пробник моделирует мирный путь, который в живой игре штатный). Глазами `make shots`:
`shot_traffic_dock.png` (пришвартованный TRADER у причала — контекст, где срабатывает продажа) и `shot_mining_beam.png`
(§5.13.17 луч цел) без регресса. Правки: `game.h`, `game.cpp`, `local.h`, `localsim.cpp`, `soak_test.cpp`.

---

#### 5.13.19 Автономный срез РЕНДЕРА — пылевые полосы и эмиссионные ядра туманности (структурный газ) (2026-07-28)
**Не по плейтесту** — автономный проход, срез РЕНДЕРА в чередовании после симуляционного §5.13.18. §5.13.8 сделал
туманность объёмным газовым полем на небесной сфере, но она осталась **единым ровным полупрозрачным слоем** (Known-Gap:
«нет пылевых полос, поглощающих свет, и нет эмиссионного разгорания у плотнейших волокон»). Этот срез углубляет
`renderNebula` двумя строго аддитивными проходами поверх прежней плотности `dens`, **не трогая** ни один параметр старой
модели облаков. Draw-only, **детерминированно** (§2.3: только `scene.fxClock` + направление луча, ноль RNG), **перспектива-only**
(§2.7: орто-ветка — ОТДЕЛЬНЫЙ код-путь `fillRect` + 3 блоба, побитово цел), изолировано от звезды/колец/астероидов.

1. **Пылевые полосы (тёмная поглощающая пыль).** ОТДЕЛЬНЫЙ доменный варп `w2` (иные фазы/частоты, чтобы полосы
   НЕ совпадали со сгустками газа) → координаты `lx/ly/lz`; поле `laneField = sin(lx·3.4+ly·2.1) + 0.6·sin(lz·6.0−lx·1.7)`
   — СУММА синусов даёт полосовую (нитевидную) структуру (в отличие от произведения-сгустков §5.13.8); `lane = min(1, |laneField|·1.7)`
   (узкие полосы: `lane<1` только у нулевого контура поля); `dens *= 0.20 + 0.80·lane` — ядро полосы гасит свечение газа до
   ~20%, и т.к. альфа ∝ `dens`, полоса ещё и прозрачнее (сквозь «пыль» проступает тёмное небо со звёздами) = физичное поглощение.
2. **Эмиссионные ядра (звёздные ясли).** После пыли: `core = dens³` — кубический отклик разгорается ТОЛЬКО у пиков
   плотности (при малой плотности `core≈0` ⇒ строго аддитивно, вне ядер вид прежний); `emit = 0.55·core`. Композит:
   `a = MAXA·dens·(1 + 0.6·core)` (ядра чуть плотнее/непрозрачнее) и `wf = clamp(0.30·dens + emit, ≤0.92)` (ядра ярче
   сдвигаются к белому, сохраняя намёк на тон газа). Обе правки вставлены между строкой контраста плотности и композитом альфы/цвета.
3. **Осознанно ОТЛОЖЕНО.** Эмиссия ВОКРУГ вкраплённых LOD-скайбокс-звёзд (они рисуются ОТДЕЛЬНО и ПОСЛЕ туманности)
   потребовала бы прокинуть `game.cluster.stars` + якорь в `renderNebula`, сломав её свойство leaf/draw-only/zero-coupling.
   Оставлено следующему срезу; процедурные ядра (п.2) уже дают «горячие» узлы без этой связности.

**Проверки (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0); **`make shot_asan` — 21/21** под
ASan/UBSan, 0 ошибок (`[mining-beam]`/`[belt-types]` байт-в-байт ⇒ не-туманные пути не тронуты); **`make soak` — 120000
кадров, invariantFails=0**, `radioClaims=3 craftDestroyed=2 rocks=648 shiny=391 deathPath=ok writeBack=ok sellWriteBack=ok
trades=0` — **точь-в-точь** бейзлайн §5.13.18 ⇒ доказан чистый draw-only (симуляция не тронута). Глазами `make shots` **21/21**:
`shot_nebula.png` — тёмная пылевая полоса рассекает магентовый газ + эмиссионные ядра в плотных областях; `shot_system_cockpit.png`
— плазменная звезда/пояс/планеты/HUD без регресса. Шот не добавлял: `shot_nebula` уже форсит перспективную туманность
(`nebulaStrength=0.60`) и детерминированно лочит эффект при `fxClock=0`. Правки: `localdraw.cpp`, `master_prompt.md`, `README.md`.

---

#### 5.13.20 Автономный срез СИМУЛЯЦИИ — композиционно-взвешенная генерация пояса (состав пояса ≡ состав системы) (2026-07-28)
**Не по плейтесту** — автономный проход, срез СИМУЛЯЦИИ в чередовании после рендер-среза §5.13.19. §5.13.15/16 сделали
породу астероида (класс/облик/темп добычи) производной от его элемента, но САМ выбор элемента в поясе был **состав-слеп**:
ветка `resourceFocus` брала элемент равновероятно, а фолбэк — первый металл со сдвига (Known-Gap: «rates спавна пород не
зависят от того, металлическая система или ледяная»). Этот срез делает класс камня производным от СИСТЕМЫ-хозяина, замыкая
петлю добычи §5.13.15–17 (появляется смысл искать металло-богатые системы). Чистая правка `buildLocalScene` (`localgen.cpp`);
draw-код не тронут.

1. **Профиль пояса до цикла (уровень системы, НОЛЬ `lrng`).** Перед циклом камней (чистая арифметика над `star`/`defs`):
   пулы индексов элементов по классам `classPool[4]` (через `rockClass(j)`) и подмножество `resourceFocus`, разложенное по
   классам, `focusPool[4]`. Веса классов `w[4]`: `silicate=1.6` (S-тип — обычный дефолт), `metal=0.5+2.6·mNorm`,
   `ice=0.5+1.8·(1−mNorm)`, `carbon=0.8`, плюс `+0.9·|focusPool[c]|` (экономический профиль системы), и `w[c]=0` там, где
   `classPool[c]` пуст (нельзя выбрать класс без элементов в таблице). `mNorm=clamp((star.metallicity−0.22)/0.95, 0, 1)` —
   диапазон генерации металличности `[0.22, 1.17]` нормируется в `[0,1]` (металличность МОЖЕТ превышать 1.0, `cluster.cpp`).
2. **Выбор в цикле по ДЕТЕРМИНИРОВАННОМУ хэшу (НЕ `lrng`).** Если `wsum>0`: класс — по CDF от `uc=fract(sin(i·78.233+12.9898)·43758.5453)`
   (константы иные, чем у §5.13.15-цвета ⇒ класс и яркостный джиттер декоррелированы); элемент — из `focusPool[cls]` (что
   система реально добывает), иначе из общего `classPool[cls]`, индекс — от второго хэша `fract(sin(i·39.425+7.311)·43758.5453)`.
   Пул пуст → остаётся базовый `elem`.
3. **Стрим-якорь (§2.3, КЛЮЧ к нулевому регрессу спавна).** Базовый выбор элемента (`irand` по `resourceFocus` / металлический
   скан) СОХРАНЁН 1-в-1 — он потребляет ровно те же розыгрыши `lrng`, что и раньше (`uniform_int_distribution` съедает
   зависящее-от-диапазона число вызовов движка). Итог лишь ПЕРЕЗАПИСЫВАЕТСЯ хэш-взвешиванием (ноль `lrng`). Поэтому всё, что
   генерируется ПОСЛЕ пояса (станция, трафик-NPC, радиоисточники, write-back-цели), спавнится ПОБИТОВО как прежде.

**Проверки (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0); **`make shot_asan` — 21/21** под
ASan/UBSan, 0 ошибок (индексация `w[4]`/`classPool`/`focusPool`/`pool[idx]` чиста; `[belt-types]` форс-пробник байт-в-байт —
он конструирует камни напрямую, минуя взвешивание); **`make soak` — 120000 кадров, invariantFails=0**,
`radioClaims=3 craftDestroyed=2 rocks=648 deathPath=ok writeBack=ok sellWriteBack=ok trades=0` — **точь-в-точь** бейзлайн
§5.13.19 (стрим сохранён), а `shiny` 391→**382** — ЕДИНСТВЕННЫЙ ожидаемый сдвиг: это ОТПЕЧАТОК ФИЧИ. Общая доля блеска
осталась ~59% (было ~60%), но теперь она РАСПРЕДЕЛЕНА по составу системы (металло-богатые → металл, бедные → лёд), а не по
прежней дихотомии пустой/непустой `resourceFocus`. Глазами `make shots` **21/21**: `shot_belt_near.png` — силикат в прицеле
(`MINING SILICATE`) среди металло/ледяных глыб; `shot_mining_beam.png` — металлический камень (`MINING METAL +42`, повышенный
выход ×1.60). Правки: `localgen.cpp`, `master_prompt.md`, `README.md`.

---

#### 5.13.21 Автономный срез РЕНДЕРА — тень колец на планете (завершение модели «Сатурна») (2026-07-28)
**Не по плейтесту** — автономный проход, срез РЕНДЕРА в чередовании после симуляционного §5.13.20. §5.13.5 дал кольцам
щели Кассини, grazing-opacity, композит по глубине и тень ТЕЛА НА КОЛЬЦАХ — но обратной тени (КОЛЬЦА НА ПЛАНЕТЕ) не было
(Known-Gap: «нет тени колец обратно на планету — только тень планеты на кольцах»). Этот срез добавляет недостающую
половину, замыкая физику освещения Сатурна. Чистый draw-only в `renderBodySphere` (`localdraw.cpp`), внутри уже
существующего блока шейдинга сферы; никаких новых полей/структур, ноль правок генерации.

1. **Геометрия (зеркало §5.13.5).** Тень тела на кольцах считает прицельный параметр луча кольцо→звезда относительно
   центра тела. Обратный эффект: из освещённой точки поверхности `S` луч к звезде (в 0,0,0), направление `−S/sl`;
   пересечение с плоскостью колец (нормаль — та же ось `A`, что у композита колец): `t = −(S−C)·A / (dir·A)`. Гейты:
   `|dir·A|>1e-6` (не параллельно), `1e-4<t<sl` (кольцо МЕЖДУ поверхностью и звездой). Точка `Q = S·(1−t/sl)`, радиус в
   плоскости от центра `C` → проверка `Ri²≤rad²≤Ro²`.
2. **Плотность = та же модель колец.** В точке пересечения берём ТУ ЖЕ формулу, что рисует кольца (мягкие кромки `edge`,
   деления Кассини двумя гауссианами, полосы `bands`): `occ = edge·gap·bands·0.80`, клампится в `[0,0.85]`. Значит сквозь
   ЩЕЛЬ Кассини (где `gap→0`) тени нет — в шоте видно как светлый разрыв в тёмной полосе (физично: щель пропускает свет).
3. **Композит.** `diff *= (1−occ)` — кольца гасят ПРЯМОЙ свет, амбиент кластера остаётся (`lightF = ambient+(1−ambient)·diff`),
   поэтому тень — глубокая полутень (~28% в умбре), не чёрная (кольца полупрозрачны). Дополнительно приглушается и
   атмосферный лимб (§5.13.4), т.к. он ∝ `diff` — физично (в тени меньше рассеяния). Вставлено между клампом `diff` и `lightF`.

**Нулевой регресс — по построению.** Весь блок под `if (hasRing && diff>0 && sl>1e-9)`; у НЕ-газовых-гигантов и у
гигантов без колец `hasRing==false` ⇒ блок не исполняется, `diff` не тронут ⇒ пиксели ПОБИТОВО прежние, а где тень есть —
это и есть новая фича. **Детерминированно** (ноль RNG, даже `fxClock` не нужен), **перспектива-only** (`renderBodySphere`
зовётся лишь под `view.perspective`; орто-тела — отдельная ветка `fillCircle`).

**Проверки (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0); **`make shot_asan` — 21/21** под
ASan/UBSan, 0 ошибок (деление на `dir·A` защищено гейтом `fabs>1e-6`; `sqrt`/`exp` на валидных аргументах); **`make soak`
— 120000 кадров, invariantFails=0**, `radioClaims=3 craftDestroyed=2 rocks=648 shiny=382 deathPath=ok writeBack=ok
sellWriteBack=ok trades=0` — **ВЕСЬ** бейзлайн §5.13.20 цел, включая `shiny=382` (чистый draw ⇒ ноль влияния на генерацию,
в отличие от §5.13.20). Глазами `make shots` **21/21**: `shot_planet_near.png` — тёмная полоса тени колец рассекает диск
газового гиганта STAR70, щель Кассини читается как светлый просвет внутри тени (Сатурн в 3D). Правки: `localdraw.cpp`,
`master_prompt.md`, `README.md`.

---

#### 5.13.22 Автономный срез СИМУЛЯЦИИ — запас руды пояса = класс породы × насыщенность системы (2026-07-28)
**Не по плейтесту** — автономный проход, срез СИМУЛЯЦИИ в чередовании после рендерного §5.13.21. §5.13.16 связал КЛАСС
породы с ТЕМПОМ добычи (`rockYieldMult`), §5.13.20 сделал состав пояса ≡ составу системы — но `rock.ore` (сколько всего
руды в камне) оставался плоским `frand(20,120)`, слепым И к классу, И к `star.miningRichness`. Причём `miningRichness`
(∈[0.45,1.95], `cluster.cpp`) влияла ТОЛЬКО на макро-добычу (`mining.cpp:79`) — локальный пояс её игнорировал целиком
(Known-Gap). Этот срез добавляет НЕЗАВИСИМУЮ ось «тоннаж руды» (в отличие от §5.13.16 «темп»): металлическая глыба в
богатой системе несёт заметно больше продаваемой массы, чем углеродная в бедной — так композиция §5.13.20 наконец
окупается экономически. Чистая генерация в `buildLocalScene` (`localgen.cpp`) + помощник в `local.h`; ноль правок рендера.

1. **Помощник `oreRichnessMult(rc, miningRichness)` (`local.h`, рядом с `rockYieldMult`).** Возвращает `qty·fRich`:
   `qty` — по классу (`ORE_QTY_METAL 1.50 / SILICATE 1.00 / ICE 0.90 / CARBON 0.80` — новые константы `LocalCfg`,
   ОТДЕЛЬНЫЕ от `MINE_YIELD_*`, т.к. «сколько руды» ≠ «как быстро»); `fRich = 0.6+0.8·clamp((miningRichness−0.45)/1.5,0,1)`
   ∈[0.6,1.4]. Чистая функция, ноль RNG (§2.3). Обе компоненты нормированы к среднему ≈1.0 (silicate·средняя-насыщенность
   = ×1.0 ⇒ дефолт-класс без сдвига по величине).
2. **Применение (`localgen.cpp`, сразу после `rock.element = elem`).** `rock.ore *= oreRichnessMult(rockClass(elem),
   star.miningRichness)`. Ключ (§2.3): множитель ложится на УЖЕ разыгранный `frand(20,120)` (строка выше сохранена
   1-в-1) ⇒ НИ ОДНОГО нового `lrng`-розыгрыша, стрим-якорь §5.13.20 не сдвинут. Диапазон итога: ×0.48 (бедный
   углерод) … ×2.1 (богатый металл) ⇒ `rock.ore` ≈ [9.6, 252]; среднее ≈ прежнее ⇒ ПЕРЕРАСПРЕДЕЛЕНИЕ тоннажа, не инфляция.

**Нулевой регресс спавна/soak — по построению.** Множитель чист (без `lrng`) и применён после стрим-якоря, поэтому поток
генерации байт-в-байт прежний: станция/трафик/радио/write-back спавнятся как раньше. Ни один инвариант `soak` не читает
`rock.ore` (`rocksSeen`/`rocksShiny` — из `spec`/`rockClass(element)`; обе write-back-пробы строят СВОЙ груз, не руду
пояса; `mineToggle` крутится, но добытая масса ни на что в soak не завязана). **Детерминированно**, генерация-only
(рендер `rock.ore` не рисует ⇒ вид пояса ПОБИТОВО прежний).

**Проверки (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0); **`make shot_asan` — 21/21** под
ASan/UBSan, 0 ошибок (путь `buildLocalScene` прогнан под санитайзерами); **`make soak` — 120000 кадров,
invariantFails=0**, `radioClaims=3 craftDestroyed=2 rocks=648 shiny=382 deathPath=ok writeBack=ok sellWriteBack=ok
trades=0` — **ВЕСЬ** бейзлайн §5.13.21 точь-в-точь (ноль отпечатка в soak — фича видна лишь в фактических `rock.ore`
в игре, не в счётчиках). Отдельная микро-проба помощника подтвердила значения (silicate@средн=×1.0, metal@богат=×2.1,
carbon@бедн=×0.48 — ограничено и осмысленно). Глазами `make shots` `shot_belt_near.png` — пояс рендерится штатно
(руда не рисуется ⇒ регресса вида нет). Правки: `local.h`, `localgen.cpp`, `master_prompt.md`, `README.md`.

---

#### 5.13.23 Автономный срез РЕНДЕРА — HUD-читаемость запаса руды (класс + остаток цели в кокпите) (2026-07-28)
**Не по плейтесту** — автономный проход, срез РЕНДЕРА в чередовании после симуляционного §5.13.22. §5.13.22 сделал
ТОННАЖ руды камня переменным (класс × насыщенность системы), но в кокпите этот сигнал был НЕВИДИМ: игрок различал
камни лишь по цвету класса (§5.13.15), а «сколько в нём руды» — никак. Этот срез выводит запас в HUD, чтобы игрок мог
ВЫБИРАТЬ богатые глыбы. Чистый draw-only в `localdraw.cpp` (секция HUD), ноль новых полей, ноль правок sim/gen.

1. **Индикатор добычи (пока бурим).** `MINING <CLASS>  +<accum>` → `MINING <CLASS>  +<accum>  ORE <остаток>`
   (`std::snprintf(... "MINING %s  +%.0F  ORE %.0F", rockClassName(rockClass(mr.element)), scene.miningAccum, mr.ore)`).
   Теперь видно, как `rock.ore` тает к нулю → тосту «ROCK DEPLETED» (§5.13.21-эпохи механика истощения) — живой отклик.
2. **Подсказка добычи (цель в зоне, ещё не бурим).** `PRESS M TO MINE` → `PRESS M TO MINE - <CLASS>  ORE <остаток>`
   для `scene.rocks[scene.minePrompt]`. Игрок читает класс+запас ДО нажатия M ⇒ прямой пейофф §5.13.22: металлическая
   глыба в богатой системе показывает заметно больший `ORE`, чем углеродная в бедной. Добавлен баунд-чек
   `minePrompt >= 0 && minePrompt < (int)rocks.size() && miningRock < 0` (раньше гейт проверял лишь `>= 0`).

**Формат/шрифт (§2.5/§2.6).** Правило «35 символов» — про ОПРЕДЕЛЕНИЯ глифов шрифта (5×7), а не про строки в `drawText`
(тот рисует произвольную длину — ср. dock-подсказка). Формат `%.0F` — заглавный (§2.6); `rockClassName` уже UPPERCASE
+ шрифт автокапс. Строки центрируются (`cx - textWidth/2`, scale 2), не перекрывают радар/панель скорости/легенду.
Цвет — янтарь `P.amber` (класс-цвета типа углерода слишком тёмные для текста на чёрном фоне — читаемость важнее).

**Нулевой регресс.** Только две строки HUD стали длиннее (плюс баунд-чек, сужающий условие показа). Ни симуляция, ни
генерация не тронуты; `soak` headless (не рисует HUD) ⇒ весь бейзлайн цел. **Перспектива и локальная орто-C-карта**
делят секцию HUD (как и прежний §5.13.16-индикатор); §2.7 бит-в-бит защищает МАКРО-виды (`render2d.cpp`), которых срез
не касается. Ноль RNG, детерминированно.

**Проверки (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0); **`make shot_asan` — 21/21** под
ASan/UBSan, 0 ошибок (новый баунд-чек `minePrompt < rocks.size()` защищает индексацию); **`make soak` — 120000 кадров,
invariantFails=0**, `radioClaims=3 craftDestroyed=2 rocks=648 shiny=382 deathPath=ok writeBack=ok sellWriteBack=ok
trades=0` — **ВЕСЬ** бейзлайн §5.13.22 точь-в-точь (soak не рендерит HUD). Глазами `make shots`: `shot_mining_beam.png`
показывает `MINING METAL  +42  ORE 80` — индикатор с остатком руды под лучом добычи (правка №1). Правка №2 (подсказка) —
тот же путь рендера (snprintf→центрированный `drawText` янтарём) с теми же полями и баунд-чеком, покрыта ASan; отдельного
шота на состояние «цель-в-зоне-но-не-бурим» в наборе нет (не ломаем инвариант «21 шот»). Правки: `localdraw.cpp`,
`master_prompt.md`, `README.md`.

#### 5.13.24 Автономный срез СИМУЛЯЦИИ — награда за розыск (bounty) на пиратах (2026-07-28)
**Не по плейтесту** — автономный проход, срез СИМУЛЯЦИИ в чередовании после рендер-§5.13.23. После ЧЕТЫРЁХ подряд
«добывающих» срезов (§5.13.16 темп, §5.13.20 состав пояса, §5.13.22 тоннаж, §5.13.23 HUD) открыт НОВЫЙ игровой слой —
боевая экономика розыска: часть пиратов помечены «в розыске» и платят бонус при убийстве. Это развитие §5.13.11
(`CONVOY SAVED`, пейофф лишь за пирата-налётчика на конвой) в самостоятельный стимул ОХОТИТЬСЯ за пиратами вообще.

1. **Генерация (детерминированно, ноль `lrng`).** В цикле процедурных пиратов (`localgen.cpp`) после розыгрыша позиции:
   `hw = fract(sin((starIndex·131+p)·51.017+4.271)·43758.5453)`; при `hw>0.60` (~38%) → `c.wanted=true`, а сумма
   `c.wantedBounty = 250 + floor(hb·27)·25` (250..900, шаг 25) по ВТОРОМУ декоррелированному хэшу `(starIndex·197+p)`.
   Та же техника «хэш индекса вместо `lrng`», что состав пояса §5.13.20 и тоннаж §5.13.22 ⇒ **ноль новых `lrng`,
   стрим-якорь §5.13.20 цел**, спавн станции/трафика/радио/камней ПОБИТОВО прежний.
2. **Пейофф (блок убийства `(E)`, `localsim.cpp`).** Если сбитый — пират с `wanted`: `game.agents[playerAgent].money +=
   c.wantedBounty`, `addResearch(1.5)`, тост `BOUNTY CLAIMED +<N> CR`, лёгкий шейк. Начисляется ПОВЕРХ базовой награды
   (`SHIP DOWN`) И поверх `CONVOY SAVED`, если тот же пират был налётчиком (все кредиты складываются; тост показывает
   заголовочную сумму розыска). Новые поля `LocalCraft.wanted`/`wantedBounty` (`local.h`).

**§2.3 / combat-инвариант / §2.1.** Флаг `wanted` НЕ читается ИИ/выбором цели/уроном — пиратов НЕ ослабляли и НЕ усиливали,
только добавили НАГРАДУ (combat-инвариант «не трогать агрессию/урон» соблюдён; чистое ADDITIVE). Деньги ограничены
(≤900 за убийство, конечное число раз) ⇒ finite. Данные — только plain-поля структуры + свободные хэши (§2.1).

**Нулевой регресс (soak ПОБИТОВО).** `make soak` 120000 кадров: `invariantFails=0`, и ВЕСЬ бейзлайн §5.13.23 точь-в-точь —
`rocks=648 shiny=382 trades=0 radioClaims=3 craftDestroyed=2 deaths=0 deathPath=ok writeBack=ok sellWriteBack=ok`.
Ключевое: `craftDestroyed=2` НЕ сдвинулся (флаг не влияет на бой ⇒ число убийств прежнее), `rocks/shiny` целы (ноль
`lrng`), инвариант «`money` конечно» держится под выплатами бонуса. Метка не рисуется (визуал — следующий рендер-срез).

**Проверки (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0); **`make shot_asan` 21/21** под
ASan/UBSan, 0 ошибок (смена layout `LocalCraft` из-за 2 новых полей UB-чиста, render-путь цел); **`make soak` — 120000
кадров, invariantFails=0**, весь бейзлайн §5.13.23 (см. выше). Позитив-probe (реплика двух хэшей по 240 кандидатам):
`wanted≈38.3%`, `bounty=[250..900]`, детерминизм (тот же вход→тот же выход) ok. Правки: `local.h`, `localgen.cpp`,
`localsim.cpp`, `master_prompt.md`, `README.md`.

#### 5.13.25 Автономный срез РЕНДЕРА — розыск в кокпите (WANTED-панель + золотое кольцо + bounty) (2026-07-28)

Награда за розыск (§5.13.24) была невидима пилоту — начислялась при убийстве, но ничто в кабине не подсказывало,
за кого платят. §5.13.25 выводит её тремя золотыми сигналами (все `255,205,60`; темп пульса `sin(fxClock·5.2)`
намеренно ≠ швартовка 3.0 / эскорт 4.5 / SOS 6.0 — цвет-близкий к янтарю розыск читается ещё и по ритму):

1. **Кольцо-розыск у борта.** В цикле кораблей `localdraw.cpp` для `c.wanted` — два `strokeCircle` `sz+9`/`sz+14`
   (вне cyan-рамки цели `sz+6` и зелёного эскорта `sz+8/sz+13`), пульс альфы 85..215.
2. **Панель цели.** Новое состояние `wnt = t.wanted` рядом с `sos`/`esc`. Розыск ставится ТОЛЬКО пиратам ⇒
   взаимоискл. с `sos` (жертва)/`esc` (патруль), поэтому ветка `wnt` вставлена ПЕРЕД фолбэком `t.hostile?red`
   в тернаре цвета метки; золотая рамка панели + чип `WANTED` (после `LOCK` в цепочке) + строка `BOUNTY %.0F CR`
   (§2.6) под корпусом, высота панели `ph += 14`.
3. **Краевой маркер.** Поиск `nearWanted` (ближайший `wanted && hullHP>0`) + `drawEdgeMarker(… 255,205,60)` —
   точная параллель `nearVic`/`nearDef` §5.13.11/.12; ведёт к премиальной цели за кадром.

Строго draw-only: ноль RNG, ноль правок симуляции/генерации; `wanted` только читается. Новый шот №18 `shot_wanted`
(пират, `wantedBounty=650`, наведён) — золотое кольцо + панель `WANTED`/`BOUNTY 650 CR`; харнес 21→**22 кадра**.

**Проверки (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0); **`make shot_asan` 22/22** под
ASan/UBSan, 0 ошибок (правка только render-путь); **`make soak` — 120000 кадров, invariantFails=0**, весь бейзлайн
§5.13.24 ПОБИТОВО (`rocks=648 shiny=382 trades=0 radioClaims=3 craftDestroyed=2`; soak headless ⇒ HUD не рисуется).
Глазами: `shot_wanted.png` — золотые кольцо+рамка+чип+`BOUNTY 650 CR`; `shot_convoy_distress`/`shot_patrol_escort`
без регресса (SOS-красный/эскорт-зелёный чисты, золото не вторгается). Правки: `localdraw.cpp`, `shot_test.cpp`,
`master_prompt.md`, `README.md`.

#### 5.13.26 Автономный срез СИМУЛЯЦИИ — свидетельство пиратства пишется в макро (NPC-vs-NPC write-back + эмерджентный розыск) (2026-07-28)

Write-back в постоянный мир (§5.13.14) имел единственную асимметрию: он срабатывал ТОЛЬКО на убийстве игроком.
Смерть NPC-vs-NPC — единственная точка `localsim.cpp`, где корабль гибнет без участия игрока (прямой DPS пирата по
не-пирату, `localsim.cpp:644`) — была для макро инертной: на убийстве стоял лишь `spawnWreck(...)` с комментарием
«без награды/фракций». §5.13.26 закрывает разрыв на ТОМ ЖЕ участке (`localsim.cpp:662`) двумя строго аддитивными
последствиями (нулевой RNG, ноль новых полей — переиспользуем §5.13.14/.24):

1. **(A) Write-back жертвы.** Если сбитый борт `o` — зеркало макро-агента (`o.agentIndex>=0` и `≠ playerAgent`),
   зовём `downgradeAgentToEscapePod(game.agents[o.agentIndex])` — ровно та семантика, что в player-kill (§5.13.14):
   корабль агента деградирует в спас-капсулу, груз/оружие сброшены, КРЕДИТЫ и глобальный RNG НЕ трогаем. Пиратский
   налёт, увиденный в кабине, теперь реально стирает торговца из персистентного мира.
2. **(G) Эмерджентный розыск налётчика.** Если убийца `c` — пират, а жертва `o` — НЕ пират, пират попадает «в
   розыск» в реальном времени: `if(!c.wanted){c.wanted=true; c.wantedBounty=300;} else {c.wantedBounty=min(1500, +200);}`.
   Гейт `c.kind==CK_PIRATE` не метит патруль, добивший пирата (правосудие ≠ преступление). Розыск §5.13.24 перестаёт
   быть только предгенерённым — он ещё и ЭМЕРДЖЕНТНЫЙ: злодейство при вас поднимает цену, зажигает золотые подсказки
   кокпита (§5.13.25) на пирате, а награда выплачивается при его убийстве (§5.13.24). Замыкается петля §5.13.11→.12→.24→.25→.26.
3. **Детерминированный пробник `npcWriteBack` (`soak_test.cpp`).** Симметричен player-kill-пробнику §5.13.14: свежий
   `Game g4`, ровно два борта (пират[0] + зеркало-торговец[1], `agentIndex=ai`), игрок отставлен на 100000 LU (вне
   радиуса осведомлённости ⇒ пират целит в NPC) и НЕ стреляет; оба пиньятся в упор, жертве держим щит=0/корпус=1 →
   первый залп пирата добивает. Ассерт: агент `ai` в макро стал `Escape Pod`, оружие=0, груз пуст. Поле `npcWriteBack=`.

**Проверки (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0); **`make soak` — 120000 кадров,
invariantFails=0**, ВСЕ числовые счётчики §5.13.25 ПОБИТОВО (`craftDestroyed=2 rocks=648 shiny=382 trades=0
radioClaims=3 deaths=0`), 0 ошибок ASan/UBSan, +1 строго аддитивное поле-пробник `npcWriteBack=ok` (доказывает, что
(A) срабатывает на NPC-vs-NPC), причём числовой детерминизм не сдвинут — значит и (G) soak-безопасен. Новый бейзлайн:
`SOAK DONE frames=120000 deaths=0 radioClaims=3 craftDestroyed=2 invariantFails=0 deathPath=ok writeBack=ok
sellWriteBack=ok npcWriteBack=ok rocks=648 shiny=382 trades=0`. **22/22** шотов без регресса (`renderLocalScene` не
тронут ⇒ `shot_asan` не требовался; `wanted`-визуал уже покрыт `shot_wanted` §5.13.25). Правки: `localsim.cpp`,
`soak_test.cpp`, `master_prompt.md`, `README.md`.

#### 5.13.27 Автономный срез РЕНДЕРА — радар кодирует состояние контакта цветом (2026-07-28)

Рекогносцировка §5.13.27 начала с ведущей идеи §5.13.26 («сигнатура свежего обломка/спас-капсулы на месте NPC-vs-NPC
гибели») и **дисквалифицировала её как НЕ-draw-only**: в момент отрисовки жертва уже swap-popнута из `scene.craft`
(`localsim.cpp:920–935`), обломок-FX несёт лишь цвет корпуса жертвы (без типа смерти/`agentIndex`), а спас-капсула
живёт только в замороженном макро-мире. Любая честная версия требует НОВОГО симуляционного сигнала, который headless
soak (его пробник `npcWriteBack` реально прогоняет `spawnWreck`) исполнил бы ⇒ риск для бейзлайна. Поэтому взят
провабельно draw-only кандидат: **раскраска контактов радара по состоянию**.

Радар (низ-справа) был ЕДИНСТВЕННОЙ панелью кабины, полностью слепой к угрозе/бедствию: цикл блипов
(`localdraw.cpp:1979`) красил точку `c.hostile ? P.red : серый` — красный у любого агрессора, серый у всех прочих.
При этом радар бьёт на 1400 LU (дальше любого краевого маркера, который указывает лишь на ОДИН ближайший контакт), так
что массовый налёт и рутинный трафик на нём были неразличимы.

1. **Раскраска блипа по живым полям (`localdraw.cpp`, цикл радара).** Единственное решение цвета заменено лестницей
   приоритета, читающей уже выставляемые каждый кадр поля борта (сброс/установка в проходе ИИ `localsim.cpp:338/341/
   410/476`): `c.wanted`→золотой `255,205,60` (пульс `sin(fxClock·5.2)`); иначе `c.underAttack`→SOS-красный `P.red`
   (пульс `·6.0`); иначе `c.defending`→зелёный `90,220,132` (пульс `·4.5`); иначе `c.hostile`→ровный красный; иначе
   тусклый серый `150,165,182`. Пульсовые темпы и цвета — те же, что у кабинных подсказок §5.13.11/.12/.25 ⇒ единый
   визуальный язык. Ветка `wanted` первой, т.к. розыскной пират одновременно `hostile` (иначе золото перебил бы
   красный). Альфа пульса 150→255 по фазе + смещение `double(i)` ⇒ блипы не мигают в унисон. Радиус/примитив блипа не
   тронуты (в дальности — `fillRect` 3×3, вне — `strokeCircle` r=2).
2. **Новый шот №19 `shot_radar_threat` (`shot_test.cpp`).** Пять бортов в пределах дальности в пяти состояниях
   (розыск/SOS/эскорт/простой враг/нейтраль), разнесённые по `(dx,dy)` (радар игнорирует ось `pup`), камера-нос вдоль
   `+X`; цель не задана (`targetCraft=-1`) ⇒ фокус на радаре. Ожидаемые пиксели блипов выверены скриптом: золото
   `(254,204,59)`, SOS-красный `(206,77,74)`, зелёный `(73,177,110)`, ровный красный `(238,88,82)`, серый
   `(119,132,148)` — все пять веток достижимы и различимы; приоритет подтверждён (борт[0] `wanted+hostile` = золотой).

**Проверки (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0); **`make shot_asan` — 23/23** под
ASan/UBSan (правка `localdraw.cpp` ⇒ обязательна), 0 ошибок; **`make soak` — 120000 кадров, invariantFails=0**, ВЕСЬ
числовой бейзлайн §5.13.26 ПОБИТОВО (`SOAK DONE frames=120000 deaths=0 radioClaims=3 craftDestroyed=2
invariantFails=0 deathPath=ok writeBack=ok sellWriteBack=ok npcWriteBack=ok rocks=648 shiny=382 trades=0`; soak
headless ⇒ `renderLocalScene` не зовётся ⇒ радар не влияет), 0 ошибок ASan/UBSan; `make shots` **23/23**, глазами
`shot_radar_threat.png` (пять цветов блипов) + перебазированы `shot_convoy_distress`/`shot_patrol_escort` без регресса
(их радар теперь тоже красит жертву `underAttack` красным и патруль `defending` зелёным). Правки: `localdraw.cpp`,
`shot_test.cpp`, `master_prompt.md`, `README.md`.

#### 5.13.28 Автономный срез СИМУЛЯЦИИ — закон охотится на розыскного (патруль → `wanted`-пират) (2026-07-28)

Рекогносцировка §5.13.28 (субагент + собственный пробник `/tmp/scprobe.cpp`, слинкованный с `LIBSOURCES` соака)
установила решающий факт: **ветка `CK_PATROL` (`localsim.cpp:382`) в headless-соаке НЕ исполняется НИКОГДА** —
процедурная генерация плодит только торговцев+пиратов, трафик-спавн только торговцев/civilian, макро заморожен ⇒
ни одного патруля во всех 10 стартовых системах. При этом `wanted`-пираты в соаке ЕСТЬ (и предген §5.13.24, и
рантайм §5.13.26(G) — эмпирически подтверждено пробником: 4 из 10 систем наращивают счётчик розыска в 120k-прогоне),
но `wanted` читался лишь на выплате-убийстве и в рендере, не в траекторной логике, оттого 120k-бейзлайн был побитово
стабилен. Значит правило, читающее `wanted` ТОЛЬКО в ветке патруля, **гарантированно байт-в-байт** к соак-бейзлайну —
не вероятностно, а по построению; при этом в живой игре, где игрок ныряет в населённые системы с патрулями, оно
полноценно работает. Так замыкается закон-цикл §5.13.24 (предген розыска) → §5.13.26(G) (розыск за убийство) →
§5.13.27 (золотой блип радара) → §5.13.28 (патруль летит на золотой контакт).

1. **Приоритет-0 «охота на розыскного» (`localsim.cpp`, ветка `CK_PATROL`).** Перед прежним сканом налётчика
   (§5.13.12) добавлен скан ближайшего `wanted`-пирата в радиусе бедствия `PATROL_DISTRESS_R` (1400 LU) и БЕЗ
   условия «вплотную к беззащитному борту» (в отличие от рейдер-скана): объявленный вне закона есть цель сам по
   себе, даже когда прямо сейчас не грабит. При находке — цель патруля=розыскной, `atkBest`=его дистанция,
   `defending=true`; иначе (нет розыскного) исполняется прежняя ветка налётчик→фолбэк целиком (обёрнута в `else`).
   Блок «спровоцированный патруль предпочитает более близкого игрока» (§5.13.12) сохранён и применяется поверх всех
   веток. Читаем живое поле `o.wanted` — ноль RNG, ноль новых полей. Огонь всё так же заперт `WEAPON_RANGE` (150 LU):
   патруль обязан сблизиться и драться в упор, а не «снайпить» с 1400 LU. Комбат-инвариант цел — правим ТОЛЬКО выбор
   цели ПАТРУЛЯ; урон/агрессию/ИИ пирата не трогаем. На радаре (§5.13.27) зелёный (`defending`) патруль теперь видимо
   сходится с золотым (`wanted`) пиратом — рендер-слайсы и sim-слайс складываются в одну читаемую картину.
2. **Новый детерминированный soak-пробник `patrolHuntsWanted` (`soak_test.cpp`).** Ветка патруля headless не
   исполняется ⇒ этот изолированный пробник — ЕДИНСТВЕННОЕ headless-покрытие ИИ патруля. Свежий `Game`, две
   подпроверки с пиньнутыми позициями (ноль RNG, ноль движения; игрок в 100000 LU и не стреляет ⇒ бой чисто
   патруль-против-пиратов): **(A)** розыскной пират ДАЛЬШЕ (120 LU), простой БЛИЖЕ (60 LU) ⇒ ассерт «урон получает
   именно РОЗЫСКНОЙ (дальний), а простой (ближний) не тронут» = приоритет-0 перебивает дистанцию (`wantedOverDistance
   =YES`); **(B)** розыскного нет, два простых пирата дальний/ближний ⇒ ассерт «бьётся БЛИЖАЙШИЙ» = фолбэк §5.13.12
   цел (`fallbackNearest=YES`). Пиратам дан огромный корпус (не гибнут за пробу ⇒ индексы стабильны), щит=0 (урон
   сразу в корпус, измерим); патруль пиньнится здоровым (никогда не бежит ⇒ `aiState=1`).

**Проверки (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0); **`make soak` — 120000 кадров,
invariantFails=0**, ВЕСЬ числовой бейзлайн §5.13.27 ПОБИТОВО (`SOAK DONE frames=120000 deaths=0 radioClaims=3
craftDestroyed=2 invariantFails=0 deathPath=ok writeBack=ok sellWriteBack=ok npcWriteBack=ok rocks=648 shiny=382
trades=0`) + новое аддитивное поле `patrolHuntsWanted=ok` (`wantedOverDistance=YES fallbackNearest=YES`), 0 ошибок
ASan/UBSan. **Шоты не требовались**: `localdraw.cpp`/`shot_test.cpp` не тронуты, а `saveShot` рисует один кадр без
шага симуляции ⇒ ИИ-правка на рендер шотов не влияет (и `localdraw.cpp` уже собран под ASan в рамках соака — 0
ошибок). Правки: `localsim.cpp`, `local.h`, `soak_test.cpp`, `master_prompt.md`, `README.md`.

#### 5.13.29 Автономный срез РЕНДЕРА — вектор перехвата закона (визуализация погони патруля за розыскным, §5.13.28) (2026-07-28)

Рекогносцировка §5.13.29 (субагент) установила ключ к draw-only-корректности: рендер может **заново вывести** цель
§5.13.28-погони прямо из живых полей сцены и гарантированно совпасть с симуляцией. Лямбда `pursuitQuarry(ci)` в
`renderLocalScene` повторяет приоритет-0 скан §5.13.28 (ближайший живой `CK_PIRATE` с `wanted` в `PATROL_DISTRESS_R`,
строгий `<` для тай-брейка) для патруля `ci`. Так как приоритет-0 ПЕРЕБИВАЕТ всё (рейдер-скан §5.13.12 и фолбэк
обёрнуты в `else`), у `defending`-патруля с розыскным в радиусе цель ГАРАНТИРОВАННО именно этот розыскной ⇒ рендер
совпадает с sim'ом по построению, без чтения приватного `atkCraft` (локальной переменной `updateLocalScene`, не
персистится) и без нового поля. Читаются только `x/y/z/kind/wanted/defending/hullHP`; гейт `kind==CK_PATROL &&
defending` не даёт cue ложно сработать на рейдер-обороне. Так замкнутый в §5.13.28 закон-цикл получил зримую развязку:
на радаре зелёный патруль сходится с золотым пиратом — а теперь ещё и виден сам вектор перехвата и метка «в погоне».

1. **3D-вектор перехвата патруль→розыскной (`localdraw.cpp`, блок «(7б, §5.13.29)» после цикла снарядов).** Для
   каждого борта `q=pursuitQuarry(i)`; при `q>=0` и обеих видимых (не за камерой, ни один конец не перекрыт звездой
   через `occ(...)`) проецируем оба конца и рисуем золотой (`255,205,60`) «бегущий» пунктир (период 18 px, штрих
   9 px, фаза `fmod(fxClock·34, period)`) плюс 2-линейную стрелку у розыскного (наконечник указывает на добычу);
   альфа пульсирует `0.55+0.45·sin(fxClock·5.2)` — тот же золотой темп 5.2, что кабина/радар §5.13.25/.27. Луч
   «летит» от патруля к жертве ⇒ направление погони читаемо мгновенно.
2. **Золотая связка блипов на радаре (`localdraw.cpp`, перед циклом блипов).** Лямбда `radarXY(c)` повторяет
   блип-трансформ (`localShipRight`/`scene.pfwd`/`RANGE`/`R`/`rcx`/`rcy`); для каждого `defending`-патруля с добычей
   рисуется золотой отрезок от его блипа к блипу розыскного. Отрисовано ДО цикла блипов ⇒ сами блипы (золото/зелёный
   §5.13.27) ложатся поверх — на 1400-LU радаре видно, КТО за КЕМ охотится, а не только что оба контакта «горячие».
3. **Строка `IN PURSUIT <label>` в панели цели (`localdraw.cpp`).** Если наведён `defending`-патруль и
   `pursuitQuarry(targetCraft)>=0`, высота панели `ph += 14` и под корпусом печатается золотая строка `IN PURSUIT`
   + метка розыскного (`qy.label`, иначе `WANTED`) — прямое имя того, кого патруль преследует. Гейт `esc`
   (`t.defending`) ⇒ у не-патрулей строки нет (нулевой регресс панели §5.13.12/.25).
4. **Чистый draw-only, байт-в-байт к соак-бейзлайну.** Ноль новых полей, ноль RNG, ноль правок sim; `renderLocalScene`
   в основном соак-прогоне НЕ зовётся ⇒ `localdraw.cpp` лишь КОМПИЛИРУЕТСЯ (под ASan), но не исполняется численным
   прогоном. `pursuitQuarry` — read-only свёртка живых полей, `radarXY`/пунктир/стрелка — чистая проекция.

**Проверки (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0); **`make shot_asan` — 24/24 шота,
0 ошибок ASan/UBSan** (обязательно при правке `localdraw.cpp`); **`make soak` — 120000 кадров, invariantFails=0**, ВЕСЬ
числовой бейзлайн §5.13.28 **ПОБИТОВО** (`SOAK DONE frames=120000 deaths=0 radioClaims=3 craftDestroyed=2
invariantFails=0 deathPath=ok writeBack=ok sellWriteBack=ok npcWriteBack=ok patrolHuntsWanted=ok rocks=648 shiny=382
trades=0`) — render-only правка не может сдвинуть headless-числа; **`make shots` — 24/24**, новый `shot_law_pursuit.png`
(defending-патруль охотится на wanted-пирата ⇒ золотой вектор перехвата + стрелка PATROL→PIRATE + зелёная панель
`ESCORT` с золотой строкой `IN PURSUIT PIRATE` + золотая связка блипов на радаре, `DIST 166 LU`) прочитан глазами и
подтверждён. Правки: `localdraw.cpp`, `shot_test.cpp`, `master_prompt.md`, `README.md`.

#### 5.13.30 Автономный срез СИМУЛЯЦИИ — защита стаи (пираты слетаются оборонять загнанного изгоя, §5.13.28) (2026-07-29)

Рекогносцировка §5.13.30 (субагент) подтвердила headless-мёртвость ветки патруля (seed 1200: `CK_PATROL=0` во всех
соак-звёздах) и отсеяла кандидатов: «побег розыскного от закона» РАЗБИЛСЯ о комбат-инвариант — бегущий пират получает
`aiState=2`, а единственный блок огня NPC (`localsim.cpp`, гейт `if (c.aiState == 1 …)`) стреляет лишь при `aiState==1`
⇒ побег ВЫКЛЮЧИЛ БЫ огонь ⇒ урон/агрессия пирата падает (запрещено). «Эволюция награды со временем» проходила инвариант,
но ИСПОЛНЯЛАСЬ бы в соаке (4 розыскных пирата живут) ⇒ безопасность держалась бы на анализе read-set, а не на
неисполнении. Выбран кандидат, безопасный ПО ПОСТРОЕНИЮ (как §5.13.28) и строго аддитивный на бою.

Правило (в хвосте ветки `CK_PIRATE`, после скана собственных целей, до `else if (c.kind == CK_PATROL)`): пират, который
САМ никого не взял целью (`atkCraft < 0 && !atkPlayer` — простаивает, 0 урона), сканирует борта; если рядом (в
`PATROL_DISTRESS_R`) есть патруль-ОХОТНИК — `CK_PATROL`, у которого в его собственном `PATROL_DISTRESS_R` есть живой
розыскной (`wanted`) пират, — этот простаивающий пират берёт ПАТРУЛЬ целью (`atkCraft = патруль`). Стая слетается
оборонять объявленного вне закона: замкнутый §5.13.24→26G→27→28→29 закон-цикл получает ОТВЕТ преступного мира.

1. **Охота выводится ЗАНОВО из стабильных полей, не из кадрового флага (`localsim.cpp`).** Условие «патруль охотится»
   переоткрывается прямым сканом (`kind==CK_PATROL` + внутренний поиск живого `wanted`-`CK_PIRATE` в его
   `PATROL_DISTRESS_R`), а НЕ чтением `o.defending` соседа. Это ровно приём §5.13.29 (рендер выводил цель заново) и ключ
   к КОРРЕКТНОСТИ: `defending` проставляется в ветке `CK_PATROL` в ходе ТОГО ЖЕ кадра, поэтому пират видел бы лишь
   патрули, обработанные РАНЬШЕ него в этом кадре, а своп-поп мёртвых при гибели тасует индексы ⇒ чтение `defending`
   дало бы зависимость от порядка бортов. Ре-вывод из позиции/вида/`wanted` от порядка НЕ зависит.
2. **Строго аддитивно; комбат-инвариант УСИЛЕН, не ослаблен.** Гейт `atkCraft < 0 && !atkPlayer` ⇒ правило трогает
   ТОЛЬКО простаивающего пирата (нулевой урон). Оно НИКОГДА не уводит пирата с игрока или торговца (их урон/агрессия
   §5.13.11/.12 нетронуты) — оно лишь ПРЕВРАЩАЕТ бездельника в атакующего, и цель — ЗАКОН. Итог: агрессии пиратов
   больше, направлена на патруль ⇒ охота за головой становится ТРУДНЕЕ, а не мягче. Побочно ралли-пират, атакуя патруль,
   зажигает пред-существующий маркер §5.13.11 (`patrol.underAttack`/`threatConvoy`) — но это уже верно для ЛЮБОГО пирата,
   бьющего патруль, и в соаке мертво; оставлено как есть (минимальный слайс).
3. **No-op в соаке ПО ПОСТРОЕНИЮ ⇒ 120k-бейзлайн побитово цел.** Внешний цикл ищет `CK_PATROL`; в замороженном кластере
   патрулей нет (§5.13.28) ⇒ цикл ничего не находит ⇒ `atkCraft` не меняется ⇒ поведение идентично §5.13.29. Ноль RNG,
   без нового поля (реюз `PATROL_DISTRESS_R` и как радиус набата, и как радиус охоты внутреннего скана).

**Пробник (`soak_test.cpp`, `packRalliesOutlaw`).** Т.к. в основном соаке правило не исполняется, добавлен изолированный
детерминированный пробник — ЕДИНСТВЕННОЕ headless-покрытие. Свежий `Game`, три борта с пиньнутыми каждый кадр позициями
(ноль дрейфа/RNG), огромные корпуса (индексы стабильны): патруль в 0; розыскная приманка на 1000 LU (в радиусе охоты
<1400 ⇒ делает патруль охотником); ПРОСТАИВАЮЩИЙ пират-стайник на 1100 LU (вне своей осведомлённости 750 ⇒ сам цели не
видит, но в радиусе набата <1400). Проверяем `aiState` (прямой выход решения о цели), а не урон: стайник на 1100 LU >
`WEAPON_RANGE 150`, стрелять не может, но захват цели виден по `aiState`. (A) приманка РОЗЫСКНАЯ ⇒ стайник обязан взять
патруль (`aiState==1`); (B) приманка НЕ розыскная ⇒ патруль не охотник ⇒ набат заперт гейтом ⇒ стайник простаивает
(`aiState==0`). Обе подпроверки зелёные: `rallyFires=YES gatedOnHunt=YES`.

**Проверки (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0); **`make soak` — 120000 кадров,
invariantFails=0**, ВЕСЬ числовой бейзлайн §5.13.28/29 **ПОБИТОВО** (`SOAK DONE frames=120000 deaths=0 radioClaims=3
craftDestroyed=2 invariantFails=0 deathPath=ok writeBack=ok sellWriteBack=ok npcWriteBack=ok patrolHuntsWanted=ok
packRalliesOutlaw=ok rocks=648 shiny=382 trades=0`) — «no-op по построению» подтверждён эмпирически. Шоты НЕ требовались
(`localdraw.cpp` не тронут — sim-слайс, прецедент §5.13.28). Правки: `localsim.cpp`, `soak_test.cpp`, `master_prompt.md`,
`README.md`.

**Следующий срез (§5.13.31, РЕНДЕР).** По чередованию симуляция→рендер — новый draw-слой (не определён; нужна свежая
рекогносцировка). Кандидаты: визуализировать сам набат стаи (лучи «изгой→защитники» или подсветка сплотившихся пиратов),
индикатор «под защитой стаи» у розыскного, или иную читаемую грань закон-цикла, которую `shot`-галерея покажет глазами
без сдвига числового бейзлайна.

#### 5.13.31 Автономный срез РЕНДЕРА — набат стаи виден (рисуем §5.13.30-защиту: оранжевый вектор тарана + связка на радаре + `UNDER PACK`) (2026-07-29)

Draw-companion к §5.13.30: симуляция уже слетает стаей оборонять изгоя, но игрок этого не ВИДИТ. Этот срез — чистый
рисующий слой (`localdraw.cpp`), делающий набат читаемым, и НИ БАЙТА симуляции не трогает. По чередованию симуляция→рендер
(§5.13.28→29→30→31) — рендер к предыдущему sim-правилу, как §5.13.29 был рендером к §5.13.28.

Ре-деривация (лямбда `rallyTarget(ci)`, ключевой приём D). Рендер НЕ читает приватный `atkCraft` пирата и не заводит нового
поля — он ПОВТОРЯЕТ решение §5.13.30 из живых полей: пират годен, если `kind==CK_PIRATE && hullHP>0 && aiState==1`, его
idle-гейт §5.13.30 закрыт (ничто не наводимо в `AW2=750²` — ни игрок, ни один не-пират), И рядом (в `PATROL_DISTRESS_R`)
есть патруль-ОХОТНИК: `CK_PATROL`, у которого в его собственном `PATROL_DISTRESS_R` живой `wanted`-пират. Возвращается
индекс тарана-патруля или −1. Это тот же приём, что `pursuitQuarry` в §5.13.29 (рендер выводил цель погони заново) —
совпадение с sim'ом ПО ПОСТРОЕНИЮ, независимо от порядка бортов/своп-попа при гибели. Префильтр `aiState==1` отсекает
беглеца (`aiState==2`, low-hull) — дёшево и необходимо; авторитетная часть — idle-гейт + скан набата, зеркало §5.13.30.

Три горячо-оранжевых cue (`255,120,32`), намеренно ОТЛИЧНЫХ от золота §5.13.29 (закон→изгой), чтобы в одном кадре сходились
две встречные силы на патруле:
1. **3D-вектор ТАРАНА пират→патруль** (блок «(7в)» после золотого «(7б)»): «бегущий» пунктир (период 16 / штрих 8,
   фаза `fmod(fxClock·46,16)`) + 2-линейная стрелка в патруль. Пульс `0.5+0.5·sin(fxClock·7.0)` — БЫСТРЕЕ золотого 5.2 ⇒
   читается как агрессия, не охота. Оба конца через `occ` (за звездой — скрыт), `behind`-клип, `llen<2`-скип. Рисуется
   ПОСЛЕ золота ⇒ на пересечении оранж поверх золота.
2. **Оранжевая связка на радаре** (после золотого pursuit-link, до цикла блипов): отрезок блип-пирата↔блип-патруля,
   альфа-пульс `45+int(pl·120)`, `pl=0.5+0.5·sin(fxClock·7.0)`; блипы поверх.
3. **Строка `UNDER PACK Xn` в панели** наведённого патруля-жертвы (под золотой `IN PURSUIT` §5.13.29, `ph += 14`):
   `packN` = сколько простаивающих пиратов таранят ИМЕННО этот патруль (`rallyTarget(j)==scene.targetCraft`) ⇒ панель
   загнанного патруля читает весь закон-цикл со своей стороны: `ESCORT` + `IN PURSUIT <изгой>` + `UNDER PACK Xn`.

**Безопасность соака ПО ПОСТРОЕНИЮ.** `renderLocalScene` НИКОГДА не зовётся headless (соак — числовой), поэтому
`localdraw.cpp` под соаком лишь КОМПИЛИРУЕТСЯ (в `shot_asan`), но не ИСПОЛНЯЕТСЯ ⇒ не может сдвинуть 120k-бейзлайн ⇒ он
побитово цел без единого прогона логики. Ноль RNG, ноль новых полей (реюз `PATROL_DISTRESS_R`/`AW2` как в §5.13.30).

**Шот (`shot_test.cpp`, `shot_pack_rally` — 25-й кадр).** Три борта: `defending`-патруль-охотник, вплотную (210 LU) к нему
`wanted`-изгой (близкий бой ⇒ его idle-гейт закрыт ⇒ ОН оранжа не даёт — корректно), и поодаль пират-налётчик на 790 LU
от патруля (>750 ⇒ сам без цели, <1400 ⇒ «слышит» закон) ⇒ ровно он получает оранжевый таран. Камера подобрана так, что
звезда (радиус 260..360 LU) вне кадра и не окклюдит лучи. Глазами (`shot_pack_rally.png`): панель `PATROL/ESCORT/DIST
894 LU/IN PURSUIT OUTLAW/UNDER PACK X1`; в 3D — изгой (слева, золотое кольцо), патруль (центр, циан-рамка), налётчик
(справа); золотой вектор патруль→изгой + оранжевый вектор налётчик→патруль; связки на радаре. Весь закон-цикл в кадре.

**Проверки (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0); **`make shot_asan` ОБЯЗАТЕЛЕН**
(исполняет `localdraw.cpp` под ASan/UBSan) — `shot_pack_rally.bmp rc=0 mode=COCKPIT bodies=11 craft=3`, `SHOTS DONE:
25/25 saved ok`, 0 ASan/UBSan; **`make soak` — 120000 кадров, ПОБИТОВО** тот же бейзлайн §5.13.28/30 (`SOAK DONE
frames=120000 deaths=0 radioClaims=3 craftDestroyed=2 invariantFails=0 deathPath=ok writeBack=ok sellWriteBack=ok
npcWriteBack=ok patrolHuntsWanted=ok packRalliesOutlaw=ok rocks=648 shiny=382 trades=0`); `make shots` — 25/25, добавлен
`shot_pack_rally.png` (проверен глазами). Правки: `localdraw.cpp`, `shot_test.cpp`, `master_prompt.md`, `README.md`.

**Следующий срез (§5.13.32, СИМУЛЯЦИЯ).** По чередованию рендер→симуляция — новое sim-правило (не определено; нужна
свежая рекогносцировка). Кандидаты (форвард-бэклог §0.2): патруль зовёт подмогу под осадой стаи (симметрия набату —
кооп-охота закона), совместный реп-кредит игроку за добивание розыскного рядом с патрулём, или попытка розыскного уйти
в варп до поимки. Требование то же: безопасность соака ПО ПОСТРОЕНИЮ или изолированный пробник, комбат-инвариант цел.

#### 5.13.32 Автономный срез СИМУЛЯЦИИ — подмога закона (патруль спешит к загнанному охотнику, ответ §5.13.30) (2026-07-29)

Ответный beat к §5.13.30: стая слетелась таранить одинокого патруля-охотника (§5.13.30, отрисовано §5.13.31) — теперь
закон ОТВЕЧАЕТ, стягивая к свалке остальной наряд. По чередованию симуляция→рендер (§5.13.28→…→31) это sim-срез: правило
живёт в `localsim.cpp`, ветка `CK_PATROL`, НОВЫЙ приоритет-0.5 МЕЖДУ §5.13.28-охотой (приоритет-0) и §5.13.12-сканом
налётчика (обёрнут теперь в ещё один `else`); фолбэк на ближайшего пирата в `AW2` и provoked-player override §5.13.12 —
без изменений.

Правило: свободный патруль (его приоритет-0 скан СВОЕГО `wanted` пуст) ищет ближайшего живого ПИРАТА в своём
`PATROL_DISTRESS_R`, который САМ сидит в `PATROL_DISTRESS_R` ДРУГОГО патруля `q`, где `q` — настоящий охотник (у `q` в его
`PATROL_DISTRESS_R` есть живой `wanted`-пират). При находке патруль берёт этого пирата целью (`defending=true`) ⇒ весь
наряд сходится туда, где изгойская стая облепила одинокого охотника.

1. **«q охотится» выведено ЗАНОВО из живых полей, не из кадрового флага (`localsim.cpp`).** Признак «q — охотник»
   переоткрывается прямым вложенным сканом (`q.kind==CK_PATROL` + поиск живого `wanted`-`CK_PIRATE` в его
   `PATROL_DISTRESS_R`), а НЕ чтением `q.defending`/`q.aiState`. Это ровно приём §5.13.29/30 (D) и ключ к КОРРЕКТНОСТИ:
   `defending` проставляется в ходе ТОГО ЖЕ кадра, поэтому патруль-респондер видел бы лишь патрули, обработанные РАНЬШЕ
   него, а своп-поп мёртвых при гибели тасует индексы ⇒ чтение флага дало бы зависимость от порядка бортов. Ре-вывод из
   позиции/вида/`wanted` от порядка НЕ зависит и совпадает с намерением ПО ПОСТРОЕНИЮ.
2. **Строго аддитивно; комбат-инвариант УСИЛЕН, не ослаблен.** Тир живёт ЦЕЛИКОМ в выборе цели ПАТРУЛЯ и не читает/пишет
   НИЧЕГО у пирата; цель всегда `CK_PIRATE` (не патруль ⇒ нет friendly-fire). Он лишь заставляет закон взять врага РАНЬШЕ
   и ДАЛЬШЕ ⇒ агрессии, направленной на охоту, больше, а не меньше ⇒ охота за головой ТРУДНЕЕ. Приоритет строго ниже
   §5.13.28-охоты (свой розыскной важнее) и строго выше §5.13.12-налётчика/фолбэка (оба обёрнуты в `else` подмоги);
   provoked-player override (§5.13.12, `c.hostile`) по-прежнему поверх всего.
3. **No-op в соаке ПО ПОСТРОЕНИЮ ⇒ 120k-бейзлайн побитово цел.** Тир может сработать лишь при ≥2 патрулях (осаждённый
   охотник + свободный ответчик). В замороженном кластере (seed 1200) патрулей 0, а оба прежних патруль-пробника
   (`patrolHuntsWanted`, `packRalliesOutlaw`) несут ровно по 1 патрулю ⇒ во всём текущем покрытии тир — no-op, и бейзлайн
   не сдвигается. Ноль RNG, без нового поля (реюз `PATROL_DISTRESS_R` + маркер `defending`).

**Пробник (`soak_test.cpp`, `patrolBacksUpSwarmed`).** Единственное headless-покрытие тира. Свежий `Game g7; init(1200)`,
ЧЕТЫРЕ борта с пиньнутыми каждый кадр позициями (ноль дрейфа/RNG), огромные корпуса (индексы стабильны): охотник A в
(0,0,0); `wanted`-изгой на (300,0,0) — <1400 от A ⇒ делает A охотником, >1400 (2121) от B ⇒ вне сканов B; пират-стайник на
(0,1000,0) — в мели A (<1400) и в окне 750<d<1400 (~1100) от B; свободный ответчик B на (0,2100,0). Ассерт на `aiState` B
(стайник за `WEAPON_RANGE` ⇒ урона нет, но захват цели виден): (A) изгой РОЗЫСКНОЙ ⇒ A охотник ⇒ B обязан взять стайника
(`aiState==1`, `respondsToSwarm`); (B) изгой НЕ розыскной ⇒ A не охотник ⇒ подмога заперта, налётчика нет (нет
торговцев), фолбэк капнут радиусом 750 при стайнике на 1100 ⇒ B простаивает (`aiState==0`, `gatedOnHunt`). Обе зелёные:
`respondsToSwarm=YES gatedOnHunt=YES`.

**Проверки (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0); **`make soak` — 120000 кадров,
invariantFails=0**, ВЕСЬ числовой бейзлайн §5.13.30/31 **ПОБИТОВО** (`SOAK DONE frames=120000 deaths=0 radioClaims=3
craftDestroyed=2 invariantFails=0 deathPath=ok writeBack=ok sellWriteBack=ok npcWriteBack=ok patrolHuntsWanted=ok
packRalliesOutlaw=ok patrolBacksUpSwarmed=ok rocks=648 shiny=382 trades=0`) — «no-op по построению» подтверждён
эмпирически. Шоты НЕ требовались (`localdraw.cpp` не тронут — sim-слайс, прецедент §5.13.28/30). Правки: `localsim.cpp`,
`soak_test.cpp`, `master_prompt.md`, `README.md`.

#### 5.13.33 Автономный срез РЕНДЕРА — подмога закона (сине-белый вектор выручки, draw-слой к §5.13.32) (2026-07-29)

Рендер-ответ к §5.13.32: закон уже стягивает свободные патрули к загнанному охотнику — теперь это ВИДНО. По
чередованию: §5.13.30 sim → .31 render → .32 sim → **.33 render**. Замыкает визуализацию всего закон-цикла в одном кадре.

- **`localdraw.cpp` — лямбда `backupTarget(ci)`** (близнец `pursuitQuarry` §5.13.29 / `rallyTarget` §5.13.31; ре-деривация
  D — читает решение из живых полей, не из флага/поля кадра ⇒ порядко- и swap-pop-независима, совпадает с sim по
  построению). Гейт `CK_PATROL`+жив (НЕ на `defending`: патруль может защищать и по P1-обороне налётчика — re-derive raw,
  безопасно, т.к. backup — приоритет 0.5 над P1). Возвращает `-1`, если у самого `ci` есть живой `wanted`-пират в
  `PATROL_DISTRESS_R` (⇒ `ci` сам ОХОТНИК, а не свободный респондер); иначе ближайший строго-`<` пират, который в радиусе
  бедствия ДРУГОГО патруля `q`, реально ведущего охоту (у `q` живой `wanted`-пират в радиусе). Возвращает индекс ПИРАТА
  (как sim `atkCraft = backupPir`), НЕ осаждённого охотника — этот нюанс правит предикат счётчика в панели.
- **(7г) сине-белый вектор** (`rgba(120,180,255)`, пульс 4.2, марш 28 — холоднее/размереннее оранжа 7.0/46 и золота: закон
  действует дисциплиной, не яростью): «бегущий» пунктир + стрелка респондер-патруль→пират. Синяя связка на радаре (после
  оранжевой ⇒ рисуется поверх). Всё чистый draw-only: ноль RNG, ноль новых полей, ноль правок sim.
- **Панель ЗАГНАННОГО охотника — строка `BACKUP INBOUND Xn`** под `UNDER PACK` (гейт `esc && tPursuit>=0` — только для
  патруля-охотника, ESCORT + ведёт погоню §5.13.29). Счёт — НЕ равенством `backupTarget(j)==targetCraft` (как в UNDER
  PACK), а предикатом «возвращённый пират в `PATROL_DISTRESS_R` от НАШЕГО охотника»: `backupTarget` отдаёт пирата, не
  охотника, поэтому равенство здесь неверно (рекон-грабля, поймана до кода).

**Один кадр = весь закон-цикл гон→набат→подмога:** золото (закон→изгой §5.13.29) + оранж (стая→закон §5.13.31) + синь
(закон→закон §5.13.33) сходятся на одном загнанном охотнике.

**Шот (`shot_test.cpp`, `shot_law_backup` — 26-й кадр).** Четыре борта под x≈8000 (звезда в origin позади камеры ⇒ ноль
окклюзии/ореола): `defending`-патруль-ОХОТНИК H (наведён ⇒ панель-цель), впритык `wanted`-изгой (делает H охотником ⇒
золото `IN PURSUIT OUTLAW`), простаивающий пират стаи в мелее H (⇒ оранж `UNDER PACK X1`) и поодаль СВОБОДНЫЙ патруль R
(изгой в 1690>1400 ⇒ сам не охотник; пират стаи в 1300<1400 ⇒ берёт его целью подмоги). Камера смотрит +x на H;
`fxClock=0.30` — все три пульса (золото/оранж/синь) в яркой фазе. Геометрия выверена вручную под все три лямбды сразу.
Глазами (`shot_law_backup.png`): панель показывает весь стек строк на H — `PATROL/ESCORT` (зелёный) · `DIST`/`CLS` · щит/
корпус · `IN PURSUIT OUTLAW` (золото) · `UNDER PACK X1` (оранж) · `BACKUP INBOUND X1` (синь); в 3D — золотой вектор к
изгою, оранжевый таран стаи в H, сине-белый диагональный вектор выручки R→пират. Радар-блипы прижаты к краю (все борта
>1400 LU от игрока — обязательный внутренний зазор 1690 LU нельзя одновременно уместить в кадр при fov 0.60 И в радиус
радара 1400; геометрически исключено), но синяя связка всё равно рисуется. Главный пруф (3D-вектор + 6-строчная панель)
чёткий.

**Проверки (все зелёные):** `make clean && make` 0 предупреждений; `--smoke`/`--localsmoke` exit 0. `make shot_asan`
(исполняет `localdraw.cpp` под ASan/UBSan) — `shot_law_backup.bmp … craft=4`, `SHOTS DONE: 26/26 saved ok`, ноль ASan/
UBSan. `make soak` — `SOAK DONE frames=120000 deaths=0 radioClaims=3 craftDestroyed=2 invariantFails=0 … patrolHuntsWanted=ok
packRalliesOutlaw=ok patrolBacksUpSwarmed=ok rocks=648 shiny=382 trades=0` — **ПОБИТОВО** бейзлайн §5.13.32 (срез
draw-only, `renderLocalScene` в соаке не зовётся ⇒ no-op по построению). `make shots` 26/26, `shot_law_backup.png`
(26841 байт) проверен глазами. Правки: `localdraw.cpp`, `shot_test.cpp`, `master_prompt.md`, `README.md`.

**Следующий срез (§5.13.34, СИМУЛЯЦИЯ).** По чередованию рендер→симуляция — новое sim-правило (не определено; нужна
свежая рекогносцировка). Ветка возможностей форвард-бэклога §0.2: ответ-бит к завершённому закон-циклу гон→набат→подмога
— напр. стая, потеряв изгоя (убит/сбежал), рассыпается по построению из живых полей, либо экономический след розыска
(награда меняет спрос/цены на теле-рынке). Держи чередование и «no-op по построению» до плейтеста.

#### 5.13.34 Автономный срез СИМУЛЯЦИИ — облава (закон гонится за одиозной дичью тем быстрее, чем крупнее награда) (2026-07-29)

Ответ-бит к завершённому закон-циклу (§5.13.24→33): награда за розыск, прежде дремавшая суммой, впервые значит
МЕХАНИЧЕСКИ. По чередованию: §5.13.32 sim → .33 render → **.34 sim**. Патруль, реально ведущий P0-охоту (§5.13.28) на
живого `wanted`-пирата, летит тем резвее, чем крупнее награда за его голову — громкая слава делает закон неотвратимее.

- **`local.h` — `LocalCfg::MANHUNT_SPEED_GAIN = 0.5`** (единственная новая величина среза): максимум прибавки к предельной
  скорости патруля при полной награде (1500 CR ⇒ ×1.5). Ни нового поля у борта, ни ре-бейка CHROMOCORE — чисто
  кинематический множитель геймплея (как `NPC_BOOST_MULT`), §2.4-safe.
- **`localsim.cpp` — файл-статик `manhuntSpeedMult(scene, ci)`** рядом с `rodriguesRotate` (до `updateLocalScene`).
  Ре-деривация приёма D (как рендер-лямбды §5.13.29/31/33): дословно повторяет P0-скан ИИ — ближайший ЖИВОЙ
  `wanted`-`CK_PIRATE` в `PATROL_DISTRESS_R` (строгое `<`). Цель совпадает с фактическим `atkCraft` патруля ПО ПОСТРОЕНИЮ:
  P0 вытесняет все младшие тиры (else-каскад), значит наличие розыскного в радиусе ⇒ патруль гонится именно за ним. Гейт
  `CK_PATROL && defending && жив`: P0 ставит `defending=true`, а player-override (патруль предпочёл более близкого игрока,
  строка 552) и бегство/низкий корпус (`aiState≠1` ⇒ строка 580) его ГАСЯТ ⇒ гейт отсекает ровно те случаи, где патруль
  летит НЕ на розыскного. Возвращает `1.0 + GAIN·heat`, где `heat = clamp((wantedBounty−250)/(1500−250), 0, 1)` — одна
  нормировка кроет и предген-награду (250..900), и эмерджентную (300..1500); не-патруль / нет розыскного в радиусе / малая
  награда ⇒ `1.0` (нулевой регресс).
- **`localsim.cpp` — врезка в кадровое движение** (`effMax *= manhuntSpeedMult(scene, i)`, там же, где `NPC_BOOST_MULT`).
  Правится ТОЛЬКО потолок скорости патруля-охотника; выбор цели не трогается ⇒ рендер-лямбды §5.13.29/31/33 остаются верны
  ДАРОМ — потому шоты не нужны, `localdraw.cpp` не тронут.

**Комбат-инвариант УСИЛЕН, строго АДДИТИВНО:** закон только БЫСТРЕЕ на крупной дичи, никогда медленнее (награда у пола ⇒
множитель 1.0 = прежнее поведение); скорость/агрессия/урон ПИРАТА не тронуты. Ноль RNG (§2.3), без нового поля у борта.

**Проверки (все зелёные):** `make clean && make` 0 предупреждений; `--smoke`/`--localsmoke` exit 0. `make soak` (под ASan/
UBSan) — `SOAK DONE frames=120000 deaths=0 radioClaims=3 craftDestroyed=2 invariantFails=0 … patrolHuntsWanted=ok
packRalliesOutlaw=ok patrolBacksUpSwarmed=ok manhuntScalesWithBounty=ok rocks=648 shiny=382 trades=0` — **ПОБИТОВО**
бейзлайн §5.13.33 (ветка `CK_PATROL` в замороженном мире не исполняется ⇒ no-op ПО ПОСТРОЕНИЮ). Новый пробник
`manhuntScalesWithBounty`: две фазы, идентичные КРОМЕ награды (патруль в 0, розыскной на 1200 LU; позиция патруля пиньнута,
СКОРОСТЬ копится) — награда 1500 ⇒ терминальная скорость `60→90` (=maxSpeed·1.5), награда 250 ⇒ ровно `60` (нулевой
регресс), high строго > low; эталоны сверены с `MANHUNT_SPEED_GAIN`. Шоты не требовались (правится скорость, не выбор
цели; прецедент §5.13.28/30/32). Правки: `local.h`, `localsim.cpp`, `soak_test.cpp`, `master_prompt.md`, `README.md`.

**Следующий срез (§5.13.35, РЕНДЕР) — визуализация облавы (ниже).**

#### 5.13.35 Автономный срез РЕНДЕРА — жар облавы (золотой вектор погони калится с наградой дичи) (2026-07-29)

Draw-only спутник §5.13.34: та самая «интенсивность облавы», что в sim КАЛИТ СКОРОСТЬ патруля (`manhuntSpeedMult`), теперь
ЧИТАЕТСЯ ИЗ КОКПИТА. По чередованию: §5.13.33 render → .34 sim → **.35 render**. Золотой вектор перехвата §5.13.29
(патруль→розыскной) и строки его панели тем горячее, чем крупнее награда за голову дичи — громкая слава видна как добела
калёная трасса погони.

- **`localdraw.cpp` — золотой вектор погони (блок «(7б)»)**: перед выбором цвета введён `heat = clamp((qc.wantedBounty −
  250)/(1500 − 250), 0, 1)` — ПОБИТОВО та же нормировка, что sim-`manhuntSpeedMult` (localsim.cpp). `qc` — дичь, чей индекс
  уже свёл `pursuitQuarry(i)` (приём D ⇒ ровно тот пират, чья награда движет облавой в sim; рендер = sim ПО ПОСТРОЕНИЮ).
  Три согласованные модуляции, все вида `base + heat·delta`: тон `rgba(255, 205+heat·50, 60+heat·195, …)` калится
  золото→бело-калёный (`g`/`b` ровно к 255 при heat=1); базовая альфа `70 + heat·50 + pulse·150` ярче; марш штрихов
  `fxClock·(34 + heat·17)` бежит до ×1.5 (34→51) — зеркало прибавки скорости §5.13.34. При `heat=0` (награда у пола 250)
  все три РОВНО прежние (`int(0·x)==0`) ⇒ нулевой визуальный регресс.
- **`localdraw.cpp` — панель наведённого патруля**: строка `IN PURSUIT <label>` §5.13.29 перекрашена тем же `hot`-тоном
  (heat-лерп; при heat=0 = прежний `rgba(255,205,60,255)`), и ПОД ней добавлена новая строка `MANHUNT %.0F CR` — награда
  дичи ГЛАЗАМИ ЗАКОНА (не нужно перенаводиться на самого пирата, чтобы увидеть ставку облавы). Резерв высоты панели поднят
  `ph += 14 → += 28` под обе строки (ревью: худший стек строк даёт низ панели ≤154 ≪ H=800 — клиппинга нет).
- **`shot_test.cpp` — новый 27-й кадр `shot_manhunt`**: клон геометрии `shot_law_pursuit`, но награда пирата 1500 (не 650)
  ⇒ `heat=1`; цель — патруль ⇒ панель `ESCORT` + бело-калёные `IN PURSUIT PIRATE` + `MANHUNT 1500 CR`, вектор погони добела
  и штрихи ×1.5. Пара к `shot_law_pursuit` (650 CR, спокойное золото) — рядом читается жар облавы.

**Комбат-инвариант нетронут:** чистый draw-only, ноль правок sim/полей/RNG (§2.3); `wantedBounty` читается СЫРЫМ и идёт в
draw-time лерп — не CHROMOCORE-ребейк (§2.4); HUD-число `%.0F` uppercase (§2.6). Формула heat дословно совпадает с sim ⇒
цвет трассы и скорость патруля не могут разойтись ПО ПОСТРОЕНИЮ.

**Adversarial-ревью (субагент, VERDICT: SHIP).** Все 8 пунктов PASS: heat=0 побитово прежний (цвет+марш+текст панели);
формула совпадает с sim во всех трёх точках (localsim.cpp:48-49, localdraw.cpp:1586-1587/2185-2186); `pursuitQuarry`
возвращает −1 либо валидный индекс, гейты `q<0`/`tPursuit>=0` предшествуют чтению `wantedBounty`; резерв 28 кроет обе
строки с запасом; C++11-only; шот выставляет `wanted/defending/CK_PATROL/CK_PIRATE` в радиусе и хранит `ok==total`.
Единственная нота — альфа доходит до 270 при heat=1 — полностью поглощена кламп-хелпером `rgba()` (косметика, не дефект).

**Проверки (все зелёные):** `make clean && make` 0 предупреждений; `--smoke`/`--localsmoke` exit 0. `make shot_asan`
(исполняет `localdraw.cpp` под ASan/UBSan) — `shot_manhunt.bmp … rc=0`, `SHOTS DONE: 27/27 saved ok`, ноль ASan/UBSan.
`make soak` — `SOAK DONE frames=120000 deaths=0 radioClaims=3 craftDestroyed=2 invariantFails=0 … patrolHuntsWanted=ok
packRalliesOutlaw=ok patrolBacksUpSwarmed=ok manhuntScalesWithBounty=ok rocks=648 shiny=382 trades=0` — **ПОБИТОВО**
бейзлайн §5.13.34 (`renderLocalScene` в соаке не зовётся ⇒ no-op ПО ПОСТРОЕНИЮ). `make shots` 27/27, `shot_manhunt.png` и
`shot_law_pursuit.png` проверены глазами (контраст 1500 CR добела vs 650 CR золото подтверждает кодировку heat). Правки:
`localdraw.cpp`, `shot_test.cpp`, `master_prompt.md`, `README.md`.

#### 5.13.36 Автономный срез СИМУЛЯЦИИ — благодарность закона (co-op добивание розыскного → +реп фракции патруля) (2026-07-29)

Sim-спутник §5.13.24: денежная выплата за розыскного впервые получает РЕПУТАЦИОННОЕ эхо. По чередованию: §5.13.34 sim →
.35 render → **.36 sim**. Если игрок ДОБИВАЕТ `wanted`-пирата, которого ГНАЛ патруль (P0-охота §5.13.28), фракция этого
патруля отвечает `+6` rep ПОВЕРХ выплаты §5.13.24 — зеркало §5.13.11 `CONVOY SAVED`, но по оси ЗАКОНА: закон помнит, кто
помог закрыть облаву. Дичь у пирата не отнимается — благодарность строго ПОВЕРХ денег.

- **`localsim.cpp` — блок благодарности (в player-kill `(E)`, после начисления `BOUNTY CLAIMED`)**: гейт
  `c.kind==CK_PIRATE && c.wanted && game.playerFaction>=0`. «Патруль гнал ИМЕННО `c`» нельзя взять из прямого P0-скана
  §5.13.28 (он пропускает `hullHP≤0`, а убитый `c` уже мёртв — удаление отложено до блока L), потому ре-деривуется приёмом
  D через ИНВЕРТИРОВАННЫЙ P0-скан: среди `defending`-`CK_PATROL` в `PATROL_DISTRESS_R` от `c` берётся тот, для которого `c`
  был ближайшей ЖИВОЙ розыскной дичью (внутренний скан подтверждает: ни один ДРУГОЙ живой `wanted`-`CK_PIRATE` не ближе к
  этому патрулю; `w==j` пропускает сам труп). Т.к. P0 в проходе ИИ отрабатывает РАНЬШЕ подшагов-столкновений (когда `c` был
  ещё жив) и вытесняет младшие тиры, найденный патруль ПО ПОСТРОЕНИЮ и есть тот, чей `atkCraft` был нацелен на `c`. При
  находке — `game.adjustFactionRelation(playerFaction, pat.faction, +6)` (симметрично, клампится [−128,128]), `pushNews`,
  тост `LAW GRATEFUL +REP` (3 c) и лёгкая тряска (≥20). Не нашли ⇒ ничего (обычная выплата §5.13.24 не тронута).
- **`soak_test.cpp` — пробник `lawGratitude`**: свежий `Game glg; glg.init(1200)`; `F` = первая фракция ≠ `playerFaction`
  (в worldgen ≥1 NPC-фракция всегда предшествует игроку ⇒ `F` валиден). Сцена A: `wanted`-пират добивается игроком в
  ~300 LU от `defending`-патруля фракции `F` (в `PATROL_DISTRESS_R`) ⇒ ассерт `factionRelation(pf,F)==rel0+6`. Сцена B: тот
  же патруль отставлен на 2000 LU ⇒ ассерт «убийство есть, но реп НЕ двинут» (гейт радиуса цел). `rel0` перезахватывается в
  начале каждой сцены ⇒ двусторонний тест (застрявший-вкл, застрявший-выкл и двойной-грант — все три падают). Пробник
  крутится ПОСЛЕ 120k-цикла и всех прежних пробников ⇒ его `Game`/RNG не перетряхивают бейзлайн.

**Комбат-инвариант §0.2-G цел (не только пройден — не затронут):** блок исполняется ТОЛЬКО после смерти пирата (внутри
гейта `before>0 && c.hullHP<=0 && playerValid`), трогает лишь `factionRelations`/новость/тост/тряску; урон/агрессия/время-
в-бою пирата не читаются и не пишутся ⇒ на вопрос фильтра «ослабляет ли это аптайм пирата?» ответ NO. Ноль RNG (§2.3), ноль
новых полей (реюз `PATROL_DISTRESS_R`/`defending`/`wanted`/`faction`). Реприз (§5.13.11) ниже гейтит `!c.hostile` ⇒ пират
(всегда `hostile`) не получает второй правки отношений — двойного счёта нет, как и у соседнего `CONVOY SAVED`.

**No-op в соаке ПО ПОСТРОЕНИЮ:** headless-соак спавнит только торговцев/пиратов (0 патрулей — `CK_PATROL` родится лишь из
co-located «military»/«patrol» макро-агента, `localgen.cpp`) ⇒ `gratPatrol` всегда −1 ⇒ `adjustFactionRelation` из нового
блока НИКОГДА не зовётся ⇒ `factionRelations` и ВСЕ числовые счётчики остаются ПОБИТОВО как в §5.13.35. Покрытие правила
несёт исключительно ручной патруль пробника `lawGratitude`.

**Adversarial-ревью (субагент, VERDICT: SHIP).** Все 8 пунктов PASS: (1) комбат-инвариант — блок целиком в гейте смерти,
пиратские поля не тронуты; (2) детерминизм — ноль `rng`, `adjustFactionRelation`/`pushNews` детерминированы; (3) память —
`c=scene.craft[j]` жив весь блок (ресайза `scene.craft` в подшаге нет, своп-поп мёртвых отложен до L), `scene.craft[gratPatrol]`
в границах; (4) глифы — все символы «LAW GRATEFUL +REP» есть в `render2d.cpp::glyph`, тост центрирован ⇒ ширина безопасна;
(5) C++11-only; (6) пробник двусторонний и не-флейки, `rel0` перезахват в B ⇒ shared `Game` его не портит; (7) размещение
после BOUNTY и до реприза — без двойного счёта. Ноты — все LOW/косметика, не блокеры: **(a)** варп-дрейф: блок гейтит на
кадровый `defending` (позиции начала кадра), но дичь ре-деривует по позициям середины подшага — при `dtHours` до 6 ч бортам
можно сместиться и дать ложное +/−; влияние ограничено нацжом ±6 rep, БЕЗ краша/инварианта/RNG, и это ТОТ ЖЕ приём-D
аппрокс, что уже в проде (`manhuntSpeedMult`/`pursuitQuarry`) — идиома проекта, не регресс; **(b)** тай-брейк ровного
расстояния (бит-точное равенство double ⇒ практически никогда); **(c)** затенение имени `p` устранено переименованием
внутреннего `p → pat` (совпадает с конвенцией `manhuntSpeedMult`); **(d)** тост перекрывает `BOUNTY CLAIMED` (деньги всё
равно начислены) и при `pat.faction==playerFaction` self-guard гасит правку, но тост «+REP» всё равно всплывёт (пробник
берёт `F≠pf`, в игре свои патрули у игрока редки). Переименование `p→pat` — единственная правка после ревью, чисто
косметическая, поведение-сохраняющая; лестница перепройдена (ниже) ⇒ бейзлайн остался ПОБИТОВО.

**Проверки (все зелёные):** `make clean && make` 0 предупреждений; `--smoke`/`--localsmoke` exit 0. `make soak` (под ASan/
UBSan) — `SOAK DONE frames=120000 deaths=0 radioClaims=3 craftDestroyed=2 invariantFails=0 deathPath=ok writeBack=ok
sellWriteBack=ok npcWriteBack=ok patrolHuntsWanted=ok packRalliesOutlaw=ok patrolBacksUpSwarmed=ok
manhuntScalesWithBounty=ok lawGratitude=ok rocks=648 shiny=382 trades=0` — **ПОБИТОВО** бейзлайн §5.13.35 (те же
`radioClaims=3`/`craftDestroyed=2`/`rocks=648`/`shiny=382`/`trades=0`) плюс новый `lawGratitude=ok`. Шоты НЕ требовались
(СИМ-срез: `saveShot` не шагает сим, `localdraw.cpp` не тронут ⇒ кадры не меняются; их по-прежнему 27). Правки:
`localsim.cpp`, `soak_test.cpp`, `master_prompt.md`, `README.md`.

**Следующий срез (§5.13.37, РЕНДЕР) — кокпит-подсказка благодарности закона (ниже).**

**Следующий срез (§5.13.36, СИМУЛЯЦИЯ).** По чередованию рендер→симуляция — новое sim-правило (не определено; нужна свежая
рекогносцировка). Кандидаты §0.2: стая, потеряв изгоя (убит/сбежал), рассыпается из живых полей; реп-бонус за совместное с
патрулём добивание розыскного; розыскной рвётся в варп из системы, закон/игрок ловят до прыжка. Держи чередование и
«no-op по построению» до плейтеста.

#### 5.13.37 Автономный срез РЕНДЕРА — рендер стояния (панель наведённой цели несёт `STANDING <WORD> ±N`, draw-слой к §5.13.36) (2026-07-29)

Draw-only спутник §5.13.36: та самая РЕПУТАЦИЯ, что в sim растёт от co-op добивания розыскного (`+6` благодарности §5.13.36,
а прежде §5.13.11 `CONVOY SAVED` и расправы), теперь ЧИТАЕТСЯ ИЗ КОКПИТА. По чередованию: §5.13.35 render → .36 sim →
**.37 render**. Транзиентный тост «LAW GRATEFUL +REP» гаснет за 3 c и не оставляет per-frame следа — стойкое ПОСЛЕДСТВИЕ есть
сама релейшн; панель наведённой цели теперь несёт её строкой `STANDING <WORD> ±N`, число ползёт вверх зелёным по мере
накопления и краснеет при вражде.

- **`localdraw.cpp` — строка `STANDING` в панели цели (блок под баром корпуса)**: гейт `showStanding = (t.faction>=0 &&
  game.playerFaction>=0 && t.faction!=game.playerFaction)`; при нём резерв высоты `ph += 14` и — ниже опциональных
  BOUNTY/IN PURSUIT/MANHUNT/UNDER PACK/BACKUP INBOUND — рисуется строка. Значение берётся НА ЛЕТУ: `rel =
  game.factionRelation(playerFaction, t.faction)`, тир — общий классификатор `classifyFactionRelationTension(rel)` (единый
  источник порогов, faction.cpp — тот же, что макрослой). Словарь+цвет по тирам: `ENEMY`/`HOSTILE` — красный
  (238,88,82 / 238,120,82), `TENSE` — амбер (245,191,78), `NEUTRAL` — тусклый `P.dim`, `FRIENDLY` — зелёный (90,220,132),
  `ALLY` — ярко-зелёный (120,240,150). Строка `STANDING %s %+d` (`%s`+`%+d`, uppercase-safe §2.6). Пираты бесфракционны
  (`faction=−1`) ⇒ гейт ложен ⇒ строка им НЕ рисуется.
- **`shot_test.cpp` — новый 28-й кадр `shot_law_standing`**: `setFactionRelation(playerFaction, 0, 96)` ⇒ дружественный
  патруль в фокусе панели несёт бело-зелёную `STANDING ALLY +96` (читается глазами — прямой ПОКАЗ накопленного итога
  закон-цикла §5.13.36/§5.13.11). Прежние law-шоты (цель — патруль с фракцией, релейшн у пола 0) теперь дополнительно несут
  `STANDING NEUTRAL +0` — регресс-якорь тусклого тира.

**Комбат-инвариант нетронут:** чистый draw-only, ноль правок sim/полей/RNG (§2.3); `factionRelation` читается СЫРЫМ на кадр —
не CHROMOCORE-ребейк (§2.4); HUD-строка `STANDING %s %+d` uppercase-safe (§2.6). §0.2-G цел ТРИВИАЛЬНО: пираты бесфракционны
⇒ строка для них не рисуется ⇒ читаемость/аптайм пирата не тронуты (его урон/агрессия/время-в-бою не читаются и не пишутся).
Тир и цвет — та же таблица порогов, что у макрослоя ⇒ кокпит и карта отношений не разойдутся ПО ПОСТРОЕНИЮ.

**Проверки (все зелёные):** `make clean && make` 0 предупреждений; `--smoke`/`--localsmoke` exit 0. `make shot_asan`
(исполняет `localdraw.cpp` под ASan/UBSan) — `SHOTS DONE: 28/28 saved ok`, ноль ASan/UBSan. `make soak` — `SOAK DONE
frames=120000 deaths=0 radioClaims=3 craftDestroyed=2 invariantFails=0 deathPath=ok writeBack=ok sellWriteBack=ok
npcWriteBack=ok patrolHuntsWanted=ok packRalliesOutlaw=ok patrolBacksUpSwarmed=ok manhuntScalesWithBounty=ok
lawGratitude=ok rocks=648 shiny=382 trades=0` — **ПОБИТОВО** бейзлайн §5.13.36 (`renderLocalScene` в соаке не зовётся ⇒
no-op ПО ПОСТРОЕНИЮ). `make shots` 28/28, `shot_law_standing.png` проверен глазами (`STANDING ALLY +96` ярко-зелёным),
прежние law-шоты несут `STANDING NEUTRAL +0`. Правки: `localdraw.cpp`, `shot_test.cpp`, `master_prompt.md`, `README.md`.

**Следующий срез (§5.13.38, СИМУЛЯЦИЯ).** По чередованию рендер→симуляция — новое sim-правило. Кандидат §0.2: «коп-килл» —
добивание патруля-ОХОТНИКА поднимает награду его дичи СИЛЬНЕЕ базового +200 (§5.13.26G) ⇒ калёная облава дороже (строго
аддитив — награда пирата только растёт). Держи чередование и «no-op по построению» до плейтеста.

#### 5.13.38 Автономный срез СИМУЛЯЦИИ — коп-килл: убийство патруля-ОХОТНИКА калит награду его дичи (sim-спутник §5.13.37) (2026-07-29)

SIM-спутник §5.13.37. По чередованию: §5.13.36 sim → .37 render → **.38 sim**. Срез закрывает петлю, что закон-цикл оставлял
открытой. Патруль гонит розыскного (§5.13.28), облава калится тем горячее, чем крупнее награда (§5.13.34/35), а добьёт закон — его
фракция благодарит игрока (§5.13.36). Но когда изгой ВЫИГРЫВАЛ ту дуэль — не происходило НИЧЕГО. Теперь происходит: розыскной пират,
ДОБИВШИЙ патруль, что вёл по нему P0-охоту, получает +`COP_KILL_BOUNTY` (500 CR) ПОВЕРХ обычного +200 за смерть не-пирата (§5.13.26G)
— +700 за копа против +200 за торговца — так что убийство закона делает СЛЕДУЮЩУЮ облаву горячее (быстрее §5.13.34, белее §5.13.35),
а не холоднее. Цена убийства копа есть бо́льшая цена на голове самого убийцы.

- **`local.h` — константа `LocalCfg::COP_KILL_BOUNTY = 500.0`**: прибавка к награде СВЕРХ §5.13.26G-овских +200; документирована
  как строго аддитивная (награда пирата лишь растёт, тот же потолок 1500 CR).
- **`localsim.cpp`, блок смерти NPC-vs-NPC**: (1) `const bool cWasWantedBefore = c.wanted` захвачен в ВЕРХУ блока убийства —
  ДО §5.13.26G-бампа, что ставит `c.wanted = true`; так пират-первопреступник (чистый до этого убоя) корректно ОТСЕЧЁН — патруль мог
  гнать лишь УЖЕ розыскного. (2) После §5.13.26G-бампа: если розыскной пират `c` добил патруль `o`, ПЕРЕВЫВОДИМ, был ли `c` его
  P0-дичью — `c` обязан быть ближайшим ЖИВЫМ `wanted`-пиратом к павшему патрулю в `PATROL_DISTRESS_R` (нет иного `wanted` строго
  ближе к `o`) — ПРЯМОЙ скан, зеркало `manhuntSpeedMult` (§5.13.34), тот же приём D из стабильных живых полей ⇒ совпадает с целью
  §5.13.28-охоты ПО ПОСТРОЕНИЮ. При да — `wantedBounty += COP_KILL_BOUNTY`, кламп к тем же 1500. Рядового пирата таран-стаи
  (§5.13.30), убившего патруль, валит УЖЕ геометрия (`cWasQuarry` — он не ближайший `wanted`) ⇒ лишь обычные +200. Намеренно БЕЗ
  гейта `o.defending` (в отличие от §5.13.36): `c` — P0-дичь ПО ГЕОМЕТРИИ, а `o.defending` ложно потерял бы законный случай «загнал
  патруль до бегства/оверрайда на игрока, затем добил» (награда лишь растёт ⇒ инвариант цел при любом состоянии патруля).
- **`soak_test.cpp`, двусторонний пробник `copKillRaisesBounty`**: сцена A пиньнит безоружный патруль и розыскного убийцу в 30 LU
  (внутри `WEAPON_RANGE`) ⇒ ассерт награды в `B0 + 200 + 500` (=1350); сцена B добавляет БЛИЖЕ (10 LU) безоружную розыскную
  приманку ⇒ убийца не ближайший `wanted` ⇒ ассерт лишь `B0 + 200` (=850). Застрявшее «вкл» (всегда +700) валит B, застрявшее
  «выкл» валит A.

**Комбат-инвариант §0.2-G УСИЛЕН, не просто соблюдён:** единственный эффект — бо́льшая награда на пирате, убившем копа, отчего
последующая облава БЫСТРЕЕ (§5.13.34) и ЯРЧЕ (§5.13.35) — горячее дичь, больше агрессии закона, больше времени в бою. Ни корпус,
ни урон, ни цель, ни таймер, ни агрессия пирата не читаются и не уменьшаются ⇒ вопрос фильтра «уменьшает ли это урон/агрессию/аптайм
пирата?» — НЕТ. Ноль RNG (§2.3 — скан читает лишь позиции/`kind`/`wanted`/`hullHP`). Не CHROMOCORE-ребейк (§2.4): пишет геймплейное
поле `wantedBounty`, не запечённую стату корабля. Без новых полей у борта (1 тюнинг-константа + 1 локальный bool). No-op в соаке ПО
ПОСТРОЕНИЮ: headless-соак не спавнит `CK_PATROL` ⇒ гейт `o.kind==CK_PATROL` не срабатывает за все 120k кадров ⇒ покрытие целиком на
пробнике `copKillRaisesBounty`, а числовой бейзлайн побитово цел.

**Проверки (все зелёные):** `make clean && make` 0 предупреждений; `--smoke`/`--localsmoke` exit 0. `make soak` (под ASan/UBSan):
`cop-kill probe: escalatesOnQuarryKill=YES noEscalationWhenNotQuarry=YES`; `SOAK DONE frames=120000 deaths=0 radioClaims=3
craftDestroyed=2 invariantFails=0 deathPath=ok writeBack=ok sellWriteBack=ok npcWriteBack=ok patrolHuntsWanted=ok
packRalliesOutlaw=ok patrolBacksUpSwarmed=ok manhuntScalesWithBounty=ok lawGratitude=ok copKillRaisesBounty=ok rocks=648 shiny=382
trades=0` — **ПОБИТОВО** бейзлайн §5.13.36 (`radioClaims=3`/`craftDestroyed=2`/`rocks=648`/`shiny=382`/`trades=0`) плюс новый
`copKillRaisesBounty=ok`, ноль ASan/UBSan. Шоты НЕ требовались (СИМ-срез: `saveShot` не шагает сим, `localdraw.cpp` не тронут ⇒
кадры не меняются, их по-прежнему 28). Адверсарное ревью (read-only субагент) вернуло **SHIP** по всем 10 проверкам (комбат-инвариант,
детерминизм, память, соундность ре-деривации, тайминг `cWasWantedBefore`, двойной счёт, кламп, ригор пробника, C++11, тень/init);
две косметические заметки (точность комментария + отсутствие гейта `o.defending`) свёрнуты в комментарий кода — правка только
комментария, логика/бинарь неизменны. Правки: `local.h`, `localsim.cpp`, `soak_test.cpp`, `master_prompt.md`, `README.md`.

#### 5.13.39 Автономный срез РЕНДЕРА — `STANDING`-тир красит ПРИЦЕЛ (рамка цели + курсовой лид-пип), draw-пара к §5.13.37 (2026-07-29)

РЕНДЕР-спутник §5.13.38. По чередованию: §5.13.37 render → .38 sim → **.39 render**. §5.13.37 отгрузил строку `STANDING <WORD> ±N`
в панели наведённой цели; теперь то же стояние несёт и САМ ПРИЦЕЛ — рамка цели в 3D-мире и курсовой лид-пип упреждения красятся по
тиру отношений игрока с фракцией локнутой цели, чтобы стояние читалось с прицела, без чтения строки панели.

- **`localdraw.cpp` — новый файловый хелпер `standingCue(const Game&, int targetFaction, SDL_Color&)`** (перед закрытием
  анонимного namespace): гейт `targetFaction < 0 || playerFaction < 0 || targetFaction == playerFaction` ⇒ `false`; иначе
  `switch(classifyFactionRelationTension(game.factionRelation(playerFaction, targetFaction)))` возвращает `true` + цвет тира для
  Enemy/Hostile/Tense/Friendly/Ally, а `Neutral` ⇒ `false` (нет выраженного стояния ⇒ дефолт вызывающего). Тот же классификатор
  порогов, что и панель §5.13.37 (единый источник faction.cpp); 5 RGB-литералов тира — зеркало палитры панели (намеренная
  косметическая дубль: switch панели §5.13.37 НЕ трогаю, чтобы не возмущать отгруженный срез). Применён в двух местах
  `renderLocalScene`: (1) **рамка цели** (была `SDL_Color bcol = P.cyan` ⇒ если `standingCue` дал цвет, `bcol` = тир-цвет;
  жёсткий Tab-лок держит прежнюю альфу 170 от `bcol`); (2) **курсовой лид-пип** упреждения (был
  `tc.hostile ? rgba(255,150,90,205) : rgba(240,205,130,185)` ⇒ стояние важнее сырого `hostile`, но АЛЬФА сохранена
  `tc.hostile ? 205 : 185`). `tc` = `scene.craft[scene.targetCraft]` — тот же борт, что и рамка ⇒ `tc.faction` консистентен.
- **`shot_test.cpp` — новый 25-й кадр `shot_standing_reticle`** (case 25, последний): вражефракционный `CK_TRADER` (`faction=0`,
  `hostile=false`), `setFactionRelation(playerFaction, 0, -100)` ⇒ тир ENEMY, `targetCraft=0; lockTarget=0` ⇒ и рамка, и лид-пип
  КРАСНЫЕ ИЗ СТОЯНИЯ (`hostile=false` ⇒ красный hostile-дефолт исключён ⇒ причина изолирована). Пара к `shot_law_standing`
  (§5.13.37, тир ALLY +96 ⇒ зелёные рамка/пип). Case 25 мутирует реп фракции 0 в −100 ТОЛЬКО ПОСЛЕ того, как case 24 отрисовал ту
  же фракцию при +96 ⇒ порядок-зависимо, но корректно (комментарий в case 24 это отмечает).

**Комбат-инвариант §0.2-G цел:** пираты бесфракционны (`faction < 0`) ⇒ `standingCue` возвращает `false` на ПЕРВОЙ строке ⇒ рамка
пирата остаётся `P.cyan`, а его лид-пип — `tc.hostile ? rgba(255,150,90,205) : rgba(240,205,130,185)` ПОБИТОВО как раньше. Вопрос
фильтра «уменьшает ли это урон/агрессию/аптайм пирата?» — НЕТ (чистый draw-only). Ноль RNG (§2.3 — только чтение `factionRelation`
+ чистая арифметика классификатора). Не CHROMOCORE-ребейк (§2.4 — читает существующее поле `faction` + игровые отношения, ничего
не пишет). Без новых полей у борта. `factionRelation` двойне-баунд-безопасен (`validFaction(A) && validFaction(B)` + плоский
индекс-баунд ⇒ ноль OOB для любого `faction`, что может нести `LocalCraft`; строго безопаснее уже отгруженного вызова §5.13.37).
Дефолт-путь (`standingCue` = `false`) ПОБИТОВО идентичен до-изменению: `bcol` — копия `P.cyan`, перезаписывается лишь при `true`;
`lc` не тронут при `false`.

**Проверки (все зелёные):** `make clean && make` 0 предупреждений; `--smoke`/`--localsmoke` exit 0. `make soak` (под ASan/UBSan):
`SOAK DONE frames=120000 deaths=0 radioClaims=3 craftDestroyed=2 invariantFails=0 ... copKillRaisesBounty=ok rocks=648 shiny=382
trades=0` — **ПОБИТОВО** бейзлайн §5.13.36 (`renderLocalScene` в соаке не зовётся ⇒ SIM-бейзлайн нетронут), ноль ASan/UBSan.
**`make shot_asan` 29/29** сохранены, ноль ASan/UBSan; `make shots` 29/29. Визуальный плейтест (BMP→PNG через `sips`, смотрел
глазами): враг=красный / союзник=зелёный / нейтрал=циан прицел, дефолт-цвета не тронуты. Адверсарное ревью (read-only субагент)
вернуло **SHIP** по всем 10 проверкам (комбат-инвариант, draw-only, детерминизм, память/OOB, data-oriented, C++11, корректность
тиров, альфа/регресс, отсутствие полей/ребейка, ригор шота); 3 косметические заметки (порядок-зависимость шотов 24/25 — уже
отмечена комментарием; смена цвета пипа у hostile+ENEMY не-пирата — намеренная «стояние > сырой hostile»; дубль тир→RGB — можно
свести к общему `tierColor`, косметика) — ни одна не блокирует. Правки: `localdraw.cpp`, `shot_test.cpp`, `master_prompt.md`,
`README.md`.

#### 5.13.40 Автономный срез СИМУЛЯЦИИ — «месть фракции»: знак co-op-добивания зависит от стояния с фракцией патруля (sim-спутник §5.13.39) (2026-07-29)

СИМ-спутник §5.13.39. По чередованию: §5.13.37 render → .38 sim → .39 render → **.40 sim**. §5.13.36 всегда давал `+6` rep за
co-op-добивание розыскного, которого ПРЯМО СЕЙЧАС гнал патруль. Теперь ТОТ ЖЕ убой читается ПРОТИВОПОЛОЖНО по тому, где игрок
УЖЕ стоит с фракцией этого патруля: помог облаве дружеских/нейтральных — благодарность (`+6`), влез в операцию ВРАЖДЕБНЫХ —
вооружённое вмешательство (`−6`). Знак берётся из ТОГО ЖЕ классификатора порогов, что панель §5.13.37 и прицел §5.13.39, — эмерджентный
закон-цикл замкнулся на репутацию, которую игрок сам же и накопил.

- **`local.h` — `LocalCfg::LAW_GRATITUDE_REP = 6`** (после `COP_KILL_BOUNTY`): единый источник |Δrep|, заменил литерал `6` из
  §5.13.36. `constexpr int`, ноль новых полей у борта.
- **`localsim.cpp` — блок §5.13.36 (`gratPatrol ≥ 0`) переписан на знак от тира.** После свода охотника `gratPatrol` (тот же
  ИНВЕРТИРОВАННЫЙ P0-скан §5.13.36, БЕЗ изменений): `patFac = scene.craft[gratPatrol].faction`; `tier =
  classifyFactionRelationTension(game.factionRelation(game.playerFaction, patFac))`; `hostileFac = (tier==Enemy || tier==Hostile)`
  (реп ≤ −48); `dRep = hostileFac ? −LAW_GRATITUDE_REP : +LAW_GRATITUDE_REP`; `adjustFactionRelation(playerFaction, patFac, dRep)`.
  Ветвление cue: `hostileFac` ⇒ news «Reprisal: you gunned down a hostile faction's quarry» (kind 3) + тост `FACTION REPRISAL -REP`;
  иначе прежние «Bounty served…» (kind 2) + `LAW GRATEFUL +REP`. Тряска/`toastTimer` не тронуты.
- **`soak_test.cpp` — новый трёхсторонний пробник `factionReprisal=`** (сразу после `lawGratitude`): свежий `Game`, `F` = первая
  фракция ≠ `playerFaction`, ТРИ сцены = Scene A §5.13.36 (сидячий розыскной по носу `px+26` с корпусом→1, здоровый неподвижный
  патруль `F` в `py+300`), но реп `(pf,F)` ПРЕДпосеян симметричным `adjustFactionRelation(pf, F, seed−cur)`: (C) `seed=−60`/Hostile
  ⇒ ассерт `Δrep == −6`; (D) `+40`/Friendly ⇒ `+6`; (E) `−30`/Tense ⇒ `+6`. Ломается на первое изменение rep и сверяет с `rel0+want`.
  Поле `factionReprisal` вшито в строку `SOAK DONE`.

**Комбат-инвариант §0.2-G цел:** блок исполняется ПОСЛЕ смерти пирата (`before>0 && c.hullHP<=0`), читает лишь `.faction` ПАТРУЛЯ
и двигает реп/новость/тост/тряску — ни одного боевого поля пирата, его ИИ, урона или аптайма. Вопрос фильтра «уменьшает ли урон/
агрессию/аптайм пирата?» — НЕТ; если `−6` на что и влияет, то в сторону БОЛЬШЕЙ будущей враждебности фракции. Ноль RNG (§2.3 — целые
сравнения + тернарники). Не CHROMOCORE-ребейк (§2.4 — пишет геймплейный `factionRelations`, не запечённую стату). Без новых полей у
борта. **Безопасность/OOB:** `factionRelation` двойне-защищён (`validFaction(A)&&validFaction(B)` до `size_t`-каста индекса ⇒ любой
`patFac` вне диапазона ⇒ 0 ⇒ Neutral ⇒ `+6`, ноль OOB/мусора). **Диагональ:** `resizeFactionRelations` пиньнит `(pf,pf)=128`, а
`adjustFactionRelation` рано выходит на `A==B` ⇒ патруль СВОЕЙ фракции ⇒ читает 128=Ally ⇒ `+6` ⇒ no-op adjust + `LAW GRATEFUL` —
ПОБИТОВО §5.13.36. Нейтраль (реп 0) ⇒ `+6` ⇒ §5.13.36-путь сохранён ⇒ прежний пробник `lawGratitude` зелёный без правок.

**Проверки (все зелёные):** `make clean && make` 0 предупреждений; `--smoke`/`--localsmoke` exit 0. `make soak` (под ASan/UBSan):
`SOAK DONE frames=120000 deaths=0 radioClaims=3 craftDestroyed=2 invariantFails=0 ... lawGratitude=ok copKillRaisesBounty=ok
factionReprisal=ok rocks=648 shiny=382 trades=0` — **ПОБИТОВО** бейзлайн §5.13.39 (headless-соак не спавнит `CK_PATROL` ⇒ блок
no-op по построению), новый пробник `faction-reprisal: hostile[-60]->-6=YES friendly[+40]->+6=YES tense[-30]->+6=YES`, ноль
ASan/UBSan. Шотов по-прежнему **29** (СИМ-срез кадров не добавляет). Адверсарное ревью (read-only субагент) вернуло **SHIP** по
A–H (корректность знака, память/OOB, диагональ-своя-фракция, двусторонность пробника, отсутствие регресса §5.13.36, взаимодействие
с соседним faction-ship reprisal, кламп) и 5 хард-правилам; единственная незапирающая заметка (пробник не пиньнил тир Tense) закрыта
ТРЕТЬИМ сидом E (−30 ⇒ +6) ещё ДО коммита ⇒ наивный «rep<0 ⇒ −6» теперь валит пробник. Правки: `local.h`, `localsim.cpp`,
`soak_test.cpp`, `master_prompt.md`, `README.md`.

**Следующий срез (§5.13.41, РЕНДЕР).** По чередованию симуляция→рендер — draw-правило. Кандидат §0.2 (форвард-бэклог §4.3):
draw-парой показать знак §5.13.40 в кокпите — cue у момента добивания (зелёная `LAW GRATEFUL`-вспышка vs красная `FACTION REPRISAL`)
ИЛИ тинт золотого вектора погони §5.13.29 по `STANDING`-тиру фракции патруля (реюз `standingCue` §5.13.39 ⇒ игрок ВИДИТ, чью облаву
усилит/сорвёт ещё ДО выстрела). Держи чередование и «no-op по построению» до плейтеста.

#### 5.13.41 Автономный срез РЕНДЕРА — «вектор погони красит стояние»: золотой вектор §5.13.29 берёт цвет STANDING-тира фракции патруля (render-спутник §5.13.40) (2026-07-29)

Рендер-спутник §5.13.40. По чередованию: §5.13.38 sim → .39 render → .40 sim → **.41 render**. §5.13.40 сделал ЗНАК
co-op-добивания зависимым от стояния с фракцией патруля, но игрок узнавал знак лишь ПОСЛЕ выстрела (по тосту). Теперь тот же
исход телеграфируется ДО выстрела: золотой пунктирный вектор погони §5.13.29 (маршевый след от охотящегося патруля к его
розыскной жертве — и в мире, и линком на радаре) КРАСИТСЯ тем же тиром `standingCue` §5.13.39, что панель STANDING §5.13.37 и
прицел §5.13.39. Enemy/Hostile ⇒ красный (грядёт reprisal −rep §5.13.40), Friendly/Ally ⇒ зелёный (грядёт gratitude +rep),
Tense ⇒ янтарь, Neutral/без-фракции ⇒ ПРЕЖНЕЕ золото (ноль регресса). Меняется лишь HUE — heat/pulse/march-альфа §5.13.35
нетронуты. Три читалки (панель STANDING §5.13.37, прицел §5.13.39, вектор §5.13.41) согласованы ПО ПОСТРОЕНИЮ через единый
классификатор `classifyFactionRelationTension`.

- **`localdraw.cpp` — мировой вектор (~1614-1625).** После расчёта `gold`: `SDL_Color vec = gold;` затем
  `{ SDL_Color sc; if (standingCue(game, pc.faction, sc)) vec = rgba(sc.r, sc.g, sc.b, gold.a); }` — где `pc = scene.craft[i]`
  ОХОТЯЩИЙСЯ патруль (источник `pursuitQuarry`). На `false` `vec` остаётся `gold` ⇒ вызов `SDL_SetRenderDrawColor` побитово
  прежний. Альфа явно = `gold.a` (heat/pulse не трогаются). Стрелка-наконечник (~1640) наследует тот же цвет ⇒ вектор одноцветен.
- **`localdraw.cpp` — линк на радаре (~2343-2356).** Та же форма для радар-линии патруль→жертва: `rc` инициализируется прежним
  литералом `rgba(255,205,60,45+int(pl*120.0))`, затем `standingCue(game, scene.craft[i].faction, sc)` перекрашивает RGB,
  сохраняя `rc.a`. Мир и радар читают ОДНУ фракцию (патруль `i`) ⇒ хью не разойдутся.
- **`standingCue` (986-998, ТОЛЬКО ЧТЕНИЕ, реюз §5.13.39).** `static bool standingCue(const Game&, int targetFaction, SDL_Color&)`
  — false для faction<0 / playerFaction<0 / своей фракции / Neutral (⇒ caller держит золото); иначе пишет тир-цвет и true. Уже
  в области видимости `renderLocalScene` (звался на §5.13.39 прицеле) — новых прототипов/включений не нужно.
- **`shot_test.cpp` — пара кадров 26/27** (перед итоговым `SHOTS DONE`): та же геометрия «патруль гонит розыскного» (борт-о-борт,
  ~300 LU врозь по Y, равная глубина ~530 LU, звезда за камерой), различие ТОЛЬКО в репутации: 26 `setFactionRelation(pf,1,-100)`
  ⇒ ENEMY ⇒ КРАСНЫЙ (`shot_reprisal_vector.bmp`); 27 `+96` ⇒ ALLY ⇒ ЗЕЛЁНЫЙ (`shot_gratitude_vector.bmp`), затем восстанавливает
  реп в 0 (гигиена; 0 — истинный дефолт `game.cpp` для пар с игроком). Оба ПОСЛЕ фракция-1 сцен 20-23 ⇒ те не тронуты. Шотов 29 → **31**.

**Комбат-инвариант §0.2-G цел:** чистая перекраска по ЧТЕНИЮ репутации — ни одно боевое поле пирата, его ИИ, урон или аптайм не
затронуты (пираты вообще faction<0 ⇒ `standingCue` для них false). Вопрос фильтра «уменьшает ли урон/агрессию/аптайм пирата?» —
НЕТ. Ноль RNG (§2.3). Не CHROMOCORE-ребейк (§2.4 — читает геймплейный `factionRelation`, не пишет ни запечённую стату, ни поле).
Без новых полей у борта — только локали `vec`/`rc`/`sc`. **Безопасность:** `vec`/`rc` инициализированы золотом ДО условия; `sc`
читается лишь при `true`, а `standingCue` пишет `col` перед каждым `return true`; `factionRelation` двойне-защищён `validFaction`
⇒ мусорная фракция ⇒ 0 ⇒ Neutral ⇒ false ⇒ золото (ноль OOB/неинициализированного цвета). Своя фракция патруля ⇒ false ⇒ золото.

**Проверки (все зелёные):** `make clean && make` 0 предупреждений; `--smoke`/`--localsmoke` exit 0. `make soak` (под ASan/UBSan):
**ПОБИТОВО** бейзлайн §5.13.40 (`localdraw.cpp` НЕ в headless-пути ⇒ `factionReprisal=ok`, `invariantFails=0`), ноль ASan/UBSan.
`make shot_asan` → `SHOTS DONE: 31/31 saved ok` (оба новых кадра `rc=0 craft=2`, ноль санитайзера); `make shots` → 31/31.
**Визуальная триада** (sips BMP→PNG, Read PNG): `shot_reprisal_vector` = длинный КРАСНЫЙ пунктир патруль→пират через середину
экрана + наконечник + красная рамка/пип + `STANDING ENEMY −100` + `DIST 534 LU`; `shot_gratitude_vector` = ЗЕЛЁНЫЙ, `STANDING
ALLY +96`, `DIST 534 LU` (та же геометрия/награда, различие лишь в стоянии); контроль `shot_law_pursuit` = ЗОЛОТО, `STANDING
NEUTRAL +0`, `DIST 166 LU` (нейтральный путь без регресса). Адверсарное ревью (read-only субагент) вернуло **SHIP** claim-by-claim
(корректность обоих блоков, побитовость нейтрального пути, одноцветность мир+радар+наконечник, отсутствие новых полей/RNG/мутаций,
восстановление репы в истинный дефолт 0, порядок кадров 26/27 последними ⇒ 20-23 не тронуты, счёт 31, OOB/неинициализированный
цвет, 5 хард-правил) — дефектов не найдено. Правки: `localdraw.cpp`, `shot_test.cpp`, `master_prompt.md`, `README.md`.

**Следующий срез (§5.13.42, СИМ).** По чередованию рендер→симуляция — sim-правило. Кандидаты §0.2 (форвард-бэклог §4.3):
репутация с фракцией патруля влияет на боеготовность/скорость реакции патруля (враждебные быстрее вскидываются на игрока) ИЛИ
cop-kill растит не только баунти §5.13.38, но и численность/частоту волны патрулей. Держи чередование и «no-op по построению» до плейтеста.

#### 5.13.42 Автономный срез СИМУЛЯЦИИ — «закон, который ты ПЕРЕШЁЛ, охотится на тебя» (враждефракционный патруль берёт игрока целью БЕЗ провокации, sim-спутник §5.13.41) (2026-07-29)

**Что и зачем.** «Месть фракции» §5.13.40 карала co-op-добивание −6 rep, а §5.13.41 красила вектор погони в цвет стояния — но
до сих пор накопленная вражда с фракцией НИКАК не меняла ПОВЕДЕНИЕ её патрулей: они охотились на игрока только если он выстрелил
первым (`c.hostile`). Этот срез даёт мести зубы: патруль фракции, с которой игрок стоит на тире **Enemy/Hostile** (реп ≤ −48),
берёт ИГРОКА целью БЕЗ свежей провокации. Замкнут эмерджентный виток закона: преступление → розыск → облава §5.13.34 →
благодарность/месть §5.13.36/40 (репутация ↑/↓) → **и сама репутация теперь спускает на тебя патрули §5.13.42**.

**Правка (одна ветка).** В `updateLocalScene()` ветка `CK_PATROL`: СУЩЕСТВУЮЩИЙ гейт предпочтения игрока расширен с `c.hostile`
до `(c.hostile || factionHuntsPlayer)`, где `factionHuntsPlayer` вычисляется лишь для НЕспровоцированного патруля с валидной
фракцией как `classifyFactionRelationTension(factionRelation(playerFaction, c.faction)) ∈ {Enemy, Hostile}`. Тот же классификатор
питает панель `STANDING` §5.13.37, тир-цвет прицела §5.13.39 и вектор погони §5.13.41 ⇒ читалка и поведение согласованы ПО
ПОСТРОЕНИЮ. Реюз ГЕЙТА `pd2 < atkBest` (а не новый тир-приоритет): враждефракционный патруль бросит пирата-дичь ради игрока
ТОЛЬКО если игрок БЛИЖЕ — ровно как уже делает спровоцированный патруль; охота §5.13.28 при более близком пирате цела.

**Инвариант §0.2-G усилен.** Правило добавляет патрулю ЦЕЛЬ, у пирата ничего не читается/пишется. Патруль, отвлёкшийся на
игрока, оставляет пирату аптайма БОЛЬШЕ, не меньше ⇒ «уменьшает урон/агрессию/аптайм пирата?» — НЕТ. Пираты бесфракционны ⇒
`factionHuntsPlayer` для них всегда ложь. Ноль RNG (§2.3), без новых полей, не CHROMOCORE-ребейк (§2.4, читаем лишь
`factionRelation`); `factionRelation` двойне-защищён ⇒ мусор/своя фракция ⇒ 0 ⇒ Neutral ⇒ false ⇒ прежнее поведение.

**Проверки (все зелёные).** `make clean && make` 0 предупреждений; `--smoke`/`--localsmoke` exit 0. `make soak` (под ASan/UBSan):
**ПОБИТОВО** бейзлайн §5.13.41 (`frames=120000 deaths=0 invariantFails=0 rocks=648 shiny=382 trades=0`, per-star breakdown цел,
все прежние пробники `ok` + новый `lawHuntsHostile=ok`, ноль ASan/UBSan). Новый двухфазный пробник `lawHuntsHostile` (свежий
`Game`, сцена = РОВНО один борт: патруль фракции `F`, `resize(1)` ⇒ ни пиратов ни иных целей, `hostile=false` пиньнется каждый
кадр ⇒ триггер ЧИСТО стояние; игрок пиньнут в 300 LU: >`WEAPON_RANGE` 150 ⇒ огонь не достаёт, <`AW2` 750 ⇒ в осведомлённости, не
стреляет): **(A)** реп −100/Enemy ⇒ патруль берёт игрока (`aiState==1`, `huntsAtEnemyStanding`); **(B, контроль)** реп 0/Neutral ⇒
простаивает (`aiState==0`, `idleAtNeutral`) — баг «всегда охотится» валит B, «никогда» валит A ⇒ проба доказывает, что триггер
именно СТОЯНИЕ, а не провокация.

**⚠️ Почему soak — no-op (ЭМПИРИЧЕСКИ проверено, важно для будущих ревью).** Адверсарное ревью вернуло **SHIP** claim-by-claim
(приоритет/порядок, `c.defending=false` корректно-by-design, безопасность `playerTargetable`/`pd2`-сентинела, точность порогов,
границы, детерминизм, валидность пробы, счёт printf, правила §2.1/§2.2/§2.4/§0.2-G — дефектов нет), НО оно же выдвинуло поправку:
«`init()` создаёт military-агентов (`game.cpp:3672`), мапящихся в `CK_PATROL` (`localgen.cpp:453`) ⇒ патрули СПАВНЯТСЯ в соаке ⇒
прежняя формулировка „нет патрулей в соаке“ ложна». **Эта поправка ОПРОВЕРГНУТА замером.** Врем. инструмент в главном 120k-цикле
насчитал `patrols=0` на ВСЕХ 10 звёздах маршрута `{-1,0,1,2,3,5,7,11,23,99}`. Причина: 18 военных агентов действительно есть и
стоят (`enRoute=0`), но гарнизонами на `controlledStars` своих фракций, разбросанных по кластеру-1200 (замерено: 78/792/1073/314/
630/15/…), а `buildLocalScene` co-локейтит агента ЛИШЬ при `currentStar==звезда` (`localgen.cpp:438`) — множества не пересекаются
(`atVisitedStars=0`). Итог: «нет патрулей в соаке» — ВЕРНО, но по причине disjoint-маршрута + co-location-гейта, а НЕ «заморозки
мира». Тот же механизм задним числом объясняет no-op §5.13.28/30/32/34/36/38/40. Инструмент удалён после замера; бейзлайн чист.

**Следующий срез (§5.13.43, РЕНДЕР).** По чередованию — render-спутник этого правила: кокпит-cue «закон охотится на ТЕБЯ».
Враждефракционный патруль, локнувший игрока по СТОЯНИЮ (§5.13.42, не по его выстрелу), несёт видимый маркер (рамка/edge-стрелка/
строка `HUNTING YOU`), чтобы игрок ПОНЯЛ, почему на него напали без провокации (реюз `standingCue` §5.13.39 + логика цели §5.13.42).
Правки: `localsim.cpp`, `soak_test.cpp`, `master_prompt.md`, `README.md`. Держи чередование и «no-op по замеру» до плейтеста.

---

#### 5.13.43 Автономный срез РЕНДЕРА — кокпит-cue «HUNTING YOU»: видно, что закон охотится на ТЕБЯ по стоянию (render-спутник §5.13.42) (2026-07-29)

**Что и зачем.** Render-спутник §5.13.42 (чередование .41 render → .42 sim → **.43 render**). §5.13.42 научил патруль брать
ИГРОКА целью БЕЗ провокации, если тот стоит с его фракцией на тире Enemy/Hostile — но в кокпите это выглядело как
немотивированная атака. Срез даёт игроку ПРИЧИНУ на экране: над бортом такого патруля загорается багровая подпись
`HUNTING YOU` + самый быстрый/яркий пульс-кольца, а если охотник за кадром — краевая стрелка цветом стояния. Игрок
ПОНИМАЕТ, почему на него напали без его выстрела.

**Ключевое решение — ноль новых полей и НУЛЬ нового sim-поведения.** Задача из бэклога просила render-cue «без изменения
симуляции». Выполнено буквально: оживлено СУЩЕСТВОВАВШЕЕ, но МЁРТВОЕ поле `LocalCraft::aiTarget` (объявлено `local.h:246`,
до сих пор только `init=-1` в `localgen.cpp` 194/456/494/522, НИГДЕ в симуляции не читалось — проверено grep'ом: 6 вхождений,
все init/декларация). Симуляция пломбирует его read-only сразу после присвоения `aiState` (`localsim.cpp:630`):
`c.aiTarget = (c.aiState==1) ? (atkPlayer ? LOCAL_TARGET_PLAYER : (atkCraft>=0 ? atkCraft : -1)) : -1`. Так как НИ ОДНО
sim-решение это поле не читает (стр.638 читает `atkCraft`, стр.658 — `aiState`, НЕ `aiTarget`) ⇒ 0 влияния на поведение ⇒
соак остаётся ПОБИТОВО идентичным.

**Предикат cue = «сработал override §5.13.42 по стоянию».** Рендер (`localdraw.cpp:1536`):
`c.kind==CK_PATROL && c.aiState==1 && c.aiTarget==LOCAL_TARGET_PLAYER && !c.hostile`. Эквивалентность:
`aiTarget==LOCAL_TARGET_PLAYER` ⟺ `atkPlayer` было истинно; для ветки `CK_PATROL` `atkPlayer=true` возможно ТОЛЬКО на
строке §5.13.42 (стр.574; стр.402 — лишь `CK_PIRATE`, стр.579 — лишь `CK_TRADER/CIVILIAN`) ⇒ предикат ⟺ сработал override
.42. Гейт `!c.hostile` отсекает СПРОВОЦИРОВАННЫЙ патруль (его враждебность самоочевидна и без подсказки) — остаётся ровно
неспровоцированная охота ПО СТОЯНИЮ. Бегущий/простаивающий патруль ⇒ `aiTarget=-1` ⇒ cue не горит.

**Грамматика.** Цвет — тот же `standingCue` §5.13.39 (Enemy багровый `rgba(238,88,82)`, Hostile оранжево-красный
`rgba(238,120,82)`; fallback `P.red` ⇒ ноль неинициализированного цвета). Пульс — темп 7.4, отличный от всех прежних
(причал 3.0 / эскорт 4.5 / розыск 5.2 / SOS 6.0), альфа 110..255 (ярче всех статусных), круги `sz+10`/`sz+15`. За кадром —
краевой маркер `nearHunt` (селектор нужен `!c.hostile`), DISJOINT со старым `nearHos` (нужен `c.hostile`,
`localdraw.cpp:1964`) ⇒ ноль двойного маркера; когда охотник, получив выстрел, флипает `hostile=true`, он выпадает из
`nearHunt` и попадает в `nearHos` (красный) — чистый хэндофф.

**Правила.** Data-oriented (§2.1), C++11/SDL2 (§2.2), ноль RNG (§2.3), не CHROMOCORE-ребейк (§2.4 — читается лишь
`factionRelation`), §0.2-G цел (render-only, пираты не тронуты; пираты бесфракционны ⇒ `standingCue`→false, и их cue не
касается — гейт `CK_PATROL`).

**Проверки (все зелёные).** `make clean && make` 0 предупреждений; `--smoke`/`--localsmoke` exit 0. `make soak` (под
ASan/UBSan): **ПОБИТОВО** бейзлайн §5.13.42 (`frames=120000 deaths=0 invariantFails=0 rocks=648 shiny=382 trades=0`,
per-star breakdown цел, все прежние пробники `ok`, ноль ASan/UBSan) + новый пробник `patrolTargetSignal=ok`. Пробник читает
`aiTarget` НАПРЯМУЮ (не через урон): **(A)** свежий `Game`, одинокий патруль фракции `F`, `resize(1)`, реп Enemy ⇒
`aiState==1 && aiTarget==LOCAL_TARGET_PLAYER`; **(B, анти-ложняк)** тот же патруль + розыскной пират БЛИЖЕ игрока ⇒ патруль
целит В ПИРАТА (`aiTarget>=0`, НЕ сентинел игрока) — запирает точный предикат cue, т.к. `localdraw` вне headless-пути.
`make shot_asan` 34/34 (ноль ASan/UBSan), `make shots` 34/34. **+3 шота** (не обычный +1): `shot_hunted_standing`
(Enemy-патруль, `aiTarget=PLAYER` ⇒ cue ГОРИТ: багровое `HUNTING YOU` + кольца, панель `STANDING ENEMY −100`) +
`shot_hunted_none` (тот же Enemy-патруль в бою, но `aiTarget`=индекс пирата ⇒ cue ПОГАС) — меняется ТОЛЬКО `aiTarget` ⇒
изолирует гейт + `shot_hunted_edge` (тот же охотник, нос камеры отвёрнут ⇒ борт ЗА КАДРОМ ⇒ покрыта OFF-SCREEN ветка
`nearHunt`/`drawEdgeMarker`, багровая стрелка на правом ободке; ревью item #3); глазами (sips→PNG) подтверждено (в A подпись
есть, в B той же панели — нет, в edge — краевая стрелка справа). Счётчик 31 → **34**.

**Ревью.** Параллельно запущено read-only адверсарное ревью (Explore-субагент): проверяет A1 (`aiTarget` мёртв до среза),
A2 (populate ⇒ побитово), A3 (единственный путь `atkPlayer=true` для патруля), B1–B3 (эквивалентность предиката, disjoint
`nearHunt`/`nearHos`, fallback цвета), правила и валидность пробы. Вывод субагента трактуется как ДАННЫЕ и
перепроверяется независимо (в §5.13.42 субагент ОШИБСЯ про `aiTarget` — здесь ключевые claim'ы перепроверены прямым
чтением `localsim`/`localdraw`: стр.638/658 читают `atkCraft`/`aiState`, не `aiTarget`; `nearHos`@1964 требует `c.hostile`).
**Итог ревью: все claim'ы CONFIRMED, дефектов логики нет.** По его находкам внесены 3 усиления устойчивости (не багфиксы):
**(item #4)** дублирующийся 4-термовый предикат вынесен в ОДИН общий хелпер `huntsPlayerByStanding()` (`localdraw.cpp`), зовомый
обеими площадками (on-screen cue + off-screen `nearHunt`) — чтобы не разъехались; заодно в хелпер сложен `hullHP>0` (on-screen
площадка его раньше НЕ имела — защитно, cue не рисуется над трупом). **(item #3)** добавлен `shot_hunted_edge` — покрытие
OFF-SCREEN краевой ветки, которую on-screen пара #28/#29 не задевала. **(B1, точность)** прозой уточнено, что `⟺` неточно у
края: беглец (`aiState=2 ⇒ aiTarget=−1`) не даёт cue — это ЖЕЛАЕМО, и уже покрыто самим `aiState==1` в предикате. После правок
`localdraw.cpp` вся лестница перепрогнана НАЧИСТО и снова зелёная (соак побитово, `shot_asan` 34/34, триада глазами).

**Следующий срез (§5.13.44, СИМ).** По чередованию — sim-слайс: репутация влияет на ГОТОВНОСТЬ/скорость реакции патруля ИЛИ
коп-килл растит численность/частоту волны патрулей (см. §4.3; фильтр §0.2-G обязателен — кандидат НЕ должен ослаблять
урон/агрессию/аптайм пирата). Держи чередование и «no-op по замеру» до плейтеста.

---

#### 5.13.44 Автономный срез СИМУЛЯЦИИ — ВЕНДЕТТА закона: скорость погони патруля растёт с глубиной вражды (sim-спутник §5.13.42/43) (2026-07-29)

**Что и зачем.** Sim-спутник §5.13.42/43 (чередование .42 sim → .43 render → **.44 sim**), замыкающий закон-цикл на входе
симуляции. §5.13.42 научил патруль брать игрока целью по стоянию (реп ≤ −48); §5.13.43 показал это в кокпите, оживив мёртвое
поле `aiTarget`. Теперь симуляция ВПЕРВЫЕ ПОТРЕБЛЯЕТ этот `aiTarget`: патруль, что гонится ЗА ИГРОКОМ по стоянию, летит тем
резвее, чем ГЛУБЖЕ минус его фракции к игроку. Поле `aiTarget` дорастает из чисто-рендерного (§5.13.43) до полноценного ВХОДА
sim — накопленная вражда наконец диктует не только ЧТО патруль делает, но и НАСКОЛЬКО ЯРОСТНО.

**Механика — зеркало облавы §5.13.34, но ключ иной.** Новая `vendettaSpeedMult(const Game& game, const LocalScene& scene, int
ci)` рядом с `manhuntSpeedMult` (`localsim.cpp`). Гейт: борт — живой `CK_PATROL` И `c.aiTarget==LOCAL_TARGET_PLAYER` И фракция
валидна и не своя (`c.faction>=0 && c.faction!=game.playerFaction`); иначе 1.0 (нулевой регресс). Накал:
`heat = clamp((−48.0 − rep)/80.0, 0, 1)`, где `rep = game.factionRelation(game.playerFaction, c.faction)`; возврат
`1 + LocalCfg::VENDETTA_SPEED_GAIN·heat`. Маппинг: реп −48 (порог Hostile, где включается охота §5.13.42) ⇒ heat 0 ⇒ ×1.0;
реп −96 ⇒ 0.6; реп −128 (дно) ⇒ 1.0 ⇒ ×1.6. Отличие от облавы (`heat` по награде розыскного): здесь `heat` по ЛИЧНОЙ
репутации, а знаменатель 80 = |−128 − (−48)| — ширина диапазона от порога охоты до дна. `VENDETTA_SPEED_GAIN=0.6 > MANHUNT
0.5` — сознательно: личная вражда неотвратимее профессиональной охоты за головой. Множится в `effMax` (`localsim.cpp:~951`):
`c.maxSpeed·(boost?1.8:1)·manhuntSpeedMult·vendettaSpeedMult`.

**Дизъюнктность с облавой — ПО ПОСТРОЕНИЮ (два доказательства).** На одном борту активен максимум ОДИН из двух множителей.
(1) `manhuntSpeedMult` гейтится на `c.defending`, а override §5.13.42, ставящий `atkPlayer` (⇒ позже `aiTarget=PLAYER`),
ГАСИТ `c.defending=false` (`localsim.cpp:596`) ⇒ патруль, гонящий игрока, не `defending` ⇒ облава возвращает 1.0. (2) Сверх
того, в `manhuntSpeedMult` добавлен явный guard `if (c.aiTarget == LOCAL_TARGET_PLAYER) return 1.0;` — избыточен к (1), но
делает инвариант локально-очевидным и устойчивым к будущим правкам гейта `defending`. Так `manhunt>1 ⇒ aiTarget!=PLAYER ⇒
vendetta=1` и `vendetta>1 ⇒ aiTarget==PLAYER ⇒ manhunt=1`.

**Живой `aiTarget` свежий по фазировке.** `vendettaSpeedMult` читает `c.aiTarget`, который ИИ-цикл спломбировал в этом же
кадре (`localsim.cpp:630`, §5.13.43). Движение — ОТДЕЛЬНЫЙ проход (`for i` со строки ~938) ПОСЛЕ полного ИИ-цикла ⇒ на момент
замера скорости ВСЕ борта уже зафиксировали `aiTarget` ⇒ ни гонки, ни зависимости от порядка. Точка цели для `atkPlayer` —
ровно позиция игрока (`tgx=scene.px…`, `сближение` без стендоффа), тормоз огня только при `tdist<WEAPON_RANGE`(150) ⇒ вне 150
LU патруль держит полный `effMax`.

**Правила.** Data-oriented (§2.1 — свободная функция, C-поля, ноль OOP), C++11/SDL2 (§2.2), ноль RNG и ноль касания
глобального `rng` (§2.3 — читается лишь детерминированный `factionRelation`), не CHROMOCORE-ребейк (§2.4). Комбат-инвариант
§0.2-G УСИЛЕН: патруль лишь БЫСТРЕЕ гонит ИГРОКА (больше времени-в-бою, не меньше); ПИРАТА не трогаем совсем; реп выше −48 ⇒
×1.0 = прежнее поведение. `factionRelation` двусторонне-баунд-безопасен (мусор/своя фракция отсечены гейтом).

**Проверки (все зелёные).** `make` 0 предупреждений; `--smoke`/`--localsmoke` exit 0. `make soak` (ASan/UBSan): **ПОБИТОВО**
бейзлайн §5.13.43 (`frames=120000 deaths=0 invariantFails=0 rocks=648 shiny=382 trades=0`, per-star breakdown цел, ВСЕ прежние
пробники `ok` — в т.ч. `manhuntScalesWithBounty=90.00/60.00` неизменен ⇒ новый guard не сдвинул старое поведение) + новый
пробник `vendettaScalesWithStanding=ok`. Пробник: свежий `Game`, одинокий патруль фракции `F` + игрок (`resize(1)` ⇒ ни
пиратов, ни иных целей), пин в 600 LU (>WEAPON_RANGE 150 ⇒ огонь не тормозит; <AW2 750 ⇒ игрок в осведомлённости, `atkBest=AW2`
нетронут ⇒ pd2=360k<562k ⇒ override §5.13.42 берёт игрока); пин ПОЗИЦИИ но НЕ скорости (нужен разгон accel=300 до effMax).
**(A)** реп −128 ⇒ `deepEnmity=96.00 (exp 96.00 = 60·1.6)`; **(B, контроль)** реп −48 ⇒ `threshold=60.00 (exp 60.00)` —
критично: при −48 override §5.13.42 ВСЁ ЕЩЁ берёт игрока (тир Hostile), но heat=0 ⇒ ×1.0; ассерт `bothHuntPlayer=yes`
подтверждает, что ОБА борта реально целят игрока ⇒ бонус растёт с ГЛУБИНОЙ вражды, а не с фактом охоты. `faster=yes` (96>60).
Рендер НЕ тронут ⇒ `make shot_asan` 34/34, `make shots` 34/34 — БЕЗ новых кадров (как §5.13.34: облава тоже без шота; `saveShot`
сим не шагает ⇒ sim-правка на кадр не влияет).

**Следующий срез (§5.13.46, СИМ).** По чередованию — sim-слайс. Кандидат §0.2 (форвард-бэклог §4.3): реп-затухание/«остывание»
вражды со временем (heat спадает, если игрок не грешит — сейчас реп меняется лишь по событиям, никогда сама), ЛИБО коп-килл
растит численность/частоту волны патрулей. Фильтр §0.2-G обязателен (кандидат НЕ должен ослаблять урон/агрессию/аптайм пирата).
Держи чередование и «no-op по замеру» до плейтеста.

---

#### 5.13.45 Автономный срез РЕНДЕРА — ЖАР ВЕНДЕТТЫ: кокпит-cue «HUNTING YOU» калится глубиной твоей вражды (render-спутник §5.13.44) (2026-07-29)

**Что и зачем.** Render-спутник §5.13.44 (чередование .44 sim → **.45 render**), замыкающий связку .42→.43→.44→.45 на выходе
рендера. §5.13.43 зажёг багровую подпись «HUNTING YOU» + пульс-кольца, когда закон берёт игрока целью по стоянию (гейт
`huntsPlayerByStanding`); §5.13.44 научил sim гнать такой патруль тем резвее, чем глубже минус его фракции (`heat`). §5.13.45
МОДУЛИРУЕТ кокпит-cue тем же `heat`: глубже вражда ⇒ ГОРЯЧЕЕ тон (анил к бело-калёному), РЕЗЧЕ пульс, ЯРЧЕ база. Теперь один
взгляд на cue читает не только «за тобой охотятся», но и «НАСКОЛЬКО тебя ненавидят» — визуальный dial той же вражды, что в
.44 диктует скорость.

**Механика — приём D (heat выведен draw-side из живой репутации).** Как §5.13.35 калил вектор облавы по `heat` §5.13.34, так и
здесь `heat` НЕ хранится в новом поле, а ре-выводится на отрисовке из живой репутации формулой, ПОБИТОВО совпадающей с
`vendettaSpeedMult` (§5.13.44): `rep = game.factionRelation(game.playerFaction, c.faction); heat = clamp((−48.0 − rep)/80.0,
0, 1)`. Новый файл-статик хелпер `vendettaHuntColor(const Game&, const LocalCraft&, double& heatOut)` (`localdraw.cpp`, сразу
за `huntsPlayerByStanding`): базовый тон — цвет стояния (`standingCue`, fallback `P.red` для нейтрала/без-фракции), тёплый анил
`rgba(hc.r + heat·40, hc.g + heat·150, hc.b + heat·150, 255)` ⇒ багрянец → бело-горячий (тёплая ось, НЕ cyan; `rgba@39`
клампит [0,255], поэтому переполнение при heat→1 БЕЗОПАСНО и намеренно, как §5.13.35). On-screen cue (`huntsPlayerByStanding`):
темп пульса `7.4 + heat·3.7` (7.4 → 11.1, ×1.5 — тот же множитель, что марш облавы §5.13.35), база альфы `110 + heat·50`
(110 → 160), поверх — синус-пульс `pl·145`; `rgba` клампит пик. Подпись/кольца берут `hc`.

**Анти-дрейф — общий хелпер (как §5.13.43 делил предикат).** Off-screen краевой маркер `nearHunt` (§5.13.43) берёт ТОТ ЖЕ
`vendettaHuntColor` ⇒ тон on/off-screen НЕ разъедется при будущих правках (тот же приём, что общий `huntsPlayerByStanding`/
`standingCue` в §5.13.43, чтобы cue и маркер не спорили о факте охоты). Маркер статичен (`(void)heat` — берём лишь тон, не
темп/яркость: краевая стрелка не пульсирует).

**Нулевой регресс при heat=0 — доказан арифметически.** Порог включения охоты §5.13.42 — реп −48 ⇒ heat=0. Тогда `int(0·40)=0`,
`int(0·150)=0` ⇒ тон РОВНО `standingCue`/`P.red`; темп `7.4 + 0·3.7 = 7.4`; база `110 + int(0·50) = 110` ⇒ cue ПОБИТОВО
идентичен §5.13.43. Каждый heat-член исчезает — новизна видна лишь когда реп проседает НИЖЕ −48.

**Правила.** Data-oriented (§2.1 — свободный файл-статик, C-поля, ноль OOP), C++11/SDL2 (§2.2), ноль RNG и ноль касания
глобального `rng` (§2.3 — читается лишь детерминированный `factionRelation`), не CHROMOCORE-ребейк (§2.4). Чистый **draw-only**:
ни нового поля, ни записи в sim/`LocalCraft`/`Game`, лишь чтение `factionRelation` на кадр ⇒ **`make soak` ПОБИТОВО** (localdraw
в headless не бежит). Комбат-инвариант §0.2-G тривиально соблюдён (рендер урон/аптайм не трогает).

**Проверки (все зелёные).** `make` 0 предупреждений; `--smoke`/`--localsmoke` exit 0. `make soak` (ASan/UBSan): **ПОБИТОВО**
бейзлайн §5.13.44 (`frames=120000 deaths=0 invariantFails=0 rocks=648 shiny=382`, все пробники `ok`, вкл.
`vendettaScalesWithStanding`). `make shot_asan` **36/36** (0 ASan/UBSan), `make shots` **36/36** — +2 шота-эндпоинта:
`shot_hunted_vendetta_hot` (реп −128, heat 1.0, `fxClock` в пике синуса ⇒ бело-горячая подпись/кольца, STANDING ENEMY −128) и
`shot_hunted_vendetta_cold` (реп −48, heat 0 ⇒ РОВНО §5.13.43, оранж-багрянец, STANDING HOSTILE −48 — пин нулевого регресса).
A/B-контраст (горячий бело-калёный/резкий vs холодный §5.13.43-идентичный) подтверждён глазами. Оба шота восстанавливают реп в 0
после себя (не текут в соседние сцены).

**Следующий срез (§5.13.46, СИМ).** По чередованию — sim-слайс. См. форвард-бэклог §4.3 (кандидаты: реп-«остывание» вражды со
временем ЛИБО рост волны патрулей по коп-киллам; фильтр §0.2-G обязателен). Держи чередование и «no-op по замеру» до плейтеста.

---

#### 5.13.46 Автономный срез СИМУЛЯЦИИ — СТАЯ ЗАКОНА: глубокая вражда сзывает патрули в скоординированную охоту на игрока (sim-спутник §5.13.42/.44) (2026-07-29)

**Что и зачем.** Sim-спутник линии .42→.43→.44→.45 (чередование .45 render → **.46 sim**). §5.13.42 дал патрулю ВРАЖДЕБНОЙ
фракции брать ИГРОКА целью по стоянию — но через РЕЮЗ гейта `pd2 < atkBest`: патруль бросит пиратскую дичь ради игрока лишь
если игрок БЛИЖЕ. Значит патруль с более близким розыскным пиратом гонит ПИРАТА, а игрок-враг остаётся «на потом». §5.13.46
добавляет ПАК-override: если рядом стоят ДВА (и более) враждебных игроку патруля, что оба его видят, они БРОСАЮТ своих пиратов
и сходятся на игроке — как пираты сзывают стаю к розыскному (§5.13.30) и патрули идут на подмогу облепленному охотнику
(§5.13.32), но здесь мишень — ИГРОК. Глубокая вражда перестаёт быть пассивным штрафом к скорости (.44) и становится
КООРДИНАЦИЕЙ: численность врагов вокруг тебя теперь сама превращается в приоритет.

**Механика — новый тир, ПОСЛЕДНИЙ в ветке `CK_PATROL`.** Вставлен сразу за player-override §5.13.42 (последним оператором
ветки, перед `} else { // CK_TRADER/CIVILIAN`), где `factionHuntsPlayer`/`atkPlayer`/`atkCraft`/`atkBest`/`pd2`/`AW2` уже в
области видимости. Гейт входа: `factionHuntsPlayer && playerTargetable && !atkPlayer && pd2 < AW2` — патруль Enemy/Hostile-
стояния (тот же тир `classifyFactionRelationTension`, что §5.13.42; ветвь `!c.hostile`), игрок жив и в осведомлённости ЭТОГО
патруля, а §5.13.42 игрока НЕ взял (`!atkPlayer` ⇒ пиратская дичь оказалась ближе). Тогда скан соседей: ∃ ли другой живой
`CK_PATROL` `o` в `PATROL_DISTRESS_R` от `c`, чья фракция (`o.faction>=0 && ≠ playerFaction`) тоже стоит Enemy/Hostile с
игроком И у кого игрок в осведомлённости (`|scene.p − o| < AW2`)? Если да — `atkBest=pd2; atkPlayer=true; atkCraft=-1;
c.defending=false`: патруль присоединяется к охоте на игрока. `atkBest=pd2` критично — как в §5.13.42: иначе `tdist =
sqrt(atkBest)` (дистанция до цели для fire-range-чека ниже по ветке) осталась бы дистанцией до ПИРАТА, и патруль целил бы «по
призраку». Т.к. тир СТОИТ ПОСЛЕДНИМ, он перебивает выбор P0-скана §5.13.28 / подмоги §5.13.32 / рейдера §5.13.12 / §5.13.42.

**Приём D — порядок-независимость без дедлока.** «Сосед РАСПОЛОЖЕН охотиться» ре-выводится из ЖИВЫХ `kind`/`faction`/`hullHP`/
позиций соседа — НЕ из его спломбированного за кадр `aiTarget`/`defending`/`aiState`. Поэтому когда обрабатывается патруль A
(и смотрит на B), а позже патруль B (и смотрит на A), КАЖДЫЙ судит другого по неизменной за кадр РАСПОЛОЖЕННОСТИ (стояние +
позиция + видит-игрока), а не по тому, успел ли другой уже закоммитить цель. Итог не зависит от порядка бортов в векторе и от
swap-pop удалений: оба флипаются на игрока в ОДНОМ кадре, без рекурсии и без взаимной блокировки «жду, пока сосед решит».
Нужна ПАРА: одинокий враждебный патруль с более близким пиратом по-прежнему гонит пирата (§5.13.42 не сработал, соседа нет ⇒
пак-тир пропущен) — это контроль пробы.

**Правила.** Data-oriented (§2.1 — правка в свободной функции, C-поля, ноль OOP), C++11/SDL2 (§2.2), ноль RNG и ноль касания
глобального `rng` (§2.3 — читаются лишь детерминированный `factionRelation` + позиции), не CHROMOCORE-ребейк (§2.4). Ноль
новых полей (реюз `atkBest`/`atkPlayer`/`atkCraft`/`c.defending` + константа `PATROL_DISTRESS_R`). Комбат-инвариант §0.2-G
УСИЛЕН: тир лишь ПЕРЕКЛЮЧАЕТ патруль с пирата на игрока (никогда наоборот) ⇒ у пирата аптайма БОЛЬШЕ, у игрока боя БОЛЬШЕ;
строго аддитивно (нет пары / реп > −48 / игрок вне `AW2` ⇒ прежнее поведение §5.13.42). Эмерджентный бонус: cue «HUNTING YOU»
(§5.13.43) + жар вендетты (§5.13.45) авто-визуализируют КАЖДОГО члена стаи — у всех теперь `aiTarget==LOCAL_TARGET_PLAYER`,
рендер получил стаю даром.

**Проверки (все зелёные).** `make` 0 предупреждений; `--smoke`/`--localsmoke` exit 0. `make soak` (ASan/UBSan): **ПОБИТОВО**
бейзлайн §5.13.44/.45 (`frames=120000 deaths=0 invariantFails=0 rocks=648 shiny=382`, все прежние пробники `ok`) — ветка
`CK_PATROL` в 120k-цикле не бежит (18 воен-гарнизонов disjoint маршруту soak ⇒ `patrols=0`) ⇒ тир no-op по построению. Новый
изолир-пробник `lawPackHuntsPlayer` (свежий `Game`, синтетические борты, позиции пиньнуты каждый кадр): **(A)** два Enemy-
патруля в 300 LU от игрока и 600 LU друг от друга, у каждого свой `wanted`-пират в 80 LU (ближе игрока ⇒ §5.13.42 сам игрока
не берёт) ⇒ пак-тир флипает ОБОИХ ⇒ `aiTarget==LOCAL_TARGET_PLAYER` у обоих (`packOverridesGate=YES`); **(B, контроль)**
одиночка + его ближний `wanted`-пират ⇒ гонит ПИРАТА, НИКОГДА игрока (`loneChasesPirate=YES`) ⇒ пиньнит «нужна ПАРА».
`make shot_asan`/`make shots` **36/36** — БЕЗ новых кадров: SIM-срез, `saveShot` сим не шагает, `localdraw` не тронут (как .34/.44).

**Следующий срез (§5.13.47, РЕНДЕР).** По чередованию — render-слайс: визуальный спутник стаи §5.13.46 (кандидаты §4.3:
кокпит-счётчик «PACK ×N» членов облавы / связующие «нити стаи» между ними / общий вектор схождения). Фильтр §0.2-G обязателен.
Держи чередование и «no-op по замеру» до плейтеста.

---

#### 5.13.47 Автономный срез РЕНДЕРА — ВИДНАЯ СТАЯ ЗАКОНА: баннер «PACK HUNT X%d» + сходящиеся нити радара (render-спутник §5.13.46) (2026-07-29)

**Что и зачем.** Render-спутник §5.13.46 (чередование .46 sim → **.47 render**). §5.13.46 научил пару+ враждебных игроку патрулей
БРОСАТЬ пиратов и сходиться на игроке — но игрок узнавал об этом лишь по разрозненным per-craft cue «HUNTING YOU» над каждым
бортом (§5.13.43/.45): три подписи в разных углах экрана не читаются как ОДНА скоординированная угроза, а неспровоцированный
охотник вне 3D-кадра мог быть представлен на радаре лишь тускло-серым блипом (§5.13.27), неотличимо от мирного трафика. §5.13.47
даёт стае ДВА связных, дешёвых, чисто-рисовательных «голоса»: экранный баннер-тревогу и сходящиеся к игроку нити радара.

**Механика — две draw-only подсказки.** (1) **БАННЕР** (после блока тоста, сверху-центр `y=46` — под тостом `y=24`): один проход
по `scene.craft` считает `packN` охотников-по-стоянию общим файл-предикатом `huntsPlayerByStanding(c)` (`c.kind==CK_PATROL &&
c.aiState==1 && c.aiTarget==LOCAL_TARGET_PLAYER && !c.hostile && c.hullHP>0`) и запоминает `packCol` = тон самого ГОРЯЧЕГО
(`if (heat >= maxHeat)`, старт `maxHeat=0`/`packCol=P.red` ⇒ даже целиком «холодная» стая берёт реальный цвет первого борта, а
не фолбэк). При `packN >= 2` печатает `PACK HUNT X%d` цветом `packCol`, альфа `150 + heat·50 + pl·55` (макс ровно 255, `rgba`
клампит), пульс `sin(fxClock·(7.4 + maxHeat·3.7))` — ТОТ ЖЕ темп, что on-screen cue §5.13.45. (2) **НИТИ** (в блоке радара, ПОСЛЕ
трёх существующих связок — погоня §5.13.29 / набат §5.13.31 / подмога §5.13.33 — и ДО цикла блипов): проход по `scene.craft`, для
каждого `huntsPlayerByStanding` — `radarXY(c)` → `SDL_RenderDrawLine` к центру `(rcx,rcy)` тоном `vendettaHuntColor(game,c,heat)`,
альфа `55 + pl·130`, пульс с фазой `+ double(i)·0.7` (нити не бьют в унисон).

**Асимметрия гейтов N≥2 / N≥1 (осознанная).** Баннер — только при `N>=2`: это и есть ОПРЕДЕЛЕНИЕ «стаи» из §5.13.46 (одиночка
пак не сзывает), а её уже озвучивает per-craft «HUNTING YOU» §5.13.43 ⇒ баннер над одиночкой был бы дублем-шумом. Нити — при
`N>=1`: каждая нить ЧЕСТНА (этот борт реально охотник) И чинит слепое пятно радара — неспровоцированный охотник несёт
`!hostile`, а блип-лестница §5.13.27 красит такой контакт (`!wanted && !underAttack && !defending && !hostile`) тускло-серым,
неотличимо от мирного трафика; цветная нить к центру его выдаёт. Негативы БЕСПЛАТНЫ: N=1 (`shot_hunted_standing` #28) и N=0
(`shot_hunted_none` #29) уже в наборе — при них баннер молчит, при N=0 нет и нити.

**Приём D + анти-дрейф.** Обе подсказки НЕ читают никакого нового поля/кадрового флага — ре-выводят «кто охотник» из ЖИВЫХ
`kind/aiState/aiTarget/hostile/hullHP` тем же файл-предикатом `huntsPlayerByStanding`, что и per-craft cue §5.13.43, и красят тем
же `vendettaHuntColor`, что on-screen cue §5.13.45. Значит баннер, нити, надпись над бортом и (для нитей) язык радара НЕ МОГУТ
разъехаться в том, КОГО считать охотником и каким тоном красить — единственный источник истины. `radarXY` для `dd>RANGE` даёт
`kk=R/dd` ⇒ результирующий радиус `kk·dd = R` ⇒ точка садится РОВНО на обод радара ⇒ нить не улетает за панель (та же математика,
что у блипов и трёх прежних связок).

**Правила.** Data-oriented (§2.1 — правки в теле `renderLocalScene`, C-поля, ноль OOP), C++11/SDL2 (§2.2), ноль RNG и ноль
касания глобального `rng` (§2.3 — читаются лишь `factionRelation` + позиции + `fxClock`), не CHROMOCORE-ребейк (§2.4), HUD-текст
через 5×7-шрифт (§2.5, автокапс `PACK HUNT X%d`). Ноль новых полей. Комбат-инвариант §0.2-G ТРИВИАЛЕН: чистый рендер, симуляция
не тронута ⇒ урон/агрессия/время-в-бою пирата измениться физически не могут.

**Проверки (все зелёные).** `make` 0 предупреждений; `--smoke`/`--localsmoke` exit 0. `make soak` (ASan/UBSan): **ПОБИТОВО** тот
же бейзлайн (`frames=120000 deaths=0 invariantFails=0 rocks=648 shiny=382`, `lawPackHuntsPlayer=ok` + все прежние пробники `ok`) —
`renderLocalScene` в headless не зовётся ⇒ draw-срез физически не двигает числа. `make shots`/`make shot_asan` **37/37** (все
`rc=0`; ASan/UBSan без единой диагностики). Новый `shot_pack_hunt` (три охотника: два фракции 0 при реп −128 ⇒ heat 1.0 ⇒
бело-калёные, третий фракции 1 при −70 ⇒ heat ≈0.28 ⇒ теплее, но не белый; все <1400 LU ⇒ на радаре) прочитан ГЛАЗАМИ: баннер
`PACK HUNT X3` сверху-центр, три нити сходятся к центру радара, три «HUNTING YOU» разных тонов, панель `STANDING ENEMY −128`.
Контроль-негатив `shot_hunted_standing` (N=1) перепроверен глазами: баннера НЕТ, одна нить к центру. Дифф
(localdraw.cpp+shot_test.cpp) прогнан адверсарным ревью — **SHIP, без функциональных дефектов** по всем 8 областям
(границы/OOB, арифметика альфы [баннер ∈ [150,255], нить ∈ [55,185], `rgba` клампит], тай-брейк баннера всегда
самосогласован [первый охотник перебивает старт-`P.red` до того, как `packN` дойдёт до 2], асимметрия гейтов N≥2/N≥1,
анти-дрейф [нить и cue фазозаперты одним `i`], честность+гигиена шота [реп-дефолт 0 подтверждён по game.cpp], детерминизм
[ноль `rng`], точность комментов); две НЕ-проблемы (не дефекты): мёртвый-но-безопасный guard `if(a>255)` и намеренное
отсутствие per-craft фазы у баннера-агрегата.

**Следующий срез (§5.13.48, СИМ).** По чередованию — sim-слайс. См. форвард-бэклог §4.3 (кандидаты: реп-«остывание» вражды со
временем ЛИБО рост волны патрулей по коп-киллам; фильтр §0.2-G обязателен). Держи чередование и «no-op по замеру» до плейтеста.

---

## 6. Методология проверок (применяй ко всему новому коду)

### 6.1 Параллельная сборка субагентами (проверенный паттерн)
Крупную фичу дели на **несколько .cpp, по одному на субагента**, а общие заголовки/контракты/
интеграцию (`*.h`, `main.cpp`, `Makefile`) держи за собой (оркестратор). Так три файла
локального режима (`localgen/localsim/localdraw`) писались параллельно без конфликтов.
Каждый субагент валидирует свой файл в изоляции:
```bash
c++ -std=c++11 -I/opt/homebrew/include/SDL2 -D_THREAD_SAFE -fsyntax-only <file>.cpp
```
`-fsyntax-only` делает полный семантический анализ (ловит несуществующие имена/поля), но НЕ
линковку. Реальная проверка связок — `make`.

### 6.2 Уровни проверки (все должны быть зелёными перед сдачей среза)
1. `make` — сборка без ошибок.
2. `--smoke` (макро) и `--localsmoke` (локал) в обычной сборке → exit 0.
3. ASan+UBSan сборка + оба smoke → exit 0, **0 ошибок** (§3.1). Ловит то, что exit-0 не ловит.
4. **`make shot_asan`** — скриншот-харнес под ASan/UBSan (обязателен при правках `localdraw.cpp`:
   покрывает попиксельные `renderStarPlasma`/`renderBodySphere`, sub-rect lock, композит колец) → 0 ошибок.
5. `make soak` — детерминированный soak-тест локального режима под ASan/UBSan → exit 0, 0 инвариант-фейлов (§6.5).

### 6.3 Урок этой сессии (реальный баг, найденный ASan)
Глиф `'B'` в таблице шрифта был **30 символов вместо 35** → `drawText` читал за границей
строкового литерала (`global-buffer-overflow`). Баг был **латентным и в макро-режиме** (обычная
сборка его не ловит — только ASan). Исправлено; таблица теперь единственная (в `render2d.cpp`),
все 48 глифов проверены на длину 35:
```bash
grep -oE 'return "[01]+";' render2d.cpp | grep -oE '[01]+' | \
  awk '{ if (length($0)!=35) print "BAD ("length($0)"): "$0 }'
```
**Вывод:** для чужого/нового кода всегда прогоняй ASan; не доверяй одному exit 0.

### 6.4 Дисциплина
- `<task-notification>`, `<system-reminder>`, «task tools haven't been used» — это **автоматические
  события, НЕ ввод пользователя**. Не считай их подтверждением/согласием.
- Убирай за собой временные артефакты (`game_asan`, `soak_asan`, `*.dSYM`) — `make clean` их сносит.

### 6.5 Soak-тест локального режима (`make soak`, файл `soak_test.cpp`)
Зачем: 12-кадровые headless-smoke почти не двигают время и всегда идут по CASE B генерации, поэтому
НЕ покрывают глубокий космос, многооборотные луны, радио-клейм/бой/смерть. Soak — отдельная `main()`
(линкуется с `LIBSOURCES`, без `main.cpp`), гоняет `updateLocalScene` по ~10 системам + глубокому
космосу тысячами кадров, ведёт игрока к радио/кораблям/телам, каждый кадр проверяет инварианты
(конечность координат, `fx<=FX_MAX`, отсутствие разрастания векторов) и отдельно детерминированно
проверяет ветку смерти игрока (враждебный пират вплотную, корпус=1). Последний прогон: 120k кадров,
0 фейлов, 0 санитайзер-ошибок. **Держи soak зелёным при правках локального режима.**

---

## 7. Дорожная карта / что дальше

### 7.1 Немедленно: фидбек по локальному режиму
Срез 1 + два прохода углубления (§5.10) в дереве, ждут плейтеста. Вопросы, по которым крутить дальше:
- **Масштаб/зум и скорости** — не слишком ли пусто/медленно даже с варпом? (крутилки — §5.4).
- **Читаемость HUD/радара/частиц** — панели, бары, off-screen маркеры, радар, тряска, размеры шрифта.
- **Ощущение боя и добычи** — урон/награды/щиты/скорость добычи/дальности/лут.
- **Баланс смерти** — штраф аварийного прыжка (30% корпуса, −20% денег) — не слишком мягко/жёстко?
- **Радио/детекторы** — интересны ли награды, верны ли пороги детектора (`sensors` 1.08 / 1.20)?

Когда придёт фидбек — итерируй по нему (это и есть «дальше» в цикле).

#### 7.1.1 Плейтест №1 — 3D-режим (фидбек пользователя, 2026-07-28)
Первый живой плейтест после перехода в полный 3D (§5.13). Дословно: «уже лучше 3д, но стрелочка
жёлтая игрока как-то странно выглядит, надо как бы вид из "глаз" камеры корабля; также меня убил
пират сразу, но вот это плюс — сражение реально ощущается сочно».

- ✅ **3D подтверждён как улучшение** — направление верное, продолжаем.
- ✅ **Бой ощущается «сочно» (juice работает).** Пират убил игрока сразу — пользователь считает это
  **плюсом** (интенсивность/напряжение). **НЕ ослаблять** урон/агрессию ИИ ради «мягкости»: juice
  из прохода 1 (§5.10) валидирован реальным плейтестом. Баланс сложности трогать только если
  дальнейший фидбек скажет «слишком карательно» — по умолчанию оставить как есть.
- ✅ **СДЕЛАНО (срез 2, 2026-07-28): вид из «глаз» корабля (кокпит / от первого лица).**
  Жёлтая стрелка игрока в полёте убрана; камера-преследователь заменена на кокпит. Как сделано
  (совпадает с планом ниже, всё выполнено):
  - Глаз камеры **в позиции корабля** (`buildLocalCamera`: `center = {px,py,pz}`, `eye=ship`),
    взгляд вдоль `pfwd`, верх `pup`; базис `makeCameraFrameBasis(pfwd,pup)`.
  - Собственная стрелка игрока **не рисуется** в перспективе (`localdraw.cpp`, ветка `else` секции
    ИГРОК). Вместо неё центральная **мушка-прицел** (кольцо + перекрестие в проекции `player+pfwd·600`,
    фолбэк в центр экрана если позади) — естественный центр по курсу стрельбы.
  - Индикатор щита игрока — **дуга-оверлей** вокруг мушки; добавлен **маркер прогрейда** (вектор
    скорости, зелёный, `player+velocity·(600/speed)` если `speed>0.5`).
  - **Оптический зум/FOV** на `+/−` (`localFovScale` 0.40..1.60, `focal=winW·localFovScale`) заменил
    прежний долли chase-камеры.
  - **Стрелка игрока сохранена в карте сверху** (ветка `!view.perspective`) — там она читаема как
    тактический курсор; переключатель `C` оставлен.
  - **Потолки размера частиц** по типу (TRAIL/SMOKE 8, DEBRIS 22, SPARK 40, MUZZLE 90, RING 300) +
    near-cull — иначе близкий выхлоп (глаз теперь в корабле) раздувался `R·focal/depth` на весь экран.
  - Проверки пройдены — полная лестница §6.2 + soak 120k (0 фейлов) + `make shots` (кадры просмотрены).
    Закоммичено (`58e7136`), затем ориентир эклиптики (`ef7609a`).
- ✅ **СДЕЛАНО (2026-07-28): читаемость боя — лид-прицел + панель цели (щит/сближение).**
  Проактивный полиш под похвалу «сражение реально ощущаются сочно» — **строго аддитивно, врага НЕ
  ослабляли** (стоячий инвариант «combat feels juicy — DO NOT nerf»). Как сделано (`localdraw.cpp`):
  - **Лид-пип (упреждение)** в кокпите для `scene.targetCraft`: снаряд игрока несёт скорость корабля
    (`p.vx = pvx + dir·PROJ_SPEED`, localsim), поэтому перехват решается в относительной скорости
    `u = targetVel − playerVel`: `(u·u−S²)t² + 2(r·u)t + r·r = 0`, `S=PROJ_SPEED=520`, берём наименьший
    `t>0`. Направление прицела ∝ `r+u·t`; пип на радиусе 600 LU (как мушка). Янтарный (нейтрал) /
    красно-янтарный (враг). Гейт: дистанция `<1500 LU`. При стоячей цели по курсу пип совпадает с мушкой.
  - **Панель цели** (сверху-справа): добавлены **полоса щита** (циан, над корпусом, если `maxShield>0`;
    панель растёт до 72 px) и **скорость сближения** `CLS %+.0F` (проекция `−(r·u)/dist` на луч; `+` =
    сближаемся → янтарь, `−` → серый). Хелперы `panel/bar/drawText/textWidth`, шрифт 5×7 (UPPERCASE, `%.0F`).
  - Харнес: 6-й кадр **`shot_combat_lead`** — цель наперерез (vy=220), нос на текущую позицию цели,
    статичный детерминированный рендер (сим не шагаем): янтарный пип заметно смещён от мушки.
  - Проверки: полная лестница §6.2 + **харнес под ASan/UBSan** (гоняет новый draw с активной целью,
    0 ошибок) + soak 120k (invariantFails=0) + `make shots` 6/6 (кадры просмотрены).

#### 7.1.2 Плейтест №2 — масштаб, окклюзия, обзор (фидбек пользователя, 2026-07-28)
Второй живой плейтест. Дословно: «движение должно ловить курсор (в локальном мире у нас типа как
шутер); сквозь звезду просвечивают планеты; про масштабы — сделай умно, звезда должна быть гигантской,
чтоб в неё влезли тысячи планет, а корабли — ничто в сравнении; тебе не надо держать в памяти никаких
моделек, всё абсолютно контекстно и геометрически просто — есть поле, наш 3D-мир, и там где рядом
звезда — там просто заполняется её веществом пространство, тупо сфера, у нас чистая геометрия, так что
аналитически через сферы мы можем локально показывать гигантские структуры (мы рисуем не звезду — мы
знаем координаты и радиус); при стрельбе какой-то синий квадрат — это точно баг».

Все 5 пунктов **✅ СДЕЛАНЫ** (реализация и «как сделано» — **§5.13.1**):
- ✅ **Мышелук (шутерный обзор).** `SDL_SetRelativeMouseMode` в кокпите; дельты мыши → yaw/pitch,
  вложены в поворот Родрига. ЛКМ стреляет. §5.13.1 п.4.
- ✅ **Планеты не просвечивают сквозь звезду.** Аналитическая окклюзия луч↔сфера для всех объектов.
  §5.13.1 п.2.
- ✅ **Гигантская звезда «умно», через геометрию.** Принято дословно: звезда = центр `(0,0,0)` +
  `starRadius`, рисуется аналитически (касательная к сфере → экранный радиус; изнутри — заливка
  плазмой). Никаких мешей/моделей. §5.13.1 п.1.
- ✅ **Масштаб: тысячи планет в звезду, корабли — ничто.** `starRadius 90..135`, планеты единицы LU,
  корабли-крапинки. §5.13.1 п.3.
- ✅ **Синий квадрат при стрельбе.** Был муззл в самом глазу, раздутый перспективой; спавн смещён +
  аддитивный bloom + кап размера. §5.13.1 п.5.

Открытых пунктов по плейтесту №2 нет.

#### 7.1.3 Плейтест №3 — масштаб звезды (фидбек пользователя, 2026-07-28)
Третий живой плейтест после мышелука + аналитической сферы (§5.13.1). Дословно: «да мышелку
работает, но масштаб всё равно звезда маленькая, либо я не пон… ну понял мою идею? [скриншоты]
уже лучше». Мышелук подтверждён; масштаб всё ещё не тот.

✅ **СДЕЛАНО (реализация — §5.13.2):** диагноз — вид звезды зависит только от `D/R`, а орбиты
уходили к R·13. Орбиты переписаны как доли радиуса (внешняя ≤ R·3.4), звезда ×2.9 (260..360 LU),
старт D/R≈2.8 (~82% высоты). Звезда теперь доминирует кадр на всём пути от старта к внешней планете
(визуально подтверждено на шотах). Заодно: фикс «спиц» эклиптики + бесшовный порог заливки R·1.7.

Открытых пунктов по плейтесту №3 нет. Масштаб принят; плейтест №4 вскрыл отдельную проблему —
рендер самой звезды (см. §7.1.4).

#### 7.1.4 Плейтест №4 — звезда-плазма, изотропия, «бедный рендер» (фидбек пользователя, 2026-07-28)
Четвёртый живой плейтест (масштаб §5.13.2 одобрен: «по орбите вращаются круто»). Дословно: «баг
костыль и говно — если я подлетаю к звезде, как будто переключается режим и всё становится жёлтым,
причём со всех сторон, это точно баг. Я дал тебе задачу математическую изотропную — звёзды и планеты
это просто точки в пространстве, у них 2 параметра: радиус и положение (орбитируют — сделано хорошо).
Но звезда сделана неправильно, ты что-то нахардкодил, а надо было заполнять пространство как функция
от радиуса всех тел в системе, без костылей, с физичными масштабами. Рендер вообще бедный — используй
шейдеры, чтобы звезда прям была шаром плазмы».

✅ **СДЕЛАНО (реализация — §5.13.3):** баг подтверждён — `if (D≤R*1.7) fillRect(весь экран)` заливал
кадр плазмой независимо от направления взгляда («жёлтый со всех сторон»). Костыль-заливка И биллборд-
диск удалены, заменены **попиксельным аналитическим ray-sphere «шейдером»** (`renderStarPlasma`):
изотропная сфера (центр+радиус), плазма как функция геометрии луча (лимб-даркенинг + многооктавная
турбулентность + корона), рендер в стриминг-текстуру (SDL2-only). Никаких порогов/режимов. Решающая
проверка `shot_star_lookaway`: вблизи, глядя ПРОЧЬ от звезды — чёрный космос, не плазма (изотропно).
Орбиты не тронуты. Ждём живого плейтеста №5 (ощущение плазма-шара на реальном полёте, богатство fx).

### 7.2 Радиосигналы + детекторы — СДЕЛАНО (проход 2, §5.10)
Реализовано в локальном режиме: `LocalRadioSource` в глубоком космосе и системах, детектор из
`game.tech.sensors` (3 тира), reveal по дальности с бонусом тира, клейм = деньги + исследование +
элемент, HUD-радар. Дальнейшее углубление (нейтринный / тёмной материи как отдельные модули
прогрессии, связка с `anomaly.cpp` / plans_10, редкие эндгейм-источники) — открытый простор, если
пользователь захочет.

### 7.3 Кандидаты на усиление локального режима (идеи, не обязательства)
Сделано в проходах 1–2 (§5.10): живой урон/смерть игрока + аварийный прыжок, двигающиеся/вращающиеся
астероиды, реакция фракций на киллы, ИИ с отступлением на низком корпусе, juice (следы/вспышки/тряска).
Ещё открыто:
- Продолжение макро-симуляции в фоне (или «прошло N времени» по выходу из локала).
- Больше жизни в системе: стыковка NPC, внутрисистемные торговые маршруты, группы/эскорты.
- Глубже ИИ: звенья / wingman-тактика, скоординированные манёвры, отклонение от препятствий.
- Экономика лута/радио: редкие модули-дропы, цепочки квестов-сигналов, привязка к контрактам.
- ✅ **Полный 3D-обзор + LOD-скайбокс кластера — СДЕЛАНО (§5.13).** Перспективная камера от корабля,
  3 оси (+roll) на тело-относительном базисе, HUD/радар под вращение, фон из реальных `ClusterStar` по
  LOD. §2.7 решён (перспектива только в локале). Дальнейшее (полировка ощущения/баланса) — по плейтесту.

### 7.3.1 Двигательная модель — что дальше (§12)

Приоритеты по убыванию ценности, все обсуждены с пользователем:

1. **Оптимизация экономики.** Симуляция стоит 160 мс на игровой год; на скорости x10 нужно 160%
   секунды, цикл упирается в потолок подшагов и отстаёт. Горячее: `econQualityPow`,
   `computeSubstitution`, `Market::update`, ГСЧ (§12.6). Маршруты и двигатели уже вычищены.
2. **Потратить посчитанные траиты** (§12.8): `handlingRisk` → радиация, износ корпуса,
   контрабандный статус; `nuclearStability` → распад запаса со временем.
3. **Три новые величины из тех же полей** (§12.8): теплоёмкость по Дюлонгу-Пти как настоящий
   потолок камеры; температура плавления как ветка апгрейдов сопла; сечение захвата нейтронов
   как третий расходник.
4. **Честная лестница скоростей** (§12.3) — «деление внутрисистемно → термояд 0.1c →
   антивещество 0.5c». Физически безупречно, но меняет премису: стартовый корабль перестаёт
   летать между звёздами. ОТДЕЛЬНАЯ ВЕТКА, не правка константы.
5. **Аварийный сценарий для сухого бака.** Корабль без топлива, нажавший STOP, дрейфует вечно —
   тормозить нечем. Формально честная физика, но игрока запирает. Сигнал бедствия, буксировка
   за долю груза — механика, не баг.
6. **Стоимость времени в OPTIMAL.** Сейчас оптимизатор считает только кредиты за вещество и
   потому всегда советует ползти на 20% крейсера. Добавить цену года — и размен оживёт.

### 7.4 Незакрытый контент-бэклог (см. `plans_*.md` и `design.md`)
Контентные системы plans_1..10 реализованы, но всегда есть простор углублять экономическую спираль
из `design.md` (медитативный гринд, капитал → возможности → больше капитала). При заходе в любую
систему сначала перечитай её `plans_N.md`.

---

## 8. Как расширять этот документ

Этот файл — точка входа для будущих агентов. Держи его актуальным:
- Завершил фичу/срез → обнови §5/§7 (что сделано, что дальше), добавь новые файлы в §4.
- Нашёл новый инвариант/грабли → добавь в §2 (ограничения) или §6.3 (уроки).
- Изменил тюнинг/крутилки → синхронизируй §5.4 с `local.h`.
- Новая крупная механика → отдельный раздел по образцу §5 (идея → решения пользователя →
  данные → контракты → интеграция → управление → упрощения) + строка в §4.
- Не дублируй код здесь — ссылайся на файлы и функции. Документ описывает «почему/где», код — «как».

### Инварианты статуса на момент написания (2026-07-28)
> ⚠️ ЗАМОРОЖЕННЫЙ снимок на дату написания (доведён лишь до §5.13.2). **Актуальный статус — §0
> TL;DR и §9 changelog (доведены до §5.13.30), плюс плейбук §0.2.** Ниже — исторический контекст.
- Ветка `local-flight-mode` (ответвлена от `main` по конвенции «на `main` — сперва ветка»).
  Сборка зелёная. Оба smoke + `make soak` (обычные и ASan/UBSan) — exit 0, 0 ошибок.
- Локальный режим полёта: **срез 1 + два прохода углубления (§5.10) + полный 3D (§5.13) + вид из
  кокпита (срез 2, §7.1.1) + ориентир эклиптики + читаемость боя (лид-пип/панель цели) готовы и
  провалидированы**. Покрытие: soak 120k кадров (радио-клеймы, киллы, луны, смерть + аварийный прыжок) —
  0 инвариант-фейлов; `make shots` — 6/6 кадров (вкл. `shot_combat_lead`).
  **Плейтест №1 пройден (§7.1.1):** 3D ок, бой сочный; пункт «вид из кокпита» **закрыт** в срезе 2.
- **Единая панель действий (§5.11) — свежий срез, ждёт плейтеста.** Правит `main.cpp`/`local.h`/
  `localsim.cpp`/`localdraw.cpp`/`ui.cpp`; все уровни проверки (§6.2) зелёные.
- **§5.13 ✅ РЕАЛИЗОВАНО (не только зафиксировано):** полный 3D по 3 осям (Elite, +roll,
  тело-относительный базис) + **перспективная камера из кокпита (от первого лица, срез 2)** +
  переключаемый вид-карта (клавиша `C`) + LOD-скайбокс всего кластера. Камера — единым
  `buildLocalCamera` (игра и харнес `make shots`). Архивопрос §2.7 решён — **перспектива только в
  локальном режиме** (выбор пользователя «Перспектива (Elite)»). Все уровни проверки (§6.2) + soak
  120k + `make shots` — зелёные. **Закоммичено** (`58e7136` 3D+кокпит, `ef7609a` горизонт,
  `87230d5` лид-пип).
- **§5.13.1 ✅ РЕАЛИЗОВАНО (проход углубления №3, плейтест №2):** аналитическая звезда-сфера +
  окклюзия луч↔сфера + гигантский масштаб + мышелук + фикс синего квадрата. Все проверки + soak 120k
  + 10 шотов зелёные. Правки: `localdraw.cpp`/`localgen.cpp`/`localsim.cpp`/`local.h`/`main.cpp`/
  `shot_test.cpp` + этот документ (коммитятся по стоячей директиве «комит пуш и работай дальше»).
- **§5.13.2 ✅ РЕАЛИЗОВАНО (проход углубления №4, плейтест №3):** орбиты переписаны как доли радиуса
  (вид звезды зависит только от D/R; внешняя орбита ≤ R·3.4), звезда ×2.9 (260..360 LU), старт D/R≈2.8
  → звезда доминирует кадр по всей системе; + фикс «спиц» эклиптики и бесшовный порог заливки R·1.7.
  Правки: `localgen.cpp`/`localdraw.cpp`/`shot_test.cpp` + этот документ. Все проверки + soak 120k +
  10 шотов зелёные.
- Весь код и документы (локальный слой + углубление + 3D-переход + `soak_test.cpp` + цели Makefile
  `asan`/`soak` + ранее незакоммиченные модули кора + `plans_*.md`/`design.md`/`master_prompt.md`)
  в ветке `local-flight-mode`. Мердж в `main` — по решению пользователя.
- Бинарные артефакты (`/game`, `game_asan`, `soak_asan`, `*.dSYM`, `SDL2-*.dmg`, `test_app/`) — в
  `.gitignore` (сборка идёт через системный SDL2 по `sdl2-config`), в репозиторий не кладём.

---

## 9. Журнал прогресса (changelog)

Хронология крупных вех (свежие сверху). Детали — в ссылающихся разделах; здесь только «что и когда».

- **2026-08-05** — 🚀 **Двигательная модель: топливо и рабочее тело как смеси элементов** (§12).
  Новые `drive.{h,cpp}`. Топливо перестало быть счётчиком: два разных вещества, энергия из кривой
  связи Вайцзеккера, релятивистский Циолковский со стеной достижимости, зола как товар, три
  семейства движков, окно `HOLD / TANKS` с двумя ручками и автоподбором под цель, лестница
  скоростей корпусов (0.121c → 0.488c), выбор цели отделён от вылета (`DESTINATION` / `GO`),
  перегруз трюма разрешён, локальный полёт бесплатен. Симуляция на 40% дешевле. Сейвы — версия 12,
  старые отвергаются. balance 12 → 32 проверки, uiclick 18 → 27.
- **2026-08-04** — 🎬 **Оболочка игры: заставка, меню, комикс, асинхронная генерация** (§11).
  Новые `shell.{h,cpp}`. До симуляции: клеточное поле собирает **TENEVIK GAMES** (разбегание
  симулируется мгновенно и пишется в ленту — игрок сразу видит рассыпанное поле; лента
  отматывается назад; курсор физически толкает клетки тем сильнее, чем быстрее ведёшь мышь,
  ЛКМ — щелчок-взрыв; сбитые сходят с ленты и упрямо возвращаются сами) → **главное меню** (те же клетки
  пересобираются в заголовок STARCLUSTER) → **комикс-пролог** из 5 кадров, листается кликом →
  экран генерации. `Game::init` (9 с) ушёл в фоновый `std::thread`. Легенда хоткеев из правого
  нижнего угла заменена на центрированную карточку `UI::drawControlsCard` (F1 и финальный экран
  оболочки). Партия стартует **на паузе**, стартовый капитал **100 Cr** вместо 3600 (замер: квота
  первого периода закрывается за 5–6 рейсов на 5 сидах). Новый headless-прогон **`--shellsmoke`**
  (оболочка сама себе жмёт «дальше») — зелёный под ASan/UBSan. Заодно починена регрессия
  музыки: зерно выбора трека было константой, поэтому каждый запуск играл одну и ту же
  последовательность — теперь это мешок-перестановка на живом зерне (§11.2.1).

- **2026-08-04** — 🧪 **Регрессия баланса + ловушка первых корпусов** (§10.12, §10.13). Экономику не
  покрывала ни одна проверка, хотя ВСЕ крупные баги сессии были именно в ней. Восемь одноразовых
  стендов превращены в постоянные: новая цель **`make balance`** (9 проверок — проскальзывание
  существует, рейс не печатает капитал, сводка не врёт, у старта есть чем торговать, трюм и разведка
  окупаются, лестница цен без дыр, стартовый корпус числится классом, квота проходима). Пороги
  широкие: ловим поломку механизма, а не замораживаем баланс. **Ловушка корпусов:** стартовый
  корабль звался `"Player"` и в `shipClasses()` не числился ⇒ зачёт старого корпуса не срабатывал на
  первой покупке, а первые два покупаемых корпуса были по трюму МЕНЬШЕ стартового (50 и 100 против
  110) — восемь рейсов накоплений ради даунгрейда. Добавлен класс `Hauler` (110/4000/10000 Cr),
  лестница стала непрерывной (худший скачок x1.9). Запрет невыгодных покупок НЕ вводился (торговля
  свободна, решает игрок) — верфь показывает `VS YOURS CARGO ±N`; убран мёртвый `+1000000` из
  подписи кнопки. **Два моих ВЕРНЫХ вывода оказались неверны и записаны как урок** (§10.12): разрыва
  прогрессии 1000→1 000 000 нет, а «насыщение дохода на трюме 300» было артефактом замера по одной
  сделке — на смешанном грузе трюм 4000 даёт 29139 против 3023, и разведка даёт ещё вдвое.
  Лестница: `make` 0-warn, оба smoke, `uiclick` PASS, `balance` PASS 9/9, **soak 120k invariantFails=0**.
  Правки: `balance_test.cpp` (новый), `ship.cpp`, `game.cpp`, `ui.cpp`, `Makefile`, доки.

- **2026-08-04** — 🧾 **Биржа: полная карта под фильтром + переименование в BROKERAGE** (§10.11).
  По фидбеку: под фильтром по элементу сняты ОБА отсечения (ранний отсев «там не дороже» и порог
  top-N) — теперь виден весь разведанный рынок по элементу, включая убыточные направления
  (красные строки); на 61 разведанном рынке фильтр даёт 60 строк, недостающая — система стоянки,
  которая в список не попадает по построению (сделке нужны два разных рынка), её цена вынесена в
  шапку как точка отсчёта. Окно переименовано `EXCHANGE` → **BROKERAGE**: биржи в привычном смысле
  здесь нет, а термин уже введён тутором (`HIGH-FREQUENCY BROKERAGE`); заодно исправлена фраза
  Тимертии `recent stockings` (по-английски — чулки). Лестница: `make` 0-warn, оба smoke,
  `uiclick` PASS, **soak 120k invariantFails=0**. Правки: `game.cpp`, `ui.cpp`, `main.cpp`, доки.

- **2026-08-04** — 🔎 **Биржа: фильтр по элементу + биржа следует за игроком** (§10.11). В окно влезает
  ~20 строк при 118 элементах, поэтому «покажи всё, что я знаю про железо» через общий список было
  недостижимо. Кнопка `FILTER BY ELEMENT` разворачивает таблицу Менделеева (переиспользованы
  `periodicCell`/`marketCellColor` из окна торговли — привычная раскраска дефицита/избытка), клик по
  клетке оставляет ТОЛЬКО этот элемент по ВСЕМ разведанным системам; потолок списка под фильтром
  поднят 200→2000. **Починен баг:** `window.star` фиксировался при открытии, и после перелёта биржа
  показывала прежнюю систему (котировки чужие, список пропадал). Сам снимок системы стоянки
  обновлялся исправно каждый тик — ломалось окно; теперь `updateExchangeBoard` переводит его на
  систему стоянки. Лестница: `make` 0-warn, оба smoke, `uiclick` PASS, **soak 120k invariantFails=0**.
  Правки: `game.cpp/h`, `ui.cpp/h`, `master_prompt.md`, `README.md`.

- **2026-08-04** — 📈 **Биржа: бесконечный список + отвергнутая модель цены** (§10.11). Радиус поиска
  снят — сводка охватывает ВСЁ разведанное (до 10 000 систем) и растёт с картой игрока; список
  листается колесом (новый `UI::handleMouseWheel`) и кнопками, в заголовке `n-m OF total`.
  Наивный пересчёт при полной разведке давал 390 370 строк за **205 мс**; исправлены две вещи —
  объём закупки зависит только от элемента, а считался внутри цикла по системам (вынесен в
  предрасчёт), и введён ограниченный top-200 с отсечением по оптимистичной оценке сверху ⇒
  **20.6 мс**. Сверх того сводка кэшируется (`WindowState::exchangeBoard`, пересчёт при смене
  системы или раз в 2 года). Повторное посещение обновляет базу через существующий `observeStar` —
  отдельного механизма не потребовалось. **Экстраполяция цены измерена и ОТВЕРГНУТА:** «сползание к
  опорной цене скопления» оказалось хуже снимка (на 20 годах −31% точности), потому что цены систем
  устойчивы и возврат к средней выбрасывает информацию; реализация откачена, урок и стенд записаны в
  §10.11, чтобы заход не повторяли. Лестница: `make` 0-warn, оба smoke, `uiclick` PASS,
  **soak 120k invariantFails=0**. Правки: `game.cpp/h`, `ui.cpp/h`, `main.cpp`, `master_prompt.md`.

- **2026-08-04** — 🔧 **Правки по плейтесту биржи** (§10.9, §10.10). (1) **Откачен посев стартовой
  разведки**: попытка наполнить сводку, выдав фракции игрока `homeStar`, убивала главную петлю —
  карта цен должна добываться ногами. `homeStar` фракции игрока НАМЕРЕННО −1; проверено, что петля
  работает (старт: 1 рынок / 0 строк → после облёта 6 соседей: 7 рынков / 40 строк).
  (2) **Кнопка EXCHANGE налезала на текст**: стояла вторым этажом над рядом действий. Перенесена в
  общий ряд, окно системы расширено 440→550 (ряд доходит до 535 px), окно биржи 560→680.
  (3) **Лицензия стоила 5000** — три рейса — и обесценивала ветку роста флота. `LICENCE_PRICE_K`
  5→1000 ⇒ вторая лицензия стоит **миллион**: смена масштаба игры, а не апгрейд.
  (4) **В шрифте не было семи глифов**, которые UI печатает: `|`×30, `(`/`)`×9, `#`, `@`, `=`, `~`
  (на скриншоте это выглядело как `TRADE: BUY ?B?`). Добавлены генератором с проверкой длины 35;
  длинные тире в строках событий заменены на дефисы. Лестница: `make` 0-warn, оба smoke,
  `uiclick` PASS, **soak 120k invariantFails=0** (числа как в бейзлайне).
  Правки: `game.cpp`, `game.h`, `ui.cpp`, `render2d.cpp`, `main.cpp`, `master_prompt.md`, `README.md`.

- **2026-08-04** — ✅ **Биржа: сводка маршрутов + лицензии** (§10.9). Кросс-системного вида на рынок
  не было вообще (`TRADE` — терминал одной системы), а система знаний фракций работала невидимо.
  Новое окно `WindowKind::Exchange` (клавиша **E**, кнопка `EXCHANGE`) показывает сделки «купить
  здесь — продать там» ТОЛЬКО по разведанным рынкам, с возрастом данных и доверием; строки красятся
  по доверию. Три вещи потребовали замера, а не догадки: (1) сводка была ПУСТА на старте — у фракции
  игрока `homeStar` оставался −1, посев знаний её пропускал, игрок знал одну систему при 298 соседях;
  (2) сводка ВРАЛА в разы — обещала +7869 там, где реальность давала +285, потому что решает ЁМКОСТЬ
  рынка назначения (замер: 0.37 единицы в год против 242 у источника, разница в 650 раз), и прокси по
  здешней глубине смещён ровно в худшую сторону; (3) «полный трюм» перестал быть оптимумом — прибыль
  по объёму имеет максимум, сводка перебирает 10 объёмов и подсказывает СКОЛЬКО везти. После правок
  проверка честности (взять верхнюю сделку и реально исполнить): свежая разведка — ошибка −19…+8%,
  разведка 40-летней давности — −50…+25%, то есть `confidence` стало означать измеримую величину.
  Лицензии в том же окне: `+LICENCE` (цена растёт с числом лицензий и поднимает квоту) и
  `SETTLE QUOTA` (закрыть остаток кредитами по курсу 1.5 — без наценки богатый игрок просто
  откупался бы и торговля стала бы не нужна). **Покупка корабля теперь требует свободной лицензии** —
  вместо заградительной константы `price + 1000000`, которая просто отключала механику. Побочно:
  `Market::executionPrice` разложен на переиспользуемый `marketExecutionFactor(units, depth, selling)`.
  Лестница: `make` 0-warn, оба smoke, `uiclick` PASS, `econ` ok, **soak 120k invariantFails=0**
  (числа как в бейзлайне — регресса нет). Добавлен `PROMPT_frontend_shell.md` — промт для отдельного
  чата на оболочку игры (загрузка/меню/заставки) с зафиксированными границами правок.
  Правки: `game.cpp/h`, `ui.cpp/h`, `market.cpp/h`, `main.cpp`, `master_prompt.md`, `README.md`.

- **2026-08-04** — 🚨 **ПРЕД-РЕЛИЗНЫЙ АУДИТ: здоровье проекта, экономика, геймлуп** (§10).
  Цикл автономных срезов §5.13 остановлен ради подготовки к релизу. Найдено и починено четыре
  проблемы в основаниях: (1) **релиз не содержал ассетов** — `build.yml` не клал `timertia.png`
  и музыку никуда, а пути были относительно CWD (у `.app` это `/`) ⇒ игра выходила немой и без
  портрета Тимертии в интро; введён `assetPath()` поверх `SDL_GetBasePath()` + упаковка в CI;
  (2) **сейв не работал в бандле** (CWD) ⇒ `SDL_GetPrefPath`; (3) **мир был недетерминирован** —
  `rng` засевался из `random_device`, а `Game::seed` лишь ЗАПИСЫВАЛ случайное число (`seed = rng()`),
  плюс выбор музыкального трека тянул из того же `rng`; один бинарь давал `deaths=5968` и `718`
  в двух прогонах soak ⇒ **все прежние отчёты «соак побитово» недействительны**; теперь
  `Game::init` делает `rng.seed(seed)`, `Cluster::generate` получает тот же seed (раньше там
  стояла константа 42 ⇒ карта звёзд не менялась между партиями), у аудио свой `musicRng`,
  добавлен `--seed N`; (4) **легенда управления 29 хоткеев была закомментирована** ⇒ игрок не мог
  узнать о клавише `L`, то есть **весь трек §5.13 был невидим для покупателя**; возвращена на `F1`.
  Включены `-Wall -Wextra` (их не было вовсе) и вычищены все 18 предупреждений: мёртвые функции,
  неприменявшиеся лимиты цен `PRICE_FLOOR_K`/`PRICE_CEIL_K`. Удалён случайный «System Access Fee»
  (1%-модалка со случайной суммой — наказание без решения игрока). Из git убрано ~60 МБ артефактов
  (19 скриптов `patch_*`/`fix_*`, `patch.cpp`, `test_ui.cpp`, `test_rng.cpp`, 7 zip, 7 dmg, 9 dll,
  6 папок сборок). Вычищены опечатки тутора. `advanceGame` получил потолок подшагов (был возможен
  ~0.7 с фриза на x10). **Экономика:** введено проскальзывание — `Market::executionPrice()` как
  интеграл уже существующей реакции `applyTrade` (ни одной новой константы); было исполнение всего
  объёма по цене ДО сделки ⇒ замер показывал **x44 капитала за рейс**, стало +21…+146%;
  «купить максимум» перестало быть стратегией. Добавлен `pickStarterSystem()` — гарантированный
  стартовый арбитраж (было 2 сида из 5 без единого прибыльного маршрута, стало 5/5); цены не
  подкручиваются, выбирается лишь точка входа. **Геймлуп:** новая механика — лицензионная квота
  оборота (тысячелетний период как релятивистская корректировка рынка, квота 10 000 Cr с ростом 1%
  за период, динамическая ставка от `marketClusterLevel()`, провал = заморозка торговли и выкуп
  по F2, строка в PLAYER STATUS); формат сейва поднят до **v8**. **Население:** 124 борта → 1011,
  28 контрактов → 324 (`AGENT_TARGET_FULL`/`CONTRACT_TARGET_FULL`, считаются от размера мира);
  патрулей вдвое больше пиратов. Починен инвариант §5.13.28: **баг был в пробнике, не в игре** —
  сцена стояла внутри звезды и оба пирата горели от `dmgRate=1200·h`, маскируя сигнал; сцена
  унесена на `starRadius·6` по +Z, игровой код не тронут. Лестница: `make` 0-warn, оба smoke,
  `uiclick` PASS, `econ` ok, **soak 120k invariantFails=0** и 3 прогона байт-в-байт.
  Правки: `main.cpp`, `game.cpp/h`, `ui.cpp/h`, `market.cpp/h`, `cluster.cpp/h`, `anomaly.cpp`,
  `resource.cpp`, `soak_test.cpp`, `Makefile`, `.gitignore`, `.github/workflows/build.yml`,
  `master_prompt.md`, `README.md`.

- **2026-07-28** — ✅ **Автономный срез рендера: вспышки-СОБЫТИЯ на короне звезды (flare-цикл)** (§5.13.13).
  Точечное углубление §5.13.6: корона перестала быть статичной. В `renderStarPlasma` (ветка короны) — две
  активные области, дрейфующие по лимбу с `fxClock`, периодически эруптируют (`flareBoost`): всплеск
  яркости ×до 2.2, локальный вырост короны +30%, сдвиг цвета к горячему бело-голубому `255,248,235`.
  Полностью детерминированно (только `fxClock`, ноль RNG — §2.3), `std::exp` лишь при `cosang>0.6`.
  **Нулевой регресс при `fxClock=0`**: на всех прежних звёздных шотах `flareBoost` на видимом лимбе <2%,
  корона изотропна как была; `coronaK`/bbox/амбиентная корона не тронуты. Новый прицельный шот
  `shot_star_flare` (D/R=2.3, `fxClock=26` → область в пике эрупции на правом лимбе). Проверки: `make`
  0-warn, оба smoke, `shot_asan` (**19** сценариев, правки `localdraw.cpp` ⇒ обязательно, 0 ошибок),
  soak 120k (invariantFails=0, deathPath=ok — рендер не влияет на симуляцию), **19/19** шотов просмотрены
  глазами (вспышка видна в `shot_star_flare`; `shot_star_near/lookaway/occlusion/inside/hot` без регресса).
  Правки: `localdraw.cpp`, `shot_test.cpp`, `master_prompt.md`, `README.md`.
- **2026-07-28** — ✅ **Автономный срез (Slice C): эскорт-патрули реагируют на бедствие конвоя** (§5.13.12).
  Прямое углубление Slice B: патруль (`CK_PATROL`) перестал тупо гоняться за «ближайшим пиратом» и стал
  осмысленным защитником. Слышит рейд с `PATROL_DISTRESS_R=1400 LU`, среди пиратов ищет **налётчика**
  (пирата ближе `RAID_NEAR=260 LU` к торговцу/civilian — по СЫРЫМ позициям, порядко-независимо) и
  приоритетно летит на перехват (флаг `defending`); нет налётчиков — фолбэк на прежнюю «ближайший пират в
  `AW2`». Огонь всё так же гейтится `WEAPON_RANGE=150 LU` — патруль долетает и бьётся в упор, НЕ снайпит с
  1400 LU (enemy nerf не введён, чит-эскорта нет). Читаемость: зелёный «эскорт»-пульс у патруля (кольца
  `sz+8/sz+13` — вне циан-рамки цели `sz+6`), зелёный краевой маркер «помощь идёт», панель цели `ESCORT`
  (зелёная рамка/метка/чип). Всё зелёное `90,220,130` = «свои» (≠ SOS-красный/янтарь). Строго аддитивно —
  пираты НЕ ослаблены (combat invariant), global rng не тронут (§2.3). Новый шот `shot_patrol_escort`.
  Проверки: `make` 0-warn, оба smoke, `shot_asan` (**18** сценариев, правки `localdraw.cpp` ⇒ обязательно),
  soak 120k (deaths=0, craftDestroyed=2, invariantFails=0, deathPath=ok — агрегат идентичен до-Slice-C),
  **18/18** шотов (регресс `shot_convoy_distress`/`shot_combat_cockpit` чист). Правки: `local.h`,
  `localsim.cpp`, `localdraw.cpp`, `shot_test.cpp`, `master_prompt.md`, `README.md`.
- **2026-07-28** — ✅ **Автономный срез (Slice B): бедствие конвоя + пейофф за защиту** (§5.13.11).
  Окружающие рейды «пират vs торговец» стали читаемым, действенным событием. Пират (`CK_PIRATE`) в
  атаке (`aiState==1`) на конкретный не-пиратский борт помечает жертву `underAttack` (+ себя
  `threatConvoy`/`threatVictimFaction`); флаги чистятся отдельным пре-проходом до ИИ (индекс жертвы может
  быть > пирата). Жертва получает красный SOS-маяк (кольцо у борта + краевой маркер за кадром) + панель
  бедствия (SOS-рамка/метка/чип); на фронте рейда (0→>0) — тост `CONVOY RAID` (≠ POI `DISTRESS CALL`).
  Пейофф в блоке убийства `(E)`: сбил угрожавшего пирата → +200 CR + исследование + тост `CONVOY SAVED`
  + положительный rep-бамп фракции жертвы (`adjustFactionRelation`). Строго аддитивно — пираты НЕ
  ослаблены (combat invariant), global rng не тронут (§2.3). Новый шот `shot_convoy_distress`. Ревью-субагент
  на диффе среза подтвердил все 5 инвариантов (детерминизм / индексы / аддитивность / порядок пре-прохода /
  строгий гейт бонуса), багов нет; по его RISK-заметке тост `CONVOY RAID` дебаунсирован через `distressCooldown`
  (см. §5.13.11 п.4) и снят устаревший комментарий в `local.h`. Проверки: `make` 0-warn, оба smoke,
  `shot_asan` (**17** сценариев, правки `localdraw.cpp` ⇒ обязательно), soak 120k (invariantFails=0,
  deathPath=ok), **17/17** шотов (регресс `shot_combat_cockpit` чист). Правки: `local.h`, `localsim.cpp`,
  `localdraw.cpp`, `shot_test.cpp`, `master_prompt.md`, `README.md`.
- **2026-07-28** — ✅ **Автономный срез: ЧИТАЕМОСТЬ трафика** (§5.13.10). Поведение §5.13.9 не менялось —
  его сделали видимым из кабины. Прилёт/отлёт дают циан-вспышку «варпа» (`emitWarp`: кольцо + аддитивное
  ядро + искры), уход от причала — синеватый пуфф форсажа, стоянка `errand==1` — пульсирующее янтарное
  кольцо-швартовку (≠ синий щит/циан-рамка), а над `SIG` в панели радара — тальник `IN <n> DOCK <n>`
  (`rh` 158→178). Строго аддитивно, FX под `FX_MAX` (`fxPeak` 6–9 → 16–26), орто-карта побитово цела.
  Новый шот `shot_traffic_dock`. Проверки: `make` 0-warn, оба smoke, `shot_asan` (**16** сценариев),
  soak 120k (invariantFails=0, deathPath=ok), **16/16** шотов. Правки: `localsim.cpp`, `localdraw.cpp`,
  `shot_test.cpp`, `master_prompt.md`, `README.md`.
- **2026-07-28** — ✅ **Автономный срез СИМУЛЯЦИИ: живой трафик NPC** (§5.13.9). Первый срез,
  углубляющий поведение, а не рендер. Не-боевые NPC получили жизненный цикл рейса
  (`LocalCraft.errand` 0 круиз к рынку → 1 стоянка «у причала» → 2 отлёт к краю + деспаун);
  чисто-локальные (`agentIndex<0`) изредка покидают систему, прилёты `(L2)` подсаживают свежих
  торговцев с края до `TRAFFIC_CAP=10` по таймеру — трафик течёт в обе стороны, система «дышит».
  Зеркала макро-агентов курсируют, но не покидают систему (без рассинхрона; write-back отложен → **§5.13.14**).
  Пираты не тронуты (combat invariant). Локальный seeded RNG (§2.3), global rng и макро не тронуты.
  Шот не добавлял (поведение только в динамике — покрыто soak). Проверки: `make` 0-warn, оба smoke,
  `shot_asan` (**15** сценариев, новые поля безопасны), soak 120k (invariantFails=0, deathPath=ok;
  реальные системы наполняются до 10, deep space пустеет), **15/15** шотов. Правки: `local.h`,
  `localsim.cpp`, `localgen.cpp`, `master_prompt.md`, `README.md`.
- **2026-07-28** — ✅ **Автономный срез рендера: туманность как объёмное газовое поле** (§5.13.8).
  Последний «плоский» элемент кадра в перспективе (`fillRect` + 3 хеш-круга) заменён `renderNebula`
  (`localdraw.cpp`): попиксельное поле на небесной сфере — цвет/плотность = функция направления
  мирового луча (та же конвенция, что скайбокс/`renderStarPlasma` ⇒ вращается синхронно со звёздами),
  многооктавный шум (пухлые сгустки + волокна) + доменный варп + крупномасштабный градиент «банки»
  (ось из тона, global rng не тронут), полупрозрачная (звёзды просвечивают), дрейф `fxClock`. Только
  перспектива ⇒ орто-карта `C` побитово цела (§2.7). Новый forced-шот `shot_nebula`. Проверки: `make`
  0-warn, оба smoke, `shot_asan` (**15** сценариев), soak 120k (invariantFails=0, deathPath=ok),
  **15/15** шотов. Орбиты §5.13.2 не тронуты. Правки: `localdraw.cpp`, `shot_test.cpp`,
  `master_prompt.md`, `README.md`.
- **2026-07-28** — ✅ **Автономный срез рендера: освещённые «глыбовые» астероиды** (§5.13.7). Пояс —
  крупнейший оставшийся «плоский» разрыв (45–89 камней-дисков). Новый `renderRockLit` (`localdraw.cpp`):
  попиксельный ray-sphere с ламбертовым терминатором, **выгрызенным** силуэтом (`reff=Rb·(1+disp)`,
  `disp` внутрь ⇒ силуэт ⊆ диска, нет переполнения bbox), кратерами (`crater⁴`) и мозаичностью,
  **кувырканием** через переиспользованный `LocalRock.spin` (поворот выборки шума вокруг Z; global rng
  не тронут). Бюджет: шейдер для `screenR≥5`, мелочь — фазовый диск; всё гейтится на `perspective` ⇒
  орто-карта побитово цела (§2.7). Новый шот `shot_belt_near`. Проверки: `make` 0-warn, оба smoke,
  `shot_asan` (**14** сценариев), soak 120k (invariantFails=0, deathPath=ok), **14/14** шотов. Орбиты
  §5.13.2 не тронуты. Правки: `localdraw.cpp`, `shot_test.cpp`, `master_prompt.md`, `README.md`.
- **2026-07-28** — ✅ **Автономный срез рендера: полировка звезды-плазмы** (§5.13.6). `renderStarPlasma`
  обогащён СТРОГО в рамках изотропной философии (чистая функция геометрии луча): спектральный контраст
  O→M (горячая сине-белая ↔ холодная красно-оранж), хромосферный ободок у лимба (H-alpha), протуберанцы/
  вспышки на короне (угловые плюмы + дрейф по `fxClock`), пятна-сунспоты (тёмные клетки), доменный варп
  2-го порядка + доп. октава. Холодный предел = ТОЧНО прежний оранж (без регресса тёплых). Новый шот
  `shot_star_hot` (пульсар). Проверки: `make` 0-warn, оба smoke, `shot_asan` (13), soak 120k, 13/13.
- **2026-07-28** — ✅ **Автономный срез рендера: кольца газовых гигантов в перспективе** (§5.13.5).
  Ray↔plane аннулюс в экваториальной плоскости (нормаль = ось вращения): щели Кассини, радиальные полосы,
  мягкая тень планеты на кольцах, grazing-opacity, композит по глубине сфера↔кольцо (передняя дуга над
  планетой, задняя за сферой), окклюзия звездой. Встроено в `renderBodySphere`. Добавлена цель
  `make shot_asan`. Проверки: вся лестница + soak 120k + 12 шотов (`shot_planet_near`=Сатурн) + ASan.
- **2026-07-28** — ✅ **Автономный срез рендера: планеты и луны как освещённые ray-sphere сферы**
  (§5.13.4). Тот же приём, что звезда (§5.13.3), распространён с самосветящейся звезды на отражающие
  тела: свет — звезда в (0,0,0), ламбертов терминатор день/ночь → фазы/серпы; попиксельная окклюзия
  луч↔звезда; поверхность из нормали по типу тела (полосы газ-гиганта, мозаичность скалы, альбедо+полюса
  льда, моря луны); мягкий терминатор + атм. ободок. Только перспектива; орто-карта и станции — диски
  (побитово не тронуты). Проверки: вся лестница + soak 120k + 12 шотов + ASan сферы.
- **2026-07-28** — ✅ **Плейтест №4 → проход углубления №5: изотропная сфера-плазма (ray-sphere «шейдер»)**
  (§5.13.3, §7.1.4). Фидбек: «баг костыль — подлетаю, всё жёлтое со всех сторон… звезда неправильно…
  используй шейдеры, шар плазмы; рендер бедный». Баг подтверждён: `if (D≤R*1.7) fillRect(весь экран,
  плазма)` заливал кадр НЕЗАВИСИМО от взгляда. Костыль-заливка и биллборд-диск удалены, заменены
  **попиксельным аналитическим ray-sphere «шейдером»** `renderStarPlasma` (`localdraw.cpp`): тело =
  центр+радиус; для каждого пикселя мировой луч из глаза ∩ сфера, плазма из геометрии (лимб-даркенинг
  `mu=√(1−dmin²/R²)` + многооктавная турбулентность с доменным варпом + запечённая корона); рендер в
  стриминг-текстуру ARGB8888 (SDL2-only, BLEND). Изотропно по построению, ноль порогов/режимов/хардкода.
  Диск непрозрачен → согласован с ray-sphere окклюзией. Новый решающий шот `shot_star_lookaway` (вблизи,
  нос прочь → чёрный космос). Проверки: `make` 0-warn, оба smoke, ASan smoke, **`shot_asan` (11
  сценариев под ASan/UBSan, покрыл попиксельную запись шейдера)**, soak 120k (invariantFails=0), 11/11
  шотов. Орбиты §5.13.2 не тронуты (одобрены). Правки: `localdraw.cpp`, `shot_test.cpp`, `master_prompt.md`.
- **2026-07-28** — ✅ **Плейтест №3 → проход углубления №4: геометрия D/R (звезда доминирует ВЕЗДЕ)**
  (§5.13.2, §7.1.3). Фидбек «масштаб всё равно звезда маленькая». Диагноз: экранный размер звезды
  зависит ТОЛЬКО от отношения `D/R` (`screenR = focal·R/sqrt(D²−R²)`), а §5.13.1 растил абсолютный
  радиус при орбитах до R·13 → звезда ~5% экрана. Фикс: (1) **орбиты как доли R** —
  `orbitRadius = starRadius·(1.20 + 0.42·i + frand(0,0.14))`, внешняя ≤ R·3.44; (2) **звезда ×2.9** —
  `starRadius = 260 + met·100`; (3) старт `px = starRadius·2.8` (D/R≈2.8, ~82% высоты); (4) спавны
  NPC/радио подняты из тела звезды (`starRadius·1.3`); (5) порог заливки `R·1.0008 → R·1.7`
  (бесшовно); (6) фикс «спиц» эклиптики (`camZ>0.06 → 0.14`). Геймплейные радиусы не трогали
  (пропорциональны). Правки: `localgen.cpp`/`localdraw.cpp`/`shot_test.cpp`. Проверки: `make`
  (0 предупр.), оба smoke, ASan/UBSan, `make soak` 120k (invariantFails=0, deathPath=ok),
  `make shots` **10/10** — все зелёные, звезда-доминанта визуально подтверждена.
- **2026-07-28** — ✅ **Плейтест №2 → проход углубления №3: гигантская звезда, окклюзия, мышелук**
  (§5.13.1, §7.1.2). По фидбеку №2 закрыты 5 пунктов: (1) **звезда как аналитическая сфера** — рисуем
  не диск по дистанции, а касательную к сфере (`screenR = focal·R/sqrt(D²−R²)`, изнутри — заливка
  плазмой); принята идея пользователя «чистая геометрия, никаких моделей — центр + радиус». (2)
  **окклюзия луч↔сфера** (`segIntersectsSphere`) для всех объектов → планеты больше не просвечивают
  сквозь звезду (без z-буфера). (3) **гигантский масштаб** — `starRadius 90..135 LU`, планеты единицы
  LU, корабли-крапинки (тысячи планет влезут в звезду). (4) **мышелук** — `SDL_SetRelativeMouseMode`
  в кокпите, дельты мыши → yaw/pitch в поворот Родрига, ЛКМ стреляет (шутерный обзор). (5) **синий
  квадрат при стрельбе** — был муззл в самом глазу, раздутый перспективой; спавн смещён + аддитивный
  bloom + кап размера. Правки: `localdraw.cpp`/`localgen.cpp`/`localsim.cpp`/`local.h`/`main.cpp`/
  `shot_test.cpp`. Проверки: `make` (0 предупр.), оба smoke, ASan/UBSan оба smoke, `make soak` 120k
  (invariantFails=0, deathPath=ok), `make shots` **10/10** (+`shot_star_near`/`_occlusion`/`_muzzle`/
  `_inside`) — все зелёные, визуально подтверждено.
- **2026-07-28** — ✅ **Читаемость боя: лид-прицел + панель цели (щит/сближение)** (§5.13 п.2, §7.1.1):
  проактивный полиш под похвалу пользователя «сражение реально ощущаются сочно» — **чисто аддитивно, врага
  НЕ ослабляли**. (1) **Лид-пип (упреждение)** в кокпите для текущей цели: решаем перехват в относительной
  скорости `u = targetVel − playerVel` (снаряд несёт скорость корабля), квадратка `(u·u−S²)t² + 2(r·u)t +
  r·r = 0`, берём наименьший `t>0`; янтарный маркер на радиусе 600 LU (тот же, что мушка), красно-янтарный
  для врага. Показывается на дистанции завязки (<1500 LU). (2) **Панель цели** (сверху-справа): добавлены
  **полоса щита цели** (циан, над корпусом, если `maxShield>0`) и **скорость сближения** `CLS %+.0F`
  (проекция относительной скорости на луч визирования; `+` = сближаемся, янтарь). Панель растёт по высоте
  при наличии щита. Только `localdraw.cpp` (sim/rng/бой не тронуты). В харнес добавлен 6-й кадр
  **`shot_combat_lead`** (цель наперерез, статичный детерминированный кадр — пип заметно смещён от мушки).
  Проверки: `make` (0 предупр.), оба smoke, ASan/UBSan оба smoke + **харнес под ASan/UBSan** (гоняет новый
  draw с целью), `make soak` 120k (invariantFails=0, deathPath=ok), `make shots` 6/6 — все зелёные; лид-пип
  и панель визуально подтверждены (пират по центру под мушкой, янтарный пип слева — куда целить по
  цели, идущей наперерез 220 LU/ч).
- **2026-07-28** — ✅ **Ориентир эклиптики в кокпите (искусственный горизонт)** (§5.13 п.2):
  проактивный полиш 6DOF-полёта — «линия схода» плоскости системы (X-Y) в кокпите показывает крен
  (наклон линии) и тангаж (смещение) относительно диска системы; снимает дезориентацию, добавленную
  осью крена. Реюз `projectDirectionWithBasis` (как скайбокс), тела орбит ложатся на линию. В глубоком
  космосе не рисуется (нет опорной плоскости). Только `localdraw.cpp` (чистый HUD-draw, sim/rng/бой не
  тронуты). Проверки: `make`, оба smoke, ASan/UBSan оба smoke (рендерят новый draw), `make soak` 120k
  (invariantFails=0), `make shots` 5/5 — все зелёные; горизонт визуально подтверждён (level → горизонталь,
  крен 0.55 → наклон, глубокий космос → пусто).
- **2026-07-28** — ✅ **Вид из кокпита (срез 2, от первого лица)** (§5.13 п.2, §7.1.1): закрыл
  открытый пункт плейтеста №1. Глаз камеры теперь в самом корабле (взгляд вдоль `pfwd`), собственная
  жёлтая стрелка игрока в полёте убрана — вместо неё центральная **мушка-прицел + маркер прогрейда +
  дуга щита**; стрелка сохранена только в карте сверху. `+/−` теперь **оптический зум/FOV**
  (`localFovScale`) вместо долли chase-камеры. Камера вынесена в единый помощник **`buildLocalCamera`**
  (`local.h`) — общий для игры и нового **скриншот-харнеса `shot_test.cpp`** (`make shots`: софт-рендер
  SDL → BMP → PNG). Добавлены **потолки размера частиц** по типу (близкий выхлоп больше не заливает
  экран). Правки: `local.h`/`main.cpp`/`localdraw.cpp` + `shot_test.cpp`/`Makefile`/`.gitignore`.
  Проверки: `make`, оба smoke, ASan/UBSan smoke, `make soak` 120k, `make shots` 5/5 (кадры просмотрены) —
  все зелёные, 0 ошибок. (Закоммичено — `58e7136`.)
- **2026-07-28** — 🎮 **Плейтест №1 3D-режима** (§7.1.1): пользователь подтвердил, что 3D — улучшение,
  и что бой «ощущается сочно» (juice не ослаблять, даже быстрая смерть от пирата — это плюс). Открыт
  один пункт: жёлтая стрелка игрока в chase-виде смотрится странно → **вид из кокпита (от первого лица)**.
  Только документирование фидбека; код не менялся. *(Пункт закрыт срезом 2 — см. запись выше.)*
- **2026-07-28** — ✅ **Полный 3D локального режима РЕАЛИЗОВАН** (§5.13). Полёт по трём осям
  (yaw/pitch/**roll**) на **тело-относительном ортонормированном базисе** (`pfwd`/`pup`, вращения
  Родрига + Грам–Шмидт — без gimbal lock); **перспективная камера от корабля** (chase) с
  переключением на орто-карту сверху (клавиша `C`); **LOD-скайбокс всего кластера** из реальных
  `ClusterStar` по истинным направлениям. §2.7 обновлён: **перспектива разрешена только в локальном
  режиме** (пользователь выбрал «Перспектива (Elite)»); макро-путь ПОБИТОВО не тронут. Добыча
  перенесена `E`→`M` (E занят roll). Правки: `camera.h`/`local.h`/`localsim.cpp`/`localgen.cpp`/
  `main.cpp`/`localdraw.cpp`/`soak_test.cpp`. Проверки: `make`, оба smoke, ASan/UBSan smoke, `make
  soak` 120k кадров — все зелёные, 0 ошибок. (Закоммичено — `58e7136`.)
- **2026-07-28** — Зафиксирован **целевой дизайн локального режима** (§5.2 п.5–6, §5.13): полный
  **3D по трём осям (как Elite, +roll), камера от корабля**, и **весь кластер виден фоном как
  LOD-скайбокс** из реальных `ClusterStar`. Открытый вопрос — §2.7 орто vs перспектива (нужен ОК).
  Пока только документ; реализация — по «go» пользователя. *(Реализовано в тот же день — см. запись выше.)*
- **2026-07-28** — **Единая кликабельная панель действий** (§5.11): контекстные кнопки
  (active/inactive) поверх хоткеев; чинит «не зайти в звезду» (видимая кнопка `L ENTER`). Добавлен
  `scene.minePrompt` как единый источник доступности добычи. Плюс замечание **2D-камера vs полный
  3D** (§5.12) и кандидат роадмапа chase-cam (§7.3). Все уровни проверки зелёные. Коммит на `local-flight-mode`.
- **2026-07-28** — Синхронизация документа и версионирование: весь контекст/прогресс/планы сессии
  сведены в `master_prompt.md`; проект впервые полностью закоммичен и запушен в ветку
  `local-flight-mode` (кор + контентные модули + локальный слой ранее лежали незакоммиченными).
- **2026-07-27** — Локальный режим, **проход 2** (контент + глубокий космос, §5.10): луны, кольца
  планет, туманности, радиосигналы + детекторы (закрыл §7.2), HUD-радар.
- **2026-07-27** — Локальный режим, **проход 1** (ощущение + глубина, §5.10): частицы/juice,
  экранная тряска, щиты, убиваемый игрок + аварийный прыжок, умный ИИ, лут, живые астероиды,
  реакция фракций. Плюс soak-тест (§6.5) и цели Makefile `asan`/`soak`.
- **2026-07-27** — Локальный режим, **срез 1** (§5): инерционный полёт, кеплеровы орбиты, добыча
  на месте, живой бой, стыковка = рынок, варп-ускорение, follow-cam + HUD. Вынесены `camera.h`,
  `render2d.*`. Найден и починен ASan-баг шрифта (§6.3).
- **(ранее, до этой сессии)** — Кор игры (§4.1) и контентные модули `plans_1..10` (§4.2).

---

## 10. Пред-релизный аудит (2026-08-04) — АКТУАЛЬНАЯ передача контекста

> Этот раздел заменяет §0.1 в роли «первое, что читает новый агент». Цикл автономных срезов
> локального режима (§5.13.*) остановлен: пользователь готовит игру к релизу и попросил
> аудит на явные баги, хардкод и мёртвый код, а также усиление геймлупа первого часа.

### 10.1 Что оказалось сломано в основаниях

Аудит нашёл четыре проблемы, каждая из которых обесценивала целый пласт прежней работы.

**A. Релиз не содержал ассетов.** `.github/workflows/build.yml` собирал бинарь и клал рядом
только SDL-фреймворки/DLL. `timertia.png` (портрет Тимертии — первое, что видит игрок, интро
запускается сразу) и `music/processed/*.mp3` не попадали НИКУДА. Плюс код грузил их по путям
относительно CWD, а у `.app` по двойному клику CWD = `/`. Итог: released-сборка была немой и
без персонажа. Починено помощником `assetPath()` (`main.cpp`) поверх `SDL_GetBasePath()` —
ассеты ищутся рядом с бинарём (в `.app` это `Contents/Resources`), с откатом на CWD для
dev-запуска; в CI добавлено копирование ассетов во все три платформенных артефакта.

**B. Сейв не работал в релизе.** F5/F9 писали `starcluster.save` в CWD ⇒ в бандле это `/`,
отказ по правам. Переведено на `SDL_GetPrefPath("starcluster","Starcluster")`; загрузка
пробует сначала пользовательский каталог, затем CWD (старые сейвы не теряются).

**C. Мир был недетерминирован — см. §2.3.** Ключевое следствие для будущего агента:
**числовые бейзлайны soak/shots в §5.13.* и §9 до 2026-08-04 недействительны.** Они
сравнивали случайные числа. Сейчас три прогона soak подряд дают побитово одно и то же.

**D. Легенда управления была мёртвым кодом.** `drawControlHints()` — 29 хоткеев — стояла
закомментированной в `ui.cpp`. Игрок не мог узнать ни об одном действии, кроме пяти,
упомянутых в туторе. В том числе о клавише `L` — входе в локальный режим полёта. То есть
**весь трек §5.13 (плазма-шейдер звезды, кольца, освещённые астероиды, туманность, живой
трафик, закон-цикл) был для покупателя невидим.** Возвращена на `F1`, включена по умолчанию;
`L ENTER SYSTEM` — первой строкой; в панель OBJECTIVES добавлен пункт `FLY THE SYSTEM: PRESS L`
(поле `Game::everEnteredLocal`).

### 10.2 Мёртвый код и хардкод (вычищено)

- 18 предупреждений после включения `-Wall -Wextra` (§3): мёртвые `cachedRouteDistanceFromShip`,
  `activeContractForAgent`, `fmt`, `fmtInt`, `cargoAmount`; неиспользуемые `PRICE_FLOOR_K` /
  `PRICE_CEIL_K` (лимиты цен, которые ни разу не применялись); неиспользуемые локальные
  переменные; знаковые сравнения. Все устранены, сборка чистая с флагами.
- Удалён случайный «System Access Fee»: при въезде в чужую систему с шансом 1% всплывала
  модалка со случайной суммой `rand(1000)+проценты`. Наказание без решения игрока и без связи
  с чем-либо. Его тему занял системный тариф лицензии (§10.4). Убраны `Game::pendingTariff/
  tariffFee/tariffFaction`, `UI::drawTariffModal` и обработчик кликов модалки в `main.cpp`.
- Из git удалено ~60 МБ артефактов: 19 одноразовых скриптов `patch_*.py`/`fix_*.py`/`.patch`,
  `patch.cpp`, `test_ui.cpp`, `test_rng.cpp` (не собирались ни одной целью), 7 zip, 7 dmg,
  9 dll, готовые бинари и 6 папок собранных релизов. `.gitignore` расширен, чтобы не вернулись.
  Место готовых сборок — GitHub Releases, их делает `build.yml`.
- Опечатки в туторе (`suply`, `supercondctivity`, `finde`, `insignts`, «NASH EQUILIBRIUM proofs
  that») — по решению пользователя это были баги, а не глитч-персонаж; вычищены.
- `advanceGame` (`main.cpp`) крутил подшаги без потолка: при просадке кадра на скорости x10
  это 250 вызовов `update` подряд (~0.7 с фриза) с положительной обратной связью. Введён
  `MAX_SIM_STEPS_PER_FRAME = 32` — симуляция скорее отстанет, чем повесит окно.

### 10.3 Экономика: проскальзывание цены (важно для баланса)

Симптом: замер реального рейса через игровые функции торговли давал **x44 капитала за один
рейс** (купил на 321 Cr, продал за 14163). Причина не в спреде (спред 30x — задумка, см.
`make econ`), а в том, что `sellCargo`/`buyCargo` исполняли **весь объём по цене ДО сделки**,
а `applyTrade` двигал цену только ПОСЛЕ. Проскальзывания не существовало.

Решение — `Market::executionPrice(elementIndex, units, selling)` (`market.cpp`): средняя цена
по сделке как интеграл той же экспоненциальной реакции, что уже применял `applyTrade`:

```
продажа: p(x) = p0·exp(−k·x/depth)  ⇒  avg = p0·(1 − e^(−z))/z ,  z = k·N/depth
покупка: p(x) = p0·exp(+k·x/depth)  ⇒  avg = p0·(e^(+z) − 1)/z
```

`k = IMPACT_K = 0.35` и `depth = Market::depthOf()` — те же, что в `applyTrade`; **ни одной
новой константы**. Продажа кэпа не требует (avg монотонно убывает к нулю — «вывалил вдесятеро
больше, чем рынок съедает» само обесценивает груз); покупка ограничена лишь численно (`z ≤ 40`),
объём и так упирается в деньги. `buyCargo` подбирает объём двумя итерациями по средней цене.

**Геймплейный смысл:** появился второй параметр решения. Раньше вопрос был один — «где спред
больше». Теперь ещё и «насколько ГЛУБОК тот рынок». «Купить максимум» из оптимальной стратегии
стало убыточной (замер: −951 Cr), правильно подобранный объём даёт +2214. Замер по 5 сидам:
прибыль хорошего рейса +21…+146% капитала.

**Гарантированный стартовый арбитраж.** На 2 сидах из 5 у стартовой системы не было ни одного
прибыльного маршрута в радиусе двух прыжков — первые пять минут были лотереей. Добавлены чистые
`starterRouteScore()` / `pickStarterSystem()` (`game.cpp`, перед `Game::init`): игрок стартует
в той из систем своей фракции, у которой в радиусе 8 ly больше внятных маршрутов (сосед платит
≥2x И оба рынка имеют глубину ≥4). **Цены не подкручиваются** — выбирается лишь точка входа в
уже сгенерированный мир. Теперь 5/5 сидов имеют прибыльный старт.

### 10.4 Лицензионная квота оборота — новый крючок геймлупа

Дизайн принят пользователем. Ядро: с каждой продажи игрока удерживается тариф; за отчётный
период надо уплатить тарифов не меньше квоты, иначе лицензию отзывают и торговля замерзает
до выкупа.

- **Период — тысячелетие** (`LICENCE_PERIOD_YEARS = 1000`), и это лор: скопление ~100 световых
  лет в поперечнике, цены в его концах расходятся на века, сверить их можно только
  релятивистской корректировкой раз в тысячу лет. На рубеже в ленту падает
  `Millennial relativistic market correction`. При «1 год = 1 секунда» это ~17 минут на x1.
- **Квота первого периода 10 000 Cr** (было 1 000, поднято 2026-08-06), растёт **на 1% каждый
  период** (`LICENCE_QUOTA_GROWTH`) независимо от исхода — чтобы нельзя было вечно жить на одном
  отработанном маршруте.
  ⚠️ **Почему подняли и что это стоило (замер, три сида).** При игре «как живой игрок» — возя в
  радиусе ~8 ly стартовым Hauler-ом — выходит **~105 Cr тарифа за рейс при ~47 годах на рейс**.
  Значит квота 1 000 закрывалась за **~440 лет**, меньше половины тысячелетия, и три четверти
  периода обязательство просто не ощущалось. 10 000 при том же корпусе требуют **~4400 лет**,
  то есть **первый период в одиночку стартовым бортом НЕ берётся оборотом** и честно кончается
  доплатой кредитами (`playerSettleQuota`, 1.5× недобора ≈ 12–15 тыс. Cr при капитале в десятки
  тысяч) — ощутимый укус, но не стена: добыча, контракты и локальный полёт живы. Со второго
  корпуса пропускная способность растёт на порядок, и квота снова берётся оборотом.
  ⚠️ **Цена лицензии выводится из квоты** (`licenceQuotaBase * LICENCE_PRICE_K * licenceCount`),
  поэтому вместе с квотой множитель опущен **1000 → 100**: миллион за второй борт — это design
  point, а пропускная способность игрока от размера квоты не изменилась.
- **Ставка тарифа динамическая**: `LICENCE_TARIFF_BASE · marketClusterLevel()`, зажата в
  `[0.05, 0.14]`. `marketClusterLevel()` (`market.cpp`) — медленное среднее стоимости услуг по
  всем рынкам с постоянной времени ~250 лет, то есть «релятивистский оборот» скопления.
  Богатеющее скопление берёт с оборота больше. Без RNG ⇒ воспроизводится вместе с seed.
- **Провал = заморозка торговли + выкуп** (решение пользователя): `BUY`/`SELL` игрока
  заблокированы через `playerTradingBlocked()`, выкуп по **F2** ценой `2×недобор` (минимум 400).
  Добыча (M), контракты и локальный полёт продолжают работать — есть чем откопаться.
- **UI:** строка в PLAYER STATUS `QUOTA n/N CR  nY LEFT  TARIFF n%`; зелёная (норма взята),
  янтарная (успеваешь в темпе), красная (не успеваешь либо лицензия отозвана).
- **Сейв:** формат поднят до **версии 8**, запись `LICENCE`. Сейвы v6/v7 грузятся (начинают
  новый период с текущего момента).
- Поля: `Game::licenceQuotaPaid / licenceQuotaBase / licencePeriodEnd / licenceTariffRate /
  licenceBuyback / licenceCount / licenceRevoked / licencePeriodsMet`.
- **Не сделано (следующий срез):** покупка ДОПОЛНИТЕЛЬНОЙ лицензии (`licenceCount` уже есть и
  участвует в `licenceQuotaTarget()`, но купить её пока негде). Замысел пользователя: каждая
  новая лицензия = ещё один корабль И прибавка к квоте, то есть расширение — добровольное
  повышение себе ставки.

### 10.5 Население мира

Было 124 борта и 28 контрактов на 10 000 звёзд — один корабль на 75 систем. Игрок мог сделать
десяток прыжков и не встретить никого, а вся живая машинерия §5.13 (конвои под рейдом, эскорт,
набат стаи, облавы за головы) почти никогда не попадалась ему на глаза. Введены
`AGENT_TARGET_FULL = 1024` и `CONTRACT_TARGET_FULL = 320` (`game.h`); фактические числа
считаются ОТ РАЗМЕРА мира, поэтому тестовые сцены на 1200 звёзд остаются лёгкими.

| роль | было | стало | доля |
|---|---|---|---|
| торговцы | 73 | 563 | 55% |
| авантюристы | 20 | 153 | 15% |
| патрули | 12 | 120 | 12% |
| колонисты | 6 | 78 | 8% |
| разведчики | 6 | 48 | 5% |
| пираты | 6 | 48 | 5% |
| контракты | 28 | 324 | — |

Патрулей намеренно ВДВОЕ больше пиратов: фронтир должен быть опасным, а не непроходимым.
Цена: `Game::update` подорожал с 1.32 до 2.69 мс на шаг (27% бюджета кадра при 100 FPS на x1).

### 10.6 Исправленный инвариант §5.13.28 (важный урок о тестах)

soak валил `patrol-hunts-wanted probe: wantedOverDistance=NO fallbackNearest=NO`. Разбор:
**игровой код был исправен**, сломан сценарий теста. Пробник ставил патруль в (0,0,0), а двух
пиратов на 60 и 120 LU от начала координат — то есть **все три борта внутри звезды**
(`starRadius` по §5.13.2 = 260..360 LU). Урон от столкновения с телом (`dmgRate = 1200·h` ⇒
ровно 60 HP за кадр при `dtReal=0.05`) жёг обоих пиратов независимо от патруля, поэтому
условие «ближний НЕ тронут» не могло выполниться никогда. Отладочный прогон показал
`aiTarget=1` — патруль корректно предпочитал розыскного-дальнего ближнему-простому.

Починка: вся тройка смещена на `starRadius·6` по +Z (внешняя орбита ≤ R·3.4 ⇒ там пусто).
Взаимные дистанции те же 60/120 LU, геометрия проверки не изменилась. **Изменён только
`soak_test.cpp`, игровой код не тронут ни строчкой.** Результат: `invariantFails=0`,
`deaths=3933`/`craftDestroyed=7` — как до правки, нулевой регресс.

**Урок:** прежде чем «чинить» игру под падающий пробник, проверь, что сам сценарий пробника
физически осмыслен. Локальная сцена — это звезда радиусом в сотни LU, а не пустая коробка.

### 10.7 Состояние лестницы проверок на момент сдачи

| проверка | результат |
|---|---|
| `make` (с `-Wall -Wextra`) | 0 предупреждений |
| `./game --smoke` / `--localsmoke` | exit 0 |
| `make uiclick` | PASS, 0 failures |
| `make econ` | отчёт строится, спред 30x на месте |
| `make soak` | 120k кадров, **invariantFails=0**, 0 ошибок ASan/UBSan |
| детерминизм | 3 прогона soak байт-в-байт |

### 10.9 Биржа: сводка маршрутов и лицензии (окно `E`)

Раньше кросс-системного вида на рынок не было вообще: окно `TRADE` — это терминал ОДНОГО
рынка (таблица Менделеева той системы, где стоишь). Система знаний фракций (`playerKnownPrice`,
возраст, `confidence`) работала, но игроку её никто не показывал. Новое окно
`WindowKind::Exchange` (`ui.cpp`, открывается клавишей **E** и кнопкой `EXCHANGE` в окне
системы) делает её видимой и добавляет управление лицензиями.

**Сводка маршрутов.** `Game::playerArbitrageBoard(originStar, maxDeals)` (`game.cpp`) строит
список сделок «купить здесь — продать там» ТОЛЬКО по разведанным рынкам в радиусе 25 ly.
Каждая строка несёт возраст данных и `confidence`; строка красится по доверию, поэтому
протухшая разведка видна глазом. Неразведанное не показывается вовсе — знание и есть ресурс.

**Три вещи, которые пришлось померить, а не угадать:**

1. *Сводка пуста на старте — И ТАК ЗАДУМАНО.* Была попытка «починить» пустоту, выдав фракции
   игрока дом (`homeStar`) и вместе с ним посев знаний о соседях. **Откачено по требованию
   пользователя:** карта цен не выдаётся, её добывают ногами, и в этом вся петля мотивации.
   ⚠️ Не «чини» это снова. `homeStar` фракции игрока НАМЕРЕННО остаётся −1.
   Проверено, что петля работает: старт — 1 разведанный рынок и 0 строк; после облёта шести
   соседей — 7 рынков и 40 строк сводки. Наполнение идёт через `observeStar` на стоянке.
2. *Сводка врала в разы.* Первая версия считала выручку по голой известной котировке:
   обещала **+7869**, реальность давала **+285**. Причина — ЁМКОСТЬ рынка назначения.
   Замер: система платит 133 за единицу, но переваривает **0.37** единицы в год против
   **242** у системы-источника — разница в 650 раз. Попытка взять глубину здешнего рынка как
   прокси не помогает: она смещена ровно в худшую сторону (дёшево там, где производят много,
   то есть глубина большая; дорого там, где не производят, то есть глубина крошечная).
   Теперь ёмкость назначения берётся ЖИВАЯ. Обоснование: ёмкость — это размер хозяйства, она
   меняется на порядки медленнее котировки, поэтому «что я узнал при обследовании» и «сколько
   он переваривает сейчас» практически совпадают. Устаревают именно ЦЕНЫ, и они по-прежнему
   берутся из снимка со своим возрастом и доверием.
   **Известное упрощение:** строго говоря, ёмкость тоже стоило бы хранить в системе знаний —
   это четвёртый вектор в `factionMarket*`, в `SignalPacket`, в `SignalMemoryRecord` и в
   сейве (формат ушёл бы в v9). Перед релизом такой размен признан неоправданным. Если будешь
   доделывать — зеркаль `factionMarketDemandPressure` во всех ~12 точках.
3. *«Полный трюм» перестал быть оптимумом.* При тонком рынке назначения прибыль по объёму
   имеет МАКСИМУМ (покупка дорожает, продажа дешевеет). Сводка перебирает 10 объёмов и
   показывает лучший — то есть подсказывает не только КУДА, но и СКОЛЬКО.

**Проверка честности сводки** (взять верхнюю сделку, реально исполнить, сравнить):

| данные разведки | confidence | ошибка прогноза |
|---|---|---|
| свежие | 78–94% | −19% … +8% |
| 40-летней давности | 2–7% | −50% … +25% |

То есть `confidence` теперь означает измеримую величину: игрок видит «доверие 7%» и понимает,
что цифру надо перепроверить. Прибыль верхней сделки — +1351…+2100 при капитале 3600.

**Лицензии в том же окне.**
- `+LICENCE <цена>` — `Game::playerBuyLicence()`. Цена `licenceQuotaBase · LICENCE_PRICE_K ·
  licenceCount`, то есть каждая следующая дороже и **поднимает квоту** (§10.4). Расширение —
  сознательная ставка, а не бесплатный апгрейд. `LICENCE_PRICE_K = 1000` ⇒ вторая лицензия
  стоит **миллион**: по решению пользователя второй борт — не апгрейд, а смена масштаба игры,
  и цель он должен задавать на сотни рейсов. Прежние 5000 закрывались за три рейса и
  обесценивали всю ветку роста флота.
- `SETTLE QUOTA <цена>` — `Game::playerSettleQuota()`: закрыть остаток квоты кредитами по
  курсу `LICENCE_SETTLE_K = 1.5`. Наценка обязательна: при 1:1 богатый игрок просто откупался
  бы каждое тысячелетие и торговля — смысл квоты — стала бы не нужна.
- Шапка окна показывает `LICENCES / HULLS / FREE`.

**Покупка корабля теперь требует свободной лицензии.** В `Game::buyAdditionalShip` стояла
заградительная константа `sc.price + 1000000.0`, которая просто отключала механику. Заменена
на содержательное условие: каждый борт летает по своей лицензии, купленной заранее на бирже.

### 10.11 Биржа: бесконечный список, кэш, и отвергнутая модель цены

**Список стал безграничным.** Радиус поиска (25 ly) убран: сводка охватывает ВСЁ, что игрок
когда-либо разведал, вплоть до всех 10 000 систем, и растёт вместе с его картой. Дальние
строки не прячутся — показывается дистанция, решает игрок. Список листается (колесо мыши
через новый `UI::handleMouseWheel`, плюс кнопки `UP`/`DOWN`), в заголовке — `n-m OF total`.

**Стоимость пришлось чинить дважды.** Наивная версия при полной разведке: 390 370 строк и
**205 мс** на пересчёт. Две правки:
1. *Структурная ошибка*: объём закупки зависит ТОЛЬКО от элемента и кошелька, а считался
   заново внутри цикла по тысячам систем. Вынесен в предрасчёт `legs[]` (один раз на элемент).
2. *Ограниченный top-N*: держим 200 лучших строк, а не все. Порог `worst` (прибыль худшей в
   текущей top-N) плюс оптимистичная оценка сверху отбрасывают кандидата ДО дорогого перебора
   объёмов.

| разведано систем | строк | пересчёт |
|---|---|---|
| 2 | 59 | 0.1 мс |
| 501 | 200 | 1.9 мс |
| 2 000 | 200 | 5.7 мс |
| 10 000 | 200 | **20.6 мс** (было 205) |

20 мс — всё ещё два кадра, поэтому сводка **кэшируется** в `WindowState::exchangeBoard` и
пересчитывается по `UI::updateExchangeBoard`: при смене системы стоянки или раз в 2 игровых
года. Повторное посещение системы обновляет её запись в базе знаний через уже существующий
`observeStar` — сводка подхватывает это на ближайшем пересчёте, отдельного механизма не нужно
(проверено: после повторного визита возраст 0Y, доверие 100%, наблюдение = реальность).

**Что вообще попадает в список (частый вопрос).** Строка появляется, только если сделка
ПРИБЫЛЬНА: цена назначения выше здешней хотя бы на 5%, и после проскальзывания с обоих концов
и тарифа остаётся плюс. Большинство пар «элемент × система» это не проходит — везти железо туда,
где оно своё, убыточно. Плюс общий список режется до 200 лучших по прибыли. Поэтому в общем виде
показаны не «все позиции системы», а только те, на которых можно заработать.
**Под фильтром правила другие** (см. ниже): там показывается ВСЁ разведанное по элементу,
включая убыточные направления — они красятся красным. Система стоянки в список не попадает
никогда (сделке нужны ДВА разных рынка), её цена вынесена в шапку как точка отсчёта.

**Фильтр по элементу (таблица Менделеева).** Общий список ранжирован по прибыли и в окно влезает
около 20 строк, а элементов 118 — «покажи всё, что я знаю про железо» через общий список
недостижимо. Кнопка `FILTER BY ELEMENT` разворачивает ту же таблицу Менделеева, что и окно
торговли (переиспользованы `periodicCell` и `marketCellColor` — игрок узнаёт привычную раскраску
дефицита/избытка), клик по клетке оставляет в списке ТОЛЬКО этот элемент по ВСЕМ разведанным
системам. Под фильтром потолок списка поднят с 200 до 2000 И сняты оба отсечения (ранний отсев
«там не дороже» и порог top-N): список по одному элементу короткий по построению (не больше числа
разведанных систем), и игроку нужна полная карта «где почём», а не только готовые сделки.
Проверено на 61 разведанном рынке: фильтр даёт 60 строк — все, кроме системы стоянки.
Повторное нажатие кнопки снимает фильтр.

**Название.** Окно называется **BROKERAGE**, а не EXCHANGE: биржи в привычном смысле здесь нет
(живого канала с чужой системой не существует), и «exchange» читалось бы как «обмен/размен».
Термин уже введён в игре — тутор Тимертии говорит `you can perform HIGH-FREQUENCY BROKERAGE`.
Заодно исправлена её же фраза `recent stockings` (по-английски это чулки).

**Баг: биржа не следовала за игроком.** `window.star` фиксировался в момент открытия, поэтому
после перелёта окно продолжало показывать ПРЕЖНЮЮ систему: котировки покупки считались чужими,
список пропадал, а снимок текущей системы в сводку не попадал. Важно: сам снимок системы стоянки
игра обновляет каждый тик (`Game::update` → `observeStar`, проверено — возраст 0.00Y и доверие
100% на каждом тике), ломалось именно окно. Теперь `updateExchangeBoard` переводит окно на
систему стоянки и сбрасывает прокрутку.

**⚠️ Модель цены: измерена и ОТВЕРГНУТА.** Была реализована экстраполяция «сползание к опорной
цене скопления с весом `confidence`». Идея выглядела обоснованной — симуляция действительно
тянет цены к опорной через `importBandDrive`, а `confidence = exp(-age/tau)` это ровно вес
наблюдения, то есть ноль новых констант. **Замер показал, что она ХУЖЕ простого снимка.**
Медианная ошибка в разах, 200 разведанных рынков:

| возраст данных | снимок | сползание к опорной | итог |
|---|---|---|---|
| 20 лет | 1.45x | 1.59x | **−31% точности** |
| 40 лет | 1.55x | 1.63x | −14% |
| 60 лет | 1.58x | 1.62x | −6% |

Причина содержательная: **цены систем устойчивы**. Система, дорогая по железу, дорога по железу
и через век — её недра и нужды не меняются. Возврат к средней по скоплению выбрасывает ровно эту
информацию. Реализация откачена; `Game::playerProjectedPrice()` оставлен как единственная точка,
где живёт модель, и возвращает наблюдение. Не повторяй этот заход без замера — стенд лежит в
истории этого раздела.

*Куда копать за настоящей моделью:* в системе знаний уже хранятся `supplyPressure` и
`demandPressure` на момент наблюдения — это НАПРАВЛЕНИЕ движения цены, а не уровень. Прогноз по
тренду («видел дефицит ⇒ с тех пор подорожало») может выиграть там, где возврат к среднему
проиграл. Проверять той же методикой: медианная |log(оценка/реальность)| против снимка.

### 10.10 Шрифт: семь отсутствующих глифов

Плейтест-скриншот показал `TRADE: BUY ?B? / SELL ?V?` и `MINING OFF ?DOCK + M?` — скобки
рисовались заглушкой. Систематическая проверка (сопоставить таблицу `render2d.cpp::glyph()`
со всеми строковыми литералами `ui.cpp`/`main.cpp`) нашла **семь** отсутствующих символов,
которые UI реально печатает: `|` (30 раз), `(` и `)` (по 9), `#` (4), `@` (2), `=`, `~`.
Все добавлены. Плюс в нескольких строках событий стояло длинное тире `—`, которого в шрифте
нет и не будет — заменено на дефис.

⚠️ **Правило §2.5 в действии:** каждый глиф — ровно 5 столбцов × 7 строк = **35 символов**.
Нарушение = out-of-bounds в `drawText` (в проекте это уже ловил ASan, см. §6.3). Добавлять
глифы генератором с проверкой длины, а не руками. Регрессия одной строкой:

```bash
python3 -c "import re;s=open('render2d.cpp').read();print([ (m.group(1),len(m.group(2))) for m in re.finditer(r\"case '(?:\\\\)?(.)': return \\\"([01]*)\\\";\", s) if len(m.group(2))!=35] or 'все 35')"
```

### 10.12 Регрессия БАЛАНСА (`make balance`) — читай перед правкой экономики

**Зачем.** Лестница проверок покрывала память (ASan) и инварианты локального режима (soak),
но экономику — ничем. При этом ВСЕ крупные ошибки этой сессии оказались именно там: сделка
исполнялась без проскальзывания и давала **x44 капитала за рейс**; сводка обещала **+7869**
там, где реальность давала **+285**; стартовая система на 2 сидах из 5 не имела ни одного
прибыльного маршрута; первые два покупаемых корпуса были по трюму МЕНЬШЕ стартового. Каждая
из них была найдена одноразовым стендом, который умирал вместе с сессией. Теперь это
постоянные проверки в `balance_test.cpp`, цель `make balance`.

Пороги намеренно ШИРОКИЕ: задача не заморозить баланс, а поймать поломку МЕХАНИЗМА.

| проверка | что ловит | текущее значение |
|---|---|---|
| проскальзывание существует | возврат к исполнению по цене ДО сделки | котировка 2.91 → крупная продажа 0.42 |
| рейс не печатает капитал | любое возвращение x44 | максимум 1.32x |
| сводка не врёт | прогноз разошёлся с исполнением | худшая ошибка 1% |
| у старта есть чем торговать | регресс `pickStarterSystem` | 0 пустых из 6 сидов |
| трюм окупается | вместимость перестала значить | 110 → 8544, 4000 → 71047 (x8.3) |
| разведка окупается | обесценивание главной петли | 30 рынков ≤ 300 рынков |
| лестница цен без дыр | разрыв, где нечего купить | худший скачок x1.9 |
| стартовый корпус — класс | безымянный корпус ⇒ нет зачёта цены | `Hauler`, 10000 Cr |
| квота проходима | развал главного таймера | 3.3 рейса на квоту (на квоте 1 000; после подъёма до 10 000 — 123 рейса, см. §10.4) |

**⚠️ Урок о собственных измерениях.** В ходе этой работы я дважды сделал НЕВЕРНЫЙ вывод,
измерив не то:
1. «Разрыв прогрессии между квотой 1000 и лицензией 1 000 000». Его НЕТ — лестница плотно
   забита модулями (700–22 000) и корпусами (15 000–900 000). Вывод был сделан без замера.
2. «Доход насыщается на трюме 300, значит 40 из 50 корпусов экономически мертвы». Замер
   делался по ОДНОЙ сделке из сводки, тогда как игра позволяет набивать трюм РАЗНЫМИ
   элементами. Честный замер смешанного груза: трюм 110 → 3023, трюм 4000 → **29139**
   (84 товара за рейс), и с ростом разведки 40 → 2000 систем: 29139 → **52863**. Вместимость
   и разведка работают обе. Остаточное плато выше 4000 — не потолок экономики, а то, что в
   ОДНОЙ системе всего ~84 товара с прибыльным направлением; крупный трюм окупается на
   многосистемной закупке, которую игра уже позволяет.

Мораль: прежде чем объявлять механику сломанной, проверь, что стенд меряет ИГРУ, а не
ограничение собственного инструмента (в п.2 потолок задавал мой же `keep = 200` в сводке).

### 10.13 Ловушка первых корпусов

Стартовый корабль строился вручную в `Game::init` под именем `"Player"` и в `shipClasses()`
не числился. Два следствия:
- `buyShip` ищет текущий корпус по имени, чтобы зачесть его цену в новый. Не находил ⇒
  **на первой же покупке зачёт не работал**, игрок платил полную стоимость.
- Первые два покупаемых корпуса — `Light Courier` (трюм 50) и `Smuggler Runabout` (трюм 100) —
  по трюму МЕНЬШЕ стартового (110). Игрок копил 15 000 (восемь рейсов) и получал даунгрейд.

Починено: добавлен класс **`Hauler`** (трюм 110, топливо 4000, 10 000 Cr) с параметрами
стартового корпуса, `Game::init` строит корабль под этим именем. Лестница стала непрерывной
(худший скачок x1.9), зачёт работает с первой сделки.

Невыгодные покупки при этом НЕ запрещены — по прямому указанию пользователя торговля свободна
и «выгодно или нет решает игрок». Вместо запрета верфь теперь показывает строку
`VS YOURS CARGO ±N FUEL ±N`, красную при ухудшении. Заодно убран мёртвый `sc.price + 1000000`
в подписи кнопки (заградительная константа удалена ещё в §10.9, но UI её показывал): кнопка
второго борта теперь пишет `NEED LICENCE`, если свободной лицензии нет.

### 10.8 Что осталось нерешённым

- **~7 секунд чёрного экрана при старте** (генерация 10 000 систем). Существовало и раньше
  (замерено на старом населении: те же 7.19 с) — значит дело не в агентах. Нужен экран
  загрузки или ленивая генерация.
- ~~**Покупка дополнительной лицензии**~~ — **СДЕЛАНО (§10.9)**, окно биржи.
- ~~**Экран арбитража**~~ — **СДЕЛАНО (§10.9)**. Осталось *дальше*: возможность ПОКУПАТЬ
  свежие рыночные сводки у станций и скаутов (сейчас разведка только своими ногами), и
  хранение ёмкости рынка в системе знаний вместо живого чтения (см. упрощение в §10.9).
- **Оболочка игры** (загрузка, главное меню, заставка студии, комикс-слайды с лором) — делается
  в отдельном чате, промт для него: `PROMPT_frontend_shell.md`. Там же зафиксированы границы
  правок, чтобы ветки не конфликтовали.
- **Неиспользуемые исходники музыки** в `music/` (~46 МБ): игра грузит только `music/processed/`.
  Оставлены намеренно как исходный материал — решение пользователя не запрашивалось.
- **Скорость x10 с населением 1011** требует живой проверки (потолок подшагов поставлен, но
  руками не тестировался).

---

## 11. Оболочка игры (2026-08-04) — заставка, меню, комикс, асинхронная генерация

Задача была поставлена отдельным промтом `PROMPT_frontend_shell.md` (он остаётся актуальным как
описание границ). Реализовано в новых `shell.h` / `shell.cpp`; `main.cpp` тронут только точкой
входа, игровой цикл ниже не изменён.

### 11.1 Порядок экранов

```
запуск -> заставка студии -> главное меню -> [NEW GAME] -> комикс (5 кадров) -> экран генерации
                                          -> [LOAD GAME] ------------------> экран загрузки
                                                                                    |
                                                                        карточка управления
                                                                        PRESS ENTER TO BEGIN
                                                                                    |
                                                                              симуляция
```

`--smoke` / `--localsmoke` пропускают оболочку целиком (иначе CI повис бы на ожидании клавиши) —
там `game.init()` зовётся напрямую из `main`.

### 11.2 Заставка: одно клеточное поле на все экраны

Надпись **TENEVIK GAMES** набирается тем же шрифтом 5×7 (`render2d.cpp::glyph`), но каждый
зажжённый пиксель глифа разворачивается в блок `SUPER×SUPER` (=2×2) клеток — получается ~850
физических клеток. Дальше три фазы:

0. **Подготовка (`primeLogo`, вне экрана).** 165 шагов случайного блуждания со слабым исходом из
   центра прогоняются **мгновенно** при инициализации; каждый шаг пишется в ленту
   `std::vector<short> tape` (шаг × клетка × 2). Поле ставится в ПОСЛЕДНИЙ кадр ленты.
   ⚠️ Разбегание игроку **не показывается** (решение пользователя): он с первого кадра видит уже
   рассыпанное поле, чтобы сразу мешать сборке, а не смотреть, как оно рассыпается.
1. **PH_REWIND** — пауза `REWIND_LEAD` = 0.45 с (успеть прочитать хаос и дотянуться до мыши),
   затем лента отматывается назад со скоростью 0.85 кадра за шаг: буквально «промотка», ~3.2 с.
2. **PH_HOLD** — слово держится 1.35 с, затем клетки **пересобираются в заголовок STARCLUSTER**
   (та же машинерия `retarget`, новые слоты). Ни одна клетка не выбрасывается: лишние становятся
   звёздами фона, недостающие досыпаются с краёв.

По вертикали блуждание сильнее горизонтального (1500 против 900): надпись шире, чем выше, и при
изотропном разбросе два слова оставались бы лежать двумя цветными полосами — цвет выдавал бы ответ
до сборки.

**Курсор — физический толкатель.** Три разных жеста, все в `applyCursor` / `applyPunch`:

| жест | эффект |
|---|---|
| ведение мышью | расталкивание из-под острия (`PUSH_RADIAL`) + передача **собственной скорости курсора** (`PUSH_DRAG`), радиус 110 px |
| зажатая ЛКМ | тот же толчок ×`PUSH_HELD_GAIN` (2.1) |
| щелчок ЛКМ | разовый взрыв: радиус 240 px, мгновенный импульс 1900 px/с, без `dt` |

⚠️ Скорость курсора считается в **настоящих px/с** (`rel` за кадр, делённое на длительность кадра),
а не в «пикселях за кадр». Первая версия копила пиксели за кадр — сила толчка зависела от частоты
кадров, а не от того, как быстро игрок ведёт мышь; резкий взмах и медленное ведение били одинаково.
Скорость клетки ограничена `CELL_MAX_SPEED` = 3200 px/с, иначе щелчок в упор телепортировал бы её
за экран.

⚠️ **ЛКМ на заставке НЕ пропускает её** — кнопка занята толчком. Пропуск только `Enter`/`Space`/`ESC`
(`keyAdvance`, отдельно от общего `advance`).

Сбитая клетка **сходит с ленты** (`freed=1`) и дальше идёт домой сама: критически задемпфированная
пружина + шум, амплитуда которого падает с расстоянием до слота — визуально это «клетка
пересчитала свой путь». Пока лента не кончилась, помешать можно сколько угодно; после этого растёт
`stubborn`, жёсткость пружины увеличивается со временем, и собраться они соберутся в любом случае.
Если игрок сбил больше 30 клеток, на фазе HOLD появляется строка
`N CELLS SCATTERED - ALL OF THEM CAME BACK`.

**Почему лента, а не чистое возвращение по пружине.** Пружинная траектория читается как «магнит»,
лента — как перемотанная плёнка: клетки повторяют собственное дрожание задом наперёд. Ради этого и
держим 165 кадров × 850 клеток × 4 байта ≈ 560 КБ — мелочь по меркам мира на 10 000 звёзд.

### 11.2.1 Музыка: мешок перестановок (регрессия, найденная пользователем)

Симптом: «музыка снова не перемешана, всегда играет одна и та же». Причина — зерно выбора трека
было **константой** `0x51A7C0DE` (и в `main.cpp`, и в оболочке), поэтому каждый запуск давал
идентичную последовательность. Константа появилась при починке недетерминизма мира (§10.1(C)) —
правильная мысль «аудио не должно двигать симуляцию» была реализована через фиксацию зерна вместо
изоляции генератора.

Решение — `Shell::MusicState` + `Shell::pumpMusic` (`shell.cpp`), один мешок на весь сеанс,
общий у оболочки и главного цикла:
- зерно берётся из `SDL_GetPerformanceCounter()` — живое на каждый запуск;
- плейлист тасуется целиком (Fisher–Yates) и играется насквозь: **за круг звучат все треки**,
  ни один не повторяется, пока не отзвучали остальные;
- на стыке двух перестановок первый трек меняется местами с последним, если он совпал с только что
  отыгранным — иначе один трек мог бы прозвучать дважды подряд.

§2.3 не нарушен: глобальный `rng` симуляции музыка по-прежнему не трогает, мир при одном seed
остаётся побитово одинаковым. Проверено замером: 10 запусков подряд дали первые треки
`4 0 2 0 0 5 3 1 1 5` (все 6 индексов встречаются).

**Фон — настоящее шаровое скопление** (`buildCluster` / `stepCluster` / `drawCluster`), а не
декорация. 720 звёзд в **потенциале Пламмера**: падают к общему центру масс, проскакивают
разреженное ядро насквозь и уходят наружу с другой стороны. Смягчение потенциала (`+a²`) убирает
сингулярность в центре, интегратор — полуявный Эйлер (симплектический, энергия не уплывает).
Считается в 3D, на экран идёт **ортографическая проекция** — той же геометрией, что звёздная карта
игры (§2.7: перспективы в макро-слое нет). Глубина `z` даёт размер и яркость, яркость дополнительно
растёт к ядру, оттенки — спектральные классы O..M. Курсор толкает звёзды и щелчком ЛКМ тоже.

⚠️ **Скорости обязаны идти из настоящей функции распределения.** Первая версия брала «случайную
долю круговой скорости в случайном направлении» — это НЕ равновесие: ядро получало почти нулевые
скорости, гало избыточные, и за десяток секунд скопление раздувалось и растворялось в пыль (видно
на плейтесте). Правильно — отбор с исключением по ФР Пламмера: `g(q) = q²(1−q²)^3.5`, где
`q = v / v_esc(r)`. Плюс **приливная граница** на `3.6·a`: отражение радиальной скорости с
коэффициентом 0.82. У настоящих скоплений её ставит галактика, здесь она заодно сливает энергию,
которую накачивает курсор, — иначе за минуту возни мышью скопление всё равно раздулось бы.

Замер устойчивости (68 с работы меню): rms-радиус **183…190 px** (колебание ±2%), население ядра
158…179 из 720 (пуассоновский шум), звёзд за приливной границей — **0**. Периоды орбит у всех
разные, поэтому синхронного «дыхания» не возникает.

**Скопление появляется вместе с меню.** На заставке студии фона нет вообще: `clusterFade = 0`,
скопление не рисуется и даже не считается — иначе клетки логотипа тонут в звёздной пыли. С входом
в `PH_MENU` оно проявляется за 0.9 с. Проверено логом фазы: `ph=0 fade=0.00` → `ph=1 fade=0.00`
→ `ph=2 fade=0.31 → 1.00`.

Клеточный автомат Жизни, стоявший здесь раньше, удалён целиком (не оставляем мёртвый код, §10.2).

### 11.2.2 Баг «собрались — обрыв — собрались снова» (найден пользователем)

Симптом: в первые полсекунды поле явно сходится к слову, потом скачком возвращается в хаос и
начинает сходиться заново.

Причина: `integrate(..., homing=true)` применял тягу домой ко ВСЕМ клеткам, включая те, что ещё
стоят на ленте. Во время промотки это было незаметно — лента каждый шаг переписывала их позиции
поверх. Но в паузе `REWIND_LEAD` лента ещё не двигалась, ничего не переписывало, и пружина успевала
утащить всё поле к слотам; первый же кадр отмотки возвращал их назад.

Починка — одна строка: **клетка на ленте физике не подчиняется** (`if (homing && !c.freed) continue`).

**Как ловилось.** Скриншотами это не отлаживается — нужен был числовой след. Временный замер
печатал каждый кадр среднее расстояние клеток до своих слотов:

```
t=0.433  meanDist=135.3   <- поле почти собралось
t=0.533  meanDist=323.8   <- скачок назад = тот самый «обрыв»
```

После починки кривая монотонна: 0.45 с ровно 335.3 (пауза, поле неподвижно), дальше плавный спуск
до нуля, максимальный скачок вверх — **0.00 px**. Урок общий: для дефектов «что-то дёргается»
заводи скалярный след и смотри на монотонность, а не на кадры.

### 11.2.3 Заторы у слотов: столкновения только для «горячих» клеток

Симптом (плейтест): заголовок в меню собирается дырявым — часть клеток встаёт **рядом** со своими
слотами и там застревает навсегда.

Причина: столкновения действовали на все физические клетки, в том числе на те, что спокойно
доезжают до слота. У самого слота возникало равновесие — пружина толкает внутрь, расталкивание
соседей наружу, — и клетка вставала в него намертво. В меню это было видно ярче всего: `stubborn`
там зафиксирован на 0.35 и не растёт, поэтому пружине неоткуда взять силу, чтобы продавить затор.

Починка: столкновение разбирается, только если **хотя бы одна клетка пары «горячая»** — то есть
летит быстрее, чем её тянула бы одна лишь пружина (`sp > dist·SPRING_K/SPRING_DAMP·1.25 + 140`).
Две спокойно едущие клетки проходят друг сквозь друга. Сбитая летит и бьётся обо всё, включая уже
уложенные клетки (те от удара сами становятся горячими — получается бильярд), но финальное
сближение больше ничем не блокируется.

Замеры (среднее и максимальное расстояние до слота, фон-звёзды исключены):

| сценарий | результат |
|---|---|
| без вмешательства, меню | `mean 0.00`, `worst 0.0` — все клетки точно в слотах |
| 12 с непрерывного обстрела автопилотом, затем тишина | `t+2с mean 63` → `t+4с mean 5.2` → `t+6с mean 0.63` → фаза закрывается на 20.8 с |

То есть затор не просто редок, а невозможен: поле всегда сходится.

### 11.3 Асинхронная генерация мира

`Game::init(10000)` занимает ~9 с (замер `time ./game --smoke`). Он ушёл в `std::thread` (в проекте
`std::thread` уже использовался в `rebuildRouteCache`), а игрок в это время листает комикс.

**Правило владения:** до `join()` мир принадлежит рабочему потоку, главный поток `game` не читает
вообще. Оболочка рисует только собственное поле клеток. Второе правило — §2.3: у оболочки свой
`std::mt19937(0x7E4E71)`, глобальный `rng` симуляции она не трогает ни разу, иначе один seed снова
начал бы давать разные миры.

Закрытие окна во время генерации не «отпускает» поток: цикл ждёт `workerDone` и делает `join()`,
иначе поток пережил бы свой `Game`.

Индикатор прогресса **намеренно неопределённый** (бегущий сканер + реально прошедшие секунды).
Честная полоса требует разбить `Game::init` на фазы с колбэком — это правка `game.cpp`, чужой
территории по границам промта; врать процентами, которых мы не измеряем, хуже, чем не показывать их.

`LOAD GAME` идёт тем же путём: `Game::loadFromFile` внутри **сам перегенерирует кластер**
(`game.cpp:2896`), то есть грузится столько же, сколько новая игра. Если загрузка провалилась,
рабочий поток уже поднял свежий мир, и оболочка честно возвращает `OUTCOME_NEW_GAME`.

### 11.4 Комикс-пролог

Пять кадров `comic/slide1..5.png` (через `assetPath()`, см. §10.1(A); в CI копируются во все три
артефакта). Кадров пока нет — тогда рамка рисуется пустой с надписью `PANEL PENDING`, и виден
только текст. Соглашение об ассетах — `comic/README.md`. Подписи живут в `SLIDE_TEXT` (`shell.cpp`),
материал — §Scope / §Relativity And Time / §Signals из `lore.md` и лицензионная квота §10.4.

**Листается только кликом** (решение пользователя): автопролистывания нет, темп задаёт игрок.
Первый клик дорисовывает печатающийся текст, второй — переключает кадр. `ESC` пропускает пролог.
Пока мир не готов, в углу висит `CLUSTER FORMING...`.

### 11.5 Что изменилось в самой игре

- **Партия стартует на паузе** (`paused = !smoke` в `main.cpp`). В smoke пауза не включается —
  иначе 12 кадров не прогнали бы симуляцию.
- **Стартовый капитал 100 Cr** вместо 3600 (`game.cpp`, решение пользователя). Замер по 5 сидам:
  первый рейс даёт 1170–1580 Cr, квота ТОГДАШНЕГО первого периода (1000 Cr тарифов) закрывалась
  за **5–6 рейсов**, к её закрытию капитал 14–18 тыс. То есть нищий старт квоту §10.4 не ломает.
  (С 2026-08-06 квота первого периода — 10 000 Cr; разбор последствий — в §10.4.)
  ⚠️ Проверка `один рейс не печатает капитал` в `balance_test.cpp` из-за этого была переписана:
  мерить прибыль в долях СТАРТОВОГО кошелька нельзя, на объёме, который не двигает рынок,
  отношение равно голому спреду скопления (30x by design). Теперь она меряет отношение при
  ОПОРНОМ капитале 3600 Cr, а рядом стоит вторая, честно проверяющая механизм: `отдача падает с
  ростом кошелька` (100 → 3600 → 120 000). Это тот же урок, что §10.6.
- **Легенда хоткеев ушла из правого нижнего угла.** Вместо вечной колонки из 33 строк —
  центрированная карточка `UI::drawControlsCard` (5 групп: FLIGHT / TRADE / SHIP / VIEW / GAME).
  Её показывает финальный экран оболочки перед стартом партии и клавиша `F1` в игре (`ESC` тоже
  закрывает — обработчик стоит ДО общего выхода по `ESC`). ⚠️ Совсем убирать карточку нельзя:
  кроме неё игрок нигде не узнаёт про `L` (вход в локальный полёт) — §10.1(D). Поэтому строка
  `L ENTER SYSTEM` в ней подсвечена янтарным.
- **`SOUND ON/OFF` из меню** живёт всю партию: главный цикл не заводит музыку при `soundOn=false`.

### 11.6 Проверки

Новый headless-прогон **`--shellsmoke`**: оболочка в режиме автопилота сама жмёт «дальше» и водит
курсором по полю (толкучка тоже попадает под санитайзер), проходит весь путь заставка → меню →
комикс → генерация и выходит. Первые 7 секунд автопилот молчит, чтобы заставка отработала целиком; он же щёлкает ЛКМ и держит её, чтобы `applyPunch` и усиленный толчок тоже попадали под санитайзер.
Потолок 120 с — прогон не может повиснуть.

| проверка | результат |
|---|---|
| `make` (`-Wall -Wextra`) | 0 предупреждений |
| `./game --smoke` / `--localsmoke` | exit 0 |
| **`./game --shellsmoke`** | exit 0 |
| **`./game_asan --shellsmoke`** | exit 0, 0 ошибок ASan/UBSan |
| `make uiclick` | PASS, 0 failures |
| `make balance` | PASS, 0 failures (12 проверок) |
| `make soak` | 120k кадров, invariantFails=0 |

### 11.7 Что осталось незакрытым

- **Кадры комикса не нарисованы** — пользователь добавит PNG в `comic/`. Формат — `comic/README.md`.
- **Полоса генерации неопределённая.** Честный прогресс = разбить `Game::init` на фазы с
  `std::atomic<int>` счётчиком; 4 строки в `game.cpp`, но это правка чужой территории.
- **Ввод seed в меню** не сделан (в промте был как «возможно»). `game.seed` выставляется из
  `--seed N`, в меню seed только показан в подвале.
- **`CONTINUE` (быстрая загрузка последнего сейва)** не сделан по решению пользователя: состав
  меню оставлен ровно `NEW GAME / LOAD GAME / SOUND / EXIT`.
- **Вступительная новелла Тимертии по-прежнему стартует сразу после оболочки** — теперь игрок
  подряд получает комикс и туториал. Стоит посмотреть на плейтесте, не многовато ли.

## 12. Двигательная модель (2026-08-05) — топливо, рабочее тело, релятивизм

Крупнейшая переделка после оболочки. Топливо перестало быть абстрактным счётчиком и стало
физической моделью на тех же элементах, что и торговля. **Читать перед любой правкой полёта,
экономики перевозок или окна HOLD.** Подробности — `ship.md`, `elements.md`, `economy.md`.

### 12.1 Что было сломано (диагноз до правок)

- `fuelElement` ставился один раз в конструкторе и **не менялся нигде во всём проекте** —
  весь континуум fusion/fission был мёртв, все вечно жгли водород.
- Бак мерился в ШТУКАХ, а не в объёме: он вмещал в 212 раз больше массы урана, чем водорода.
- Заправка была магией: `buyFuel` покупал прямо в бак, минуя трюм. Добытый в шахте элемент
  в бак попасть не мог.
- `handlingRisk` не использовался вообще; `ShipClass` не имел поля скорости — покупка лучшего
  корпуса не давала ни единицы хода.

### 12.2 Модель

**Два РАЗНЫХ вещества.** Ядерный двигатель — это источник энергии плюс способ превратить её
в тягу. Водород в ЯРД не сжигают (окислителя нет), его НАГРЕВАЮТ:

- **Топливо** остаётся в активной зоне и ПЕРЕГОРАЕТ → зола падает в трюм и продаётся;
- **Рабочее тело** улетает в сопло безвозвратно.

Энергия — одна формула на обе ветви, из кривой связи Вайцзеккера (она уже была в элементах):

```text
specificEnergy(Z) = bindingPeak - bindingPerNucleon(Z)      // ноль ровно на железе
```

Размен, который держит выбор живым: **синтез сильнее на МАССУ (H/U = 7.7x), деление на ОБЪЁМ
(U/H = 12.6x)**, а баки ограничены объёмом. Отсюда стартовый корабль сам собой получился
ториевым ЯРД на водороде — NERVA, выведенная, а не назначенная.

**Смеси.** Бункер и бак — такие же `vector<Resource>`, как трюм. Любой элемент грузится куда
угодно; порог поджига (`driveIgnitionFraction`) — плавная сходимость, а не запрет. Железо в
реакторе не «запрещено», оно балласт. Свойства смеси — средневзвешенные по массе, поэтому
обмануть физику смешиванием нельзя by design.

**Уравнение самоссылочно.** В сопло уходит только рабочее тело, топливо остаётся золой той же
массы — значит топливо разгоняет само себя:

```text
W = (P + F) * (e^x - 1);   F = W * (gamma_ve - 1) / E
=>  F = k*P / (1 - k),     k = (gamma_ve - 1)/E * (e^x - 1)
```

**При `k >= 1` решения нет ВООБЩЕ** — топливо, нужное чтобы везти топливо, съедает само себя.
Это честный предел достижимости, и именно он делает деление непригодным для релятивистских
скоростей истечения, а синтез — пригодным.

**Релятивизм.** Складываются БЫСТРОТЫ, не скорости: бюджет `2*artanh(peak)`, расход в полёте
`dw = dv/(1-v^2)`, энергия струи `(gamma-1)`. Без этого бюджет «крейсер 0.5c плюс торможение»
давал ровно `1.0c` — величину несуществующую.

### 12.3 Единственная подгонка — и почему она обязана быть

`DELTAV_SCALE = 0.004` сжимает маршрутный delta-V **в 250 раз**. Для рейса «разгон до 0.28c и
торможение» при `ve = 0.0264c` честный Циолковский требует массовое число `e^21.2` — это
**1.6 миллиарда** килограммов рабочего тела на килограмм корабля. Без сжатия межзвёздный полёт
невозможен ни на каком топливе, что, собственно, правда и о реальности.

Сжатие сохраняет всю ФОРМУ физики (экспоненту, ценовой оптимум, стену достижимости) и переносит
только масштаб. Величина подобрана **по сквозной метрике**, а не на вкус: при 0.015 перевозка
стоила дороже торговой маржи, «в пути» падало с 95% до 46%, и простаивающие агенты каждый такт
заново гоняли дорогой поиск сделок.

Честная альтернатива, обсуждённая с пользователем и ОТЛОЖЕННАЯ: лестница «деление
внутрисистемно → термояд 0.1c → антивещество 0.5c». Физически безупречна (0.1c реально берётся
факелом на водороде: `ve <= 0.137c`, массовое число 6.6), но стартовый корабль на делении тогда
не летает между звёздами вообще — это смена премисы, отдельная ветка.

### 12.4 Степени свободы игрока

Девять: состав топливной смеси, состав рабочего тела, тип движка (термо/ионный/факел,
эксклюзивный слот), ручка режима `throttle`, крейсерская скорость `cruiseFraction`, объёмы
бункера и бака (модули), масса груза, класс корпуса, модули на ход.

Не степени свободы: разгон/торможение (определяется геометрией), и это правильно.

`throttle` (0..1) выбирает скорость истечения внутри ДОСТИЖИМОЙ полосы (границы задают `k < 1`
и объём баков, а не сырой потолок сопла). Ось подписана не «эконом/форсаж», а честно — ЧЕМ
платим: `PROP` слева, `FUEL` справа, потому что крайне левое положение НЕ самое дешёвое.

Рабочая точка ЗАФИКСИРОВАНА в `Ship::cruiseExhaust` и меняется только через `shipTuneDrive`
(вылет, ручка, смена состава). Это принципиально: если подбирать ve на каждом вызове,
планировщик и реальный расход считают по разным ценам и разъезжаются.

### 12.5 Грабли, на которые я наступил (не повторяй)

1. **Циолковский по тикам от постоянной массы.** `sum(e^xi - 1)` НАМНОГО меньше `e^(sum xi) - 1` —
   экспонента выпуклая. Занижало расход в 6 раз. Нужна дифференциальная форма
   `burn = m0*(1 - e^(-dv/ve))` от ТЕКУЩЕЙ массы.
2. **Переоптимизация ve под dV каждого тика** — режим двигателя менялся посреди рейса.
3. **Конечная масса без топлива.** Топливо не улетает, оно становится золой и остаётся на борту.
   Учитывать обязательно, иначе расход занижается тем сильнее, чем тяжелее заправка.
4. **Padding маршрутного запаса не работает.** Лишнее рабочее тело корабль везёт на себе и жжёт
   пропорционально — соотношение не меняется. Проверено замером, не рассуждением.
5. **Выбор топлива по одной энергии** вытаскивает наверх сверхтяжёлые, живущие секунды. Нужны
   `nuclearStability`, достижимость и цена. И `(1 - nobleStability)` на делительную ветвь:
   из благородного газа не собрать критическую сборку.
6. **Снятие модуля до проверок.** Неудачная замена движка оставляла корабль вообще без двигателя.
7. **Тесты, которые правят `throttle` напрямую**, ничего не проверяют: рабочая точка
   зафиксирована, надо звать `shipTuneDrive` — так же, как это делает игра.

### 12.6 Производительность (важно для будущих правок)

Симуляция на реальном шаге игры (`update(0.01)`, 10 000 звёзд, ~1000 агентов):

| | на игровой год |
|---|---|
| до всей двигательной работы | 272 мс |
| после (сейчас) | **159 мс** |

Два найденных ускорения:
- `elementIndex` был линейным поиском по 118 элементам с ДВУМЯ сравнениями строк (до 236 за
  вызов). Смеси сделали его горячим → теперь `unordered_map`, O(1).
- `findBestTrade` перебирал 118 элементов и для КАЖДОГО заново проверял знание об одних и тех же
  96 системах. Список известных направлений вынесен наружу: `factionKnowsOwnerAt` упал с 1603
  профильных сэмплов до 47.

**Что осталось горячим:** модель замещения на рынках (`econQualityPow`, `computeSubstitution`,
`Market::update`) и ГСЧ. Это НЕ маршруты и НЕ двигатели. 160 мс/год = 16% секунды на x1 (нормально),
но на x10 нужно 160% — цикл упирается в потолок подшагов и отстаёт. Следующая оптимизация — там.

### 12.7 Тонкости поведения, которые выглядят как баги

- **STOP тормозит, а не телепортирует в ноль**, но время зависит от МАССЫ: гашение 0.109c
  занимает 15.9 года с полными баками и **1.15 года почти пустым** (ускорение =
  `min(конструктивный предел, тяга/масса)`). На лёгком корабле на x10 это 0.12 секунды —
  выглядит мгновенным и **это правильно**. Индикатор `BRAKING` нужен для тяжёлого случая.
- **Корабль, вставший между систем, не пристыкован ни к одной** (`currentStar = -1`).
  Перемещение привязано к КООРДИНАТАМ: маршрут считается от корабля (было 9.6 ly от порта
  вылета вместо реальных 3.6), торговать из пустоты нельзя, улететь можно к любой системе.
- **Локальный полёт (`L`) бесплатен** — `local*.cpp` про баки не знает вовсе. Закреплено тестом.
- **Перегруз трюма РАЗРЕШЁН**: `cargoCapacity` — паспортная норма, а не стена. Грузить можно
  всегда, взлетать с перегрузом нельзя. Без этого нельзя вылить бак, чтобы сменить схему.

### 12.8 Что НЕ сделано (заделы, посчитано но не потрачено)

Уже считается моделью, но как ось развития не работает:
`handlingRisk` (только режет КПД — мог бы давать радиацию, износ корпуса, контрабандный статус),
`nuclearStability` (только выбор стартового топлива — мог бы давать распад запаса со временем).

Выводится из тех же полей без единого спецкейса: **удельная теплоёмкость** по Дюлонгу-Пти
(`~3R/A`) как настоящий потолок камеры вместо подогнанного `chamberEnergy`; **температура
плавления** как материальный предел сопла и отдельная ветка апгрейдов; **сечение захвата
нейтронов** как ТРЕТИЙ расходник рядом с топливом и рабочим телом.

Общий принцип: одна формула на все 118 элементов, порог — плавная сходимость, а не запрет,
ни одного элемента по имени.

### 12.9 Проверки

| проверка | результат |
|---|---|
| `make` (`-Wall -Wextra`) | 0 предупреждений |
| `make balance` | PASS, **32 проверки** (было 12) |
| `make uiclick` | PASS, **27 проверок** (было 18) |
| `make soak` | 120k кадров, invariantFails=0 |
| в пути (сквозная метрика ИИ) | 96% (базовая до работы — 95%) |
| save/load смесей | round-trip чист, версия **12**, старые отвергаются |

Новые балансовые проверки покрывают: цикл трюм→бак, загрузку любого элемента в любую ёмкость,
разбавление смеси балластом, возврат золы ближе к железу, размен масса/объём, мёртвое железо,
выгоду своего топлива против станционной наценки, эксклюзивность слота движка, разрешённый
перегруз, ручку режима, оптимум в середине шкалы, **схождение прогноза с реальным полётом**
(её отсутствие скрыло сразу четыре бага), дешёвый медленный крейсер, работу OPTIMAL,
расходимость быстроты, бесплатный локальный полёт, гладкость лестницы скоростей, физичность
STOP и дрейф по координатам.


---

## 13. Собственность на систему (2026-08-06) — колонии сведены к покупке

Колония перестала быть отдельной подсистемой. Теперь это **право собственности на
систему**: игрок покупает уже живущее хозяйство целиком, и оно продолжает
симулироваться ровно так же, как до сделки. Одна кнопка `COLONY` (клавиша `C`) —
одна операция.

### 13.1 Что убрано (и почему)

| убрано | почему |
|---|---|
| `Game::playerFoundColony` (основание + «подкрепление» за 2500) | два разных способа получить то, что теперь делает покупка |
| `colonyFoundingRequirements`, `shipBuildRequirements`, `consumeMaterialRequirements`, `pickLocalMaterialElement`, `addMaterialRequirement`, `materialNeedTrait`, `MaterialNeed`, `MaterialRequirement` | вся эта машинерия существовала ТОЛЬКО ради основания колонии и найма корабля |
| `Game::playerHireShip` (клавиша `H`) | наём борта через колонию дублировал верфь; расширение флота живёт на лицензиях (§10.4) |
| `colonyShipHiringCapacity`, `colonyHasShipyardCapacity` | остались без единого вызова |

Сохранено намеренно: `Colony` со всеми полями, `Game::updateColonies`,
очередь строительства (**это и есть «колония развивает себя сама»**),
`stockpile` и контракты `ColonySupply` — чужой NPC-слой, он не мешает.

### 13.2 Попутно исправленный баг (UB)

`Game::buyShip` и `Game::buyAdditionalShip` делали `colonies[starIndex]` —
индексировали вектор колоний номером ЗВЕЗДЫ. Колоний два десятка, звёзд тысячи,
то есть это было чтение за границей вектора объектов со `string`/`vector` внутри,
а «проверка верфи» сравнивала мусор. Гейт по верфи содержательно живёт в модулях
(`minShipyard`), поэтому проверка убрана, а не «починена».

Второй, найденный уже тестом: `setStarOwner` меняет владельца ТОЛЬКО у звезды.
Колония на ней — отдельная запись, её переносит `transferColonies`. Забыть
второй вызов = купить систему, чья касса работает на прежнего хозяина.

### 13.3 Цена системы

Порядок — **МИЛЛИАРД** (тир капитальных кораблей: Battlecruiser 5e9). Владение
системой — цель на всю партию, а не покупка между рейсами. Замер по трём сидам
(1200 систем): медиана ~1.0e9, дешёвый фронтир ~2e8, богатейшая ~1.2e10.

Цена — произведение независимых множителей, каждый отвечает «во сколько раз
система ценнее пустого камня» (`Game::systemPrice` → `SystemPrice`, константы
`SYSTEM_PRICE_*` в `game.h`):

```
price = BASE
      * (1 + население   / POP_REF)
      * (1 + индустрия   / IND_REF)
      * (1 + обитаемость * HAB_W)
      * (1 + оборот недр / TURNOVER_REF)     // ЧТО ЗА ЭЛЕМЕНТЫ В СИСТЕМЕ
      * (1 + инфраструктура*W + верфь*W)     // что уже построено
      * (1 + сила владельца * FOREIGN_W)     // суверенная надбавка
```

`оборот недр` = `Σ productionRate[i] * prices[i]` местного рынка, то есть годовой
выпуск В КРЕДИТАХ. Именно поэтому «какие элементы в системе» получается
контекстно и без единой таблицы: система, богатая иридием, дороже богатой песком
ровно во столько раз, во сколько иридий дороже песка — рынок это уже посчитал.

Окно показывает КАЖДЫЙ множитель отдельной строкой: игрок читает не число, а из
чего оно собрано.

### 13.4 Деньги

- цена уходит **целиком в казну прежней фракции**, которая тут же тратит её на
  свою экспансию и флот. Деньги не исчезают из мира — ты финансируешь соседа, у
  которого купил. У ничейной системы продавца нет и надбавки нет;
- колония копит прибыль в своей кассе (`Colony::localLedger`) и сама тратит её на
  свою очередь строительства — без игрока;
- игрок кладёт и забирает из кассы **только стоя в этой системе**
  (`playerColonyDeposit` / `playerColonyWithdraw`). Империя остаётся МАРШРУТОМ, а
  не строкой в меню.

### 13.5 Дармовой рынок

В своей системе сделка не обменивает деньги: товар, топливо и рабочее тело берутся
и отдаются по цене 0, без пошлин и лицензионного тарифа (`freeMarketFor` в
`game.cpp`, ветки в `sellCargo` / `buyCargo` / `buyConsumable`).

**Но запас и цена двигаются как при обычной сделке.** Это не уступка балансу, а та
же физика: склад конечен, и владелец, вывозящий его подчистую, поднимает себе же
`strain`, душит рост населения и роняет собственный доход. Ограничение встроено в
модель, а не приделано лимитом из воздуха.

Два следствия, о которых стоит помнить:
- дармовая закупка ограничена **трюмом**, а не деньгами (иначе «максимум» вычерпал
  бы систему в корабль, который столько не увезёт);
- продажа за 0 не платит лицензионный тариф, то есть **квоту через свою колонию не
  закрыть** — торговать всё равно надо снаружи.

Даром — только кораблю ПОД УПРАВЛЕНИЕМ игрока. Нанятые борта той же фракции живут
по общим правилам, иначе они возили бы дармовой товар на чужие рынки.

### 13.6 Интерфейс

Новый `WindowKind::Colony` (`ui.cpp: drawColonyWindow` / `handleColonyWindowMouseDown`):

- система не твоя → разбор цены по множителям + `BUY SYSTEM`;
- система твоя → население/индустрия/инфра/`UNMET NEEDS`, доход Cr/год, касса,
  текущая стройка, поле суммы и `DEPOSIT` / `WITHDRAW` / `TAKE ALL`.

Окно торговли в своей системе пишет `YOUR COLONY, ALL PRICES 0`, а кнопки
переименовываются в `TAKE` / `GIVE` / `FILL FUEL+PROP`. Кнопка в окне системы и в
нижней панели зеленеет и читается `MY SYS`, когда система уже твоя.

### 13.7 Проверки

| проверка | результат |
|---|---|
| `make` (`-Wall -Wextra`) | 0 предупреждений |
| `make balance` | PASS, **36 проверок** (было 32) |
| `make uiclick` | PASS, **32 проверки** (было 27) |
| `make econ` | без регресса (strain 11% средний) |
| `make asan` + `--smoke` + `--localsmoke` | чисто |
| `make soak` | 120k кадров, invariantFails=0 |
| save/load | владелец, касса и счётчик покупок переживают round-trip |

Новые балансовые проверки: **порядок цены системы** (медиана обязана остаться
миллиардом, каждый множитель `>= 1`, разброс есть), **покупка двигает владельца и
сохраняет кредиты** (сколько списано — столько пришло продавцу, дважды не купить,
бедного не пускают), **свой рынок даром, но конечен** (цена 0, запас тает, цена
растёт, трюм соблюдён), **касса сохраняет деньги** (и закрыта издалека).
Новые клик-проверки бьют по геометрии кнопок окна колонии.

### 13.8 Побочный эффект: квота поднята до 10 000

Прямое следствие §13.5 (продажа за 0 не платит лицензионный тариф ⇒ квоту через свою колонию
не закрыть, торговать надо снаружи) — квоту имеет смысл сделать РЕАЛЬНЫМ обязательством.
`LICENCE_QUOTA_BASE` поднята **1 000 → 10 000**, `LICENCE_PRICE_K` опущен **1000 → 100**
(цена лицензии выводится из квоты, а миллион за второй борт — design point). Замер, границы
и цена решения — в **§10.4**, регрессия — `make balance`, «квота закрывается за разумное число
рейсов» (границы расширены 1..60 → 20..250 плюс проверка, что доплата кредитами остаётся
подъёмной).

### 13.9 Что дальше по этой ветке (не сделано)

- продажа своей системы обратно (сейчас покупка односторонняя);
- реакция фракции на выкуп её территории (сейчас отношения не двигаются —
  вариант «в казну + просадка отношений» пользователь отклонил в пользу простого);
- дистанционный обзор своих систем одним списком (сейчас надо облетать);
- расходы колонии на оборону/флот из своей кассы.

---

## 14. Локализация (2026-08-06) — русский язык всей игры

### 14.1 Что сделано

В главное меню добавлен пункт **ЯЗЫК: ENG / RUS**, и весь интерфейс переводится
целиком: меню, комикс-пролог, карточка управления, HUD, все окна, диалоги
Тимертии, журнал событий, названия кораблей, модулей и **всех 118 элементов**.
Выбор языка помнится между запусками — лежит в `<savePath>.lang` рядом с сейвом.

Английский остаётся языком по умолчанию и работает как раньше: при `LANG_EN`
`tr()` возвращает исходную строку, не делая вообще ничего.

### 14.2 Как это устроено — и почему НЕ через правку мест вызова

Вся игра рисует текст ровно одной функцией — `UI::drawText` (`render2d.cpp`).
Это единственная точка, через которую проходит каждая надпись, поэтому перевод
живёт **внутри неё**, а не в 300 местах вызова:

```
drawText(...)  ->  I18N::tr(text)  ->  словарь  ->  растеризация 5x7
```

Ключевое следствие: строка приходит в `tr()` **уже собранной** — числа подставлены
`snprintf`-ом. Словарь поэтому работает не по формату, а по готовому тексту:
находит в нём английские слова и обороты и заменяет их, оставляя числа, символы
элементов, имена звёзд и пунктуацию нетронутыми. `"CARGO 12.5 / 40.0 T"`
превращается в `"ТРЮМ 12.5 / 40.0 T"` без единой правки в `ui.cpp`.

Порядок разбора в `I18N::translate` (`i18n.cpp`):

1. **точное совпадение всей строки** — таблица `EXACT`: предложения со знаками
   препинания и форматы, которые переводятся ДО `snprintf`;
2. **жадный оборот** — таблица `WORDS`: ключом берётся кусок ИСХОДНОЙ строки от
   первого слова оборота до последнего, вместе со знаками между ними. Сначала
   пробуется самый длинный, поэтому `BUY SYSTEM` выигрывает у `BUY`, а
   `HOLD / TANKS` и `ADRIFT - NO PORT` совпадают целиком;
3. **пословно**; несовпавшее слово остаётся как есть.

Результат кэшируется (`memo`), поэтому вызов каждый кадр бесплатен. Кэш чистится
при 8192 записях: строки с числами меняются каждый кадр и иначе текли бы.

### 14.3 Шрифт: кириллица в том же растре 5x7

Перевести строки было мало — их нечем рисовать. В `render2d.cpp` добавлены
33 заглавных кириллических глифа (`cyrillicGlyph`) в том же формате «7 строк по
5 бит», что и латиница, плюс разбор UTF-8 в `drawText` (`decodeUtf8`) и
складывание строчных в заглавные (а-я → А-Я, ё → Ё) — шрифт по-прежнему
однорегистровый.

### 14.4 ГРАБЛИ — прочитай до правок

- **Байты против символов.** Кириллица в UTF-8 двухбайтовая, и всякий
  `text.size() * 6` в раскладке начинал врать вдвое: подписи уезжали, значения
  наезжали на свои же метки, перенос строки срабатывал на середине фразы.
  Введены `UI::textLength` / `UI::textPrefix` / `UI::textWidth`; последняя ещё и
  меряет уже ПЕРЕВЕДЁННУЮ строку. Все места ширины переведены на неё
  (`drawStat`, `drawButton`, `centerText`, `wrapText`, `wrapPlain`, кнопки
  нижней панели в `main.cpp`). **Новый код ширину текста считает только через
  `UI::textWidth`.**
- **Печатная машинка переводится ДО набора.** Подписи комикса и реплики Тимертии
  печатаются посимвольно. Переводить обрезок бессмысленно (словарь ищет целые
  слова), поэтому `getTutorialText` и `drawComic` зовут `tr()` на полной строке,
  а машинка идёт по СИМВОЛАМ через `textPrefix` — обрезка по байту рвала бы
  букву пополам.
- **Двухбуквенные ключи опасны.** Символы элементов (`In`, `No`, `Se`, `Ta`,
  `Cr`, `Ar`, `Pb`...) идут в интерфейс обычным текстом и попали бы под
  пословный перевод. В словаре намеренно НЕТ ключей `IN`, `NO`, `SE`, `TA`, `CR`,
  `AR`. `CR` (кредиты) и символы элементов остаются латиницей — это
  международные обозначения. **Добавляя короткий ключ, сверься с таблицей
  Менделеева.**
- **Имена звёзд переводятся через реестр имён собственных** (§25), а не через
  словарь: `Varen-417` → «Варен-417». Ключ — основа без номера, значение —
  она же кириллицей. Раньше имя было `Star_37` и не переводилось вовсе, потому
  что `_` считается частью слова и токен `STAR_37` в словаре не значился; это
  по-прежнему так для старых сейвов. Убери `_` из `wordByte` — и звёзды из
  таких сейвов переименуются в «ЗВЕЗДА_37».
- **Колонки таблиц.** `snprintf` выравнивает по БАЙТАМ, а перевод меняет длину.
  Шапка биржевой сводки поэтому лежит в `EXACT` целиком и собрана по смещениям
  формата строки (куски литерала = колонки). Трёхбуквенные коды хозяйственных
  функций (`FUS`→`СИН`, `STR`→`КОН`...) переведены ровно в три буквы — они стоят
  в колонке `%-4s`.
- **Ключ не должен начинаться или кончаться знаком препинания** — такой оборот
  словами не соберётся (ключ строится от первого слова до последнего). Подписи
  вроде `BUNKER (FUEL)` живут в `EXACT`.

### 14.5 Проверка

Добавлена цель **`make uishots`** (`ui_shot_test.cpp`): рендерит HUD и каждое окно
в BMP→PNG через программный рендерер SDL, аргумент `ru`/`en` выбирает язык.
Нужна именно глазами: русский текст длиннее английского, и переполнение панели
на числах не ловится. Ей найдены и починены все наезды подписей на значения.

Вся лестница проверок зелёная: `--smoke`, `--localsmoke`, `make uiclick`,
`make balance`, `make soak` (120k кадров, invariantFails=0), `make shots`.

### 14.6 Что дальше по этой ветке (не сделано)

- компактные коды в окне верфи (`CG:`, `HW:`, `LW:`, `AR:`, `U:`) оставлены
  латиницей: `AR` сталкивается с символом аргона, а переводить половину блока
  хуже, чем не переводить его вовсе;
- названия фракций (`Aster Compact`, `Helion League`...) оставлены как имена
  собственные;
- заголовок окна ОС (отладочная строка) не переводится;
- строчные кириллические глифы: шрифт однорегистровый, всё приводится к
  заглавным — как и было с латиницей.

---

## 15. Сквозной аудит (2026-08-06) — что нашлось и что починено

Сессия без новых фич: чтение всего проекта на предмет расхождений кода с
документами, тихих багов и мест, где замысел не доехал до реализации.
**База была полностью зелёной** (`make balance` 36/36, `make uiclick` 32/32,
`make econ`, `make soak` 120k `invariantFails=0`, ASan `--smoke`+`--localsmoke`),
поэтому всё найденное — то, что лестница проверок НЕ ловила.

### 15.1 Починено (девять правок, каждая с замером до/после)

**A. Модули не переживали смену корпуса (`Game::buyShip`).** Бонусы модулей
ЗАПЕЧЕНЫ в поля корабля (правило CHROMOCORE §2.4), а `shipApplyClass`
переписывает эти поля табличными значениями нового класса. Список
`ship.modules` при этом оставался нетронутым. Итог: игрок покупал корпус
ступенью выше и **молча терял всю навеску** — трюм, броню, корпус, сенсоры, —
причём слоты оставались занятыми, а `unequipModule` потом вычитал бонусы,
которых на корабле не было. Теперь `buyShip` запекает установленные модули
заново тем же `applyModuleToShip`, что и `equipModule`: один закон на установку
и на пересадку. Замер: Cargo Pod I + Ablative Plating I на Light Courier дают
трюм 90 (было 50) и броню 11 (было 5).

**B. Модули переживали ГИБЕЛЬ корабля (`downgradeAgentToEscapePod`).** Тот же
корень, обратный знак. Список модулей оставался в спас-капсуле, хотя поля уже
обнулены классом капсулы. Снятие такого «фантома» уводило характеристики в
минус: `Aegis Bulwark` (−600 корпуса) на капсуле с `maxHullHP = 34.5` давал
**−565**, и модуль при этом возвращался в трюм — из уничтоженного корабля можно
было выгрести апгрейды. Путь достижим И игроком (гибель в локальном полёте,
`main.cpp` → `playerDestroyed`), И NPC (`robAgent`, write-back §5.13.14/.26).
Теперь модули гибнут вместе с корпусом — как груз.

**C. Второй борт рождался в центре скопления (`Game::buyAdditionalShip`).**
`Ship newShip(sc.name, 0, 0, 0, …)` ставил корабль в `(0,0,0)`, а
`currentStar` — на верфь: два разных места. Перемещение привязано к
КООРДИНАТАМ (§12.7), поэтому окно считало маршрут от звезды, а корабль летел от
центра скопления — расхождение до сотни световых лет. Теперь борт рождается в
координатах своей звезды.

**D. `maxModules` не сохранялся (save/load).** Число слотов апгрейда приходит
из таблицы классов (1 у капсулы, 7 у Battlecruiser, 20 у Fortress) и ничем не
выводится, но в сейв не писалось. После загрузки **всем** бортам возвращался
конструкторский дефолт `3`: капитал молча терял слоты, перехватчик — получал.
Сейв поднят **12 → 13** (политика `architecture.md`, «Save/Load Policy»: бампить
версию и отвергать старые — норма). Проверено round-trip'ом: 7 → 7.

**E. Нерелятивистская энергия в ограничителе расхода** (`shipConsumeForDeltaV`).
`fuelLimitedBurn` считал «сколько рабочего тела оплачено топливом» по
классическому `E = ve^2/2`, а фактическое списание двумя строками ниже — по
релятивистскому `(gamma_ve - 1)`. Одна величина, две формулы. На рабочих
скоростях истечения (до **0.3207 c** на форсаже, замер `make balance`)
расхождение 8.5%: ограничитель разрешал выбросить больше рабочего тела, чем
оплачено, и в УРЕЗАННОМ манёвре (когда бак кончается) корабль получал delta-V из
ниоткуда. Теперь `fuelAvail * energyPerMass / exhaustSpecificEnergy(ve)` — та же
функция, что и в списании. Все 36 балансовых проверок не сдвинулись: ветка
достижима только при исчерпании запаса.

**F. Двигатель перенастраивался В ПОЛЁТЕ.** `agentSetThrottle`,
`agentSetCruiseFraction` и `agentOptimiseForTarget` не проверяли `enRoute` и
звали `shipTuneDrive`, то есть сдвигали ЗАФИКСИРОВАННУЮ рабочую точку посреди
рейса — грабля §12.5.2, только запускаемая рукой игрока. Кнопка `OPTIMAL` при
этом уже рисовалась неактивной (`canOptimise` проверяла `enRoute`), но клик всё
равно проходил: гейт жил в отрисовке, а не в правиле.
Гейт — именно **`enRoute`**, а не «стоим в системе»: после экстренного `STOP`
корабль встаёт посреди пустоты (`enRoute=false`, `currentStar=-1`), и там
перенастроить схему под новую цель не только можно, но и НУЖНО — это и есть
смысл кнопки. В окне `HOLD` обе шкалы гаснут и подписываются `LOCKED`.

**G. Удалён мёртвый 2D-прототип** `civ.{h,cpp}`, `galaxy.{h,cpp}`,
`graphic.{h,cpp}` (208 строк). Он не входил в `SOURCES`, то есть НИКОГДА не
компилировался, но объявлял `class Civ`/`class Galaxy` с методами и
`Star* controlled[]` — ровно ту форму, которую запрещает §2.1, лежащую в дереве
для следующего читателя. `architecture.md`, `README.md` и карта кода §4
обновлены.

**H. Денежный уровень скопления вынесен в партию** (`Game::clusterPriceLevel`).
Он копится в ценовом слое (`market.cpp`: его кормит каждый обновлённый рынок),
но раньше жил ТОЛЬКО в файловом глобале: не сохранялся, не сбрасывался при
загрузке. На нём висит и ставка лицензионного тарифа, и ценовой привод
`gLevelDrive` — единственное, что закрепляет НОМИНАЛЬНЫЙ уровень цен скопления.
Значит ставка тарифа зависела от того, сколько прожил ПРОЦЕСС, а не партия:
загрузил сейв в свежем процессе — одна ставка, после часа игры — другая. Теперь
накопитель зеркалится в поле `Game` (`updateMarkets`), идёт в сейв и
возвращается в ценовой слой при загрузке (`marketSetClusterLevel`). Замер:
партия сохранена при уровне 0.404104, в том же процессе прогнана ДРУГАЯ партия
(глобал уполз на 0.407122), загрузка вернула ровно 0.404104 — и в `Game`, и в
ценовом слое. **Правка численно нейтральна**: на бейзлайне `level=0.4084`,
`rate=0.0500`, котировка `1.942` — до и после совпадают.

**I. Кошелёк перестал быть ловушкой при покупке второго борта.**
`buyAdditionalShip` давал новому агенту `money = 0` и ТУТ ЖЕ переводил на него
`playerAgent`: игрок сразу после сделки оказывался на пустом корабле без единого
кредита (деньги остаются у капитана, который платил) и без предупреждения.
Управление больше не перебрасывается — переключение осталось осознанным
действием `W SWITCH`. Появился перевод кредитов между своими бортами
(`playerOtherShipHere` / `playerTransferCredits`, кнопки `GIVE CR` / `TAKE CR` в
окне `HOLD`, сумма — то же поле шага, что и переливы вещества).
Гейт — оба борта стоят В ОДНОЙ системе и ни один не в полёте, тот же, что у
кассы колонии (§13.4): кошелёк принадлежит КАПИТАНУ и лежит на борту, поэтому
второй борт обязан ВЕРНУТЬСЯ, чтобы отдать выручку. +5 клик-проверок
(`make uiclick` 32 → 37), включая «улетевший борт нельзя пополнить издалека».

Лестница после ВСЕХ правок (A–I): `make` 0 предупреждений, `make balance`
**36/36 PASS**, `make uiclick` **37/37 PASS**, `make soak` 120k
`invariantFails=0` (счётчики побитово те же: `deaths=3508`, `craftDestroyed=7`,
`rocks=573`), ASan `--smoke` + `--localsmoke` чисто. Сейв **12 → 13**.

### 15.2 Открытые вопросы — закрыто

Все три развилки, стоявшие здесь, закрыты: масштаб экономики — **разделом 17**,
доход колонии и казна фракции — **разделом 18**, тысячелетняя сверка и мёртвые
константы тарифа — **разделом 19**.

### 15.3 Что проверялось и оказалось в порядке

- Границы индексов в `localsim.cpp` (`agentIndex`, `playerAgent`) — везде
  проверены перед разыменованием, включая все четыре точки write-back.
- `save/load`: композиция звёзд, недра и `resourceFocus` не сохраняются
  намеренно и восстанавливаются из `seed` — это корректно ровно потому, что
  `SEED` пишется в файл (§2.3).
- Гигиена сборки: `-Wshadow` даёт 3 попадания на весь проект (все безобидные),
  `-Wfloat-equal` — 3, все три осознанные tie-break'и в сортировке сигналов.
- Экономика: проскальзывание, глубина рынка, `executionPrice`, стена
  достижимости `k >= 1`, «свой рынок даром, но конечен» — совпадают с
  `economy.md` и §13.5.

---

## 16. Счёт фракции ходит светом (2026-08-06)

Кошелёк лежит на борту (§15.1I) — это физично, но одним кораблём империю не
обслужить. Нужен счёт, доступный из любой системы. Мгновенный общий счёт в мире,
где новость идёт со скоростью света, был бы ровно тем «фейковым глобальным
знанием», которое запрещает `architecture.md`. Поэтому счёт есть, но у него есть
СКОРОСТЬ.

### 16.1 Правило

Счёт фракции (`Faction::treasury`) обновляется МГНОВЕННО там, где прошла
транзакция. Но **потратить можно только то, о чём уже знает всё скопление**:

```text
доступно = treasury − Σ(приход, который свет ещё не разнёс)
```

Взнос, сделанный у звезды `S` в момент `t`, становится доступным в
`t + creditClearYears(S)`. Снятие в поплавок НЕ попадает: оно бьёт по счёту
немедленно и потому консервативно — за световые годы отсюда нельзя снять то,
что уже снято здесь. Плавает только приход, та половина, где спешка была бы
жульничеством, а заодно это делает двойную трату невозможной по построению.

### 16.2 Почему именно так

Двойная трата — не абстракция, а прямое следствие двух бортов в разных концах
скопления. Если счёт общий и мгновенный, можно снять одни и те же кредиты
одновременно в двух местах, и никакой «банк» этого не заметит быстрее света.
Правило «тратим только покрытое» — это единственное, что можно реализовать
честно, и оно же превращает ограничение в механику: в начале партии (один борт)
разницы не видно вовсе, а в поздней игре продажа на одном краю скопления не
финансирует покупку на другом, пока не пройдёт сверка. Либо жди, либо вези
кошельком на борту.

### 16.3 Геометрия

`Game::creditClearYears(star)` = `|звезда − центр| + радиус` скопления. Центр и
радиус считаются один раз в `Cluster::generate` (производные от координат, в
сейв не идут). Это ВЕРХНЯЯ оценка точного `max` расстояния до всех звёзд:
перебирать 10 000 звёзд на каждую транзакцию нельзя, а ошибка идёт в сторону
осторожности банка — единственную безопасную. Замер на 600 звёздах: радиус
101.4 ly, сверка из родной системы **111 лет** — то есть чуть дороже одного
торгового рейса, ощутимо и соразмерно.

### 16.4 Интерфейс

Окно биржи (`E`) — оно и так финансовое (лицензии, квота):
- в шапке `ACCOUNT <покрыто> CR CLEARED` и, если есть непокрытое,
  `<сумма> CR IN FLIGHT` янтарём. Две цифры, и вторая объясняет первую;
- кнопки `TO ACCOUNT` / `FROM ACCOUNT` над нижним рядом; сумма — то же поле, что
  и везде. Взнос доступен на любой стоянке, снятие — только на покрытую сумму,
  иначе кнопка объясняет причину в строке события.

### 16.5 Реализация

| где | что |
|---|---|
| `cluster.{h,cpp}` | `centreX/Y/Z`, `radiusLy` — считаются в `generate` |
| `game.h` | `struct CreditFloat {faction, amount, clearsAt}`, `Game::creditFloat` |
| `game.cpp` | `creditClearYears`, `factionTreasuryAt`, `factionClearedTreasury`, `factionCreditsInFlight`, `playerAccountDeposit`, `playerAccountWithdraw`; чистка поплавка в `Game::update`; сейв `CREDIT_FLOAT` |
| `ui.cpp` | `ExchangeLayout::accountIn/accountOut`, шапка, обработчик |

⚠️ **Поплавок ОБЯЗАН быть в сейве.** Без него загрузка «печатает» деньги: счёт
полон, в пути пусто, значит всё сразу доступно. Проверено пробником:
внёс 6000 → в пути 6000, покрыто 0; после round-trip — те же 6000 в пути.

### 16.6 Проверки

`make uiclick` — 6 новых клик-проверок: взнос уводит кошелёк на счёт; взнос
числится «в пути»; снятие отказывает, пока свет не покрыл; время сверки
ограничено световым поперечником скопления; после ожидания поплавок пуст;
те же кнопки платят. Вся лестница зелёная.

### 16.7 Что не сделано

- NPC-фракции поплавком не пользуются: их казна тратится ИИ-логикой мгновенно.
  Это осознанно — ограничение придумано для игрока, у которого есть борта в
  разных концах скопления, а фракция и так абстракция;
- пошлины и лицензионный тариф капают в `treasury` без поплавка (они малы и
  идут потоком, а не разовыми взносами);
- переводить со счёта прямо на другой борт нельзя — только через кошелёк того
  борта, который стоит в системе. Это и есть смысл ограничения.

---

## 17. Масштаб мира (2026-08-06) — система перестала быть посёлком

### 17.1 Диагноз

Вопрос пользователя был «почему нет сделок на миллионы». Ответ оказался не про
правила торговли, а про РАЗМЕР покупателя. Замер до правки (seed 42, 1200 систем):

| величина | было |
|---|---|
| население системы | медиана **13 166 человек** (`500 + u*25000`) |
| оборот системы | **2 581 Cr/год** |
| ВВП ВСЕГО скопления | **3.27e6 Cr/год** |
| глубина рынка по элементу | медиана **35 Cr** |
| один рейс | 74 000 Cr = **28.7 годовых оборотов** системы |
| цена системы 9.2e8 | **356 449** годовых оборотов той же системы |

Проскальзывание работало ровно как задумано: нельзя продать в рынок больше, чем
рынок съедает. Просто рынок был посёлком, и полный трюм рудовоза был для него
пятилетним ВВП. Потолок прибыли не назначался — он ВЫВОДИЛСЯ из размера
покупателя. А лестница прогрессии (корпуса 1e4..1e15, лицензия 1e6, система
1e9) писалась для экономики на пять порядков крупнее. Два ряда чисел никогда не
сверяли друг с другом.

### 17.2 Что изменено (источник, а не следствия)

- **`cluster.cpp` — население.** Было равномерное `500 + u*25000` (одинаково
  унылые посёлки везде). Стало **логарифмически равномерное по 3.5 порядкам,
  умноженное на обитаемость**: `10^(6 + 3.5u) * (0.25 + habitability)`. Порядок
  обращений к `rng` НЕ изменён (тираж берётся на прежнем месте, значение
  считается ниже, когда известна обитаемость), поэтому вся остальная генерация
  побитово та же. Замер: медиана **4.7e7**, p10 1.8e6, max 4.1e9;
- **`market.cpp` — размер хозяйства стал МНОЖИТЕЛЬНЫМ.** Было
  `pop*0.0015 + industry*24` — два слагаемых, из которых на прежнем масштабе
  побеждало второе, то есть размер экономики определял безразмерный «уровень
  индустрии», а не число жителей. Стало `население * (0.0015 + industry*0.0024)`:
  хозяйство системы — это её ЛЮДИ, а индустриализация лишь поднимает потребление
  на душу. Миллиард человек с плохими заводами потребляет больше, чем посёлок с
  хорошими;
- **`tradeAccess`** отсчитывается от типичной системы, а не от абсолютных 85
  услуг — иначе после пересчёта доступ у ВСЕХ упирался бы в 1.0, «глухой
  фронтир» исчез бы как класс и вместе с ним широкая ценовая вилка;
- **`POPULATION_TYPICAL` + `starPopulationWeight`** (`cluster.h`): стратегические
  оценки ИИ сравнивают системы ДРУГ С ДРУГОМ, поэтому читают население в долях
  типичной системы. Одна нормировка вместо десятка пересчитанных магических
  коэффициентов (было девять мест вида `population * 0.0000X`);
- потолок прироста колонии стал **относительным** (`4%/год` вместо абсолютных
  1200 человек — на новом масштабе это была бы стена);
- `SYSTEM_PRICE_POP_REF` 20 000 → **5.0e7**, `SYSTEM_PRICE_TURNOVER_REF` 2 200 →
  **1.1e7**: оба опорных числа — МЕДИАНА скопления, то есть типичная система даёт
  по каждому множителю ровно ×2;
- `LICENCE_QUOTA_BASE` 10 000 → **65 000**, `LICENCE_PRICE_K` 100 → **15**. Это
  не подгонка: смысл квоты задаётся числом РЕЙСОВ, а не суммой. Тариф с рейса
  вырос 81 → 533 Cr, значит та же сотня рейсов стоит 65 000; множитель цены
  лицензии опущен во столько же раз, чтобы вторая лицензия осталась миллионом
  (975 000).

### 17.3 Что получилось

**Главное — кривая влияния на рынок ровно та, которую заказал пользователь:**

| корпус (трюм) | доля котировки при продаже полного трюма |
|---|---|
| Hauler 110 (старт) | **0.9989** — новичок не двигает рынок вообще |
| Mining Barge 1500 | 0.9858 |
| Heavy Freighter 4000 | 0.9627 |
| Megafreighter 20 000 | 0.8311 — начинает чувствоваться |
| Giga-Freighter 150 000 | **0.3290** — лейт-гейм роняет цену втрое |
| Tera-Freighter 2e6 | 0.0262 — вывалить некуда |

| величина | стало |
|---|---|
| оборот системы | **1.11e7 Cr/год** |
| ВВП скопления | **9.04e10 Cr/год** |
| глубина рынка | **218 098 Cr** (было 35) |
| цена системы | медиана **1.09e9** = **83 годовых оборота** этой же системы |

**Куш разрешён, но редок.** Решение пользователя: мелкий торговец, нашедший
выгодную сделку, — молодец, множитель в разы это нормально, кредитов у него всё
равно мало. Плохо не «много за рейс», а «много В КАЖДОЙ СОСЕДНЕЙ СИСТЕМЕ».
Замер распределения по 60 системам стоянки при кошельке 3600 Cr:

| | множитель капитала за рейс |
|---|---|
| p10 | 1.6x |
| медиана | **3.0x** |
| p90 | 4.9x |
| доля систем с кушем >5x | **5–7%** |
| тот же замер при кошельке 120 000 | медиана **0.09x** — трюм упёрся, куш не масштабируется |

Поэтому балансовая проверка переписана: вместо «максимум < 3x» (калибровка на
мелкий рынок, где потолок держало проскальзывание) она теперь меряет
РАСПРЕДЕЛЕНИЕ — типичная сделка скромна, куш редок. Это строго сильнее прежней.

### 17.4 Побочно найденный и починенный баг

`commandAgentToStar` считал цифры нехватки ДО вызова `startJourney`, а тот
первым делом перенастраивает движок по ценам порта вылета (§12.4) — то есть
меняет рабочую точку, а вместе с ней и требуемое вещество. Получалось
`route blocked: fuel 2/2 short 0`: игрок залил ровно столько, сколько ему
показали, и всё равно получал отказ без объяснимой причины. Диагностика
перенесена ПОСЛЕ попытки (попытка ничего не ломает — она фиксирует ту самую
точку, по которой корабль и полетит). Побочный выигрыш: балансовая проверка
«прогноз маршрута сходится с полётом» поднялась с **75%/105% до 101%/101%**.

### 17.5 Грабли — читай до правок масштаба

1. **Тронешь генератор населения — перемеряй `SYSTEM_PRICE_POP_REF`,
   `SYSTEM_PRICE_TURNOVER_REF` и `LICENCE_QUOTA_BASE.`** Первые два — медианы
   скопления, третье — сотня рейсов. Иначе цена системы уедет на порядки (при
   первом прогоне без пересчёта она встала на **2.35e15**).
2. **Кредитные константы меряй в РЕЙСАХ, а не в кредитах.** Доплата за квоту в
   тесте теперь считается в «рейсах валовой выручки» (9.1) — величина
   безразмерная и переживает любой следующий пересчёт масштаба.
3. **`tradeAccess` обязан оставаться разбросанным.** Если порог насыщения ниже
   типичного масштаба, все системы становятся «хорошо обслуживаемым ядром»,
   ценовая вилка сужается везде и пропадает смысл летать на отшиб.
4. **Опорные цены (`buildReferencePrices`) должны собираться на ТОМ ЖЕ
   масштабе**, что живые рынки, иначе «типичная цена» считается по деревне, а
   рынки — метрополии, и полоса импорта едет вся.
5. Порядок обращений к `rng` в генераторе трогать нельзя без нужды: сдвинешь —
   изменится весь мир при том же seed.

### 17.6 Проверки

`make` 0 предупреждений, `make balance` **36/36 PASS**, `make uiclick`
**43/43 PASS**, `make econ` (strain 11% средний — как и был), `make soak` 120k
`invariantFails=0`, ASan `--smoke` + `--localsmoke` чисто.
Счётчики soak изменились (`deaths` 3508 → 1836) — это ожидаемо и НЕ регресс:
мир при том же seed стал другим, потому что изменился генератор населения.

### 17.7 Что осталось

Доход колонии (`colonyIncomeAt`) по-прежнему формула, а не интеграл симуляции —
теперь она хотя бы нормирована на типичную систему и не даёт 1.4 Cr/год, но
вести её надо от годового выпуска недр, который и так считается в `SystemPrice`.
Это следующий шаг, и он стал осмысленным только сейчас: до пересчёта масштаба
доход считался в неправильных единицах. Подробности — §15.2.

---

## 18. Доход колонии и клиринговая палата (2026-08-06)

Два последних открытых вопроса §15.2, закрытых вместе, потому что оказались
одним: **куда текут деньги владельца системы**.

### 18.1 Доход колонии — интеграл симуляции, а не формула

Было: `income = star.industry * 0.55 + population * 0.00002`. Синтетика поверх
сгенерированного поля `industry ∈ 0.4..2.6`, которая не могла дать больше единиц
кредитов, что бы в системе ни происходило (замер до правки: **1.4 Cr/год**).

Стало:

```text
income = systemTurnover(star) * COLONY_OWNER_DUTY * marketAccess * (1 - strain)
```

где `systemTurnover = Σ productionRate[i] * prices[i]` — годовой выпуск недр В
КРЕДИТАХ. Величина вынесена в `Game::systemTurnover` и теперь **один источник**
и для цены системы (§13.3), и для дохода её владельца: раньше та же сумма
считалась внутри `systemPrice` отдельным циклом.

Что это чинит по существу:
- доход живёт вместе с ценами, растёт от построенного и **падает, когда склад
  вычерпан** — §13.5 («владелец, вывозящий склад подчистую, роняет собственный
  доход») наконец выполняется буквально, через `strain`;
- замер: медиана дохода колонии **11.4 млн Cr/год** (была 1.4).

`COLONY_OWNER_DUTY = 0.029` — суверенный сбор с местного хозяйства, брат-близнец
пошлины с заходящих кораблей. Величина **отбалансирована, а не выведена**, и это
осознанно: цена системы остаётся произведением контекстных множителей (люди,
заводы, обитаемость, недра, построенное, суверенитет), а окупаемость получается
их СЛЕДСТВИЕМ. Решение пользователя дословно: «вывод стоимости колонии должен
быть из контекста, а не хардкод… просто отбалансить, чтобы в среднем да 10к лет
окупалась, но не факт и не гарантия».

Замер по 1200 системам: медиана окупаемости **10 050 лет**, p10 **4 436**,
p90 **44 159**. ⚠️ Разброс — ФИЧА: богатая людьми и заводами система дороже,
чем даёт её выпуск, тихая рудная — наоборот. Именно поэтому «какую систему
купить» вопрос, а не таблица. Окно продажи показывает строку
`YIELD … CR/Y - PAYS BACK IN … Y` — честный ответ на единственный вопрос игрока.

### 18.2 Колонии телеграфируют прибыль — универсально и по свету

Раньше касса колонии ждала, пока за ней прилетят, а фракция получала лишь
абстрактный скос `income * 0.42`. Теперь колония держит в кассе ровно столько,
сколько нужно её текущей стройке, а **весь остальной доход уходит владельцу**.
Для игрока приход ложится в поплавок §16: империю больше не надо облетать ради
денег, но и мгновенной она не становится — потратить можно, когда известие
покрыло скопление.

Записи поплавка **склеиваются по году сверки** (`Game::addCreditFloat`): колония
телеграфирует каждый тик, и без склейки вектор рос бы на десятки тысяч записей
за партию. Живых записей на фракцию — не больше, чем лет светового поперечника.

### 18.3 Клиринговая палата — межзвёздный банк

Новая фракция `"Clearing House"`. Не игрок и не держава: держит богатые системы
в ядре скопления (дом выбирается со смещением к центру — банку важно, чтобы
свет от него доходил до любого края быстрее всего), собирает **все** лицензионные
деньги (тариф с продаж, выкуп отозванной лицензии, цена новой, досрочное
погашение квоты) и тратит их как государство: раз в такт субсидирует САМУЮ
задыхающуюся колонию скопления — ту, где выше доля незакрытых нужд. Банк не
знает «свой-чужой»: он гасит нестабильность, потому что от неё зависят его же
сборы.

⚠️ **Это закрыло дыру, которую внёс §16.** Лицензионный тариф капал в казну
ФРАКЦИИ ИГРОКА, а §16 дал игроку доступ к этой казне — значит игрок платил тариф
сам себе и тут же снимал обратно. Замер до правки: удержано 33.6 Cr, зачтено в
квоту 33.6 Cr, **снято обратно 33.6 Cr**. Квота не стоила ничего. Теперь
доступно к снятию 0.0 — деньги ушли банку.

### 18.4 Попутно найденный баг генерации

Банк выбирал себе дом среди ВСЕХ звёзд, включая уже занятые, и
`claimInitialHoldings` молча отбирал систему у державы. На seed 42 Aster Compact
оставался **с одной звездой** вместо четырёх; `pickStarterSystem` выбирал
стартовую систему из неё одной, и та оказывалась в пустоте — **ближайший сосед
17.5 ly вместо 3.7**, после чего четыре балансовых проверки не находили, куда
лететь. Банк селится только на ничейной звезде.

Урок общий: `setStarOwner` не спрашивает, был ли у звезды хозяин. Любой новый
код, назначающий владельца при генерации, обязан сам проверять `ownerFaction < 0`.

### 18.5 Проверки

`make` 0 предупреждений, `make balance` **36/36 PASS**, `make uiclick`
**43/43 PASS**, `make soak` 120k `invariantFails=0`, ASan `--smoke` +
`--localsmoke` чисто. Пробники: доход колонии 1.4 → 11.4 млн Cr/год, медианная
окупаемость 10 050 лет, тариф не возвращается игроку, поплавок ограничен.

### 18.6 Что осталось

- NPC-фракции поплавком не пользуются (§16.7) — их казна тратится мгновенно;
- банк субсидирует по одной колонии за такт: этого хватает, чтобы деньги не
  копились мёртвым грузом, но полноценной кредитной политики (займы игроку,
  ставка, дефолт) у него нет;
- продажа своей системы обратно по-прежнему не реализована (§13.9).

---

## 19. Тысячелетняя сверка стала настоящей (2026-08-06)

Последний открытый вопрос §15.2. Одно число делало ДВЕ работы с противоположной
нормировкой, и обе делало плохо.

### 19.1 Что было сломано (замер до правки)

| | |
|---|---|
| якорь `ECON_CREDITS_PER_SERVICE` | 140 |
| стоимость услуги на рынках | p10 31.3, медиана **48.7**, p90 100.4 |
| уровень = медиана / якорь | 0.348 → `gLevelDrive` 1.056 при клампе 1.2 |
| что показывал накопитель | **0.409** — и никогда бы не пришёл к 0.348 |
| ставка тарифа | **0.0500 = пол**, при `BASE 0.08` и потолке `0.14` |

1. **Накопитель не измерял, а тащился.** Шаг 0.4% на обновление рынка,
   стартовое значение задавали СИНТЕТИЧЕСКИЕ рынки `buildReferencePrices`
   (450 обновлений до первого настоящего). Обратная связь контура была
   разорвана: он не отвечал на то, что делают цены.
2. **`LICENCE_TARIFF_BASE = 0.08` и `LICENCE_TARIFF_MAX = 0.14` не действовали
   НИКОГДА.** Ставка вечно лежала на полу 0.05: рынок втрое дешевле якоря, а
   ставка считалась как `BASE × (уровень относительно якоря)`.
3. **Сверка была новостью, а не пересчётом** — уровень не двигался ступенькой.

Две поправки к тому, что утверждалось в §15.2 раньше: механизм эластичности
спроса (`rationing`) НЕ мёртв — он урезает нужду на 100% рынков; и привод цен
НЕ в насыщении на прогретом мире (1.056 против клампа 1.2), хотя при основании
партии он в кламп упирается.

### 19.2 Решение — разделить две работы

Решение пользователя: клампы `0.05..0.14` оставить, якорь не трогать, починить
только замер.

- **привод цен** отвечает на «где мы относительно ЯКОРЯ» — это контур
  управления номинальным уровнем, он и должен мерить от
  `ECON_CREDITS_PER_SERVICE`. Накопитель убран, вместо него честный замер:
  медиана `serviceCostAvg` по реальным рынкам;
- **ставка тарифа** отвечает на «насколько скопление подорожало С НАЧАЛА
  ПАРТИИ» — это про деньги игрока, а не про якорь. Появилось поле
  `Game::clusterPriceBase` (уровень при основании), и
  `licenceTariffRate = LICENCE_TARIFF_BASE × (уровень / база)`, клампованная.

Отсюда `LICENCE_TARIFF_BASE` наконец означает написанное — ставку первого
тысячелетия, — а клампы становятся границами дрейфа вместо вечного пола.
Пересчёт уровня происходит **только на сверке**: она перестала быть таймером и
стала событием, ровно как обещает пролог.

### 19.3 Сглаживание — не украшение, а необходимость

Первый прогон дал ИДЕАЛЬНЫЙ двухтактный цикл: уровень 0.265 ↔ 0.428 ↔ 0.297 ↔
0.430, ставка 0.080 → 0.129 → 0.090 → 0.130 через раз. Это классическое
колебание обратной связи с редкой выборкой: снимок раз в тысячу лет применяется
на полную. Банк теперь берёт новый уровень **на полпути** к замеру, и контур
сходится:

```text
основание 0.2650 (ставка 0.0800 = BASE ровно)
сверка 1  0.3466  ставка 0.1046
сверка 2  0.3579  ставка 0.1080
сверка 3  0.3589  ставка 0.1083
сверка 8  0.3584  ставка 0.1082   <- устойчиво, без цикла
```

### 19.4 Найденный при этом баг в МОЕЙ ЖЕ проверке

Проверка «куш возможен, но редок», добавленная в §17, и пробники к ней делили
прибыль на кошелёк, **не выдавая игроку этот кошелёк**: игрок оставался со
стартовыми 100 Cr, а делили на 3600. Все числа («типичная сделка 3.0x, куш у
5% систем») были шумом. Реальная картина после явной выдачи кошелька:

| кошелёк | медиана множителя | куш >5x |
|---|---|---|
| 400 Cr | **28.6x** | 98% |
| 3 600 Cr | 6.1x | 63% |
| 30 000 Cr | 2.9x | 0% |
| 100 000 Cr | **0.92x** | 0% |

То есть замысел выполняется даже лучше, чем казалось: у бедного куш есть почти
всегда (и он мал в кредитах), у богатого его нет вовсе — капитал сам себя не
удваивает. Проверка переписана под этот безразмерный инвариант: отдача падает по
всей лестнице кошельков, на бедном конце куш есть, на богатом его нет.

⚠️ **Урок общий:** проверка, которая делит на величину, обязана эту величину
ВЫСТАВЛЯТЬ. Иначе она зелёная и не меряет ничего.

### 19.5 Пересчёт квоты

Ставка выросла 0.05 → 0.08, но валовая выручка с рейса упала, поэтому планку
пришлось не поднять, а ОПУСТИТЬ: `LICENCE_QUOTA_BASE` 65 000 → **40 000**
(тариф с рейса 332 Cr ⇒ 120.6 рейса — тот же design point, что и всегда),
`LICENCE_PRICE_K` 15 → **25**, чтобы вторая лицензия осталась миллионом.
Метод из §17.5 подтвердился: считай по рейсам, а не по интуиции.

### 19.6 Проверки

`make` 0 предупреждений, `make balance` **36/36 PASS**, `make uiclick`
**43/43 PASS**, `make econ` (strain 7% средний), `make soak` 120k
`invariantFails=0`, ASan `--smoke` + `--localsmoke` чисто. Пробник дрейфа:
ставка 0.0800 → 0.1082 за восемь сверок без колебаний, round-trip сейва чист.

### 19.7 Что осталось знать

- Уровень между сверками — КОНСТАНТА. Быстрая работа по ценам делается
  остальными членами (дисбаланс, покрытие, полоса импорта), и это правильно:
  скопление физически не может согласовать курс чаще, чем свет обходит его край;
- рынок втрое дешевле якоря, и это НЕ баг: якорь задаёт стартовые цены, пол,
  потолок и порог эластичности спроса, а равновесие ищет симуляция. Опускать
  якорь к замеру пользователь отказался — это утащило бы за собой всю кредитную
  лестницу заново;
- `p10` цен упирается в абсолютный пол 0.02 — это элементы, бесполезные по
  физике (у них все `q ≈ 0`), и так и задумано.

---

## 20. Локальная добыча перестала печатать деньги (2026-08-06)

Пред-релизная сессия минорных твиков. Вопрос пользователя: пояс астероидов —
это правильно и хорошо, но он обязан давать **минимально возможный профит**,
выведенный из контекста системы: майнится то, чем система И ТАК богата, значит
местный рынок этим завален и берёт за гроши. Заработать можно только УВЕЗЯ
намайненное — то есть обычной торговлей. Пояс — способ нищего подняться с нуля,
а не источник капитала.

### 20.1 Три ошибки, найденные замером

Замер до правок (seed 42, 1200 систем, 80 поясов; «жадный игрок» = сортирует
камни по Cr/час и берёт лучшие; якорь — **трюм обычного местного товара**,
величина безразмерная и переживающая любой пересчёт масштаба §17.5):

| | было |
|---|---|
| трюм добычи как попало | 289 Cr = 0.60 трюма товара, **p90 21.3 трюма** |
| трюм добычи жадно | 34 811 Cr = **56.4 трюма товара**, p90 124.8 |
| Cr/час жадно | 3 648 |
| трюм по массовой доле пояса (проверка) | **7.37 трюма товара** |

Стартовый кошелёк при этом — 100 Cr. Три независимых промаха складывались:

**A. Элемент камня выбирался РАВНОМЕРНО по классу породы.** `focusPool` работал
только когда в `resourceFocus` попадал элемент нужного класса; иначе брался
весь `classPool`. Итог: **29% глыб оказывались тем, чего в системе НЕТ**, а за
дефицит местный рынок платит **2.57 опорной цены скопления** (против 1.00 за
то, чем система богата). Пояс раздавал импорт даром.
Стало: элемент — по CDF **запаса недр** (`star.resources`, тот же вектор, из
которого `market.seed` выводит `productionRate`). Одна честная величина вместо
развилки focus/не-focus: редкость приходит из `abundanceWeight × supplyBias`
генератора звезды, ноль хардкода.

**B. Состав пояса считался в ЕДИНИЦАХ, а трюм меряется МАССОЙ.** Камни примерно
одинаковы по тоннажу, поэтому их ЧИСЛО должно идти от массовой доли элемента в
коре. По единицам побеждал водород (обилие 10 300 против 384 у кремния), а
весит единица водорода в 28 раз меньше. Вес домножен на `resourceUnitMass`, и
дополнительно на две физические величины, которые уже посчитаны в
`resource.cpp` и до сих пор не использовались поясом:
- **плотность конденсированной фазы** — астероид это голый камень в вакууме,
  что легко улетает, того в нём и нет (H 0.17 против Fe 8.67);
- **ядерная устойчивость** — камню миллиарды лет, нуклеосинтеза в нём нет:
  франций с полураспадом в минуты рудой не бывает. До этой правки жадный игрок
  штатно находил глыбы Fr/At/Po/Pu — самые дорогие за тонну после водорода.

**C. Буровая снимала одинаковое ЧИСЛО ЕДИНИЦ в час независимо от элемента.**
Единица урана весит в 250 раз больше единицы водорода, а трюм ограничен массой:
тяжёлым элементом он забивался в те же 250 раз быстрее. Отсюда весь хвост
«повезло на глыбу — вот тебе состояние» (p90 у игрока, который вообще не
выбирал камни, был 21.3 трюма товара). Стало: `MINE_RATE_BASE` — **масса в
час**; в трюм ложится `масса / вес единицы`. Резак снимает тонны, а не атомы.

**Плюс новая ось — СОРТНОСТЬ жилы (`LocalRock::grade`).** Во сколько раз этого
элемента в породе своего класса больше или меньше, чем у типичного элемента
того же класса (медиана, не среднее — среднее утаскивают водород и железо).
Сплошная железная глыба черпается быстро, прожилка редкого металла в такой же
глыбе — по грамму. Это выравниватель Cr/час по поясу: дорогое встречается редко
И идёт медленно. Клампы `ORE_GRADE_MIN/MAX = 0.02..2.5` — страховка от
вырождения, а не ручка баланса: разброс приходит из `star.resources`.
Сортность **показана в HUD** (`MINING … GRADE X0.42 …` и та же строка в
подсказке `PRESS M TO MINE`) — иначе «почему эта глыба идёт вдесятеро
медленнее» читалось бы как поломка, а не как выбор игрока.

### 20.2 Что получилось

| | было | стало |
|---|---|---|
| трюм добычи как попало | 289 Cr (0.60 трюма товара) | 351 Cr (**0.79**) |
| ...его p90 | **21.3 трюма товара** | **1.37** |
| трюм добычи жадно | 34 811 Cr (56.4 трюма) | 11 227 Cr (**19.3**) |
| Cr/час жадно | 3 648 | **1 416** |
| проверка (массовая доля пояса) | 7.37 трюма товара | **2.32** |

Главное — не медиана, а **хвост**: у игрока, который просто летает и бурит что
подвернулось, случайный джекпот исчез (21.3 → 1.4 трюма товара). Типичный заход
даёт **0.79 трюма обычного местного товара** — ровно заказанные гроши: полный
трюм добычи ≈ полный трюм самого заурядного, что тут и так лежит на складе.

Постоянная проверка: `make balance` → «локальная добыча — гроши» (§20.4).

### 20.3 ⚠️ Что НЕ починено — решать пользователю

**A. Стоимость тонны груза различается в игре в ~70 раз.** (Частично закрыто
§20.6 — см. ниже; ниже оставлена исходная диагностика, потому что сама причина
никуда не делась.) Цены — за ЕДИНИЦУ
(моль), а трюм ограничен МАССОЙ. Отсюда водород стоит **~370 Cr за тонну
груза** против **~5 Cr** у обычного товара; за ним идут He, Li и долгоживущие
экзотики. Это свойство ВСЕЙ экономики, а не пояса: любой даровой источник
водорода — печатный станок, и жадный игрок, охотящийся именно за ледяными
глыбами, всё ещё снимает 19 трюмов товара за заход. Правки состава могут только
понизить ВЕРОЯТНОСТЬ такой глыбы, но не выплату. Развилка:
1. лёд/водород вообще не добывается из камня (физически защитимо: свободный H₂
   в астероиде не держится, «лёд» — это вода/аммиак, то есть O/N/C) — но класс
   ROCK_ICE со своей палитрой §5.13.15 при этом обеднеет;
2. так и задумано: лёд — это топливо синтеза, и добыча льда есть топливная
   торговля;
3. чинить плотность стоимости во всей экономике — не пред-релизная правка.

**B. Пояс возрождается ПОЛНЫМ при каждом входе в локальный полёт.**
`buildLocalScene` детерминирована от `starIndex`, истощение камней (`rk.ore`)
нигде не сохраняется. Вышел `L`, вошёл обратно — пояс целый.
**Решение пользователя: так и оставить** — пояс бесконечен, но платный (§20.7).

**C. Макро-симуляция в локальном полёте ЗАМОРОЖЕНА** (`main.cpp`, явным
комментарием). Значит добыча не стоит игроку игрового времени вовсе: ни дрейфа
цен, ни событий. **Решение пользователя: так и задумано.**

### 20.4 Проверки

`make` 0 предупреждений, `make balance` **37/37 PASS** (новая — «локальная
добыча — гроши»), `make uiclick` **44/44 PASS**, `make econ` (strain 7% средний),
`make soak` 120k `invariantFails=0` — счётчики **побитово те же** (`deaths=1836`,
`rocks=573`, `shiny=343`): выбор элемента и сортность считаются от
ДЕТЕРМИНИРОВАННОГО хэша индекса камня, ноль обращений к `lrng` (§5.13.20),
поэтому станция/трафик/радио спавнятся как прежде. ASan `--smoke` +
`--localsmoke` чисто.

### 20.5 Минорные правки интерфейса той же сессии

- **`SELL ALL` / `ПРОДАТЬ ВСЁ`** в окне торговли — новая кнопка под `SELL`
  (`Game::agentSellAllCargo`, партии сдаются по одной, каждая по своей цене
  исполнения — тот же путь, что и ручная продажа, поэтому проскальзывание
  считается честно). Гейт: живой рынок И непустой трюм. На своём рынке подпись
  `GIVE ALL`. Продаёт ТОЛЬКО `ship.cargo`: бак и рабочее тело не груз, их
  сливают через окно `HOLD`.
- **Груз на борту виден в таблице элементов** — вторая, ВНУТРЕННЯЯ зелёная
  рамка ячейки. Именно вторая, а не замена цвета: внешняя рамка говорит про
  рынок (что система добывает — циан, что ей нужно — красный), внутренняя — про
  корабль. Раньше продать привезённое можно было, только вспомнив символ и найдя
  его глазами среди 118 клеток.
- **Строка `STOCK … USE …` перестала уезжать за край окна.** Величины масштаба
  §17 (сотни миллионов) печатались полным числом; прочесть «275574988» всё равно
  нельзя. Теперь тот же формат, что и у денег (`creditsLabel`): `275.57 M`.
  Русское `USE` было «ПРИМЕНЕНИЕ» — читалось как «для чего годится» (а это
  соседние строки `Q`/`ДОЛЯ`) и не влезало; стало **«РАСХОД»**. Тройка
  `ЗАПАС / РАСХОД / ОХВАТ` теперь читается как одно: сколько лежит, сколько
  съедают за год, на сколько лет хватит.
- Минимальная высота окна торговли 420 → **456**: столбец действий вырос на ряд.
- `make uishots` теперь снимает окно торговли С ГРУЗОМ на борту и выбранным
  элементом — иначе оно снималось в самом пустом состоянии, ровно без тех строк,
  которые и переполняются.

### 20.6 Пояс стал тяжёлым (решение развилки §20.3A)

Решение пользователя по развилке «лёд не добывается / так и задумано / чинить
экономику»: **ни то, ни другое — разбавить составом**. Дословно: «сделай ещё и
от массы контекст, так и получится, что водород редкий потому что лёгкий, а
почти весь астероид — это тяжёлое, но тоже контекстно от системы».

Отсюда четвёртый множитель веса элемента в поясе — **удержание** (`oreRetention`
в `local.h`):

```text
удержание(A) = 1 - exp(-A / ORE_ESCAPE_MASS),   ORE_ESCAPE_MASS = 24
```

Физика: у астероида нет гравитации, чтобы держать летучее. Тепловая скорость
молекулы падает как `1/sqrt(m)`, убегает то, что быстрее второй космической,
поэтому доля улетевшего экспоненциально зависит от массы. Водород — 0.04,
углерод — 0.39, железо — 0.89, уран — ≈1. `ORE_ESCAPE_MASS = 24` (магний/вода) —
тот рубеж, ниже которого в реальных телах пояса летучих почти нет.

Класс `ROCK_ICE` при этом НЕ обеднел как задумывался §5.13.15: азот и кислород
(A = 14 и 16) держатся вдвое хуже железа, но держатся — это и есть настоящий
астероидный лёд (вода/аммиак), а не свободный водород, которого в камне не
бывает. Палитра класса цела.

Итог замера: **масса пояса — 84% тяжёлого (A ≥ 24)**, 16% лёгкого.

| | §20.2 (было) | §20.6 (стало) |
|---|---|---|
| трюм добычи / трюм товара, как попало | 0.79 | **0.80** |
| ...жадно | 19.3 | **3.5** |
| Cr/час жадно | 1 416 | **540** |
| проверка `make balance` | 2.32 | **1.17** |

Константа `ORE_ESCAPE_MASS` проверена свипом (24 / 36 / 52): кривая насыщается,
разница между значениями в пределах шума, поэтому взято физически осмысленное
24. Остаток разрыва (жадный игрок всё ещё снимает 3.5 трюма товара против 0.8 у
обычного) — та же цена тонны из §20.3A, и он теперь в разы меньше.

---

### 20.7 Бур платит энергией реактора (решение развилки §20.3B)

Решение пользователя: пояс пусть будет бесконечным, но «игрок платит двигателем
за добычу (энергией) немного совсем — чтобы нельзя было вечно бесплатно
фармить, а иногда заполнять себе реактор топливом».

Новый примитив `shipDrawReactorEnergy(ship, energy, ashOut)` (`ship.{h,cpp}`):
энергия из реактора НЕ на движение. Жжёт топливо из бункера тем же законом, что
и полёт (`specificEnergy` смеси / `NUCLEON_REST_MEV`), но **без КПД струи** —
реактор греет бур, а не разгоняет рабочее тело, — и той же золой отвечает.
Возвращает реально выданную энергию: меньше запрошенной, если бункер сух.
Заодно вынесен общий `spillAsh` — прежде пересчёт «сгорело → зола» жил внутри
`shipConsumeForDeltaV` и был бы скопирован вторым экземпляром.

В `localsim` бур платит ДО зачисления руды: `wantEnergy = массаПороды ×
MINE_ENERGY_PER_MASS`; сколько энергии реактор выдал, столько породы и срезано.
Пустеющий бункер режет подачу, а не выдаёт руду в долг; на нуле бур гаснет с
`NO REACTOR FUEL` / «РЕАКТОР БЕЗ ТОПЛИВА». Зола ложится в трюм ровно как в
полёте (`consumeAndStoreAsh`): она заменяет сгоревшее топливо, общая масса не
растёт. Остаток бункера показан прямо в строке добычи — `MINING METAL GRADE
X1.00 +42 ORE 80 **FUEL 100%**`, иначе отказ прилетал бы без предупреждения.

**⚠️ Числа калибровки ниже относятся к первой редакции §20.7; после §20.9 они
пересчитаны (`MINE_ENERGY_PER_MASS = 1.02e-5`, плюс `DRILL_POWER_FRACTION`), а
замеры остались теми же. Рассуждение о том, ПОЧЕМУ расход обязан быть мелким,
в силе.**

Калибровка упирается в жёсткий факт:
топливо стартового корпуса — актиний по **263 Cr за единицу массы**, полная
заправка с нуля стоит **140 440 Cr** при стартовом кошельке 100 Cr. Тонна руды
продаётся за ~4 Cr. Значит реактор может потратить не больше ~1.5% намайненной
массы в топливе, иначе добыча уходит В МИНУС по кредитам. Замер на выбранном
значении (реальный прогон `updateLocalScene`, 23 системы):

| | как попало | жадно |
|---|---|---|
| цена сожжённого топлива / выручка | **23%** (p90 40%) | 1.2% |
| бункера на полный трюм | **0.1%** | 0.1% |
| доля золы в трюме | 0.3% | 0.3% |

То есть добыча остаётся прибыльной (+77% к затратам), шкала топлива на глазах
ползёт вниз, но полного бункера хватает примерно на **800 трюмов**. Сделать
расход ощутимее в БАКОВЫХ долях нельзя, не сделав добычу убыточной, — это прямое
следствие §20.3A. Если захочется жёстче, крутить надо не эту константу, а цену
тонны груза.

Петля, которую это создаёт: заправиться за кредиты в начале игры невозможно
(140 440 Cr), поэтому единственный способ восполнить бункер — намайнить летучее
(лёд: азот/кислород, §20.6) и перелить его в реактор через окно `HOLD / TANKS`.
Железо и силикаты топливом не являются (balance: «железо не топливо»), так что
выбор породы наконец значит и для выживания, а не только для выручки.

Постоянная проверка: `make balance` → «бур жжёт топливо реактора» — держит два
свойства сразу: с топливом бур даёт руду и уводит бункер вниз, с пустым бункером
не даёт НИЧЕГО и гаснет. Второе важнее: если гейт отвалится, добыча снова станет
бесплатной, и это не заметит ни одна другая проверка.

### 20.8 Проверки после §20.6–20.7

`make` 0 предупреждений, `make balance` **38/38 PASS** (две новые), `make
uiclick` **44/44 PASS**, `make econ` (strain 7% средний), `make soak` 120k
`invariantFails=0` — счётчики **побитово те же** (`deaths=1836`, `rocks=573`,
`shiny=343`), ASan `--smoke` + `--localsmoke` чисто, `make shots` рисует строку
добычи с сортностью и остатком бункера.

---

### 20.9 Бур перестал быть плоской константой

Замечание пользователя по §20.7: «у нас ведь разные реакторы, разный лут с
астероидов, разные трюмы и разные цены в системах — а ты просто нахардкодил?»
Проверка кода показала, что замечание справедливо на треть, и эта треть была
крупной.

**Что уже выводилось** (и продолжает): элемент камня — из недр системы (масса ×
плотность × ядерная устойчивость × удержание, §20.1/§20.6); сортность жилы —
оттуда же; запас руды — класс породы × `miningRichness` системы; масса топлива
на тонну — из `specificEnergy` смеси в бункере (лучшее топливо → меньше жжём);
цена сожжённого — из МЕСТНЫХ цен, потому что топливо это обычный элемент рынка.
Цены в системах и вместимость трюма в ФИЗИКУ добычи не входят намеренно: первое
меняет только выручку, второе — только сколько тонн влезет.

**Что было плоским.** Скорость добычи задавала одна константа
`MINE_RATE_BASE = 9.0` тонн/час — **одинаковая для спасательной капсулы и для
«Фортресса»**. Ни корпус, ни движок на добычу не влияли вовсе. И `КПД
установки` нигде не участвовал: `shipDrawReactorEnergy` считал энергию топлива
без единой поправки на то, какой движок стоит.

**Как теперь.** Один закон вместо двух несвязанных констант:

```text
энергия на тонну = MINE_ENERGY_PER_MASS / (класс породы × сортность жилы)
мощность бура    = shipReactorPower(корабль) × DRILL_POWER_FRACTION
тонн в час       = мощность бура / энергия на тонну
топлива в час    = мощность бура                     (та же величина, один раз)
```

- `shipReactorPower` (`ship.{h,cpp}`) — паспортная мощность установки:
  **тяга корпуса × КПД поставленного движка** (`driveReactorEfficiency`, новая
  функция в `drive.{h,cpp}`). Обе величины уже лежали в таблицах. Это НЕ
  пересчёт джоулей, и так и подписано; важно, что ни одна из двух не зависит от
  содержимого бака — иначе бур глох бы от пустого бака рабочего тела, которое
  реактору не нужно. Отдельный `driveReactorEfficiency` нужен потому, что
  `driveJetEfficiency` берёт наценку за ионизацию и опасность РАБОЧЕГО ТЕЛА — к
  потребителю на борту это отношения не имеет.
- `MINE_YIELD_*` (класс породы) переехал со скорости на энергию. Числа те же,
  смысл честнее: «богатая порода» = больше тонн с того же джоуля, а не магически
  быстрее.
- `MINE_RATE_BASE` **удалён**: скорость больше не назначается, она выводится.
- Две оставшиеся константы не дублируют друг друга и отвечают за разное:
  `DRILL_POWER_FRACTION` — сколько топлива в час жжёт включённый бур (сторона
  КОРАБЛЯ); `MINE_ENERGY_PER_MASS` — сколько топлива уходит на тонну (сторона
  КАМНЯ). Скорость — их частное.

Замер (реальный прогон `updateLocalScene`, медианы по 16 системам; «жадно» =
игрок сортирует камни по Cr/час):

| корпус | режим | трюм/трюм товара | часов | бункера | топливо/выручка |
|---|---|---|---|---|---|
| Hauler | как есть | 0.69 | 33 | 0.11% | **22.8%** |
| Hauler | жадно | 2.50 | 48 | 0.14% | 3.7% |
| Mining Barge | как есть | 0.86 | 474 | 0.91% | 12.3% |
| Mining Barge | жадно | 1.36 | 353 | 0.68% | 9.2% |
| Heavy Freighter | как есть | 0.91 | 254 | 0.90% | 19.4% |
| Heavy Freighter | жадно | 1.01 | 213 | 0.75% | 16.2% |

Константы подобраны так, чтобы **баланс §20.7 не сдвинулся**: стартовый Hauler
даёт те же ~9 тонн/час и те же 22.8% выручки на топливо. Всё остальное — новое:
тяжёлый борт грызёт камень в 2.6 раза быстрее стартового, спасательная капсула
не грызёт вообще.

Побочно вскрылось приятное: **преимущество «жадного» игрока падает с размером
трюма** (2.50 → 1.36 → 1.01). Премиальных глыб в поясе единицы, и на большой
борт их не хватает — приходится брать пояс целиком, то есть по средней цене.
Оптимизация выбора камней остаётся игрой ранней, мелкой прогрессии, чем она и
должна быть.

⚠️ **Найдено, но НЕ починено:** `mining.cpp` (макро-добыча на стоянке) до сих
пор масштабируется как `1 + ship.utility * 0.01`, а `utility` — это СЕНСОРЫ, и
он идёт от 0 у стартового корпуса до 200 000 у «Фортресса». То есть на топовом
борту макро-добыча ускоряется в две тысячи раз, а «Mining Barge» (utility 10)
копает медленнее «Перехватчика» (utility 15). Локальная добыча этот множитель
не использует. В таблице корпусов вообще нет поля «добывающая установка» —
`Mining Barge` называется рудовозом, но механически это просто большой трюм.
Развилка для пользователя: завести честное поле бурильной установки в
`ShipClass` (+ модуль под него) — или оставить как есть. **Закрыто §20.11:
установка стала МОДУЛЕМ.**

### 20.10 Проверки после §20.9

`make` 0 предупреждений, `make balance` **39/39 PASS** (новая — «бур
масштабируется с реактором»), `make uiclick` **44/44 PASS**, `make econ`
(strain 7% средний), `make soak` 120k `invariantFails=0` — счётчики побитово те
же (`deaths=1836`, `rocks=573`, `shiny=343`), ASan `--smoke` + `--localsmoke`
чисто.

---

## §20.11 Капсула грызёт, буровая стала модулем (2026-08-06)

Два решения пользователя по итогам §20.9.

### Капсула обязана добывать

Дословно: «капсула должна грызть самое слабое, иначе игрок никогда на капсуле не
сможет вылезти из нищеты». Проверка показала, что дело было не в мощности бура, а
в **нулевом трюме**: `Escape Pod` имел `cargoCapacity = 0`, поэтому бур упирался в
`CARGO FULL` на первом же кадре. Игрок, потерявший корабль, оставался вообще без
способа заработать — тупик, а не наказание. Хуже: с нулевым трюмом капсула не
могла и торговать, то есть отрезался весь экономический контур сразу.

Трюм капсулы **0 → 12** (меньше десятой доли стартового «Тягача»). Мощность бура
у неё и так самая слабая в игре по построению §20.9: тяга 1.0 и КПД 0.40 против
18.0 и 0.55 у стартового корпуса. Замер: **0.2 тонны/час** против 3.6 у «Тягача»
и 9.4 у «Тяжёлого грузовика» — полный трюм капсулы набирается за ~60 часов.
Подняться со дна можно, но это долгая дорога, и это правильная цена за потерю
борта.

### Буровая установка — модуль

`ShipClass` намеренно НЕ трогали: буровая — это оборудование, а не свойство
корпуса. Появилось поле `Ship::miningRig` (запекается модулем, CHROMOCORE §2.4),
`ModuleDef::miningBonus` и три ступени в слоте `Special` — отдельном от сенсоров,
потому что добыча и разведка это разные приборы:

| модуль | цена | верфь | эффект |
|---|---|---|---|
| Mining Laser I / БУРОВОЙ ЛАЗЕР I | 800 | 0 | ×3 темп |
| Mining Laser II / БУРОВОЙ ЛАЗЕР II | 4 600 | 1 | ×7 темп |
| Ore Refinery Rig / ОБОГАТИТЕЛЬНАЯ УСТАНОВКА | 19 000 | 2 | ×19 темп |

Первая ступень доступна с верфи 0 и стоит 800 Cr намеренно: бур — это то, чем
игрок выбирается из нищеты, в том числе на спасательной капсуле (замер: капсула
2.01 → **6.02** тонны за 10 часов).

⚠️ Установка даёт **ТЕМП, а не выгоду**: `shipDrillPower = shipReactorPower ×
(1 + miningRig)` входит в мощность, а не в энергию на тонну. Грызёшь быстрее — и
ровно во столько же раз быстрее жжёшь топливо. Цена тонны руды не меняется, то
есть буровая покупает ВРЕМЯ, а не прибыльность. Иначе она стала бы обязательной
покупкой, а не выбором стиля.

«Mining Barge» после этого — не «просто большой трюм с именем», а лучшая
платформа под буровые: 5 слотов и трюм на 1500. Родного бонуса у корпуса нет и
не задумано.

### Побочно пойманная грабля §15.1B в новом месте

`Ship::miningRig` — ЗАПЕЧЁННОЕ поле, а `shipApplyClass` переписывает поля
корабля табличными значениями нового класса и про новое поле, разумеется, не
знает. Значит буровая пережила бы и пересадку на другой корпус, и **гибель
борта**: `downgradeAgentToEscapePod` чистит `modules`, но поля обнуляет именно
`shipApplyClass`. Ровно та же форма ошибки, что A и B в §15.1, третий раз.
`shipApplyClass` теперь обнуляет `miningRig`; `buyShip` запекает модули заново
ПОСЛЕ этого вызова (§15.1A), поэтому законная навеска не теряется.
Закрыто проверкой «буровая гибнет вместе с корпусом».

**Урок на будущее: любое новое поле корабля, которое заполняется модулем,
обязано быть (а) обнулено в `shipApplyClass` и (б) записано в сейв.** Ни то, ни
другое не выводится автоматически, и оба раза ошибка тихая.

### Сейв 13 → 14

`miningRig` пишется рядом с `maxModules` по той же причине (§15.1D): список
модулей при загрузке заново не применяется, поэтому запечённое поле обязано
ехать в файле. Проверено round-trip'ом: 6.0 → 6.0. Старые сейвы отвергаются —
норма по `architecture.md`.

### Проверки

`make` 0 предупреждений, `make balance` **40/40 PASS** (новая — «буровая гибнет
вместе с корпусом»), `make uiclick` **44/44 PASS**, `make econ` (strain 7%),
`make soak` 120k `invariantFails=0`, счётчики побитово те же
(`deaths=1836`, `rocks=573`, `shiny=343`), ASan `--smoke` + `--localsmoke` чисто,
`make uishots` — капсула в верфи показывает `CG:12`.

---

## 21. Шкала лицензии — под устный счёт (2026-08-06)

Пред-релизная сессия минорных правок по скриншоту. Четыре замечания
пользователя, три из них — интерфейс, одно — баланс.

### 21.1 Тариф 10%, квота 10 000 — и что за это отдано

Дословно: «квота почему 40к когда надо 10к? и тариф сделай дефолт 10% а то
сложно будет игрокам считать».

```text
LICENCE_TARIFF_BASE  0.08  -> 0.10
LICENCE_QUOTA_BASE  40000  -> 10000
LICENCE_PRICE_K        25  -> 100     (вторая лицензия ровно 1 000 000 Cr)
```

Ставка 10% выбрана ради устного счёта: «отдаю десятину с каждой продажи»
считается без калькулятора, а строка `КВОТА 0/10000 CR … ТАРИФ 10%` делится на
десять в уме. `LICENCE_PRICE_K` двигается обратно квоте — это правило из §13.8
не менялось: миллион за второй борт остаётся design point.

⚠️ **Что при этом отдано.** Планка квоты со времён §13.8 задавалась НЕ суммой, а
числом РЕЙСОВ (~120 за период), и при каждом пересчёте масштаба её перемеряли
(§17.5, §19.5). Замер после этой правки: тариф с рейса **414 Cr ⇒ 24.1 рейса**.
То есть квота закрывается за четверть тысячелетия, и остаток периода игрока
больше ничто не подгоняет — механизм «квота держит в маршруте весь период»
ослаблен сознательно, в обмен на читаемость шкалы. Это решение пользователя,
принятое после того, как цена была названа. Проверка
«квота закрывается за разумное число рейсов» остаётся зелёной (её границы —
20..250 рейсов — ловят ПОЛОМКУ, а не точное число), но теперь стоит у нижнего
края коридора: опустишь квоту ещё — упрёшься в неё.

Хочешь вернуть строгость — считай по рейсам, а не по кредитам: при ставке 0.10
сотня рейсов это ~41 000 Cr, и `LICENCE_PRICE_K` тогда снова ~25.

**Старые сейвы не затронуты:** `licenceQuotaBase` и `licenceTariffRate` лежат в
файле сейва (они дрейфуют по ходу партии), поэтому новые числа получают только
новые партии. Формат сейва не менялся — сейв 14 в силе.

### 21.2 Интерфейс: убрано лишнее

- **Панель `СИСТЕМЫ КОРАБЛЯ` перестала рассказывать про добычу.** Там вечно
  висела строка `ДОБЫЧА ВЫКЛ (СТЫК + M)` — подсказка о клавише, которая в
  обзоре скопления не нужна: бурение живёт в локальном полёте и там же себя
  показывает (`MINING … GRADE …` над камнем, §20.1). Панель ужалась 120 → 106
  px, дальше по столбцу всё поехало вверх на 14.
- **Из списка задач убрано `ДОБЫЧА РУДЫ: СТЫКОВКА + M`**, из стартового журнала
  — `Press M in a system to mine ore…`. Про добычу игрок узнаёт из локального
  полёта, куда его зовёт подсвеченная задача `ЛЕТАТЬ В СИСТЕМЕ: L`.
  ⚠️ Сама макро-добыча по `M` на стоянке (`mining.cpp`) НЕ удалена и работает —
  она просто больше не рекламируется. Карточка `F1` её по-прежнему перечисляет.
- **Кнопка `AUTO` в окне торговли удалена.** `Game::agentAutoTrade` отказывает
  молча при непустом трюме, в полёте и когда `findBestTrade` не нашла сделки —
  для игрока это выглядело как «кнопка ничего не делает». Столбец действий
  сократился на ряд (`refuel`/`hold` поднялись на 36 px). Хоткей `T` на
  автоторговлю оставлен: он документирован карточкой `F1` и никого не путает.
- Из словаря `i18n.cpp` вычищены осиротевшие ключи `AUTO` и
  `MINE ORE: DOCK + PRESS M`.

### 21.3 Проверки

`make` 0 предупреждений, `make balance` **40/40 PASS** («квота закрывается за
разумное число рейсов»: квота 10000, тариф с рейса 414 ⇒ 24.1 рейса, доплата
15 000 Cr = 3.6 рейса выручки), `make uiclick` **44/44 PASS**, `make econ`
(strain 7% средний), `make soak` 120k `invariantFails=0` — счётчики побитово те
же (`deaths=1836`, `rocks=573`, `shiny=343`), ASan `--smoke` + `--localsmoke`
чисто, `make uishots` — HUD без строки добычи, окно торговли без `AUTO`.

---

## 22. Пред-релизные твики (2026-08-06): раскол астероида и размер скопления

Пользователь, готовя релиз: (1) в локальном режиме выработанный астероид не
разрушался — «висят пустые камни», нужен сочный шейдерный распад; (2) число
звёзд должно РАЗЫГРЫВАТЬСЯ: обычно около 10 000, потолок 16 384, пол 8 192.

### 22.1 Выработанная глыба выбывает из сцены

Раньше `rk.ore` уходил в ноль, печатался тост `ROCK DEPLETED` — и всё: та же
модель оставалась в поясе неотличимой от полной. Пояс превращался в кладбище
декораций, по которому игрок заново облетал уже выжатые камни (единственный
признак — отсутствие подсказки добычи при подлёте вплотную).

Теперь последняя тонна запускает раскол:

- `LocalRock::breakT` — РЕАЛЬНЫЕ секунды с момента выработки (0 = камень цел),
  `LocalRock::gone` — раскол доигран, камня в сцене нет. Обе величины считаются
  в блоке (G) `localsim.cpp` (дрейф пояса), там же камень становится `gone` по
  достижении `LocalCfg::ROCK_BREAK_TIME` (1.10 с).
- Время РЕАЛЬНОЕ, а не игровое (как `fxClock`/`hitFlash`): под warp (×60) камень
  не рассыпается за один кадр, зрелище остаётся зрелищем. Куски при этом
  продолжают дрейфовать с поясом — глыба разваливается на ходу.
- ⚠️ Камень гасится ФЛАГОМ, а не `erase()`: `scene.miningRock` и
  `scene.minePrompt` хранят ИНДЕКСЫ в `scene.rocks`, и удаление из вектора
  сдвинуло бы их на чужие камни. Все обходы пояса (выбор цели бура, подсказка,
  отрисовка) пропускают `gone`.
- `spawnRockBurst` (`localsim.cpp`, рядом с `spawnWreck`/`emitWarp`) — это НЕ
  взрыв корабля: ни лута, ни огненных обломков, камень не горит. Пыль и крошка
  цвета САМОЙ породы, тусклое кольцо осевшей пыли, и лишь горстка янтарных искр
  — раскалённый рез бура, гаснет первой. Скорости масштабированы радиусом глыбы,
  иначе камешек в 1.5 LU швырял бы крошку на сотню LU. Тряска — по близости.
- Тост стал `ROCK SHATTERED`; заодно в словарь добавлены `ROCK SHATTERED` и
  `CARGO FULL` — второй висел непереведённым с §14.

### 22.2 Шейдер раскола: клинья Вороного на той же ray-sphere

Ни одной новой модели и ни одной частицы-заменителя — распад считает тот же
попиксельный `renderRockLit`, что рисует целую глыбу:

1. Сфера делится на `ROCK_SHARD_COUNT` (10) клиньев Вороного по опорным
   направлениям спирали Фибоначчи, провёрнутой на seed камня (соседние глыбы
   рассыпаются по-разному).
2. Каждый клин РАЗЪЕЗЖАЕТСЯ вдоль своего направления: луч проверяется на
   пересечение со СМЕЩЁННОЙ сферой осколка и принимается, только если попал в
   СВОЙ клин (ближайшее опорное направление к нормали = номер осколка).
   Отсюда ключевое свойство: при `brk=0` смещения нулевые, объединение клиньев
   ПОБИТОВО равно исходной глыбе — ни скачка, ни «поп-ина» в момент раскола.
   Дальше между кусками открываются настоящие щели, сквозь которые виден фон.
3. Рёбра клиньев (разница первого и второго скалярных произведений) светятся
   раскалённым резом; эмиссия гаснет вчетверо быстрее разлёта (`exp(-brk*4)`) —
   первый кадр это сетка огненных трещин по ещё собранной глыбе, дальше просто
   разлетающийся холодный камень. Кромка при этом ЗАТЕМНЯЕТСЯ и после остывания
   — без этого осколки читались бумажными лепестками, а не кусками с толщиной.
4. Куски гаснут вразнобой (у каждого свой порог), альфа пишется попиксельно.
5. Дальняя крапинка и орто-карта клиньев не разглядят — там пятно вспухает и
   гаснет (`f *= 1-brk²`, радиус ×(1+0.9·brk)).

Стоимость: bbox растёт вместе с разлётом (иначе куски обрезались бы по краю
прежней рамки), поэтому добавлена отбраковка осколков СО СПИНЫ — клин, чьё
направление смотрит от глаза дальше чем на 90°+полуугол ячейки, не даёт ни
одного пикселя. Порог −0.80 взят с запасом против 45°-оценки полуугла; проверено
попиксельным сравнением: три фазовых скриншота с отбраковкой и без — идентичны.

⚠️ Ветка целого камня (`brk<=0`) не тронута ни одной операцией: тот же
ray-sphere, та же альфа 255, ни glow, ни затемнения кромки. Скриншоты
`shot_belt_near` / `shot_mining_beam` обязаны остаться прежними.

### 22.3 Скопление — одно число: 8 192 системы

Раздел пережил две правки пользователя подряд, и промежуточные редакции в коде искать не
надо, их нет:

1. Первая редакция РАЗЫГРЫВАЛА размер по seed (логнормально, 8 192…16 384, медиана 10 000).
   Пользователь, увидев цену потолка (§22.4), розыгрыш отменил — `rollStarCount`,
   `STAR_COUNT_MIN/MAX` и балансовая проверка разброса удалены.
2. Вторая держала ДВА числа: `STAR_COUNT = 10000` («опорное скопление», знаменатель
   плотности) и `WORLD_STAR_COUNT = 8192` (размер партии). Пользователь спросил, зачем
   опора, если мир такого размера не создаётся, — и был прав: опора делала ровно одну
   работу, служила знаменателем. Числа сведены в ОДНО.

Итог: **`STAR_COUNT = 8192`** — и размер мира, и мера полного мира. Меньшие миры (тесты,
харнесы: 200/300/400/1200) передают свой размер в `Game::init` и получают долю населения,
как и раньше.

⚠️ **Цели населения выражают ПЛОТНОСТЬ, а записаны абсолютом** — значит они живут со
`STAR_COUNT` в паре. Пользователь выбрал держать ВСЕ ТРИ числа мира степенями двойки:

| | было (на 10 000) | стало (на 8 192) | плотность была → стала |
|---|---|---|---|
| систем | 10 000 | **8 192** | — |
| `AGENT_TARGET_FULL` | 1024 | **1024** | 0.1024 → **0.125** борта/систему (+22%) |
| `CONTRACT_TARGET_FULL` | 320 | **256** | 0.032 → 0.03125 контракта/систему (−2%) |

Цена решения названа прямо, а не спрятана: **трафик НА СИСТЕМУ гуще на 22%**. В абсолюте
кораблей ровно столько же, сколько в вышедшей версии — замер после правки даёт **1011
бортов**, то есть буква в букву прежнее число; сгустился мир, а не выросла нагрузка.
Доска заданий не изменилась вовсе (−2% — шум округления). Промежуточный вариант с
«плотность до единицы прежняя» (838/262) отвергнут пользователем ради круглых чисел.

⚠️ Соак-бейзлайн от этого СДВИНУЛСЯ, и это ожидаемо: мир 1200 систем получает 140 бортов
вместо 121, отсюда `deaths=1836→1915`, `radioClaims=9→12`, `craftDestroyed=10→12`. Пояс не
затронут (`rocks=573`, `shiny=343` — прежние), `invariantFails=0`. Новые числа и есть
бейзлайн; расхождение с ними в будущем — сигнал.

Балансовая проверка `население мира идёт от его размера` пиньнит связку: полный мир
добирает свою цель (>90% и не выше её), а мир вчетверо меньше получает ту же ПЛОТНОСТЬ,
а не ту же кучу.

### 22.4 ⚠️ ПОЧЕМУ вниз, а не вверх: цена систем КВАДРАТИЧНА

Замерено (`Game::init`, seed 42):

| систем | пиковая память | генерация |
|---|---|---|
| **8 192 (партия)** | **509 МБ** | **5.28 с** |
| 10 000 (было) | 656 МБ | 7.37 с |
| 12 049 | 838 МБ | 11.1 с |
| 16 384 | ~1.2 ГБ | ~20 с |

Корень ОДИН и он не в новом коде: `Game::routeNextHop` — таблица следующего прыжка для
ВСЕХ ПАР систем, `N²` элементов по 2 байта (10 000 → 200 МБ, 16 384 → 537 МБ), и строится
она поиском 32 ближайших соседей перебором `N²` (`rebuildRouteCache`, `game.cpp`). Сама
симуляция масштабируется линейно (5 лет: 0.74 с при 10 000). Поэтому потолок 16 384 был
отвергнут, а шаг вниз до 8 192 снял **22% памяти и 28% времени генерации** даром.

⚠️ Хочешь мир БОЛЬШЕ — сперва чини корень: сетка вместо перебора соседей (время) и/или
маршрутизация по запросу вместо таблицы всех пар (память). И не забудь пересчитать цели
населения (§22.3) — они привязаны к `STAR_COUNT` плотностью, а не абсолютом. Наивная замена перебора МЕНЯЕТ
граф маршрутов для того же seed (порядок обхода решает ничьи между равноудалёнными
звёздами), то есть ломает воспроизводимость партий по `--seed N`.

### 22.5 Проверки

`make` 0 предупреждений, `make balance` **41/41 PASS** (+«население мира идёт от его
размера»: 8 192 → 1011 бортов = 0.1234/систему, 1200 → 140 = 0.1167/систему, доска
256→37), `make soak` 120k `invariantFails=0` под ASan/UBSan — новый бейзлайн `deaths=1915`, `radioClaims=12`, `craftDestroyed=12` (плотнее мир,
§22.3), пояс прежний `rocks=573`/`shiny=343`, новый зонд
`rock-shatter probe: gone=YES reselect-blocked=YES duration=1.10s (exp 1.10) burstFx=32`, `make econ` PASS, `make uiclick` PASS, `make shots` **40/40**
(+3 фазы раскола: `shot_rock_break_a/b/c`), `make shot_asan` 40/40 чисто,
ASan `--localsmoke` exit 0.

---

## 23. Заказы: доска ожила, журнал заговорил (2026-08-07)

### 23.0 Сначала — терминология

**«Заказ» и «контракт» — ОДНО И ТО ЖЕ.** Сущность в коде одна (`Contract`,
`contract.h`), окно одно (`WindowKind::Contracts`, кнопка `JOBS`), доска одна.
Двоилось только в словаре: кнопка переводилась «ЗАКАЗЫ», а все события —
«КОНТРАКТ ПРИНЯТ / ВЫПОЛНЕН / ПРОСРОЧЕН». Игрок видел два слова и искал за ними
две механики. Словарь сведён к одному слову — **ЗАКАЗ** (`i18n.cpp`).

Типов заказа семь (`ContractType`): `Delivery`, `ColonySupply` (везут груз),
`Courier`, `Scout`, `Bounty`, `Escort`, `Raid`. В доске они пишутся тремя
буквами (`DEL`/`CUR`/`SCT`…, `contractTypeLabel` в `game.cpp`), в журнале —
словом целиком: словарь §14 переводит только целые слова, и «DEL» осталось бы
латиницей.

### 23.1 Что нашлось: доска работала и была бесполезна

Механика была цела — заказ брался, груз грузился, доставка платила. Мёртвым
было СОДЕРЖАНИЕ, и обе поломки невидимы глазами.

**A. Заказ проигрывал свободной торговле в 10..30 раз.** Замер на трёх сидах,
кошелёк выдан и сделка исполнена по-настоящему (§19.4):

| | Cr за ГОД полёта |
|---|---|
| заказ (было) | **0.5 .. 1.7** |
| свободный рейс, кошелёк 100 Cr | 7.6 .. 23.2 |
| свободный рейс, кошелёк 10 000 Cr | 88 .. 200 |

И это ещё лестно: заказ ЗАНИМАЕТ трюм, то есть торговать одновременно нельзя.
Заказ был строго доминируем — брать его не следовало никогда. Причина:
награда собиралась из литералов вида `distance * 3.5`, то есть платила за
расстояние по линейке, а не за годы жизни, которые рейс отнимает.

**B. Девять сроков из пятнадцати нельзя было выдержать в принципе.** Срок был
прямой `distance / 0.11 + 3` (у курьера `/0.16`, у разведки `/0.22`), а полёт
в игре считается моделью с разгоном и торможением. На коротких плечах разгон
и торможение — почти весь рейс, и в линейной формуле их нет вовсе.

**C. Взятый заказ не проваливался НИКОГДА.** `updateContracts` жёг по сроку
только НЕВЗЯТЫЕ объявления. Замер: 443 года сверх срока — `failed=0`, заказ
всё ещё на руках и готов заплатить свои 45%. Срок был ценником, а не
обязательством.

### 23.2 Как починено

Всё считается ОДНОЙ величиной — **опорным рейсом**: сколько лет уйдёт на этот
маршрут у стартового «Hauler» с полным трюмом (`contractReferenceShip`,
`contractRouteYears`, `game.cpp`). Заказчик не знает, кто возьмётся, поэтому
мерит рейс по обычному грузовику — но ТОЙ ЖЕ моделью полёта, что и ETA игрока.

- **плата** = `CONTRACT_PAY_PER_YEAR` (9.0) × годы опорного рейса, плюс
  надбавки каждого типа (дефицит, ценность неизвестного, вражда, угроза) —
  они объясняют, чем ЭТОТ заказ особенный;
- **срок** = опорный рейс × `CONTRACT_DEADLINE_SLACK` (1.35); охоте, конвою и
  разведке сверх этого даётся ещё четверть рейса — им мало долететь;
- **провал**: сверх срока даётся ещё один такой же рейс
  (`CONTRACT_GRACE_FACTOR`), и заказ сгорает. Просрочка внутри запаса платит
  `CONTRACT_LATE_FACTOR` (0.45), как и раньше.

⚠️ `contractRouteYears` считает через `plannedRouteTravelTime`, а НЕ через
`cachedRouteTravelTime`. Вторая всегда ведёт по прыжкам, а прыжковый маршрут
разгоняется и тормозит на каждом плече — срок расходился с реальным рейсом
в полтора раза.

Итог замера после правки:

| | было | стало |
|---|---|---|
| заказ, Cr/год полёта | 0.5 .. 1.7 | **6.8 .. 13.6** |
| доля от свободного рейса нищего игрока | 0.03x .. 0.1x | **0.42x** |
| выполнимо по сроку | 6 из 15 | **15 из 15** |
| взятый заказ может сгореть | нет | **да** |

Заказ теперь ровня небогатому рейсу и намеренно НЕ богаче: он не требует ни
капитала (груз выдают), ни знания рынка (цель названа), ни угадывания спреда.
Платил бы он больше торговли — исчез бы смысл торговать.

### 23.3 Журнал стал журналом

Был кассовой лентой: `[ГОД] @ система : ±N Cr` и ничего больше. По ней нельзя
было понять, что игрок везёт и куда, — а терялось именно это: взял заказ,
улетел, забыл.

У строки журнала появился ВИД (`enum JournalKind`, `game.h`) и свой текст:

| вид | цвет | пример |
|---|---|---|
| `JobAccepted` | **белый** | `ВЗЯТ #3 ДОСТАВКА 160 LI > STAR_193 СРОК 140Y` |
| `JobCompleted` | **зелёный** | `#1 ДОСТАВКА 160 LI > STAR_35 +1980 CR` |
| `JobFailed` | **красный** | `#2 ДОСТАВКА 160 LI > STAR_150 ПРОСРОЧЕН` |
| `Money` | зелёный/красный | `-1240 CR` — касса как была |

Текст собирается ПО-АНГЛИЙСКИ (`Game::contractJournalText`), переводит его сам
словарь в `UI::drawText` (§14); имена звёзд, номера и числа он не трогает.

⚠️ **Одна выплата — одна строка.** Касса пишется из разницы по кошельку за
такт, поэтому награда за заказ попадала бы в журнал ДВАЖДЫ: зелёной строкой и
безымянной кассовой. Отсюда `Game::journalExplained` — сумма, уже объяснённая
отдельной строкой; она вычитается из кассовой разницы и обнуляется каждый такт.
Проверка `testJournalCountsPayoutOnce` пиньнит ровно это.

### 23.4 Проверки

`make` 0 предупреждений, `make balance` **41/41 PASS** (+4 новых),
`make uiclick` 43/43 PASS, `make econ` PASS (strain 7% средний),
`make soak` 120k `invariantFails=0` под ASan/UBSan (`deaths=1915` — бейзлайн
§22 не сдвинулся), ASan `--smoke` + `--localsmoke` exit 0,
`make uishots` — журнал снят ЗАПОЛНЕННЫМ (три цвета, русские строки в рамке).

Новые регрессии в `balance_test.cpp`:

- `testJobPaysLikeARun` — заказ соизмерим свободному рейсу ТОГО ЖЕ кошелька
  (полоса 0.25x..3.0x — ловим механизм, не число);
- `testJobDeadlinesAreReachable` — ≥80% видимых заказов выполнимы стартовым
  кораблём;
- `testTakenJobCanExpire` — взятый заказ сгорает и пишет красную строку;
- `testJournalCountsPayoutOnce` — выплата в журнале ровно один раз.

⚠️ Мерить плату за заказ можно ТОЛЬКО в Cr за ГОД ПОЛЁТА и только против
свободного рейса того же кошелька (§17.5 п.2 + §19.4). В кредитах эти
константы бессмысленны и уедут при первом пересчёте масштаба мира.

### 23.5 Что осталось знать

- **Журнал не переживает сохранение.** `transactions` нигде не пишется в сейв —
  так было и до §23, формат сейва не трогался. Загрузился — журнал пуст.
- **Провал взятого заказа теперь случается и у NPC.** Груз при этом остаётся у
  них в трюме — заказчик его не отбирает. Побочный доход, но копеечный: NPC
  берут дешёвый профицит.
- **`contract.deposit` — мёртвое поле**, везде 0. Задаток не реализован ни для
  кого; если понадобится подпирать срок деньгами игрока — вот готовое место.
- **`pickSurplusResource` выбирает груз по МАЛОЙ удельной массе**, поэтому
  доставка почти всегда возит водород и лёгкие газы. Это тот же перекос
  плотности стоимости, что описан в §20.3-A, и чинится он там же, а не здесь.

---

## 24. Репутация возчика: заказы стали геймплеем (2026-08-07)

### 24.0 Что это и зачем

§23 починил доску заказов, но она осталась ПЛОСКОЙ: заказ на сотом часу игры
был неотличим от заказа на первой минуте. §24 превращает её в ось развития —
репутация растёт от сдач, открывает работу на порядки крупнее, и на верху
лестницы заказ уже не влезает в один корабль.

Все развилки здесь — решения пользователя, и они перечислены явно, потому что
«почему так» здесь важнее «как»:

| развилка | выбор |
|---|---|
| репутация | **своя у каждой фракции** (не одна на скопление) |
| гигантский заказ | **порог по суммарной вместимости флота В СИСТЕМЕ** |
| «овертайм» | **оба**: срочные заказы дороже + надбавка за досрочную сдачу |
| длина грайнда | **1000 заказов** до потолка («гринд есть гринд») |
| форма кривой | **рано видно, верх далеко** (показатель ~sqrt) |
| штраф за провал | **по размеру заказа, ×2** |
| казна фракции | **потолок награды** |
| размер доски | **8 заказов на систему** (было 4–5 вразнобой) |

### 24.1 Репутация

`Game::factionReputation` — одно число на фракцию, в СДАННЫХ ЗАКАЗАХ.

- **успех: всегда +1**, независимо от размера. Единица шкалы — один сданный
  заказ, и потолок (`REPUTATION_CAP_JOBS` = 1000) честно стоит своей тысячи;
- **провал: −`2 * sqrt(масса / JOB_CARGO_BASE)`**, то есть ПО РАЗМЕРУ. Замер:
  базовый заказ −2, средний −45, топовый −163 сданных.

Асимметрия здесь — не небрежность, а весь смысл верха лестницы: репутацию,
которую копил тысячу заказов, ты ставишь на кон каждым крупным рейсом.

Заказчик, тир и репутация — ВСЕГДА ОДНА ФРАКЦИЯ: владелец системы, где висит
доска. До §24 доставку выдавал владелец ЦЕЛИ, и с репутацией это разъехалось бы
насмерть (тир открывает одна фракция, репутация растёт у другой).

### 24.2 Тир — непрерывная функция, а не ступеньки

```
тир = sqrt(репутация / 1000)          # «рано видно, верх далеко»
масса = 60 * (400000/60) ^ тир        # экспонента: равные шаги = равные проценты
плата = плата за рейс * 200000 ^ тир  # верх обязан бить торговлю
```

Замер кривой:

| сдано | тир | звание | трюм заказа | какой корпус |
|---|---|---|---|---|
| 0 | 0.00 | НИКТО | 60 т | Hauler (старт, 110) |
| 10 | 0.10 | ЗНАКОМЫЙ | 145 т | Light Freighter |
| 50 | 0.22 | ДОВЕРЕННЫЙ | 430 т | Medium Freighter |
| 200 | 0.45 | ПОДРЯДЧИК | 3 080 т | Heavy Freighter |
| 500 | 0.71 | МАСТЕР | 30 300 т | 2× Megafreighter |
| 750 | 0.87 | ЛЕГЕНДА | 123 000 т | Giga-Freighter |
| 1000 | 1.00 | ПЕРВЫЙ ВОЗЧИК | 400 000 т | ~3× Giga-Freighter |

Тир КОНКРЕТНОГО заказа кидается случайно в пределах потолка, и бросок смещён
вниз кубом (`rollContractTier`). Отсюда «базовые заказы и с урежением высокие»
БЕЗ отдельного списка редкостей и без второго слота на доске: у заслуженного
возчика на доске по-прежнему в основном обычная работа.

Живой замер доски (seed 42, потолок): 8 строк, из них семь на 0–1 700 т и одна
на **336 400 т за 640 199 716 Cr**.

### 24.3 Груз — по НУЖДАМ рынка, а не по массе

`pickSurplusResource` (выбор по `(1.18-давление)*sqrt(запас)/удельная масса`)
заменён на `pickNeededResource`. Прежняя формула ДЕЛИЛА НА МАССУ и потому почти
всегда выдавала водород и лёгкие газы: заказ означал не «системе нужен вольфрам»,
а «у нас завалялось что полегче». Теперь ведущая величина — нужда ЦЕЛИ
(`demandRate`, незакрытый спрос, ценовое давление, `strain`), голод считается в
МАССЕ, а масса из выбора убрана совсем: сколько везти — решает тир.

Порядок в генераторе стал обратным прежнему и это суть §24:
**величина (тир) → цель на нужной дальности → что ей нужно.**
Дальность — вторая ось тира: базовый заказ к соседям, топовый через всё
скопление (`contractRangeShare`, у курьера и разведки — через штраф за
расстояние, тающий с тиром).

### 24.4 Казна фракции — потолок награды

`finishContract` обрезает награду 35% казны заказчика. Фракция не выставляет
заказ, который не может оплатить, поэтому миллиардные заказы водятся только у
богатых и редко: «урежение» верха выходит из ЭКОНОМИКИ, а не из искусственного
рандома, и богатая фракция становится целью паломничества.

### 24.5 Овертайм — оба механизма

- **СРОЧНЫЙ заказ** (`JOB_RUSH_CHANCE` = 22%): срок режется до
  `JOB_RUSH_DEADLINE`, ставка растёт в `JOB_RUSH_PAY` = 1.7. Метится словом
  `RUSH` и красным цветом строки — выбор делается на берегу, а не в полёте.
- **ДОСРОЧНАЯ сдача**: надбавка до +50% (`JOB_EARLY_BONUS_MAX`), тем выше, чем
  больше запаса срока осталось.

⚠️ **Срез срока нельзя ставить «на глаз».** Первая попытка резала до 0.62, то
есть до запаса 0.84 от опорного рейса — МЕНЬШЕ, чем нужно самому быстрому
прогону. Срочный заказ становился не азартом, а ловушкой: 1.7 ставки × 0.45
просрочки = 0.77, гарантированно ХУЖЕ обычного. Замер поймал сразу (17
сдаваемых из 24). При 0.80 запас выходит 1.08 — успеешь на хорошем прогоне и не
успеешь на плохом, что и есть ставка.

### 24.6 Флот как ёмкость

`playerFleetCapacityAt(star)` — суммарный СВОБОДНЫЙ трюм бортов игрока,
СТОЯЩИХ в этой системе (борт в пути не считается, его здесь нет). Заказ берётся,
только если флот покрывает объём; груз режется по бортам (капитан грузится
первым), носители записываются в `Contract::carriers`, и все, кроме капитанского,
уходят к цели САМИ. Сдать можно, только когда ВЕСЬ караван на месте.

⚠️ **Флот — ТОЛЬКО У ИГРОКА.** Первая версия этого не различала, и получилось
буквально: NPC брал заказ, а «караваном» ему записывался стоявший рядом корабль
ИГРОКА — тот молча улетал с чужим грузом. Доска у игрока пустела навсегда,
потому что он вечно был в пути. Симптом в замере: `enRoute=1`,
`last='hauling contract'` через год после старта партии.

⚠️ **`contractRouteShip` обязана клампить груз своим трюмом.** Иначе прогноз
крупного заказа считается для корабля с сорокакратным перегрузом, ETA уходит в
бесконечность, и на доске крупный заказ выглядит нелетающим.

### 24.7 Сейв 15 — и старые сохранения живы

Версия 14 → 15. Всё новое (тир, срочность, носители, репутация, ЖУРНАЛ) лежит в
ОТДЕЛЬНЫХ блоках `JOBEXTRA` / `REPUTATION` / `JOURNAL`, а не дописано в конец
строк `CONTRACT`/`FACTION`. Поэтому загрузчик читает и 14, и 15: нет блока —
остаются значения по умолчанию, и чужая партия продолжается как заказы нулевой
репутации. Привязка тира к заказу идёт ПО ID, а не по индексу.

Журнал (§23) теперь тоже сохраняется — это была явная дыра §23.5.

### 24.8 Проверки

`make` 0 предупреждений, `make balance` **46/46 PASS** (+5 новых),
`make uiclick` 43/43 PASS, `make soak` 120k `invariantFails=0` под ASan/UBSan
(`deaths=1915` — бейзлайн §22 не сдвинулся), ASan `--smoke` + `--localsmoke`
exit 0, `make uishots` (панель фракций и шапка доски сняты с НЕНУЛЕВОЙ
репутацией — на нулях они показывают одно слово и не проверяют ширину званий).

Новые регрессии:

- `testJobTierCurveSpansTheHullLadder` — кривая монотонна и кроет лестницу от
  стартового трюма до «не влезает ни в один достижимый корпус»;
- `testFailureCostsMoreThanSuccessGives` — асимметрия успеха и провала цела;
- `testReputationUnlocksBiggerJobs` — на живом мире, не на формуле;
- `testFleetCapacityGatesBigJobs` — один борт получает отказ, два берут заказ,
  носителей ровно два;
- `testSaveKeepsReputationAndJournal` — round-trip репутации, журнала и тира.

⚠️ Мерить `JOB_TIER_PAY_TOP` можно ТОЛЬКО от целевой суммы наверху. Первая
попытка стояла на 260 «на глаз» и дала верх в 2.3 млн — в 650 раз мимо
миллиарда. Считай: топовый опорный рейс ~800 лет × 9 Cr/год = 7 200 Cr платы за
рейс, значит для 1.5 млрд нужен множитель ~200 000.

### 24.9 Что осталось знать

- **Обрыв на самой верхней ступени.** Замер бутстрапа (сколько заказов тира T
  окупают флот под тир T+0.1): 0.0 → 37, 0.1 → 10, 0.2–0.5 → 1–4, 0.6 → 9,
  0.7 → 12, 0.8 → 7, а **0.9 → 93**. Причина не в §24, а в лестнице корпусов:
  между Megafreighter (80 млн) и Giga-Freighter (12 МЛРД) стоит скачок в 150
  раз. 93 топовых заказа укладываются в те ~190 сдач, что и так остались до
  потолка, поэтому лестница не рвётся — но это самое узкое её место.
- **Фантазийный верх лестницы корпусов заказами не окупается никогда.**
  Tera-Freighter — 2 000 000 т за 8 ТРИЛЛИОНОВ, Fortress — за 900 триллионов,
  при топовом заказе порядка 6e8. Регрессия намеренно меряет только корпуса
  дешевле 100 млрд.
- **Репутация не тратится и не гниёт.** Ни распада со временем, ни трат на
  что-либо, кроме доступа к заказам. Если понадобится — вот место.
- **NPC тир не получает**: у них нет репутации, `rollContractTier` для них
  всегда даёт потолок фракции, а берут они по своему трюму. Крупные заказы для
  NPC физически неподъёмны, и они просто висят на доске до срока.
- **`contract.deposit` по-прежнему мёртвое поле** (§23.5).

---

## 25. Имена систем: фонемный генератор (2026-08-07)

### 25.0 Что это и зачем

До §25 система звалась `Star_417`. Восемь тысяч таких имён (§22) — это не имена,
а индексы: игрок не мог сказать «та, где дёшево железо», он мог только записать
номер. Теперь перед номером стоит произносимое слово: **`Varen-417`** — и то же
самое имя по-русски **«Варен-417»**.

Номер оставлен НАМЕРЕННО, а не как отладочный хвост: по нему систему ищут в
списках и называют вслух, когда основа вылетела из головы. Два ключа к одной
системе — звук и число, — и оба человеческие. Слово `Star` при этом убрано:
в интерфейсе и так всё про звёзды, и приставка отъедала ширину панелей.

### 25.1 Имя собирается фонемами, а не буквами

`starNameFor(seed, index)` в `cluster.cpp`. Слог = **начало (может быть пустым) +
гласная + необязательный конец**, слогов 1–3 (12% / 54% / 34%).

Отсюда «человечность»: в имени физически не может оказаться «аывгрш». Каждый
слог открывается согласной и закрывается гласной, а стечения согласных берутся
только из готового списка (`br`, `kl`, `str`), а не собираются случайно.

Правила, каждое из которых закрывает конкретного уродца, вылезшего при отладке:

| правило | что чинит |
|---|---|
| после закрытого слога начало следующего — только ОДИНОЧНАЯ согласная | `Marnza` |
| начало слога не повторяет последнюю букву предыдущего | `Gill` + `la` = `Gillla` |
| `s`+`h`, `t`+`s`, `c`+`h` на стыке — перекат | склейка в чужую фонему `sh` |
| `h` не встаёт сразу после закрытого слога | `Gonnhi` |
| двойные концы (`ll`, `ss`, `nd`, `rk`) — ТОЛЬКО в конце слова | `Arkgullte` |
| дифтонг (`ai`, `ou`, `ia`) — не больше ОДНОГО на имя | `Kiamauchem` |
| слог с голой гласной — только первый и только чистой гласной | `Iogevun` |
| потолок основы 9 символов | `Spolouhass` |

Разброс длин на 24 576 именах (3 зерна): 3 — 1948, 4 — 4303, 5 — 4821,
6 — 4661, 7 — 3851, 8 — 3233, 9 — 1744, длиннее — 15 штук (все с апострофом).
Апостроф внутри слова (`To'zoute`) выпадает у ~10% трёхсложных: «разные знаки»
из задания, но редко — иначе он перестаёт быть особой приметой.

### 25.2 Генератор НЕ трогает поток случайных чисел скопления

Имя — чистая функция от `(seed, index)` на собственном splitmix64. Это не
эстетика: `Cluster::generate` тиражирует население, спектр и руду ОДНИМ потоком
`mt19937`, и лишнее обращение к нему сдвинуло бы весь мир (см. §17.5). Своя
последовательность на имя означает ещё и то, что имя звезды #417 одинаково в
сейве, в новой партии с тем же зерном и в любом харнесе.

### 25.3 Русский: одна таблица, а не транслитерация наугад

Таблицы фонем **парные** — `{"gr", "гр"}`, `{"ou", "оу"}`. `starNameCyrillic`
перечитывает готовую латиницу той же таблицей жадно слева направо (длинные
куски первыми, иначе `sh` распадётся на «сх»). Поэтому кириллическая запись —
не догадка по буквам, а тот же самый набор кусков.

Дальше работает механика §14: **имя переводится там же, где и весь текст —
внутри `drawText`**, поэтому в кириллицу оно попадает РАЗОМ везде: в заголовке
панели системы, в заказах, в журнале, в новостях, в целях полёта, в именах
планет (`localgen` строит их как `имя + "-" + номер`) и колоний. Ни одно из
сорока мест вызова `star.name` править не пришлось.

Для этого в i18n добавлен **реестр имён собственных**:

- `I18N::registerProperNoun(en, ru)` — отдельный словарь, наполняемый на лету;
- `Cluster::registerNames()` зовётся из `generate()` (со сбросом реестра — новый
  мир, новые имена) и **повторно после загрузки сейва**: там имена приходят из
  файла, а не из зерна, и могут быть сняты другой сборкой;
- ключ — основа БЕЗ номера (`VAREN`), потому что `-` разбивает токен; номер
  дописывается сам, а варианты `Varen-417-3` и `Varen-417_Prime` собираются из
  того же ключа;
- кэш переводов `tr` вынесен наружу и чистится при регистрации: запись
  «ЦЕЛЬ: VAREN-417», сделанная ДО регистрации, пережила бы её.

### 25.4 Грабли: имя не должно совпасть со словом интерфейса

Словарь §14 не знает, что `MINE` в строке — имя собственное, и система по имени
Mine превратилась бы в «ШАХТУ-417» — причём и в заказах, и в журнале. Поэтому
генератор спрашивает `I18N::isInterfaceWord()` ЗАРАНЕЕ и перекатывает имя (до 6
попыток, на последней просто добавляет слог — цикл обязан быть конечным при
любом словаре). Проверено на 32 768 именах (4 зерна): **0 совпадений**, и все
32 768 переводятся в кириллицу целиком, без латинских огрызков.

Обратная защита стоит в `registerProperNoun`: регистрация ключа, который уже
что-то значит в интерфейсе, игнорируется. Она нужна для СТАРЫХ сейвов, где имя
пришло из файла и генератор его не проверял.

### 25.5 Грабли: реестр пишется ИЗ ФОНОВОГО ПОТОКА

Самое опасное в §25 — не фонетика. Мир генерируется в рабочем потоке (§11:
`shell.cpp` запускает `init`/`loadFromFile` в `std::thread`, пока главный поток
рисует комикс). Значит, имена регистрируются ИЗ ФОНОВОГО ПОТОКА ровно тогда,
когда главный переводит подписи комикса, — и таблицы i18n впервые в жизни
оказались общими для двух потоков. Вставка в `unordered_map` во время чужого
`find` — не «редкая рассинхронизация», а падение с рехэшем.

Починено тремя вещами:

- **замок** (`dictMutex`) на весь разбор строки в `translate`, на регистрацию и
  на `isInterfaceWord`. Один захват на строку, а не на токен: `translate`
  зовётся только при промахе кэша, поэтому на кадр приходятся единицы захватов;
- **кэш переводов стал `thread_local`**. Чужой поток не должен иметь права
  выбросить строку, ссылку на которую уже вернул `tr` (сигнатура —
  `const std::string&`, и ссылка живёт до следующего кадра);
- **счётчик поколений** реестра вместо общей чистки кэша: чужие кэши не
  достать, поэтому поток сам сравнивает поколение и чистит свой.

Проверено под ThreadSanitizer: стенд «поток генерирует три скопления, пока
второй непрерывно переводит» — 0 предупреждений. Контрольный стенд с
незащищённой картой на том же компиляторе даёт 7 — то есть проверка не пустая.

### 25.6 Своё имя своей системе

Купил систему — назови её. В окне собственности у СВОЕЙ системы появилось поле
`SYSTEM NAME` («ИМЯ СИСТЕМЫ»): клик — правка, Enter — применить, Esc — отмена,
клик мимо поля — тихая отмена. Правится только ОСНОВА: номер приписывается сам,
потому что он адрес системы в списках, заказах и журнале, а не украшение.

Три решения, каждое из которых можно было принять иначе:

- **Только стоя в системе.** Как и касса, и покупка: «империю приходится
  облетать — она маршрут, а не строка в меню» (§13). Не на месте — поле
  показывает `RENAME ON SITE ONLY` и не пускает.
- **12 БУКВ, а не 12 байт.** Имя можно набрать кириллицей, где буква — два
  байта; лимит в байтах урезал бы русское имя вдвое, а `backspace` по байту
  оставлял бы в имени битую половину буквы (и она дошла бы до сейва).
- **Пробел не пропускается.** Имя системы — одно слово: с пробелом оно стало бы
  оборотом из двух токенов, и словарь §14 разбирал бы его по частям.

Введённое имя идёт в тот же реестр (`registerProperNoun`), что и сгенерированные,
поэтому `HOME-212` тут же читается как «ХОМЕ-212». Регистрируется только
латиница: кириллическое имя словарю не нужно — он ключуется латиницей, и
кириллический токен `wordByte` даже не собирает.

⚠️ Единственная щель: имя, набранное латиницей и совпавшее СО СЛОВОМ интерфейса
(`MINE`), в русской игре покажется переводом этого слова («ШАХТА-212»).
Генератор от этого защищён перекатом (§25.4), а игрока не перекатишь — но
попасть в щель можно только играя по-русски и набрав при этом английское слово
из словаря.

### 25.7 Что осталось знать

- **Апостроф в сейве безопасен**: `saveToken` экранирует только пробелы и `%`.
  Старый сейв с `Star_417` тоже откроется — имя просто останется латиницей.
- **Уникальность основ не гарантируется и не нужна**: на 8192 системах основа
  повторяется примерно у 300 (7873 различных). Различает системы номер, а
  тёзки — это как раз то, как выглядит настоящий каталог.
- **Шрифт всё равно рисует заглавными** (`glyphCp` складывает строчные в
  прописные, в том числе кириллицу), поэтому регистр в имени — вопрос кода и
  логов, а не картинки.

### 25.8 Проверки

`make` 0 предупреждений, `make balance` **49/49 PASS** (+3 новых),
`make uiclick` 43/43 PASS, `make soak` 120k `invariantFails=0` под ASan/UBSan
(`deaths=1915` — бейзлайн §22/§24 не сдвинулся), ASan `--smoke` + `--localsmoke`
exit 0, `make uishots` — имена стоят кириллицей в заголовке панели системы, в
доске заказов и в строке маршрута.

Новые регрессии:

- `testStarNamesAreNamesNotIndices` — 8192 имени: формат `основа-номер`, в
  каждой основе есть гласная, нет утроенных букв, ни одного совпадения со
  словом интерфейса, имя детерминировано от `(seed, index)`;
- `testStarNamesSpeakRussianAfterLoad` — имена переживают сейв БАЙТ В БАЙТ и
  после загрузки переводятся внутри фразы («ЦЕЛЬ: Зонзил-3»), без латинских
  огрызков. Именно эта проверка ловит забытый `registerNames()` на загрузке;
- `testPlayerRenamesOwnSystem` — переименование СВОЕЙ системы сохраняет номер и
  сразу звучит по-русски, а три отказа (чужая система, пустое имя, попытка
  издалека) остаются отказами.

Окно собственности снимается теперь ДВАЖДЫ (`uishot_colonyown_*`,
`uishot_colonyrename_*`): в общем списке система чужая, и правой колонки с
кассой и полем имени там просто нет. Обе подсказки поля уложены в 28 знаков —
ширину колонки — на обоих языках.

---

## 26. Вступительная новелла: топливо, квота и всё остальное (2026-08-07)

### 26.0 Чего в ней не хватало

Новелла Тимертии — единственный обучающий текст игры (`getTutorialText` в
`ui.cpp`). До §26 в ней было 15 реплик, и она рассказывала ровно про одно:
баланс, панель систем корабля, окно системы и рынок с равновесием Нэша.

При этом ДВЕ вещи, которыми игра наказывает новичка, в ней отсутствовали:

1. **Квота лицензии** (§21). Главный таймер партии. Игрок узнавал о ней, только
   когда терминалы гасли и появлялась красная строка `ЛИЦЕНЗИЯ ОТОЗВАНА`.
2. **Топливо и рабочее тело** (§12). Самая дорогая для новичка модель: маршрут
   отказывается прокладываться (`route blocked: drive cannot reach on this
   propellant`), а ПОЧЕМУ — нигде не сказано. Слова «бункер» и «бак» игрок
   встречал впервые в окне `ТРЮМ / БАКИ`, если вообще его находил.

Плюс молчание про заказы (§23, §24), локальный полёт (`L`) и покупку системы
(`C`) — всё это жило только в панели `ЗАДАЧИ` строкой в три слова.

### 26.1 Что стало

28 реплик (шаги 0..27, константа `TUTORIAL_LAST_STEP`), четыре блока:

| Шаги | О чём | Что открывает |
|---|---|---|
| 0–5 | знакомство, баланс, **квота**, панель систем корабля | стрелка на строку `КВОТА` |
| 6–11 | система, рынок, таблица элементов, что купить и куда везти | окно системы, окно рынка |
| **12–18** | **бункер / трюм / бак, что горит и что выбрасывается, `КУПИТЬ ТОПЛ+РАБ.ТЕЛО`, ручки `ТЯГА` и `КРЕЙСЕР`, `ОПТИМУМ` и запирание в рейсе** | **окно `ТРЮМ / БАКИ`** |
| 19–21 | заказы, как их мерить (Cr/год полёта), потолок по репутации | **доска `ЗАКАЗЫ`** |
| 22–27 | локальный полёт `L`, верфь `U`, покупка системы `C`, хромоядра, `F1` | — |

Топливный блок открывает окно ТЕМ ЖЕ приёмом, что раньше применялся к системе и
рынку: рассказывать про бункер и бак, пока их не видно, бесполезно.

### 26.2 `TutorialCue` вместо трёх out-параметров

Было: `getTutorialText(game, step, int& arrow, bool& openSystem, bool& openTrade)`.
С двумя новыми окнами параметров стало бы пять. Заменено на

```cpp
struct TutorialCue {
    int arrow = 0;                              // 0 — без стрелки
    bool openWindow = false;
    WindowKind window = WindowKind::SystemInfo; // значимо только при openWindow
};
```

Открытие окна — один `switch` по `cue.window` в `advanceVisualNovel`. Добавить
шаг с новым окном теперь стоит одной строки в `case`.

Стрелка-указка получила цель **6** — строка квоты в панели `СТАТУС ИГРОКА`
(`y=126`; сама панель стоит на `y=84`, строка внутри неё на `+45`). Заодно
`<-- TARGET` попало в словарь как `<-- СЮДА`: слова `TARGET` в одиночку в
словаре нет, и указка на русском оставалась латиницей.

### 26.3 Грабля: русский текст меряется по УЗКОМУ окну

Коробка диалога — фиксированные 150 px: заголовок `ТИМЕРТИЯ`, текст с `+60`,
строка 16 px. Влезает **пять строк**. Ширина строки считается как
`(ширина_окна - 80) / 12` знаков, то есть на 1440 px это 113 знаков и не
переполнится вообще ничего.

Поэтому в `ui_shot_test.cpp` меряется НЕ снимаемая ширина, а `NARROW_W = 960`:
73 знака в строке. Самая длинная реплика (шаг 3, квота) ложится в **3 строки из
5** на обоих языках. Харнес прогоняет все 28 шагов и печатает
`VN OVERFLOW: step N takes M lines`, если реплика не влезла.

Снимков новеллы два:

- `uishot_vn_*` — самая длинная реплика (проверка коробки);
- `uishot_vnfuel_*` — шаг 16 (ручки), где новелла сама открыла `ТРЮМ / БАКИ`:
  видно, что коробка диалога легла между колонками ёмкостей и ручками, и обе
  части окна остались читаемыми.

Ради харнеса `wrapText` вынесен в `ui.h`, а `drawVisualNovel` вызывается с
`tex = NULL` — портрет к раскладке текста отношения не имеет.

### 26.4 Открытый вопрос к плейтесту

Комикс пролога (§11) плюс 28 реплик подряд — это много до первого действия
игрока. Ранняя заметка §7 «стоит посмотреть на плейтесте, не многовато ли»
теперь стоит вдвое острее. Если резать — резать блоками: 19–21 (заказы) и 22–27
(остальное) можно отложить до первого захода в порт, топливный блок 12–18 —
нельзя, без него не улетишь.

### 26.5 Проверки

`make` 0 предупреждений, `make uiclick` 43/43 PASS, `make balance` 49/49 PASS,
`make uishots` — обе новые картинки, `VN OVERFLOW` не сработал ни на одном из
28 шагов ни на одном языке. Все 13 новых реплик заведены в словарь `EXACT`
(`i18n.cpp`); термины в русском тексте взяты ровно те, что подписаны в окне —
`БУНКЕР`, `ТРЮМ`, `БАК`, `ТЯГА`, `КРЕЙСЕР`, `ОПТИМУМ`, `КУПИТЬ ТОПЛ+РАБ.ТЕЛО`,
`ЗАКАЗЫ`, `КВОТА`.

---

## 27. Совет Тимертии перестал врать (2026-08-07)

### 27.0 Симптом

Скриншот игрока: на стартовой системе Тимертия говорит «пик предложения ВОДОРОД
в Суром-822, а высший спрос на ВОДОРОД — в Суром-822». Одна и та же система на
обеих половинах сводки, и обе про водород.

### 27.1 Причина: остров старого кода

Три реплики новеллы (`ui.cpp`, шаги 10, 11 и 100) остались написаны ДО ценовой
модели и ранжировали рынки **массой запаса и массой спроса**:

- у водорода запаса 1.8e9 против сотен у металлов ⇒ «покупай» всегда указывало
  на водород. Это ровно грабля §24.3 (`pickSurplusResource` делил на массу и
  выдавал водород), оставшаяся непочиненной во втором месте;
- потребление в массе тоже максимально у самой населённой системы ⇒ «продавай»
  указывало на неё же, и обе половины сходились в ОДНУ систему;
- цена в расчёт не входила ВООБЩЕ. Замер: сид 42 — «продавать в Vouvu-129», где
  цена **1.74** против **2.38** дома; сид 1337 — 1.48 против 1.73. Совет
  отправлял торговать в убыток;
- разведка игнорировалась: `game.markets[i]` читался напрямую, мимо
  `playerKnowsMarket`. В радиусе 15 ly 85–303 системы, разведана ровно одна.

Больше нигде в игре этого не было: `grep maxSupply` даёт только эти три места,
`make balance` на момент находки — 46/46 PASS. Сломан был советчик, не экономика.

### 27.2 Мерка: Cr за ГОД полёта

`Game::playerBestRun` (game.cpp) — «что взять здесь и где продать», ранжировано
**кредитами за год полёта**, той же меркой, которой §23 велит мерить заказы.
Мерка выбрана замером, а не на глаз:

| | абсолютная прибыль | Cr/год |
|---|---|---|
| сид 1337 | Tantalum → Soham-655, 3929 Cr за **313 лет** = 12.5 Cr/год | Tantalum → Pekospa-829, 3819 Cr за **33 года** = 114 Cr/год |
| на живом стенде | жирнейшая строка биржи 4909 Cr за 158 лет = 31.1 Cr/год | совет 4348 Cr за 33 года = **130.2 Cr/год** |

По абсолюту почти ничья, по времени — вчетверо. Мерка сама притягивает цель на
1.6–2.6 ly, то есть в 15–25 лет пути: первая цель партии оказывается по соседству,
а не за 40 световых лет.

⚠️ **Отклонение цены от опорной как сигнал ПОКУПКИ не работает.** Замер: у
победивших рейсов домашняя цена 0.92x, 0.95x, 1.00x и даже 1.51x опорной. Дом
всегда «по средней»; деньги лежат в том, что ЦЕЛЬ дорога в 30–50 раз. Поэтому в
реплике названы обе цены — здешняя и тамошняя, — а не отношение к опорной.

Причина дешевизны («добывают в 3.7 раза больше, чем сжигают») берётся из
`productionRate/demandRate`. ⚠️ У синтетических сверхтяжёлых потребление нулевое
и отношение улетает в 1e12 — поэтому `pressure` считается только там, где
потребление есть, а иначе реплика о нём молчит.

Перебор — 128 ближайших систем, **1 мс** на мире 8192. Цель, до которой маршрут
не строится (`agentRouteCost().feasible`), отбрасывается: совет, которого не
достичь баками, — не совет, а ловушка (§12.5).

### 27.3 Что Тимертия вправе знать (решение пользователя)

- **шаги 10–11 (обучение, один раз)** — всезнание: первую цель партии она
  называет по-настоящему, `knownOnly=false`;
- **шаг 100 (сводка по прибытии)** — только разведанное, `knownOnly=true`.
  На первой системе партии рейса нет по построению, и она честно говорит: «этот
  рынок у нас единственный, взвесить его не с чем; здесь избыток — титан по 0.99».

Совет обучения считается ОДИН раз и лежит в `VisualNovelState::hintRun`: реплики
«берите X» и «продайте в Y» обязаны говорить про один рейс, а между ними мир
идёт и цены ползут.

### 27.4 Попутно: один seed давал РАЗНЫЕ миры

Найдено при замере — три одинаковых прогона в одном процессе дали разные числа.
`marketClusterLevel` — кэш в market.cpp; авторитетное значение живёт в
`Game::clusterPriceLevel` и проталкивается вниз, но ВПЕРВЫЕ это происходит уже
после засева рынков. Второй мир в процессе релаксировал цены под остаток первого:

```
уровень до init:  1.0000 / 0.2652 / 0.4641   (три подряд мира, seed 42)
добыча/потребл.:  5.8    / 13.1   / 7.2
цена сбыта:       90.98  / 71.79  / 82.38
```

Лечится строкой `marketSetClusterLevel(1.0)` в `Game::init` рядом с `rng.seed`.

**Игра этого не показывала** — одна партия на процесс (`grep init(STAR_COUNT)`:
два места, и второе — `--econreport`). Зато балансовый стенд строит миры
десятками и мерил каждый следующий по остаткам соседа: пороги там намеренно
широкие, поэтому 46/46 держались, но числа в отчёте плыли (после правки
«репутация открывает заказы» читается x10679 вместо x1648 — это НЕ регресс
баланса, это первый честный замер).

### 27.5 Грабля: перевод переставил %s и число

Русская строка сводки была написана как «%s ... %.2f ... %s ... %.2f», а
английский ключ — «%s ... %.2f ... %.2f ... %s». `snprintf` берёт аргументы по
счёту: double прочитался как указатель, и игра падала ровно на реплике — **и
только на русском**. Закрыто `I18N::formatSpecsConsistent` и проверкой
`testTranslationsKeepFormatOrder` (785 точных строк, расхождений 0).

Оттуда же второе правило: **годы пишутся буквой `Y`, как везде в интерфейсе, а
не словом**. Русское числительное требует согласования («24 года», но «25 лет»),
а число приходит из расчёта. `%.1f св.года` безопасно — там всегда дробь.

### 27.6 Проверки

`make` 0 предупреждений, `make balance` **51/51 PASS** (+5 новых), `make uiclick`
43/43 PASS, ASan `--smoke` exit 0, `make uishots` на обоих языках — `VN OVERFLOW`
не сработал; шаг 100 теперь тоже меряется харнесом (он живёт вне ленты обучения и
до сих пор не мерился вовсе): 2–3 строки из 5 в обоих исходах.

Новые регрессии:

- `testAdviceIsARealRun` — совет ведёт в ДРУГУЮ систему и в плюс, три сида;
- `testAdviceMeasuresPerYear` — Cr/год совета не ниже Cr/год жирнейшей строки биржи;
- `testAdviceRespectsSurvey` — на свежей партии сводки нет, после разведки есть;
- `testSameSeedSameWorld` — два мира одним seed в одном процессе совпадают до 1e-9;
- `testTranslationsKeepFormatOrder` — перевод не переставляет `%s` и числа.

### 27.7 Что осталось знать

- **Тимертия не знает про заказы.** Её сводка сравнивает только свободные рейсы,
  хотя доска (§23–24) висит в том же порту и мерится той же меркой Cr/год.
  Естественное продолжение: одна фраза «а вот заказ на ту же дорогу даёт больше».
- **Шаг 100 не пересчитывается по `V`.** Клавиша только показывает и прячет уже
  сочинённую реплику; сводка сочиняется по прибытии в НОВУЮ систему. Игрок,
  нажавший `V` через сто лет, читает столетний совет.
- **`localSurplusElement` не смотрит на цену** — только на добычу против
  потребления. Для запасного ответа («сравнивать не с чем») этого довольно, но
  как самостоятельный совет он не годится: см. замер в §27.2.

---

## 28. Энергоплата за моделирование (2026-08-07)

### 28.0 Что стало с клавишей V

Было: `V` зажигала и гасила коробку с УЖЕ сочинённой репликой. Реплика рождалась
по прибытии в новую систему, поэтому игрок, нажавший `V` через сто лет, читал
столетний совет (§27.7).

Стало: `UI::toggleVisualNovel` — гасит, если коробка горит, а если погашена,
**пересчитывает сводку по нынешней обстановке**. И берёт за это топливо реактора.

### 28.1 Три решения пользователя

| развилка | выбор |
|---|---|
| что Тимертия знает на `V` | **всё**, мимо разведки — «фан-сервис» |
| глубина просчёта | вчетверо шире обычной сводки (512 систем против 128) |
| чем ограничен | **не картой, а реактором**: каждое нажатие жжёт топливо |
| форма платы | **плоская**: один процент бака за нажатие, а не смета по рынкам |

Знание перестало быть ограничителем совета — им стала энергия. Сводка по
ПРИБЫТИИ (шаг 100) осталась прежней: бесплатной и только по разведанному. Платный
просчёт — отдельный шаг 101, отказ на сухом бункере — 102.

### 28.2 Цена нажатия

`INSIGHT_BUNKER_SHARE = 0.01` — **один процент бака за просчёт**, плоско.
Списывается через `shipDrawReactorEnergy`, тем же законом и с той же золой в
трюм, что и буровая (§20.7) и полёт: массу платы переводим в энергию как
`масса × specificEnergy / NUCLEON_REST_MEV`.

Замер: стартовый бункер 219.8 массы, нажатие — **2.1979 (ровно 1.00%)**, сотня
нажатий осушает корпус.

⚠️ **Доля берётся от ПОЛНОГО бака, а не от остатка.** От остатка вышла бы апория:
каждый раз минус процент того, что есть, — бак не пустеет никогда, и чем хуже у
игрока дела, тем дешевле ему совет. Проверка `testInsightBurnsReactorFuel` меряет
долю в узком окне 0.9–1.1% именно ради этой подмены.

⚠️ **Цена по числу разобранных рынков не прижилась.** Сначала плата считалась
как `энергия × 512 систем`: смета была честной, но игрок видит выскочившее
сообщение, а не смету, и цена обязана читаться одним числом. Заодно та смета
дважды мимо попадала по величине — 0.2% при 1e-6 (пятьсот нажатий до сухого бака,
ловушка для любопытного) и 0.02% при 1e-7 (пять тысяч нажатий, то есть платы нет
вовсе).

Выключение коробки бесплатно: платят за работу реактора, а не за нажатие клавиши.
На сухом бункере просчёта нет вовсе — тот же закон, что у буровой, которая на
пустом бункере просто выключается.

### 28.3 Проверки

`make` 0 предупреждений, `make balance` **52/52 PASS** (+1), `make uiclick` 43/43,
ASan `--smoke` и `--localsmoke` exit 0, `make uishots` на обоих языках — шаги 101
и 102 добавлены в обмер коробки (3/5 и 2/5 строк), `VN OVERFLOW` не сработал.

`testInsightBurnsReactorFuel` — нажатие стоит ровно процент бака и даёт шаг 101,
повторное нажатие гасит бесплатно, на сухом бункере выходит шаг 102.

---

## 29. Разбор скриншота: что НЕ было багом (2026-08-07)

Две цифры со скриншота игрока проверены и оказались замыслом. Записано, чтобы не
проверять их в третий раз.

### 29.1 «КАЗНА КОЛОНИИ 4677, ДОХОДА 20 947 008/Y»

Не противоречие. `updateColonies` КАЖДЫЙ ТАКТ сливает кассу владельцу, оставляя
ровно резерв текущей стройки: касса — рабочий баланс очереди, а не накопитель
(«империю больше не надо облетать ради денег», §16). 4677 — это остаток под
стройку, а 20.9 млн/год уходят в казну фракции и в поплавок игрока.

Сама величина тоже в масштабе: доход = оборот × `COLONY_OWNER_DUTY` (0.029) ×
доступ × здоровье, система стоит порядка 1e9 (`testSystemPriceOrder`), то есть
окупается примерно за полвека — при рейсе в 20–100 лет это осмысленный срок.

⚠️ Что здесь и правда плохо — не число, а ПОДПИСЬ: «КАЗНА» рядом с «ДОХОД/Y»
читается как копилка, которая обязана расти. Если править — править слово.

### 29.2 Актиний в бункере стартового корабля

Замысел, и он описан в `defaultFuelElement` (ship.cpp): критерий — не «где
больше энергии», а энергия В ЕДИНИЦЕ ОБЪЁМА × доля поджига × ядерная стабильность
/ цена. Бункер ограничен ОБЪЁМОМ, поэтому побеждает деление, а не синтез: «ЯРД —
это урановая зона, греющая водород», отсюда актиний в бункере и водород в баке.

⚠️ Но выигрыш ТОНКИЙ, и это стоит знать: Ac 0.0360, Th 0.0344, Li 0.0319,
Fr 0.0272, Ra 0.0267, Pa 0.0255, He 0.0254, H 0.0249. Первое место взято с
отрывом 4.6%, а вся восьмёрка укладывается в 45%. Любая правка плотности, цены
или `handlingRisk` в таблице элементов может молча переставить стартовое топливо
на торий или литий. Если хочется, чтобы новичок стартовал на чём-то более
внятном, — это правка одной строки таблицы, а не критерия.

---

## 30. Релизная проверка: Windows-сборка не запускалась (2026-08-07)

### 30.1 Что проверено перед релизом

`make game` — 0 предупреждений при `-Wall -Wextra`. `make balance` PASS (0 failures),
`make econ` PASS, `make uiclick` 43/43 PASS, `make soak` под ASan/UBSan —
120 000 кадров, `invariantFails=0`, все 15 проб зелёные. ASan `--smoke` и
`--localsmoke` — exit 0. `make uishots` на ОБОИХ языках: худшая реплика Тимертии
3 строки из 5, `VN OVERFLOW` не сработал.

Пути ассетов сверены с упаковкой: `assetPath("music/processed")`, `timertia.png`,
`comic/slide%d.png` — ровно то, что раскладывает `build.yml`.

### 30.2 ⚠️ Главное: поставка Windows была неполной, и .exe не стартовал

В `build.yml` DLL перечислялись РУКАМИ — `SDL2.dll`, `SDL2_mixer.dll`,
`libwinpthread-1.dll`. Разбор таблицы импорта готового артефакта
(`x86_64-w64-mingw32-objdump -p`) показал, что `SDL2_mixer.dll` из MSYS2 **жёстко
импортирует** кодеки:

    libFLAC.dll  libmpg123-0.dll  libopusfile-0.dll
    libvorbisfile-3.dll  libwavpack-1.dll  libxmp.dll

а те тянут `libogg`/`libopus`/`libvorbis`. Ни одной из них в папке не было.

Это не «музыка не играет». Windows разрешает импорты при ЗАГРУЗКЕ образа:
игрок получал окно «не найден libmpg123-0.dll» и НЕ ЗАПУСКАЛ игру вовсе. На
машине разработчика ошибка невидима — там MSYS2 добавляет `/mingw64/bin` в PATH.

Починка — не «дописать шесть строк», а убрать ручной список: сборка обходит
импорты вширь через `objdump` и копирует всё, что нашлось в `/mingw64/bin`,
пока очередь не опустеет. Системные DLL Windows отсеиваются сами — их в
`/mingw64/bin` нет.

⚠️ Следом добавлен ОТДЕЛЬНЫЙ шаг `Verify DLL closure`: он повторно обходит все
импорты готовой папки и **валит сборку**, если чего-то нет. Ручной список был
неполон полгода и молчал об этом; гарантия обязана быть механической, иначе
следующая зависимость приедет так же тихо.

### 30.3 Архив для игроков

Собирается из артефакта `Starcluster-Multiplatform` (job `package-multiplatform`
ждёт все три сборки). В zip не должно попасть macOS-мусора: `.DS_Store`,
`__MACOSX/`, AppleDouble-файлы `._*`. Отсюда `zip -r -X` с явными `-x`.

---

## 31. Хайтек-этаж: экзотическая материя (2026-08-12)

### 31.1 Зачем

Поздняя игра упиралась в потолок: трюм с иридием перестаёт что-либо решать
задолго до того, как игрок накопит на систему за 1e9. Нужен был ВТОРОЙ ЭТАЖ
торговли — с другим порядком цен и другой географией.

Он выведен, а не назначен. Таблица элементов кончается там же, где кончается
химия: максимум, который можно вытащить из ядра, — около 8 МэВ на нуклон
(кривая связи, §12). Всё, чем должна жить поздняя игра, лежит ЗА этим пределом,
и это не «редкие товары», а три состояния вещества:

| | Что это | Где рождается | Доля систем |
|---|---|---|---|
| `ANTIMATTER` (AM) | антиводород, 1863 МэВ/нуклон пары | производится: завод + светимость | рынок 8.9%, источник 275 из 8192 |
| `NEUTRONIUM` (NM) | вырожденное вещество | собирается с нейтронной звезды или из диска ЧД | рынок 3.6%, источник 37 |
| `CONDENSATE` (QC) | когерентная подложка | только система при МЁРТВОЙ звезде (белый карлик) | рынок 4.9%, источник 28 |

⚠️ **Ключевое свойство — РЕДКОСТЬ.** У каждого вещества свой тип звезды, и
источники надо ИСКАТЬ: разведка впервые получает поздний смысл. Первая версия
гейта по конденсату брала «просто холодные» звёзды — красных карликов в
скоплении три четверти, конденсат оказался у 58% систем и перестал быть поводом
куда-то лететь. Мёртвых звёзд полпроцента — вот это повод.

### 31.2 Модель рынка (`exotic.h`, `exotic.cpp`)

Другая, чем у элементов, и намеренно. Обычный рынок — живая система замещения,
которая тикает каждый такт по всем 8192 звёздам, потому что по нему ходят сотни
агентов. Экзотику трогает ОДИН корабль, и держать ради него ещё один проход по
скоплению — расточительство.

- Цель запаса — **чистая функция звезды**: `45 + 640*источник + 90*потребитель`.
  Второе слагаемое не декоративное: без него ввозной буфер был 45 единиц против
  отсека в 360, полный корабль обваливал цену втрое и рейс давал **1.07x**
  капитала — этаж существовал, но не имел смысла.
- Цена = `база × уровень цен скопления × f(источник, потребитель) × g(выработанность)`.
  Вилка `f` — 0.34…2.94, до **8.6x** между чистым источником и чистым
  потребителем. `g` — степенной закон: выбранный подчистую источник не
  кончается, а дорожает.
- Запас считается **ЛЕНИВО**: в сейв попадают только звёзды, где игрок реально
  торговал; отклонение релаксирует к цели за `EXOTIC_RECOVERY_YEARS = 260`.
- Средняя цена исполнения — численный интеграл по 12 шагам, проба берётся
  **в середине шага**. По левому краю обе ошибки смещения работали в пользу
  игрока, и круг «купил-продал на той же звезде» выходил валово положительным.

**Замер:** рейс полным отсеком даёт **2.04x** капитала (2.5e7 → 5e7 Cr),
источник после закупки дорожает на 16%.

### 31.3 Аннигиляционный катализ

Антивещество не «улучшает» двигатель — оно **едет в бункере вместе с топливом и
входит в смесь**. Никакой новой физики: смесь и так средневзвешенная по массе,
просто у этого компонента 1863 МэВ/нуклон вместо 1…8. Доля в тысячную по массе
утраивает энергию смеси и улетает вместе с рейсом (`consumeBunkerMass` списывает
пропорционально массе).

⚠️ **Реактор жжёт ЧИСТОЕ топливо, без антивещества** (`shipDrawReactorEnergy`
зовёт `summarize(ship.fuel, …)` напрямую). Антивещество впрыскивается в камеру
тяги, а не в активную зону: катализировать им нагрев бура нечего. Сначала было
написано иначе, и сеанс добычи или одно нажатие `V` (§28) молча уничтожали
антивещество по 240 000 Cr за единицу.

### 31.4 Переоснастка корпуса

Ячейка удержания (`containmentLevel`, 4 ступени по 90 единиц) — входной билет:
без неё экзотику везти не в чем. Нейтрониевая броня (`platingLayers`, до 8
слоёв) — +120 брони, +900 корпуса и **+60 сухой массы** за слой: единица
нейтрониума вчетверо тяжелее любого элемента, поэтому «наварить ещё» всегда
размен между живучестью и разгоном.

⚠️ **Обе ступени живут НА КОРПУСЕ (`Ship`), а не в партии.** Сначала лежали в
`Game`, и это давало две ошибки сразу: восемь слоёв, наваренных на первый борт,
применялись и ко второму (оплачено один раз), а после загрузки ёмкость
восстанавливалась только у пилотируемого борта — экзотика на втором оказывалась
в отсеке нулевого объёма.

⚠️ **Масса инертной экзотики входит в полезную нагрузку планировщика**
(`shipInertExoticMass` в `shipRouteCost`/`shipTuneDrive`). Без этого полный
отсек (до 1440 единиц массы при сухой массе корпуса 5…400) был для расчёта
топлива НЕВИДИМ: маршрут считался по одной массе, а жёгся по другой, и корабль
вставал посреди перелёта ровно на том рейсе, ради которого ячейку и покупали.

⚠️ **Капсула после гибели теряет экзотику и переоснастку.** Иначе отсек
конденсата на полтораста миллионов переживал взрыв (трюм с рудой — нет), а сама
капсула получала броню линкора.

### 31.5 Кузница ядер

Конденсат — ТЕЛО хромокора. Кузница (верфь ≥2) даёт ядро **выбранного** стата,
в отличие от рулетки `addResearch`. Кредитная часть намеренно скромная рядом с
ценой конденсата: платит не кошелёк, а РЕЙС к мёртвой звезде.

---

## 32. Микромир перестал быть печатным станком (2026-08-12)

### 32.1 Ферма перезаходом

Сцена детерминирована по `(seed, звезда)`, а вход не стоит ни времени, ни
топлива. Значит `L`, `L` возвращали те же радиоисточники, тех же пиратов с теми
же наградами и полный щит — бесконечно.

- **Радиоисточники**: заявка живёт в партии (`LocalClaims.radioMask`), уже
  забранный источник рождается погашенным.
- **Награды за головы**: платит СИСТЕМА из конечного бюджета охраны путей —
  доля годового оборота (`BOUNTY_SYSTEM_SHARE = 0.15`, пол 2500 Cr),
  восстанавливается за 40 лет МАКРО-времени. Перезаход времени не двигает,
  поэтому расстрелять один и тот же пояс дважды за деньги нельзя. Деньги
  списываются из казны владельца системы.
- **Щит** живёт в партии (`playerShieldFrac`), а не в сцене.
- **Урон зеркалам** реальных бортов пишется наружу каждый кадр: раньше наружу
  шла только смерть (§5.13.14) и продажа груза (§5.13.18).

### 32.2 Хромокоры исчезали при пересадке

`shipApplyClass` переписывает поля корпуса табличными значениями, а материалы,
тактика и кинематика не хранятся больше нигде. Модули после покупки
перезапекались (§15.1B), хромокоры — нет: **три ветки прокачки из семи умирали
с первой же пересадки**, продолжая показывать «>1.00» в интерфейсе. Закон
запекания вынесен в `shipApplyChromocoreFactors` — один на выдачу ядра и на
пересадку; полный пересбор — `Game::rebakePlayerBakedBonuses`.

⚠️ Полное перезапекание зовётся **только там, где `shipApplyClass` обнулил
поля**. При загрузке и при инкрементальных апгрейдах — нельзя: бонусы уже в
полях, и повторное наложение их удвоит.

### 32.3 Предупреждение о столкновении

Мерилось шаром радиусом `playerMaxSpeed * WARP_MULT * 1.5` ≈ **8550 LU** —
больше всей играбельной системы (край 600…1400 LU). Условие было истинным
ВСЕГДА под варпом: экран непрерывно трясся, заливался красным и мигал «DANGER»
без всякого урона. Теперь меряется ВРЕМЯ ДО УДАРА вдоль вектора скорости.

---

## 33. Акции держав (2026-08-12)

Вторая половина мысли о собственности. Систему купить можно (§13), но это
миллиард и это МЕСТО. Доля в державе географии не имеет и продаётся в любой
момент; платит она заведомо хуже — владелец берёт всю пошлину с оборота,
акционер только ту четверть, которую держава распределяет.

⚠️ **Цена идёт от ДОХОДА, а не от активов.** Первая версия считала казну плюс
цену всех подвластных систем по формуле §13 — замер её и похоронил: 20 млн Cr за
акцию при дивиденде 18 Cr, окупаемость МИЛЛИОН лет. Цена системы собрана из
контекстных множителей, доход идёт от оборота, и отношение между ними гуляет на
пять порядков. Акция — право на будущие выплаты:
`цена = (казна + доход × SHARE_CAPITALISATION_YEARS) / SHARE_FLOAT`. Окупаемость
задана построением: 50 / 0.25 = **200 лет** против полувека у системы.

**Книга ПУБЛИКУЕТСЯ**, а не считается на лету: на такте фракций обновляется одна
держава по кругу. Котировка отстаёт от жизни на несколько лет, и тот, кто там
летал, знает про потерянную систему раньше биржи. Возраст отчёта виден в окне.

Дивиденды идут на СЧЁТ фракции игрока и подчиняются световой сверке (§16).
Больше четверти державы не продадут; акции своего владения не купить; дивиденд
не платится сверх казны державы.

---

## 34. Связки, которых не хватало (2026-08-12)

- **Прокачка от торговли.** Очки капали только с аномалий, добычи и боя (14
  точек по коду): игрок, играющий в то, ради чего игра сделана, не получал ни
  одного хромокора за партию. Теперь платит разведка (новый рынок — +3) и
  удачная сделка (логарифм от прибыли). Замер: 60 разведанных рынков = первое
  ядро.
- **Лестница целей.** Панель задач была списком из четырёх пунктов и после них
  не ставила целей никогда. Теперь 14 ступеней до нейтрониевой брони, панель
  показывает окно вокруг первой незакрытой. `licencePeriodsMet`, который
  писался и не читался никем, наконец что-то значит.
- **Бой на доске заказов.** Сбитый в микромире пират закрывает заказ на свою
  голову (`Contract::targetDown`) — свой или ничей, но не тот, что везёт
  конкурент.

---

## 35. Автопилот флота (2026-08-12)

Купленный за лицензию борт был мебелью: `updateAgents` пропускал все корабли
игрока, у них нет ИИ, и польза от них была одна — суммарный трюм под крупный
заказ. Теперь борт ставится на автопилот кнопкой `AUTO` в окне трюма (действует
на борт, стоящий РЯДОМ, — значит поставить под автопилот летящий физически
нельзя).

⚠️ `updateFleetTrader` — **узкая копия** `updateTrader`, а не вызов его:
1. тот берёт заказы сам, а репутация и носители груза — дело игрока;
2. тот в холостом ходу дёргает `randomer(rng, 12)`, то есть **сдвигает
   глобальный поток** — прямое нарушение §2.3, из-за которого все прежние
   соак-бейзлайны стали бы несравнимы. Здесь пауза константой;
3. тот не понимает, что борт-носитель взятого заказа обязан стоять на месте.

Заправка: `startJourney` заправляет борта игрока только под автопилотом
(`!playerControlled || autoTrade`) — иначе автопилот выгребал бы бюджет на груз
и вставал без топлива.

⚠️ Автопилот торгует **по знанию фракции** и потому требует И рынка, И
владельца (`factionKnowsMarketAt` + `factionKnowsOwnerAt`). Флот не умнее
капитана: разведаете — полетит.

**Замер:** 200 000 Cr капитала → 214 000 за 400 лет, четыре сделки, и всё это
время борт платит лицензионный тариф в квоту.

---

## 36. Выросшая колония меняет свой рынок (2026-08-12)

`needs` и `productionRate` задавались ТОЛЬКО в `Market::seed`, то есть масштаб
экономики системы был заморожен на момент генерации мира. Колония растила
`star.population` и `star.industry`, стройка поднимала инфраструктуру — рынок не
замечал этого вовсе. Самая длинная петля игры была разомкнута: строить незачем,
оборот не растёт, доход владельца стоит.

`Market::rescale` пересчитывает размер тем же законом, что и засев (люди ×
индустриализация), не трогая цены, запасы и события. Ступенькой, при расхождении
масштаба на 5%. Замер: утроенное население поднимает годовую нужду в 3.94 раза.

---

## 37. Что дальше (передача следующему агенту)

Проверено и НЕ починено — сознательно, чтобы не раздувать сессию:

1. **Репутация упирается в потолок 1000 без выхода** (§24). Звание `PRIME
   CARRIER` с 902.5, выше — ничего. Кандидат: репутация открывает доступ к
   книгам державы без задержки (§33) или к её экзотическим складам.
2. **`Contract::deposit` всегда 0** — залога за заказ не существует, поэтому
   провал стоит только репутации. Механика написана наполовину.
3. **`Colony::stockpile` копится и не тратится**, `miningYieldAccum` пишется и
   не читается.
4. **В своей системе выручка ноль** (`freeMarketFor`) — добывать и продавать
   «дома» нельзя ни за деньги, ни в квоту. Это ЗАМЫСЕЛ (§13), но игроку нигде
   не объяснено.
5. **Новелла Тимертии (§26) молчит про весь новый этаж**: про ячейку
   удержания, экзотику, кузницу, акции и автопилот. Она кончается на шаге 27.
6. **Локальные убийства зовут `addResearch`**, а тот при переходе порога тянет
   из ГЛОБАЛЬНОГО `rng` (`chromo.cpp`). Заявленное исключение, но правило §2.3
   формально нарушено: поток сдвигается.

---

## 38. Буксир: единственный настоящий тупик закрыт (2026-08-12)

Корабль с сухими баками, нажавший `X STOP`, тормозить нечем:
`shipConsumeForDeltaV` возвращает ноль, скорость не падает НИКОГДА, `enRoute`
остаётся `true` вечно. А на `!enRoute` завязано всё — покупка, продажа, добыча,
заказы, верфь. Игрок летел в пустоту с вечной надписью `BRAKING`, и выхода не
было вообще: только F9 или новая партия. Формально честная физика (§7.3.1 п.5
называла это открытым вопросом) запирала партию насмерть.

Через `TOW_WAIT_YEARS = 6` беспомощного дрейфа сигнал слышат: буксир берёт треть
кошелька и половину трюма, ставит корабль в ближайший порт и заливает по глотку
обоих расходников (`shipEmergencyPrime`) — ровно столько, чтобы доползти до
рынка, и не столько, чтобы происшествие ничего не стоило.

---

## 39. Разбор первого часа игры (2026-08-12)

Отдельный аудит: пройти по коду глазами нового игрока. Нашлось, что несколько
обещаний интерфейса просто не выполняются.

| Было | Стало |
|---|---|
| `ESC` выходил из игры мгновенно и без сохранения, а карточка F1 учит, что он «закрывает» | закрыть верхнее окно → спросить → сохранить и выйти |
| `R ROB` в карточке без предупреждения: на стартовом корпусе ~70% потерять корабль | два нажатия, первое называет шансы (`Game::robOdds`) |
| Отозванная лицензия морозила и продажу НАКОПАННОГО, вопреки своему же комментарию | продавать можно, вся выручка гасит выкуп, долг закрыт — торговля открыта |
| Панель целей налезала на панель фракций (промах 66 px при 6 фракциях) | `UI::factionPanelHeight` — одна формула на обе |
| Кнопки «T TRADE» / «U FIT» подписаны буквами, делающими другое | подписи сняты |
| Клавиша `B` тратила ВЕСЬ кошелёк, игнорируя поле ОБЪЁМ | уважает поле, как и кнопка |
| В карточке F1 не было управления внутри системы, куда она же отправляет | группа `IN SYSTEM` |
| Семь кодов прокачки (`IN1.00 CH1.00 …`) не расшифровывались нигде | имена вместо кодов |
| Коробка новеллы не говорила, чем листается | мигающая подсказка клавиши |

⚠️ **`UI::closeWindow` была объявлена в `ui.h` с самого начала и ни разу не
определена** — окна закрывались только крестиком, прямо в обработчике клика.

### 39.1 Что осталось (передача дальше)

1. **Квота первого периода недостижима торговлей**: 10 000 Cr при тарифе 469 за
   рейс — 21 рейс по ~118 лет против периода в 1000. Это ЗАМЫСЕЛ (§21.1), выход
   — `SETTLE QUOTA`, и теперь про него сказано в цели. Но новелла всё ещё грозит
   отзывом, не называя второго пути.
2. **Числа без единиц** остались в окне системы (`ST 300 CT 4`, координаты без
   `ly`), в разборе элемента (`STOCK 5.86 M`, `Q0.96`, `MASS 52` — атомная, а
   читается как масса партии) и в строке заказа (`250` — единицы, `85.3Y` — до
   срока).
3. **Трёхбуквенные коды функций элементов** (`FUS`, `CND`, `STR`, `RCT`, …) в
   клетках таблицы Менделеева — легенды нет.
4. **`miningYieldAccum`** пишется и не читается (единственное оставшееся мёртвое
   поле экономики).

---

## 40. Сводка состояния (2026-08-12)

Деньги игрока лежат в ШЕСТИ разных местах — кошельки бортов, счёт фракции, то,
что ещё летит светом (§16), кассы колоний, портфель акций (§33), экзотика в
ячейках (§31), — и сложить их было негде. Поздняя игра из-за этого не отвечала
на единственный вопрос, который в ней есть: «я вообще расту?».

`Game::playerNetWorth()` собирает всё в одно число; журнал (`I`) начинается с
него и с разбора по местам. ⚠️ Экзотика оценивается ТАМ, ГДЕ БОРТ СТОИТ:
увезённая к потребителю она стоит втрое, и считать это заранее было бы враньём
в свою пользу.

---

## 41. Биржа стала событием (2026-08-12)

Публикация книги державы (§33) сравнивается с прошлой, и движение больше 10%
попадает в ленту новостей — с прямым указанием, если игрок в доле. До этого
войны и потери систем происходили за кадром, а биржа была таблицей без сюжета.

---

## 42. ⚠️ ГЛАВНОЕ: игра запиралась на третьем рейсе (2026-08-12)

**Самая дорогая находка сессии, и лестница проверок её не ловила.** Каждая
отдельная механика была здорова — `make balance` из 75 проверок был зелёным, —
а ВМЕСТЕ они запирали партию. Прогон «разумной игры» (заправиться, спросить
совет, закупиться, слетать, продать) на трёх сидах давал одно и то же: **2..4
сделки и вечные 10 000 Cr состояния**, то есть один корпус и пустой кошелёк.

Четыре ямы, все на одном пути:

1. **Кнопка «залить» заливала под пробку.** Бак рабочего тела держит объём на
   сотню рейсов, поэтому полный долив стоил **26 000 … 112 000 Cr при рейсе в
   3 000 … 10 000** — и Тимертия рекомендует эту кнопку как «короткий ответ»
   (реплика 14). Теперь заправка идёт ПОД НАЗНАЧЕННЫЙ МАРШРУТ с запасом в
   половину, не тратит больше `REFUEL_WALLET_SHARE` кошелька и показывает цену
   на себе. Настоящая дорога стоит **12…22% рейса** — вот столько и надо.
2. **«Купить максимум» тратило всё до копейки**, не оставив на топливо. Игрок
   оказывался с полным трюмом и пустыми баками. Планировщик NPC этой ямы не
   знает с самого начала (`findBestTrade` отсекает по `cargoCost + fuelCost >
   money`); теперь тот же закон и у игрока.
3. **Трюм не был потолком покупки**: платная ветка `buyCargo` ограничивала
   объём только деньгами, и игрок платил за груз, который не увезёт.
4. **Советчик молчал при сухом баке**: он ищет ВЫПОЛНИМЫЕ маршруты, а с пустым
   баком не выполним ни один. Игрок читал «ничего не стоит топлива» и не знал,
   что топлива-то и нет.

Плюс `playerBestRun` **не вычитал дорогу** из обещанной прибыли, хотя
`findBestTrade` вычитает её с самого начала: советовались рейсы, которые сам
совет и делал убыточными.

**Замер после правок:** 13 000 → 509 000 Cr за 418 лет (seed 1234), 14 000 →
436 000 (seed 42), рост на всех трёх сидах вместо остановки.

### 42.1 Урок для следующего агента

Все 75 проверок `make balance` меряют МЕХАНИЗМЫ по отдельности: проскальзывание,
честность сводки, сходимость маршрута, окупаемость трюма. Ни одна не играла в
игру. Новая проверка `testAdvisorRunsCompound` делает именно это — десять рейсов
подряд по-человечески, без знаний, которых у игрока нет, — и падает, если
вернётся любая из четырёх ям.

**Если добавляешь механику, спроси: не запирает ли она игрока ВМЕСТЕ с
остальными?** Ни одна из четырёх ям не была багом сама по себе.

---

## 43. Мёртвая звезда отдаёт нейтрониум (2026-08-12)

Редкий бонус при добыче у пульсара/чёрной дыры всегда сопровождался новостью
«exotic matter siphoned from pulsar», а в трюм падал обычный дорогой элемент:
экзотики в игре тогда просто не было. Теперь она есть, и мёртвая звезда — её
единственный источник (§31), поэтому и ковыряние в её коре даёт именно
нейтрониум. Нужна ячейка удержания; без неё работает прежний путь.

Так замкнулась ещё одна петля: локальная добыча, редкие типы звёзд и
хайтек-этаж до сих пор были тремя разными играми.

Заодно закрыто последнее мёртвое поле экономики: `miningYieldAccum` копился с
самого начала и не читался никем.

---

## 44. Финальная проверка сессии (2026-08-12)

Адверсариальная проверка своего же свежего кода. Две находки тяжёлые, и обе —
одного класса: **функция делала половину пары, и вторая половина отсутствовала
молча**.

1. ⚠️ **Дивиденд не зачислялся.** `payShareDividends` звал только
   `addCreditFloat`, то есть создавал ЗАДЕРЖКУ, ничего не положив в казну. А
   доступное к трате — это `казна − в пути`, значит каждая выплата ВЫЧИТАЛА из
   счёта: замер — казна 0, в пути 820 675 Cr, доступно 0, внесённые игроком
   деньги не снимались уже никогда. Все остальные вызывающие
   (`playerAccountDeposit`, излишек колонии) делают пару «зачислить и
   придержать».
2. ⚠️ **Цена на кнопке заправки не совпадала со списанной**: котировка не
   применяла ни потолок кошелька (`REFUEL_WALLET_SHARE`), ни запас склада, а
   покупка применяла оба. 232 133 Cr на кнопке против 600 Cr списания.
3. `refillCost` считал дорогу по СЫРЬЕВОЙ цене, хотя купить расходник можно
   только через станцию с наценкой 90%: оба планировщика занижали дорогу вдвое.
4. Резерв на топливо при «купить максимум» снимался с ПУСТОГО трюма, а расход
   растёт вместе с массой.
5. Выручка, ушедшая в погашение отозванной лицензии, шла в исследования.

**Урок:** там, где деньги переходят между кошельком, казной и поплавком,
проверяй ОБЕ стороны проводки. Ни одна из этих ошибок не ломала инварианты
soak и не печатала денег — они просто делали числа бессмысленными.

---

## 45. Android собирался своим списком исходников (2026-08-12)

Новый `exotic.cpp` попал в `Makefile`, поэтому собрались игра, все пять харнесов
и три из четырёх платформ CI. Android упал связкой `undefined symbol`: у него в
`android/app/jni/src/Android.mk` лежал РУЧНОЙ список файлов с комментарием «тот
же список, что и в Makefile».

Это ровно §30 ещё раз: список, который надо помнить, рано или поздно забудут.
Теперь `Android.mk` читает `SOURCES` прямо из корневого `Makefile`
(`$(shell sed -n 's/^SOURCES = //p' …)` — ndk-build это GNU make), а пустой
результат обрывает сборку через `$(error)`.

⚠️ **Добавил .cpp — проверь, что его подхватят ВСЕ сборки.** Mac, Windows и
Linux в CI зовут `make game` и читают `SOURCES` сами; после этой правки Android
тоже. Старые `0mac_make` / `0windows_make` не синхронизированы и в CI не
участвуют (это записано в «Known Gaps» README).
