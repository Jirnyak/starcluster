# MASTER PROMPT — Starcluster

> Единый вводный документ для агента в новом чате. Прочитай **целиком** перед любой работой.
> Он самодостаточен: миссия, жёсткие ограничения, карта кода, реализованный локальный
> режим полёта, методология проверок, дорожная карта. Документ **живой** — расширяй его
> по мере работы (в конце есть раздел «Как расширять этот документ»).

---

## 0. TL;DR для быстрого старта

- Проект: **Starcluster** — космо-экономическая игра на **C++11 + SDL2** (только SDL2, ничего больше).
- Путь: `/Users/jirnyak/Mirror/starcluster`. Сборка: `make` → бинарь `./game`.
- Пользователь пишет **по-русски**, дал **полную автономию** «сделать игру интересной». Отвечай по-русски.
- Рабочий цикл: **срез → фидбек → дальше**. Делаешь играбельный кусок → пользователь играет → правишь по фидбеку.
- Максимально **используй субагентов** для параллельной работы (пользователь явно просил).
- **Жёсткие архитектурные правила** — раздел 2. Нарушение = переделка. Читай до кода.
- Свежая крупная фича: **локальный режим полёта** (раздел 5). Срез 1 + два прохода углубления
  (juice/бой/контент — §5.10) собраны и провалидированы (smoke + ASan/UBSan + soak-тест 120k кадров). Ждёт плейтеста.
- Поверх хоткеев добавлена **единая кликабельная панель действий** (§5.11) — контекстные кнопки
  active/inactive; чинит «не зайти в звезду».
- ✅ **СДЕЛАНО (2026-07-28):** локальный режим — **полный 3D по трём осям (Elite, +roll,
  тело-относительный базис без gimbal lock), перспективная камера от корабля** (переключаемый
  орто-вид-карта по `C`), **весь кластер виден фоном как LOD-скайбокс** из реальных `ClusterStar`.
  §2.7 обновлён: перспектива только в локале. Все проверки + soak 120k зелёные. Детали — **§5.13**.
- 🎮 **Плейтест №1 пройден (§7.1.1):** 3D одобрен, бой «сочный» (не ослаблять!).
- ✅ **СДЕЛАНО (2026-07-28, срез 2):** **вид из кокпита (от первого лица)** — глаз камеры в позиции
  корабля, взгляд вдоль носа; собственная стрелка убрана, вместо неё прицел-«мушка» по курсу +
  маркер вектора скорости (prograde). Chase-cam заменён на кокпит; вид-карта (`C`) не тронута.
  Камера сведена в единый помощник **`buildLocalCamera`** (`local.h`), общий для игры и
  скриншот-харнеса (`make shots`). `+/−` в 3D теперь оптический зум/FOV (`localFovScale`). Близкий
  собственный выхлоп больше не раздувается в экранные квадраты (кап размера частиц по типу). Все
  проверки + soak 120k зелёные; 5 скриншотов сняты и проверены. Детали — **§5.13 / §7.1.1**.
- 🎮 **Плейтест №2 пройден → проход углубления №3 (§5.13.1, §7.1.2):** по фидбеку закрыты 5 пунктов —
  **звезда как аналитическая сфера** (чистая геометрия: центр + радиус, никаких моделей; изнутри —
  заливка плазмой), **окклюзия луч↔сфера** (планеты не просвечивают сквозь звезду), **гигантский
  масштаб** (`starRadius 90..135 LU`, планеты/корабли — крапинки), **мышелук** (шутерный обзор в
  кокпите, ЛКМ — огонь), фикс **синего квадрата при стрельбе**. Все проверки + soak 120k + 10 шотов
  зелёные.
- 🎮 **Плейтест №3 пройден → проход углубления №4 (§5.13.2, §7.1.3):** «масштаб всё равно звезда
  маленькая». Инсайт: вид звезды зависит только от **отношения D/R** — орбиты уходили к R·13.
  Переписаны как **доли радиуса** (внешняя ≤ R·3.4), звезда ×2.9 (**260..360 LU**), старт D/R≈2.8
  (~82% высоты) → звезда доминирует кадр ВЕЗДЕ. + фикс «спиц» эклиптики, бесшовный порог заливки
  R·1.7. Все проверки + soak 120k + 10 шотов зелёные.
- 🎮 **Плейтест №4 пройден → проход углубления №5 (§5.13.3, §7.1.4):** масштаб одобрен, но «баг
  костыль — подлетаю, всё жёлтое со всех сторон… звезда сделана неправильно… используй шейдеры, шар
  плазмы». Баг = `fillRect(весь экран)`-заливка при D≤R·1.7. Заменён **попиксельным ray-sphere
  «шейдером»** (`renderStarPlasma`): изотропная сфера (центр+радиус), плазма = функция геометрии луча
  (лимб-даркенинг + турбулентность + корона), стриминг-текстура (SDL2-only). Ноль порогов/хардкода.
  Проверка `shot_star_lookaway` (вблизи, глядя прочь → ЧЁРНЫЙ космос). soak 120k + 11 шотов + ASan
  шейдера — зелёные.
- Коммит/пуш — **только по прямой просьбе** пользователя; если на `main` — сперва ветка.

---

## 1. Миссия и стиль работы

### 1.1 Что просил пользователь (дословно, из ранних сессий)
> «игра пока очень скучная и пустая но я даю тебе ПОЛНУЮ АВТОНОМИЮ УЛУЧШИТЬ ЭТУ ИГРУ И
> СДЕЛАТЬ ИНТЕРЕСНОЙ ДЛЯ ИГРОКОВ … основной кор уже создан надо теперь максимально
> геймплей сделать интересным для игроков у тебя неограниченные ресурсы неограниченно
> токенов по максимуму используй субагентов»

Расшифровка действующих директив:
- **Полная автономия** улучшать игру и делать её интересной. Не спрашивай разрешения на очевидные шаги — действуй.
- **Неограниченные токены** → не жалей вычислений; **максимально задействуй субагентов** (параллельные `Agent`/`Workflow`).
- Кор готов — фокус на **геймплее и его читаемости**.

### 1.2 Зафиксированные пользователем решения (не переоткрывать без спроса)
- Чинить **и контент, и легибельность** одновременно («И то, и то»).
- **«Сок» (juice) в текущем минималистичном стиле**: НИКАКИХ новых рендереров, спрайтов,
  шейдеров, звука. Только существующие примитивы (векторная 2D-графика, 5×7 шрифт).
- Доставка через **срез → фидбек → дальше**: небольшой играбельный вертикальный срез,
  пользователь тестит, ты правишь по фидбеку.

### 1.3 Язык и тон
- Пиши пользователю **по-русски**. Технические идентификаторы/код — как есть (англ.).
- Отчитывайся честно: если тест упал — покажи вывод; если шаг пропущен — скажи прямо.

---

## 2. ЖЁСТКИЕ АРХИТЕКТУРНЫЕ ОГРАНИЧЕНИЯ (CRITICAL)

Эти правила повторяются во всех `plans_*.md`. Нарушение ломает стиль/детерминизм проекта.

1. **Data-oriented design. НИКАКОГО ООП-наследования, `virtual`, новых классов-иерархий,
   `new`.** Только C-style `struct`, непрерывные `std::vector`, числовые id/индексы.
   Логика — свободные функции над данными.

2. **Только C++11 и только SDL2.** Никаких сторонних библиотек, никакого C++14+ синтаксиса
   (проверяется флагом `-std=c++11`).

3. **Детерминированный глобальный ГПСЧ.** Есть `extern std::mt19937 rng;` и
   `int randomer(std::mt19937&, int max)` — ВКЛЮЧИТЕЛЬНЫЙ диапазон `[0, max]`.
   - **Локальный режим НЕ трогает глобальный `rng`** (это рассинхронизировало бы макро-симуляцию).
     Для локальной случайности заводи **свой** `std::mt19937`, засеянный от сида звезды/индекса.

4. **Правило CHROMOCORE (запекание).** Материалы/кинематика/тактика уже ЗАПЕЧЕНЫ в поля
   корабля (`cargoCapacity, maxHullHP, speed, acceleration, heavyWeapons, lightWeapons,
   armor`). Код, читающий корабль (в т.ч. локальный), **читает СЫРЫЕ поля и НЕ домножает их снова**.

5. **Кастомный 5×7 битмап-шрифт.** Каждый глиф — строка ровно **35 символов** ('0'/'1'),
   5 столбцов × 7 строк. Горизонтальный шаг пера `6*scale`, вертикальный `8*scale`. Текст
   авто-переводится в ВЕРХНИЙ регистр. Таблица глифов — единственная, в `render2d.cpp::glyph()`.
   ⚠️ Если добавляешь символ — строка ДОЛЖНА быть 35 символов (иначе out-of-bounds в `drawText`, см. §6.3).

6. **printf-формат в ВЕРХНЕМ регистре:** `%.0F`, `%.2F`, `%d` (шрифт всё равно капсит).

7. **Ортографическая 3D→2D проекция — по умолчанию.** Макро/кластер/карта СТРОГО
   ортографические (без деления на перспективу): `depth` — только для порядка отрисовки
   (painter) и затухания (fade). Ветка `perspective==false` в `projectPointWithBasis`
   ПОБИТОВО совпадает с исходником — макро-путь не изменён.
   - **Исключение (РЕШЕНО пользователем, §5.13): перспектива разрешена ТОЛЬКО в локальном
     режиме полёта** (`View3D::perspective==true`, стиль Elite). Это единственное место с
     делением на глубину. Верхний вид «карты» в локальном режиме (клавиша C) — снова
     ортографический. Макро НИКОГДА не включает перспективу. См. `camera.h`.

---

## 3. Сборка, запуск, тесты

```bash
make                       # сборка -> ./game  (см. Makefile, SOURCES перечисляет все .cpp)
./game                     # запуск игры (SDL-окно)
make asan                  # санитайзер-сборка -> ./game_asan (ASan+UBSan, для проверок §3.1)
make soak                  # собрать и прогнать soak-тест локального режима (§6.5)
make clean                 # удалить бинари (game, game_asan, soak_asan, *.dSYM)

# Smoke-тесты (headless, для CI/проверки без окна):
SDL_VIDEODRIVER=dummy ./game --smoke        # макро-путь: 12 кадров, ожидаемый exit 0
SDL_VIDEODRIVER=dummy ./game --localsmoke   # локальный путь: авто-вход в систему,
                                            # инъекция ввода (огонь/добыча/тяга/варп), exit 0
```

Компилятор: `c++ … -O3 -std=c++11 -I/opt/homebrew/include/SDL2 -D_THREAD_SAFE -L/opt/homebrew/lib -lSDL2`.

### 3.1 Проверка памяти (ASan/UBSan) — делай это для нового кода
```bash
make asan                                                                    # -> ./game_asan (цель в Makefile)
SDL_VIDEODRIVER=dummy ASAN_OPTIONS=detect_leaks=0 ./game_asan --localsmoke   # exit 0, 0 ошибок
SDL_VIDEODRIVER=dummy ASAN_OPTIONS=detect_leaks=0 ./game_asan --smoke        # ~1.5 мин под ASan, exit 0
make clean                                                                   # убрать одноразовые бинари/*.dSYM
```
ASan под `--smoke` идёт долго (~90 с) из-за тяжёлой макро-симуляции — это НЕ зависание.
Для локального режима есть ещё `make soak` — детерминированный soak-тест на тысячи кадров (§6.5).

### 3.2 Шумовые диагностики (игнорировать)
clangd в редакторе пишет `'SDL.h' file not found`, `Unknown type name 'SDL_Color'/'SDL_Rect'/'Uint8'`,
`Use of undeclared identifier 'SDL_SCANCODE_*'` — это **безвредный шум language-server** (не знает
`-I/opt/homebrew/include/SDL2`). Компилятор через `make` собирает чисто. Игнорируй эти диагностики.

---

## 4. Карта кода

### 4.1 Макро-слой (исходная игра — «кор»)
| Файл | Роль |
|---|---|
| `main.cpp` | Точка входа, SDL-цикл, ввод, камера (макро + локальная), рендер-диспетчер. |
| `game.{h,cpp}` | Центральный `struct Game`: агенты, кластер, экономика, сейв/лоад, `advanceGame()`. |
| `cluster.{h,cpp}` | Звёздный кластер (~10 000 звёзд), `ClusterStar` (name, stellarClass, population, metallicity, resourceFocus, координаты). |
| `galaxy.{h,cpp}` | Галактическая структура/генерация. |
| `resource.{h,cpp}` | Элементы/ресурсы: `elementDefinitions()`, `elementCount()`, `ElementDefinition{symbol, metallicTrait, basePrice,…}`, `resourceUnitMassByIndex()`. |
| `market.{h,cpp}` | Рынки, цены, спрос/предложение; чёрный рынок/контрабанда. |
| `ship.{h,cpp}` | `struct Ship` (сырые запечённые поля, `cargo`, `hullHP`, `enRoute`, координаты). `shipCargoMass()`. |
| `agent.{h,cpp}` | `struct Agent` (ship, money, currentStar, type ["pirate"/…], faction). |
| `faction.{h,cpp}` | Фракции, `colorR/G/B`. |
| `colony.{h,cpp}` / `civ.{h,cpp}` | Колонии, население, цивилизация. |
| `contract.h` | Контракты (bounty, пассажиры/VIP). |
| `graphic.{h,cpp}` | Низкоуровневая графика (макро). |
| `ui.{h,cpp}` | Оконный UI, торговое окно, панели. Использует примитивы `namespace UI` из `render2d`. |

### 4.2 Контентные модули (реализованы по `plans_*.md`)
| Файл | План | Что делает |
|---|---|---|
| `modules.{h,cpp}` | plans_1 | Модульная кастомизация корабля (двигатели, трюмы, баки) — прогрессия «жажда корабля». |
| `chromo.cpp` | plans_2 | Хромокоры: эндгейм-мультипликаторы (навигация/экономика/сенсоры/добыча), запекаются в поля. |
| `market.cpp` (доп.) | plans_4 | Контрабанда, нелегальные элементы, патрули. |
| `spaceevents.cpp` | plans_5 | Динамические рыночные ивенты (Бум/Эпидемия/Война/Прорыв). |
| `combat.cpp` | plans_6 | Data-oriented охота за головами / упрощённый автобой (макро). |
| `mining.cpp` | plans_7 | Ручная добыча руды на астероидах (макро-механика). |
| `anomaly.cpp` | plans_10 | Аномалии/брошенные корабли (deep-space POI). |
| *(в игре)* | plans_3/8/9 | Пассажиры/VIP, наёмный флот, исследование/сканирование — см. соответствующие планы. |

`design.md` — фундаментальный gameplay loop («медитативный гринд», экономическая спираль капитала).
`plans_1.md … plans_10.md` — детальные ТЗ каждой контентной фичи (контекст, ограничения, шаги). **Читай их, когда трогаешь соответствующую систему.**

### 4.3 Локальный слой (эта сессия — раздел 5)
| Файл | Роль |
|---|---|
| `camera.h` | Проекция 3D→2D (вынесена из main): `View3D` (+`perspective/focal/nearPlane`), `CameraBasis` (+явный right/up/fwd), `ProjectedPoint` (+`behind`/`camX..Z`), `makeCameraBasis` (орто, ПОБИТОВО как раньше), **`makeCameraFrameBasis`** (frame-базис от `pfwd`/`pup`, §5.13), `projectPointWithBasis` (орто-ветка не тронута / перспектива для локала), **`projectDirectionWithBasis`** (LOD-скайбокс), `depthFade`. Только `<cmath>`. inline. |
| `render2d.{h,cpp}` | `namespace UI` (external linkage): `Palette P`, `color/fillRect/strokeRect/panel/glyph/drawText/bar` + новые `fillCircle/strokeCircle`. Единственная таблица шрифта. |
| `local.h` | Модель данных локальной сцены + `namespace LocalCfg` (тюнинг) + 3 контракта функций + inline-хелперы `localSetForward`/`localShipRight`/**`buildLocalCamera`** (единая сборка камеры кокпит/карта для игры и харнеса, §5.13). Включает `camera.h`. |
| `localgen.cpp` | `buildLocalScene()` — детерминированная генерация: звезда, планеты + кольца + луны, пояс, туманность, корабли, радиоисточники; стартовая ориентация носа через `localSetForward` + Z-разброс тел/пояса (объём для 3D, §5.13). |
| `localsim.cpp` | `updateLocalScene()` — физика/орбиты/варп + **3-осевое вращение (Родриг + Грам–Шмидт по `pfwd`/`pup`, §5.13)** + смышлёный ИИ/бой/щиты/смерть/лут/добыча/радио-клейм/частицы за кадр. |
| `localdraw.cpp` | `renderLocalScene()` — оба режима (ветка `view.perspective`): **3D-кокпит от первого лица (перспектива, глаз в корабле) + LOD-скайбокс кластера** / орто-карта сверху; сцена + частицы + HUD/радар/тряска поверх `UI::` и проекции `camera.h`. В кокпите свой корабль не рисуется (мушка+прогрейд вместо стрелки); частицы клампятся по типу, чтобы близкий выхлоп не заливал экран. Читает `cluster.h` для скайбокса. §5.13. |
| `soak_test.cpp` | Вне игры (своя `main`, линкуется с `LIBSOURCES`). Детерминированный soak-прогон локального режима под ASan; цель `make soak` (§6.5). |
| `shot_test.cpp` | Вне игры (своя `main`, `LIBSOURCES`). Скриншот-харнес: рендерит **11 кадров** микромира (система/крен/карта/бой/глубокий космос/`shot_combat_lead` — цель наперерез для лид-пипа/`shot_star_near` — у поверхности звезды/`shot_star_lookaway` — вблизи, но нос ПРОЧЬ ⇒ чёрный космос (проверка изотропии сферы §5.13.3)/`shot_star_occlusion` — ближнее тело видно, дальнее скрыто сферой/`shot_muzzle` — вспышка выстрела/`shot_star_inside` — глаз внутри вещества звезды) в BMP через программный рендерер SDL той же `buildLocalCamera`, что и игра; цель `make shots` → BMP→PNG через `sips`. Собирается и под ASan/UBSan (`renderLocalScene` + попиксельный `renderStarPlasma`). Артефакты в `.gitignore`. §5.13 / §5.13.1 / §5.13.3. |

`features.h` — общие фиче-флаги (при наличии). `SDL2-2.30.10.dmg`, `test_app/` — вспомогательное, не трогать в геймплее.

---

## 5. Локальный режим полёта («микромир») — главный незакрытый трек

### 5.1 Идея
Поверх макро-кластера игрок «ныряет» в одну звёздную систему (или в пустой глубокий космос)
и летает в **реальном масштабе**, инерционно (как в Elite). Звезда в центре, планеты по
кеплеровым орбитам, пояс астероидов, другие корабли, **живой бой**, **добыча руды на месте**,
**стыковка с планетой = рынок**. Пустота между звёздами в основном пуста, но бой и события работают.

**Целевой дизайн (решено пользователем 2026-07-28, §5.13):** локальный режим — полный **3D по трём
осям** (полёт как в Elite, настоящие манёвры), камера от корабля, и **весь звёздный кластер виден
фоном как LOD-скайбокс** по реальным координатам `ClusterStar`. Текущий вид-сверху → §5.12.

### 5.2 Решения пользователя (зафиксированы, не переоткрывать)
1. **Полёт = инерционный (Elite-style):** тяга + дрейф, не «аркадный доворот».
2. **Вход в режим — в любой момент** (клавиша `L`).
3. **Первый срез обязан включать ВСЕ ЧЕТЫРЕ:** (а) другие корабли летают, (б) добыча астероидов
   на месте, (в) живой бой, (г) стыковка с планетой = рынок. — Сделано.
4. **Темп = «долго + автопилот-ускорение»:** реалистично большие расстояния; удержание `Shift`
   ускоряет течение времени (supercruise-подобный варп ×60).
5. **Полный 3D по трём осям, полёт как в Elite** (2026-07-28) — **СДЕЛАНО**. Три оси
   (yaw/pitch/**roll**) манёвренности реально решают в бою/навигации; инерция (п.1) сохранена.
   Реализация — **тело-относительный ортонормированный базис** (`pfwd`/`pup`, right=`pfwd×pup`)
   вместо мировых углов Эйлера: устраняет gimbal lock и делает крен честным. Камера — **из кокпита
   (от первого лица, глаз в корабле)** (срез 2, 2026-07-28); ортогональный вид-карта сверху остаётся
   переключаемым тактическим режимом (клавиша `C`), но не основным. Детали реализации — §5.13.
6. **Весь кластер виден из локального режима как LOD-скайбокс** (2026-07-28) — **СДЕЛАНО**:
   реальные `ClusterStar` рисуются фоном по истинным направлениям (`star.pos − anchor` в
   координатах кластера), LOD по обратной дистанции от якоря (ближние/ярче — крестик/2px,
   дальние — точка). Настоящий фон из данных, детерминированный, без глобального `rng`. §5.13.

### 5.3 Единицы и время
- Пространство: **LU** (local units). Время: локальные **ЧАСЫ**.
- Базовый темп: `1 реальная сек = HOURS_PER_SEC (=1) локальный час`. Удержание варпа умножает поток
  времени на `WARP_MULT (=60)`.
- Орбиты кеплеровы: `orbitSpeed ~ 40 / r^1.5`. Позиции тел пересчитываются из `localHours` каждый кадр.
- Физика идёт **субшагами** (чтобы снаряды не «протуннелировали» цель под варпом):
  `N = clamp(ceil(dtHours/0.05), 1, 64)`, шаг `h = dtHours/N`.

### 5.4 Тюнинг (`namespace LocalCfg` в `local.h`) — крутилки баланса
```
HOURS_PER_SEC   = 1.0     WARP_MULT       = 60.0
PLAYER_ACCEL    = 30.0    PLAYER_MAXSPEED = 95.0    TURN_RATE = 2.4 (рад/реальную сек)
DOCK_RANGE      = 26.0    MINE_RANGE      = 20.0
PROJ_SPEED      = 520.0   PROJ_LIFE_HOURS = 1.2     WEAPON_RANGE = 150.0   FIRE_COOLDOWN_H = 0.10
SHIELD_REGEN_DELAY_H = 3.0   SHIELD_REGEN_RATE = 7.0/ч   PLAYER_SHIELD_BASE = 22.0 (+ armor*1.5)
LOOT_SCOOP_RANGE = 24.0   LOOT_LIFE_HOURS = 300.0   NPC_BOOST_MULT = 1.8 (форсаж при бегстве)
RADIO_REVEAL_RANGE = 300.0 (×(1+0.6*tier))   RADIO_CLAIM_RANGE = 36.0   FX_MAX = 3600 (жёсткий потолок пула частиц)
```
Прочие числа баланса (в коде): урон игрока `heavy*0.6 + light*0.4 + 2.0`; урон NPC `… + 1.0`;
награда за килл `150 + maxHullHP*6` + `addResearch(3.0)`; скорость добычи `9.0` массы/час +
`addResearch(0.2*dtHours)`; hit-radius по кораблю `5.0 + armor*0.1`, по игроку `5.0`.

### 5.5 Модель данных (`local.h`) — кратко
Перечисления: `LocalBodyKind { LB_ROCKY, LB_GASGIANT, LB_ICE, LB_STATION, LB_MOON }`,
`LocalCraftKind { CK_TRADER, CK_PIRATE, CK_PATROL, CK_CIVILIAN }`,
`LocalFxKind { FX_SPARK, FX_DEBRIS, FX_TRAIL, FX_RING, FX_MUZZLE, FX_SMOKE }`,
`LocalRadioKind { RS_DERELICT, RS_CACHE, RS_DISTRESS, RS_ANOMALY }`.
`constexpr int LOCAL_TARGET_PLAYER = -1000;` — sentinel «цель ИИ = игрок» для `aiTarget`.
- `LocalBody` — тело на орбите (orbitRadius/Phase/Speed/inclination, x/y/z, radius, kind, `parent`
  (для лун — индекс планеты, орбита считается вокруг неё), `ringInner/ringOuter` (кольца газовых
  гигантов), r/g/b, `hasMarket`, name).
- `LocalRock` — астероид, **двигается**: `orbitR/orbitAng/orbitVel` (пояс медленно вращается вокруг
  звезды) + `spin/spinVel` (собственное вращение); x/y/z пересчитываются каждый кадр; `ore/element/rgb`.
- `LocalCraft` — корабль: позиция/скорость/цель, maxSpeed/accel, hull, `shield/maxShield/shieldRegenTimer`,
  **сырые** heavy/light/armor, faction, `kind`, `agentIndex` (>=0 → зеркалит макро-агента), `hostile`,
  `aiState/aiTarget` (ИИ), `boost` (форсаж), `hitFlash/thrustGlow` (juice), таймеры, r/g/b, label.
- `LocalProjectile` — снаряд (позиция/скорость, life, damage, `team` 0=игрок/1=враг, rgb-трассер).
- `LocalFx` — частица (позиция/скорость, life/maxLife в часах, size, kind, rgb). Пул ≤ `FX_MAX`.
- `LocalLoot` — канистра из обломков (позиция/скорость, `element/amount`, life). Авто-сбор в зоне.
- `LocalRadioSource` — радиоисточник (x/y/z, `kind`, `tier` 0..2, `strength`, `reward/research/element/
  amount`, `revealed/resolved`, label). Ловится детектором ≥ своего тира.
- `LocalScene` — вся сцена: active, starIndex (-1 = deep space), hasStar, звезда, `localHours`,
  векторы bodies/rocks/craft/shots/**fx/loot/radio**, туманность (`nebulaRGB/nebulaStrength`),
  игрок (px..pz, pvx..pvz, **ориентация телом-относительным базисом `pfwdX..Z`+`pupX..Z`**
  вместо углов Эйлера — см. §5.13, fireCooldown, playerMaxSpeed/Accel,
  **pShield/pMaxShield/pShieldTimer, playerDestroyed, playerHitFlash**), `detectorTier` (из
  `game.tech.sensors`), `lockTarget`, `shake/fxClock` (тряска/анимация), miningRock/miningAccum,
  dockPrompt/**minePrompt** (ближайший рынок/руда в зоне — источник доступности кнопок панели §5.11),
  toast/toastTimer, warping/thrusting.
- `LocalInput` — ввод за кадр: thrust/brake/yawL/yawR/pitchU/pitchD/**rollL/rollR**/fire/warp
  (удержания) + mineToggle(**M**)/dock/cycleTarget (edge-фронты за 1 кадр).

### 5.6 Три контракта (реализации в трёх файлах)
```cpp
// localgen.cpp — детерминированно строит сцену. Сид: 0x9E3779B9u ^ uint32_t(starIndex)
//   (starIndex>=0), иначе 0xD1B54A35u. НИКОГДА не трогает глобальный rng. game — только чтение.
void buildLocalScene(const Game& game, int starIndex, LocalScene& scene);

// localsim.cpp — двигает сцену на кадр (dtReal — реальные сек). Мутирует и сцену, и game:
//   добыча/лут кладут элементы в трюм игрока (уважая cargoCapacity через shipCargoMass); бой меняет
//   money/hullHP; при hull<=0 ставит scene.playerDestroyed (обработка — в main.cpp, §5.7);
//   килл чужой фракции даёт adjustFactionRelation(-8); радио-клейм — money/research/элемент.
//   Возвращает индекс звезды для стыковки, иначе -1.
int updateLocalScene(Game& game, LocalScene& scene, const LocalInput& in, double dtReal);

// localdraw.cpp — рисует сцену и HUD. Одна функция обслуживает ДВА режима, ветвясь на
//   view.perspective: (1) 3D-кокпит — перспективная камера от первого лица (глаз в корабле,
//   свой корабль не рисуется: мушка+прогрейд) + LOD-скайбокс кластера; (2) карта — орто вид
//   сверху (клавиша C), старый top-down путь ПОБИТОВО. Камера/базис строятся buildLocalCamera
//   (local.h): makeCameraFrameBasis для кокпита / makeCameraBasis для карты.
//   Переиспользует camera.h-проекцию и render2d.h-примитивы (namespace UI). §5.13.
void renderLocalScene(SDL_Renderer*, const Game&, const LocalScene&,
                      const View3D&, const CameraBasis&, int winW, int winH);
```

Генерация (`buildLocalScene`): звезда по `stellarClass`; 3–6 планет (rocky/gas/ice), крупнейшая
rocky получает `hasMarket`; при `population>0` добавляется STATION; пояс 45–89 астероидов (элемент
из `resourceFocus`, либо при `metallicTrait>0.6` fallback на индекс 25); 0–8 co-located макро-агентов
(сырые поля корабля; `hostile` если `type=="pirate"`; цвета фракций); 1–3 торговца, 0–2 пирата;
игрок стартует на `innermostR+120`, ориентация носом внутрь к звезде через `localSetForward(scene,-1,0,0)`
(тело-относительный базис, §5.13). Deep space (`starIndex<0`): `hasStar=false`, 0–2 торговца, игрок в
начале координат, нос `localSetForward(scene,1,0,0)`; тела/пояс получают Z-разброс (объём для 3D).

### 5.7 Интеграция в `main.cpp` (где искать)
- Инклюды: `camera.h`, `local.h` (после `ui.h`). Флаг `--localsmoke` (устанавливает `smoke=true; localSmoke=true`).
- Состояние (~строки 520–534): `LocalScene localScene; View3D localView; double localZoom=0.8;`
  edge-флаги `localMineEdge/localDockEdge/localTargetEdge`. При `localSmoke` — build + `active=true`
  + перенос игрока к `rocks[0]` для headless-покрытия добычи.
- Обработка клавиш (~640–673): `L` — вход/выход (anchor = currentStar игрока если пристыкован и
  `!enRoute`, иначе `selectedStar`, иначе `-1`). В активной сцене: `ESC` выход; `E/K/Tab` → edge;
  `+/-` → `localZoom` (clamp 0.05..40.0); затем `continue` (макро-клавиши подавляются).
- Апдейт (~776–811): при `localScene.active` собирается `LocalInput` из `SDL_GetKeyboardState`
  (W/S/A/D/R/F/Space/Shift) + edge-флаги → `updateLocalScene`. Возврат `dockStar>=0` → выход из локала
  + `UI::openTradeWindow`. Иначе если `!paused` → `advanceGame`. **Пока сцена активна, макро-симуляция
  ЗАМОРОЖЕНА.** При `localSmoke` в апдейт инъецируется синтетический ввод (огонь всегда; добыча на
  кадре 0; тяга с кадра 4; варп с кадра 6) — прогоняет все ветки headless.
- Рендер (~824–835): камера строится **единым помощником `buildLocalCamera` (`local.h`, тот же код,
  что и в скриншот-харнесе `make shots`)** прямо перед `renderLocalScene`, режим зависит от флага:
  **кокпит (3D от первого лица)**: `View3D{perspective=true, focal=winW·localFovScale, center=корабль
  (глаз в корабле)}` + `makeCameraFrameBasis(pfwd,pup)`; **карта** (клавиша `C`):
  `View3D{perspective=false, center=player, scale=localZoom}` + `makeCameraBasis`.
  Далее `renderLocalScene` + present + учёт smoke-кадров + пейсинг + `continue` (макро-рендер пропускается).
- **Аварийный прыжок (смерть игрока):** сразу после `updateLocalScene`, если `localScene.playerDestroyed`
  — выходим из локала, восстанавливаем `hullHP` до 30% max, списываем 20% денег, `lastEvent` + `pushNews`.
  Обрабатывается ПЕРЕД веткой стыковки (`dockStar>=0`).

### 5.8 Управление (игрок) — 3D-полёт по трём осям (§5.13)
```
L        вход/выход из локального режима (из макро — по выбранной/пристыкованной звезде)
Мышь     обзор (мышелук, yaw/pitch) — SDL relative mode, только в 3D-кокпите  ← §5.13.1
W        тяга по носу        S  тормоз (гашение вектора скорости)
A / D    рыскание (yaw)      R / F  тангаж (pitch)   Q / E  крен (roll)   ← 3 оси, без клэмпа (резерв к мыши)
Space/ЛКМ огонь              Shift  варп времени ×60 (удержание)
M        вкл/выкл добычу ближайшего астероида в зоне  (перенесено с E — E теперь roll)
C        переключить вид: 3D-кокпит от первого лица (перспектива) ⇄ карта сверху (орто)
K        стыковка с телом-рынком в зоне → открывает торговое окно
Tab      переключить HUD-цель
+ / -    в кокпите — оптический зум/FOV (localFovScale); в карте — ортозум (localZoom)
ESC      выход из режима
```
Оси поворота — **тело-относительные** (yaw вокруг «верха» корабля, pitch вокруг «правого»,
roll вокруг носа); нет мировых углов Эйлера → нет gimbal lock, полный переворот допустим.

> С этой сессии все дискретные действия продублированы **кликабельной панелью** снизу (§5.11);
> движение (W/S/A/D, R/F, Shift-варп) осталось на клавишах — «удержание» на кнопку не мапится.
> В **3D-кокпите** курсор захвачен (мышелук), поэтому ЛКМ там **стреляет**, а панель действий
> ловит клики только в **карте** (`C`); мышь-обзор отключается в карте/меню (§5.13.1 п.4).

### 5.9 Осознанные упрощения (что ещё НЕ сделано — это не баги)
- Пока игрок в локальном режиме, **макро-симуляция заморожена** (`advanceGame` не вызывается);
  фон не идёт, «прошло N времени по выходу» пока нет.
- NPC-vs-NPC бой считается **прямым DPS** (не снарядами): индексы кораблей нестабильны под
  swap-pop удалением, хранить владельца по индексу — баг. Бой с участием игрока — настоящие снаряды.
- Луны/кольца/туманность — **косметика** (на физику/геймплей не влияют, только визуальная навигация).
- Что БЫЛО упрощением среза 1 и теперь СНЯТО — см. §5.10 (игрок убиваем, астероиды двигаются, радио есть).

### 5.10 Углубление после среза 1 (два прохода — реализованы и провалидированы)
Оба прохода правили тот же набор файлов (`localgen/localsim/localdraw` + интеграция в `main.cpp`),
собирались параллельно субагентами (§6.1) и прошли все уровни проверки, включая soak (§6.5).

**Проход 1 — ощущение + глубина симуляции:**
- **Juice/частицы:** пул `LocalFx` — следы двигателя, вспышки выстрела, искры/обломки/дым попаданий,
  большой взрыв при смерти корабля, пыль от добычи. Прозрачность/размер по life/maxLife. Потолок `FX_MAX`.
- **Экранная тряска:** `scene.shake` (пиксели), растёт на попаданиях/взрывах/смерти, затухает; в draw
  смещает копию камеры (HUD не трясётся).
- **Щиты:** у игрока и NPC. Поглощают урон до корпуса, регенерят через `SHIELD_REGEN_DELAY_H` со
  скоростью `SHIELD_REGEN_RATE`. База игрока `PLAYER_SHIELD_BASE + armor*1.5`.
- **Игрок убиваем:** пол `hullHP>=1.0` снят; при hull<=0 → `playerDestroyed` + взрыв + аварийный прыжок (§5.7).
- **Смышлёный ИИ:** цели пересчитываются каждый кадр по `kind` (пират→ближайшая жертва; патруль→пират;
  торговец/гражданский→бегство от пирата, иначе странствие). Сближение / страйф на дистанции / бегство
  с форсажем (`NPC_BOOST_MULT`) при низком корпусе; упреждение при стрельбе.
- **Лут:** обломки роняют `LocalLoot` (элемент + кол-во), дрейфуют/тают (`LOOT_LIFE_HOURS`),
  авто-подбор в `LOOT_SCOOP_RANGE` с учётом трюма.
- **Живые астероиды:** пояс медленно вращается вокруг звезды, камни крутятся.
- **Реакция фракций:** килл небандитского корабля → `adjustFactionRelation(-8)` + вся фракция в
  системе становится враждебной. Килл пирата — награда без штрафа репутации.

**Проход 2 — контент + глубокий космос:**
- **Луны:** `LB_MOON` с `parent`, орбита вокруг планеты (sim добавляет позицию родителя). В генерации
  добавляются ПОСЛЕДНИМИ, чтобы не сдвигать индексную арифметику пояса/промежутков.
- **Кольца планет:** `ringInner/ringOuter` у ~55% газовых гигантов; рисуются концентрическими окружностями.
- **Туманность:** `nebulaRGB/nebulaStrength`, детерминированно по системе; плоская подсветка + мягкие пятна.
- **Радио + детекторы:** `LocalRadioSource` (kind/tier/reward/research/element). `detectorTier` из
  `game.tech.sensors` (0/1/2 при <1.08 / ≥1.08 / ≥1.20). Проявляется в `RADIO_REVEAL_RANGE*(1+0.6*tier)`
  если `detectorTier>=tier`; клейм в `RADIO_CLAIM_RANGE` даёт деньги + исследование + элемент в трюм.
  HUD-радар (справа снизу) + мировые/краевые маркеры. Это закрывает §7.2.

### 5.11 Единая панель действий (кликабельные кнопки) — срез, ждёт плейтеста
Проблема (фидбек пользователя, дословно): «стало слишком много горячих клавиш … не смог зайти
в звезду … надо сделать из них единую универсальную кнопочную панель … кнопки active/inactive».
Решение: **единая контекстная панель кнопок внизу экрана.** Доступные действия — яркие и
кликабельны, недоступные — затемнены и не срабатывают. Хоткеи НЕ убраны: панель добавлена
поверх них (кнопка и клавиша зовут один и тот же `dispatch`).

- **Где живёт:** целиком в `main.cpp` (он владеет `game`/`ui`/`localScene`/выбором/`buildLocalScene`).
  Переиспользованы публичные примитивы `UI::` (`fillRect/strokeRect/drawText/P`) + инлайновый
  hit-test по `SDL_Rect`. Готовые `contains()`/`drawButton()` в `ui.cpp` — внутренняя линковка
  (анонимный namespace), снаружи не видны, поэтому не тянулись — панель написана заново на публичных примитивах.
- **Данные (файловая область `main.cpp`):** `enum ActionId` (ACT_ENTER/GO/STOP/TRADE/SHIPYARD/
  REPAIR/HIRE/PAUSE/SPEED/FIRE/MINE/DOCK/TARGET/ZOOM_IN/ZOOM_OUT/EXIT) +
  `struct ActionButton { SDL_Rect rect; std::string label; SDL_Color color; bool enabled; bool on; int action; }`.
- **Логика (лямбды в `main.cpp`):** `buildBar()` собирает контекстный набор с флагами `enabled`/`on`;
  `drawBar()` рисует ряд по центру снизу; `dispatch(action)` — **единая точка исполнения** (и клик,
  и хоткей идут через неё). Клик по активной → `dispatch`; клик по неактивной проглатывается;
  клик мимо кнопок в макро-режиме → прежний пикинг звезды/агента (сохранён).
- **Макро-набор:** `L ENTER` (enabled если есть якорь-звезда — пристыкованная иначе выбранная;
  **это и чинит «не зайти в звезду»** — кнопка видима и никогда не проваливается в пустоту, плюс
  минует подавление клавиш при редактировании объёма в торговом окне), `G GO`/`X STOP` (маршрут),
  `T TRADE`, `U YARD`, `J REPAIR` (только пристыкован + есть урон), `H HIRE` (пристыкован),
  `PAUSE/PLAY` (on=пауза), `SPD X{1,2,5,10}` (цикл скорости).
- **Локальный набор:** `SPC FIRE` (по кулдауну), `M MINE`/`M STOP` (в зоне руды; on=добывает),
  `K DOCK` (в зоне рынка), `TAB LOCK` (есть цели; on=есть захват), **`C 3D`/`C MAP`** (переключатель
  кокпит⇄карта), `- ZOOM`/`+ ZOOM` (границы; в кокпите — оптический зум/FOV `localFovScale` 0.40..1.60,
  в карте — ортозум `localZoom`), `L EXIT`. Движение (W/S/A/D, R/F, **Q/E-крен**, Shift-варп) осталось на
  клавишах — «удержание» на кнопку не мапится.
- **Единый источник доступности добычи:** в sim добавлен `scene.minePrompt` (зеркалит `dockPrompt`;
  ближайший астероид в `MINE_RANGE`, `localsim.cpp`) + подсказка «PRESS M TO MINE» (`localdraw.cpp`;
  добыча перенесена с `E` на `M`, т.к. `E` теперь крен). UI не дублирует скан дальности — читает готовый флаг.
- **Раскладка:** один ряд по центру; `h=24, gap=6, pad=14, scale=1`; ширина кнопки `pad + len*6*scale`.
  В локале панель выше (`winH-h-34`), в макро ниже (`winH-h-6`); панели новостей/ивентов слева снизу
  подняты в `ui.cpp` (news 52→72, event 50→70), чтобы не перекрываться панелью.
- **Проверки:** `make` + оба smoke + ASan/UBSan smoke + `make soak` 120k — зелено, 0 ошибок.

### 5.12 [ИСТОРИЧЕСКОЕ] Технический фон: почему был вид «2D сверху» и что переход в 3D не ломал §2
> ✅ УСТАРЕЛО (2026-07-28): полный 3D **уже реализован** — см. **§5.13 (DONE)**. Вид-карта сверху
> теперь переключаемый (клавиша `C`), а не основной режим. Ниже — исходный технический фон
> (почему сначала был вид сверху и почему 3D укладывался в §2); оставлено как история решения.

Исходный вопрос пользователя: «почему игра 2D в системе? когда надо полное 3D».

**Мир уже трёхмерный — «2D» это только КАМЕРА.** Модель данных полностью 3D: у тел и камней есть
`x/y/z`, у орбит — наклонение (`inclination`), у игрока не только `pyaw`, но и `ppitch` (тангаж);
снаряды/лут/частицы живут в 3D. Проекция (`camera.h`) **ортографическая**: `depth` идёт только на
порядок отрисовки (painter) и затухание (fade) — это архитектурное правило §2.7, не случайность.

**Почему камера сейчас строго сверху (yaw=pitch=0):**
- `localView` создаётся дефолтным `View3D()`; заданы только центр (позиция игрока) и `scale` (зум),
  углы остаются 0 → чистый вид сверху (шапка `localdraw.cpp` это фиксирует; `main.cpp` §983–987).
- **Легибельность.** Вид сверху + 5×7 шрифт + векторные примитивы читаются как тактическая карта:
  орбиты — концентрические окружности, дистанции честные, ничто не загораживает цель. Это осознанный
  выбор под минималистичный стиль (§1.2), а НЕ техническое ограничение.
- Ось Z сейчас почти косметична: наклонение орбит и `ppitch` видны лишь через painter-порядок и
  fade, но не как ощущаемая «высота».

**Важно: полный 3D НЕ требует нарушать §2.** `makeCameraBasis(View3D)` уже принимает произвольные
yaw/pitch — то есть камеру-преследователь (chase-cam), поворачивающуюся за носом корабля, можно
включить **без перспективного деления, без новых библиотек, оставаясь в §2** (ортография, C++11, SDL2).
Это работа трёх шагов: (а) завязать `localView.yaw/pitch` на `pyaw/ppitch` игрока; (б) переписать
HUD/радар/off-screen-маркеры под вращающуюся камеру; (в) отладить читаемость (сейчас её даёт именно
неподвижный «вид сверху»).

**Когда полный 3D действительно НУЖЕН (а не просто «красивее»):**
- Когда вертикальный манёвр должен *решать* — настоящий бой по трём осям, уклонение по высоте,
  мгновенное чтение «цель выше/ниже меня». Сверху ось Z схлопнута, поэтому `ppitch` игрока сейчас
  почти не влияет на восприятие — это главный аргумент «за 3D».
- Когда навигация по Z станет содержательной (многослойные пояса, засады из-под плоскости эклиптики).

**Против (почему не сразу):** вид сверху максимально читаем «из коробки»; вращающаяся 3D-камера —
заметная работа по HUD/маркерам/радару и риск потерять ясность ради вида. Поэтому это **кандидат
роадмапа (§7.3), а не срочный долг.** Рекомендация: делать 3D **опциональным chase-cam режимом**
(переключатель клавишей/кнопкой), сохранив вид-карту сверху как основной тактический.

> ОБНОВЛЕНО §5.13: пользователь решил сделать 3D **основным** режимом (вид-карта — переключаемый),
> плюс LOD-скайбокс всего кластера. Открытый вопрос орто(§2.7)-vs-перспектива вынесен в §5.13.

### 5.13 Полный 3D + LOD-скайбокс кластера — ✅ РЕАЛИЗОВАНО (2026-07-28)
Решение пользователя (§5.2 п.5–6): локальный режим переведён в **полноценный 3D по трём осям,
полёт как в Elite**, и **весь звёздный кластер виден фоном как LOD-скайбокс**. Отменяет прежнюю
рекомендацию «3D опционально» из §5.12 — 3D теперь **основной** режим (вид-карта — переключаемый).
Архитектурный выбор проекции — **вариант (б): перспектива ТОЛЬКО в локальном режиме** (см. ниже);
пользователь явно выбрал «Перспектива (Elite)». §2.7 обновлён под это исключение.

**Что реализовано:**
1. **Полёт по 3 осям (Elite):** yaw + pitch + **roll**. Реализация — НЕ мировые углы Эйлера, а
   **тело-относительный ортонормированный базис** корабля: `pfwd` (нос), `pup` (верх), правый =
   `pfwd×pup`. Каждый кадр повороты применяются вращением Родрига вокруг собственных осей корабля
   (yaw вокруг `pup`, pitch вокруг right, roll вокруг `pfwd`), затем Грам–Шмидт ре-ортонормирует
   базис. Убирает gimbal lock, делает крен честным. Инерция (тяга+дрейф) сохранена. `localsim.cpp`.
   - **Управление:** A/D — yaw, R/F — pitch, **Q/E — roll**, W/S — тяга, Shift — варп. Добыча
     перенесена с `E` на **`M`** (конфликт устранён). См. §5.8.
2. **Камера из кокпита (от первого лица, срез 2 — 2026-07-28):** `eye = позиция корабля` (глаз в
   самом корабле), взгляд вдоль `pfwd`, верх `pup`; базис камеры = `makeCameraFrameBasis(pfwd, pup)`
   (`camera.h`). Проекция **перспективная** (`View3D::perspective=true`, `focal=winW·localFovScale`,
   `localFovScale` 0.40..1.60 — оптический зум на `+/−`; near-plane отсечение). Собственная стрелка
   игрока в полёте **не рисуется** — вместо неё центральная мушка-прицел + маркер прогрейда (вектор
   скорости) + дуга щита (§7.1.1). Переключатель **`C`** даёт ортогональный вид-карту сверху
   (`perspective=false`, старый top-down путь ПОБИТОВО сохранён; там жёлтая стрелка игрока осталась).
   Камера строится единым помощником **`buildLocalCamera` (`local.h`)** — тот же код для игры и для
   скриншот-харнеса (`make shots`), так что кадр в тесте побитово совпадает с игровым.
   - **Ориентир эклиптики (искусственный горизонт, 2026-07-28):** в кокпите рисуется «линия схода»
     плоскости системы (мировая X-Y, нормаль +Z; см. `localgen`) — набор направлений `(cosθ,sinθ,0)`
     через тот же `projectDirectionWithBasis`, что и скайбокс. **Наклон линии = крен, смещение
     относительно мушки = тангаж** относительно диска системы → снимает дезориентацию в 6DOF. Тела
     орбит лежат на этой линии (проверка корректности). В глубоком космосе (`!hasStar`) опорной
     плоскости нет — линия не рисуется. Тусклый холодный цвет, под мушкой. `localdraw.cpp` секция (8a).
   - **Лид-прицел + панель цели (читаемость боя, 2026-07-28):** для текущей цели в кокпите — **пип
     упреждения** (перехват в относительной скорости `u=targetVel−playerVel`, т.к. снаряд несёт скорость
     корабля; квадратка `(u·u−S²)t²+2(r·u)t+r·r=0`, наименьший `t>0`; янтарь/красно-янтарь, радиус 600 LU,
     гейт `<1500 LU`). Панель цели (сверху-справа) получила **полосу щита** (циан) и **скорость сближения
     `CLS`**. Строго аддитивно, врага не ослабляли. Детали — §7.1.1. `localdraw.cpp`.
3. **LOD-скайбокс из настоящего кластера:** реальные `ClusterStar` (`cluster.h`) рисуются фоном по
   истинным направлениям `normalize(star.pos − anchor)` в координатах кластера (`projectDirectionWithBasis`
   — точка «на бесконечности», важна только ориентация камеры). Якорь = текущая звезда, в глубоком
   космосе — центроид кластера. LOD по обратной дистанции от якоря (нормировка на среднюю): ближе
   среднего — крестик/2px и ярче, дальше — 1px и тусклее; цвет по `stellarClass`. Вращается вместе с
   кораблём → ориентация и «ты внутри скопления». Детерминированно, без глобального `rng`. `localdraw.cpp`.

**Как это уложилось в §2 (архитектура):**
- **Перспектива — только локальный режим.** Ветка `perspective==false` в `projectPointWithBasis`
  осталась ПОБИТОВО как исходник → макро/кластер/карта не изменены (§2.7 обновлён под исключение).
- Никакого ООП/`virtual`/`new`-иерархий: добавлены поля в `View3D`/`ProjectedPoint`/`CameraBasis`
  (POD) и свободные функции `makeCameraFrameBasis`/`projectDirectionWithBasis`. C++11 + SDL2.
- Локальный рендер по-прежнему НЕ трогает глобальный `rng` (скайбокс — из данных кластера и хешей).

**Рендер (`localdraw.cpp`) — одна функция обслуживает оба режима, ветвясь на `view.perspective`:**
- Экранный радиус тела: перспектива `R·focal/depth`, орто `R·scale`. Порядок — painter по глубине.
- Тряска: перспектива — угловое дрожание камеры (равномерный сдвиг на любой глубине); орто — сдвиг центра.
- Плоские кольца (орбиты/кольца гиганта/орбита луны) рисуются ТОЛЬКО в орто-карте (в перспективе это
  были бы неверные эллипсы). Фейд сцены мягче в перспективе (глубина достигает тысяч LU).
- Радар — тело-относительный: `right`/`pfwd` (нос → вверх радара). Off-screen-маркеры учитывают `behind`.

**Файлы:** `camera.h` (перспектива + frame-базис), `local.h` (`pfwd`/`pup` + roll-инпут + хелперы
`localShipRight`/`localSetForward` + **`buildLocalCamera`** — единый билд камеры для игры и харнеса,
включает `camera.h`), `localsim.cpp` (вращения Родрига + Грам–Шмидт), `localgen.cpp`
(стартовая ориентация + Z-разброс тел/пояса), `main.cpp` (`buildLocalCamera` кокпит/карта, `localFovScale`,
клавиши Q/E/M/C, `+/−`), `localdraw.cpp` (перспективный рендер из кокпита + мушка/прогрейд + LOD-скайбокс
+ режимо-зависимый HUD/радар + потолки размера частиц + **лид-пип упреждения и панель цели со щитом/
сближением**), `soak_test.cpp`/`shot_test.cpp` (наведение через `localSetForward`; харнес рендерит кадры
тем же `buildLocalCamera`, включая кадр `shot_combat_lead` с целью наперерез).

**Проверки (все зелёные, 0 ошибок):** `make`; `--smoke`/`--localsmoke` (exit 0); `make asan` +
оба smoke под ASan/UBSan (exit 0) + **харнес под ASan/UBSan** (гоняет `renderLocalScene` с активной целью);
`make soak` — **120000 кадров, invariantFails=0, deathPath=ok**; `make shots` — 6 кадров (6/6 ok),
просмотрены визуально.

**Плейтест №1 (2026-07-28):** 3D подтверждён как улучшение; бой ощущается «сочно» (не ослаблять).
Открытый пункт «жёлтая стрелка игрока в chase-виде выглядит странно» → **✅ ЗАКРЫТ в срезе 2**:
переход на вид из кокпита (от первого лица), стрелка убрана в полёте, добавлены мушка + маркер
прогрейда. Детали и запись «как сделано» — **§7.1.1**.

#### 5.13.1 Проход углубления №3 — плейтест №2: гигантская звезда, окклюзия, мышелук (2026-07-28)
Фидбек №2 (§7.1.2) вскрыл 5 пунктов. Ключевая идея пользователя — **чистая геометрия, никаких
моделей**: «звезда это координаты центра + радиус, всё; мы не рисуем звезду — мы аналитически знаем
сферу и локально заполняем ею пространство». Реализовано ровно так — везде через `хиты луч↔сфера`,
без буфера глубины и без хранения меша.

1. **Аналитический рендер звезды как сферы (не диск по дистанции).** Звезда — сфера в начале мира
   `(0,0,0)` с радиусом `starRadius`. Экранный радиус диска берём из геометрии касательной, а не из
   `R·focal/D`: `D = |eye| = sqrt(cx²+cy²+cz²)` (центр звезды в мировом нуле), угловой радиус
   `α = asin(R/D)`, **`screenR = focal·tan(α) = focal·R/sqrt(D²−R²)`**. При `D≤R` (глаз внутри вещества
   звезды) — **заливка всего экрана** плазмой. Это чинит «масштаб»: у поверхности `D→R` радиус
   корректно уходит в бесконечность (старый `R·focal/D` насыщался и звезда казалась мелкой). Диск —
   аддитивная корона + 3 концентрических заливки с лимб-даркенингом (тёмный край → яркое ядро).
   Клампится до `4·max(winW,winH)`. Ветка только для `perspective`; в орто-карте (`C`) — прежний диск.
   `localdraw.cpp`.
2. **Окклюзия луч↔сфера (планеты больше не просвечивают сквозь звезду).** Свободная функция
   `segIntersectsSphere(eye, point, center=0, R)`: отрезок глаз→тело перекрыт, если ближний корень
   `t0 = −b − sqrt(b²−c)` лежит в `[0,L]`. Лямбда `occ(wx,wy,wz)` (активна только при `hasStar &&
   perspective`) прогоняется для КАЖДОГО типа объектов перед отрисовкой: тела/астероиды/лут/корабли/
   выстрелы/частицы `→ continue|return`; радиоисточники за звездой становятся edge-маркерами вместо
   точки. Без z-буфера (painter-only 2D) — окклюзия чисто аналитическая. `localdraw.cpp`.
3. **Гигантский масштаб (тысячи планет влезут в звезду, корабли — ничто).** `localgen.cpp`:
   `starRadius = 90 + metallicity·45` (90..135 LU; было 28+met·10). Планеты: скалистые `2.5..5`,
   газовые `7..14`, ледяные `3.5..7`; пояс `0.6..1.8`; станция `3.0`; луны `1.0..2.4` (в `local.h`
   дефолты `LocalBody.radius 8→3`, `LocalScene.starRadius 30→110`). Первая орбита отодвинута за
   поверхность: `starRadius·1.55 + …`; шаг орбит `160..320`. Магический радиус кораблей в кокпите
   `radiusPx(3.0→1.6)`. Итог: звезда доминирует кадр, тела/корабли — крапинки (визуально подтверждено).
4. **Мышелук (FPS-обзор в кокпите).** `SDL_SetRelativeMouseMode` включается, когда `localScene.active
   && !localMapMode && !localSmoke` (`main.cpp`); дельты `SDL_GetRelativeMouseState` × `LOCAL_MOUSE_SENS
   (0.0025)` → `li.mouseYaw = dx·s`, `li.mousePitch = −dy·s`. В `LocalInput` — поля `mouseYaw/mousePitch`
   (это **готовые углы за кадр**, НЕ умножаются на `turn`). В `localsim.cpp` вложены в тот же поворот
   Родрига: `yawA = (клав) − mouseYaw`, `pitchA = (клав) + mousePitch` (мышь вправо→нос вправо,
   мышь вверх→нос вверх). ЛКМ в кокпите теперь **стреляет** (`localFireHeld`), а панель действий (§5.11)
   ловит клики только в карте. Клавиши A/D/R/F/Q/E остаются как резерв. `main.cpp`/`local.h`/`localsim.cpp`.
5. **Синий квадрат при стрельбе — баг устранён.** Причина: муззл-частица рождалась в самом глазу
   (камера в корабле) и `R·focal/depth` раздувала её на пол-экрана синим прямоугольником. Фикс:
   спавн смещён вниз от линии прицела (`−pup·1.6`) и вперёд (`+dir·5`), `size 0.8`, `life 0.05`;
   рендер `FX_MUZZLE` — **аддитивный** двойной bloom (цвет + белое ядро), кап размера `90→14`.
   `localsim.cpp` (спавн) + `localdraw.cpp` (рендер). Визуально: маленькая белая вспышка, без квадрата.

**Проверки прохода №3 (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0);
`make asan` + оба smoke под ASan/UBSan (0 ошибок); `make soak` — **120000 кадров, invariantFails=0,
deathPath=ok**; `make shots` — **10 кадров** (добавлены `shot_star_near`, `shot_star_occlusion`,
`shot_muzzle`, `shot_star_inside`), просмотрены визуально: гигантская звезда с лимб-даркенингом,
дальняя планета скрыта звездой (ближняя видна), маленькая аддитивная вспышка выстрела, заливка
плазмой изнутри звезды.

#### 5.13.2 Проход углубления №4 — плейтест №3: геометрия D/R, звезда доминирует ВЕЗДЕ (2026-07-28)
Фидбек №3 (§7.1.3): «масштаб всё равно звезда маленькая… понял мою идею?». Мышелук и
аналитический рендер сферы (§5.13.1) работают, НО в реальном полёте звезда мелкая. Причина —
**геометрия, а не рендер**.

**Ключевой инсайт (resolution-independent).** Экранный радиус звезды зависит ТОЛЬКО от отношения
`D/R` (дистанция до центра ÷ радиус), не от абсолютного масштаба: `screenR = focal·R/sqrt(D²−R²)`,
`focal = winW·fovScale`. Доля высоты кадра (16:9, fovScale 0.60): `fill ≈ 2.133 / sqrt((D/R)²−1)`.
Значит равномерное раздувание всех LU-величин НЕ меняет вид — важно только отношение орбит к
радиусу. Проверено: D/R=2.8→~82%, 3.44→~65%, 2.4→~98%, 1.45→100%.

**Почему было мелко.** §5.13.1 растил абсолютный `starRadius` (90..135), но орбиты шли `R·1.55`,
затем `+160..320` за планету (до 6) → внешние орбиты ≈ **R·13** → D/R≈13 → звезда ~5% экрана.
Игрок почти всегда далеко. Фикс — задать **орбиты как доли R** и держать D/R малым по всей системе.

1. **Орбиты как доли радиуса (`localgen.cpp`).** Двухветочная формула заменена на линейную по `i`,
   count-independent: `orbitRadius = starRadius · (1.20 + 0.42·i + frand(0, 0.14))`. При 6 планетах
   внешняя ≤ **R·3.44**. Кеплер `orbitSpeed = 40/pow(orbit,1.5)` оставлен.
2. **Звезда-гигант ×2.9 (`localgen.cpp`).** `starRadius = 260 + met·100` (260..360 LU; было 90..135).
   Тела крупнее для видимости, но ≪ R: скалистые `6..12`, газовые `18..38`, ледяные `9..18`,
   пояс `1.5..4.5`, станция `9`, луны `3..7` (offset `frand(14,34)+moonNo·frand(10,20)`).
3. **Выбор игрока — Option B «Доминирует + воздух».** Внешние орбиты ≤ R·3.4: звезда доминирует
   всюду (≥~60% высоты), но есть воздух для навигации/боя; крошечные планеты для контраста. Старт
   `px = starRadius·2.8` (D/R≈2.8, звезда ~82%, нос к звезде вдоль −X). py=pz=0.
4. **Спавны не внутри звезды.** Нижние границы NPC (co-located/traders/pirates) и радио падали
   ВНУТРЬ нового 260..360 LU радиуса → подняты к `starRadius·1.3` / `innermostOrbit`; радио сжато
   до `outermostOrbit·1.1` (не улетает в D/R>5). Геймплейные радиусы (MINE/DOCK/SCOOP/CLAIM/AI/radar/
   скорость) НЕ трогали — абсолютные дистанции сопоставимы со старыми, они пропорциональны.
5. **Порог заливки R·1.7 (`localdraw.cpp`).** `if (D <= R*1.0008)` → `if (D <= R*1.7)`: диск и так
   переполняет экран уже при D/R≈1.45, поэтому переход бесшовный, а тангенциальный тёмный зазор в
   полосе (1.0, 1.7)·R исчезает. Близкие планеты рисуются шагом (4) ПОСЛЕ звезды → видны поверх заливки.
6. **Фикс «спиц» эклиптики (`localdraw.cpp`).** Ориентир-эклиптика при near-edge-on направлениях:
   `focal/camZ` (порог `camZ>0.06`) выбрасывал соседние точки за экран в разные стороны → длинные
   лучи. Порог поднят до `camZ>0.14` — near-edge-on отсекается раньше, лучи к бесконечности не рисуются.

Важная находка (инвентарь констант): **физической коллизии со звездой/телами НЕТ** — влёт в звезду
чисто косметический (occ + заливка), смерть только от HP→0. Тугая упаковка орбит вокруг гиганта
безопасна. `shot_test.cpp` по дистанциям не трогали (сценарии ratio-based на `starRadius`,
авто-подстройка), только коммент near-сценария → «D/R=1.25<1.7 ⇒ стена плазмы».

**Проверки прохода №4 (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0);
`make asan` + оба smoke под ASan/UBSan (0 ошибок); `make soak` — **120000 кадров, invariantFails=0,
deathPath=ok** (генерация здоровая: тела 7–11, камни 47–84, корабли 2–5, радио 2–4); `make shots` —
**10/10**, просмотрены: `shot_system_cockpit` (звезда ~80% высоты + планеты-пылинки + чистая линия
эклиптики, спиц НЕТ), `shot_star_occlusion` (~98%, ближняя планета видна, дальняя скрыта),
`shot_system_rolled` (наклонная эклиптика без спиц), `shot_star_near` (бесшовная заливка без зазора).

#### 5.13.3 Проход углубления №5 — плейтест №4: изотропная сфера-плазма (ray-sphere «шейдер») (2026-07-28)
Фидбек №4 (§7.1.4): «баг костыль… подлетаю к звезде — как будто переключается режим и всё
становится жёлтым причём со всех сторон… звезда сделана неправильно, ты что-то нахардкодил, а надо
было заполнять пространство как функция от радиуса всех тел… без костылей… используй шейдеры чтобы
звезда прям была шаром плазмы… рендер вообще бедный». Орбиты/орбитальное движение (§5.13.2) —
одобрены («по орбите вращаются круто»), НЕ трогаем.

**Диагноз (баг подтверждён).** §5.13.2 п.5 `if (D <= R*1.7) fillRect(весь экран, плазма)` — это
костыль-заглушка: при заходе ближе 1.7·R **весь кадр** заливался ровным цветом НЕЗАВИСИМО от
направления взгляда → «жёлтый со всех сторон», в т.ч. когда смотришь ПРОЧЬ от звезды. Плюс биллборд-
диск (`fillCircle` вокруг проекции центра) — не изотропен и не физичен. Пользователь прав: тело — это
**центр + радиус**, а плазма должна быть **функцией геометрии луча**, одинаковой со всех сторон.

**Решение — попиксельный аналитический ray-sphere «шейдер»** (`renderStarPlasma` в `localdraw.cpp`),
заменил ОБА старых пути (заливку и диск). Никаких порогов и «переключения режима»:
1. **Изотропия по построению.** Для каждого пикселя восстанавливаем мировой луч из глаза
   (`dir = right·(sx/focal) + up·(−sy/focal) + fwd`, нормировка) и аналитически пересекаем со сферой
   (центр 0,0,0, радиус R): `b=O·dir`, `dmin² = |O|²−b²`. Попадание ⇔ `dmin²≤R²` И дальний корень
   `t1=−b+√(R²−dmin²)>0` (иначе сфера позади — промах). Результат зависит только от R и |eye|
   относительно луча ⇒ смотришь на звезду — плазма, отвернулся — космос, влетел внутрь (D<R, вход
   te=0 от глаза) — вещество вокруг с направленной структурой. Ноль хардкода направления.
2. **Плазма-шар (шейдинг из геометрии).** Лимб-даркенинг `mu=√(1−dmin²/R²)`; многооктавная
   турбулентность с доменным варпом (3 `sin` по мировой нормали поверхности + дрейф по `fxClock`) →
   «кипящие» филаменты/гранулы, не гладкий градиент. Цветовая рампа лимб(глубокий оранж-красный)→ядро
   (бело-горячее) по `mu²`. Запечённая корона за лимбом (промахи с `b<0`, `dmin∈(R, 1.55R)`, спад g²).
3. **Рендер (SDL2-only, без GL).** Персистентная стриминг-текстура ARGB8888 половинного разрешения,
   `SDL_BLENDMODE_BLEND` + линейный upscale: диск непрозрачен (alpha=255 → честно перекрывает скайбокс,
   согласуясь с ray-sphere окклюзией `occ`), корона полупрозрачна. Далёкую звезду считаем только в bbox
   проекции (`screenR·1.55`), вблизи/внутри — весь кадр. Чистый SDL — детерминированные шоты (софт-
   рендерер) продолжают работать; шейдер покрыт ASan/UBSan через `shot_asan`.
4. **«Спицы».** Их источник в скрине №4 — снятые концентрические `strokeCircle` короны; после удаления
   диска остаётся ТОЛЬКО задуманная одиночная линия-эклиптика (искусственный горизонт, `camZ>0.14`),
   на edge-on центрированной звезде — одна чистая горизонталь, радиальных лучей нет.

**Новые сценарии шотов (`shot_test.cpp`).** Добавлен **решающий** `shot_star_lookaway` (глаз D/R=1.3,
нос ПРОЧЬ от звезды → обязан быть ЧЁРНЫЙ космос, не плазма — прямая проверка, что костыль-заливка
удалён и рендер изотропен). Комменты near/inside переписаны под сферу (не «стена/заливка»).

**Проверки прохода №5 (все зелёные):** `make` (0 предупр.); `--smoke`/`--localsmoke` (exit 0);
`make asan` + оба smoke (0 ошибок ASan/UBSan); **`shot_asan`** — все 11 сценариев под ASan/UBSan
(покрывает lock/попиксельную запись шейдера, вкл. полноэкранный inside-star) — 0 ошибок; `make soak`
— **120000 кадров, invariantFails=0, deathPath=ok**; `make shots` — **11/11**, просмотрены:
`shot_star_lookaway` (ЧЁРНЫЙ космос вблизи, глядя прочь — баг устранён, изотропно ✔),
`shot_system_cockpit`/`shot_star_near`/`shot_star_inside` (богатый плазма-шар: лимб-даркенинг +
филаменты + корона), `shot_star_occlusion` (FRONT на диске виден, BACK за сферой честно скрыт).

---

## 6. Методология проверок (применяй ко всему новому коду)

### 6.1 Параллельная сборка субагентами (проверенный паттерн)
Крупную фичу дели на **несколько .cpp, по одному на субагента**, а общие заголовки/контракты/
интеграцию (`*.h`, `main.cpp`, `Makefile`) держи за собой (оркестратор). Так три файла
локального режима (`localgen/localsim/localdraw`) писались параллельно без конфликтов.
Каждый субагент валидирует свой файл в изоляции:
```bash
c++ -std=c++11 -I/opt/homebrew/include/SDL2 -D_THREAD_SAFE -fsyntax-only <file>.cpp
```
`-fsyntax-only` делает полный семантический анализ (ловит несуществующие имена/поля), но НЕ
линковку. Реальная проверка связок — `make`.

### 6.2 Уровни проверки (все должны быть зелёными перед сдачей среза)
1. `make` — сборка без ошибок.
2. `--smoke` (макро) и `--localsmoke` (локал) в обычной сборке → exit 0.
3. ASan+UBSan сборка + оба smoke → exit 0, **0 ошибок** (§3.1). Ловит то, что exit-0 не ловит.
4. `make soak` — детерминированный soak-тест локального режима под ASan/UBSan → exit 0, 0 инвариант-фейлов (§6.5).

### 6.3 Урок этой сессии (реальный баг, найденный ASan)
Глиф `'B'` в таблице шрифта был **30 символов вместо 35** → `drawText` читал за границей
строкового литерала (`global-buffer-overflow`). Баг был **латентным и в макро-режиме** (обычная
сборка его не ловит — только ASan). Исправлено; таблица теперь единственная (в `render2d.cpp`),
все 48 глифов проверены на длину 35:
```bash
grep -oE 'return "[01]+";' render2d.cpp | grep -oE '[01]+' | \
  awk '{ if (length($0)!=35) print "BAD ("length($0)"): "$0 }'
```
**Вывод:** для чужого/нового кода всегда прогоняй ASan; не доверяй одному exit 0.

### 6.4 Дисциплина
- `<task-notification>`, `<system-reminder>`, «task tools haven't been used» — это **автоматические
  события, НЕ ввод пользователя**. Не считай их подтверждением/согласием.
- Убирай за собой временные артефакты (`game_asan`, `soak_asan`, `*.dSYM`) — `make clean` их сносит.

### 6.5 Soak-тест локального режима (`make soak`, файл `soak_test.cpp`)
Зачем: 12-кадровые headless-smoke почти не двигают время и всегда идут по CASE B генерации, поэтому
НЕ покрывают глубокий космос, многооборотные луны, радио-клейм/бой/смерть. Soak — отдельная `main()`
(линкуется с `LIBSOURCES`, без `main.cpp`), гоняет `updateLocalScene` по ~10 системам + глубокому
космосу тысячами кадров, ведёт игрока к радио/кораблям/телам, каждый кадр проверяет инварианты
(конечность координат, `fx<=FX_MAX`, отсутствие разрастания векторов) и отдельно детерминированно
проверяет ветку смерти игрока (враждебный пират вплотную, корпус=1). Последний прогон: 120k кадров,
0 фейлов, 0 санитайзер-ошибок. **Держи soak зелёным при правках локального режима.**

---

## 7. Дорожная карта / что дальше

### 7.1 Немедленно: фидбек по локальному режиму
Срез 1 + два прохода углубления (§5.10) в дереве, ждут плейтеста. Вопросы, по которым крутить дальше:
- **Масштаб/зум и скорости** — не слишком ли пусто/медленно даже с варпом? (крутилки — §5.4).
- **Читаемость HUD/радара/частиц** — панели, бары, off-screen маркеры, радар, тряска, размеры шрифта.
- **Ощущение боя и добычи** — урон/награды/щиты/скорость добычи/дальности/лут.
- **Баланс смерти** — штраф аварийного прыжка (30% корпуса, −20% денег) — не слишком мягко/жёстко?
- **Радио/детекторы** — интересны ли награды, верны ли пороги детектора (`sensors` 1.08 / 1.20)?

Когда придёт фидбек — итерируй по нему (это и есть «дальше» в цикле).

#### 7.1.1 Плейтест №1 — 3D-режим (фидбек пользователя, 2026-07-28)
Первый живой плейтест после перехода в полный 3D (§5.13). Дословно: «уже лучше 3д, но стрелочка
жёлтая игрока как-то странно выглядит, надо как бы вид из "глаз" камеры корабля; также меня убил
пират сразу, но вот это плюс — сражение реально ощущается сочно».

- ✅ **3D подтверждён как улучшение** — направление верное, продолжаем.
- ✅ **Бой ощущается «сочно» (juice работает).** Пират убил игрока сразу — пользователь считает это
  **плюсом** (интенсивность/напряжение). **НЕ ослаблять** урон/агрессию ИИ ради «мягкости»: juice
  из прохода 1 (§5.10) валидирован реальным плейтестом. Баланс сложности трогать только если
  дальнейший фидбек скажет «слишком карательно» — по умолчанию оставить как есть.
- ✅ **СДЕЛАНО (срез 2, 2026-07-28): вид из «глаз» корабля (кокпит / от первого лица).**
  Жёлтая стрелка игрока в полёте убрана; камера-преследователь заменена на кокпит. Как сделано
  (совпадает с планом ниже, всё выполнено):
  - Глаз камеры **в позиции корабля** (`buildLocalCamera`: `center = {px,py,pz}`, `eye=ship`),
    взгляд вдоль `pfwd`, верх `pup`; базис `makeCameraFrameBasis(pfwd,pup)`.
  - Собственная стрелка игрока **не рисуется** в перспективе (`localdraw.cpp`, ветка `else` секции
    ИГРОК). Вместо неё центральная **мушка-прицел** (кольцо + перекрестие в проекции `player+pfwd·600`,
    фолбэк в центр экрана если позади) — естественный центр по курсу стрельбы.
  - Индикатор щита игрока — **дуга-оверлей** вокруг мушки; добавлен **маркер прогрейда** (вектор
    скорости, зелёный, `player+velocity·(600/speed)` если `speed>0.5`).
  - **Оптический зум/FOV** на `+/−` (`localFovScale` 0.40..1.60, `focal=winW·localFovScale`) заменил
    прежний долли chase-камеры.
  - **Стрелка игрока сохранена в карте сверху** (ветка `!view.perspective`) — там она читаема как
    тактический курсор; переключатель `C` оставлен.
  - **Потолки размера частиц** по типу (TRAIL/SMOKE 8, DEBRIS 22, SPARK 40, MUZZLE 90, RING 300) +
    near-cull — иначе близкий выхлоп (глаз теперь в корабле) раздувался `R·focal/depth` на весь экран.
  - Проверки пройдены — полная лестница §6.2 + soak 120k (0 фейлов) + `make shots` (кадры просмотрены).
    Закоммичено (`58e7136`), затем ориентир эклиптики (`ef7609a`).
- ✅ **СДЕЛАНО (2026-07-28): читаемость боя — лид-прицел + панель цели (щит/сближение).**
  Проактивный полиш под похвалу «сражение реально ощущаются сочно» — **строго аддитивно, врага НЕ
  ослабляли** (стоячий инвариант «combat feels juicy — DO NOT nerf»). Как сделано (`localdraw.cpp`):
  - **Лид-пип (упреждение)** в кокпите для `scene.targetCraft`: снаряд игрока несёт скорость корабля
    (`p.vx = pvx + dir·PROJ_SPEED`, localsim), поэтому перехват решается в относительной скорости
    `u = targetVel − playerVel`: `(u·u−S²)t² + 2(r·u)t + r·r = 0`, `S=PROJ_SPEED=520`, берём наименьший
    `t>0`. Направление прицела ∝ `r+u·t`; пип на радиусе 600 LU (как мушка). Янтарный (нейтрал) /
    красно-янтарный (враг). Гейт: дистанция `<1500 LU`. При стоячей цели по курсу пип совпадает с мушкой.
  - **Панель цели** (сверху-справа): добавлены **полоса щита** (циан, над корпусом, если `maxShield>0`;
    панель растёт до 72 px) и **скорость сближения** `CLS %+.0F` (проекция `−(r·u)/dist` на луч; `+` =
    сближаемся → янтарь, `−` → серый). Хелперы `panel/bar/drawText/textWidth`, шрифт 5×7 (UPPERCASE, `%.0F`).
  - Харнес: 6-й кадр **`shot_combat_lead`** — цель наперерез (vy=220), нос на текущую позицию цели,
    статичный детерминированный рендер (сим не шагаем): янтарный пип заметно смещён от мушки.
  - Проверки: полная лестница §6.2 + **харнес под ASan/UBSan** (гоняет новый draw с активной целью,
    0 ошибок) + soak 120k (invariantFails=0) + `make shots` 6/6 (кадры просмотрены).

#### 7.1.2 Плейтест №2 — масштаб, окклюзия, обзор (фидбек пользователя, 2026-07-28)
Второй живой плейтест. Дословно: «движение должно ловить курсор (в локальном мире у нас типа как
шутер); сквозь звезду просвечивают планеты; про масштабы — сделай умно, звезда должна быть гигантской,
чтоб в неё влезли тысячи планет, а корабли — ничто в сравнении; тебе не надо держать в памяти никаких
моделек, всё абсолютно контекстно и геометрически просто — есть поле, наш 3D-мир, и там где рядом
звезда — там просто заполняется её веществом пространство, тупо сфера, у нас чистая геометрия, так что
аналитически через сферы мы можем локально показывать гигантские структуры (мы рисуем не звезду — мы
знаем координаты и радиус); при стрельбе какой-то синий квадрат — это точно баг».

Все 5 пунктов **✅ СДЕЛАНЫ** (реализация и «как сделано» — **§5.13.1**):
- ✅ **Мышелук (шутерный обзор).** `SDL_SetRelativeMouseMode` в кокпите; дельты мыши → yaw/pitch,
  вложены в поворот Родрига. ЛКМ стреляет. §5.13.1 п.4.
- ✅ **Планеты не просвечивают сквозь звезду.** Аналитическая окклюзия луч↔сфера для всех объектов.
  §5.13.1 п.2.
- ✅ **Гигантская звезда «умно», через геометрию.** Принято дословно: звезда = центр `(0,0,0)` +
  `starRadius`, рисуется аналитически (касательная к сфере → экранный радиус; изнутри — заливка
  плазмой). Никаких мешей/моделей. §5.13.1 п.1.
- ✅ **Масштаб: тысячи планет в звезду, корабли — ничто.** `starRadius 90..135`, планеты единицы LU,
  корабли-крапинки. §5.13.1 п.3.
- ✅ **Синий квадрат при стрельбе.** Был муззл в самом глазу, раздутый перспективой; спавн смещён +
  аддитивный bloom + кап размера. §5.13.1 п.5.

Открытых пунктов по плейтесту №2 нет.

#### 7.1.3 Плейтест №3 — масштаб звезды (фидбек пользователя, 2026-07-28)
Третий живой плейтест после мышелука + аналитической сферы (§5.13.1). Дословно: «да мышелку
работает, но масштаб всё равно звезда маленькая, либо я не пон… ну понял мою идею? [скриншоты]
уже лучше». Мышелук подтверждён; масштаб всё ещё не тот.

✅ **СДЕЛАНО (реализация — §5.13.2):** диагноз — вид звезды зависит только от `D/R`, а орбиты
уходили к R·13. Орбиты переписаны как доли радиуса (внешняя ≤ R·3.4), звезда ×2.9 (260..360 LU),
старт D/R≈2.8 (~82% высоты). Звезда теперь доминирует кадр на всём пути от старта к внешней планете
(визуально подтверждено на шотах). Заодно: фикс «спиц» эклиптики + бесшовный порог заливки R·1.7.

Открытых пунктов по плейтесту №3 нет. Масштаб принят; плейтест №4 вскрыл отдельную проблему —
рендер самой звезды (см. §7.1.4).

#### 7.1.4 Плейтест №4 — звезда-плазма, изотропия, «бедный рендер» (фидбек пользователя, 2026-07-28)
Четвёртый живой плейтест (масштаб §5.13.2 одобрен: «по орбите вращаются круто»). Дословно: «баг
костыль и говно — если я подлетаю к звезде, как будто переключается режим и всё становится жёлтым,
причём со всех сторон, это точно баг. Я дал тебе задачу математическую изотропную — звёзды и планеты
это просто точки в пространстве, у них 2 параметра: радиус и положение (орбитируют — сделано хорошо).
Но звезда сделана неправильно, ты что-то нахардкодил, а надо было заполнять пространство как функция
от радиуса всех тел в системе, без костылей, с физичными масштабами. Рендер вообще бедный — используй
шейдеры, чтобы звезда прям была шаром плазмы».

✅ **СДЕЛАНО (реализация — §5.13.3):** баг подтверждён — `if (D≤R*1.7) fillRect(весь экран)` заливал
кадр плазмой независимо от направления взгляда («жёлтый со всех сторон»). Костыль-заливка И биллборд-
диск удалены, заменены **попиксельным аналитическим ray-sphere «шейдером»** (`renderStarPlasma`):
изотропная сфера (центр+радиус), плазма как функция геометрии луча (лимб-даркенинг + многооктавная
турбулентность + корона), рендер в стриминг-текстуру (SDL2-only). Никаких порогов/режимов. Решающая
проверка `shot_star_lookaway`: вблизи, глядя ПРОЧЬ от звезды — чёрный космос, не плазма (изотропно).
Орбиты не тронуты. Ждём живого плейтеста №5 (ощущение плазма-шара на реальном полёте, богатство fx).

### 7.2 Радиосигналы + детекторы — СДЕЛАНО (проход 2, §5.10)
Реализовано в локальном режиме: `LocalRadioSource` в глубоком космосе и системах, детектор из
`game.tech.sensors` (3 тира), reveal по дальности с бонусом тира, клейм = деньги + исследование +
элемент, HUD-радар. Дальнейшее углубление (нейтринный / тёмной материи как отдельные модули
прогрессии, связка с `anomaly.cpp` / plans_10, редкие эндгейм-источники) — открытый простор, если
пользователь захочет.

### 7.3 Кандидаты на усиление локального режима (идеи, не обязательства)
Сделано в проходах 1–2 (§5.10): живой урон/смерть игрока + аварийный прыжок, двигающиеся/вращающиеся
астероиды, реакция фракций на киллы, ИИ с отступлением на низком корпусе, juice (следы/вспышки/тряска).
Ещё открыто:
- Продолжение макро-симуляции в фоне (или «прошло N времени» по выходу из локала).
- Больше жизни в системе: стыковка NPC, внутрисистемные торговые маршруты, группы/эскорты.
- Глубже ИИ: звенья / wingman-тактика, скоординированные манёвры, отклонение от препятствий.
- Экономика лута/радио: редкие модули-дропы, цепочки квестов-сигналов, привязка к контрактам.
- ✅ **Полный 3D-обзор + LOD-скайбокс кластера — СДЕЛАНО (§5.13).** Перспективная камера от корабля,
  3 оси (+roll) на тело-относительном базисе, HUD/радар под вращение, фон из реальных `ClusterStar` по
  LOD. §2.7 решён (перспектива только в локале). Дальнейшее (полировка ощущения/баланса) — по плейтесту.

### 7.4 Незакрытый контент-бэклог (см. `plans_*.md` и `design.md`)
Контентные системы plans_1..10 реализованы, но всегда есть простор углублять экономическую спираль
из `design.md` (медитативный гринд, капитал → возможности → больше капитала). При заходе в любую
систему сначала перечитай её `plans_N.md`.

---

## 8. Как расширять этот документ

Этот файл — точка входа для будущих агентов. Держи его актуальным:
- Завершил фичу/срез → обнови §5/§7 (что сделано, что дальше), добавь новые файлы в §4.
- Нашёл новый инвариант/грабли → добавь в §2 (ограничения) или §6.3 (уроки).
- Изменил тюнинг/крутилки → синхронизируй §5.4 с `local.h`.
- Новая крупная механика → отдельный раздел по образцу §5 (идея → решения пользователя →
  данные → контракты → интеграция → управление → упрощения) + строка в §4.
- Не дублируй код здесь — ссылайся на файлы и функции. Документ описывает «почему/где», код — «как».

### Инварианты статуса на момент написания (2026-07-28)
- Ветка `local-flight-mode` (ответвлена от `main` по конвенции «на `main` — сперва ветка»).
  Сборка зелёная. Оба smoke + `make soak` (обычные и ASan/UBSan) — exit 0, 0 ошибок.
- Локальный режим полёта: **срез 1 + два прохода углубления (§5.10) + полный 3D (§5.13) + вид из
  кокпита (срез 2, §7.1.1) + ориентир эклиптики + читаемость боя (лид-пип/панель цели) готовы и
  провалидированы**. Покрытие: soak 120k кадров (радио-клеймы, киллы, луны, смерть + аварийный прыжок) —
  0 инвариант-фейлов; `make shots` — 6/6 кадров (вкл. `shot_combat_lead`).
  **Плейтест №1 пройден (§7.1.1):** 3D ок, бой сочный; пункт «вид из кокпита» **закрыт** в срезе 2.
- **Единая панель действий (§5.11) — свежий срез, ждёт плейтеста.** Правит `main.cpp`/`local.h`/
  `localsim.cpp`/`localdraw.cpp`/`ui.cpp`; все уровни проверки (§6.2) зелёные.
- **§5.13 ✅ РЕАЛИЗОВАНО (не только зафиксировано):** полный 3D по 3 осям (Elite, +roll,
  тело-относительный базис) + **перспективная камера из кокпита (от первого лица, срез 2)** +
  переключаемый вид-карта (клавиша `C`) + LOD-скайбокс всего кластера. Камера — единым
  `buildLocalCamera` (игра и харнес `make shots`). Архивопрос §2.7 решён — **перспектива только в
  локальном режиме** (выбор пользователя «Перспектива (Elite)»). Все уровни проверки (§6.2) + soak
  120k + `make shots` — зелёные. **Закоммичено** (`58e7136` 3D+кокпит, `ef7609a` горизонт,
  `87230d5` лид-пип).
- **§5.13.1 ✅ РЕАЛИЗОВАНО (проход углубления №3, плейтест №2):** аналитическая звезда-сфера +
  окклюзия луч↔сфера + гигантский масштаб + мышелук + фикс синего квадрата. Все проверки + soak 120k
  + 10 шотов зелёные. Правки: `localdraw.cpp`/`localgen.cpp`/`localsim.cpp`/`local.h`/`main.cpp`/
  `shot_test.cpp` + этот документ (коммитятся по стоячей директиве «комит пуш и работай дальше»).
- **§5.13.2 ✅ РЕАЛИЗОВАНО (проход углубления №4, плейтест №3):** орбиты переписаны как доли радиуса
  (вид звезды зависит только от D/R; внешняя орбита ≤ R·3.4), звезда ×2.9 (260..360 LU), старт D/R≈2.8
  → звезда доминирует кадр по всей системе; + фикс «спиц» эклиптики и бесшовный порог заливки R·1.7.
  Правки: `localgen.cpp`/`localdraw.cpp`/`shot_test.cpp` + этот документ. Все проверки + soak 120k +
  10 шотов зелёные.
- Весь код и документы (локальный слой + углубление + 3D-переход + `soak_test.cpp` + цели Makefile
  `asan`/`soak` + ранее незакоммиченные модули кора + `plans_*.md`/`design.md`/`master_prompt.md`)
  в ветке `local-flight-mode`. Мердж в `main` — по решению пользователя.
- Бинарные артефакты (`/game`, `game_asan`, `soak_asan`, `*.dSYM`, `SDL2-*.dmg`, `test_app/`) — в
  `.gitignore` (сборка идёт через системный SDL2 по `sdl2-config`), в репозиторий не кладём.

---

## 9. Журнал прогресса (changelog)

Хронология крупных вех (свежие сверху). Детали — в ссылающихся разделах; здесь только «что и когда».

- **2026-07-28** — ✅ **Плейтест №4 → проход углубления №5: изотропная сфера-плазма (ray-sphere «шейдер»)**
  (§5.13.3, §7.1.4). Фидбек: «баг костыль — подлетаю, всё жёлтое со всех сторон… звезда неправильно…
  используй шейдеры, шар плазмы; рендер бедный». Баг подтверждён: `if (D≤R*1.7) fillRect(весь экран,
  плазма)` заливал кадр НЕЗАВИСИМО от взгляда. Костыль-заливка и биллборд-диск удалены, заменены
  **попиксельным аналитическим ray-sphere «шейдером»** `renderStarPlasma` (`localdraw.cpp`): тело =
  центр+радиус; для каждого пикселя мировой луч из глаза ∩ сфера, плазма из геометрии (лимб-даркенинг
  `mu=√(1−dmin²/R²)` + многооктавная турбулентность с доменным варпом + запечённая корона); рендер в
  стриминг-текстуру ARGB8888 (SDL2-only, BLEND). Изотропно по построению, ноль порогов/режимов/хардкода.
  Диск непрозрачен → согласован с ray-sphere окклюзией. Новый решающий шот `shot_star_lookaway` (вблизи,
  нос прочь → чёрный космос). Проверки: `make` 0-warn, оба smoke, ASan smoke, **`shot_asan` (11
  сценариев под ASan/UBSan, покрыл попиксельную запись шейдера)**, soak 120k (invariantFails=0), 11/11
  шотов. Орбиты §5.13.2 не тронуты (одобрены). Правки: `localdraw.cpp`, `shot_test.cpp`, `master_prompt.md`.
- **2026-07-28** — ✅ **Плейтест №3 → проход углубления №4: геометрия D/R (звезда доминирует ВЕЗДЕ)**
  (§5.13.2, §7.1.3). Фидбек «масштаб всё равно звезда маленькая». Диагноз: экранный размер звезды
  зависит ТОЛЬКО от отношения `D/R` (`screenR = focal·R/sqrt(D²−R²)`), а §5.13.1 растил абсолютный
  радиус при орбитах до R·13 → звезда ~5% экрана. Фикс: (1) **орбиты как доли R** —
  `orbitRadius = starRadius·(1.20 + 0.42·i + frand(0,0.14))`, внешняя ≤ R·3.44; (2) **звезда ×2.9** —
  `starRadius = 260 + met·100`; (3) старт `px = starRadius·2.8` (D/R≈2.8, ~82% высоты); (4) спавны
  NPC/радио подняты из тела звезды (`starRadius·1.3`); (5) порог заливки `R·1.0008 → R·1.7`
  (бесшовно); (6) фикс «спиц» эклиптики (`camZ>0.06 → 0.14`). Геймплейные радиусы не трогали
  (пропорциональны). Правки: `localgen.cpp`/`localdraw.cpp`/`shot_test.cpp`. Проверки: `make`
  (0 предупр.), оба smoke, ASan/UBSan, `make soak` 120k (invariantFails=0, deathPath=ok),
  `make shots` **10/10** — все зелёные, звезда-доминанта визуально подтверждена.
- **2026-07-28** — ✅ **Плейтест №2 → проход углубления №3: гигантская звезда, окклюзия, мышелук**
  (§5.13.1, §7.1.2). По фидбеку №2 закрыты 5 пунктов: (1) **звезда как аналитическая сфера** — рисуем
  не диск по дистанции, а касательную к сфере (`screenR = focal·R/sqrt(D²−R²)`, изнутри — заливка
  плазмой); принята идея пользователя «чистая геометрия, никаких моделей — центр + радиус». (2)
  **окклюзия луч↔сфера** (`segIntersectsSphere`) для всех объектов → планеты больше не просвечивают
  сквозь звезду (без z-буфера). (3) **гигантский масштаб** — `starRadius 90..135 LU`, планеты единицы
  LU, корабли-крапинки (тысячи планет влезут в звезду). (4) **мышелук** — `SDL_SetRelativeMouseMode`
  в кокпите, дельты мыши → yaw/pitch в поворот Родрига, ЛКМ стреляет (шутерный обзор). (5) **синий
  квадрат при стрельбе** — был муззл в самом глазу, раздутый перспективой; спавн смещён + аддитивный
  bloom + кап размера. Правки: `localdraw.cpp`/`localgen.cpp`/`localsim.cpp`/`local.h`/`main.cpp`/
  `shot_test.cpp`. Проверки: `make` (0 предупр.), оба smoke, ASan/UBSan оба smoke, `make soak` 120k
  (invariantFails=0, deathPath=ok), `make shots` **10/10** (+`shot_star_near`/`_occlusion`/`_muzzle`/
  `_inside`) — все зелёные, визуально подтверждено.
- **2026-07-28** — ✅ **Читаемость боя: лид-прицел + панель цели (щит/сближение)** (§5.13 п.2, §7.1.1):
  проактивный полиш под похвалу пользователя «сражение реально ощущаются сочно» — **чисто аддитивно, врага
  НЕ ослабляли**. (1) **Лид-пип (упреждение)** в кокпите для текущей цели: решаем перехват в относительной
  скорости `u = targetVel − playerVel` (снаряд несёт скорость корабля), квадратка `(u·u−S²)t² + 2(r·u)t +
  r·r = 0`, берём наименьший `t>0`; янтарный маркер на радиусе 600 LU (тот же, что мушка), красно-янтарный
  для врага. Показывается на дистанции завязки (<1500 LU). (2) **Панель цели** (сверху-справа): добавлены
  **полоса щита цели** (циан, над корпусом, если `maxShield>0`) и **скорость сближения** `CLS %+.0F`
  (проекция относительной скорости на луч визирования; `+` = сближаемся, янтарь). Панель растёт по высоте
  при наличии щита. Только `localdraw.cpp` (sim/rng/бой не тронуты). В харнес добавлен 6-й кадр
  **`shot_combat_lead`** (цель наперерез, статичный детерминированный кадр — пип заметно смещён от мушки).
  Проверки: `make` (0 предупр.), оба smoke, ASan/UBSan оба smoke + **харнес под ASan/UBSan** (гоняет новый
  draw с целью), `make soak` 120k (invariantFails=0, deathPath=ok), `make shots` 6/6 — все зелёные; лид-пип
  и панель визуально подтверждены (пират по центру под мушкой, янтарный пип слева — куда целить по
  цели, идущей наперерез 220 LU/ч).
- **2026-07-28** — ✅ **Ориентир эклиптики в кокпите (искусственный горизонт)** (§5.13 п.2):
  проактивный полиш 6DOF-полёта — «линия схода» плоскости системы (X-Y) в кокпите показывает крен
  (наклон линии) и тангаж (смещение) относительно диска системы; снимает дезориентацию, добавленную
  осью крена. Реюз `projectDirectionWithBasis` (как скайбокс), тела орбит ложатся на линию. В глубоком
  космосе не рисуется (нет опорной плоскости). Только `localdraw.cpp` (чистый HUD-draw, sim/rng/бой не
  тронуты). Проверки: `make`, оба smoke, ASan/UBSan оба smoke (рендерят новый draw), `make soak` 120k
  (invariantFails=0), `make shots` 5/5 — все зелёные; горизонт визуально подтверждён (level → горизонталь,
  крен 0.55 → наклон, глубокий космос → пусто).
- **2026-07-28** — ✅ **Вид из кокпита (срез 2, от первого лица)** (§5.13 п.2, §7.1.1): закрыл
  открытый пункт плейтеста №1. Глаз камеры теперь в самом корабле (взгляд вдоль `pfwd`), собственная
  жёлтая стрелка игрока в полёте убрана — вместо неё центральная **мушка-прицел + маркер прогрейда +
  дуга щита**; стрелка сохранена только в карте сверху. `+/−` теперь **оптический зум/FOV**
  (`localFovScale`) вместо долли chase-камеры. Камера вынесена в единый помощник **`buildLocalCamera`**
  (`local.h`) — общий для игры и нового **скриншот-харнеса `shot_test.cpp`** (`make shots`: софт-рендер
  SDL → BMP → PNG). Добавлены **потолки размера частиц** по типу (близкий выхлоп больше не заливает
  экран). Правки: `local.h`/`main.cpp`/`localdraw.cpp` + `shot_test.cpp`/`Makefile`/`.gitignore`.
  Проверки: `make`, оба smoke, ASan/UBSan smoke, `make soak` 120k, `make shots` 5/5 (кадры просмотрены) —
  все зелёные, 0 ошибок. (Закоммичено — `58e7136`.)
- **2026-07-28** — 🎮 **Плейтест №1 3D-режима** (§7.1.1): пользователь подтвердил, что 3D — улучшение,
  и что бой «ощущается сочно» (juice не ослаблять, даже быстрая смерть от пирата — это плюс). Открыт
  один пункт: жёлтая стрелка игрока в chase-виде смотрится странно → **вид из кокпита (от первого лица)**.
  Только документирование фидбека; код не менялся. *(Пункт закрыт срезом 2 — см. запись выше.)*
- **2026-07-28** — ✅ **Полный 3D локального режима РЕАЛИЗОВАН** (§5.13). Полёт по трём осям
  (yaw/pitch/**roll**) на **тело-относительном ортонормированном базисе** (`pfwd`/`pup`, вращения
  Родрига + Грам–Шмидт — без gimbal lock); **перспективная камера от корабля** (chase) с
  переключением на орто-карту сверху (клавиша `C`); **LOD-скайбокс всего кластера** из реальных
  `ClusterStar` по истинным направлениям. §2.7 обновлён: **перспектива разрешена только в локальном
  режиме** (пользователь выбрал «Перспектива (Elite)»); макро-путь ПОБИТОВО не тронут. Добыча
  перенесена `E`→`M` (E занят roll). Правки: `camera.h`/`local.h`/`localsim.cpp`/`localgen.cpp`/
  `main.cpp`/`localdraw.cpp`/`soak_test.cpp`. Проверки: `make`, оба smoke, ASan/UBSan smoke, `make
  soak` 120k кадров — все зелёные, 0 ошибок. (Закоммичено — `58e7136`.)
- **2026-07-28** — Зафиксирован **целевой дизайн локального режима** (§5.2 п.5–6, §5.13): полный
  **3D по трём осям (как Elite, +roll), камера от корабля**, и **весь кластер виден фоном как
  LOD-скайбокс** из реальных `ClusterStar`. Открытый вопрос — §2.7 орто vs перспектива (нужен ОК).
  Пока только документ; реализация — по «go» пользователя. *(Реализовано в тот же день — см. запись выше.)*
- **2026-07-28** — **Единая кликабельная панель действий** (§5.11): контекстные кнопки
  (active/inactive) поверх хоткеев; чинит «не зайти в звезду» (видимая кнопка `L ENTER`). Добавлен
  `scene.minePrompt` как единый источник доступности добычи. Плюс замечание **2D-камера vs полный
  3D** (§5.12) и кандидат роадмапа chase-cam (§7.3). Все уровни проверки зелёные. Коммит на `local-flight-mode`.
- **2026-07-28** — Синхронизация документа и версионирование: весь контекст/прогресс/планы сессии
  сведены в `master_prompt.md`; проект впервые полностью закоммичен и запушен в ветку
  `local-flight-mode` (кор + контентные модули + локальный слой ранее лежали незакоммиченными).
- **2026-07-27** — Локальный режим, **проход 2** (контент + глубокий космос, §5.10): луны, кольца
  планет, туманности, радиосигналы + детекторы (закрыл §7.2), HUD-радар.
- **2026-07-27** — Локальный режим, **проход 1** (ощущение + глубина, §5.10): частицы/juice,
  экранная тряска, щиты, убиваемый игрок + аварийный прыжок, умный ИИ, лут, живые астероиды,
  реакция фракций. Плюс soak-тест (§6.5) и цели Makefile `asan`/`soak`.
- **2026-07-27** — Локальный режим, **срез 1** (§5): инерционный полёт, кеплеровы орбиты, добыча
  на месте, живой бой, стыковка = рынок, варп-ускорение, follow-cam + HUD. Вынесены `camera.h`,
  `render2d.*`. Найден и починен ASan-баг шрифта (§6.3).
- **(ранее, до этой сессии)** — Кор игры (§4.1) и контентные модули `plans_1..10` (§4.2).
