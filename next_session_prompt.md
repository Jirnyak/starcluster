# СЛЕДУЮЩИЙ ЗАХОД: заказы не доживают до сдачи

Прошлые задания выполнены — разбор в `master_prompt.md` §52 и §53. Коротко:

- **сейв — версия 21** и доносит будущее: полный мир 8192 звезды, 150 лет до
  сейва и 40 после, казна 3.90836e9 против 3.90836e9, всё до цифры;
- **все шесть развилок закрыты** — пять правкой, шестая (закрытый порт) решением
  «не трогать, вражда бьёт по тарифу»;
- **урон сведён в одну систему** (§53.1): макро-бой тратит те же поля оружия в те
  же `hullHP`, что и локальный, а порт латает пришвартовавшийся борт;
- **хромоядра** (§53.2): темп и цена кузницы оставлены — это редкий дорогой
  контент, а выбор стата §52.3 сделал бесплатным (он идёт за занятием);
- ⚠️ по дороге выяснилось, что **`rng` — глобал ПРОЦЕССА**, и на этом стояли
  ложные числа §51: два мира в одном процессе нельзя крутить вперемешку.

Все харнесы зелёные: `balance_full` 105/105, `soak` invariantFails=0, `uiclick`
PASS. Цена такта — 165.4 мс по записанной процедуре.

---

## Что осталось: срок заказа, а не плата за него

### Что уже установлено (§53.4, §54)

Пробник научен мерить заказ ПРАВИЛЬНО — не «вместо рейса», а как попутный груз:
цель задаёт заказ, товар подбирается под неё, считается совокупность «плата плюс
догрузка» против чистого лучшего рейса.

| сид | взято / отвергнуто | (заказ+догрузка)/рейс | лучший заказ | свободный рейс |
|---|---|---|---|---|
| 1234 | 0 / 123 | −0.041 | 12.06 Cr/год | 3046 Cr/год |
| 909  | 0 / 151 | −0.017 | 8.97 Cr/год  | 4072 Cr/год |

Вклад заказа — порядка **0.4% от рейса**. `CONTRACT_PAY_PER_YEAR = 9.0` осталась
от экономики ДО пересчёта §15–19, мир подорожал на два порядка.

### ⚠️ Но коэффициент — НЕ первая работа

Обе попытки тюнинга отклонены замером (§54.3):

- **3046** (паритет): заказы вытеснили торговлю, 9–14 рейсов вместо 60;
- **700**: отношение легло около 1, заказы берутся и отвергаются — но карьера
  просела (13 / 2 / 14 рейсов), а репутация осталась **0–1**.

Последнее и есть ключ: **заказы берутся и не доходят до сдачи** — срываются по
сроку на длинных плечах. Пока заказ не доживает до сдачи, любая плата
бессмысленна: репутация не копится ни при какой.

### Порядок работы

1. **Сначала срок.** Понять, почему принятый заказ не сдаётся: `CONTRACT_DEADLINE_SLACK`,
   `contractDeadlineFor`, ветка провала и `applyContractFailureReputation`.
   Инструмент — `journey_probe`: печатать взято / сдано / провалено по сроку.
2. **Потом плата.** Когда заказы начнут доходить, подбирать
   `CONTRACT_PAY_PER_YEAR` под цель «заказ — заметная прибавка к рейсу, а не
   замена ему». Верх лестницы держать на месте пересчётом `JOB_TIER_PAY_TOP`
   (произведение «плата за год × множитель» = топовый заказ ~1.5 млрд, §24).
3. **Приёмка:** 60 рейсов на трёх сидах, заказы берутся И сдаются, репутация
   заметно больше нуля, второй корпус берётся не позже, чем сейчас (22–23-й рейс).

## Железные правила (не меняются)

- **Меряй шагом ИГРЫ** `0.01` года (§48.9, §50).
- **Упавшая проверка — сначала подозреваемая, потом судья.** За пять заходов
  подряд ломалась ПРЕДПОСЫЛКА, а не механика (§47.8, §48.5, §51.5, §52.1, §52.2,
  §53.3).
- ⚠️ **`rng` — глобал процесса.** Копии мира крутить ПООЧЕРЁДНО, возвращая
  генератор в исходное состояние.
- **Ни одного числа из воздуха.** Либо выведено, либо замерено, и замер записан
  рядом.
- **Комментарии по-русски**, про «почему», с ⚠️ на граблях.
- **Локализация**: ширину мерить только `UI::textWidth`; панель целей держит
  208 px, запас на русском — 4 px (§50).
- **Сейв — версия 21.** Новое поле = 22 + ветка + проверка «переживает сейв» **и**
  «будущее после загрузки совпадает» (`testSaveCarriesTheFuture`). Сохранять надо
  не только цифры, но и ЧАСЫ, которые их двигают (§52.1).
- **Новое глобальное состояние сбрасывать в `Game::init`.**

## Не трогать

Стартовые 100 Cr · крейсер 0.5 и четверть бункера (§48.4) · квота 10 000 и тариф
10% (§21) · `STAR_COUNT = 8192` и квадратичная цена систем (§22) · плато поздней
игры (§46.12) · ничейных звёзд нет (§47) · гравитация скопления отвергнута
(§48.6) · касса колонии и актиний в бункере (§29) · тёмной материи нет ·
`TOW_WAIT_YEARS = 100` (§50.5) · макро-время заморожено на время локального
полёта (§32) · **порт враждебной державы не закрывается** (§52.3) ·
**`miningRichness` не истощается — кончается только досягаемый пояс** (§52.3) ·
**темп хромоядер и цена кузницы** — редкий дорогой контент, выбор стата и так
бесплатен занятием (§53.2).

## Инструмент

Харнесы: `make game | balance | balance_full | econ | uiclick | soak | shots |
shot_asan | uishots`.

Пробники (в `Makefile` НЕ входят, собираются вручную):
`journey_probe.cpp` — путь новичка целиком, главный инструмент;
`save_probe.cpp` — сейв на полном мире с прокруткой обеих копий;
`save_diff.cpp` — ГДЕ именно сейв теряет мир (два сейва построчно + память);
`repair_probe.cpp` — разброс местной цены услуги по скоплению;
`tick_probe.cpp` — цена такта (процедура — в `CLAUDE.md`);
`strand_measure.cpp`, `leg_probe.cpp`, `deal_probe.cpp`, `top_probe.cpp`,
`advisor_probe.cpp`.

```sh
g++ journey_probe.cpp agent.cpp anomaly.cpp chromo.cpp cluster.cpp colony.cpp \
  combat.cpp drive.cpp econ.cpp exotic.cpp faction.cpp game.cpp i18n.cpp \
  localdraw.cpp localgen.cpp localsim.cpp market.cpp mining.cpp modules.cpp \
  render2d.cpp resource.cpp ship.cpp spaceevents.cpp stb_image.cpp ui.cpp \
  -O2 -std=c++11 $(sdl2-config --cflags) $(sdl2-config --libs) -lSDL2_mixer \
  -o journey_probe
SDL_VIDEODRIVER=dummy ./journey_probe 1234 1200 60
```

⚠️ В zsh список исходников в переменную не убирать: `$SRC` там не разбивается на
слова, и `g++` получает одно длинное «имя файла».
